<!-- GENERATED by build-audit.mjs — 不要手改这个文件,改 flags-data.js 然后重跑 -->
<!-- registry sha256: c0f34b971566fe04 · 快照时间: 2026-08-15T17:32:54.164Z -->
裁决分布:留 116 · 分歧 3 · 固化 5

证据强度:按默认 85 · 本轮取证 38 · 查无依据 1

#### 生命周期 / 存活检测(23 条)

| 开关 | 在干嘛 | 现在 | 为什么是这个状态 | 改了怎么生效 | 裁决 |
|---|---|---|---|---|---|
| `liveness_alerts`<br><sub>FLYWHEEL_LIVENESS_ALERTS</sub> | 一个 runner 已经拿到 ship 批准、却在真正 ship 之前死掉了——这种情况要不要发人工告警。开着=发。 | 开(没人设过=代码默认) | 没人设过,跑代码默认「开」。Bridge 启动时读一次,改了要重启。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `prune_park_guard`<br><sub>FLYWHEEL_PRUNE_PARK_GUARD</sub> | 两条会「删东西」的清扫(强转 completed、删 CommDB 行)在动手前,先看这个 session 有没有声明自己 park 了;声明了就否决清扫。 | 开(没人设过=代码默认) | 没人设过,默认「开」。它取代了以前靠人手维护排除名单的做法。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `readopt_parked_roles`<br><sub>FLYWHEEL_READOPT_PARKED</sub> | Bridge 重启后重新认领还活着的 runner 时,认哪些状态。开着=四种停靠状态都认;关掉=只认 running。 | 开(没人设过=代码默认) | 没人设过,默认「开」。关掉会漏掉所有 park 在非 running 状态的 role——那正是它当初被造出来修的 bug。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `liveness_activity_window_ms`<br><sub>FLYWHEEL_LIVENESS_ACTIVITY_WINDOW_MS</sub> | 告警正文里判断「这个 runner 大概还活着 / 大概已经死了」用多长的活动窗口。默认 10 分钟。 | `600000`(没人设过=代码默认) | 没人设过,默认 600000。它**只影响告警措辞,不影响任何裁决**——注册表原文就这么写的。<br>▸ 非法值会在运行时被 sanitize 回默认,所以显示值=真实生效值。 | Bridge:要重启 | **留**<br><sub>本轮取证</sub> |
| `lead_dual_active_scan`<br><sub>FLYWHEEL_DUAL_ACTIVE_SCAN</sub> | 检测同一个 Lead 被起了两份(双活),立刻告警并标记后起的那个。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `progress_resume_killswitch`<br><sub>FLYWHEEL_PROGRESS_RESUME</sub> | 重启/被杀之后,runner 能不能从 progress.md 的游标接着做。关掉=每次都从头开始。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `heartbeat_readopt`<br><sub>FLYWHEEL_HEARTBEAT_READOPT</sub> | 心跳服务重新认领已存在的 session(而不是当成新的)。 | 开(没人设过=代码默认) | 没人设过,默认「开」。关掉会退回 FLY-172 之前的行为。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `liveness_pane_dead`<br><sub>FLYWHEEL_LIVENESS_PANE_DEAD</sub> | 用 tmux pane 是否已死来判断 runner 死活。 | 开(没人设过=代码默认) | 没人设过,默认「开」。和 zombie_reconcile / heartbeat_readopt 合成同一个判活谓词。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `worktree_autoclean`<br><sub>FLYWHEEL_WORKTREE_AUTOCLEAN</sub> | 一轮活干完之后自动清掉 git worktree。 | 开(没人设过=代码默认) | 没人设过,默认「开」。这是好几处清扫共用的总逃生口——关掉会同时冻结好几层清理。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `crash_reaper`<br><sub>FLYWHEEL_CRASH_REAPER</sub> | 回收崩掉/变成孤儿的 runner 进程。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `stale_terminal_close`<br><sub>FLYWHEEL_STALE_TERMINAL_CLOSE</sub> | session 已经是终态、但它的 tmux 还活着超过阈值 → 自动去收掉(防泄漏)。关掉=只发通知不收。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `commdb_fsm_reconcile`<br><sub>FLYWHEEL_COMMDB_FSM_RECONCILE</sub> | 启动时对账:CommDB 说还在跑、但状态机已经终态且 tmux 已经死的,清掉。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `commdb_residue_harvest`<br><sub>FLYWHEEL_COMMDB_RESIDUE_HARVEST</sub> | 清掉只存在于 CommDB 的孤儿登记,和 StateStore 里的幽灵行。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `terminal_commdb_sync`<br><sub>FLYWHEEL_TERMINAL_COMMDB_SYNC</sub> | session 变成 failed/blocked 时,把终态异步同步回 CommDB。 | 开(没人设过=代码默认) | 没人设过,默认「开」。这是上面两条清扫要修的问题的根因层。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `viewer_session_reaper`<br><sub>FLYWHEEL_VIEWER_SESSION_REAPER</sub> | Bridge 启动时清掉泄漏的 viewer-<execId> tmux session。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `chrome_session_reaper`<br><sub>FLYWHEEL_CHROME_REAPER</sub> | 周期清理泄漏的无头 Chrome 进程(agent-browser 用完没收干净的)。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `ghost_guard_wait_ms`<br><sub>FLYWHEEL_GHOST_GUARD_WAIT_MS</sub> | 起 runner 时等多久算「确实起来了」的确认窗口。默认 90 秒。 | `90000`(没人设过=代码默认) | 没人设过,默认 90000。纯时间旋钮。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `fleet_sensor_tmux_killswitch`<br><sub>FLYWHEEL_FLEET_SENSOR_TMUX</sub> | 整台 tmux server 掉了的时候,按舰队级别一次性处理(成组迁终态、开一张 ticket、按 Lead 分组通知),而不是一个个 runner 各自崩。 | 开(没人设过=代码默认) | 没人设过,默认「开」。关掉会退回一个个 runner 各自被 crash-reaper 收的旧行为。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `commdb_protection`<br><sub>FLYWHEEL_COMMDB_PROTECTION</sub> | CommDB 里「还没被回答的、需要人处理的」行,在过期/收尾/清扫三条路径上都保住不删。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 / 命令行:下次调用生效 | **留**<br><sub>按默认</sub> |
| `zombie_reconcile`<br><sub>FLYWHEEL_ZOMBIE_RECONCILE</sub> | 僵尸 session 探真:连续两轮探到 pane 不在就宣告失败,并给 Lead 发一份「没推上去的活」清单。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `continuity_preflight`<br><sub>FLYWHEEL_CONTINUITY_PREFLIGHT</sub> | 重新派工之前,先去 origin 上对一下同名分支和已开的 PR,免得从 main 另起一条分叉、把前一轮的活丢掉。 | 开(没人设过=代码默认) | 没人设过,默认「开」。FLY-1718 刚加的安全回滚开关。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `push_guard`<br><sub>FLYWHEEL_PUSH_GUARD</sub> | 给 runner 的 worktree 装一个 pre-push 钩子,挡住对「已开 PR 的分支」做非快进推送或删除。 | 开(没人设过=代码默认) | 没人设过,默认「开」。只在建 worktree 那一刻读,所以改了只影响之后新建的 worktree。 | Bridge:要重启 | **留**<br><sub>本轮取证</sub> |
| `doa_backoff`<br><sub>FLYWHEEL_DOA_BACKOFF</sub> | 重新派工前先看上一个 runner 是怎么死的;连续死就按 1/2/4/8 分钟退避,第五次交给 Lead 决定。 | 开(没人设过=代码默认) | 没人设过,默认「开」。FLY-1718 的一部分。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |

#### ship / 批准链(23 条)

| 开关 | 在干嘛 | 现在 | 为什么是这个状态 | 改了怎么生效 | 裁决 |
|---|---|---|---|---|---|
| `auto_qa_killswitch`<br><sub>FLYWHEEL_AUTO_QA</sub> | 全局总闸:code review 之后要不要自动起一个独立 QA runner。关掉=全舰停自动 QA。 | 开(没人设过=代码默认) | 没人设过,默认「开」;实际是否起 QA 还要看每个项目自己的 qa.auto。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `codex_hard_gate_killswitch`<br><sub>FLYWHEEL_CODEX_HARD_GATE</sub> | 硬门:任何 PR 没拿到 Codex APPROVED 就卡住,不许进 auto-QA、不许 merge。关掉=应急放行。 | 开(没人设过=代码默认) | 没人设过,默认「开」。这是 Lead 明确要求必须可靠的那道门。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `review_severity_policy_killswitch`<br><sub>FLYWHEEL_REVIEW_SEVERITY_POLICY</sub> | 跨家族 review 的收敛政策:MEDIUM/LOW 不阻塞、并且尊重 Lead 已经裁决过的 finding。关掉=回到旧判法。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `merge_approval_gate_killswitch`<br><sub>FLYWHEEL_MERGE_APPROVAL_GATE</sub> | 防抢跑:已经 merged 的东西,只有经过 verifyApproval 才算数;否则挂 merge_block 并大声告警(不自动 revert)。 | 开(没人设过=代码默认) | 没人设过,默认「开」。<br>▸ Bridge 侧和 CLI 侧各读一次,两边不一致时会 split-brain——注册表原文自己写明了这个风险。 | Bridge:要重启 / 命令行:下次调用生效 | **留**<br><sub>按默认</sub> |
| `qa_done_gate_killswitch`<br><sub>FLYWHEEL_QA_DONE_GATE</sub> | 要求 QA 通过才让 session 收尾:需要 QA 的 session 必须有一份对得上当前 head 的 passed 记录。 | 开(没人设过=代码默认) | 没人设过,默认「开」。豁免口写得很明确(无代码/无 PR/无 QA 标签/项目关了 qa.auto)。<br>▸ 与上一条同样存在 Bridge/CLI 双读的 split-brain 风险。 | Bridge:要重启 / 命令行:下次调用生效 | **留**<br><sub>按默认</sub> |
| `ship_ci_guard`<br><sub>FLYWHEEL_SHIP_CI_GUARD</sub> | 批准和最终 ship 之前,现场查一次 GitHub CI 是不是绿的。 | 开(没人设过=代码默认) | 没人设过,默认「开」。每次 CLI 调用现读,所以改了下一次调用就生效。 | 命令行:下次调用生效 | **留**<br><sub>按默认</sub> |
| `ship_ready_notify`<br><sub>FLYWHEEL_SHIP_READY_NOTIFY</sub> | 非 land 类的工程 workflow 停在 founder 门口时,同时通知 owning Lead 和 founder thread「这条可以 ship 了」。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `ship_ready_remind_ms`<br><sub>FLYWHEEL_SHIP_READY_REMIND_MS</sub> | 上面那条停多久没人处理就提醒 Lead。默认 30 分钟。 | `1800000`(没人设过=代码默认) | 没人设过,默认 1800000。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `founder_auto_approve`<br><sub>FLYWHEEL_FOUNDER_AUTO_APPROVE</sub> | Annie 在 [FLY-XX] thread 里说一句「可以」或点 ✅ → 系统认成 founder 批准 → 写 ship 门 → runner 自己 ship。 | 开(没人设过=代码默认) | 没人设过,默认「开」。这是她日常批准的主通路。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `stale_ship_rewake`<br><sub>FLYWHEEL_STALE_SHIP_REWAKE</sub> | 把卡在「已批准待 ship」状态的 runner 重新唤醒(补上漏掉的自动 ship)。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `auto_linear_done`<br><sub>FLYWHEEL_AUTO_LINEAR_DONE</sub> | runner 自己 ship 完、确认 merged 之后,自动把 Linear issue 翻成 Done。 | 开(没人设过=代码默认) | 没人设过,默认「开」。只有拿到 merge 证据才会走到这一步。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `deferred_founder_approval`<br><sub>FLYWHEEL_DEFERRED_FOUNDER_APPROVAL</sub> | 如果 Annie 批准的时候这条正好被 hold 住,先把批准存起来,hold 解开后自动补上,而不是当场拒绝她。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `held_declined_reply`<br><sub>FLYWHEEL_HELD_DECLINED_REPLY</sub> | 被 hold 住的时候,给 Annie 在 thread 里发一句明文解释,而不是只回一个 ❓ 让她猜。 | 开(没人设过=代码默认) | 没人设过,默认「开」。这是当初明确提出的硬要求之一。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `deferred_approval_ttl_ms`<br><sub>FLYWHEEL_DEFERRED_APPROVAL_TTL_MS</sub> | 上面那份「暂存的批准」能存多久。默认 45 分钟,过期要她重新确认。 | `2700000`(没人设过=代码默认) | 没人设过,默认 2700000。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `ship_gate_grace_ms`<br><sub>FLYWHEEL_SHIP_GATE_GRACE_MS</sub> | Annie 的文字/✅ 对 ship 门的放行 grace。默认 15 秒;把它设成 600000 就等于回到旧的 10 分钟行为——**这个数值本身就是它的 kill-switch**。 | `15000`(没人设过=代码默认) | 没人设过,默认 15000。 | Bridge:要重启 | **留**<br><sub>本轮取证</sub> |
| `ship_gate_rebind`<br><sub>FLYWHEEL_SHIP_GATE_REBIND</sub> | QA 把证据提交上去导致 PR head 前移时,自动把 ship 门重新绑到新 head,而不是直接丢掉这道门。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `external_merge_reconcile`<br><sub>FLYWHEEL_EXTERNAL_MERGE_RECONCILE</sub> | 有人在流程外把 PR merge 了(executor 直接合的残局),用一个兜底 pass 把账收敛回来。 | 开(没人设过=代码默认) | 没人设过,默认「开」。注册表原文强调:**兜底不是许可**。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `merge_reconcile_window_days`<br><sub>FLYWHEEL_MERGE_RECONCILE_WINDOW_DAYS</sub> | 上面那个兜底 pass 往回看几天。默认 7 天。 | `7`(没人设过=代码默认) | 没人设过,默认 7。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `ship_gate_retire`<br><sub>FLYWHEEL_SHIP_GATE_RETIRE</sub> | 重新绑定之后,把被取代的那道旧 ship 门退休掉——同一时刻只留一道可绑的门。 | 开(没人设过=代码默认) | 没人设过,默认「开」。关掉会回到「僵尸门堆着」的旧现状。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `ship_gate_card`<br><sub>FLYWHEEL_SHIP_GATE_CARD</sub> | 把发给 Annie 的 ship 卡片当成主载体(15 秒 grace),而不是靠 10 分钟的兜底节奏。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `ship_gate_card_grace_ms`<br><sub>FLYWHEEL_SHIP_GATE_CARD_GRACE_MS</sub> | 发出 ship 卡片之前等多久。默认 15 秒。 | `15000`(没人设过=代码默认) | 没人设过,默认 15000。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `tier2_prefix_norm`<br><sub>FLYWHEEL_TIER2_PREFIX_NORM</sub> | 批准语义:允许剥掉纯语气前缀,让「嗯ship」也能确定性地当成「ship」。 | 开(没人设过=代码默认) | 没人设过,默认「开」。关掉会让这类说法掉到更模糊的判定层。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `qa_auto`<br><sub>qa.auto</sub> | 逐项目开关:这个项目的 PR 过了 review 之后要不要自动起 QA runner。 | 逐项目:1/6 开(显式设过:flywheel=true) | 六个项目里只有 flywheel 自己显式开了,其余五个都是默认关。这是刻意的——Flywheel 自研自测。 | Bridge:秒级生效 | **留**<br><sub>本轮取证</sub> |

#### founder 通路 / issue 显示(22 条)

| 开关 | 在干嘛 | 现在 | 为什么是这个状态 | 改了怎么生效 | 裁决 |
|---|---|---|---|---|---|
| `gatepoller_circuit`<br><sub>FLYWHEEL_GATEPOLLER_CIRCUIT</sub> | 轮询连续失败时的熔断(不要疯狂重试)。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `issue_gate_supersede_mode`<br><sub>FLYWHEEL_ISSUE_GATE_SUPERSEDE</sub> | issue 上一堆门互相取代时的巡检模式。enforce=真去收敛,observe=只记账,0=停止新的改动。 | `enforce`(没人设过=代码默认) | 没人设过,默认 enforce。已经盖过 superseded 戳的历史记录永久有效,设 0 也不会回滚。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `founder_review_gate_exclude`<br><sub>FLYWHEEL_FOUNDER_REVIEW_GATE_EXCLUDE</sub> | Annie 回一个字母的时候,候选门里排除掉 review_design / review_code 这两类(免得她一句「ok」被安到 review 门上)。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `founder_thread_notify`<br><sub>FLYWHEEL_FOUNDER_THREAD_NOTIFY</sub> | 卡在门上的时候给 Annie 发 thread 通知。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `founder_reply_deliver`<br><sub>FLYWHEEL_FOUNDER_REPLY_DELIVER</sub> | 把 Annie 在门上的回复回投给对应的 runner。 | 开(没人设过=代码默认) | 没人设过,默认「开」。这条关了 = 她说了话但没人收到。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `founder_notify_retry_max`<br><sub>FLYWHEEL_FOUNDER_NOTIFY_RETRY_MAX</sub> | founder 动作台账的投递重试上限,超了标 failed 并告警。默认 5 次。 | `5`(没人设过=代码默认) | 没人设过,默认 5。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `founder_reply_retry_max`<br><sub>FLYWHEEL_FOUNDER_REPLY_RETRY_MAX</sub> | 上面那条投递最多重试几次,超了就进死信并告警。默认 10 次。 | `10`(没人设过=代码默认) | 没人设过,默认 10。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `founder_reply_deadletter_age_ms`<br><sub>FLYWHEEL_FOUNDER_REPLY_DEADLETTER_AGE_MS</sub> | 重试的另一半阈值:超过这个时长也进死信。默认 30 分钟。 | `1800000`(没人设过=代码默认) | 没人设过,默认 1800000。和次数上限是双阈值。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `founder_reply_unreachable`<br><sub>FLYWHEEL_FOUNDER_REPLY_UNREACHABLE</sub> | Annie 回复了、但那个 runner 已经联系不上——这属于数据不一致,发告警。 | 开(没人设过=代码默认) | 没人设过,默认「开」。这条是 FLY-1570 拆看门狗时**特意保留**的:它做的是数据一致性对账,不是追人。 | Bridge:要重启 | **留**<br><sub>本轮取证</sub> |
| `ask_hygiene`<br><sub>FLYWHEEL_ASK_HYGIENE</sub> | runner 收尾时,把挂在它名下、没人回答的提问清理掉。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 / 命令行:下次调用生效 | **留**<br><sub>按默认</sub> |
| `founder_milestone_notify`<br><sub>FLYWHEEL_FOUNDER_MILESTONE_NOTIFY</sub> | 给 Annie 推里程碑消息。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `issue_status_emoji`<br><sub>FLYWHEEL_ISSUE_STATUS_EMOJI</sub> | issue thread 标题上的状态 emoji + 重连标记。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `issue_status_word`<br><sub>FLYWHEEL_ISSUE_STATUS_WORD</sub> | 同一套状态,用文字而不是只用 emoji 标出来。 | 开(没人设过=代码默认) | 没人设过,默认「开」。关掉退回纯 emoji。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `issue_attach_pin`<br><sub>FLYWHEEL_ISSUE_ATTACH_PIN</sub> | 在 issue thread 里钉一条 tmux attach 的救援命令,方便人直接连进去看。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `issue_display_refresh`<br><sub>FLYWHEEL_ISSUE_DISPLAY_REFRESH</sub> | 三个显示面(标题、置顶、状态行)统一从真实状态派生,并在整个生命周期各个节点触发刷新。关掉=退回旧的单点刷新。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `issue_display_sweep_ticks`<br><sub>FLYWHEEL_ISSUE_DISPLAY_SWEEP_TICKS</sub> | 显示自愈的兜底扫描周期(多少个 tick 扫一次)。默认 60,约 3 分钟;设 0 = 关掉扫描。 | `60`(没人设过=代码默认) | 没人设过,默认 60。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `done_thread_reconcile`<br><sub>FLYWHEEL_DONE_THREAD_RECONCILE</sub> | 兜底扫描:Done/Canceled 的 issue 如果 thread 还没归档,补归档。 | 开(没人设过=代码默认) | 没人设过,默认「开」。QA 房的 Bridge 会被显式注入 0 做隔离,免得扫到真 Linear。 | Bridge:秒级生效 | **留**<br><sub>本轮取证</sub> |
| `done_thread_reconcile_interval_min`<br><sub>FLYWHEEL_DONE_THREAD_RECONCILE_INTERVAL_MIN</sub> | 上面那个兜底扫描多久跑一次。默认 360 分钟;0 = 只在启动跑一次。 | `360`(没人设过=代码默认) | 没人设过,默认 360。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `done_thread_reconcile_dryrun`<br><sub>FLYWHEEL_DONE_THREAD_RECONCILE_DRYRUN</sub> | 让兜底扫描只记录不真归档(观察模式)。 | 关(没人设过=代码默认) | 没人设过,默认关。它是排障工具,平时就该关着。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `done_thread_reconcile_max_per_run`<br><sub>FLYWHEEL_DONE_THREAD_RECONCILE_MAX_PER_RUN</sub> | 每轮最多归档多少条(防 Discord 限流)。默认 25。 | `25`(没人设过=代码默认) | 没人设过,默认 25。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `terminal_thread_archive`<br><sub>FLYWHEEL_TERMINAL_THREAD_ARCHIVE</sub> | issue 全段结束 + Linear 已 Done + pane 已死 → 分钟级自动归档它的 thread。 | 开(没人设过=代码默认) | 没人设过,默认「开」。关掉只剩 6 小时一次的兜底扫描。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `disposition_receipt`<br><sub>FLYWHEEL_DISPOSITION_RECEIPT</sub> | Lead 处置完一条告警之后,在 issue thread 里落一条看得见的回执。记账永远写,这个开关只管「要不要投递给人看」。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |

#### 消息层 / 信箱(2 条)

| 开关 | 在干嘛 | 现在 | 为什么是这个状态 | 改了怎么生效 | 裁决 |
|---|---|---|---|---|---|
| `mailbox_queue`<br><sub>FLYWHEEL_MAILBOX_QUEUE</sub> | 信箱的租约重投 + 合批投递 + 死信闸。设成 0 会在运行时切回旧投递流。 | **开(有人显式设过)** | **这是 11 条被显式设过的之一:`=1`。** 但默认本来就是开,所以这一行不改变行为——它是一条「刻意写出来的宣告」,配合部署时的 ACK 就绪闸使用(restart-services 在闸没过时会把它压回 0)。 | Bridge:要重启 | **留**<br><sub>本轮取证</sub> |
| `delivery_secret_path`<br><sub>FLYWHEEL_DELIVERY_SECRET_PATH</sub> | 投递 ACK 用的那把 HMAC 密钥文件放哪。默认 ~/.flywheel/delivery-secret。 | `~/.flywheel/delivery-secret`(没人设过=代码默认) | 没人设过,走默认路径。这是路径类配置,不该热改。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |

#### DAG / workflow 派工(16 条)

| 开关 | 在干嘛 | 现在 | 为什么是这个状态 | 改了怎么生效 | 裁决 |
|---|---|---|---|---|---|
| `design_html_gate`<br><sub>FLYWHEEL_DESIGN_HTML_GATE</sub> | design 节点收工之前必须交一份给 Annie 看的设计 HTML,而且要带能对上当前 HEAD 的可信证据。 | 开(没人设过=代码默认) | 没人设过,默认「开」。这是治理门,`=0` 只作 operator 应急放行。 | Bridge:要重启 / 命令行:下次调用生效 | **留**<br><sub>按默认</sub> |
| `boot_sha_check`<br><sub>FLYWHEEL_BOOT_SHA_CHECK</sub> | Bridge 启动时打印自己跑的是哪个 commit,并跟 origin/main 比一下;落后就告警。纯观测,不影响启动。 | 开(没人设过=代码默认) | 没人设过,默认「开」。分支 checkout(开发/QA 房)会自动静音。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `engine_dead_exec_sweep`<br><sub>FLYWHEEL_ENGINE_DEAD_EXEC_SWEEP</sub> | DAG 里执行体死了之后的恢复扫描。关掉=暂停做新的替换决定。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `workflow_rework_reentry`<br><sub>FLYWHEEL_WORKFLOW_REWORK_REENTRY</sub> | QA 或 Annie 要求返工时,重新进原来那个 actor;设 0 = 只 hold 住并告警,不驱逐也不另起。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `engine_unlaunched_tripwire`<br><sub>FLYWHEEL_ENGINE_UNLAUNCHED_TRIPWIRE</sub> | 对「已准入但没真起来」的 workflow 重试做告警、围栏和恢复。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `bridge_loop_guard`<br><sub>FLYWHEEL_BRIDGE_LOOP_GUARD</sub> | Bridge 事件循环卡死时自己退出,好让 launchd 把它拉起来(launchd 自己盖不住这个缺口)。 | 开(没人设过=代码默认) | 没人设过,默认「开」。注意:它只在 start() 时看一次,事后设 0 停不了已经跑起来的那个。 | Bridge:要重启 | **留**<br><sub>本轮取证</sub> |
| `cron_stale_guard`<br><sub>FLYWHEEL_CRON_STALE_GUARD</sub> | 起 run 撞 409 的时候,先看拦住它的是不是一个早就该失效的旧 blocker。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `workflow_template_dispatch`<br><sub>FLYWHEEL_WORKFLOW_TEMPLATE_DISPATCH</sub> | 总闸:派工走不走「模板 DAG」这条新路。关掉=回到旧的单会话派工。 | **开(有人显式设过)** | **被显式设过:`=1`(2026-07-31 Annie 拍板恢复 DAG 派工)。** ⚠️ 上一版审计管线会把它读成**关**——它那一行带行尾注释,老解析器把注释也算进值里了。本轮已修,并用行为反证:这次体检自己就是以 generalized workflow 节点跑的,只有它开着才会存在。<br>▸ 同时是 FLY-1436 的应急急停杆。红线:不许顺手改。 | Bridge:秒级生效 | **留**<br><sub>本轮取证</sub> |
| `workflow_gate_carrier`<br><sub>FLYWHEEL_WORKFLOW_GATE_CARRIER</sub> | 新起的 run 把 ship 批准冻结在「到达门口时」的载体上。已经在跑的老 run 不受影响。 | **开(有人显式设过)** | **被显式设过:`=1`。** 它只影响下一个新 run;真正决定行为的是 run 自己冻结的 epoch,不是这个 env。 | Bridge:秒级生效 | **留**<br><sub>本轮取证</sub> |
| `workflow_turn_divergence_alerts`<br><sub>FLYWHEEL_WORKFLOW_TURN_DIVERGENCE_ALERTS</sub> | 引擎和 CommDB 对「轮到谁」判断不一致时,发严重告警。默认关=只在影子模式记录,不吵人。 | 关(没人设过=代码默认) | 没人设过,默认关。**这是刻意的**:注册表原文说「先观察影子记录,再决定开不开」。它 8 月 11 号才加,现在动它没有观察依据。 | Bridge:秒级生效 | **留**<br><sub>本轮取证</sub> |
| `land_node`<br><sub>FLYWHEEL_LAND_NODE</sub> | 引擎自己拥有的 land 节点(合入 → 关 session → 清 worktree → 翻 Done/归档)。设 0 停止新的 land 激活,已经认领的继续收敛。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `workflow_generalized_templates`<br><sub>FLYWHEEL_WORKFLOW_GENERALIZED_TEMPLATES</sub> | 让 schema-v2 那套模板参与选取/准入/提交。 | **开(有人显式设过)** | **被显式设过:`=1`,`.env` 第 151 行,光秃秃没有任何注释。** 注册表描述里仍写着「默认关,为了字节兼容」和一串前置条件。⚠️ **它的启用不属于 7-31 那次拍板** —— 那条拍板注释挂在第 161 行的 `workflow_template_dispatch` 上,和这一行无关。<br>▸ 本轮**三次判断被推翻**,全过程见 audit.md §5 的 D-2 段。终稿口径:两条独立的腿 —— ①【谁批准的】查不到批准记录(而且 `workflow_generalized_templates` 连一条机械翻转记录都没有);②【是否在被依赖】重度在用(FLY-1693 e2e、529 房全链、今天四单含 E5 四单一体、claims 链当日二十多个 verdict)。**实证不顶替批准记录,没有批准记录也不等于它坏了。** Tadashi 的工程判断(是判断不是事实):属 registry 文档债,行动是更新条款、不是关开关。方向落在 ship 授权链上且无批准记录可援引 ⇒ 必须 founder 定。 | Bridge:秒级生效 | **分歧**<br><sub>本轮取证</sub> |
| `workflow_vendor_at_dispatch`<br><sub>FLYWHEEL_VENDOR_AT_DISPATCH</sub> | 每个 workflow 节点在起的时候现读「当前批准的派工配置」,而不是用当初钉死的快照。设 0 立刻回到钉死快照。 | 开(没人设过=代码默认) | 没人设过,默认「开」。注册表标明这是应急逃生口。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `workflow_claims_write`<br><sub>FLYWHEEL_WORKFLOW_CLAIMS_WRITE</sub> | workflow claims 的影子写入 + 已登记执行的准入。 | **开(有人显式设过)** | **被显式设过:`=1`,`.env` 第 142 行,光秃秃没有任何注释。** 注册表原文仍写着「Must remain off until the pinned fresh-spawn E2E and peer-credential hardening gates pass」。⚠️ **它的启用也不属于 7-31 那次拍板。**<br>▸ 与 `workflow_generalized_templates` 同一件事、同一个行动项。**本轮取到的机械证据(它比另一条硬)**:`~/.flywheel/audit.db` 的 `fleet_admin_audit` 记着这一行的翻转 —— `2026-07-19T00:54:09Z`,经 fleet 控制台(origin=loopback),`rawFrom:null → rawTo:"1"`,`applied`;8 分钟后 `teamlead.db` 落下第一条 `workflow_run`(01:02:05)。⚠️ 但这条记录只证明**什么时候、经什么面被翻开**,**不证明谁批准的** —— fleet 审计行不含人的身份;而 `founder_consent_audit` 的 action 词表里根本没有 flag 翻转这一类,所以它的沉默既不是批准也不是未批准,是**这条通路从来不记 flag**。 | Bridge:秒级生效 | **分歧**<br><sub>本轮取证</sub> |
| `workflow_claims_read`<br><sub>FLYWHEEL_WORKFLOW_CLAIMS_READ</sub> | 让已登记的 workflow run 用 claims 来判断能不能 ship、以及读权威 head。 | **开(有人显式设过)** | **被显式设过:`=1`。** Bridge 和 CLI 都是活的权威消费方。 | Bridge:秒级生效 / 命令行:下次调用生效 | **留**<br><sub>本轮取证</sub> |
| `instruction_path_check`<br><sub>FLYWHEEL_INSTRUCTION_PATH_CHECK</sub> | design review 的自动指令要绑定「已经提交上去的 plan 文件路径和内容指纹」,并由 Bridge 校验结果。 | 开(没人设过=代码默认) | 没人设过,默认「开」。FLY-1718 的一部分,跨进程授权面,按政策不给网页开关。 | Bridge:要重启 / 命令行:下次调用生效 | **留**<br><sub>按默认</sub> |

#### cmux / tmux 视图(9 条)

| 开关 | 在干嘛 | 现在 | 为什么是这个状态 | 改了怎么生效 | 裁决 |
|---|---|---|---|---|---|
| `tmux_keepalive`<br><sub>FLYWHEEL_TMUX_KEEPALIVE</sub> | 给 tmux server 挂一个哨兵会话并关掉 exit-empty,这样最后一个业务会话没了,server 也不会跟着消失。 | 开(没人设过=代码默认) | 没人设过,默认「开」。关掉只是停止后续维护,不会自动拆掉已有哨兵。 | 命令行:下次调用生效 | **留**<br><sub>本轮取证</sub> |
| `converge_cmux_symlink`<br><sub>FLYWHEEL_CONVERGE_CMUX_SYMLINK</sub> | 把散落的 cmux 脚本部署副本收敛回指向主 checkout 的符号链接(留档之后原子替换)。 | 开(没人设过=代码默认) | 没人设过,默认「开」。它自己写明的退役条件是「全机不再存在可写部署副本路径」。<br>▸ HL×Tadashi 收敛(2026-08-15)**推翻了我的初判**:保留,不退。converge 这套机制现在还是活的 —— FLY-1784 的部署注记刚用到它(cmux 侧 bin 副本 pull 之后要靠 converge 换字节)。 | 命令行:下次调用生效 | **留**<br><sub>本轮取证</sub> |
| `cmux_autostart_exec`<br><sub>FLYWHEEL_CMUX_AUTOSTART_EXEC</sub> | 允许没人看管的 autostart 脚本直接自己跑 watcher,而不是只去守 launchd 的任务。 | 关(没人设过=代码默认) | 没人设过,默认关。它自己写明的退役条件是「只用于 launchd 控制面故障的短时诊断,稳定后退役」;7 月 24 日加的,22 天里一次都没被打开过。<br>▸ HL×Tadashi 收敛(2026-08-15):稳定标准定为「30 天无 autostart 相关告警」,**已达标** → 提议退役,但动作排在 FLY-1778 落地之后(要先有翻转审计)。⚠️ **退役动作本身仍需 Annie 点头,不由 FLY-1778 顺手带掉。** | watcher:下次调用生效 | **固化**<br><sub>本轮取证</sub> |
| `cmux_linked_view`<br><sub>FLYWHEEL_CMUX_LINKED_VIEW</sub> | cmux 的窗口拓扑用不用 linked view(一个窗对一个视图)。 | **关(有人显式设过)** | **被显式设过:`=0`,而且到今天仍然没有任何书面原因。** FLY-1413 那轮就把它列成 UNKNOWN,当时的处置是「维持 0,等 FLY-1364 ship 之后重测」。三周过去了,重测没人做,状态原样。<br>▸ Tadashi 当时的回忆是「某次 cmux 不稳定期间关掉的」——**那是回忆,不是证据**,不作为结论。 | Bridge:要重启 / watcher:下次调用生效 | **分歧**<br><sub>查无依据</sub> |
| `cmux_wal_quarantine`<br><sub>FLYWHEEL_CMUX_WAL_QUARANTINE</sub> | 某个视图的构建日志写坏了,就把它单独隔离掉,让别的视图继续对账;关掉=回到「一个坏全体停」。 | 开(没人设过=代码默认) | 没人设过,默认「开」。只隔离四类纯语法/文件身份错误,其它不确定性仍然整轮 fail-closed。 | watcher:下次调用生效 | **留**<br><sub>本轮取证</sub> |
| `cmux_roster`<br><sub>FLYWHEEL_CMUX_ROSTER</sub> | 从 launchd/manifest 推出「现在应该有哪些 Lead」的名册,再拿去跟实际在跑的对账。 | 开(没人设过=代码默认) | 没人设过,默认「开」。关掉只暂停名册派生和对账,不改既有视图行为。 | watcher:下次调用生效 | **留**<br><sub>按默认</sub> |
| `cmux_view_invariant`<br><sub>FLYWHEEL_CMUX_VIEW_INVARIANT</sub> | 校验 managed 视图当前激活的是不是正确那个窗,不对就用安全方式修回来。 | **开(有人显式设过)** | **被显式设过:`=1`。** 默认本来也是开,所以这一行同样是「刻意宣告」而非行为改变。 | watcher:下次调用生效 | **留**<br><sub>本轮取证</sub> |
| `cmux_strict_view`<br><sub>FLYWHEEL_CMUX_STRICT_VIEW</sub> | managed tab 用独立的精确窗口视图。设 0 = 有序回滚到 grouped 拓扑。 | 开(没人设过=代码默认) | 没人设过,默认「开」。Bridge 和 watcher **两边各读一次**——只改一边会留半套拓扑。 | Bridge:要重启 / watcher:下次调用生效 | **留**<br><sub>本轮取证</sub> |
| `cmux_close_request_killswitch`<br><sub>FLYWHEEL_CMUX_CLOSE_REQUEST</sub> | 关 runner 时写一个 close-request 标记,watcher 看到就立刻把过期的钉子摘掉。 | 开(没人设过=代码默认) | 没人设过,默认「开」。Bridge 和 watcher 两侧都要改才完整。 | Bridge:要重启 / watcher:要重启 | **留**<br><sub>按默认</sub> |

#### Lead 侧(逐 Lead)(7 条)

| 开关 | 在干嘛 | 现在 | 为什么是这个状态 | 改了怎么生效 | 裁决 |
|---|---|---|---|---|---|
| `codex_lead_typing`<br><sub>FLYWHEEL_CODEX_LEAD_TYPING</sub> | Codex Lead 在 Discord 里显示「正在输入…」。 | 开(没人设过=代码默认) | 没人设过,默认「开」(和 Claude Lead 对齐)。 | Lead:要重启 | **留**<br><sub>按默认</sub> |
| `roundtable_thread_autocontinue`<br><sub>FLYWHEEL_ROUNDTABLE_THREAD_AUTOCONTINUE</sub> | 圆桌话题线程的自动接续。 | 开(没人设过=代码默认) | 没人设过,默认「开」。launcher 会把结果折算成一个 *_EFFECTIVE 变量再传给下游,链路有点绕但一致。 | Lead:要重启 | **留**<br><sub>按默认</sub> |
| `lead_chrome_enabled`<br><sub>FLYWHEEL_LEAD_CHROME_ENABLED</sub> | 这个 Lead 能不能用 Chrome / claude-in-chrome。 | 逐 Lead:0/15 开 | 逐 Lead 从各自的 manifest 读。实测:**当前所有 Lead 都是 false**。 | Lead:要重启 | **留**<br><sub>本轮取证</sub> |
| `lead_core_mention_gated`<br><sub>FLYWHEEL_LEAD_CORE_MENTION_GATED</sub> | core 房里没被 @ 的消息只让 CoS 回;非 CoS 的 Lead 必须被点名才回。 | 逐 Lead(launcher 计算) | 由 launcher 按 projects.json 的拓扑自己算,不是人手设的。 | Lead:要重启 | **留**<br><sub>按默认</sub> |
| `roundtable_thread_own_bot`<br><sub>FLYWHEEL_ROUNDTABLE_THREAD_OWN_BOT</sub> | 圆桌线程里要不要把自己这个 bot 发的消息也算进去。 | 关(没人设过=代码默认) | 没人设过,默认关。开着容易造成 bot 自问自答。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `lead_dry_run`<br><sub>FLYWHEEL_LEAD_DRY_RUN</sub> | Codex Lead 的预演模式:只描述打算做什么,不真的启动。 | 关(没人设过=代码默认) | 没人设过,默认关。它是**启动时临时加在命令行上**的排障工具,本来就不该常驻在 .env 里。 | Lead:要重启 | **留**<br><sub>本轮取证</sub> |
| `lead_cross_dept_channel_ids`<br><sub>FLYWHEEL_LEAD_CROSS_DEPT_CHANNEL_IDS</sub> | Codex Lead 要去轮询、并且接受被 @ 的跨部门频道 id 列表。 | **`1512578695468941333`(有人显式设过)** | **被显式设过:一个频道 id(#leads-roundtable)。** 这是真配置值,不是开关。Lead 侧和 Bridge 侧各读一次,三类进程都要重启才换。 | Lead:要重启 | **留**<br><sub>本轮取证</sub> |

#### 治理门(6 条)

| 开关 | 在干嘛 | 现在 | 为什么是这个状态 | 改了怎么生效 | 裁决 |
|---|---|---|---|---|---|
| `founder_consent_decision_mode`<br><sub>FLYWHEEL_FOUNDER_CONSENT_DECISION_MODE</sub> | founder-consent 硬门的模式。off=不管、audit_only=只记账不拦、enforce=真拦。 | **`audit_only`(有人显式设过)** | **被显式设过:`audit_only`。** 这是 FLY-175 三段式上线的**第 1 阶**(off / audit_only / enforce 里的中间档)——先只记账不拦截,攒校准语料;第三档 enforce 还没走。 | Bridge:要重启 | **留**<br><sub>本轮取证</sub> |
| `founder_attribution_gate`<br><sub>FLYWHEEL_FOUNDER_ATTRIBUTION_GATE</sub> | 批准门的回答必须归属到 founder 侧才算数(她本人 / Bridge / consent 服务)。 | 开(没人设过=代码默认) | 没人设过,默认「开」。`=0` 只给 QA 房和应急用。 | 命令行:下次调用生效 | **留**<br><sub>按默认</sub> |
| `comm_bypass_bridge`<br><sub>FLYWHEEL_COMM_BYPASS_BRIDGE</sub> | 应急:绕过 founder-consent 直接写 ship 门。 | 关(没人设过=代码默认) | 没人设过,默认关。开一次就会留一条很响的审计记录。**必须保持关着。** | 命令行:下次调用生效 | **留**<br><sub>按默认</sub> |
| `lead_lease_bypass`<br><sub>FLYWHEEL_LEAD_LEASE_BYPASS</sub> | 应急:绕过 Lead 身份租约的写授权。 | 关(没人设过=代码默认) | 没人设过,默认关。同样是「留着但永远别开」的那类。 | 命令行:下次调用生效 | **留**<br><sub>按默认</sub> |
| `founder_ux_gate`<br><sub>founder_ux_gate.mode</sub> | 所有实质性 issue 在开工前必须先跟 Annie 对齐(brainstorm 签字),只有带豁免标签的例外。 | 逐项目:geoforge3d=enforce / joycon-typeless=enforce / personal-assistant=enforce / growth=enforce / flywheel=enforce / tidal-echo=enforce | 六个项目都是默认 enforce,没人改过。 | Bridge:秒级生效 | **留**<br><sub>本轮取证</sub> |
| `founder_ux_gate_killswitch`<br><sub>FLYWHEEL_FOUNDER_UX_GATE_ENABLED</sub> | 全局把上面那道签字门**撤掉**。⚠️ 语义是反的:默认 OFF = 门被撤掉;要恢复原来的 enforce 才需要设 =1。 | 关(没人设过=代码默认) | 没人设过,所以现在的实况是「门是撤着的」。这是 FLY-900 刻意翻的方向。<br>▸ 这条最容易被读反——名字里带 ENABLED,但默认值 false 的含义是「关掉门」。 | Bridge:要重启 | **留**<br><sub>本轮取证</sub> |

#### 账号 / 配额 / 外部依赖(10 条)

| 开关 | 在干嘛 | 现在 | 为什么是这个状态 | 改了怎么生效 | 裁决 |
|---|---|---|---|---|---|
| `codex_gate_wait`<br><sub>FLYWHEEL_CODEX_GATE_WAIT</sub> | Codex 的常驻目标在必答门没被回答时保活等着,门解开后继续;设 0 = 直接判 blocked 终态。 | 开(没人设过=代码默认) | 没人设过,默认「开」。读点在 claude-runner 包里,但持有它的适配器是 **Bridge 构造的**——所以是 Bridge 进程读。 | Bridge:要重启 | **留**<br><sub>本轮取证</sub> |
| `quota_degraded_switch`<br><sub>FLYWHEEL_QUOTA_DEGRADED_SWITCH</sub> | 允许配额守护进程执行「已经在它自己配置里开启的」受控降级换号。 | 开(没人设过=代码默认) | 没人设过,默认「开」。设 0 会立刻压制换号。 | 配额守护:要重启 | **留**<br><sub>按默认</sub> |
| `claude_account_identity_check`<br><sub>FLYWHEEL_ACCOUNT_IDENTITY_CHECK</sub> | 给 Claude 账号池的每次读写做身份核验(确认这个凭据真的是那个账号)。 | 关(没人设过=代码默认) | 没人设过,默认关。开它有前置条件:必须先灌一份可信映射并跑过 audit;7 月 16 日加的,30 天里前置一直没做。<br>▸ HL×Tadashi 收敛(2026-08-15)**推翻了我的初判**:保留,并重起 30 天表。理由 —— 前置没做恰恰说明该做:Tadashi 当天早上刚撞过 active 标签与 keychain 真身不符的污染,这条防的正是那个。 | 配额守护:要重启 / 命令行:下次调用生效 | **留**<br><sub>本轮取证</sub> |
| `voice_qa_presence_override`<br><sub>FLYWHEEL_VOICE_QA_PRESENCE_OVERRIDE</sub> | 语音链路的 QA 缝:设 =1 时假装 Annie 在场。**只给隔离测试台用。** | 关(没人设过=代码默认) | 没人设过,默认关。而且它自带保险:一旦置位又指向生产 Bridge,进程会直接拒绝启动。 | voice-bridge:要重启 | **留**<br><sub>本轮取证</sub> |
| `quota_daemon_wake`<br><sub>FLYWHEEL_QUOTA_WAKE</sub> | Bridge 收到可信的「撞配额」信号后,立刻叫醒配额守护进程。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:秒级生效 | **留**<br><sub>按默认</sub> |
| `remote_reports`<br><sub>FLYWHEEL_REMOTE_REPORTS</sub> | 远程报告发布管线(把 HTML 报告发到一个不可猜的托管 URL)。 | 开(没人设过=代码默认) | 没人设过,默认「开」。Bridge 和 CLI 两侧各自独立读——只改一侧会 split-brain。 | Bridge:要重启 / 命令行:下次调用生效 | **留**<br><sub>本轮取证</sub> |
| `fleet_console`<br><sub>FLYWHEEL_FLEET_CONSOLE</sub> | Fleet 控制台那一面 + 它的路由。关掉=回到旧 dashboard。 | 开(没人设过=代码默认) | 没人设过,默认「开」。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `reports_ttl_days`<br><sub>FLYWHEEL_REPORTS_TTL_DAYS</sub> | 报告链接保留几天。默认 7 天。 | `7`(没人设过=代码默认) | 没人设过,默认 7。 | Bridge:要重启 | **留**<br><sub>按默认</sub> |
| `runner_autocontinue`<br><sub>FLYWHEEL_RUNNER_AUTOCONTINUE</sub> | 让 runner 自动续跑(goal-driven,不用人再戳一下)。 | 关(没人设过=代码默认) | 没人设过,默认关 —— 也就是说它在生产里**从来没被开过**。注册表原文写「先单-runner canary」,那次 canary 从没发生过;7 月 4 日加的,42 天。它的真实状态是【接线完整但从未启用】,**不是**「被 FLY-1774 取代之后的死代码」—— 这两个状态不一样,不能写混。<br>▸ HL×Tadashi 收敛(2026-08-15,**结论翻过一次**):先按「有独立调用方就保留」判为留(armer 模块 + plugin.ts:9659 的独立 poller 确实只由它把门);拿到「生产从未启用」这个事实后 Tadashi 改判为**建议退役**。退的理由不是「它没用」,是**「它会误导读代码的人」**:它的意图已经被两个后继机制分食(Codex 停驻唤醒归 FLY-1774 auto-wake、Claude idle 续跑归 detection-gated recovery-nudge),armer+poller 从来没开过 = 从来没被要求证明过自己有必要;留着的唯一效果是让下一个读 plugin.ts 的人以为系统里还有第三条唤醒通道。⚠️ **退役动作本身仍需 Annie 点头,不由 FLY-1778 顺手带掉。** | Bridge:要重启 | **固化**<br><sub>本轮取证</sub> |
| `publish_broker`<br><sub>FLYWHEEL_PUBLISH_BROKER</sub> | 对外发布(promote-commit / npm publish)的唯一执行点。开启会起一个 unix socket 请求面 + Annie ✅ 审批观察。 | 关(没人设过=代码默认) | 没人设过,默认关。7 月 12 日建好,34 天里从没开过——注册表自己也写「真发布另需 token 供给 + founder 批」。<br>▸ 建议:确认近期没有对外发布计划 → 明确标成「建好待用,不排期」,或者干脆归档。 | Bridge:要重启 | **固化**<br><sub>本轮取证</sub> |

#### Runner 提示词 / 逐项目能力(6 条)

| 开关 | 在干嘛 | 现在 | 为什么是这个状态 | 改了怎么生效 | 裁决 |
|---|---|---|---|---|---|
| `doc_flow`<br><sub>doc_flow.enabled</sub> | 给 runner 的提示词里注入「写部门优先过程文档」那一段。 | 逐项目:3/6 开(显式设过:joycon-typeless=true、flywheel=true、tidal-echo=true) | 逐项目:joycon-typeless / flywheel / tidal-echo 三个开着,其余三个默认关。是刻意的分布。 | Bridge:秒级生效 | **留**<br><sub>本轮取证</sub> |
| `skill_framework_mode`<br><sub>FLYWHEEL_SKILL_FRAMEWORK_MODE</sub> | runner 用哪套 skill 框架。superpowers / matt / bare / bare-ponytail 四条臂,或者 split=按 issue 稳定哈希分流做实验。 | **`split`(有人显式设过)** | **被显式设过:`split`。** 这是正在跑的实验形态,秒级可切回 superpowers。 | Bridge:秒级生效 | **留**<br><sub>本轮取证</sub> |
| `skill_framework_split_participation`<br><sub>skill_framework.split</sub> | split 分流下,这个项目参不参与实验臂。设 false = 这个项目钉回 superpowers。**这是退出杠杆,不是启用开关。** | 逐项目:6/6 开(全部按默认) | 六个项目都是默认 true(都参与)。没人退出过。 | Bridge:秒级生效 | **留**<br><sub>本轮取证</sub> |
| `proofshot`<br><sub>skills.proofshot.enabled</sub> | 视觉验证(ProofShot)的自动触发。 | 逐项目:0/6 开(全部按默认) | 六个项目**全部默认关**,从没有项目开过。它依赖浏览器链路,而所有 Lead 的 Chrome 能力也都是关的。<br>▸ 建议:和 lead_chrome_enabled 一起看——两个都全关,说明这条能力线整体没在用。 | Bridge:秒级生效 | **固化**<br><sub>本轮取证</sub> |
| `xiaohongshu_learning`<br><sub>xiaohongshu_learning.enabled</sub> | 定期小红书收藏学习管线。 | 逐项目:0/6 开(全部按默认) | 六个项目**全部默认关**。这条能力在 FLY-222 建好,当前没有项目在用。<br>▸ 建议:确认是暂时不用还是不再用;不再用就归档,别留一个永远关着的项目开关。 | Bridge:秒级生效 | **固化**<br><sub>本轮取证</sub> |
| `ponytail`<br><sub>ponytail.enabled</sub> | 代码极简 ponytail 的逐项目 rollout。 | **项目层 dormant(设了也不生效)** | **项目层是 dormant 的**——注册表和代码注释都写明「run-infra 明确不加载它,项目里写了也没用」。这是 Annie 的例外条款。<br>▸ 它是 124 条里唯一一条「登记了但故意不生效」的。保持现状,但要在总表里显式标出来,免得有人以为设了就有用。 | 不适用 | **留**<br><sub>本轮取证</sub> |
