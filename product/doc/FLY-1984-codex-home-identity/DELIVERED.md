# FLY-1984 当前交付物是哪一份

更新: 2026-08-27 · ⚠️ 每次改版都更新本文件,**投卡前以本文件为准**

## 当前唯一有效的页

| | |
|---|---|
| 文件 | `where-v5.html`(⑤ 已按她 08-27 的意见重画) |
| URL | `https://fw-reports-a53de2.vercel.app/r/bf7eb717fb4d0346db575464cdb65714/` |
| 对应 commit | `2677e8588`(= 远端,已推) |
| 依据 | Lead 指令 `ddcb8799` + 回执 `4f582492` + 回执 `289e87c1`(授权全刷 + 给每个数字标测量日) |
| 九节 | ① 一人一个 ② 三个提问的答案 ③ 两边都留着 ④ 门开着但没人搬(改正 v4 的反向误读) ⑤ agent file 和 memory 住两个地方 ⑥ 两套分法对不上 ⑦ 三猜测逐条 ⑧ 定期 push 就行吗 ⑨ 下一步 |

## ⛔ 全部作废的页(都不要再投)

| 文件 | 为什么作废 | 作废的 URL |
|---|---|---|
| `where-v4.html` | ④ 那格「没进 + 不是被挡在外面的」是**用两个否定解释一件事**,她读成了「要加进 .gitignore」——**方向读反了**。v5 正着重写 | `e428403c…` `9042d02e…` `d7ab88b2…` |
| `memory-v2.html` | 内容对,但已整段并入 v4 → v5 | `30645ccc…` `e47a6418…` |
| `decide.html` | 她否了「几个人 vs 几副面孔」的升维;中间档(按部门共享)被她明确否掉 | `497531df…` `5162270f…` `cd662f59…` `10324e92…` |
| `explainer.html` | 第一轮长文版,形式被打回(「你跟我说的话实在太多了」)。文件按「交出去的产物不原地改」保持原样 | `278056126…` |
| `where-v5.html` 的更早三版 | `.steps` 里 `b{display:block}` 把「开始搬」切成四行 / 树里的 ⬜ 渲染成疑似乱码 | `c4f6557d…` `f5604eaa…` `05344682…`(⑤ 还是概念说明,她说「看不出来」) `3f9f5919…`(④⑦⑧ 还带着两天前的旧数) `e996c362…`(数字还没标测量日) `82844a65…`(⑤ 还写着「四个 runner 都被喂了」——**那句是错的**) |

## 数据出处

| 文件 | 覆盖 |
|---|---|
| `research.md` | 第一轮:codex home 数量 / 各家记忆条数 / 当初分家的动机 |
| `memory-v2-facts.md` | 第三~五轮:Claude Code 两层记忆 · `~/.claude` 的 git 与远端状态 · agent file 的位置 · 我改掉的三条上游说法 |

## ⚠️ 已过期、别再照着推的文件

| 文件 | 为什么 |
|---|---|
| `exploration.md` | 核心假设(把「文件夹」升维成「几个人 vs 几副面孔」)**已被 founder 明确否掉** |
| `round-1-notes.md` | 记的是第一轮长文版的状态 |

## 轮次

- `03347e13-6057-407c-9979-0dabcb46e54a` —— 指向 **v4**,Lead 说 v5 出来开新轮会自动 supersede
- `e5524ccb-…` —— 指向长文版,已 `terminal_disposed`(01:58:14 被 03347e13 自动 supersede,账本已核)

## ⑤ 这一格 2026-08-27 改了什么

她的原话:「IC 的 memory 我还是没有看出来。给我一个实际的例子,找一个实际的 lead 和 IC,把树状图和路径画清楚。」

- 旧 ⑤ 用的是 `<谁>` / `<哪个仓>` 占位符 —— 正是她跟不下去的原因。
- 新 ⑤ = 两棵**真路径**树:Honey Lemon(`flywheel-product-lead`)+ 写这一页的 runner(`~/Dev/flywheel-FLY-1984`)。
- 露出的不对称:**Lead 按名字分家(12 个文件夹),IC 按项目路径分家(1 个,所有 IC 共用)。**
- 数字改用与本页 ④⑦⑧ 相同的口径(文件夹全部条目),脚注点名了跟 ④ 的漂移,避免同页两个数打架。

⚠️ **URL 变了,变不了同一个。** `publish-report` 按内容哈希出 URL,同一份文件改一个字就是新地址;
「更新到同一个 URL」在这条工具链上做不到。**但她已写的批注不会丢**:新旧同 origin、
`localStorage` KEY 与 9 个 `data-s` 标签逐字相同(已现场比对)。

## 2026-08-27 第二次改动:④⑦⑧ 刷数字

Lead 回执 `4f582492` → `446→466` · `1028→1033` · `27 MB→28 MB`(残留 0)。

## 2026-08-27 第三次改动:全刷 + 给每个数字标测量日

Lead 回执 `289e87c1`。她的判断:真问题不是某个数过期,是**这一页的数字全都没有测量时刻**,
所以才会一轮一轮追着刷。⇒ **标上日期 = 删掉歧义,过期数字自己会说话。**
⛔ 不新增任何自动刷新/对账机制(founder 否过那类修法)。

已刷(有同口径实测值的):`462→481` · `156→149` · `87→88` · `242→244` · `97→112`(残留 0)。

全页只有两个测量日,且都是**用 git 历史定的,不是凭印象**:

| 日期 | 含义 | 页面出现 |
|---|---|---|
| `(8-27 测)` | 我今天现数的 | 11 处 |
| `(8-25 测)` | 上一版留下、今天没有重数的 | 7 处 |

⛔ **故意不刷的三处 —— 刷了会悄悄改掉那句话的意思**,每处都在页面上就地写明了理由:

| 位置 | 数字 | 为什么不刷 |
|---|---|---|
| ④ | 107 个文件提到「最近的」FLY 单号 | 「最近的」这个界定复不出来;按「任何 FLY- 引用」数得 393,**不是同一个量** |
| ⑧ | Lead 1029 里 282 / 节点 453 里 96 | 分母是那天的总数,我**没重跑这次筛查** ⇒ 分子分母一起留在 8-25 |
| ⑦ | 只有 8 个建了目录 | 8-27 按 `FLY-*` 前缀重数得 3 个,**两次数的不是同一个集合** |

⚠️ **URL 每版都会变,变不了同一个。** `publish-report` 按内容哈希出地址。
**但她已写的批注不会丢**:每版都现场比对过 —— 同 origin、`localStorage` KEY 相同、9 个 `data-s` 逐字相同。

## 2026-08-27 第四次改动:⑤ 撤回一句错话,并且换掉了这一单的答案

**起因**:Tadashi 读 spawn 代码发现 `spawn argv 里没有 --memory`,与我和 Lead 的叙述冲突。
Lead 让我在**不读盘**的前提下回答「那 466 条到底在不在我的上下文里」。

**答案是第四种,不是是/否**:
```
✅ 在的:那个目录的 MEMORY.md —— 一份【索引】,而且只到前 ~24.4KB(约第 113 行)
❌ 不在的:466 个文件的【正文】
```

**随后 Lead 授权我查两格,结果换掉了这一单的答案**:

| 查什么 | 结果 |
|---|---|
| `--memory` 在我自己 argv 里 | **0 次** —— Tadashi 对,我独立复核 |
| `append-system-prompt.md` 全文(467 行 / 34,213 字节)含记忆索引吗 | **0 命中**(试了 4 个独有字串)⇒ 不是 Flywheel 注的 |
| 我说的「四个 runner 都被喂了」 | ❌ **错的**。那三个的命中是 ① onboarding 文档告诉它路径 ② 文件变更提醒 |
| 放大采样:822 个 runner 会话 | 提到路径 461 个,**真被注入 1 个**(就是本会话) |
| 再放大到全机所有会话 | **一共 2 个**:本 runner + Honey Lemon 当前会话;两个注入的是同一个路径 |
| founder 自己在主仓的 1081 个会话 | **0 个** |
| 同期对照(今天起的 2071 / 1969) | **0 次** ⇒ 排除「旧记录格式看不出来」 |

⇒ **这一单原来的问题(共用一份 vs 一人一个)问得比现状超前了一步。**
   真实状态:**不是「共用一份」,是「桌上放着一本,基本没人翻」。**

⛔ **我拒绝了 Lead 的一条字面指令**,并先报后改:她让 ⑤ 写「索引 ✅ 自动进」,
但那对 **821/822** 的 IC 是假的。援引的是她自己上一条立的规矩 ——
「页面必须诚实优先于字面边界」;上一次它指向【去改】(页脚假话),这次指向【别改】。
她复核后采纳了我的版本。

⛔ **两格仍然不下结论**:为什么只有这两个会话被注入(要读 Claude Code 自身,超出本单);
Honey Lemon 那 88 条走的是另一条通路,**我那次注入推不出她那 88 条没进来**——这一格由她自己记。
