# FLY-2309 自动合并怎么落地 — 探索

Issue: FLY-2309 (https://linear.app/geoforge3d/issue/FLY-2309/co-create落地-自动合并怎么落地-影子跑怎么跑规矩改哪几条拆成-build-issue)
日期: 2026-09-03
基于: 无(上游是 FLY-2261 的 process-log.md §1)

## 1. 本单的真意图(一句话)

FLY-2261 已经把**判据本身**跟她一起收敛完了并且她过了卡。本单**不重开判据**,
只回答一个问题:**这套判据要真的跑起来,具体要做哪几件事、按什么顺序、谁来做。**

派工把三样列得很死:① T6 落地路径 ② prd.md ③ 拆 build issue。
⇒ 这不是一个「还要再探她定见」的开放题,是一个**把已定方向落成可执行清单**的收口题。

## 2. 从 2261 继承、本单不得重开的五条

| # | 已定 | 出处 |
| - | ---- | ---- |
| ① | 判据 = 改动类别 + 真环境证据,**不走置信度打分** | 她第 4 轮认方向 |
| ② | 默认强度二 = 真跑一次 **+ 外部可查、能被重跑的记录**(两半缺一不可) | 她第 5 轮原话 |
| ③ | 起步走补法 C:**先影子跑**,宽度最保守 = 纯文档类 | 她第 5 轮「补法 C 不错」 |
| ④ | 资格门槛:一个类别只有在「不需要任何判断、光看改了哪些文件就能判定」时才有资格 | 她第 6 轮质疑逼出 |
| ⑤ | 错判不对称:错判进自动道 = 真代价;错判出 = 零代价 ⇒ 有一丝拿不准就不进 | 同上 |

## 3. Topic 树(本单)

```
自动合并怎么落地
├── R1  影子跑到底记什么      ← 先钻(它决定两周后那张表长什么样)
├── R2  两周后交的那张表      ← 放手判据事先定死
├── R3  规矩改哪几条          ← 她说「影子跑期间一条都不用改」,那要确认「一条都不用改」成不成立
├── R4  拆成哪几个 build issue ← 交 Tadashi 队列
└── R5  边界:契约不动、PM 验收不做
```

**当前位置:R1–R5 全部在 prd.md 里落定;R3 出了一个必须先给她看的实证结果(见 research.md)。**

## 4. 我在探索阶段先去查的三件事(不是拍脑袋写路径)

1. **「机器判的类」今天真的判得准吗** —— 拿回溯数据直接算一次纯文档类错判率,
   不要等两周后才第一次看见这个数。
2. **两张表的原始记录能不能对上** —— research.md(2261)§6.2 明写「我没有把
   `ship_relevant_diff_snapshot` 与那 21 条逐条对上,两边 key 不同」。这是影子跑的地基,
   对不上就没有影子跑。
3. **强度二的证据今天有没有地方落账** —— 「外部可查、能被重跑的记录」如果没有台账,
   闸② 就是一句话而不是一道闸。

⇒ 三件全部查完了,结论在 research.md。**第 1 件查出了一个会改变落地顺序的结果。**

## 5. 边界(本单不做)

- 不动 `founder-only-authority` R1 / merge 授权契约。她**至今没有批准自动合并上线**。
- 不写实现代码 —— 本单只出 PRD + build issue 定义,实现交 Tadashi 的队列。
- PM 验收 = FLY-830,本单不做。
- 🔴 2261 里那个「9 条加需求约 2 条必须当场拦、约 7 条可下一单」的 **2/7 分法未经她确认**,
  本单任何地方引用它都带这个标注,且不拿它当承重结论。
