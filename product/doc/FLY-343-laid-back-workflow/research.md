# FLY-343 躺平工作流 — 调研(折入落点 + 设计原则抽取)

Issue: FLY-343 (https://linear.app/geoforge3d/issue/FLY-343/xhsclaude-躺平工作流cli-画板-手柄-语音改图)
日期: 2026-07-08
基于: product/doc/FLY-343-laid-back-workflow/exploration.md

> 方向已由 brainstorm gate 定:**选项 A 折入**(Honey Lemon confirm,343 独有新 scope≈0)。
> 本文档的活 = 把「躺平工作流」灵感**折进哪、抽成哪几条设计原则**做实,给 plan 的判定 + 两条
> 建议评论(FLY-906 / FLY-914)提供逐条落点证据。不碰 packages 代码。

## 1. 三个交互面 → 已有归属(逐个坐实)

### 1.1 语音改内容 / 离屏沟通 → FLY-906 Voice 产品体验设计
- FLY-906 prd.md **§17 异步多-agent 语音模式(离屏推进核心)** 已有一组「**设计原则(Annie 定,
  硬性)**」:推不拉 / FIFO / 一条一回 / 语音报头 / per-agent 声线。
- 北极星写死在 §头部:**FLY-212(离屏也顺畅工作)**;G2 = 「Annie 能在离屏(做家务/处理私事)
  时语音汇报进度 / 接收 guidance / 定下一步」。
- → 「躺平/离屏也能顺畅指挥」正是 FLY-906 的**存在动机**。FLY-343 对它 = **外部佐证 + 一条
  输入模态边界提醒**,不是新需求。

### 1.2 手柄 / 离键盘输入 → JoyCon 项目
- JoyCon 是已注册的 flywheel project(~/.flywheel/projects.json 有)。手柄类「离键盘输入」是
  它的地盘。FLY-906 明确选了**语音**当解法形状 —— 别在 Voice 里再塞多模态手柄输入(scope 混)。
- → FLY-343 的「手柄」面 = 指回 JoyCon,不在 Voice / 914 重复设计。

### 1.3 交互可视化 / 画板 / 交互页 → FLY-914 可交互 HTML 审阅件
- FLY-914 prd.md 的**收敛历史本身**就是「别过度做交互 UI」这条教训的活证据:
  - 批注创作:Annie 真机试**划词词级(B)→ 别扭 → 翻回段落级(A)**(§1)。
  - 回流:Annie 拍板**剪贴板复制**、砍掉 serverless relay(§2,FLY-931 降 backlog)。
  - 非目标纪律:「**每加一项必砍一项**」——砍实时协同光标、dark mode、富文本编辑、词级划词(§Non-goals)。
  - v1 基建**零后端**(纯 clipboard,不 POST,CSP 只需 nonce script-src)。
- → FLY-343 那条社区教训「把交互过程做成交互页,最终太难用放弃了」= **外部独立佐证** FLY-914
  已在走的克制路线。折入 = 把它登记成 914 的一条**显式设计约束/佐证**,强化现有 non-goal 纪律。

### 1.4 投影 / 大屏(第四面)→ 纯输出语境,无独立 scope
- draft 还有一面「躺着看投影/大屏」——这是**输出/可读性**语境(离屏也能看清),不是需要独立
  承接的交互 scope。现有交付物可视化(FLY-914 审阅件 + publish-report 托管页)+「离屏舒适」
  北极星(FLY-212)已覆盖「看得爽」这件事。→ 不单独设计,归入既有输出面 + 北极星。

## 2. 抽出的设计原则(要折入的东西,一共两条 + 一条边界)

| # | 原则 | 落点 | 依据 |
|---|------|------|------|
| P1 | **离屏/躺平舒适度是北极星**:让 Annie 敢离开屏幕也能顺畅指挥 = 产品追求;外部社区(XHS 47赞/42藏)在追同一人机工效方向,佐证方向对,别因「语音酷」而做(避 shiny-object)。 | FLY-906(§17 / 北极星段,当**佐证**) | FLY-906 §17 + FLY-212;exploration §3 |
| P2 | **交互可视化要克制**:交互页/画板类可视化「容易做重、难做好用」;有人做了交互页最终太难用放弃。任何新增交互 UI 先问「拇指顺不顺、能不能砍」,守住「每加一项必砍一项」。 | FLY-914(§Non-goals / 设计约束,当**佐证**) | FLY-914 §1/§2/Non-goals 收敛史;draft 社区评论 |
| B1 | **输入模态边界**:语音 = FLY-906 的解法形状;手柄类离键盘输入 = JoyCon;别在 Voice/914 里塞多模态手柄。 | 判定文档 + FLY-906 评论附带一句 | §1.2 |

## 3. 为什么不独立立项(坐实 gate 判定)

- draft 三块全部已有更成熟归属(§1),独有可建 scope ≈ 0。
- 独立立项要造「投影+手柄+语音编排层」= 撞 draft 自带的「交互可视化难做好用」教训 + FLY-212
  「造到 PR+QA 才发现没戳心坎」教训;`Low` 优先级也不支持重投入。
- 折入 = 轻、零工程、直接强化两个在建 PRD 的既有方向。**这就是 Cass + Honey 的一致判定。**

## 4. 交付边界(light delivery)

- 产出 = 本 folder exploration/research/plan(判定)+ FLY-906 与 FLY-914 各一条结构化建议评论。
- **不**改 FLY-906/914 的 PRD 正文(它们在跟 Annie 逐版共创,评论是建议不是越权改稿)。
- **不**新建 build issue、**不**碰 packages 代码。Codex design-review 过即定稿。
- FLY-343 本身 → 建议标记 folded / 关(留给 Honey/Annie 执行,判定文档写清理由)。
