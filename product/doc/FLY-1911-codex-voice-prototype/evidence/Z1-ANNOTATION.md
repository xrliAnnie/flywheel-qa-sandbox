# Z1(19:47:56.269Z 那场)的批注 —— **`pathChanged: false`,这一场不是换路对照**

> 为什么批注写成独立文件、而不是改那份 manifest:**manifest 是证据原件,不回填**。
> 这条纪律先前已经用过(`ok` 假绿那两份也没回填,只在入口声明)。
> ⇒ 想读 Z1 的人必须同时看到这一页;想复核原件的人拿到的仍是逐字未改的原件。

## 事实

| | |
|---|---|
| `pathChanged` | **false** |
| 出口 IP | `24.24.178.135` —— **换热点前后完全一样** |
| 第 1 跳 | `10.47.180.15`(热点设备) |
| **第 2 跳** | **`192.168.68.1` ← 家里路由器** |
| 第 3 跳 | `142.254.184.221`(家里宽带) |

⇒ **热点把流量绕回家里的宽带,对面看到的路径没变。**

## 成色标注(重要)

**上面那张表是 Lead 测的**(三家独立服务 + traceroute),**我没有独立复核。**

而「出口 IP **未变**」这半句的成色要说得更准 —— **Lead 自己去数过:48 份 manifest,只有成功这一份带 `egressIp`。**

| | 成色 |
|---|---|
| **这一场**的出口 IP = `24.24.178.135` | ✅ **量到的**(写在 manifest 里,可复核) |
| **前九场**的出口 IP | ⚠️ **无记录** —— 靠 Lead 现场量的两次读数 + 「家里公网 IP 一整天没变」这个**稳定性假设**推出来的 |

⇒ **所以「和前九次相同」是推断,不是测量。** Lead 已就此向 Annie 更正过一次口径。
⇒ 下一个人引用这一格时,**不许把它当成一次对照测量**。

## ⛔ 这一场**不能**用来支持什么

- ⛔ **不许**写成「换了网络路径也一样 ⇒ 板上钉钉在对面」。**路根本没换,这个结论没有证据支撑。**
- ⛔ 我先前那句「**唯一变的就是网络路径**」**已被证伪,作废**,不许再被引用。

## ⭐ 但它推出了另一件事,而且这件事比原来那个结论更硬

路**没**换,其余**一字未改**,账号戳同、`openaiApiKeyPresent` 同 —— **而它活了。**

⇒ **在所有我们已知的变量都没动的情况下,第 10 场的结果和前 9 场不同。**
⇒ **这是十场里的第一次形状改变**(前九场:`died` / `closeReason:"error"` / 33–35 秒;这一场:**从未 `closed`**)。
⇒ 所以正确读法是:**这个故障不是确定性的,至少在我们能控制的变量之外还有东西在变**
(时间、对面的状态、某个我们没记的量)。**具体是什么,未知。**

> ⚠️ 仍然 **n=1**。「不是确定性的」这个判断本身只靠这一场支撑 ——
> 再来一场死的,它就更稳;再来一场活的,方向就要重估。
> **不许把它升级成「已经好了」。**
