# FLY-1911 交接 —— 2026-08-23 23:45Z(写它是因为我 ctx 94%,可能撑不到查出成因)

> ⚠️ **更新的一份是 [`HANDOFF-2026-08-24.md`](HANDOFF-2026-08-24.md)** —— 下面「现在挡在最前面的」那一条
> (语音会话起不来)在 08-24 那一场**没有复现**;当天真正挡路的是另一件事(她听不到声音)。
> 本页其余内容仍然有效。


## 🔴 现在挡在最前面的:**语音 realtime 会话起不来,成因未知**
```
症状锚点(~/.fly1911/rb1-bridge.out,23:40 那场):
  WAIT-HUMAN 待命 → 有人进房 → MEETING 议题 528 字符 → JOINED → READY
  ⛔ 没有 "realtime started" 事件
  TX 0 · ANSWER 0 · CODEX-ERR "conversation is not running" **3,224 条**
好的对照(~/.fly1911/jf1-bridge.out,21:44 那场,同一份代码路径):
  有 "realtime started",议题答对(主题 + 三件事)
```
**已排除(每条都有实测,不是推理)**
```
额度 / auth      同一时刻文字探针跑通:401=0 · error=0 · 7 天窗口 33%
我们的改动        回滚到实测能说话的那一版,症状一样
```
⇒ **文字路径好的,语音路径起不来。** ⛔ 成因没查(Lead 让下一个人查)。

## 代码当前状态
· `prototype/bridge-hl.mjs` = **首次进房喂议题**那一版(WAIT-HUMAN 门)。
  ⚠️ 「每次进房重建会话」**未实现** —— 试过一次,那一版第一次进房就不能说话,已回滚。
  ⚠️ 我那次改动**同时做了重构(抽函数)和行为变更(每次重建)**,所以回归时分不开 ——
  📌 教训:**改动能工作的代码时,重构和行为变更不许同一次提交。**
· 回滚后仓库无 diff(那次改动从未提交)⇒ 仓库里一直是回滚版。

## 三项验收门(她定的)的成色
```
① AGENTS.md 身份  ⚠️ 语音路径**验过一次**(隔离轮 HL=0,只靠 AGENTS.md 也答对合同题),**稳定性未知**
② Memory          ✅ 已验(它跑真命令 grep 记忆目录,答案与正文逐字符合;FLY-1038 在索引里 0 次 ⇒ 排除「从索引读到」)
③ Meeting Topic   ⚠️ 修法有效验过一次(空转 11 分钟 → 进房 → 议题答对),但**她第二次进房会复发**(见上)
```

## 已排除的机制候选(议题读不到那件)
```
开场指令被丢       死 —— 空转后合同题照样答对
空转搞坏了工具     死 —— 空转后记忆题跑了 3 条命令
空转后第一轮不拿工具 死 —— 空转 660s 后第一轮就跑了 2 条命令(判据跑前写死:f97a4731e)
客户端 idle 机制   不存在 —— openai/codex rust-v0.148.0(与本机二进制同版)realtime_conversation.rs 里
                   grep idle/reconnect/heartbeat/keepalive = 0
```
⇒ **未解 1**:长空转后不去读议题。剩下的两种(服务端 / 模型判断)我们够不着。

⇒ **未解 2 —— 语音起不来的一个假说【未验 · 由 Lead 于 2026-08-23 23:5xZ 提出】**:
```
这台机上同时跑着 7 个 codex app-server,全在同一个 personal 账号上;
其中 FLY-2000 那个起于约 22:30 —— **正好落在「21:44 语音好 / 23:08 语音坏」这个窗口里**。
形状:文字正常、只有 realtime 起不来 ⇒ 像是 realtime 有单独的并发上限。
```
⚠️ **只是形状吻合,不是证据**(时间窗对得上 ≠ 因果;我们没有任何一条读数指向并发上限)。
**证伪方法**:让其它 codex runner 静几分钟,趁那个窗口起一次语音 ——
· 起得来 ⇒ 假说站得住(再考虑怎么长期共存)
· 还是起不来 ⇒ 假说死,回到「服务端 / 未知」
⛔ 那几个 runner 是 Tadashi 的,**不要自己去停** —— Lead 已经去问他了。

## ⚠️ 版本(这一格被查过两次,结论比任何一方原来说的都精确)
```
桥实际跑的二进制  bridge-hl.mjs 里【硬编码】…/releases/**0.148.0**-aarch64-apple-darwin/bin/codex
                  该文件存在,自报 `codex-cli 0.148.0`      ← 语音这条路跑的就是它
PATH 上的 codex   `codex --version` = **0.149.0**            ← 我用 login status / doctor 时用的是这个
我读的源码        openai/codex tag `rust-v0.148.0`
```
⇒ **源码版本与「桥实际跑的二进制」对得上**;
⛔ 但**与 PATH 上那个 0.149.0 对不上** —— 凡是用 PATH `codex` 量到的东西(login status、doctor)属于 0.149.0。
⚠️ 下面那两条源码结论(无 idle 机制 / 5,300 预算摘要)**成色 = 在 0.148.0 上成立**,
   若以后把桥指向 0.149.0,**需要在 0.149.0 上复核**。

## ⭐ 最该传下去的一条结构事实
```
语音 prompt = params.prompt + build_realtime_startup_context(总预算 5,300 tokens)
                Current Thread 1,200 / Recent Work 2,200 / Workspace 1,600
            + realtime_start_instructions(上限 8,192,**逐字**)
而我们塞给 thread 的身份载荷 = 73,354 字符
```
⇒ **文字路径和语音路径拿到的不是同一份上下文**;**要让语音侧可靠知道的东西,必须走逐字通道**。
⇒ 「进房时喂议题」之所以有效,正因为它走 `realtime_start_instructions`,绕开了摘要压缩。

## 其它挂着的
· **中继发给 Lead 的消息 100% 死**(`revoked_orphan`:发件身份不是注册过的 session)——
  【已定位 · 未修 · 修法有待评估副作用】(给非 runner 登记 session 可能污染巡检名册)。
· 老 founder 卡 `3d832c54` 她说作废,但**系统里没有任何角色能执行**(CLI 无 cancel/void),已报为机制缺口。
· 房:收着。⛔ 不要在没验过之前叫她。
