# HL 彩排判据(⛔ 跑之前写死,跑完不许改)  写于 2026-08-21 20:5xZ

① 中继:必须先看到它【活过】—— pid 存在 + 心跳文件存在。⛔「没看到中继坏」不算数。
② 房:探针进得去,它听见并回答(转写里有 user 那句 + 它的 assistant 回答)。
③ 查真事:问「现在有几个 runner 在跑,分别哪几个」,拿 teamlead.db `status='running'` 逐个 issue 对。
   成 = 数目和 issue 集合都对;⛔ 只对数目不算。
④ ⭐ 同一场里跑 hl-tell-lead.sh:
   成败以 **mailbox 里有没有一条 from_agent='voice-honeylemon-fly1911' 的新行** 为准,
   ⛔ 不以脚本退出码为准;且正文前缀必须是新版(⛔ 不含「她在语音房里」那半句)。
   最终确认权在 Lead 的收件箱,我只报我这边能核到的证据。
⑤ 等待音 B:从房间录音那一侧量音间隔(B=1.50s / A=0.75s);状态行:同一条消息被原地改;
   ⛔ 不念预告:SPEAK 事件数 = 0。
⑥ 收尾:桥退出后中继进程消失 + 心跳文件被清。

⛔ 任一项没过 ⇒ 原样报 Lead,不自己修了重跑。
⛔ 它做不到的时候说了什么,原样贴出来(她最担心的是它编,而彩排能看见它说话的方式)。
