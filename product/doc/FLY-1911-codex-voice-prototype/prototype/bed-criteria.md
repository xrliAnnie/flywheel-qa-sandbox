# 等待音新触发的判据(⛔ 跑之前写死,跑完不许改)  2026-08-21 23:3xZ
① 它在真的干活的那段(跑命令/思考)⇒ 房间录音响度 **非零**(和之前同一把尺子:50ms RMS)
② 它答完、轮到她说话的那段(答完后 25 秒静默窗)⇒ 响度必须是 **0**
③ 动态开关:跑动中把 bed.switch 写 0 ⇒ 正在响的那一段要在几秒内停(⛔ 不用重起桥)
⚠️ ② 是重点:①上一轮已验过,只验①不算修好。
