# 专属 CODEX_HOME 的验收判据(FLY-1911 · 冻结)

**判据来源**:Lead 2026-08-22 05:30Z 下的三条;②在跑之前按实测拆成 ②a/②b,05:39Z 提出、05:39Z 批准。

⚠️ **提出时刻 vs 首次跑出结果的时刻**(用来判断这次改判据是修正还是文过饰非):
```
②a/②b 拆分提出于  2026-08-22T05:38:51Z(报告 29806533,落库时刻现查,基于实测:memories 不是会话写的)
首次跑出结果于      尚未发生(缺 auth,会话起不来)
⇒ 拆分发生在【任何结果出现之前】
```
📌 Lead 立的边界:**判据可以改,但改的时机决定它是修正还是文过饰非。**
合法 = 跑之前 · 基于实测事实 · 提给 Lead 拍 · 不自己改;
不合法 = 跑之后 · 基于「结果不好看」· 自己调。
⚠️ 两者**产出长得一模一样**(都是一份更合理的判据)⇒ **区分它们的唯一凭据是时间戳**,所以写在这里。

## ① 隔离:不污染另外两个 home
起会话前后,`~/.codex` 与 `~/.codex-infra-bot` 的 memories 逐表行数、sessions/archived_sessions 文件数**都不变**。
· 量法:`node codex-home-probe.mjs snapshot <标签>` 前后各一次,再 `diff`。
· **状态:已有一次真实证据(401 那场)** —— 专属 home 落了整套(含 `sessions/rollout-*.jsonl`),
  同一时刻公共 = 没变、infra-bot = 没变。⚠️ 但那是一场 401、**没有真实对话**。

## ②a 会话记录落在专属 home(会话本身能证的那半)
本场的 `sessions/rollout-*.jsonl` 出现在 `~/.codex-honeylemon`,**且能指出是哪一个文件**(diff 里列出新增文件名)。

## ②b memories 里出现本场内容(要那条管线真跑一次才谈得上)
⚠️ 实测事实:**memories 不是会话写的**,是另一条管线写的 ——
公共 home `stage1_outputs` 205 行、最新一条停在 **8/20 03:09**;infra-bot 跑了几个月 **0 行**。
⇒ ②b 的前提是那条管线在专属 home 上真的跑过一次;**没跑过就写「未触发」,⛔ 不写「失败」。**

## ③ 不继承(Lead 2026-08-22 06:25Z 更正后的版本)

🔴 **先更正一个前提**:原判据的担心是「它可能带着 infra-bot 的记忆」。**实测推翻了**:
```
~/.codex-infra-bot  memories stage1_outputs = 0 行   ← 空的
~/.codex-honeylemon 新建                = 空的
~/.codex(公共)     stage1_outputs = 205 行 + memories/MEMORY.md 633KB  ← 真正有东西的是这个
```
⇒ **它用错了家,但那个家是空的** ⇒ 没有 infra-bot 的记忆可带。
⚠️ 用错家是**卫生问题 + 将来会出问题**,⛔ **不是「现在正在被污染」**;
⛔ 也**不许**把「它没带 infra-bot 记忆」写成我们的功劳 —— 那儿本来就没东西。

### ③a 会话产物写进自己的家,另外两个家都不变
(与 ① 同一份证据)

### ③b 它答不出公共 home 独有的内容
⚠️ **只能证明「没读公共的」,证明不了「摆脱了 infra-bot」** —— 那本来就没什么可摆脱。
⚠️ **必须先做阳性对照**:同一问先用 `HL_CODEX_HOME=~/.codex` 跑一次;
**若连公共 home 都答不出来 ⇒ 这把尺子测不了这件事,结论只能写「测不了」,⛔ 不许写「③b 通过」。**

## ⛔ 通用纪律
· 任何一条没达标就照实写「没达标」;⛔ 不许用 ① 的绿灯替 ②③ 背书。
· 若用**复制来的凭据**跑,报告里必须写明「**这一版凭据不是隔离的**」——那句限定不许压掉。

---
## 实测结果(2026-08-22 06:26–06:29Z,文字探针,⛔ 没占房)
```
①  隔离      ✅ 两次 diff:公共=没变 · infra-bot=没变
②a 会话落位  ✅ 专属 home sessions +1,文件名:rollout-2026-08-21T23-28-06-01a02827-b5b6-7521-bd18-e97dabf34826.jsonl
②b 记忆管线  ⬜ 未触发(会话不写 memories;那条管线这一场没跑)
③a 同 ①      ✅
③b 不读公共  🚫 **测不了** —— 阳性对照失败:用【公共 home】跑同一问,它同样答「我的上下文里没有」
             ⇒ 说明 app-server 这种起法根本不注入 memories ⇒ 这把尺子分不出「读了」和「没读」
凭据          ⚠️ **这一版凭据不是隔离的**:三个 home 共用同一个账号的同一份 auth.json(founder 拍的)
```
