# 空转条件下的三臂对照 —— **跑之前写死的预期**(⛔ 跑完不许改)

写于:2026-08-23T21:0xZ(提交时间戳为准,见 git log)

## 为什么要空转
5 场实测:起会话 → 首问的间隔 24/34/38 秒 ⇒ **都读了议题**;510/798 秒 ⇒ **都没读**。
⇒ 没有能复现 bug 的基线,A/B 的绿**全是空过绿**。⇒ 三臂一律先空转 **800 秒**(对齐 an3 的 798s)。

## 三臂(⛔ 都在同样的空转条件下)
```
ctrl  什么都不改        起桥 → 空转 800s → 冷开场问议题   预期:**不读**(复现 bug)
A     开场把议题送进上下文  起桥(议题正文进 baseInstructions)→ 空转 800s → 问议题
B     开场先热身一次工具调用 起桥 → 立刻热身一问 → 空转 800s → 问议题
```

## 每一臂量两件(第二件才是真问题)
① 窄:它知不知道这次会议的议题(主题 + 三件事对不对)
② 宽:**这一场首问的命令数**(0 = 它没去查)

## 🔴 跑之前就写下的预期(Lead 提的,我认同并登记)
**如果变量真的是「过了多久」,那么 A 和 B 可能双双失效** —— 它们修的都是**开场那一刻**,而病发在 13 分钟之后。
⇒ 若 A、B 在空转条件下双双失败,**结论不是「两个都没用」**,而是:
**「在开场做事」这个方向本身不对** ⇒ 真正的方向会变成「在她开口那一刻做」或「让会话别老化」。
⛔ 那是下一轮,现在不设计。

## ⛔ 纪律
· 三臂同样空转,不许某一臂跳过。⛔ 不许把 800s 往下压。
· ctrl 复现不了 ⇒ **停下报 Lead**,⛔ 不换一个更容易过的判据。
· ⛔ 不查机制(为什么空转会这样)——先拿到「能稳定复现」,机制只有在修法需要时才查。
