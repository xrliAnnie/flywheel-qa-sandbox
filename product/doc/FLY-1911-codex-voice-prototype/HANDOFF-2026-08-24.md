# FLY-1911 交接 —— 2026-08-24(⭐ 这是最新一份;08-23 那份的「挡在最前面」已经不是最前面了)

> 读法:先读这一页。`HANDOFF-2026-08-23.md` 里除了下面「已经变了的结论」那一栏点名的,其余仍然有效。

## 这一场发生了什么(时刻一律 UTC)

```
01:12:55  桥 tad1 起来,在房里待命(WAIT-HUMAN)
01:20:02  她进房 ⇒ 建会话、喂议题、01:20:03 realtime started(v2)
01:20:05  它说了第一句 —— 之后七分钟里一共给出 43.1 秒的话
01:20:52  她:「我什么声音也听不到啊。」
01:26:08  它还在答(最后一条 outputAudio delta)
01:27:34  她走了
```
**⇒ 它一直在说,她一句没听见。**

### ✅ 这件已经结了 —— 成因在她那一侧,她自己解决的
```
01:26:20  她原话:「搞定了,是我这边设置的问题,现在可以听到了」
```
⇒ **成因 = 她那一侧的输出设置。她现在听得到。**
⛔ 下面所有关于「为什么听不到」的排查,读的时候要知道:**问题已经关了**,
   留着是因为里面那套方法(静音负对照 / 自证排除自己的声音 / join-order 判别)还有用,
   **不是因为它还开着。**
📌 那个「右键看 User Volume」的问法也随之作废 —— **不要再等她答。**

## 🔴 已经测掉的:桥确实把声音放进了房里

| | 量到的 |
|---|---|
| 没人说话时录 60.0 秒 | Opus 包 **2997** 个照常传 · 波形峰值 **0** · 非零样本 **0.000%** |
| 它回答时录 54.9 秒 | 峰值 **17465**(约满量程 53%)· 非静音 **10.73 秒** |
| 换算对得上 | Codex 交给桥 **10.95 秒** → 房里响 **10.73 秒**,差 **2% 以内** |

⭐ **上面那格空白才是硬度所在** —— 它证明这把尺子不会自己冒出波形来。
没有它,下面那格只是「录到了东西」,说明不了任何事。

证据:`evidence/AN1-result.json` · `AN1-room-speech-only.wav`(剪出的 13 秒)· `AN1-negative-control.jsonl` · `AN1-positive.jsonl` · 尺子 `AN1-measure.py`

## ⛔ 我【没有】测到的那一半 —— 不许在别处被读成已测

**她那七分钟本身没测过。** 那时房里没有第二只耳朵。
能说的只有:同一进程、同一份代码、其间无重启无 error,十分钟后测出来是通的。
⚠️ **这不等于「她那七分钟一定有声音出去」。**
📌 **但这个问句本身已经不需要答案了** —— 01:26:20 她自己解决,成因在她那一侧。
   留着这一格是因为它是一条诚实的成色标注,⛔ 不是因为它还等着谁去补测。

## 已排掉的候选(每条都有实测)

| 候选 | 结论 | 怎么排掉的 |
|---|---|---|
| Codex 没出声 | 死 | 她那段 110 条 outputAudio delta,合计 43.1 秒 |
| 它没听懂她 | 死 | transcript 200 条,逐字对 |
| 桥↔Discord 断了 | 死 | 纯静音时数据包照样 50 包/秒稳定传 |
| **她先进房、桥后进 ⇒ 听不到** | **死** | 专门测了这个顺序:先在房里的那个照样听得见后进来的(224 包,峰值 9455)。<br>⚠️ **边界**:我测的是 **bot 当听众**,不是她真实的 Discord 客户端 |
| 昨晚那条「会话起不来」 | **这一场没复现** | 01:20:03 正常建立;**但昨晚为什么会那样仍未查出** |

## ⏳ 还开着的

1. ~~她本地的每用户音量滑块~~ —— **已作废**:01:26:20 她自己解决了(成因在她那一侧的输出设置)。
   ⛔ 别再把它当开着的项往下传。
   📌 事后看:当时那个假说的方向是对的(在她那一侧),**但方向对不等于当时就该把它当结论** ——
      真正把它定下来的是她本人的一句话,不是我的推断。
2. **昨晚「语音起不来」的成因** —— 仍未知。这一场机器上只有 1 个 codex app-server,会话起来了;
   后来我又并发起了第 2 个 realtime 会话,也起来了。⚠️ **2 个不等于昨晚的 7 个,形状吻合 ≠ 证据。**

## 🔧 这一轮改了原型一处(**只加记录,没动任何行为**)

`prototype/bridge-hl.mjs` 每 30 秒记一次 `OUT-ACCOUNT`(播放器状态 / 漏帧数 / 已播放毫秒)。

**为什么非加不可** —— 这正是今天查不动的原因:
> 播放账目原来**只在整场收尾**(RUN_MIN 到点)写一次。她 01:27 走的时候,那一场的账要到 **03:12** 才写,
> 而且是整场累计、分不出她那一段。
> ⇒ **只在收尾记账的仪器,恰好在【被中途放弃的那种场次】上什么也留不下 —— 而那正是最需要复盘的场次。**

**已真跑验过**(不是「加了就算」):`~/.fly1911/acct1-bridge.out`
```
01:48:54  状态 playing · 漏掉的帧 0 · 已播放 29980ms
01:49:24  状态 playing · 漏掉的帧 0 · 已播放 60000ms
```
📌 顺带确认:`缓冲见底次数` 是近似值(30 秒里 80 次),**`播放器漏掉的帧` 才是真尺子(0)** —— 别拿前者判断断音。

## 🛑 founder_review round `70c23dd4` = **作废(superseded),不投**

**原因**:这张卡讲的是「她听不到声音」,而**她 01:26:20 已经自己解决并告知 Lead**:
「搞定了,是我这边设置的问题,现在可以听到了」。
⇒ 卡的第 4 节(右键看 User Volume)已经作废。
🔴 **而它已经送到她面前了**:开 `founder_review` 这个动作**本身就会自动投递** ——
   Bridge **01:45:27** 往 issue thread 发了一条 **@ 她**并带托管链接的消息。
   ⇒ **她确实被一张问已解决问题的卡打扰了一次**(Lead 随后在 thread 里让她忽略)。
   ⛔ 不许把这段读成「幸好没投出去」—— **投出去了。**
📌 通则:**任何会到达她的动作都算「叫她」**;「我只是开了个 round」不成立。

⛔ **HTML 不删** —— 里面那套「静音负对照 + 自证排除自己声音」的方法留在仓库当方法与证据记录。

### ⚠️ 但「撤掉」这个动作,系统里没有任何角色能执行 —— 这是机制缺口,不是没做

| 试过的路 | 结果 |
|---|---|
| Lead 从 gate 那侧撤 | 被拒:`founder_review is founder-bound` |
| 我从 runner 侧 `respond` | 同样被拒 —— `respond.ts:43` 对 founder_review 是**无条件** throw,**不分调用者**。<br>⇒ Lead 说「只有你或她能动它」这个推断**不成立**:我也不能 |
| 直接改库 | `mailbox` 表确实有 `superseded_at` / `superseded_by` 两列,<br>但全仓唯一写它们的 SQL 在**一个测试文件**里(`runner-stopped.test.ts:199`)——**没有生产写入路径** |

⇒ **实际处置**:我把它从自己的等待条件里摘掉了(不再轮询、不以它作为完成条件),
并在这里留下作废记录。**那一行在库里仍然是 pending,`superseded_at` 仍是 NULL** ——
⛔ 不许把这段读成「已经撤掉了」。

📌 **这跟老卡 `3d832c54` 是同一个缺口**(她说作废、系统里没人能执行)。**第二次撞上了。**

📌 Lead 认下的通则,记在这里:**任何会到达她的动作都算「叫她」** —— 开卡、发链接、开 gate、@她,
同一类。禁令用「任何会到达她的动作」写,不列举通道 —— **列举一定会漏,而漏掉的那条正好会被执行。**

## 🔴 产品化必须记的一条:founder 卡里永远放不了音频

托管页注入的 CSP 是 `default-src 'none'; script-src 'nonce-…'; style-src 'unsafe-inline'; img-src data:;`
—— **没有 `media-src`** ⇒ 内嵌 `<audio>` 会被**静默**拦掉(页面照样打开,只是点了没反应)。
⇒ 波形要用**内联 SVG**画;要给她听的音频,**只能由 Lead 在 Discord 里当附件发**。

## 会过期的结论(逐条给 as-of + 重核办法)

| 结论 | as-of | 怎么重核 |
|---|---|---|
| 桥能把声音放进房 | 2026-08-24 01:33 | 重跑 `evidence/AN1-ear-perspeaker.mjs`(先起耳朵再让它说话),看非静音秒数 |
| 托管页 CSP 无 media-src | 2026-08-24 01:45 | `curl <托管URL> \| grep -o 'content="default-src[^"]*"'` |
| 桥跑的二进制 = 0.148.0 | 08-23 | `grep -n 'releases/' prototype/bridge-hl.mjs`;PATH 上的是 0.149.0,两者**不是同一个** |
| 「会话起不来」未复现 | 2026-08-24 01:20 / 01:48 | 单次观察,**不是对照实验**;下次撞上先数机器上的 app-server |
| 那 43.1 秒 / 10.95 秒 | 这一场 | 从 `AN1-codex-audio-deltas.json` 的 base64 长度换算(×0.75 ÷2 ÷24000) |

## 📌 一个可复制的动作:把拒绝语里点名的角色,跟在场的人对一遍

今晚这条是从一次真事故里掉出来的,写下来是因为它**不需要警觉,只需要照做**。

```
Lead 被拒:  "founder_review is founder-bound; only the trusted founder writer may answer"
Lead 的读法: 「只有 founder 能动它 ⇒ 那就让 runner 去动」   ← 给报错编了一个解释
对一遍:      "trusted founder writer" 是谁?不是 Lead、也不是 runner
             ⇒ 这句话里点名的角色【不在场】⇒ 它根本没说「谁行」
去读码:      respond.ts:43 对 founder_review 是【无条件 throw,不分调用者】
             ⇒ 不是「只有 founder 行」,是【谁都不行】
```

⭐ **通则(Lead 入库的那半)**:**一条拒绝消息告诉你「你不行」,它没告诉你「谁行」。**
把前者读成后者,等于给报错编了一个自己想听的解释。

⭐ **可执行动作(这一半才是能传下去的)**:**看拒绝语里点名的角色,跟在场的人对一遍。**
对不上 ⇒ 那句话没有回答「谁行」⇒ 去读码,别去推。

📌 配套的一条(同一次事故的另一面):**「停手待命」管的是「别开新工作」,从来不管
「别改一句已经写错的话」。** 文档里的假话永远该改,并且**当场报这次偏离**。

## ⛔ 不要做的

- **不要在托管页上写 localStorage / 清存储** —— 所有报告共用一个域,会删掉她在别的报告上的草稿。
  卡的交互核验只在**逐字复刻 CSP 的本地副本**上做(`evidence/AN1-verify-card.mjs`,附两个必红的突变)。
- **不要在她没答之前,往她的房里放会录音的东西** —— 她定过「房里多一个会记录的东西,默认必须是关的」。
  我做的那只耳朵默认 `KEEP_AUDIO` 关(只留每秒音量,音频丢掉),**但放不放仍然由她说。**
- **不要重启 tad1 那座桥**(pid 87812,RUN_MIN=120,约 03:12Z 到点)—— 房是给她收着的。
- **改能工作的代码时,重构和行为变更不许同一次提交**(08-23 那次的教训,这次照做了:15 行纯新增,0 删除)。
