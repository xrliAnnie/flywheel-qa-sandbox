# v7 要求 — Annie 2026-08-11 19:27-19:29（Lead 落盘，防上下文丢失）

> 写这份文件的原因：这些要求原本只存在于一条 mailbox 消息里。runner 当时 ctx 94%，
> 自动摘要随时可能触发，而**只活在上下文窗口里的指令就是会丢的指令**。
> 若你是被摘要后的同一个 runner、或是接手的新 runner：**照这份文件做，不必回头找聊天记录。**

## 现状基线

- 最后完整版本：**v6**（commit `60396d3b`，已在 origin）
- 产物：`pilot/daily-brief.html`，生成器：`pilot/build-brief.mjs`（`node build-brief.mjs` 可复现）
- 数据：`pilot/x-harvest/batch-00{1,2,3}.json`（19 条，已推）+ `pilot/x-harvest/MANIFEST.md`（底数与自纠记录）
- 卡片数：GitHub 18 + X 6

## Annie 的三条（原话）

> 「每一个 feed 你都给我带一个 number 好了，比如 1、2、3…10 这样，我看的时候就比较清楚有几条。
> 另外，每一个 feed 底下都给我留一个可以写 comments 的地方，这样我就可以及时去留 comments，
> **也有地方可以 copy paste 给你**」

> 「X 那边，你每一条看起来都是把原推的前多少个 character 给我截过来。说句实话，这没什么意义。
> **你不如直接把原推给我做一个小总结，然后用中文把这个小总结放在上面**，这样才能看得清楚一点」

## 落地清单

### ① 每条 feed 带编号
两个 tab **各自从 1 开始**（GitHub 1-18 / X 1-6）。目的是她「一眼看出有几条」。

### ② 🔴 每条底下一个备注框（推翻上一轮的移除决定）
上一轮把整组反馈组件（chip + 备注框 + 收集按钮）都摘了，**这一条被 Annie 否了**。

- **每张卡各一个** textarea（不是全页一个）
- **必须能被选中复制** —— 她的用途原话是「有地方可以 copy paste 给你」
- ⚠️ **静态页刷新会丢**：用 `localStorage` 持久化（同源；内联脚本已带 nonce，CSP 放行）。
  **若确实做不到，必须在页面上明写「刷新会丢」** —— 不能让她写完一刷新白写。
- ⛔ **只加备注框。** 她先前砍掉的三个 chip（想再看看 / 开单往下挖 / 加进我们系统）**不要回来**。

### ③ X 卡片：中文小总结在最上面
- 不再截原推前 N 个字符（她：截断的英文没有信息量）
- 每条先给一段**你自己写的中文小总结**，讲清这条在说什么
- 「看原推」链接保留；原推**自带图**保留
- 🟡 Lead 判断（已标给 Annie 可否）：**原推正文留在下面但不再当主角**；若觉得吵可全去掉，但要标出来给她否

## Lead 加的一条（低优先，可砍）

「复制全部备注」按钮 —— 把她写过的备注拼成一段纯文本，方便整段贴回给 Lead。
**不贵才做；不得占用 ①②③ 的工。** 这条是 Lead 提的，不是 Annie 要的，她可以直接否。

## 边界（不变）

- 不开 PR 到 main、不碰 ship gate
- 做完交 Lead 核 + publish，**不要自己投**
- 底数照实；做不到就写做不到

## 已定但不在本轮的

- **100 条**：Annie 已明确「现在 19 条也是 OK 的，等正式 Daily 再跑 100 条」——**本轮不要再扩量**
- X 取回通路（分块取回是硬瓶颈）：可能单开一单，等她定
