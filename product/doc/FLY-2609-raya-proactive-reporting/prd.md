# FLY-2609 重新定义 Raya 主动汇报 — PRD v1

Issue: FLY-2609 (https://linear.app/geoforge3d/issue/FLY-2609/raya产品设计-重新定义主动汇报后台记录与给-annie-看的对话分开)
日期: 2026-09-15
基于: exploration.md、research.md、t1-inventory.md；founder 2026-09-15 两轮 founder_review 回话

---

## 0. 这份 PRD 的读法

### 0.1 成色三层，不许混

| 标记 | 含义 |
|---|---|
| ✅ | 她的原话 / 她圈选的 |
| 🔶 | Lead 或我定向的 |
| ⬜ | 无出处 / 未验 |

⛔ 不把 🔶 当成她说过的话。本文沿用 FLY-1846 §0.1a 的规矩。

### 0.2 🔴 更正记录（截至本版 **四条**）与由它们长出的判据（**不抹掉，因为它们曾被依赖过**）

**更正 ①（2026-09-15，她本人纠正）**

第 2 轮页面上写了「耳机模式（我没设计）」。她的回话：

> 「耳机模式（我没设计）**这个你确定没设计吗？我们之前是做过一个 PRD 的，跟你讨论过很多遍了。**我很确定我们设计过了。」

**她是对的。** FLY-1850 PRD **v1.0 可交付定稿** §5.1–5.4 早已把耳机侧汇报语义定完。
⇒ 正确表述：**耳机侧汇报语义已在 FLY-1850 定稿，本单不重开，只承接她 2026-09-15 新加的「更节制、简洁」。**
⚠️ 我上一轮其实已自查到 1850 定稿，但**她读到的那一屏上仍写着「我没设计」** —— 正文改对了、她读的那一屏还挂着已撤回的说法，等于没改。本条留档即为此。

**更正 ②（2026-09-16，Lead 指出后重核 → Tadashi 原话再纠正一次）**

我先前写「FLY-2074 的 progress 显示 implement 6/6 尚未收口，所以不能当已上线」。**这句话错了两层：**

1. 那个 progress 文件停在 2026-08-27，是**完成后没再更新的陈旧文件**，不构成「未收口」的证据；
2. 更要紧的是 —— Tadashi 原话：「2074 是 Raya 仓的实时语音流水线，**Linear Done 是对的**；体那句『未收口』指的是 **Raya 仓生产 checkout 没验到那个 SHA，是部署状态，不是代码没合**。」
   ⇒ **我把「部署状态」的证据拿去否定「代码已合」这件事。**

**⇒ 判据（本单最该被记住的一条）：**

```
「代码合了」/「部署到了」/「激活了」/「重启生效了」/「跑过一场」 —— 不许合并成一个「做了没有」
```

同一条判据在**工程交付侧**的对应形态（🔶 Tadashi 2026-09-16）：

```
「建单了」/「派出去了」/「跑起来了」/「合入了」/「生效了」 —— 也是五件事
```

⚠️ 2026-09-15 一晚因混淆这类区分**已经错了四次**（本 Runner 两次、Lead 两次）。这一条按次数计，是本单产出的最贵的一条判据。

**🔴 由它引出的第二条判据 —— 选一个不会因为时间流逝而变成谎话的说法。**

E-1 这一件在 **8 分钟内经过三个状态**（🔶 Lead 实录）：

```
05:35Z  「今晚就派了」      → 错（当时只是建了单）
05:41Z  更正为「排队待派」   → 当时对
05:43Z  派单闸自解，真派出去 → 「排队待派」当场过期
```

⇒ **交付那一刻的状态才算数。** 写给 founder 的产物必须选一个**时间流逝也不会变成谎话**的说法：
「已经交给工程开始做了」可以，「还在排队」不可以。这不是措辞洁癖 —— 产物的寿命比状态长。

**更正 ③（2026-09-16 06:34Z，Tadashi 更正他自己 05:33 的说法；Raya 在圆桌独立抓出同一处，两个来源对上）**

| | |
|---|---|
| **错** | 「要给**编排 bot** 语音房权限」（Lead 转述给我、也已对 Annie 说过） |
| **对** | **编排 bot 已被 founder 定稿废除**。要授权的是「**每个 Lead 自己的 bot**」在 General 语音房的 Connect / Speak（+ `VIEW_CHANNEL`）角色权限 |
| **另** | 装 launchd、写 `voice-host.json` **由 Tadashi 做**，不在她要点头的事里；**她只要「放行」那一件** |

**🔴 这一条本身就是本单要治的病的又一例。**

「编排 bot」是一个**已经被 founder 废除的旧概念**，却仍然活在工程侧的口头交接里，并且**差点通过我们的产物再递回她面前**。

这与 §1.3 的 R-1 压掉她 FLY-1846 §6.3 的圈选是**同一形状**：

```
旧的东西没有被显式下线，就会自己活下去
```

⇒ 这正是本单 §6「旧合同迁移」必须逐条显式处理、而不是「新规矩写好了旧的自然就没人用了」的理由。

**🔴 由它引出的自检条 —— 改了正文就回头扫计数词。**

每次新增一条更正记录、或改动任何「格数 / 条数 / 处数」，**回头扫一遍标题、§10 变更记录、以及交叉引用里的计数词**（「两处」「四格」这类）。

2026-09-16 实例：更正 ③ 落地后，§0.2 标题仍写「两处更正记录」、§10 仍写「§2.3 改为四格」。
**改了正文、没改标题和目录** —— 与更正 ① 的「正文改对了、她读的那一屏还挂着旧说法」是同一形状，也与「旧的东西没有被显式下线就会自己活下去」同源。**这是本单在自己的产物里犯的同一个病。**

**更正 ④（2026-09-16 06:44Z，Raya 本人直述；🔴 结论撞对了，推理方式仍然错）**

| | |
|---|---|
| **我最初的结论** | 「每轮开新 thread + archive」**做不到** —— 现在看是**对的** |
| **我最初的理由** | 「Flywheel 的三条 HTTP 路由要求绑 issue，所以做不到」—— **仍然是错的** |
| **真正的理由** | 「**Raya 自己的出站面里没有这个入口**」—— 只有她本人能回答 |

我中途还推翻过一次自己（见 §5.5 的四处平台先例），把结论改成「现成能力，不需要新造」——**那次推翻，推理方向是对的（不该拿本仓源码断另一个系统），但落点又错了（把「平台有」当成「她有」）。**

**🔴 为什么「结论相同」反而更危险：**

```
错的理由 + 对的结论 = 推理方式被保留下来
```

如果按我原来的理由走，下一次遇到「Flywheel 的 API 有某某限制」，我还会再推错一次另一个系统的能力。**撞对一次，会让错误的方法看起来被验证过。**

⇒ 因此三件事都要记：
1. Lead 当初要我去核 Raya 本人、而不是接受我的推断，**是对的**；
2. 我的结论最后成立，**也是对的**；
3. **但 1 和 2 是两件事** —— 结论正确不为推理方式背书。

📎 Raya 给了这个区分最好的措辞，原句引在 §5.5：**「平台别处有能力，不代表已接到我这里。」**

并且：**进度文件与 Linear 状态不一致时，不许只取一边下结论。**

---

## 1. 问题

### 1.1 触发事件（✅ 实测复现）

两轮积压的 summary 任务被连续处理，Raya 在 #raya 短时间内发出两段长 update，并在 FLY-2131 / FLY-2382 两个工程 thread 各追加一条。Annie 看不懂做了什么，也不需要全部后台处理细节占用注意力。

### 1.2 根因：不是 Raya 话多，是合同禁止她节制（✅ 源码实核）

| # | 机制 | 证据 |
|---|---|---|
| R-1 | Bridge 每轮注入硬指令：「**无论本轮有没有 review/吸收/追问活动，都要在 #raya 发一条汇报**，并**逐字包含**（不要改写、不要省略）：`本轮 N/M 份已交;未交:…`」 | `summary-absorption-rider.ts:395` |
| R-2 | Raya `IDENTITY.md` §Visible reporting 规定逐字模板，必须含时间 / review 数 / 吸收数 / 项目数 / 追问数 / PR 列表 / roundId | Raya 仓 `IDENTITY.md` |
| R-3 | 一个 pass 最多结算**当前 slot + 上一个 slot** 两轮，各自触发一条必发汇报 ⇒ 积压必然连发 | `summary-absorption-rider.ts:400` `for (const candidate of [slotStartMs, slotStartMs - cadenceMs])` |

### 1.3 🔴 最要紧的一条：R-1 覆盖了她本人 2026-08-17 的圈选（✅ 新发现）

**FLY-1846 PRD §6.3「到点没东西说」** —— 她当时圈的是 **A「可以跳过」**，理由写在 PRD 里：

> **信息不足还开口 = 干扰源**；**规定它到点必须说话，就会逼它凑。**

而 R-1 明令「无论有没有活动都要发」。

⇒ **「每轮必报」不是一条中性的工程默认值，它推翻了 founder 已经拍过的决定。** 本单第一顺位就是把它废掉。

📎 日期精确：**她 2026-08-17 21:46 PDT 圈的 A**（1846 §6.3 逐字记录），该决定随 **FLY-1846 PRD v1.7 于 2026-08-18 定稿**。两个日期指的是「她拍」和「定稿」两件事，不是矛盾。

同源的还有 Raya `IDENTITY.md` 自己写的「A truly empty round stays silent」—— 被 R-1 每轮现场注入的更硬措辞压掉了。**两份合同互相打架，硬的那份赢了，而硬的那份是错的。**

### 1.4 量化：她实际收到的内容构成（✅ 逐条可指回原句）

从今天三轮 `report.txt` 原文拆出 12 种成分。真正是「判断 / 新情况」的（B4 她自己的判断、B5 跨项目观察）**约占 15%**；其余 **85%** 是统计头、PR 号串、roundId、对账、缺交名单，以及**整段英文运维报错原文**：

> `REFUSED: no cross-department channel configured — "roundtable" is unavailable (set FLYWHEEL_LEAD_CROSS_DEPT_CHANNEL_IDS, or pin it via …)`

这就是她说的「不以 roundId、统计数和报错串取代内容」的实物。

---

## 2. 用户与既有资产

### 2.1 用户

**Annie（founder，唯一用户）**。Raya 是她的跨项目总管。

### 2.2 本单继承、不重开的既定资产

| 单 | 已定的东西 | 本单态度 |
|---|---|---|
| **FLY-1846** Raya CoS PRD v1.7 | §6.1 事件触发 + 兜底节奏（✅ 她圈 C）· §6.3 **没东西说可以跳过**（✅ 她圈 A）· §8.7.2 周期 = **6 小时且运行期可改**（✅ 她圈 a）· §8.7.3 每日 Report **是讨论的载体不是通知**、**存在它自己的仓库** · §8.8 open PR = 未读 / merge = 已读回执 · §10.5 沉默本身是一等信号 | **全部继承**；本单只做「文字侧怎么落地」 |
| **FLY-1850** 随身语音 PRD v1.0 定稿 | §5.1 触发点 = 进语音那一刻，积压一次性给 · §5.2 **进来全部 / 出声筛过** · §5.3 筛的标准靠她说一声长出来 · §5.4 **沉默必须被主动打破** | **不重开**；只承接「更节制、简洁」 |
| **FLY-1851** 会议模式 | 她自己给过同型模式：notes 发在 issue 自己的 thread、「**我大概率不用审**」「**你不需要等我一定要给那个 notes 做什么决定**」 | **引为先例**：这次的 thread 留痕是同一个心智模型 |
| **FLY-2597** | thread 标题「要你答」+ Epic 页「待你看」同源派生 + `founder_ask` 标记 | **复用**，不另造提醒引擎 |
| **FLY-2608** | thread 里的提问必须送达 Raya 并原地回复 | 实现归它，本单只定语义 |
| **FLY-2610** | 日常对话先直接回答、少术语 | **措辞风格归它** |

### 2.3 语音链路今天到哪一步 —— 五格分开（🔶 Tadashi 2026-09-16 原话，本节唯一可信输入）

她问「现在到底实现得怎么样了」。**一句话答：今天她戴上耳机还不能真用，卡在「主机已激活」这一格。**

| 格 | 状态 | 依据 |
|---|---|---|
| **① PRD 定稿** | ✅ | FLY-1850（8-24） |
| **② 代码合了** | ✅ | FLY-2446 通用 voice 进程（会议 + 随身）9-10 合入 **#1140** |
| **③ 主机已激活** | ❌ **卡在这格** | = FLY-2598。**实现已过**：主仓 PR **#1219** 头 `055dde558` CI 全绿 + 评审 APPROVED；插件仓 PR **#28** 也绿；**05:01Z 起在 QA** |
| **④ 重启生效** | ❌ | Tadashi：**生效需合入 + 重启，合入 ≠ 生效**。launchd 服务尚未安装，谈不上重启生效 |
| **⑤ 真跑过一场** | ❌ | 2598 体明确没开真实房间、没开首场会议，**首场验证在生效之后** |

**③ 之后的分工**（QA 过 → 她按卡合入 → ）：

| 谁做 | 做什么 |
|---|---|
| 工程（Tadashi） | 装 `com.flywheel.voice`（launchd）、写注册表 huddle 块 + `voice-host.json` |
| **她本人** | **只有一件：放行**「**每个 Lead 自己的 bot**」在 General 语音房的 Connect / Speak（+ `VIEW_CHANNEL`）角色权限 |

⚠️ **不是「编排 bot」** —— 见 §0.2 更正 ③。

⚠️ **成色**：本节全部来自 Lead 向 Tadashi 求证的转述（🔶）。我本机只独立读到一条佐证 —— `engineering/doc/milestones/FLY-2598.md`（2026-09-16）写着 **Status: Pending review and ship; production activation not executed**，正文明列「生产配置/安装、Bridge 鉴权阶梯、首场 General 会议、RG、平台余额与真实双连接健康均未执行」「本实现阶段不激活主机」，与 ③④⑤ 一致。

📎 **Raya 仓那套自己的部署路径：⬜ 未验证，不得写成生产可用。** `~/.flywheel/raya/deploy-receipt.json` 当前仍是 `schemaVersion: 1` / `outcome: rolled_back`；标准载体割接（FLY-2496）要等 **07:00Z 班车出 v2 回执、Tadashi 07:10Z 验**。在那之前如实写「未验证」，**不用它支撑任何结论**。


---

## 3. 目标

| # | 目标 |
|---|---|
| G1 | **文字模式下，Raya 不再主动汇报给 Annie**；后台照跑照记 |
| G2 | **进展保持 visible** —— 过程留在 thread 里，她不一定看，但发现问题时能及时参与、调整 |
| G3 | 积压的多轮**合成一次**，不变成连续打扰 |
| G4 | 统计数 / 内部编号 / 英文报错串**离开她的视线** |
| G5 | 旧合同的迁移**显式完成**，不假装已停用 |

## 4. 非目标（Non-goals）

- ⛔ 不改 6 小时周期（她原话「这部分没问题」；issue 亦明令不自行改 cadence）
- ⛔ 不预先简化成「每 X 分钟发一次」（issue 明令）
- ⛔ 不重开耳机模式汇报语义（FLY-1850 已定稿）
- ⛔ 不取消 FLY-162 的 issue-token 分流（她说分线程没问题）
- ⛔ 不动 summary 机制本身（Lead 写 PR、Raya merge 当已读）
- ⛔ 不修 thread 双向可用的实现（FLY-2608）
- ⛔ 不碰措辞风格（FLY-2610）
- ⛔ 不另造通用提醒引擎（复用 FLY-2597）

---

## 5. 需求

### 5.1 🔑 判据换轴：从「重不重要」换成「在哪个模式」（✅ 她的原话）

> **1. 日常代码与进度跟进**：每 6 个小时（或者更频繁的周期），她需要去看一下所有其他 lead 最近新做了什么，理解大家手头的工作，并把相对应的 PR 合并进去。**这部分没问题。**
> **2. 耳机/语音模式下的统管**：**只有在进入耳机模式时**，她才需要帮我去统管所有在做的事情，跟我汇报当前发生了什么、哪些事情需要我的 action。**如果没开耳机模式、只是在文字这边，她不需要去统管所有人的工作**，只要阶段性地了解一下大家的进展就可以了。

> 我并不需要它一直给我发这个汇报，**这个汇报只有在耳机模式下才需要发给我。如果在平常的文字区，它完全不需要发这些东西。**

**R1.1** 文字模式下，Raya **不得**主动向 Annie 发送轮次汇报。
**R1.2** 「后台跑完一轮」**不再是**开口事件。
**R1.3** 耳机模式下的统管播报按 FLY-1850 §5.1–5.4 执行，本单不另定。

### 5.2 三档归属 —— T1 表（✅ 她 2026-09-15 第 2 轮**零改动全部接受**）

三档定义：**后台账本** = 只落文件/DB，Discord 上不出现；**thread 留痕** = 写进当轮 thread，她不一定看但必须在；**耳机模式才报** = 文字区绝不主动发。

| 行 | 内容 | 归属 |
|---|---|---|
| A | 后台账本行（`round`/`review`/`absorption`/`merge_blocked`/`question`/`memory`/`report`/`issue_report` 等 10 类） | 后台账本 |
| B1 | 时间 + 统计头 | thread 留痕 |
| B2 | PR 号列表 | thread 留痕 |
| B3 | roundId | 后台账本 |
| B4 | **Raya 自己的判断** | thread 留痕 + 耳机模式才报 |
| B5 | 跨项目事实观察 | thread 留痕 |
| B6 | 对账无异常 | 后台账本 |
| B7 | 机制故障（merge 被拒 / push 失败） | thread 留痕 + 告警给工程 |
| B8 | 英文报错原文 | 后台账本 |
| B9 | 缺交名单 | thread 留痕（降为一行） |
| B10 | 队列深度 | thread 留痕 |
| B11 | 要问 Lead 的问题 | thread 留痕 |
| B12 | 「我记下了你的偏好」类确认话 | 后台账本 |
| C | 合了哪些 PR / 有没有去 ship | thread 留痕 |
| D | **要 Annie 本人决定的事** | thread 留着不 archive + 走 FLY-2597「要你答」；耳机模式当场报 |
| E | 每 6 小时发 #raya 主频道的那条汇总 | **取消** |

> 左列 16 行全部来自真实数据（Raya 账本 46 行 + 三条 `report.txt` 原文 + 4 条 `issue-route-receipts`），零编造。

### 5.3 thread 生命周期（✅ 她的原话，硬要求）

**R2.1** 每轮同步 + 合 PR 时，**开一个新 thread**，过程 update 写在里面。
**R2.2** **PR 合完就 archive 掉。**
**R2.3** 只有两种情况留着不 archive：**① 中间有问题要问她　② 她有问题要问 Raya。**

> ⚠️ **需要新接线**：平台有这个能力，但 **Raya 当前的出站面里没有 thread 创建/归档入口**（她本人 2026-09-16 06:44Z 确认）。见 §5.5。

### 5.4 thread 里要记的四类（✅ 她点名，逐条不改写）

| 她的原话 | 对应表行 |
|---|---|
| 现在有什么新的 report | B5 + B10 |
| 他有没有读懂发生了什么 | B4 |
| 有没有问题需要问那些 lead | B11 |
| 他有没有去 ship | C |

### 5.5 thread 的开与关：**平台有能力，Raya 没接到**（两级分开）

| 能力 | 真实要求 | 证据 |
|---|---|---|
| 开 thread | `channelId` + 根 `messageId` + bot token，`POST /channels/{id}/messages/{mid}/threads`。**与 Linear 单无关** | `RoundtableThreadManager.ts:649`、`AlertChannelHub.ts:79`、`flag-retirement-production.ts:466`；Raya 仓 `apps/brain/src/meeting.ts:1207` |
| archive | `archiveChatThread(threadId, botToken)` → `PATCH /channels/{threadId} {archived:true}`，**只吃 threadId** | `chat-thread-utils.ts:138` |
| 自动归档 | Discord 原生 `auto_archive_duration`，roundtable 已在用 | `RoundtableThreadManager.createThreadFromMessage(msg, archiveMinutes)` |

⇒ **平台级结论**：「绑单」只是 `/api/chat-threads/{create,send,archive}` **三条 HTTP 路由的入参要求，不是平台能力边界**；Flywheel 生产里已有至少三处不绑单的 thread。

⛔ **但这不等于 Raya 能做。** Raya 本人 2026-09-16 06:44Z 的原话：

> 我手头没有在 `#raya` 创建、归档非 Linear 线程的可调用入口；**平台别处有能力，不代表已接到我这里。**

| 层 | 结论 | 证据等级 |
|---|---|---|
| 平台有没有这个能力 | **有** | 源码实核（上表四处） |
| **Raya 接到了没有** | **没有** | **当事人直述**（她本人 06:44Z） |

⇒ **E-3 需要新接线**，不是「复用现成」。上表四处证明的是**平台有**，不是**她有** —— 这两级在本单里必须分开写。

### 5.6 积压合并（✅ 承接她「不应变成连续打扰」）

**R3.1** 一个 pass 结算多个 slot 时，**合成一次留痕**（「这两轮我看到的是……」），不背靠背产生两份。
**R3.2** 合并不得丢失后台账本的逐轮记录 —— 账本仍按轮记，合并只作用于**留痕的呈现**。

### 5.7 过渡期诚实条款

**R4.1** 在对应 PR 合入前，**Raya 仍按旧合同运行**。任何产物不得声称运行规则已停用。
**R4.2** 迁移完成前，`report.txt` 等既有落盘不变，便于回溯比对。

---

## 6. 旧合同迁移（issue 明令「显式处理」）

| 旧合同条款 | 出处 | 与今天反馈的关系 | 处置 |
|---|---|---|---|
| 「无论有没有活动都要发 + 逐字缺交名单」 | `summary-absorption-rider.ts:395` | **直接冲突，且推翻了 FLY-1846 §6.3 她本人的圈选** | **废** |
| `IDENTITY.md` 逐字汇报模板（统计数 + roundId + PR 串） | Raya 仓 | 直接冲突 | **重写**：改为写进当轮 thread，按 §5.2 归档 |
| `IDENTITY.md`「post a report in your own #raya channel」 | Raya 仓 | 直接冲突 | **改**：文字模式不发 #raya |
| `IDENTITY.md`「空轮静默」 | Raya 仓 | 被 R-1 压掉 | **恢复为有效**，与 FLY-1846 §6.3 一致 |
| FLY-1850 §5.2「**文字那边只报走偏的**」 | 1850 PRD v1.0 | **收紧，不是推翻** | 三段式迁移见下 |
| FLY-1846 §6.1「事件触发 + 兜底节奏、要主动打断」 | 1846 PRD v1.7 | **按模式分流** | 主动开口的那一半**移入耳机模式**；文字侧改为 thread 留痕 |
| FLY-162 issue-token 分流 | `cos-lead-rules.md` | 不冲突 | **保留**；只管住喂进去的内容 |
| Judgment 必填 | `summaries/README.md` | 不冲突 | **保留**，它是「有没有读懂」的原料 |
| 6h cadence | flag `summary_absorption_cadence_ms` / 1846 §8.7.2 | 她确认没问题 | **不动** |

### 6.1 FLY-1850 §5.2 的三段式迁移

1. **1850 原文**：「⇒ 耳机里的规矩和文字那边【不一样】：文字那边「**只报走偏的**」(A-PRD §10)，耳机里一开始全给。」
2. **她 2026-09-15 的新口径**：「如果在平常的文字区，**它完全不需要发这些东西**。」
3. **⇒ 本单收紧为**：文字侧从「只报走偏的」收紧为「**一条都不主动报，全部转为 thread 留痕**」。耳机侧 §5.1–5.4 **原样保留，不受影响**。

⚠️ **不假装 1850 §5.2 关于文字侧的那半句仍然成立。**

---

## 7. 成功指标（骨架，不编数字）

沿用 FLY-1846 §9.2 的规矩：**只记，不设数字目标。**

| # | 记什么 | 为什么是它 |
|---|---|---|
| M1 | 文字模式下 Raya 主动发给 Annie 的消息条数 | R1.1 的直接验证；期望趋近于 0（D 类「要你答」除外） |
| M2 | 每条真正到达她眼前的消息里，「判断 / 新情况」占比 | 今天是 ~15%，这是本单要翻转的那个数 |
| M3 | 她主动去翻 thread 的次数 + 翻完之后介入调整的次数 | G2 的验证：留痕有没有真的让她「发现问题时能及时参与」 |

⚠️ M3 是**她那句「希望进展保持 visible，这样如果发现问题，我就可以及时参与、调整」**的唯一可观测形态。若长期为 0，说明留痕没起作用，应回到 T2/T3 重议，**而不是加大推送**。

---

## 8. 未决问题（topic 树与当前位置）

```
FLY-2609
├── T1 分层：哪些只落后台 / 哪些留痕 / 哪些耳机模式才报   ✅ 已收敛（v1 §5.2，她零改动通过）
├── T2 节奏：后台周期与触达解耦；积压怎么合并             ⬜ 未开（§5.6 只给了原则）
├── T3 场所：#raya 是对话场还是面板；thread 开在哪、怎么命名、要不要绑单   ⬜ 未开
├── T4 形态：一条给她的消息 / 一条留痕具体长什么样        ⬜ 未开
└── T5 迁移：旧规矩怎么下线，下线前怎么办                 ⬜ 未开（§5.7 只给了诚实条款）
```

**当前位置：T1 收敛，T2 未开。**

### 8.1 明确挂起的未结项

| # | 未结的是什么 | 状态 |
|---|---|---|
| Q1 | **Raya 具体怎么把那三条轮报发进 `#raya`** | ✅ **已结**（她本人 2026-09-16 06:44Z 直述）：走**标准 Lead 出站** `lead_actions.discord_send → /api/lead-outbound/send`。**既不是 `chat-threads/send`，也不是 bot token 直发。** 我们此前的三种猜测全不对，见 §0.2 更正 ④。 |
| Q2 | 耳机模式「更节制、简洁」具体怎么落 | ⬜ 她说「之后再慢慢聊」，排后续轮次 |
| Q3 | 语音链路实现状态的权威答复 | 🔶 Lead 正在向 Tadashi / Raya 求证，其结果优先于 §2.3 |
| Q4 | thread 开在哪个频道、怎么命名、是否绑单 | ⬜ 归 T3 |

---

## 9. 交给工程的效果清单（**只写要什么效果，不写怎么改**）

> ⚠️ **本节是效果规格，不是实施方案。** 修法、落点、拆单一律归工程（Tadashi）。
> 本单**不改任何代码、不自行 create-issue**。PM 验收 = 未来 FLY-830，现在不做。

### E-1 · 停止「每轮必发」，恢复「没东西说可以跳过」　🔴 第一顺位

| | |
|---|---|
| **要什么效果** | 文字模式下，一轮跑完**不再自动产生**一条发给 Annie 的消息。没有可说的内容时，Raya **可以不说话**，且这不算失职。 |
| **为什么第一顺位** | 现行的「无论有没有活动都要发」**推翻了 founder 已拍板、并随 FLY-1846 PRD v1.7 于 2026-08-18 定稿的决定**（FLY-1846 §6.3 她圈 A「可以跳过」）。这不是新需求，是**恢复被覆盖掉的既定决定**。 |
| **验收** | 连续若干轮无实质内容时，Annie 侧零新消息；后台账本仍逐轮完整。 |
| **PRD 依据** | §1.3 / §5.1 R1.1–R1.2 / §6 |

### E-2 · 统计头、缺交名单、内部编号、英文报错离开她的视线

| | |
|---|---|
| **要什么效果** | 按 §5.2 的 16 行归属落位：归「后台账本」的永不出现在 Discord；归「thread 留痕」的只进当轮 thread；**「每 6 小时发 #raya 主频道的那条汇总」取消**。 |
| **验收** | 抽查任一轮：Annie 可见面上没有 roundId、没有 `N/M 份已交`、没有英文报错原文。 |
| **PRD 依据** | §5.2 |

### E-3 · 每轮的过程留在一个 thread 里，合完就归档

| | |
|---|---|
| **要什么效果** | 每轮同步 + 合 PR 时开一个新 thread，过程写在里面；PR 合完就 archive；**只有**「有问题要问她」或「她有问题要问 Raya」时留着。thread 里记 §5.4 的四类。 |
| **验收** | 她随时点进去，能看到这一轮**有什么新 report / 有没有读懂 / 有没有要问 Lead 的 / 有没有去 ship**；已完成的轮次不堆在列表里。 |
| **前置** | ⚠️ **已知需要新接线**：Raya 当前出站面不含 thread 创建/归档入口（她本人 2026-09-16 06:44Z 确认）。频道 / 命名 / 是否绑单仍待 **T3 收敛**。 |
| **PRD 依据** | §5.3 / §5.4 / §5.5 |

### E-4 · 积压不变成连续打扰

| | |
|---|---|
| **要什么效果** | 一次处理多轮积压时，Annie 侧**合成一次**，不背靠背出现两份；后台账本仍逐轮分开记。 |
| **验收** | 构造两轮积压，Annie 可见面上只出现一次。 |
| **PRD 依据** | §5.6 |

### E-5 · 「要她决定的事」走已有的机器标记，不靠聊天刷存在

| | |
|---|---|
| **要什么效果** | §5.2 的 D 类走 FLY-2597 的「要你答」机制，对应 thread 不 archive；耳机模式下当场报。 |
| **验收** | 她扫 thread 标题就能认出哪些在等她；回帖后自动熄灭。 |
| **依赖** | FLY-2597 合入 |
| **PRD 依据** | §5.2 D 行 |

### E-0 · 过渡期诚实条款（贯穿以上全部）

在对应改动合入前，**Raya 仍按旧合同运行**；任何产物、任何汇报都不得声称运行规则已经停用。见 §5.7。

## 10. 本版改了什么

v1 = 首版。相对于两轮 founder_review 材料的增量：

1. 轴从「判断重不重要」换成「在哪个模式」（她第 1 轮回话），方案 A 的开口条件作废；
2. T1 表 16 行定稿（她第 2 轮零改动通过）；
3. **新发现并写入 §1.3**：「每轮必报」推翻了 FLY-1846 §6.3 她 2026-08-17 圈选的「可以跳过」；
4. 读完 FLY-1846 后确认：**1846 覆盖了开口时机 / 周期 / 每日 Report / summary 回流 / 沉默信号，但没有覆盖 thread 生命周期**；thread 留痕的**心智模型**先例在 FLY-1851（meeting notes 发 thread、「我大概率不用审」）；
5. 更正记录留档（§0.2）；截至本版共 **四条**（①耳机模式「我没设计」②FLY-2074 推理 ③编排 bot ④thread 能力：结论对、理由错）；
6. 语音链路状态按「PRD 定稿 / 代码 Done / 主机已激活」三分，§2.3。
7. §2.3 改为按阶段分格（**当时四格**：代码合了 / 部署到了 / 激活了 / 跑过一场；**后于第 9 条扩为五格**），输入换成 Tadashi 2026-09-16 的原话；更正 ② 同步扩写为两层错误，并记下「阶段不许合并」这条判据。
8. §9 由「Build issues 提案」改写为**效果清单 E-0 ~ E-5**：只写要什么效果与验收，修法/落点/拆单归工程；E-1 标为第一顺位并写明它是「恢复被覆盖的既定决定」，E-3 标明 T3 收敛前不得开工。
9. §2.3 由四格扩为**五格**（补「重启生效」，Tadashi：合入 ≠ 生效）；§0.2 判据同步补上工程交付侧的五件事形态，并记下「一晚错四次」的代价。
10. §0.2 补第二条判据：**选一个不会因时间流逝而变成谎话的说法**（E-1 在 8 分钟内经过建单/排队/已派三态的实录）。
11. §2.3 ③ 那格改为「工程做 / 她只放行一件」的分工表（更正「编排 bot」→「每个 Lead 自己的 bot」）；§0.2 补更正 ③ 并点出它与 R-1 同形：**旧的东西没有被显式下线，就会自己活下去**。
12. 修两处文档自相矛盾（§0.2 标题「两处」→「三条」、§10 第 7 条「四格」补注后扩为五格），并把**「改了正文就回头扫计数词」**立为自检条。
13. §8.1 Q1 由「未结」改为 **已结**（Raya 本人 06:44Z 直述：走 `lead_actions.discord_send → /api/lead-outbound/send`）；§5.5 拆成**平台有能力 / Raya 没接到**两级，E-3 前置改为「已知需要新接线」；§0.2 补更正 ④ 并记下**「结论撞对了不等于推理方式可以留着」**。
