# Plan: 从这里接着做 — 收敛成 PRD、交给 Tadashi

Issue: FLY-1826 (https://linear.app/geoforge3d/issue/FLY-1826)
日期: 2026-08-17
基于: `exploration.md`(为什么做、方向变过两次)· `research.md`(方法与会过期的结论)· `findings.md`(证据总账)
Status: **PRD 已定稿(Annie 2026-08-17 20:34 授权,边界见 `prd.md` §0)· PR 待开 · 交 Tadashi 的时机由 HL 定**

> **判据(Annie 定的)**:这一单被搁置三个月,另一个人只读这个文件夹,能不能接着做下去。
> 所以本文第一节就是「如果你是接手的人,从这里开始读」。

---

## 0. 接手人:从这里开始

**一句话现状**:审计做完(四轮)· 形态已校准 · 9 张单已建 · **`prd.md` 已定稿**。
Annie 2026-08-17 20:34 逐字裁了两格:**FLY-1582「改判,修」** + **PRD「可以定稿」**。
**剩下的只有:开 PR、以及等 HL 定交 Tadashi 的时机。**

> ⚠️ **先读 `prd.md` §0「定稿授权的边界」** —— 她批准的那一版**不含 Windows 重排**(平台重排是执行她的口头指令、尚未经她逐条 review)。
> 基于「批次 0 第一顺位是 Windows」做任何不可逆动作前,应再确认一次。

**先做这四件事,顺序不能换**:

1. **核 `research.md` §3 那张「会过期」表** —— 本单大量数字是 as-of 值,行号必然漂移。**不核就引用会出事。**
2. **读 `prd.md` §0 授权边界** —— 知道她批的是哪一版、哪一部分还没经她 review。
3. **查 10 张单状态**(FLY-1835…1842 · FLY-1848 · FLY-1582 已恢复 Backlog)。
4. **读 `exploration.md` §4** —— 方向被改过两次,不读会看不懂产物为什么长这样。

**最终交付物是一份 PRD,不是这份盘点。** 盘点是它的证据底座。

---

## 1. 当前状态(逐项,可核)

| 项 | 状态 | 怎么核 |
|---|---|---|
| **PRD** | **已写完**,含 Windows + 外人接入验收 | `prd.md` |
| **FLY-1582** | ✅ 她已裁「改判,修」;HL 已恢复到 Backlog | `get_issue FLY-1582` |
| 形态校准 | ✅ 已完成(她过了一句话形态 + 8 站 + 档位 A+尽量到B) | `findings.md` 第 3/4 轮段 |
| 已发布的页 | 形态页 5036px · 工程页 2966px · 答疑页 2939px · PRD 页 2269px | 各自 html |
| 9 张执行单 | 已建、**未派工**、全 Backlog | FLY-1835…1842 + FLY-1848 |
| 现有 issue | **盘点 Runner 一张都没碰**(只提议);HL 已关 FLY-652 并留承接链接 | 建议清单在 FLY-1842 |
| 分支 | `flywheel-FLY-1826`,已推,树干净 | `git status` |
| PR | **待开**(定稿授权已拿到);**开完先别交 Tadashi**,时机由 HL 定 | — |

**founder_review 是后浪顶前浪,前面几轮都被后一轮 superseded。** 她实际答过的:
形态轮(`3ecbc3bd`)· 答疑轮(`8457dba4`)· PRD 轮(`49dbd07a`,只回了「ok what's next」)。
**内容没有丢** —— 每轮的实质都在 `findings.md` 里。

---

## 2. 她回复之后:怎么收敛成 PRD

**这一节的原始任务(把形态收敛成 PRD)已经做完了** —— `prd.md` 在同文件夹,范围逐条对应她的裁决。
她已定的全部记录在 `findings.md` 第 3/4 轮段 + `prd.md` §3/§4/§6,**不用再问一遍**。

**剩下的只有一件**:她裁完 FLY-1582 之后 ——
1. 把结果写进 `prd.md` §10 open-1(裁「修」→ 它进批次 0 并从 open 里移出;裁「不修」→ R10 陪走的前提改成手工装一次给他);
2. 出**最终一轮** founder_review(页面必须带 §5-4b 那句「纯通过别点复制按钮」);
3. 通过后开 PR(base `main`),`complete --route needs_review`。

**PR 先不交 Tadashi** —— 等 FLY-559 那条也裁完,免得依赖图改两次(HL 定)。

## 3. 执行清单:8 张已建单 + 复用/关闭建议

### 3.1 已建(未派工,dispatch 归 HL 和 Tadashi)

| 编号 | 内容 | 标签 | 优先级 |
|---|---|---|---|
| FLY-1835 | 打包闭包门(治 N-1…N-6 **根因**) | Flywheel | High |
| FLY-1836 | `setup-new-project.sh` 缺失 → **安装第 2 步就死** | Flywheel | **Urgent** |
| FLY-1837 | Discord 插件客户机形态 → Lead 起不来 | Flywheel | High |
| FLY-1838 | v2 载体 FATAL 打死「早聊一句」live 路径 | Flywheel | Medium |
| FLY-1839 | macOS 自动安置只打印不执行 | Flywheel | Medium |
| FLY-1840 | launcher 经 tmux 参数传钥匙(碰生产 fleet,要字节兼容纪律) | Flywheel | Medium |
| FLY-1841 | 客户面文档只有 18 行 + Discord 4 步素材 | Flywheel-Product | Medium |
| FLY-1842 | 台账收口(9 行建议清单,**执行由 HL/Annie**) | Flywheel-Product | Medium |

### 3.2 依赖 —— 事实,不是判断

- `FLY-1836 / FLY-1837 / FLY-1582` → `FLY-1322`(真机陪走):三条任一不解,陪走走不完。
- `FLY-1582` → `FLY-1143`:1143 的三块主体都建在那条流水线上。
- `FLY-1838 + FLY-1840` → 「早聊一句」可用:**两个独立原因,只解一个不够**。

### 3.3 批次 —— 建议,可推翻

> ⚠️ **2026-08-17 改过一次** —— Annie 确认**老公用 Windows**,平台进了批次 0 第一顺位,macOS 那条移出关键路径。

`批次 0` **FLY-649(Windows/WSL2,第一顺位)** · 1836 · 1837 · 1835 · **1582(✅ 她已裁「修」)** →
`批次 1` 1322 真机陪走(**在 Windows 上**)→
`批次 2` 1838 · 1840 · 1841(+ 改那句空承诺文案)→
`批次 3` 1143。`1842` 随时。**1839(macOS 自动安置)移出关键路径 —— 老公不用 Mac。**

四处是判断不是依赖:1835 放批次 0 · 陪走排前 · 1143 靠后 · **649 排在 1836/1837 之前**(它决定那两条的验收在哪台机器上做)。
逐条依据见 `prd.md` §8/§9。

### 3.4 一件必须让接手人知道的事

**FLY-1582(发版流水线连红)不是没人发现 —— 2026-08-01 有人建单写得很完整,2026-08-14 backlog 大扫除时 Annie 裁「这个也暂时不用修,关掉吧」。**
本单补上了当时那张单没给她的两条上下文:① 这条流水线是客户拿到软件的唯一通道;② 客户通道从头到尾没开过。
**要不要改判是她的决定。** 接手人不要自行重开。

## 4. 查不了的四件(交 Annie + Tadashi)

| # | 要查什么 | 卡在哪一类 | 谁 |
|---|---|---|---|
| 1 | 发布台账实际内容 —— **发版失败根因十有八九在这** | 缺权限(ops-admin token) | Annie + Tadashi |
| 2 | customer-release 指针是不是 null | 缺权限(license key) | Annie |
| 3 | N-1…N-6 真机确认 | 缺真机 + 有干扰生产 fleet 风险 | FLY-1322 |
| 4 | 本地还能不能打出 payload | 不该在生产机上跑重型构建 | Tadashi,一条命令 |

## 5. 给接手人的操作纪律(踩过的坑,别重踩)

1. **不要在 Annie 不在线时迭代轮次。** founder_review 是后浪顶前浪,开新一轮会把上一轮从她视野里顶掉。
   本单一夜开了 5 轮,第 4 轮她没看就被顶掉了。**只有旧卡本身坏了(她读不了/回不了)才够格换卡,「再打磨一下」不够格。**
2. **不要用 gate 的 `not yet` 证明「她没看」** —— 它只证明她没提交 verdict,证明不了她没在读那个页面。
3. **现有 issue 只提议不执行**(关闭不可逆;FLY-648 就是关错的代价)。建单可以,**派工不行**。
4. **founder HTML 交付前量高度**(≤6000px)+ **验复制的失败路径**(必须同时打断 clipboard 和 execCommand)。
4b. 🔴 **`passed:false` 不等于她否决** —— 该字段是**整串精确相等**判定(都可以了/可以了/通过/LGTM/approved),
   而卡面教的「一键汇总复制」输出**永远带前缀**,所以**她照做就必然判否**(bug 单 **FLY-1847**)。
   **永远读 `feedback` 原文再判意图。**
   并且**下一版页面必须加一句**:「如果你就是通过、没有别的意见,**别点复制按钮**,直接回『都可以了』四个字;
   有意见才用复制按钮。」—— 把常见的纯通过从必然假否转成正确通过。**不要改复制按钮输出去迎合精确匹配**(会让带意见的回复被误判成通过)。
5. **给 Lead 发消息一律 file→var**(正文先 Write 到文件再 `"$(cat file)"`),发完读回 CommDB 核残渣。
6. **凡是要写「证明 / 所以 X」的断言句,先问手上那个量到底是不是 X。**

## 6. 完成判据(本单什么时候算做完)

1. Annie 裁完 FLY-1582,且最后一轮意见被收敛(**注意 §5-4b:别拿 `passed` 字段当意图**);
2. `prd.md` 落在本文件夹 —— **已完成**,内容 = 形态页的工程版 + 逐条验收,档位 = 她圈的 A+尽量到B;
3. PRD 与本文件夹的过程文档一起进 PR,base `main`;
4. **交给 Tadashi 的时机由 Annie/HL 定** —— 她说的是「等他把手上 Feature Flag 那部分全部做完」。

**本单不负责实现,也不负责 PM 验收**(PM 验收 = 未来 FLY-830,现在不做)。
