# QA Report — FLY-188 (PR #211) claude-in-chrome 真 E2E

**Tester**: qa-fly-188
**Date**: 2026-06-01
**Branch**: `feat/v1.30.2-FLY-188-screenshot-persistence` @ `b9ed2c3` (worker gif-first fix)
**Tool**: claude-in-chrome MCP (Annie 日常 Chrome 扩展，全程隔离新 tab，Annie 现有 tab 全程未碰)
**Verdict**: ✅ **PASS**

## 背景

GEO-386: agent 用 claude-in-chrome `computer` 截图，即使 `save_to_disk:true` 也只回 imageId、不落 agent 本地盘 → 无法 commit 当 QA 证据。
PR #211 (worker `b9ed2c3` 翻案后): **主路 = claude-in-chrome `gif_creator` export download**（原生唯一落盘出口，founder 可在自己 Chrome 实时看）；agent-browser/ProofShot 降级 fallback（非交互 Runner 自检流 / 需真彩 PNG/video）。

## Test 1 — 复现 GEO-386 边界 ✅ PASS

- 隔离新 tab → `https://example.com` → `computer` screenshot **`save_to_disk:true`**。
- 返回：**仅 `ID: ss_10146r1va`**（in-extension imageId），**无 agent-disk 文件路径**。
- 文件系统实证：`find` ~/Downloads /tmp $PWD 近 3 分钟 — **零** .png/.jpeg 产出；无以 imageId 命名的文件。
- 结论：截图 inline-only（存扩展内存），不落盘、不可 commit → **坐实 GEO-386，fix/hook/doc 前提成立**。

## Test 2 — gif download 主路（产品命门）✅ PASS

- `gif_creator` start_recording → navigate(`?step=2`) + scroll down/up + navigate(`example.org`) → stop = **4 frames**（实证 worker 发现：帧来自 state-changing 动作，非 bare screenshot）。
- `export download:true filename=FLY-188-e2e-claude-in-chrome-20260601-231917.gif` → 落 `~/Downloads`，99KB。
- `file` 校验：**`GIF image data, version 89a, 1095 x 837`** — 真 GIF。
- `mv` 进 repo `doc/qa/reports/FLY-188/` → `git add` 成功（status `A`，可 commit）→ 随后 unstage（QA 不 commit，留证据在 worktree）。
- 结论：**claude-in-chrome 原生 gif download 主路端到端真能用** —— 不换工具、founder 可现场看、产出可 commit 证据。正是 Annie 要的"claude-in-chrome 真 E2E"。

**证据**: `doc/qa/reports/FLY-188/FLY-188-e2e-claude-in-chrome-20260601-231917.gif`

## Test 3 — 新 hook 文案 gif-first ✅ PASS

- `EdgeWorker.ts` 截图 PostToolUse hook：`b9ed2c3` 后 **MAIN PATH = gif_creator**（start_recording first → navigate/run steps → stop → export download:true → mv + git add），明确写入"帧来自 navigate/click/scroll 动作，非 bare screenshot，光截图=0 帧"；visual-capture 降为 FALLBACK。
- 单测 `test/EdgeWorker.screenshot-upload-hooks.test.ts`：**11/11 PASS**（含 gif-first 顺序 + frames-from-actions caveat 断言）。

## 安全合规

- 全程仅操作自建隔离 tab group（886233386，已 auto-removed）；Annie 现有 tab（Discord/Design Studio/cmux）**全程未碰**。
- 仅 example.com / example.org 无害静态页；无登录/敏感页/表单；未触发 alert/confirm。
- gif 下载经 Annie 预先批准（自产无害 GIF）。
- 未 ship / 未 merge。

## 建议

SHIP-ready（从 QA 角度）。worker 已 build CI 绿；doc(qa-context.md gif-first) + hook 文案 + 单测三者一致。
