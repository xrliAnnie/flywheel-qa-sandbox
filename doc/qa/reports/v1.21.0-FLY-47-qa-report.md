# QA Report — v1.21.0 (FLY-47 + FLY-62)

**Tested SHA:** c3f2d0f
**Agent ID:** qa-fly-47
**Change types:** [feature, infrastructure]
**Date:** 2026-04-05

## Ad Hoc Feature Verification (Step 4)

### Phase 1: CLI + CommDB E2E — 13/13 PASS

| Test | Category | Description | Result |
|------|----------|-------------|--------|
| E01 | Lifecycle | Gate + respond = gate exits 0 | PASS |
| E02 | Schema | checkpoint column + 4 new columns | PASS |
| E03 | Content-ref | >2KB message → file ref | PASS |
| E04 | Pending | --json includes checkpoint, content_type | PASS |
| E04b | Content-ref | ≤2KB → no file ref | PASS |
| E05 | Timeout | Brainstorm timeout → exit 1 (fail-close, ~5s) | PASS |
| E06 | Timeout | Question timeout → exit 0 (fail-open, ~5s) | PASS |
| E07 | Resolve | resolved_at + read_at set after respond | PASS |
| E08 | Compat | Traditional ask/respond backward compat | PASS |
| E09 | Stage | Stage approve accepted | PASS |
| E16 | Error | Invalid DB path → non-zero exit | PASS |

### Phase 2: Bridge + GatePoller + Discord E2E — PASS

Full system chain verified with real Discord bot:

1. **Bridge startup** (port 9877, Peter bot, 3 lead runtimes) — OK
2. **Session creation** via `/events` API — StateStore `running`, session_count=1
3. **Gate command** `flywheel-comm gate brainstorm` — CommDB question with checkpoint
4. **GatePoller detection** within 3s poll cycle — `delivered=true`
5. **Discord verification** via Chrome MCP — #product-lead-control shows `gate_question` event with correct [BRAINSTORM] checkpoint, issue FLY-73, full message content
6. **Respond + gate unblock** — `flywheel-comm respond` → gate exits 0 → CommDB resolved_at + read_at set

### Phase 3: Worker Bug-Fix Verification (SHA 0c2d49f) — 3/3 code fixes PASS, 1 new bug found

| Fix | Description | Result |
|-----|-------------|--------|
| P0 Bridge bot identity | CLAUDEBOT_TOKEN for control+chat delivery (avoid self-filtering) | PASS |
| Gate → chat relay | `postToChatChannel()` sends gate questions to Lead chat channel | PASS |
| Forum post title fallback | Title from `issueTitle` field with fallback to empty string | PASS (code correct) |

**Verification method**: Chrome MCP on `#geoforge3d-product-chat` confirmed 4 gate question messages from ClaudeBot (not Peter bot). Discord REST API correctly bypasses plugin self-filtering.

**New bug found**: GatePoller chat dedup — `postToChatChannel()` called unconditionally in `relayToLead()`, causing chat message spam when control channel delivery fails. See qa-context.md for details.

## Product Bugs Found

| # | Bug | Root Cause | Fix | Status |
|---|-----|-----------|-----|--------|
| 1 | gate --timeout unit mismatch | CLI multiplied --timeout by 1000 (ms→ms×1000). `--timeout 5000` became 83 min | c3f2d0f: remove ×1000, poll sleeps min(interval, remaining) | Fixed |
| 2 | P0: Bridge→Lead relay broken | Discord plugin filters all bot messages (self + others) when `allowBots` not configured | 0c2d49f: Bridge uses CLAUDEBOT_TOKEN instead of per-lead token | Fixed |
| 3 | GatePoller chat dedup | `postToChatChannel()` outside dedup gate, spams chat on control failure | Reported to worker | Open |

## Skill File Updates (Step 5 Part A)

SKIPPED — no existing QA skill files for comm/bridge/edge/core domains.

## Regression Check (Step 5 Part B)

SKIPPED — no existing regression suites for affected domains.

## Overall: CONDITIONAL PASS

Phase 1+2 fully pass. Worker fixes verified (3/3 code correct). One new dedup bug found (severity: medium — causes Discord spam but doesn't block core functionality). Pending worker fix for dedup before final PASS.
