# QA Report: FLY-259 ③ Codex Lead 真交互终端 (daemon-WS TUI runtime)

**Issue**: FLY-278 (Mufasa TUI cutover execution — FLY-259 后续) — stage ① QA-only
**Verifies**: FLY-259 v1.44.0 (PR #265, merged to main)
**Plan / Handoff**: `doc/qa/test-plans/FLY-259-codex-tui-qa-handoff.md` (S1–S10) + plan §9
**Runbook (cutover, NOT in scope here)**: `doc/engineer/implementation/FLY-259-mufasa-tui-cutover-runbook.md`
**Date**: 2026-06-16
**Verifier**: runner-d0ea9175 (独立验证方 — 非 FLY-259 实现方 worker-fly-242)
**Status**: ✅ FINAL PASS with one honest gap — **10/11 scenarios live-verified**; **S5 (bridge exactly-once) NOT live-verified** (unit-test covered + bridge path NOT used by this cutover; Eng Lead accepted — see §3)
**Raw evidence**: `~/qa-fly259-evidence/` (logs / journal snapshots / Discord screenshots)

> **Scope (Tadashi 确认 + lead-instruction 551553aa SCOPE LOCK)**: 本 issue = stage ① **QA-only**。产物 = 本报告 + 可复现的隔离 QA launcher。**停在 docs PR,不 merge**。生产 cutover(停 headless / 起 TUI / 改 projects.json)= stage ②,Annie 在场 + 明确 ship GO 才做,**不在本 issue 范围,本轮零接触生产**。

---

## 0. 隔离环境(red lines:零接触生产 Mufasa / Annie 活线)

| 轴 | QA 值 | 生产值(零接触,已验全程存活) |
|---|---|---|
| CODEX_HOME | `~/.codex-259-qa`(school 账号,独立 daemon socket) | `~/.codex-mufasa`(Mufasa)/ `~/.codex-242`(Annie demo) |
| lead-id | `mufasa-tui-qa`(+ S6 的 `…-s6a`/`…-s6b`)→ 独立 hex state dir | `mufasa-lead` |
| Discord 腿 | slot-3 #ops-lead-test, `TEST_BOT_TOKEN_3`(bot 1493075160025272452) | #mufasa, `MUFASA_BOT_TOKEN` |
| daemon socket | `~/.codex-259-qa/app-server-control/app-server-control.sock` | (Mufasa 独立 socket) |
| cross-dept | **unset**(QA 不碰 #leads-roundtable) | `FLYWHEEL_LEAD_CROSS_DEPT_CHANNEL_IDS` |

**生产存活验证(QA 全程末尾)**:prod Mufasa headless **PID 792 = alive**;prod Bridge **9876 = 200**;`~/.codex-mufasa` / `~/.codex-242` **零接触**(daemon-kill 只打 qa-home socket)。

**复现入口(本 PR 提交)**:`packages/teamlead/scripts/qa-fly259-mufasa-tui-slot.sh`
- 指向 **main repo** 的 merged FLY-259 dist(`FLY259_QA_REPO`,默认 `/Users/xiaorongli/Dev/flywheel`)→ QA 验的是 ship 进 main 的真代码。
- **fail-closed 守卫**:CODEX_HOME 命中 `.codex-mufasa`/`.codex-242` → 拒;QA token == `MUFASA_BOT_TOKEN` → 拒;只抽 `TEST_BOT_TOKEN_3` 一个变量(不 `source` 整个 `.env`,避免漏入生产 cross-dept/bridge 配置)。
- knobs:`FLYWHEEL_CODEX_LEAD_MODE`(tui|headless,SP-2 用)、`QA_LEAD_ID`(S6 用独立 id)、`FLYWHEEL_CODEX_LEAD_OUTBOUND`(direct|bridge,S5)、`FLYWHEEL_LEAD_DRY_RUN=1`(零副作用)。

---

## 1. 结果总览

| # | 场景 | 结果 | 关键证据 |
|---|------|------|------|
| S0 | bring-up(daemon-WS + TUI 窗 + thread resume + persona) | ✅ PASS | `S0-bringup.log` |
| S1 | daemon kill → 重建(generation-fenced)+ 对抗双 kill | ✅ PASS | `S1-rebuild-evidence.txt` |
| S2 | TUI kill → 重建(ensureTuiWindow stale-kill) | ✅ PASS | `S2-S0-rebuild-evidence.txt` |
| S3 | founder × machine demux(TurnDemux) | ✅ PASS | `S3-demux.txt` + 截图 ss_50079eufo |
| S4 | **founder-terminal observe**(最关键,独立验) | ✅ PASS | `S4-journal-evidence.txt` |
| S5 | Bridge 出站 exactly-once(bridge 模式) | ⚠️ **NOT live-verified**(单测覆盖 + 本 cutover 不走 bridge;Eng Lead 接受,见 §3) | 实现方 266/266 单测 |
| S6 | turnless self-heal + bootstrap(rollout-at-first-turn) | ✅ PASS | `S6a-bootstrap.txt` / `S6b-turnless.txt` |
| S7 | Discord 腿 round-trip | ✅ PASS | `S7-discord-roundtrip.txt` + 截图 ss_4770f2jc4 |
| S8 | persona 注入(thread baseInstructions) | ✅ PASS | TUI capture(§2 S8) |
| S9 | chat-only 守卫(write-capable 拒启动) | ✅ PASS | `S9-workspace-write.txt`/`S9-danger-full-access.txt` |
| S10 | cwd polish(已知缺陷,记录非阻塞) | ✅ PASS(确认 display-only) | TUI footer capture |
| SP-2 | 双向 round-trip(TUI→headless 反向兼容 = 可瞬退资格) | ✅ PASS | `SP2-reverse-compat.txt` + 截图 ss_4770f2jc4 |

**Verdict**:**FINAL PASS,一个诚实 gap** —— **10/11 场景 live 验证通过**;**S5 未 live 验证**(infra:无 test Bridge + 临时搭风险/load,且 **Mufasa cutover 用 direct 非 bridge**;outbox 去重已被实现方 266/266 单测覆盖)。**无任一 FAIL**。S5 gap 已经 Eng Lead 接受(§3),stage② cutover 跟 Annie 决策时此 gap 须可见。

---

## 2. 场景详情

### S0 — bring-up ✅
`S0-bringup.log`:`home OK` → daemon `status:"connected"`/`bootstrapped`(standalone codex 0.140.0,隔离 socket) → `tui-window: real TUI up (growth-mufasa-tui-qa, thread 019ebfc0)` → `CodexLeadRuntime started`。resume 上一轮 QA thread(非开新)= 记忆延续机制工作。校账号 auth 活(turns 完成)。进程:`app-server --remote-control` daemon + `codex resume --remote … -s read-only -c approval_policy="never"` → R4 HIGH-4 multi-pin 经 CLI flags 落地。

### S1 — daemon kill → 重建 ✅
kill 隔离 home 的 remote-control daemon(只打 qa socket)→ runtime log:`daemon connection lost — fencing generation and rebuilding` → `real TUI up (…019ebfc0)` → `daemon connection re-established (new generation up)`;新 daemon PID ≠ 被杀的,窗 @46→@48,**同 thread 019ebfc0**。
**对抗(handoff S1 对抗点)**:rebuild 进行中再 kill(连续双 kill)→ 最终**恰 1 个 remote-control daemon + 恰 1 个 TUI 窗**(@49),无双 generation → generation fencing(R5 HIGH-2 / `stopIfRaced`)成立。

### S2 — TUI kill → 重建 ✅
`tmux kill-window`(精确目标)后窗自愈:window id `@9→@45`(stale-kill 旧窗 + 建新窗),同 thread 019ebfc0,runtime log `ensureTuiWindow` 的 `real TUI up`。
**观察(非 FAIL)**:一次 kill 触发 4 条 `real TUI up`,与文档「无条件 stale-kill + 重建」一致(每个 ensure 触发都无条件重建);功能正确(单窗、thread 对)。建议 follow-up 看 ensure 触发是否值得去抖(高 load 下多次重建放大 CPU)。

### S3 — founder × machine demux ✅
近乎并发发一个 machine turn(Discord)+ 一个 founder turn(TUI 打字):
- machine `S3-MACHINE-77` → `source=discord` completed + **Discord 出站回复**(截图:ops-lead-test APP「收到，我这边还在，S3-MACHINE-77。」)。
- founder `S3-FOUNDER-88` → `source=founder-terminal` observation(+1)+ TUI 显示回复,**但 Discord 无 S3-FOUNDER 任何消息**(founder 轮永不执行/不回 Discord)。
- counts 精确:discord +1、founder-terminal +1;crosstalk machine-as-founder=0 / founder-as-discord=0 → 二者不串台。

### S4 — founder-terminal observe ✅(**最关键,独立验**)
我自发 founder turn → journal.db 新增 `source='founder-terminal'`/`state='completed'`/`idem='founder:019ed244-…'`(2026-06-16 14:09:33)。契约:全表行皆 `completed`;`founder-terminal AND state<>'completed'`=**0** → 永不进 `listUnfinished()`;founder turn 前后 `discord` 计数不变(不漏 machine 路径);payload=`"founder terminal turn (observed; see TUI/rollout)"`(observation);`idempotency_key UNIQUE NOT NULL` 结构性幂等。

### S5 — Bridge 出站 exactly-once ⏸(见 §3)

### S6 — turnless self-heal + bootstrap ✅
- **S6a(fresh thread)**:全新 lead-id(无 thread-id)→ `bootstrap turn completed (rollout persisted)` → `real TUI up (…s6a, thread 019ed269)` → rollout 文件 `rollout-…019ed269….jsonl` 落地(rollout-at-first-turn 使 thread 可 resume)。bonus:`TurnDemux: dropped late event for released turn`(bootstrap straggler tombstone)。
- **S6b(turnless self-heal)**:伪造一个无 rollout 的 thread-id(`019eaaaa-…`)→ `saved thread has no persisted rollout (turnless) — starting a fresh thread { saved: '019eaaaa-…' }` → 开 fresh thread `019ed26b`(≠ 伪造)+ 建窗。**只对该特定『no rollout』错开 fresh thread,其它 resume 错 rethrow(实现方单测覆盖)→ 记忆丢失绝不静默**。

### S7 — Discord 腿 round-trip ✅
用 Annie 的 Chrome session 真发消息到 #ops-lead-test(human inbound,最忠实):`inbound-cursor.json` 推进(`…396241`→`…500594`)→ 入站路由 executor → machine turn(journal discord completed)→ direct 出站回复 **真落 Discord**(截图:ops-lead-test APP「我在呢，看到你叫我了，S7-DRAGON-42。」)→ **TUI 同步可见**同一回复。
> handoff 标注「Discord 腿是新接的,撞坑正常」—— 本轮 round-trip 一次通过,**未撞坑**。

### S8 — persona 注入 ✅
TUI 问『你是谁呀』→「我是 Mufasa，算是陪 Annie 练沟通和关心人的那个朋友，QA278-KIWI。」温暖陪练腔(非工程腔),带唯一 echo 暗号 → persona 经 `FLYWHEEL_LEAD_SYSTEM_PROMPT_FILES`(identity.md + companion-safety-contract.md,6654 chars)→ thread `baseInstructions`,经 daemon 生效。

### S9 — chat-only 守卫 ✅
`FLYWHEEL_CODEX_LEAD_SANDBOX=workspace-write` 与 `danger-full-access` 均在 `main()`(`codex-lead-tui-runtime.js:516`)拒启动,exit 1,报 `sandbox="…" is write-capable — the ③ TUI Lead is chat-only until FLY-245. Refusing to start.`(拒在任何副作用前,连 dry-run report 都没打印)。

### S10 — cwd polish ✅(确认 display-only,非阻塞)
TUI footer 显 `~/Dev/flywheel/worktrees/fly-259`(daemon cwd),不是 `-C` 的 `~/Dev/growth` —— 与 handoff S10 一致(`codex resume --remote` 保留 session cwd),纯显示,不影响功能/对话/persona。记 polish follow-up。

### SP-2 — 双向 round-trip(反向兼容 = 可瞬退资格)✅
顺序铁律(先停 TUI 证无第二 owner,再起 headless):tear down TUI owner → 起 **headless** 在**同一 state dir** → `CodexLeadRuntime started { threadId: 019ebfc0 }`(headless app-server resume TUI daemon 推进过的 thread,thread-id 不变)。**内容级连续性**:headless 被问『S3 那轮两个暗号』→「记得，是 **S3-MACHINE-77** 和 **S3-FOUNDER-88**。」—— headless 读出 TUI 期的全量历史(含 **founder-terminal** 轮 S3-FOUNDER-88)= 完整反向兼容 → **「可瞬退」资格成立**(TUI→headless 回切零记忆丢失)。

---

## 3. S5(Bridge 出站 exactly-once)— 未 live 验证(Eng Lead 已接受)

> **Eng Lead 裁定(2026-06-16)**:接受 S5 为『单测覆盖(266/266)+ 不在本 cutover 关键路径(Mufasa=direct 出站,runbook §2.2;bridge opt-in 本次不走)』。**不现在搭 test Bridge**(load 危险区 + 误碰生产 9876 风险,不值)。FINAL PASS 须诚实标此 gap;以后真要 S5 live,等 load<80 单独补。

**未 live 跑的原因**:
1. **无 test Bridge**:端口 19871-19874(test slots)全 down;仅生产 Bridge 9876 在跑(红线不能碰)。临时搭隔离 test Bridge 在当前 load(~160)下又重又有误碰生产 config 的风险。
2. **不在本 cutover 关键路径**:Mufasa cutover **用 `direct` 出站(runbook §2.2)**,bridge 是 opt-in、本次切换根本不走;handoff 亦注明「Annie 的 live 腿是 direct」。
3. **逻辑已单测覆盖**:`CodexOutboundSender` 的 outbox-db 去重在实现方 266/266 codex 套件内。

**结论**:S5 接受为『单测覆盖 + 不在 Mufasa cutover 关键路径(direct)』(Eng Lead 已裁定)。**10/11 live PASS + S5 单测覆盖足以支撑 stage② cutover 决策**;S5 live 补跑 = 待 load<80 单独搭 test Bridge(follow-up,不阻塞本 cutover)。

---

## 4. 环境备注 — load storm
本轮全程撞机器级 load 风暴(开机 30min 起 1-min 28→164,5-min 一直 ~100-150),驱动 = 开机中的 Lead fleet(9 个 claude agent)+ GUI(WindowServer/cmux/Discord/Chrome)+ Spotlight(`bfs`)+ 短时 node spike,**与本 QA 无关**(隔离实例 idle 时 0-1% CPU;teardown 后 load 仍 158 = 证非我驱动)。按 red line 全程**一次只起一个隔离实例 + 跑完即 teardown** 控制叠加;Annie 已确认 load 可接受。load average 贡献 ≈ 进程持续 CPU 占比的时间平滑,短时 codex turn(几秒 60%)对 1-min 均值仅 +~0.1,故未推高基线。
