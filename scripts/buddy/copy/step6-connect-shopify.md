先接你的店铺(Shopify)。只要一个「只能看订单」的钥匙,三步拿到:
1. 店铺后台 → Settings → Apps and sales channels → Develop apps → Create an app(起个名,比如 my-team);
2. Configure Admin API scopes → 只勾 read_orders → Save → Install app;
3. 出来的 Admin API access token 复制下来,贴进我这里的安全输入(只显示一次,贴丢了就重新 Install 一次)。
