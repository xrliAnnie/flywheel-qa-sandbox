Confirmed: the test-only seam is acceptable - the probe may append a process-exec/file-map-executable grant for /usr/bin/xattr to the exact generated read policy, used only by the probe (clearly labelled test-only, never emitted into the production policy), asserting xattr read succeeds on the Crashpad subpath and is denied outside it; existing Node data/enumeration probes unchanged. Production change stays exactly file-read-xattr on the Crashpad subpath. Wait for review 7dc7c278 before editing, then build and request the final host retest.
