Ruling updated (supersedes the subtree-only direction in 52bf81ab): parity with the Claude Lead means the Codex Lead keeps ordinary project editing, so take your first-sufficient path: drop the explicit filesystem[:workspace_roots]={.:write} override so the :workspace defaults keep .codex read-only, restore/assert nested .codex and .git read-only rules, and prove with the pinned CLI on host that writes to .codex/config.toml and .git are denied while a normal source-file write still succeeds; plus the effective MCP set pinned before thread startup with a planted-server regression. I will run the host proof for you (send me the exact commands as with the browser proofs). Root relocation only if that experiment fails. One round; head frozen while any review runs.
