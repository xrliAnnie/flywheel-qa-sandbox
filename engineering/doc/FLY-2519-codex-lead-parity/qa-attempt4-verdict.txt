QA FAIL (attempt 4) @ 0f5afbddd8cfd5d856e6439716b4814b0ebbd619 (PR #1191 non-draft, MERGEABLE; exact-head CI run 34935042184 success; pnpm install --frozen-lockfile + dist rebuilt at this head, build-identity=0f5afbddd; host unsandboxed arm64, Node 25.6.1, load avg ~22).
FIXED since 5a76d42bd (verified on host): removing the blanket (deny file-map-executable ...) works - under the final policy the launcher, /usr/bin/arch -arch arm64 <chrome> and the Chrome binary all print Google Chrome 153.0.8010.37 with exit 0 (previously SIGABRT in libRosettaRuntime), and /Library/Apple was not admitted. The new native/headed ps check (P_TRANSLATED) and its unit test are sound; scripts/__tests__/qa-fly-2519-browser-process.test.mjs passes on host.
BLOCKER C (deterministic): node scripts/qa-fly-2519-browser-canary.mjs exit 1 x3, stage=chrome-launch code=chrome_launch_unproven (29.0s/33.1s/32.2s). Host regression qa-fly-2519-browser-node.test.mjs fails at line 164 with list_pages -> Protocol error (Browser.getVersion): Target closed (1 pass, 1 fail). --version is not a real launch; an actual Chrome start still dies. Host ladder for a headed launch through the pinned launcher (--user-data-dir in qaRoot, --remote-debugging-port=0, 10s window, process group killed afterwards; stray test Chromes under /tmp/fw2519r were cleaned; the founder's Chrome was not touched):
1. final policy as shipped: SIGABRT; stderr "bootstrap_check_in org.chromium.crashpad.child_port_handshake...: Permission denied (1100)". Crashpad needs mach-register; adding mach-register alone still SIGABRTs, mach-register + mach-lookup gets further.
2. final + (allow mach-register)(allow mach-lookup) + --disable-crash-reporter --disable-breakpad: exit 21; stderr "stat /Users/xiaorongli/Library/Application Support/Google/Chrome/Crashpad: Operation not permitted". Chrome resolves the real user's default Chrome directory regardless of HOME and --user-data-dir, and the ruling's credential deny of the host Chrome profile makes that stat fatal. Adding --no-sandbox does not change this.
3. Chrome's own sandbox cannot initialise inside any outer Seatbelt: even with the outer policy replaced by (version 1)(allow default), headless Chrome aborts with "sandbox initialization failed: Operation not permitted / Failed to initialize sandbox" (SIGTRAP). With (allow default) plus --no-sandbox, headed Chrome reaches "DevTools listening".
So getting a native headed Chrome up under Seatbelt needs at least: crashpad mach rights (or a crash-reporter configuration that avoids them), a way past the host Chrome/Crashpad stat that keeps the ruling's profile denial intact, and a decision on Chrome's internal sandbox, which cannot nest inside the outer Seatbelt. Running Chrome with --no-sandbox would drop renderer isolation for untrusted page JS, so that is a Lead/security ruling rather than an implementation detail. Repro: /tmp/fw2519r/chrome-headful.mjs, chrome-ladder.mjs, chrome-manual.mjs.
PASSED at this head: credential isolation probe - EPERM for all pinned credential roots (including ~/.zshrc, ~/.npmrc, ~/.gitconfig, ~/.docker/config.json, Discord client storage), symlink escapes, write outside qaRoot, direct egress 1.1.1.1:443; write inside qaRoot allowed. D isolated real-Discord N-to-N re-run at this head - 529 test bots flywheel-test-1 / flywheel-test-2 in test-leads-roundtable, messages 1549309784639938601 (A) and 1549309785575264309 (B, message_reference to A), cross-bot readback of author ids and reply ref, replay deduped (providerWrites stays 2), foreign lead/project/unregistered channel all 403 with zero Discord calls. Not re-verified by me this round beyond exact-head CI: the origin/main merge, terminal multi-submit/legacy MCP guards and schema2 patrol changes.
Evidence: /tmp/fw2519r (canary4.log, real-discord4.json, chrome-*.mjs). No ship report (FAIL). No 529 slot room used, so no evidence-run record.
