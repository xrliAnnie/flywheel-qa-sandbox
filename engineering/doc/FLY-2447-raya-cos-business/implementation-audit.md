# FLY-2447 Raya CoS — 实施核对
Issue: FLY-2447 (https://linear.app/geoforge3d/issue/FLY-2447)
日期: 2026-09-15
基于: plan.md

This is an incomplete-work audit, not an acceptance or completion receipt.
Approved A-G and N1-N13 remain the scope. Product QA and production activation
belong to their authorized workflow owners after implementation handoff.

## Current requirements and evidence

| Batch | Inspected implementation / evidence | Remaining proof or implementation |
|---|---|---|
| A boundary | Raya business-round/operation-store/CLI; 3007b4e fixes actual deployment symlink execution. Filtered artifact loads all exports with no dependencies; CLI/business12 passed. | Final complete executable/import/installer sweep and exact-head package receipt after remaining edits. |
| B standard tools | Flywheel lead-directory, lead-actions, business-wake pass/schedule, proactive roundtable chain and durable budget. This audit: six actual test files,20 tests green. | Full affected suite/typecheck/build, N1 source-provenance integration and real authorized bidirectional test evidence. Focused green is not full gate. |
| C summary / coordination | Summary round ledger path is retained; current reconciliation and memory protocols inspect original provenance. Expected-head summary merge and inbound envelope tests: three files,59 tests green in this audit. | Old standalone lead-questions/questions.json is not consumed by the new operation entrypoint. Add explicit import/recovery preserving askId and no-send uncertainty. Final live summary/round/memory proof belongs to QA. |
| D goals / sampling / drift | goal-round and patrol-round parse existing goals.md; patrol reads SnapshotStore latest; current summary/snapshot formats remain in use. | Executable legacy preservation fixtures for IDs/history; old patrol-state carries thread/injected data which must be retained as history, never used to restore a private driver. Default full-test SnapshotStore timeout remains unresolved. |
| E daily report | Migration inventory, bound backups/import, existing/absent repo recovery, generation, message/nonce recovery and failure notices implemented. Detailed red/green receipts in implementation-validation.md. | Final all-status migration/rollback audit, full package gates, exact-head CI/review. Two natural evenings and actual founder feedback are product QA evidence, not unit tests. |
| F meetings | New MeetingRecord schema adapter, calendar and notification revisions, public voice receipts, terminal archive and slot turnover implemented; actual Flywheel trusted archive loader accepts temp output. | Legacy MeetingStore state/meetings/current.json and archive are not imported into new operations. Preserve UUID/revision/calendar and existing trusted voice/transcript evidence. Real live/ended/transcript proof remains QA. |
| G deployment | Existing updater filter confirmed; real filtered package and symlink CLI verification in Raya scripts/verify-business-package.mjs; persona daily/meeting instructions added. | Other state migrations and compatibility/rollback fixtures above; source docs/paired PRs; same-activation v2 deployment proof only after authorized activation. No registration/deploy/restart performed. |

## Gate status

- Approved design is retained; this audit does not revise it.
- TURN remains implement epoch10. No QA dispatch, ship approval request or merge.
- Latest full Raya default test at5069abf:282 pass/1 SnapshotStore5000ms timeout.
  Isolated4 and controlled single-worker52files283tests passed. Raw receipts
  committed; the default result remains red and is not an RPC waiver.
- Raya3007b4e build, whole-repo lint140files and filtered package proof passed.
- This audit's Flywheel focused tests: teamlead20 and comm59, all selected files
  actually ran. Retention consumer gate returned ok:true/errors:[].
  listPendingBusinessWakeEvents is a live operational query for undelivered
  business_wake rows; archived/terminal replay is outside its intended use.
- Mandatory pnpm lint, pnpm -r build, pnpm test:packages:run, relevant shell
  tests, full code review, PRs and exact-head CI remain outstanding.
- Milestone must remain the literal last commit before opening the PR.
  Completion requires the injected needs_review route and all repository PR
  declarations. Product/production acceptance is not claimed at that handoff.

## Next ordered work

1. Import/reconcile old standalone questions without changing their original
   identity or interpreting missing receipts as unsent. Only a proven old
   unavailable request may be prepared once against current directory capability.
2. Import/reconcile legacy meeting records with UUID, revision and calendar ID
   intact; preserve trusted current/archive/transcript state and reject conflicts.
3. Add bounded compatibility/rollback inventory fixtures for unchanged goals,
   snapshots, summary receipts and historical patrol data. Keep memory git
   history outside mutation; no thread/PID becomes a business identity.
4. Resolve the default test gate based on causal evidence, finish all mandatory
   repo gates, obtain effective review, and create the paired PR handoff.
   Keep N1-N13 and plan9.2 explicitly visible in QA instructions.

## 2026-09-15 implementation updates

- Ordered items1-2 now have source implementations and executable recovery evidence: legacy questions cc3715d/fa9b014; meetings dc26d1f/e627de4/8304103. Imports preserve raw backups/identities; matching current-turn metadata and trusted/public receipts continue normal operations. Unknown historical effects stay unknown.
- Item3 compatibility fixture completed in Raya7dade99; retained files and temporary memory Git history survive migration/replay. No production rollback performed.
- Item4 default Raya test timeout resolved by reducing durable fixture setup, retaining all assertions/default timeout/production source. Default54files300tests green plus separately added compatibility1 green.
- Flywheel lint/build now green; aggregate package suite running. Effective review, paired PRs/exact-head CI and final handoff still unproven. N1-N13/plan9.2 product acceptance remains explicit QA work; no production claims.
