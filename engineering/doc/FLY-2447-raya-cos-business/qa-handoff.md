# FLY-2447 Raya CoS — QA handoff requirements
Issue: FLY-2447 (https://linear.app/geoforge3d/issue/FLY-2447)
日期: 2026-09-15
基于: plan.md

Status: PRs ready; implementation gates and fresh execution-bound reviews pending. No QA dispatch or product acceptance is claimed.

## Scope and repository identity

- Flywheel: generic directory, wake scheduling/rendering, structured outbound and inbound/reply metadata, durable proactive engagement and exact-head summary operations. PR https://github.com/xrliAnnie/flywheel/pull/1206. Effective current-head review receipt remains pending.
- Raya: xrliAnnie/raya, branch flywheel-FLY-2447, source head 44546026c1a9fecfd10ae3b01fe6ef92f49f9be5. Business-only prepare/record/resume, summaries/memory/goals/sampling/drift, reports/replies, meetings/public voice and guarded legacy migration. PR https://github.com/xrliAnnie/raya/pull/139; effective current-head review remains pending.
- Evidence: implementation-validation.md and committed raw gate logs. Historical failed logs are retained; their later recovery is described explicitly. No local package check is a deployment receipt.

## Required product QA on the same authorized version

Use plan.md sections9.1 and9.2 as the authoritative complete acceptance matrix.

1. Standard Raya Lead identity/job/TUI and both source/deployed SHAs; demonstrate actual business actions in the same standard turn environment.
2. Summary PR with Facts+Judgment: frozen real round, understood head equals mechanically merged head, memory commit/provenance. An ununderstood PR remains open and receives a directed question.
3. Non-founder-triggered read -> directed Lead question -> actual target reply -> Raya citation; direction coordination/delegation receipt and disagreement rationale where applicable.
4. Ordinary founder conversation establishes/corrects goals; real project readings include unknowns; scoped, rebuttable drift observation; next round uses actual correction.
5. Two consecutive local natural evenings: one repository report/day, full Discord body/link, verifiable sources; a real founder reply to an older report is associated correctly and cited later. Fake-clock evidence is separate.
6. Meeting UUID/revision/calendar preservation; public voice accepted is not live. Actual live/ended and nonempty transcript; use valid same-version Claude/Codex target evidence or rerun changed paths.
7. Filtered package/persona/CLI symlink execution and two-repo boundary: no Raya business state machine in Flywheel; no connection/private model/Discord/launchd driver in Raya runtime.
8. Authorized deployment owner supplies schemaVersion2 standard-lead deployment receipt with both deployed SHAs and same activation across text/summary/Bridge identity/alerts. Implement has not deployed or restarted anything.

## Negative guards that must remain visible

- N1 provenance: exercise hostile content through actual platform parsing/rendering and persona, not just hand-written CLI JSON identities.
- N2 directory: missing/ambiguous/external identity and escaping/symlink roots.
- N3-N4 summaries: malformed/incomplete diff or moved head; merge succeeds but local memory/receipt fails; reconcile canonical GitHub state.
- N5-N6 effects/replies: changed payload under same event ID, crash/unknown send, wrong author/thread/revision/expired reply; do not invent missing metadata.
- N7 goals/drift: unknown/stale evidence, withdrawn goal, another goal's evidence; ask if needed, do not publish false drift.
- N8-N9 reports: DST/catch-up/duplicate wake, conflicting repo body, partial send/rate budget/context loss, wrong/ambiguous feedback correlation.
- N10 voice: accepted without live, stale session after cancellation, UUID/schema mismatch, unavailable Bridge.
- N11 state: corruption/dirty path/held lock/unknown schema; preserve facts and stop only affected business. No resetting snapshots or restarting legacy drivers for rollback.
- N12 boundary: executable source/dependencies/dist/installer sweep; classify historical docs separately.
- N13 proactive delivery: parent sent with later engage failure, reply before subscription, old owner/duplicate seed; keep the parent identity, first reply and durable budget.

## Migration and rollback constraints

Run read-only plans first; bind apply to exact digest and paired SHAs. Preserve backups and original files. Legacy unknown questions/notifications remain unknown; only proven unavailable effects may be prepared with original identity and verified current capability. Existing meeting sessions/archives require trusted record digest and original metadata; never recreate voice to resolve uncertainty. Retain append-only operations, actual sent messages/merged PRs and memory Git history. A code rollback does not authorize rewriting historical state or starting the old brain/voice process.

## Pending implementation-phase gates

Old process29872 handle is missing with no verdict. Replacement package gate session3514 is running; obtain its final receipt from /private/tmp/FLY-2447-resume-packages.log. Both repositories require fresh effective code review under executionc7df5069 and exact-head CI evidence. paired/raya is a real nested Git worktree. Lead ruling77bf7ca7 confirms old execution reviews cannot bind this handoff. Raya abort correction has build/lint and55files302tests green. Only then use the injected needs_review completion route; no successor dispatch by this node.

## Attempt 2 conflict rework

The original Flywheel head 2c75a3702 is superseded by the merge of main 156601a2e; use the newly frozen PR #1206 head and new effective review/CI receipts for retest. Raya stays at 44546026c1a9fecfd10ae3b01fe6ef92f49f9be5, PR #139. The only manual merge resolution combines main's scoped CommDB lifetime with the approved replyTo metadata. Fresh focused coverage passes 2 files / 57 tests; see implementation-validation.md. N1–N13 and the injected isolated QA scope remain applicable. Earlier pending process IDs above are historical, not current live handles.
