# FLY-2249 barge-in v2 — QA attempt 5(判决:FAIL · B 臂泄漏)
Issue: FLY-2249
日期: 2026-09-02
基于: qa-report-attempt3.md / attempt4.md、Lead 判据链 `1e105fe4`→`4780edff`→`49638f64`→`13ec0bba`

> **B 的处置(豁免 1/9 泄漏,或返工让 L1 证据 per-rep 可靠并追出泄漏路径)由 founder 定。**
> 本报告只给出判决与数据,不代她做这个取舍。

## 绑定头

- Raya **`f8c638b`** == `origin/fly-2249-bargein-v2`,worktree clean。
- flywheel **`d54b39c7`** == 锚 PR **#1035** head,`baseRefName = main`(`__main__`),OPEN。
- raya 仓无 CI ⇒ 本地全门:lint 干净、台架 **20/20**(f8c638b)、contracts 62 + brain 125 + voice 518 + QA 132、typecheck rc0。

## ⚠️ 仪器归属声明(Lead 裁定②)

**本轮测量仪器由 QA 自撰**,因实现体为避免共用工作树双写而停驻(`f11ed89d`),Lead 裁定采用我的 diff(`76c1d739`)。
仪器**经实现体跨族只读审三轮**:复审#2 给出 6 条(1 HIGH1 是我造的盲点)、复审#3 给出 2 条 MEDIUM、最后一轮再给 1 条 MEDIUM,全部已修(`4507a39`、`f8c638b`)。
最后两处修复按 Lead `032b0f2c` 指示**未再等签核**即出判决;判决所依据的房间数据全部采集于此前的头,不含这两处改动。
Bridge 未能为 QA 节点铸 review job(8 分钟零 job),Lead 已记为引擎缺口并改道。

## 判决

| 臂 | 判决 | 依据 |
|---|---|---|
| **A 真语音** | **PASS** | n=5,耳侧停口 714/721/727/805/857ms,**p50 727 / p95 ≈846 / max 857,全部 <1000ms**;`local_fallback` 0/5、停后 **0 帧 0 终稿** 5/5、note 语义臂 True 5/5、clock stall 0/5 |
| **B 呼吸** | **FAIL** | n=9(三轮合并):**8 干净 / 1 泄漏**。plan 硬门是 `speech_started = 0`,泄漏即失败。且该泄漏**不可归因**(见下) |
| **D 轻声** | **PASS** | 平台抵达 566/620/591ms,3/3,每轮 yield。plan 字面「门开 ≤400ms per rep」按裁定 (i) 记为**本轮不可量** |
| **C 附和** | **只给数** | leak 1/3;L3 词表**首次触发**(`bcIgn=1`) |
| 迟疑 | 记录 | 抵达 1512/1159/1690ms,3/3 穿透;2/3 未在 1500ms 内恢复;3/3 旧响应 truncated |

**总判决 = FAIL**(B 臂)。

## B 臂细节:泄漏不可归因

| 轮次 | 结果 | L1 证据 |
|---|---|---|
| r5-breath | 2 干净 / **1 泄漏** | 干净两轮有(门关、放行 0 帧);**泄漏那轮无 gate 摘要** |
| r6-breath1 | 3 干净 | **3/3 全有** |
| r6-breath2(schema v2) | 3 干净 | **仅 1/3 有** |

泄漏那一轮:`speech_started` 在刺激结束后 **114ms** 到达(距起点 634ms),与我实测的平台时延一致,
所以平台**是在对呼吸音频反应**;但该 rep **没有任何 L1 摘要**,因此我**无法判定 L1 门是否放行了那段音频**。

⚠️ **离线结论的范围更正**:离线全网格呼吸 **0/5**(L1 门从不打开)是**L1 层**结论。
真房里它仍抵达了平台 ⇒ **「呼吸从不开 L1」≠「呼吸从不抵达平台」**。
我在 attempt 3 说「呼吸零误触已立」应严格读作 L1 层断言,此处更正。

## 仪器 finding:L1 证据采集不稳定

同一刺激、同一臂、相邻头:一轮 **3/3** 带证据,下一轮 **1/3**。
成因是已知的门链不逐 rep 关闭 ⇒ 摘要落在窗外或不出。**不是结构不可能,是不可靠。**
后果:Lead 要求的「每 rep 带 L1 证据以便归因泄漏」这个保证,**仪器当前给不了**。

## 门集版本(裁定 (c)+(b))

`l1_evidence_missing` 与 flush 字段类门只对带 `summarySchemaVersion: 2` 的捕获生效。
旧摘要缺字段是**仪器版本差**,不是产品未产出证据。本报告中:
A / D / hesitant / backchannel 的数据**测于 schema v2 之前的门集**;仅 r6-breath2 为 v2。

## 诚实边界

1. **普通话 嗯/对 仍完全未覆盖**(MagicData-RAMC 为 CC BY-NC-ND,NoDerivatives 禁止切片)。
2. **不是 founder 的设备链**;公开语料的麦克风/房间/增益/编解码均不同。
3. **说话人少**:21 条来自 13 个来源。
4. **D 的「门开 ≤400ms per rep」未量到**;`gateOpenedCount` 非逐轮量(utterance 事件落在窗外)。
5. **C 只给数**:泄漏 rep 的 `output_truncated` 与恢复超预算如实记录,不附判决。
6. **仪器由我自撰**,虽经三轮跨族只读审,独立性仍弱于「仪器出自他人之手」的常态。

## 今日仪器缺陷账(全部我自己引入并修掉)

| 缺陷 | 抓到方式 |
|---|---|
| HIGH1 无触发 rep 完全不搜 ⇒ 零停口门是瞎的 | 跨族复审 |
| transition 取样过早 ⇒ 已复位的条目被报 `not_restored` | Lead 强制的重算 |
| passthrough 无打分 ⇒ 我的 F10 修法把空过变成错判失败 | 同上 |
| `flushAtMs !== null` ⇒ **`undefined !== null` 为真**,对所有旧摘要开火 | 同上 |
| `gatedUtteranceCount === 0` ⇒ 缺失字段绕过豁免守卫 | 复审#3 |
| `never_recovered` 过度宣称(分不清「未恢复」与「采集过早结束」) | 我自己查原始帧 |

**其中三条只有靠「对真实旧捕获重算」才会现形** —— 跑一轮新的,字段就在了,它们会被完美藏住。
