# 2026-08-24 晚间的两次尝试（⚠ 均因**服务/宿主类**原因作废，按裁决 (A) 不可替换）

| attempt | 窗 | 结局 | 原因 | 采到 |
|---|---|---|---|---|
| 001 | 1 | `aborted` | **`collector_guard_abort`**（exit 2）—— 采到 b4 时 **Bridge 换了身份**，主体围栏拒绝把两个实例的数据混在一起 | 4/30 区块 |
| 002 | 2 | `aborted` | **`health_unreachable`**（exit 1）—— 16:54 开跑，而 Bridge 16:48 被杀、16:50 才起来；preflight 拿不到 `/health`，**fail-closed 拒绝开跑** | 0/30 区块 |

⚠ **两条都是设计行为，不是缺陷**：一个拒绝混合两个实例的数据，另一个拒绝在不知道健康状态时开跑。

## 为什么它们作废

`collector_guard_abort` 与 `health_unreachable` 都在 `SERVICE_HOST_REASONS` 里 ⇒ **不可重跑替换**（Tadashi 裁决 (A)，见 `spec-amendments.md` §5b）。
⇒ 它们**不进任何推断**，但**留在账上、不能被洗掉** —— 这正是防幸存者偏差的那条规则的意义。

## ⚠ 它们真正的价值：它们本身就是主结论的证据

**当晚三次采集尝试，两次死于 Bridge 被它自己的 EventLoopGuard 杀掉**（15:46、16:48）。
一个 Bridge 实例只活了 **61 分钟**（15:47→16:48），而冻结设计要求被测对象身份 **2.5 小时不变**。
当时 `load1` 一度到 **61.38**（15 分钟均值 34.28）。

⇒ 见结论页 §3：**基线不可刻画**。这两个失败的 attempt 不是噪声，它们是那条结论的一部分。
