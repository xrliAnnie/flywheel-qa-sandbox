# W1 / W2 —— 在 build `f2eecf495` 上采的两窗（**已披露的先验观测，不进最终推断**）

日期: 2026-08-23 16:45–19:15 PT（W1）、19:53–22:23 PT（W2）
采集冻结 commit: `5ce14e9dee5eaa404fca7f3d7cdf250c08e5d3e1`
被测 build: `f2eecf495085589677a36deb93b84ded8bcccf08`

## 为什么它们不进最终推断

**不是因为数据有问题** —— 两窗都干净：30/30 区块两端点全 valid，畸形行 0，`buildSha` 与 worker PID 全程未变。

原因是 **W3 错过之后 00:01 班车换了 build**（现为 `9d322f602`）。补一个 W3 会让三窗跨两个 build，而 **Q0 的第二问正是「跨多个时间窗还成立吗」** —— 那样我就分不清第三窗的差异是**时段**造成的还是**换 build** 造成的。build 分裂在这里不是一条环境注记，它**混杂的就是被问的那个问题**。

⇒ Tadashi 裁定（2026-08-24）：同 build 重采三窗；这两窗按已披露先验观测留档。

## 它们说了什么（观察，不是结论）

| | L1 池化 `b̂` | L2 池化 `b̂` | L1 区块 >0.20 |
|---|---|---|---|
| W1 | **0.1282** | 0.0043 | **8/30**（最高 0.46） |
| W2 | **0.0447** | 0.0030 | **0/30**（最高 0.127） |

**两窗 L1 差 3 倍，而中位数几乎相同（都是 0.047）。**
⇒ 差异来自**少数极坏区块的有无**，不是整体水平平移 —— 与 FLY-1986 描述的「成段发作」形状一致。

判据量（A 方向下界，已做同时性校正）：**W1/L1 = 0.0140、W2/L1 = 0.0135**，两者都在 5% SLO 之下。

## ⚠ 处置

与 pilot、排练同等待遇：**永久排除在最终三窗的推断之外** —— 不进 `b_ub`/`b_lb`、不进等价检验、不进结论页的判决。
它们保留在这里是因为那个 3 倍差异**本身就是有信息量的观察**，而且它们是管线在真数据上跑通的证据。
