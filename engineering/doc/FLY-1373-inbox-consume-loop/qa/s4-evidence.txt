FLY-1373 场景④ 忙时挂起再恢复 — 真机证据
==============================================
忙态判据(已校准,两次探测器失败均为我方工具问题,非产品问题)
  v1 找 "esc to interrupt" → 该 Lead 忙态根本不显示这串
  v2 用 capture-pane -S -8 → 状态栏占 7-8 行,spinner 行被挤出取样窗口
  v3 改用 transcript 末条 type:user=收到未答(忙) / 其它=闲。发探针实测翻转,尺子有效。

执行
  20:34:20  A 组 5 条 → 把 Lead 推进 turn
  20:34:22  ✅ 确认忙态(state=user)
  20:34:22  ⚡ 忙态中投 B 组 10 条(seq 76-85)

忙态期间采样
  +1s  B 组 10 入队 / 1 销账    Lead=user(忙)
  +2s  B 组 10 入队 / 10 销账   Lead=user(仍忙)
  +3s  10/10                    Lead=assistant
  +4s  10/10                    Lead=attachment

结果
  队列:  b_total=10  consumed=10  pending=0     零丢
  Lead:  Event #76-85 各收到 1 次,共 10/10      零丢零重复

判定:PASS(不丢、不重、空闲后全部呈现)

需要如实说明的机制细节(不是缺陷,但报告不能含糊)
  "挂起"不发生在队列层。实测 +2s 时 Lead 仍在忙,10 条已全部 consumed —
  inbox loop 在 Lead 忙期间照常全速消费,缓冲发生在下游 Lead session 邮箱。
  → 耐久边界是「adapter 收妥」,不是「Lead 已读」。
  若验收意图是"队列层必须挂起",则实现与该意图不符;
  若意图是"忙时来的消息不丢、空闲后能看到",则完全满足(实测如上)。
  这条留给 Lead 判定意图,我只报实测。
