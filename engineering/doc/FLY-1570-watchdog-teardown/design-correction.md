# FLY-1570 拆 watchdog 全家 — 设计更正附录

Issue: FLY-1570 (https://linear.app/geoforge3d/issue/FLY-1570/消息层重构-a-批次1-拆-watchdog-全家)
日期: 2026-08-04
基于: plan.md(Codex 8 轮 APPROVED 版)

> 设计节点完成后收到的 Lead/founder 反馈,按增量更正落进 plan.md;本附录记录更正事实与理由,不回滚分支。

## 更正 1:receipt 升级链核对项的框架修正(lead-instruction 0e76355c,取代 92a148f8 的旧框架)

**新证据(CoS 阳性对照)**:fingerprint>200 的升级行 1649 条 **0 次 page**(≤200 的行 3759 次 page,证明 pager 本身活着);>7 天未结的 170 条同样 0 page。⇒ 这一类不是「误 page 吵 founder」的假阳性,而是 **ack 与 page 双路全死的假阴性静默失效** —— watchdog 自以为在盯,类内任何真实故障永远到不了人眼前。

**plan 更正**(§3.12 改写):裁定从「部分包含,边界逐条」升格为**「这类检测不该以 watchdog 形式存在 → 明确删除,不留死着装样子的 watchdog」**。新证据加强而非动摇拆除:删的不是工作中的保障,是只剩形骸的假保障。「值得检测的职能」不由新 watchdog 承接,由 D 单的结构性队列语义取代(租约重投不新建行 + 死信闸)—— 送没送到从此是队列属性,自嵌套与长度上限问题在结构上不存在。

**不变的部分**:三个病灶(fingerprint 整段拼接 / 长度上限 fail-closed 方向反了 / too-long 误报 required)仍不折进纯删 PR;detection-ack 端点为统一检测流保留,其长度上限病灶降级为保留端点的潜在缺陷,独立第 9 刀位保留待 Lead 拍板。1514 家族关闭前提成立:自嵌套发动机确实被本单拆掉。

## 更正 2:QA 节点硬性要求(founder 直令 2026-08-04 06:01,lead-instruction 974746ff)

**plan 更正**(§5 新增验收 6):QA 节点必须包含 529 测试房真机 E2E(真组件,禁 stub,参照 FLY-1624 标准)+ 真 Discord 送达验证(真实产物经真实发送链路进 529 隔离频道,真送达+读取确认,生产频道零污染)+ 结论绑定精确 head + 修前/修后对照数字 + 涉及重启的验收必须真实重启(不适用项须书面说明,不许沉默跳过)。founder 预批范围内的中间环节,直接执行。
