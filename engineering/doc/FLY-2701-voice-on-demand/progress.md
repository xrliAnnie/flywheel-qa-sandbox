---
issue: FLY-2701
phase: implement
phaseCursor: 0/7
updated: 2026-09-18T00:11:21.149Z
nextStep: Park awaiting Lead wake after FLY-2693 and FLY-2655 merge; then sync
  exact merged prerequisites and start slice A TDD
chunks: []
pointers: {}
---

# FLY-2701 progress
**phase**: implement (0/7)
**next**: Park awaiting Lead wake after FLY-2693 and FLY-2655 merge; then sync exact merged prerequisites and start slice A TDD

## 2026-09-18 implement dependency/interface audit

Authoritative preflight at `origin/main@f0175458414f20f6f82ca7ea12cd6a6f64899eb3`:

- FLY-2693 is still in implementation and has no PR; its remote branch contains approved design artifacts but no implementation code. FLY-2655 PR #1243 and FLY-2657 PR #1241 are open; FLY-2669 has not opened a PR. Do not copy or merge any in-flight branch. After the Lead supplies merged SHAs, fetch and prove all prerequisites are ancestors of this branch before slice A.
- FLY-2693 health/alert seam: reuse its exact `voice_daemon_unhealthy` kind and the merged `scripts/lib/voice-health.py` plus `packages/voice-codex/src/health.ts`; do not create a second alert path. Re-audit merged `LeadAlertNotifier.ts`, `scripts/lead-alert.sh`, and `bridge/{kind-contract,ticket-owner-map,alert-kind-copy}.ts`. The expected demand seam is `StateStore.getVoiceDemandSnapshot(afterSeq)` backed by `voice_health_demand_events`, with Bridge-side `record-demand`; the schedule/wake runtime must advance the same demand revision/source rather than invent a second counter.
- FLY-2655 overlap: rebase prewarm work on the merged `flywheel-voice-core` receive-health DTO, `voice-codex/src/{receive-health,discord-room,session,bridge-client,daemon}.ts`, `StateStore` receive-health columns, `voice-session-card.ts`, and the card projector/runtime lifecycle. Preserve receive-health renewals and card projection while moving lifecycle coverage earlier and starting room/frontend in parallel. Its `voice_intents` and `voice.session.{start,status,stop}` Lead capability remain separate from the new scheduling API.
- FLY-2669/FLY-2657 updater seam: before slice F, sync their merged `scripts/update-flywheel.sh`, `scripts/restart-services.sh`, `scripts/lib/updater-raya-deploy.sh`, `StateStore.ts`, `ProjectConfig.ts`, `LeadAlertNotifier.ts`, and Bridge plugin wiring. Add drain fencing around the resulting updater transaction; never overwrite their shuttle observation/legacy-owner contracts.
- Persistence seam (`packages/teamlead/src/StateStore.ts`): current `VoiceSessionRow` has no schedule/revision/readiness fields and current `reserveVoiceSession()` deduplicates only by `meeting_id`. Add `voice_schedules`, `voice_schedule_requests`, `voice_launch_attempts`, and `voice_deploy_drain`; extend sessions with `schedule_id`, `schedule_revision`, `not_before_live_at`, and `ready_at`. Keep SQL parameterized and register every new retained table/consumer. `setVoiceSessionState()` currently permits warming only to live/failed and accepts ended reasons only she-left/text-stop/voice-stop; the merged implementation must add warming to ended for `no_human` without allowing live to use that reason.
- Bridge HTTP/runtime seam: current routes live at `/api/voice/sessions`, and `VoiceSessionRuntime.tick()` serially validates, sweeps, provisions, then polls. Mount the exact new `/api/voice/schedules` router beside it; add lease-authenticated `POST /api/voice/sessions/:id/ready`. Add `voice-schedule-runtime.ts` and `voice-launchd-waker.ts` without awaiting launchd or external provisioning in the 3-second wake lane. Preserve claim as the only consumption of durable desired state and recheck schedule revision/cancel/drain in reserve, provision, claim, ready, and live transitions.
- CLI seam (`packages/flywheel-comm/src/commands/voice-session.ts`): request transport currently supports only GET/POST and commands start/stop/status. Extend it for PATCH and the exact commands `schedule-status`, `reschedule`, and `cancel-schedule`; scheduled start takes `--scheduled-at` and `--request-id` and returns schedule id/revision/late-admission. Existing immediate start/status/stop remain compatible.
- Daemon/lifecycle seam (`packages/voice-codex/src/daemon.ts`): current run performs recovery, then polls desired every 5 seconds forever; `SessionLifetime` begins only after `createSession()`. Add first-immediate desired read, successful-null idle accounting, final read, bounded cleanup, and exit 0 after 120 seconds. Lifetime must start immediately after claim and cover identity/preflight/createSession, parallel warming, wait-until-T/presence, live, and cleanup. Existing `recover()` only terminalizes/quarantines old sessions and must not be described as reconnect.
- Media/prewarm seam (`packages/voice-codex/src/session.ts`): current `GenericVoiceSession.start()` starts frontend then room serially and sets `admitted=true` before `markLive`; assistant transcript/status paths can still run before live. Start room/frontend together under one abort/deadline, observe both settlements after cancellation, report ready only after both plus lease, and hard-drop all audio/transcript/speech/queue work until live. For scheduled meetings, ready may wait for T; at T re-read presence and schedule revision before live; T+120 absence ends normally as `no_human`.
- launchd/install seam: `scripts/launchd/com.flywheel.voice.plist` is currently `RunAtLoad=true`, `KeepAlive={SuccessfulExit:false}`, `ThrottleInterval=30`; installer and `scripts/lib/restart-voice.sh` require a running KeepAlive PID. Convert to registered-dormant `RunAtLoad=false`, `KeepAlive=false`, `ThrottleInterval=1`; installer success is verified registration/identity, not PID. Add shared `voice_on_demand_contract_check` for source/installed/loaded identity and use it from installer, waker, setup convergence, census, restart/rollback. Change `units.manifest` from hold to setup with explicit byte-drift checking.
- wrapper/failure seam: `scripts/flywheel-voice-wrapper.sh` currently trusts any live PID file, invokes `restart-storm-gate.py gate voice`, and falls back directly to meta-alert. A foreign reused PID must not suppress startup. Remove the old voice storm gate only when the loaded on-demand contract is verified; otherwise retain the legacy brake during mixed deployment. Route startup classifications through merged FLY-2693 health first, leaving `startup-alert.ts`/meta-alert only as source-unavailable fallback. Bound accepted kickstarts even when wrapper startup evidence never arrives.
- deployment/drain seam: current `restart-services.sh` detects voice file changes and calls `restart_voice_managed()` at Step 3.5, with no pre-Bridge-stop drain. Implement draining then committed `voice_deploy_drain` before any stop/rollback, persist the 0600 updater receipt, reload committed fence before Bridge admission, and release only after final voice refresh/rollback verification. No voice change means no kickstart; no demand means registration refresh stays dormant.
- Focused verification after prerequisites land: add the planned StateStore/schedule/waker/drain and voice-codex timing/idle/prewarm suites; extend wrapper/install/manifest/census/supervisor/convergence/host-tmux/restart-storm coverage and include `qa-fly1501-brake-missing-alert.test.sh`. Register every new shell suite in both CI workflow and `scripts/__tests__/ci-structure.test.sh`. Do not start aggregate gates while the host is running the prerequisite PR matrices; Lead explicitly limited this waiting phase to focused checks only.
