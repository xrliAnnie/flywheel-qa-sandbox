# FLY-2789 节点成绩记录 — 探索
Issue: FLY-2789 (https://linear.app/geoforge3d/issue/FLY-2789/2787-b成绩记录-每张单每个节点记下用的模型组别并记-qa-是否一次过founder-打回次数额度花费耗时按组汇总每张一次过-qa)
日期: 2026-09-22
基于: 无

## 问题与范围
Lead 要知道每张单、每次节点执行实际用了什么模型，返工、换体后消耗多少，以及哪组更容易一次过 QA。目标来自注入的 FLY-2789 正文与 Epic FLY-2787 验收第 5 条；Linear MCP 不可用，未把另一次在线 Epic 拉取伪称为已验证。

本节点只设计、评审并发布 HTML。后续实现不改 2787-A 分流、不改生产配置、不重启；用现有账本能证明的 token 口径，不把订阅余额变化当作某张单的花费。CLI 是命令行查询入口，选它避免额外建管理页面。

## 选项
| 方案 | 好处 | 缺陷 | 取舍 |
|---|---|---|---|
| 每次读取现有可变执行表，临时按模型猜组 | 代码最少 | 换体覆盖、分组失真，无法证明首轮 QA | 拒绝 |
| 复用不可变工作流事件与用量账本，补齐缺失身份，按需查询 | 可追溯、无需独立服务 | 需明确用量归属及缺失值 | 选择 |
| 新建通用事件平台、后台聚合和仪表盘 | 可扩展 | 非本单必需，复制既有基础设施 | 拒绝 |

## 设计约束
- 单的身份按项目加 canonical issue id；run、node、attempt、execution、activation 分开保留。英文身份是稳定编号，不是可变显示名称。
- 一个 execution 换进程不增加单数；新的 attempt / replacement execution 都保留真实消耗。
- design、implement 与 qa 三个分流维度分别汇总，不把它们合成互斥组；未分流显式 unassigned。
- exact model id 取实际运行证据；requested alias 和 observed id 分栏。缺证据显示 unknown，不编精确 id。
- 首次 QA 只认已接受的结果，不把命令提交、进程退出或 reviewer 设计评审当 QA。
- founder 打回只认真实决定来源；Lead 自主返工、模型引用 founder 文本不冒充 founder 打回。
- 缺用量、未结束、历史缺口必须显式显示；零与未知不同。

## 待 Lead 纠偏的口径
已发非阻塞问题 b73345b0-9966-4203-ab26-113e4fe3b657：主指标拟为组内总投入 / 一次过 QA 单数，并额外列通过单自身平均投入；请给 2787-A 的稳定组 id / assignment receipt 契约。未得到新约束前不推断分流标签，B 可独立先记无分流。

## 验收必须保留
隔离环境逐条人工对账：一次返工、一次 founder 打回、一次换体；同事件重放不重复；生产只读 dry-run 无写入。设计文档和静态检查不能替代后续实现与 QA 的这些证据。

## 口径已收敛
Lead 已同意指标公式，指定由 B 定 A 的读接口；详见 plan.md §2.2a。无 receipt 在来源完整时为 unassigned，不从模型反推。

## Founder 追加范围（2026-09-23）
Lead 指令 fa066f60-0a86-46f4-99a0-1a14c956fe79 要求实现也分组：impl_opus / impl_sol；B 增加第三维成绩。额度降级单独列出，不计入原组。A 写服务端 model_arm_degraded 回执，B 按 plan §2.2b 读取；不能从模型变化推断。
