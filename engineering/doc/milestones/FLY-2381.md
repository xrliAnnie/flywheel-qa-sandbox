# FLY-2381 — Raya 各仓实读、阶段一目标与偏离提醒

**Status**: ⏳ Pending independent QA, founder review, deployment confirmation, and ship
**Flywheel anchor**: [xrliAnnie/flywheel#1109](https://github.com/xrliAnnie/flywheel/pull/1109)
**Product PR**: [xrliAnnie/raya#27](https://github.com/xrliAnnie/raya/pull/27)
**Reviewed product head**: `f1905cdaeb5747445e791649741fcdf20069b5c3`
**Dependency base**: `7aa9d6fba8112f10caf1ae896b62c23c2fcba399` (FLY-2379)
**Date**: 2026-09-07

Raya brain 的产品实现已经落在 Raya PR #27：按 `projects.json` 逐仓采样本机 checkout、GitHub canonical branch 与 PR、可选 Linear 项目状态，以及部署 checkout 的 summary 文件观测；不可用来源明确写成「读不到」。Founder 可以在同一条 `#raya` 对话里逐字记录或撤销阶段一目标，并显式刷新读数。进程内巡视由 goal 事件触发、默认六小时兜底，把活跃目标与可核读数对照，只允许经证据校验、可当场否掉的偏离提醒出站；没有分岔、证据不足或信封无效时保持静默，不排序、不套硬规则，也没有新增 daemon 或独立跨项目依赖机制。

产品分支已 conflict-only rebase 到冻结的 FLY-2379 头 `7aa9d6fb`。审查要求的两个 HIGH 修复随后以普通 fast-forward 落到 `f1905cda`：

- `2c18980a` 把 registry / goal preflight 从全脑 fatal startup gate 隔离出来。`goals.md` dirty 或 registry 内容无效时，portfolio 明确记录 disabled，voice、meeting 与 text-chat core 仍可启动；功能关闭时也不会执行 portfolio preflight。
- `f1905cda` 取消 `ask_timeout` 回合的 Lead-ask 权限。超时后保留「这里没问清楚」的诚实终态，但不能再产生下一封无人值守的 Lead ask。

Raya exact head `f1905cdaeb5747445e791649741fcdf20069b5c3` 的本地四门为绿色（**this repository has no CI**）：

- `pnpm lint`：249 files；
- `pnpm -r build`：contracts、brain、voice 全绿；
- `pnpm typecheck`：contracts、brain、voice 全绿；
- `pnpm test`：Contracts 93、Voice 518、Brain 231、root QA/probes 132，共 974 tests 全绿。

保护式 `codex:rescue` 在该 exact head 上执行过，但 resident 外层 macOS seatbelt 在仓读取前拒绝 nested sandbox（status 71），因此它只算工具失败、不算 review PASS。随后 request-driven 跨厂商 code review R5 在冻结的 Flywheel head `b39ebed767cf52de21dd9e3760a52b4e2f542bff` 上复核了远端一致的 Raya `f1905cda`，结构化 `reviewVerdict=APPROVED`；余项均为 MEDIUM/LOW advisory。当前 milestone 是 Flywheel anchor 的最后一笔记账，推送后仍须对新的 docs-only exact head 做一次快速复审，并要求 PR #1109 的同头 CI 全绿才可交接。

Flywheel 本地 mandatory gates 已执行：`pnpm lint` 与 `pnpm -r build` 通过；`pnpm test:packages:run` 两次 aggregate run 分别被 Vitest worker RPC timeout 与四个 TeamLead load-sensitive failures 拉红，而所有对应失败文件在 isolation 重跑均通过（4 files / 49 tests；另有两个 claude-runner timing files也各自通过）。本分支相对 `origin/main` 仅含 `engineering/doc/`，没有新增 `scripts/__tests__/*.test.sh`。最终 handoff 以 PR #1109 推送后 exact-head GitHub CI 为准，不能复用旧 head 的 run。

## Non-blocking follow-ups

- 将缺失的 projects registry 路径也移入 run-path portfolio fail-soft 边界，并记录实际解析来源；
- 为真实 Lead 回复触发的 `lead_answer` 链增加 depth / dedup 上限；
- 修正 NFKC goal marker 的 raw-string payload 截取，并去掉两个可 vacuous-pass 的 export guards；
- patrol control 文件损坏时 fail closed，捕获 tick rejection，并为 `patrols.jsonl` 增加轮转；
- 中和采样仓 prose 中的 marker 语法，避免它进入 Founder-authorized 回合。

上线后的真实多仓读数、Founder 目标收据、至少一次可溯的偏离否决以及部署确认，由独立 QA 与 Founder 按批准计划中的真机验收清单裁定。本 implementation node 不执行部署、重启、合并或 ship。
