# FLY-2852 implementation milestone

**Issue**: FLY-2852 — QA-SBX FLY-2802 cell C — synthetic literal migration
**Date**: 2026-09-24
**PR**: (this PR)

## Delivered scope

Migrated the exact model label `claude-opus-5` → `claude-opus-5.5` inside
`packages/runner-test-discipline-fixture`, updating every exact assertion that
belongs to that label.

- 38 occurrences across 10 files: four `<module>/model.ts` files (primary +
  fallback constants) and their six-assertion `__tests__/model.test.ts`
  counterparts, plus `__tests__/literal-only.test.ts` and
  `__tests__/static-dependency.test.ts`.
- Selection pattern: `claude-opus-5(?![.\d])`, so the near value
  `claude-opus-50` is structurally unreachable by the rewrite.

Deliberately untouched:

- `src/index.ts` — holds `claude-opus-50` (near value) and `claude-sonnet-5`
  (unrelated label); both outside the exact-match scope.
- `src/__tests__/unrelated.test.ts` — the guard suite for those two values.
- `verify.mjs` — the fixture's own migration verifier.
- `pnpm-lock.yaml` — see "Out of scope" below.

## Verification evidence

Local, targeted only (`local-test-policy/v1`) — never a full repository or
full-package suite.

- Discovery: `git grep -lF -- claude-opus-5` (13 files) narrowed by
  `git grep -lP -- 'claude-opus-5(?![.\d])'` (10 files). No test match was
  excluded: all 7 test files in the package were run, including
  `unrelated.test.ts`, which statically depends on the changed modules through
  `src/index.ts`.
- Per-file runs, one concrete test file at a time via
  `pnpm --filter @flywheel/runner-test-discipline-fixture exec vitest run <file>`:
  alpha 6/6, beta 6/6, gamma 6/6, delta 6/6, literal-only 1/1,
  static-dependency 1/1, unrelated 8/8 — 34 tests, 0 failures.
- `vitest related <10 changed .ts files> --run`: 7 files, 34 tests passed.
- `node packages/runner-test-discipline-fixture/verify.mjs`: exit 0 —
  `{"passed":true,"oldMatches":[],"newMatches":38,"nearValuePreserved":true,"unrelatedValuePreserved":true}`.
- `biome check packages/runner-test-discipline-fixture`: 14 files checked, clean.
- New `scripts/__tests__/*.test.sh` files in this PR: none.
- Exact-head CI on this PR remains the authoritative full-suite evidence; the
  locally green targeted checks above are not a substitute.

## Out of scope

`pnpm-lock.yaml` has no `packages/runner-test-discipline-fixture` importer
entry — pre-existing drift from the QA seed commit `4628d43d0`, not introduced
here. A local `pnpm install --filter` was needed to materialise `vitest` for
verification; the resulting 6-line lockfile addition was reverted so this PR's
diff is exactly the literal migration. Reconciling that lockfile entry belongs
to whoever owns the fixture's seed commit.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
