# FLY-2854 implementation milestone

**Issue**: FLY-2854 — QA-SBX FLY-2802 cell D2 — synthetic literal migration (codex engineer)
**Date**: 2026-09-24
**PR**: https://github.com/xrliAnnie/flywheel-qa-sandbox/pull/232

## Delivered scope

- Migrated the exact model label `claude-opus-5` to `claude-opus-5.5` inside
  `packages/runner-test-discipline-fixture`: the four `{alpha,beta,gamma,delta}/model.ts`
  primary + fallback constants and every exact assertion that belongs to that label
  (6 per module test, the literal-only contract, and the 4-element static-dependency
  expectation) — 10 files, 38 occurrences.
- Left the deliberately similar and unrelated values untouched: `claude-opus-50`
  (`nearModel`) and `claude-sonnet-5` (`unrelatedModel`) in `src/index.ts`, together
  with their 8 assertions in `src/__tests__/unrelated.test.ts`, and the fixture's own
  `verify.mjs`. The substitution used the negative lookahead `claude-opus-5(?![.\d])`
  so the `claude-opus-50` prefix match could not be rewritten.
- No behavior beyond the label value changed; no other package was touched.

## Verification evidence

Targeted selection only — no full-repository or full-package suite was run.

- Discovery before editing: `git grep -lF -- 'claude-opus-5'` returned 13 files; the
  three substring-only files (`src/index.ts`, `src/__tests__/unrelated.test.ts`,
  `verify.mjs`) were excluded because they carry `claude-opus-50` or a part-assembled
  literal rather than the exact label.
- `node packages/runner-test-discipline-fixture/verify.mjs`: exit 0 —
  `{"passed":true,"oldMatches":[],"newMatches":38,"nearValuePreserved":true,"unrelatedValuePreserved":true}`.
  Baseline before the change was exit 1 with 10 `oldMatches` and `newMatches: 0`.
- 7 test files, each run individually with the owning package via
  `pnpm --filter @flywheel/runner-test-discipline-fixture exec vitest run <concrete-file>`:
  7/7 files, 34/34 tests passed, including `unrelated.test.ts` (8/8), which pins the
  preserved near and unrelated values.
- Residual scan `grep -rP 'claude-opus-5(?![.0-9])'` over the package: no matches.
- `pnpm lint`: 0 findings in `runner-test-discipline-fixture`; the 14 reported warnings
  are pre-existing elsewhere in the repo.
- New `scripts/__tests__/*.test.sh` files in this PR: none.
- `pnpm install --filter @flywheel/runner-test-discipline-fixture` was required to obtain
  `vitest` in this worktree; `pnpm-lock.yaml` was restored afterwards and is not part of
  this PR.

Exact-head PR CI owns the full suite. The locally green targeted checks above are not
full-suite evidence.

## Handoff boundary

This node owns the migration, its targeted verification, the branch, and PR #232. Review
and ship authority stay outside this node; the DAG orchestrator advances the graph.
