# FLY-2136 — Bridge mailbox 死信扫描热循环修复

**Status**: ⏳ Pending ship
**PR**: #983
**Date**: 2026-08-28

拆掉 Bridge mailbox 热路径的直接自杀引信:为死信扫描补 partial index 与 30s 节流,把 probe facts 计算移入扫描窗口;为终态行增加按活体吞吐定容的周期归档与 ring-cursor 前进保证,并把 `content_ref` 读取/哈希移出 SQLite transaction。生产等量 `63,007 ACKED + 3,212 DEAD` 的真实 Bridge 回归中,连续 240 次 `/health` 全成功,HTTP p99 `1.42ms`,Bridge event-loop p99 `12.55ms`,0 次 episode。
