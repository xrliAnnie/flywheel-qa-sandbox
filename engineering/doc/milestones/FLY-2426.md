# FLY-2426 — 主仓锚 PR 约束 merged gate retirement

**Status**: ⏳ Implementation complete; exact-head review and PR CI pending
**PR**: [#1119](https://github.com/xrliAnnie/flywheel/pull/1119)
**Date**: 2026-09-07

## 交付

- 根因不在 founder reaction / reply pickup：FLY-2394 的历史嵌套仓 PR `#25`
  被错当成 flywheel 主仓 `#25` 查询，在当前主仓 anchor `#1103`
  仍 OPEN 时提前把 ship gate 写成 `superseded_merged`。FLY-2381 的 `#27`
  / `#1109` 是第二个同形事故；FLY-2379 因没有 `#26` ordinary candidate
  而是阴性对照。
- current workflow ship gate 的 external-merge retirement 现在必须精确命中该
  question 的 active run、`land|runner_ship` authority、未 supersede 的
  `__main__` ship target、exact-head current node binding 和同一 PR number；任一
  identity 缺失或不等都 fail closed。
- external PR merge 不再退休任何 `founder_review` artifact-review gate；正常
  review-family / issue-done 收敛不变，无 current holder 的 legacy ship-gate 行为保留。

## 证据边界

- 严格 TDD 覆盖 FLY-2394 同号碰撞、founder-review 四种 run 状态、正确
  anchor、无 PR authority、legacy 正例和 public reconciler 调用点；两个
  focused 文件共 `48 passed`，且临时旧逻辑变异会让两个生产调用点重新变红。
- WAL-consistent 生产副本回放：FLY-2394 / FLY-2381 错号退休由
  `2 -> 0`；FLY-1687 / FLY-1679 正确 legacy 主仓 merge 保持 `2 -> 2`。
- `pnpm lint` 通过（14 条既有 warning，0 error）；`pnpm -r build` 通过。
  `pnpm test:packages:run` 不是全绿：并发隔离后只剩 retention 表数断言
  `expected 176 / received 177` 稳定红，同一红灯存在于 `origin/main`，本分支
  对该测试、retention 源码和 `StateStore.ts` 无 diff。未把它冒充本单回归。
- 与 `origin/main@fb9146878` 及 `flywheel-FLY-2427@63826997` 的最终
  merge-tree 均为 0 个冲突文件。

request-driven code review R1 在普通文档收尾前的 head `f69566298` 上
`APPROVED`，无 blocking finding；其 MEDIUM / LOW advisory 已记入实施证据并回报
Lead。含本 milestone 的字面最终 HEAD 会另开 exact-head review，并由 PR
未过滤 CI 给出最终全仓结果。

本实现没有补写 founder approval，没有修改 reaction/reply parser、
`StateStore.ts`、`plugin.ts`、approval / claim / authority ledger、`pr_head_sha`、
Runner 终局或 FLY-2427 recovery，也没有 dispatch QA、merge、deploy 或重启服务。
repository-less candidate 在 parked finalization / TURN reclaim 的残余已明示交给
FLY-2428，本 PR 不宣称这一旁路已被 contained。
