# FLY-2398 — 自动合并两周影子跑

**Status**: ⏳ Implementation complete; structured exact-head review and PR CI pending
**PR**: pending creation
**Date**: 2026-09-08

## 交付

- founder 门判决事务现在以独立 savepoint 冻结机器分类、分类依据、强度二两半与 exact
  run/repo/head。观察失败不会改变 founder 原动作,但四线报表会因缺行作废;两张影子表只追加并
  登记为 retention 保护资产。
- Lead 用自己的 Discord bot 在 Lead 频道发布固定枚举声明,再经 `flywheel-comm shadow-declare`
  登记。Bridge 从名册派生 Lead 身份并核对消息作者、bot 标识、正文、时间、频道和卡绑定;普通
  founder token 的 general fallback 与 `fallbackToken` 保持既有行为,严格 label-only 解析只用于
  shadow 路径,失败返回 503。
- `shadow-table.sql` 从独立 founder action cohort 复算四条覆盖线、N1/N2/N2-wide/N3、声明卫生、
  强度二绑定与 nested 事后发现 proxy。nested 按卡计算后按 run 以
  `hit > incomparable > clear` 聚合;每个数字都带 numerator/denominator,空全集显示
  `0 / 0 (no population)`。
- 报表固定展示 N1 与 N2 各自 `0 / 全集` 的预先放手门槛。覆盖不全、无样本或任一门槛非零都
  输出 `RELEASE_HOLD`;双零也只输出 `RELEASE_CANDIDATE_ONLY`,脚本没有任何自动合并能力。
- 包装器以 SQLite `.backup` 取得 WAL-safe 副本,随后在 `mode=ro&immutable=1` 会话执行 SQL。
  窗口必须恰好 14 天且已经跑满;`snapshotStartedAt` 在备份前冻结并不得早于窗口终点,堵住备份
  跨越期末的竞态。默认起点是 migration receipt 后第一个 UTC 零点,仍须由 founder 在真实起跑前
  确认,没有硬编码 9 月 6 日。
- FLY-2396 与 FLY-2398 的精确消费者白名单共同证明新事实只进入采集、声明和只读度量路径;
  `land-executor`、`approval-signal`、`review-hold`、`post-ship-finalization` 等 merge 授权路径不读它。

## TDD、验证与审查

- 全部行为变化按 RED → GREEN 实施。最终精确文件集为 TeamLead 13 文件 267/267、
  flywheel-comm CLI 8/8、独立报表 10/10、两组 merge-reader 隔离 3/3;TeamLead typecheck、
  `git diff --check` 均通过。
- `pnpm lint` 退出 0,仅 14 个仓库既有 warning;`pnpm -r build` 退出 0。
- `pnpm test:packages:run` 原样运行两次都未绿。第一次 flywheel-comm 2,076 全过、claude-runner
  1,105 通过/2 skip 后仅 Vitest worker `onTaskUpdate` 超时;第二次在机器负载下出现既有 CLI/E2E
  固定超时。临时 detached `origin/main@ee113cab9` A/B 同样复现 prompt overflow 与 tmux server
  失败,因此保留全仓 gate 未绿事实,没有用定向复跑冒充。
- TeamLead 全包曾得到 12,029 通过、6 skip、14 失败与 1 个 worker 超时。其中唯一由本分支触发的
  FLY-2396 白名单回归已经修复并以两组隔离测试 3/3 收口;其余是既有 5 秒、real-tmux 与真实账号
  环境失败。
- 本地 `codex:rescue` companion 在读仓前被 macOS nested sandbox status 71 拒绝,不算 verdict。
  独立 Codex 同行审查随后阻塞并促成三个 HIGH 修复:任意非 14 天窗口、窗口未跑满提前出表、以及
  期末前开始的 WAL 副本。修复后的 `ae21917ec` delta review 为 APPROVED,无剩余 blocking finding。
- request-driven 跨模型 review 已对每个修复后的新 HEAD 重新注册,但 reviewer 外部账号连续返回
  organization 403,gate 仍 pending;已通过 durable Lead 问题队列请求恢复。它不是 APPROVED,
  因此 PR 创建和节点 completion 仍等待最终 structured verdict。

## 边界与后续

- 本实现节点没有 deploy、Bridge restart、production mutation、529 founder-path QA、QA dispatch、
  ship approval、merge 或自动合并。两周 production 影子跑尚未开始;真实起点须先问 founder 一句,
  跑满后再从捕获时刻可证明的 production 数据库副本出最终表。
- 对外来预制 SQLite 副本,SQL 能独立复算所有数,但不能从文件内容反推出原始复制时刻;交付者必须
  另附窗口终点后的 capture provenance。默认 live DB 路径由 wrapper 的 pre-backup 时间栅栏证明。
- 回滚只撤采集/声明/报表接线,不撤 C0 retention 注册;已存在的表、receipt 与历史证据继续保留。
  merge 授权契约始终未改。
- 本文件是 PR 分支的 literal last commit。包含它的最终 HEAD 只在 structured review 返回
  APPROVED 后推送并创建 PR;PR CI 未绿前不宣称可 ship。
