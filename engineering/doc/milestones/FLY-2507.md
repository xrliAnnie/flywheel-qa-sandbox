# FLY-2507 — TURN 等待按当前行动责任告警

**Status**: ⏳ Pending review and CI
**Date**: 2026-09-10
**PR**: 本分支 PR，创建后由交付回执关联

非当前行动者的正常停驻不再生成 TURN handoff overdue 问题；pending 等当前行动者仍保留真实交接告警。抑制原因落在同一 turn_wait_ledger，patrol 仅排除红等待，保留原有 blocked 与持有者异常判据。新增列兼容旧 readonly 库，CLI 隔离路径不静默回落生产 StateStore。

相关测试106项通过，lint/build通过。本地完整包门两次失败（45/48项，visual-capture锁和dependency超时）；单独65+42通过。Lead c09fa970/e51b82c7授权保留原始红回执开PR，精确头CI全绿和正式代码审查通过是needs_review前提。没有部署、合并或独立QA证明。完整证据见同 issue implementation-report.md。

本文件为开 PR 前 literal 最后 commit；其后冻结 HEAD 进行 review/CI 与交付。

R1 HIGH carrier 身份遗漏已修复：approved runner_ship holder / carrier delivery 的当前承接者保留真实逾期告警。新增 RED→GREEN 回归及受影响69项通过，lint/build通过；Lead e830b49b授权定向验证，全量禁令保持。旧HEAD CI全绿；修复HEAD仍待R2正式审查和CI，本提交继续作为literal最后提交。
