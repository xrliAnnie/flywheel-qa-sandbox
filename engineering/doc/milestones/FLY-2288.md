# FLY-2288 — Linear 列表查询未知参数 fail loud

**Status**: ⏳ Pending ship
**PR**: #1086
**Date**: 2026-09-04

`GET /api/linear/issues` 现在只接受已接线的 `project/state/labels/limit/slim/projectName`；
`identifier` 或其他未知 query key 会在调用 Linear 前返回 400，不再把未过滤列表首条伪装成目标 issue。

TDD 证据：旧实现对五个未知参数场景返回 200/404，新增 guard 后聚焦套件 28/28 通过；
`flywheel-teamlead` build 与根 `pnpm lint` 通过。最终 code review 与 exact-head CI 结论记录在 PR #1086。
