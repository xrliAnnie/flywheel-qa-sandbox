# FLY-2460 — Codex 原生记忆准入蒸馏
Issue: FLY-2460 (https://linear.app/geoforge3d/issue/FLY-2460)
日期: 2026-09-09
基于: ../FLY-2460-codex-self-distillation-inert/implementation-evidence.md

**Status**: ⏳ Pending final code review and QA
**PR**: https://github.com/xrliAnnie/flywheel/pull/1139
**Date**: 2026-09-09

Codex 0.153.2 在同一家的后续 root turn 才运行原生 stage1，候选需自然闲置至少一小时，phase2 另有六小时冷却。新增同一 daemon、runner 首线程之前的准入通道，最多触发两条候选、等待不超过300秒，以本 worker 的 jobs、非空输出与 phase2 选择水位区分摘要落库和首轮可读。通道保留原 goal 预算，支持取消和失败后继续启动。

受管 SQLite flag `codex_memory_distill` 默认 ON，支持全局默认与项目覆盖，新 adapter 构造时读取。回执包含水位、墙钟、token 和文件大小可用性，不含记忆正文。只读连接不修改 Codex 表，可能创建 WAL `-shm`。本单不改 FLY-2359 运输逻辑。

最终修复 `8a22b0da9` 完成批量上限2、token读取不覆盖已证实状态、单条rollout stat失败不禁用准入、同步SQLite等待≤5秒、受管家路径判定。全部行为修复有 RED→GREEN，模块51/51通过。修复后 lint、build、typecheck通过。

全仓测试已完整执行，14包通过；claude-runner1153通过/2跳过、TeamLead12066通过/6跳过，分别因包末尾Vitest `onTaskUpdate` RPC超时退出1，无断言失败。按Lead裁定唯一一次修复后claude-runner整包复跑为46文件、1163通过/2跳过，仍有相同RPC超时，退出1。记录为已接受的测试框架问题，不称本地全门绿色、不循环复跑。单fork串行和GUI文件 `**/tmux-viewer.macos.test.ts` 排除经Lead批准，17个临时文件已恢复。

529真实E1–E6和新增积压成本场景由QA按同目录`qa-runbook.md`执行，本实现节点未声称真实首轮回忆PASS；E7按Lead裁定为“无自然人口，本单不证明”。本文件是最终评审冻结HEAD的最后提交；后续评审/PR回执只写外部workflow渠道，不移动HEAD。一个新HIGH需停止并报告。没有dispatch QA、merge、deploy或生产旧家写入。

## Attempt 2 冻结

QA A/B发现触发线程的exactly-ok基础指令被phase2继承，阻止原生记忆写入。本轮903ef568f仅删除该覆盖，实际RPC断言RED→GREEN、蒸馏模块51通过；runbook改以原生输出选择、MEMORY.md及模型首轮回执判定E4，debug prompt-input零命中不判失败。canonical账号E4交QA验证。

按Lead62288f33授权用9b2a6a5bd合入main04ff8800a，保留原修复，只手工解决run-infra.ts冲突。最新main227058c73只读merge-tree无冲突。合并后lint/build通过。全仓gate完整执行exit1：claude-runner1196通过/2跳过、TeamLead12232通过/6跳过，各有末尾onTaskUpdate RPC超时；config计数25→26遗漏已修正且registry54项聚焦通过，其他13包通过。17个执行配置已恢复；不称全仓绿色。无新增shell测试。

本文件为本轮最后提交，后续一次普通push、一轮exact-head review与CI回执写外部渠道，review期间冻结HEAD。有阻塞finding先报告Lead。未dispatch QA、PR合并或部署。历史三条限制及legacy无自然人口边界见qa-runbook；不把本地验证当canonical E4证据。
