# FLY-2049 — Bridge 日志轮转真运行

**Status**: ⏳ Pending ship
**PR**: #955
**Date**: 2026-08-25

Bridge 不再长期持有 launchd 打开的 `/tmp/flywheel-bridge.log` file description：应用 stdout/stderr 改为进程内同步 short-FD append，写前按 10 MiB 阈值轮转并保留 `.1`/`.2`/`.3`。attempt 2 把日志层统一改成 availability fail-open：unsafe generation 改名为 `.corrupt.<pid>.<ts>` 后继续轮转；5 分钟 stale directory/ordinary-file lock 都可 identity-checked reclaim；连续 `rotation_stalled` 只停 rotation，active 仍继续 short-FD append，Bridge 不退出。有界 `0600` marker 保留到人工清除，外部 probe 在线首报、默认每小时重醒。canonical wrapper/daily 的 raw capture 若不安全也不碰目标，改经 `/dev/null` 继续启动。R4 trial、告警 copy 和 generation-aware readers 已一起接通。范围边界明确：68 MiB cmux watcher 与 16 MiB Codex infra-bot Lead 日志不套用 Bridge 的 Node adapter，由冻结族单 **FLY-2056** 分两节承接。

正向验收不是配置检查：fresh 真实 Node producer 在 PID 64968 保持在线时跨过故意压低的阈值，`.1` 生成 528 bytes、active 回落到 367 bytes，active/archives 均无 producer-held FD，轮转后 sentinel 继续进入 active；oldest→active 重组 `895 == 895` 且 `cmp` 完全一致，normal raw-startup 为 0 bytes。uncaught fixture 同时保留主日志 138-byte stack 与 raw 234-byte stack；module-resolution failure 进入 truncate-on-start capture。config 673/673、log rotation 14/14、packaged seams 17/17、probe 31/31、wrapper 18/18、R4、launchd 3/3 全绿；全仓 lint/build 通过。`pnpm test:packages:run` 只在未改的 core real-Terminal/osascript 两条用例因 runner GUI/XPC 不可用失败，没有伪报 PASS，明细在 research §6.5。

首次部署时现有约 106 MiB active 会先原样保留为 `.1`，第一条新日志进入小 active；按 2026-08-25 观测约 20.9 MB/day，默认三代约 1.5 天/三次轮转后自然淘汰。部署瞬时磁盘峰值约 136 MiB，无需也不得在线手工删除/移动日志。若 marker 报 `rotation_stalled`，Bridge 仍在线且 active 继续增长：operator 检查 fresh lock/权限/I/O 根因，确认安全后在正常部署窗口重启 adapter，再核验 `/health`、`.1` 与 active；unsafe generation 已自动 quarantine，stale ordinary-file/directory lock 会在 5 分钟后自愈。R1 四条 MEDIUM 已逐条处置：marker 不再 boot-clear；short-FD syscall cost 作为正确性 tradeoff 接受并留待实际 profiling；unsafe generation 与 stale lock 两条可用性风险已修。side-log 后续仍是 **FLY-2056**，本 PR 不混入其 Bash/launchd 与 TUI stream 生命周期改造。
