# FLY-2459 — Codex 部门 Lead 派管 runner 与受控迁移

**Status**: Engine conflict rework R1 HIGH fixed; awaiting R2 code review and exact-head CI
**PR**: https://github.com/xrliAnnie/flywheel/pull/1162
**Date**: 2026-09-11

## 交付

- 显式 codexRunnerActions 开关与共享能力解析；六个派管 runner 工具接入两个既有 MCP 入口，保留严格 schema、调用时身份和能力复验、写租约、作用域幂等及 FLY-127 部门闸。
- 受控手动迁移工具贯通静态 preflight、停止旧 owner、配置/manifest CAS、Discord cutoff/unresolved 交接、generic preflight 与失败条件回滚；窗口外只读因果核验和成功 intent 原子退休。
- 管理台只读迁移指引和实际配置展示，区分配置与生效；不开放在线跨厂商写入。
- R1 两条 HIGH 在 92698c90c 修复：旧 Claude carrier 按 exec 后 tmux argv 验证，新 Codex carrier 接受 generic launcher 的 scripts/../dist 拼写。参数捕获测试执行仓库实际 exec 语句，以临时替身验证参数，不冒充生产进程 proof。

## 验证

- 本地 pnpm lint、pnpm -r build exit 0；R1 修复后迁移相关 6 文件/45 tests passed、四个新增 shell suites exit 0。
- 原完整变更集：config 27、comm 207 passed/1 skipped、TeamLead 430；另 start-e2e 42、DAG entry 57 passed。
- pnpm test:packages:run 仍为 exit 1（onTaskUpdate timeout），遵从 Lead 裁定保留，不用定向通过替代聚合绿色；最终精确头 CI 待 PR 核验。
- R2 对 53fefb6fd APPROVED，无 HIGH；五条非阻塞代码审查 advisory 只归档于 code-review.md，未扩大实现范围，未改 pinned plan。
- 首轮 PR CI 的四处本单清单/夹具遗漏已修复：CI step inventory、canonical capability stub、kill-path 三项登记、打包路径审计行。定向结构/枚举、canonical 11、kill inventory 5、package-onboard 28 均通过，lint exit 0；原 CI run 34590690444 已结束：四个底层失败、其余九项通过（含三个 TeamLead 分片），汇总 CI OK 为红；该历史红灯不改写，最终头仍需新 CI。

## 交接边界

本提交刷新最终交接里程碑，并保持其为分支 literal last commit。审查通过后由 implement 走 needs_review，QA 由 DAG 推进。真实浏览器只读指引/XSS/无写入口验证交 QA；Honey Lemon 生产切换、真实频道 @ 派单及授权班车后复验需后续独立授权窗口。本节点没有改生产 registry/env、重启服务、请求 ship、合并、部署或派发 QA；未修改 implement 模板或 CLAUDE.md。

详情：engineering/doc/FLY-2459-codex-department-lead/{plan,migration-decision,implementation-evidence,code-review,honey-lemon-cutover}.md。

## 最终 R4 批次

4d6841bc0 补精确 residue/retention 消费登记、未发布 runner-start 导出别名、静态 href 审计行及真实打包 auth-link 夹具。扫描规则和生产 preflight 不变，新增正反例覆盖路径、域、仓库访问及未链接 auth。完整本地 Quick Gate exit 0；真实 packaging smoke 24/24、相关行为55/55、Gate4 masking13/13通过。R3 对 ebf8fc50d APPROVED；该头 CI34592040517 的两项底层失败仍按历史红灯记录，其余11项绿。Lead 授权最终 R4（不自行 R5）；本里程碑提交后一次推送并冻结，最终头需 review APPROVED 与 CI全绿后才 needs_review/park。


## 冲突返工 attempt 2

基线 ec6921379110965f3aac99918e72f04d23d08ddc 的 CI34594328858 全绿；落地主干冲突触发 engine rework。404b85aa9 合并 origin/main ca869ad6d，保留 main 收件人解析和本单稳定 ID/原子发送语义；c8bdbc43e 更新实际 session receipt 夹具，不放宽校验。RED6 → send/recipient28 GREEN；action/context/config40、真实编译 MCP 双入口子进程2、四个新增 shell suites 全绿；lint/build exit0。全包运行 exit1（两个 config 扫描超时），定向38通过但不代替聚合绿；最终精确头 CI 为准。

本里程碑保持 literal last commit；推送后冻结并登记本次返工 R1 code review。只有新头 APPROVED + CI绿才 complete needs_review PR1162，交 rework_verify/QA 重验及新的 founder gate。若 completion 409 清单含 park_wake，按 Lead dc97e1ab-223d-4eef-996f-6efc8dfa6dd1 原地报告，不 park、不重试。未操作生产。


## Rework R2 — migration wave disposition

R1 对 a34478e97 返回 CHANGES_REQUESTED；该头 CI34645477308 全绿。仅修 HIGH `migration-intent-wedges-updater-wave`，提交 e5929ae6a：以本轮 checkout/原 restart owner/admission authority 作只读检查，现场证明原 source 完整未动、回滚 source 已恢复或已部署 target 完整存活后返回 skipped，普通 wave 包含目标与其他 Leads；迁移写入仍严格绑定计划 SHA。中断/未知/漂移/失权继续拒绝，不增加重试或生产操作。七条 MEDIUM/LOW 仅归档。

TDD: coordinator RED5/2 → GREEN；五个 migration coordinator/authority/source/activation/window 文件共40通过，含同轮 preflight 失败回滚、陈旧 SHA 无写入、后续部署不重放迁移、source/target 负例。四个新增 shell suites exit0，wave 测试证明 skipped 后目标及其他 Lead 均走普通重启；lint/build exit0，最终定向40及 lint再次 exit0。日志 `/tmp/fly2459-r2-{disposition-red,focused-final,lint-final,build,lead-backend-migration-wave}.log`。本轮遵从 Lead 不重跑宿主全包，最终精确头 CI 待验证。

本里程碑为最后提交，随后一次 push/freeze/R2。R2 APPROVED + 新精确头 CI 绿后才 needs_review PR1162；R3 仍是 Lead 给定上限。保留原 completion 409/park_wake 禁止 park/重试规则。
