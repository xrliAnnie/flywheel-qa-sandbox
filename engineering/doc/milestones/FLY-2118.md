# FLY-2118 — Lead 巡检只看自己的 Runner

**Status**: ⏳ Pending ship
**PR**: #978
**Date**: 2026-08-28

Lead patrol 的可见面改为 owner-first：每个 Department Lead 只枚举、capture 和报告自己名下的 canonical runner pane，不再先扫全机再做跨界过滤。无主 pane 由 Bridge 的专属 sweeper 继续兜底并只路由给 Claw；巡检规则、Claw 身份合同和实现已同步，Step B 人工根因判重保持不变。

QA attempt 1 发现的 FLY-1560 family-word residue 已移除；Claw alert sink 不可用时现在会明确失败，不再把未送达事件写入 30 分钟 cooldown。QA attempt 2 发现的 tmux 3.5a 兼容问题也已修复：pane 身份改用实际存在的 `pane_pid + session_created`，格式漂移会明确失败，同时只有 canonical runner pane 才进入严格校验，普通 tmux window 不会拖垮孤儿兜底。修复后的 code review R8 已 APPROVED。
