# FLY-2074 — Raya 按需实时语音流水线

**Status**: ⏳ Pending ship
**PR**: #964 (Flywheel docs) · xrliAnnie/raya#2
**Date**: 2026-08-27

在 Raya foundation 上交付独立 `apps/voice` 进程,把 FLY-1911 原型重写成正式的实时语音管线:Discord receiver/uplink、Codex App Server realtime v2、downlink/player 两条 20ms 常开流,有界 jitter/write queue、显式静音、等待音混合、generation-safe 协调器、三腿失效语义、文字状态行、原子会话状态与真实 context-usage 埋点。进程内 rejoin/reconnect/resume 仍严格遵守 founder 8-20 禁令;运行腿失败持久化后 exit1,由 launchd fresh 拉起并明确写「记得:否」。

Founder 8-27 将运行模型改为按需:平常 voice 进程不运行;founder/明示 allowlist 只在 `#raya` 精确发「进入语音模式」时写 0600 `voice-mode.requested` 并 kickstart。voice plist 是 `RunAtLoad=false + KeepAlive.SuccessfulExit=false`;最后授权人离房或文字 stop 时先清 marker、发「我下线了」、drain/stop Codex、exit0,launchd 保持停止。`#raya` 是文字/状态频道;语音复用 founder 现有且当时唯一的 `General` Voice Channel(`1485787273193853170`),没有新建语音频道。

真机证据与失败一并留证:

- founder 用精确最小权限 OAuth URL 邀请 Raya(receipt `1542408908239536138`);guild/text/voice 有效权限逐项验证。
- production brain/voice plist 从 `~/Library/LaunchAgents` 真注册。最终 E2E 结束时 voice `runs=8 / last exit code=0 / state=not running / no pid`,marker absent。crash-relaunch 的可证明边界只到这份现场 attestation,不得写成日志仍可重放:`Lead 2026-08-27 00:01 PT hand-verified crash-relaunch (57192->13352, runs 2->3); logs since truncated by restart, not re-derivable; a fresh in-General re-run is deferred to a founder-approved window, NOT run to avoid surprising her.`
- founder 明确让 QA 执行后,QA 在同一 voice runtime(`67bd68c@07:19:33Z`)的 13 分钟窗口 `07:29:37Z–07:42:06Z` 连续触发六轮。**用户可见“一问一答”只有 1/6,3 轮硬超时**;不能只引用 r6 写成 clean pass。
- 受控 QA bot 必须同时进入 text trigger 与 voice QA 两个 allowlist,Raya 自身 bot id 永远拒绝。验收后临时 QA allowlist 已从 mode-600 production env 删除,brain 重载为 pid 16997;voice production preflight 仍 ready。

六轮原始结果(完整 JSONL、hash 与机器可读汇总在 `engineering/doc/FLY-2074-raya-voice-pipeline/evidence/`):

- r1 07:29:37Z:FAIL `Raya text reply timed out after 20000ms`;ack 约 27.6s。
- r2 07:30:34Z:harness passed,但 572 packets / 1,716 bytes = 每包 3 bytes 纯 Opus silence;无 transcript/回答;ack 31.6s。
- r3 07:33:14Z:FAIL realtime ready timeout 120s。
- r4 07:36:35Z:FAIL realtime ready timeout 120s。
- r5 07:38:57Z:harness passed,但测量点 959 packets 中 129 non-silent、peak 2,107(约 6%);只有 bed/wait audio,无 assistantTranscript/回答。
- r6 07:41:29Z–07:42:06Z:唯一完整 pass。start `1542438894472138852` → 状态 `1542438902722334770` → user/assistant transcript;内部 `sent=1423 / voice=258`;独立 ear 收到 5,325 包、2,425 non-silent、peak 32,768;stop 后 Raya 发「我下线了」。

Lead 07:27 口述 launchd `runs=4`;六次 trigger 后最终 `runs=8`,所以有两次 trigger 没产生新 launch。代码与 launchctl 文档确认「无 `-k` 的 kickstart 可 zero-exit/no new launch」机制并匹配现象;**把缺失的两次具体归到 r3/r4 是 inference,不是直接测量**。本次代码将 start 改成 `launched | already_healthy | requested_but_down` 三态:只有观察到 running + pid 才回启动 ack;zero-exit/no launch 保留 marker并进入 requested-but-down 可见面。

旧 Raya `9487b19` / 174 tests / gate `e6771f8d-1d4f-4f77-af30-e943ce04a927` 已过期;foundation merge 后 `7ca0b751` 的 review `9aa4dd4d-f304-4ffc-8184-c9069e41afeb` 明确 CHANGES_REQUESTED。QA rework 的 Raya exact head 是 `b782260ffd3a74a3fd233d246e41a9723c1d88f7`,通过 lint/build/typecheck 与 contracts 22 + brain 58 + voice 103 = **183 tests**。本 milestone 是 Flywheel PR 的最后一笔 commit;之后两仓 head 冻结并注册 fresh exact-head review,审批以外部 gate record 为准,不在 gate 期间用 progress/doc commit 移动 head。merge-time checklist 仍要求 Raya PR #2 合并后从稳定 `~/.flywheel/raya/code` 重新 build/install,确认 plist 入口离开 feature worktree,验证 brain running/voice stopped 后才删 worktree。

**相邻耐久性 gap**:Discord 侧 30 分钟静默 soak 尚未执行、数据点为 0;只保留此前未触及的 Codex-side ≥30 分钟证据。它与六轮 1/6 flakiness 相邻披露,两者不混写。未 merge/ship,也未请求紧急重启票。
