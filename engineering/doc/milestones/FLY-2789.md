# FLY-2789 — 按节点记录模型、组别、用量、耗时与 QA/founder 成绩

**Status**: ⏳ Pending effective code review and QA
**Date**: 2026-09-23

交付前必须由有效代码评审批准当前精确 HEAD；先前 HEAD 的 verdict 不向后继提交继承。

本单新增只追加、可追溯的 workflow scorecard：每个 activation 记录精确模型、设计/
实现/QA 维度的 arm 与 policy version，每个原生 Claude/Codex turn 记录 token 和节点
耗时；QA 首过、founder 打回与 proven-dead replacement 均用稳定事件身份幂等落账。
同一单跨 run、返工、换体或 identifier/UUID alias 时先归并为一个 issue cohort，既不
重复计数，也不漏掉后续执行。

分组报表把 assigned、unassigned、unknown、mixed、degraded 与异常证据分开；降级单
不计入原组。只有终态且用量完整的 cohort 才进入「每张一次过 QA 的单平均额度」和
provider pool 百分比，缺失或不支持的原生计数不会伪装成 0。Claude 与 Codex token
保持各自口径并明确不可跨供应商直接比较。Lead 入口是只读 CLI，支持逐单明细、分组
汇总、时间窗、QA eligibility、分页和外部容量配置。

本地验证：lint 通过（25 条既有 warning）；13 个受影响 workspace project build 与
9 个 core dependents typecheck 通过；定向测试 TeamLead 74/1 skipped、runner 315、
flywheel-comm 1；`vitest related` 为 TeamLead 7893/4 skipped、runner 434/2 skipped、
edge-worker 345、core 23/2 skipped、flywheel-comm 1；runner shell guard 46/46；
`git diff --check` 通过。代码评审 R1 的两项 importer 修正另有 focused 23/23、
TeamLead+依赖 build 通过；最新 TeamLead related 为 564/565 文件通过（7883 passed /
4 skipped / 1 unrelated failed），唯一失败的 chat-thread 文件独立重跑 68/68 通过，
不把该 related 运行写成全绿。代码评审 R2/R3 再补 production synthetic 行缺省
`requestId`，并让 Codex delta 同时兼容跨 turn 延续累计与跨 turn 重置、继续拒绝同一
turn 内倒退。R4 再修复真实 Codex rollout 在同一 `turn_id` 中刷新 `turn_context` 时
误报冲突、回滚整份 session 用量的问题；稳定 execution/activation/source generation
一致时幂等保留首次时间和 offset，真实身份冲突仍 fail closed。直接测试 22/22，最新
TeamLead related 565/565 文件全绿（7884 passed / 4 skipped），TeamLead+依赖 build、
改动文件 Biome 与 `git diff --check` 通过。生产
`teamlead.db` 只读 dry-run 返回
`unavailable_schema`，证明未部署 schema 时诚实拒绝且不迁移；这不是生产功能验收，
也不是 exact-head CI。

R5 曾批准旧 HEAD `763b1368c036f04fe3f57789d3ee5179aa5999ae`，但 QA 的
exact-head CI run `35855834994` 暴露三项确定性遗漏，因此该评审不再覆盖当前 HEAD：
retention consumer gate 未登记 scorecard 对 `workflow_run_event` 的只读依赖，配置真值
未把每次执行身份 `FLYWHEEL_WORKFLOW_ACTIVATION_ID` 列为非 flag，以及 scorecard 直接
读取了 founder 原始 verdict 表。修复提交 `64a60e3d7` 分别登记 protect consumer、补齐
非 flag 说明，并只从既有派生表 `ship_judgment_outcome` 读取 founder 成绩；未扩大
FLY-2396 的八文件 allowlist。Founder 打回只按 `founder_verified` 的唯一 rework
`outcome_id` 计数，as-of 以 `decided_at` 为准而不使用可能延迟数日的 `observed_at`；
已有 gate holder 但截至 as-of 没有 eligible outcome 时明确为 missing，不伪装成零。

本轮 rework 的精确验证：scorecard 与 FLY-2396 focused 9/9、最终 TeamLead
`vitest related` 8/8、config related 326/326、retention 测试 10/10 且实际 gate 返回
`ok: true`；root lint 退出 0（仍为 25 条无关既有 warning）；TeamLead 与 12 个依赖
build、config 与 12 个依赖 build、TeamLead 及 config 的所有 workspace dependents
typecheck、`git diff --check` 均通过。用每个改动文件的完整路径、文件名和父目录执行
`git grep -lF` 后，保留 scorecard CLI、FLY-2396 boundary、config drift/final-ledger 与
retention gate 等直接消费者验证；排除只记录历史的 engineering/product 文档、自引用
和仅因通用父目录命中的非消费者。本轮没有新增 `scripts/__tests__/*.test.sh`。旧 CI
同时出现 qa-fly-1986 计数和 tmux dead-pin marker 两项无关波动，本提交没有改动它们；
QA 仍须在新的 literal-last HEAD 上重新冻结并运行完整 exact-head CI。

实现没有改分流算法、生产配置、QA/ship 判决，没有 dispatch QA、merge、ship、deploy
或重启。冻结 HEAD 的完整 CI 与隔离模拟验收仍由 QA 节点负责。本文件是 PR 的
literal-last commit。
