# FLY-2390 — 判据 c 版本信号聚合

**Status**: ⏳ Pending ship
**PR**: #1156
**Date**: 2026-09-10

按批准计划聚合 FLY-942 原始观测、版本身份、watchdog heartbeat/源健康、版本 bug 与 Annie 日报反应，产出带完整理由和证据的 green / hold / unknown。增加原子观测/游标、bug intent 恢复、日报发布意图与反应扫描、受保护审计表和 14 天原始数据保留。B6 节奏分频仅消费，未实现。

回放夹具提供三态及确定性 HTML 日报，结构检查验证全部证据区块和 512KiB 上限；实际 Bridge app 挂载 HTTP 集成验证 200、HTML 与版本响应头。**visual acceptance deferred to QA**；未部署、未安装 launchd、未向真实 Discord 发送。

lint/build 通过，新增 shell suite 及相关定向测试通过。本地全包 exit 1：TeamLead 980 文件通过、1 个既有归档预算断言失败（100/64），另有 onTaskUpdate RPC timeout；仅一次隔离复核 4/4 通过，保留全包失败。详见[实施记录](../FLY-2390-criteria-c-aggregator/implementation.md)。精确 HEAD review、CI 与 QA 验收仍需独立取得。

R1 修复：按 Lead 授权 defe9584 移除 evaluator 的仓库脚本越界导入，retention registry 复用包内常量；heartbeat 改为摄取后 outbox 计数，正常日报落地不再自污染；补 PATH 登记。两项回归先红后绿，定向55/55、PATH21/21、lint/build通过。本地全包修复后仍失败：981文件通过、既有 quota-probe 8失败及RPC timeout；完整记录保留，不再重跑。新 HEAD R2、CI 与 QA 视觉仍待验证；visual acceptance deferred to QA。

R2在820fc9af2有效APPROVED；随后CI暴露release-contract不在payload闭包。按Lead授权6a0e650d补包清单、已登记原生JS资产与精确文件白名单，保留缺入口/未登记/TypeScript拒绝。打包hermetic33/33，真实npm pack/install、包导入、Bridge /health及Lead链路smoke24/24，lint通过。最后R3与新精确HEAD CI待验证；本地全包红和QA视觉边界仍保留，不作部署或验收通过声明。

技术合并main bece7de15：只解决StateStore相邻import并保留双方逻辑。build/tsc/lint通过，TeamLead416+Comm139项定向回归及retention consumer gate通过；批准计划字节不变。合并头须重新取得exact-head review与CI；历史本地全包失败和QA视觉边界不变。

Engine attempt 2 技术合并 main ca869ad6：StateStore 顶部 readiness/session-terminal imports 均保留，无新功能。build/lint/tsc、TeamLead 98 项、Comm 17 项、两套新增 shell、retention 与日报结构验证通过；依 Lead 8fbc920c 不重跑宿主全包，历史失败保留。推送后冻结，重新取得 review 与精确 HEAD CI；QA 重测由工作流接手。

Replacement 3 恢复具名 stash 0ec8e711，无冲突；按 Lead d3fbd3cf 裁定修两条 R1 HIGH：shell/Bridge 的 STATE_DIR 根语义统一，零 publication 或无反馈频道 fail-closed unknown。回归旧代码6项红，修复后50项绿，两套shell绿，lint/build exit0。计划原文未改，补充裁定见 amendment-founder-signal.md。本头等待 R2 与精确头 CI；历史全包红不改记绿，QA重验和fresh approval由下游负责。

Rework R2 新 HIGH：同秒迟到且排序较小的 shell 事件会被游标漏掉。改为现有只追加 claims 表 rowid 顺序；旧游标列升级后幂等重放，观测时间保留。RED 复现两条遗漏；53项定向测试与旧表升级/reopen、501/5001分页、原子回滚通过；lint/build和retention gate通过。非阻断建议归档不扩。新头需最终R3及精确头CI；旧头CI随机临时路径hi断言失败保留（未改voice-core，隔离11/11）。

Engine attempt 4: merged main 26ebc4931, preserving both import and retention registration groups; merged registry/fixture assertions are 155 protected-current, 219 total. No new feature behavior. Locked dependency sync resolves initial missing-yauzl build/load failures. lint/build, 86 readiness/retention tests, 99 isolated Comm tests, two task shell suites and report structure checks pass. Initial full package run failed (CLI timeout and stale compiled validator); rebuilt full suite is running and final receipt belongs in PR/report. Pinned plan unchanged. Fresh head review/CI required before needs_review #1156; visual acceptance deferred to QA.

Engine attempt 5: merged main 7ed515939 with only three manual conflict resolutions, preserving both StateStore imports and both release-contract/phase-protocol packaging assets. Plan unchanged. lint/build, 86 focused tests, 33 packaging tests, two task shell suites, retention gate and report structure pass. Full package run exit 1: config ledger two 15-second timeouts; one isolated rerun repeats them (9 passed), unchanged file. Original failures retained; no further rerun or adjacent fix. Fresh exact-head review/CI pending; needs_review #1156 follows review. Visual acceptance remains QA-owned.

Attempt 5 final CI disposition: first run only onTaskUpdate RPC error; one authorized rerun then failed DirectEventSink.test.ts:1605 (4091 passed, one failed, one skipped). Single local isolate 65/65 passed; source/test unchanged. Lead 6351c7bf authorizes needs_review with red receipts, no third rerun or code change, separate flake follow-up. implementation.md contains final receipts; this final documentation-only head needs refreshed review.
