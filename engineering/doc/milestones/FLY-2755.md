# FLY-2755 — Script Tests 六片再平衡与 mailbox 输出隔离

**Status**: ⏳ Pending effective code review and exact-head CI
**Date**: 2026-09-19

Script Tests 从五片扩为六片，105 个既有 Test/Integration named step 以完整
YAML 对象重新分配为 15/15/15/31/14/15 项。六片保留相同的独立 setup、20
分钟 cap、85% FLY-1870 tripwire 和 `if: always()` 收尾；没有提高阈值、关闭
tripwire、删减套件或弱化断言。`script-tests` 仍使用 full-history checkout，
其余五片保持 shallow checkout。CI OK 的 needs、heavy job 清单、required-jobs
manifest、结构守卫及 FLY-889 冗余守卫均已纳入第六片。

结构回归先在旧 workflow 上精确红于缺失 `script-tests-6` 及 CI OK 聚合，再随
最小 workflow/guard 改动转绿。before/after 多重集合对账确认 105 个测试步骤
无缺失、无重复、无内容漂移；shell census 仍登记 337 个 shell suite（285 CI、
53 manual），tripwire 自测 36/36 通过。历史四轮按实际 step timestamp 回放的
六片保守投影最高为 734s；最终 `usage ≤70%`（1200s cap 下 elapsed ≤840s）
只以本 PR exact-head GitHub CI 六条 tripwire 日志验收，不把投影当作绿灯。

Script Tests 1/5 的 FLY-1572 `exit 5` 已定性为测试 harness 的输出通道回归，
不是 mailbox migration 业务回归：成功迁移会把 `[slow-sql]` 写到 stderr，旧
测试用 `2>&1` 与 JSON stdout 合流后再交给 `jq`，因此出现
`Invalid numeric literal at line 12, column 10` 并以 jq 的 exit 5 退出。回归
夹具先稳定复现该红点；修复后只把 stdout 交给 jq，stderr 单独保留并回放，
同时显式保留 CLI 非零退出码。迁移状态、权限修复和 sidecar negative guards
均未改。修复后的 shell suite 在完整 aggregate 前后各连续 3 次通过。

首轮代码审查指出三条 full-CI/ship guard 仍把 heavy jobs 固定为 13，且
mailbox shell harness 在迁移命令失败时会吞掉捕获的 stderr。修复把三条守卫
同步到 14 个 heavy jobs，测试 fixture 改为读取仓库的权威 required-jobs
manifest，并给 migration result reporting 增加非零退出码与 stdout/stderr
证据保留回归。没有改变 CI 阈值、迁移实现或业务断言。

审查修复工作树上的本地验证已通过 `pnpm lint`、`pnpm -r build` 和完整
`pnpm test:packages:run`。最新 package gate 收据记录基准头
`d9409baab52500fa7de30fffb7b6fa8f17c4af26`，19 个 workspace 全部
`status=passed`、零 assertion failure；其中 Flywheel Comm 为 209 files、
2784 passed、3 skipped，TeamLead 为 1500 files、17842 passed、7 skipped。
聚焦的 CI structure、FLY-889、shell enumeration、tripwire、full-CI reuse 与
mailbox 套件也全部通过。

Lead 触发的 PR 旧头 `d34878b3a5ea65b324a39dd6a665f0c395e905b5`
重跑（run `35426253660`, attempt 2）17 jobs 全绿。六片 tripwire 实测依次为
581s/48%、704s/58%、707s/58%、525s/43%、658s/54%、707s/58%；最高
707s/58%，满足 ≤840s/≤70%。FLY-1572 migration shell suite 已在新 5/6
分片打印 `migrate-fly1572-mailbox: PASS`。这份旧头结果证明分片设计容量成立；
审查修复提交后的自动 exact-head CI 仍须单独全绿后才能完成实现交接。

实现节点没有 dispatch QA、merge、ship、deploy、重启服务或修改生产状态。
本文件是 PR 的 literal-last commit；推送后只对该精确头取得有效代码审查和
自动 exact-head CI。最终 CI 证据通过 runner report/完成收据绑定，不再为回填
运行结果移动已审查头。
