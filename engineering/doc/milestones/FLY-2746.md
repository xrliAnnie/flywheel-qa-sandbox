# FLY-2746 — Ubicloud CI runner switch

**Status**: ⏳ Pending review
**PR**: #1271
**Date**: 2026-09-21

Changes every job in `.github/workflows/ci.yml` (10 jobs at implementation;
11 after syncing main's FLY-2755 six-shard split) to the reversible repository
variable `CI_RUNNER`, and every job in `.github/workflows/ship-on-comment.yml`
(3 jobs) to its own reversible variable `SHIP_RUNNER`, both with `ubuntu-latest`
as the unset fallback. Ship stays on its own knob because `issue_comment`
workflows only load from the default branch and its `gh`/`jq`/`timeout`/
`actions/github-script` dependencies are unproven on Ubicloud; the switch
condition is recorded in the PR body and `scripts/ci-ubicloud/README.md`.
No test, matrix entry, permission, job condition, or release guard is removed.
Two Lead-authorized test edits, no assertion changed: the `start_body` wait
cap in `scripts/__tests__/fly1697-v2-lease-body.test.sh` goes from 10 s to
30 s, and `scripts/test-cmux-sync-hooks-integration.sh` stops suppressing
Scenario F setup stderr and waits (≤2 s) for the isolated tmux server to exit
after `kill-server`, both surfaced by running this CI on `ubicloud-standard-2`.

Ubicloud Canary run `35395475216` succeeded: every non-skipped payload job,
including Quick Gate, was assigned a real `ubicloud-standard-2` runner. The
runner-variable test, combined workflow-startup suite, CI structure contract,
and release-workflow structure contract are green. The implementation head did
not set `CI_RUNNER`; QA set `CI_RUNNER=ubicloud-standard-2` on 2026-09-19 and
this head (synced to main e82193650) runs its exact-head CI on Ubicloud.

After merge, the successor must capture the first real PR's all-green per-job
Ubicloud runner evidence, then delete the variable and capture a subsequent run
returning to `ubuntu-latest`, and set `SHIP_RUNNER` only after a dependency
receipt exists on the Ubicloud label. This PR
does not merge, deploy, dispatch QA, or modify Ubicloud account settings.
