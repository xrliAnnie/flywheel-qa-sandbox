# FLY-2051 — Claude 切号通知按 kind 路由

**Status**: ⏳ Pending ship
**PR**: #956
**Date**: 2026-08-25

quota-monitor 的 `account_switched`、`account_switch_degraded`、
`quota_switch_confirmation` 改走环境变量配置的 `#flywheel-notification`，保留 founder mention；
其余告警继续留在 `#flywheel-alerts`。切号正文包含切前/切后真实 identity-anchor email、
5h/7d/Fable 的 used/left 与 `MM-DD Ddd HH:mm` PT reset；正文按 founder 终裁不显示 pane revive
状态，内部 revive 行为保持不变。配额区使用纯 ASCII 等宽表，used/left/reset 的 raw 与 wcwidth
列位均钉为 8/15/22，最长行 37 字符并通过 Lead 窄宽审计。正向与阴性路由均已真实 Discord
终点回读验收。
变更随下一班 `00:00/12:00` updater 重启后生效，不触发紧急重启。
