# FLY-2364 — 生产管理台 8 条 polish 回合

**Status**: ⏳ Implementation + QA ruler rework complete; exact-head code review pending
**PR**: [#1096](https://github.com/xrliAnnie/flywheel/pull/1096)
**Date**: 2026-09-05

## 交付

- 设计结论保留唯一生产渲染器 `fleet-console-html.ts`；没有新增
  `console-next-html.ts` 或 `/console-next` 路由，因此不会留下两份长期漂移的管理台。
- 在生产渲染链路补齐项目栏重复标题去重、Lead 模型共享列头与 Infra 归组措辞、DAG
  模板绑定标识、后端 `loop_when` 中文语义、前端 loop 名称/id fallback 与仅在整数上显示的
  次数。既有 A1/A2/A5/C7/C8 行为继续由当前生产渲染器直接复测。
- Round 1 review 指出的窄屏回归已回修：共享列头隐藏时恢复每行可见标签，三个模型下拉均有
  `aria-label`。没有触碰 S 区结构项、新板块、新写路径、`CLAUDE.md`、Bridge 进程或部署。
- QA claim 870 的返工只修改验收尺子与 README：新增 fixture/production 双模式、真实 manifest
  守卫、逐项三态结果和部署后 `--require-all` fail-close；产品代码与测试期望均未改变。

## TDD 与验证

- RED：对照尺子精确报 `B6,D,A4,A3`；窄屏回修先新增失败回归，再以最小 CSS/markup
  改动转绿。
- 返工后的 fixture 臂仍为 11/11 `PASS`；真实 `:9876`（Bridge `79f6fc3`）为 6 `PASS`、
  5 `PENDING_DEPLOY`、0 `FAIL`，而 `--require-all` 在班车部署前按预期退出非零。C7 合法标签
  从 `fleet-console-html` 构建产物的 `LOCK_KINDS` 静态提取，不用页面 DOM 自证。
- 聚焦管理台回归 5 文件 64/64 通过；`pnpm -r typecheck`、`pnpm lint`（仅仓库既有
  14 条 warning/info）和 `pnpm -r build` 均退出 0；没有新增
  `scripts/__tests__/*.test.sh`。
- 精确 `pnpm test:packages:run` 执行时，request-driven reviewer 同时持有机器级
  `/tmp/flywheel-proofshot-port.lockd`，使 `flywheel-comm` 的 47 个 capture case 一致报
  `ELOCK_TIMEOUT`；相关文件与 `origin/main` 字节一致。reviewer 结束并正常释放锁后，同一
  `visual-capture.test.ts` 单文件 65/65 通过。其余观察到的全仓失败也只出现在未改动的既有
  macOS/tmux 或高并发时序测试；对应隔离复跑均通过，唯当前 tmux 对含 TAB 窗口名的拒绝可在
  `origin/main` 原样复现。
- request-driven cross-family code review Round 2 在
  `27d469ae3d2f626098909a13ed5e359ebef331fe` 上 APPROVED；LOW advisories 已通过正式报告通道
  交给 Lead。包含 production-aware 尺子、本 milestone 的字面最终 HEAD 仍会单独送审。

## Playwright 对照证据

生产渲染函数的只读 fixture 为 `11 项 / 0 红`：项目栏分组标题 8→2 且 6 个项目均可见；
工程/产品 DAG 各 9 节点；design/implement/qa/generic 四种样式可区分；7/7 DAG 模型行标出
「模板绑定」；Flags 高 0.93 屏、可见 CLI 重复 0、硬编码「四种」0、浏览器错误与非 GET 请求
均为 0。窄屏实测共享列头隐藏、每行标签恢复、3 个下拉有独立字段名。

结构化数字、可复跑脚本与截图位于
`engineering/doc/FLY-2364-console-polish-backport/evidence/`；真实生产的预部署数字和六张截图在
`prod-9876-predeploy-2026-09-05/`。implement 节点按边界不重启或部署 Bridge，不虚报现有
`:9876` 已更新；部署后由 QA/Lead 运行 README 中带 `--require-all` 的命令完成八项全绿终验，
并把最终截图收入 ship report。
