# FLY-2873 — Codex 测试房附件读取链路修复

**Status**: ⏳ Pending exact-head code review, scope CI, and QA
**PR**: #1324
**Date**: 2026-09-25

529 测试房的 full-access Codex Lead 现在默认使用 bridge 出站；部署在启动 Lead 前生成并校验
Bridge URL/API token，并把同一组规范变量传给 Bridge 与 Lead。测试房同时把 carrier evidence、
assertions 和 receipts 隔离到 slot 目录，避免多个 room 的 FleetPoller 争写共享 HOME 状态、让
本来有效的 slot carrier 间歇性变成 `carrier_expired`。显式 direct 仍作为阴性对照，companion
配置仍默认 direct。

相关 shell 回归、四个附件/Lead/Bridge Vitest 文件、lint 和受影响 workspace build 已通过；
生产 launcher/runtime/附件路由文件与 `origin/main` 的 blob SHA 一致。本阶段未开拆 529 房、
未改 bot1 权限、未跑本地全量测试或 full CI，房内真实截图与文本读取留给独立 QA 在冻结头验证。
