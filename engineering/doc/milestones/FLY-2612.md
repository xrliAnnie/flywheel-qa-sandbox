# FLY-2612 — Linear 既有 Issue 父 Epic 编辑与读回

**Status**: ⏳ Pending ship
**PR**: 本分支 `flywheel-FLY-2612` 的实现 PR，随本提交推送后创建
**Date**: 2026-09-15

共享 Bridge Linear 工具现可在既有 Issue 上显式设置或清除 `parentId`，并由精确读取返回真实 `parent {id, identifier}`。父关系写入复用既有 master-only、team/project/scope 约束，拒绝跨域、指向自身、成环及不完整祖先链；写后必须读回匹配才报告成功，模糊失败会标记 `mutationMayHaveSucceeded`，未新增 Raya 私有客户端或第二套凭据。

红绿测试覆盖路由转发、真实 scope helper、循环拒绝、`null` detach、幂等重复移动及读回；最终同步 `origin/main` 后 5 个相关文件 130/130 通过，lint 与全仓 build 退出 0。完整包门保留宿主争用导致的红 aggregate，并以获准的零断言失败 `PACKAGE_GATE_RECEIPT` 及单文件隔离证据记账；最终 code review 与 CI 由精确头结构化回执判定。实现阶段未改真实 Linear Issue，未 dispatch QA、merge、deploy 或 restart。
