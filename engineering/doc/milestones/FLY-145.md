# FLY-145 — QA-FLY-127 S6 retry Product-Test sandbox fixture

**Status**: ⏳ Pending ship
**PR**: #263
**Date**: 2026-09-26

No-code QA fixture:交付 S6 四 Lead 矩阵的 exploration / research / plan 与设计 HTML(迁入 `engineering/doc/FLY-145-s6-retry-product-test/`),不改任何产品代码。被测行为是 `Product-Test` issue 只由 `flywheel-test-2` start + spawn、`flywheel-test-1/3/4` reply=start=spawn=0;plan 要求四路 fan-out、cos roster、scope gate enabled 与 Bridge ingress capture receipt 齐备才可判 PASS,否则 `INCONCLUSIVE`。本地测试刻意未运行(无代码变化),真实矩阵由 QA 节点执行;PASS 且 FLY-1189 harness 依赖处理后才归档 FLY-145。
