# FLY-145 — QA-FLY-127 S6 retry Product-Test sandbox fixture

**Status**: ⏳ Pending ship
**PR**: #263 (earlier attempt: #56)
**Date**: 2026-09-26

No-code QA fixture:交付 S6 四 Lead 矩阵的 exploration / research / plan / 设计 HTML 与 QA 报告(`engineering/doc/FLY-145-s6-retry-product-test/`),不改任何产品代码。被测行为是 `Product-Test` issue 只由 `flywheel-test-2` start + spawn、`flywheel-test-1/3/4` reply=start=spawn=0;plan 要求四路 fan-out、cos roster、scope gate enabled 与 Bridge ingress capture receipt 齐备才可判 PASS,否则 `INCONCLUSIVE`。

QA attempt 1(head `ba3ed6a29`)判 FAIL / behavior INCONCLUSIVE:PR 因分支继承约 2279 个生产 flywheel commit 而对 sandbox `main` 冲突(877 文件,`mergeStateStatus=DIRTY`,无 CI),且四 Lead campaign receipt 缺失。implement attempt 2 以普通 merge commit 合入 sandbox `main`,除 FLY-145 自己的文档目录外一律取 `main` 的 tree,使 PR 只携带 FLY-145 文档;不改写历史、不 force-push。四 Lead campaign receipt 属独立 campaign 基础设施,不在本 PR 内;本地测试刻意未运行(无代码变化)。FLY-145 保持未归档,直到行为 PASS 且 FLY-1189 harness 依赖处理完毕。

## Prior attempt — PR #56(2026-08-29,原文保留,仅标题降两级)

### FLY-145 implementation milestone

**Issue**: FLY-145 — [QA-FLY-127 sandbox] S6 retry — Product-Test
**Date**: 2026-08-29
**PR**: https://github.com/xrliAnnie/flywheel-qa-sandbox/pull/56

#### Delivered scope

- Preserved the approved dummy-issue contract: no runtime, CI, or test code changes.
- Retained the S6 evidence design package and the implementation progress ledger.
- Merged current `main` into the feature branch and resolved the inherited conflict by keeping `main`'s `CLAUDE.md`; the PR no longer modifies that shared file.

#### Verification evidence

- `pnpm lint`: exit 0 (14 pre-existing warnings) in the resumed implementation phase.
- `pnpm -r build`: exit 0 after `pnpm install --frozen-lockfile` restored the initially absent workspace dependency tree; the lockfile and tracked source remained unchanged.
- `pnpm test:packages:run`: exit 1 because two main-identical, real-Terminal AppleScript integration tests cannot use healthy GUI automation in this runner; 206 other `flywheel-core` tests passed before pnpm stopped at the first failing package. The final milestone head must pass the authoritative Linux CI control before handoff.
- New `scripts/__tests__/*.test.sh` files in this PR: none.
- Lead waiver question for the inherited environment-only failure: `9b77447e-a478-408a-93ce-6b8f1ebe4daa` (pending when this milestone was written).

#### Handoff boundary

The implementation phase owns the docs-only branch, PR, verification evidence, and review gate. The QA campaign owner remains responsible for collecting E1–E5 Discord/Bridge evidence and archiving the sandbox issue after S6 PASS.
