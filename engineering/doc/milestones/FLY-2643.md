# FLY-2643 — Visible TUI as the fleet default

**Status**: ⏳ QA5 rework reviewed APPROVED at `8fad8d635`; synced with main after FLY-2755 shard rebalance; pending fresh exact-head review and ordinary PR CI
**PR**: [#1251](https://github.com/xrliAnnie/flywheel/pull/1251)
**Date**: 2026-09-20

Production Lead and active Runner admission now requires a live, attachable cmux/tmux TUI bound to the exact carrier identity. The shared rule is loaded by internal Lead governance and the Runner contract; install, installed/live verification, restart waves, patrol STEP 1, and fleet cmux reconciliation all fail closed or surface an explicit coverage gap when the expected window, model body, identity, or cmux attachment cannot be proved.

The new read-only verifier checks Lead and Runner identities without repairing or switching windows. Codex remote resume inherits the server-owned thread permissions required by Codex 0.154.0, while delayed stable-pane proof prevents a successful `tmux new-window` from being reported as `real TUI up` after the pane has already died. Packaged installs include the verifier and its runtime dependencies, and the restart barrier preserves determinate failures while revalidating successful carrier identities within a bounded fleet budget.

Focused verification covered the visibility verifier, Lead rules, launcher/install behavior, restart services, cmux synchronization, patrol, packaging/convergence, remote TUI binding, delayed liveness, alerting, and changed shell syntax. Lint and recursive build passed. Full local package aggregation was intentionally skipped under the Lead's FLY-2467 contention ruling; CI run `35315731643` passed every dependency job and aggregate `CI OK` at implementation head `afe7ee0e79e700d1c617b713218100fb110b17eb` before this milestone-only commit.

The milestone-only head's full CI run `35318905845` exposed two independent gate failures. TeamLead shard 3/4 timed out once in `lead-capability-read.test.ts` and passed on the single authorized failed-job rerun. Script shard 2/5 passed every suite but hit the FLY-1870 capacity tripwire twice (`1060s` and `1027s` versus the `1020s` budget). This PR had added `agent-visibility-rules.test.sh` and `agent-visibility.test.sh` there, measuring about `23.2s` together, so they were moved intact to the same run's lightest shard, Script 4/5. The strict CI inventory test passes after that rebalancing; this post-rebalance head still requires fresh exact-head review and CI.

Exact-head review then found that an exact four-worker visibility batch emptied its PID array before the final wait. Under the production macOS `/bin/bash` 3.2 runtime with `set -u`, the empty expansion aborted the restart after all four probes completed. Both worker-drain loops now use the Bash-3-safe nounset expansion, and a four-candidate regression executes under `/bin/bash`. The full restart suite passes `159/159`, the host-tmux restart-mount guard passes `5/5`, and the strict CI structure guard remains green; this review-fix head requires a new exact-head review and CI run.

QA1 then exposed a host-specific parser defect: tmux 3.7c rewrites TAB format delimiters to underscores when invoked without a UTF-8 locale. Both Codex Lead TUI readers now use a printable delimiter, with a real isolated no-locale tmux differential proving the old TAB protocol fails while live identity and retained exit evidence remain readable. The public verifier now clears inherited tmux client/socket context while retaining the authoritative state root, treats malformed exit-1 sidebar output as inconclusive rather than a missing surface, and classifies an active pending Runner target as `missing_window`. Fleet reconciliation also requires shared and private Lead panes to be live, so a retained dead pane cannot satisfy the roster.

Fresh QA1 verification passed: TUI reader tests `29/29`, public visibility `32/32`, roster `26/26`, Lead attach health `14/14`, cmux sync `583/583`, patrol snapshot `411/411`, launcher/install `73/73`, visibility-rule and strict CI-structure guards, `pnpm lint` with zero errors, and `pnpm -r build`. The repository's full local package aggregate remains intentionally unrun under the Lead's contention ruling; the pushed milestone-last head must obtain fresh code review and exact-head CI before handoff.

The first QA1 exact-head CI run `35330736093` caught one direct compatibility regression in `fly1663-cmux-v2.test.sh`: private roster health required the entire `list-panes` output to be one live row, so both an older fixture and any valid multi-pane `main` window were falsely reported missing. The census now accepts any exactly parsed `main|main|0` row while still rejecting a private window containing only retained dead panes. The focused v2 suite passes `10/10`, the retained-dead roster suite remains `26/26`, and the full cmux sync suite remains `583/583`; lint, recursive build, Lead attach health, and CI structure were rerun green. That superseded CI run also saw an unrelated transient `jq` parse failure in the unchanged FLY-1572 mailbox test, which passed immediately in a focused local rerun. This new milestone-last head requires fresh exact-head review and CI.

QA2 then proved that the formal Codex carrier still stopped one layer short of founder visibility: `reconcile_v2_lead_workspaces` rostered the live shared tmux panes but created cmux workspaces only for private Claude carriers. A pre-existing Raya or infra-bot cmux cell therefore launched an attach command for an absent `cmux-<lead>` linked session and exited. Codex TUI roster reconciliation now resolves exactly one live shared source window, constructs and re-proves the strict linked-session topology, adopts or creates the exact cmux workspace through the existing guarded paths, and refuses convergence until the current cmux generation exposes one exact workspace ref with a committed receipt. A mutation regression proves that returning from reconciliation without that receipt remains red.

QA2 also reproduced patrol under Bridge's real `env -i` launch shape. The visibility verifier derived the authoritative home from `FLYWHEEL_STATE_DIR` but let its cmux-sync child inherit the scratch `HOME` and `TMUX_TMPDIR`, making every true surface inconclusive. That child now receives the authoritative `HOME` and state root while inherited tmux client/socket selectors are cleared. The Bridge-shaped regression proves the full visible level passes only when those values are correct; the existing missing-window and identity-mismatch cases remain red.

Post-rework verification passed: Codex v2 reconciliation `11/11`, public visibility `33/33`, roster `26/26`, Lead attach health `14/14`, full cmux sync `583/583`, patrol snapshot `411/411`, `pnpm lint` with zero errors, recursive build, changed-shell syntax, and diff checks. Per the explicit QA2 constraint, the local full package aggregate was not run. A merge-tree against current `origin/main` `745afe084e941811a95133fb8e6329a3c95b4a87` is clean. A merge-tree against PR #1256 head `4f0bee6bebe46563177a81c6667ce570c5033169` reports the already-known content conflict only in `packages/teamlead/src/lead-backends/codex/__tests__/codex-lead-tui-runtime.rotation.test.ts`; this rework did not touch that file.

No service restart, deployment, host activation, production carrier migration, QA dispatch, or merge was performed. Real-machine QA must still prove the formal mechanism creates attachable Raya, Mufasa, and infra-bot windows that remain alive for at least 60 seconds, capture screenshots, exercise Codex 0.154.0 legacy and capability branches, and demonstrate the dead-pane mutation goes red. FLY-2643 now owns the formal Codex cmux linked-view mechanism; the temporary `~/.flywheel/tmp/raya-view-stopgap.sh` window is not acceptance evidence.

QA3 then retained the same unreceipted infra-bot cell after the ordinary reconciler had already restored its exact linked tmux session. The audited `ops_rebuild` path now admits that otherwise-preservation-only W1 shape only when the active target, requested ref, source window, generation, strict linked topology, held mutator lease, and zero-client observation all match. The restored fingerprint and final close guard repeat the proof before closing the stale cell; the standard create path then writes the replacement's current-generation committed receipt. The production-shaped regression starts from `workspace:18` with surface title `~`, screen `[exited]`, no receipt, and an already repaired linked session; it proves one exact close, one rebuild, `ledger_committed_ref` success, and zero watcher signals.

QA4 also identified current-generation `claim_intent` WALs whose canonical name, staging session, and original shared window had all disappeared. Recovery previously returned `1`, which preserved the impossible transaction and aborted the complete WAL sweep. The recovery path now removes that WAL only after a generation-pinned inventory proves both session names absent and proves the original window id absent from every tmux session, followed by a generation recheck. Any malformed identity, surviving object, inventory error, or generation drift still preserves the WAL fail-closed. A Raya-shaped regression using nonce `1789680364` proves the abandoned record is audited and removed and a lexically later unrelated WAL still recovers in the same sweep.

Post-QA4 verification passed: full cmux sync `585/585`, changed-shell syntax, and diff checks; `pnpm lint` exited zero with only the repository's existing warnings, and `pnpm -r build` exited zero. Per the explicit QA4 boundary, the local full package aggregate was not run. The required read-only `codex:rescue` companion was invoked but failed before repository review because the resident macOS sandbox rejected nested `sandbox-exec` with status 71; this is not treated as a review verdict, and no raw `codex exec` fallback was used.

The pre-sync merge-tree at implementation head `d4169eaf82c8ee54e88028a3fa4845a65e205cca` reported the known rotation-test conflict against both current `origin/main` `6a6a8abcc297c595638bffcf2c5323734be0ca29` and merged PR #1256 head `4f0bee6bebe46563177a81c6667ce570c5033169`. Because GitHub could not create a conflicted PR merge ref or start exact-head CI, the branch took the contract-authorized technical merge from `origin/main`. Its sole conflict resolution preserved both suites in `codex-lead-tui-runtime.rotation.test.ts`: FLY-2643's five stable-visibility cases and FLY-2696's two persona-projection cases share the combined harness and pass together (`53/53`). `pnpm install --frozen-lockfile`, recursive build, and lint all pass after the sync; the two QA4 cmux shell files are byte-identical to the pre-sync head, and no Runner adapter changed. The post-sync merge-tree against `origin/main` is clean. A raw merge-tree against the already-merged, pre-squash #1256 head still reports duplicate-patch conflicts in `ProjectConfig.ts` and the rotation test; #1256's shipped content is already present through main and its affected focused suite is green. No watcher, production tmux session, Lead service, or carrier was killed or restarted.

Exact-head code review approved `62e6853a1f39f502d869560627e6215d58e9296e`, and CI run `35373502513` passed all 16 jobs including aggregate `CI OK`. The Lead then promoted one non-blocking advisory to required security rework: a pane tail such as `Authorization: Bearer <token>` redacted only the word `Bearer` and left the credential in logs. A focused regression first failed with the raw token still present; terminal-tail sanitization now redacts standalone Bearer shapes and consumes the complete value for token, secret, authorization, and API-key assignments. The focused TUI window suite passes `29/29`, the combined rotation suite passes `53/53`, lint exits zero with the repository's existing warnings, and recursive build passes. The other six review advisories remain follow-ups in PR #1251 and are not new issues. This milestone-last head requires fresh exact-head review and CI.

The first post-security exact-head CI run `35395660958` failed only TeamLead shard 3/4: the unchanged FLY-2662 predeploy replay fixture fixed time at `2026-09-17T21:00:01Z`, but its worktree-absence audit used the real wall clock, so the fixture's 24-hour land generation had expired and produced `worktree_absence_receipt_failed:stale_land_generation`. Current `origin/main` fixed that repository-wide date bomb in `d8b3f3cd5` (#1267). The contract-authorized technical merge is clean; the previously failing focused suite now passes `2/2`. The preserved WIP backup is older than and fully superseded by the committed QA4 implementation, so it was not cherry-picked.

Fresh post-merge verification passed: focused Codex TUI window and rotation suites `82/82`, full cmux synchronization `585/585`, changed-shell syntax under `/bin/bash`, `pnpm lint` with zero errors and the repository's existing warnings, and `pnpm -r build`. The local full package aggregate was again intentionally not run under the explicit QA4/Lead boundary. This milestone is the literal last commit; the pushed head still requires a fresh exact-head code-review verdict and ordinary PR CI. No `ci-full ensure`, QA dispatch, service restart, production change, merge, or ship action was performed.

The next exact-head review rejected `ef854ef5d` on three HIGH findings. First, a Claude private Lead whose manifest or address drifted was demoted to `config-drift`, after which W1-dead discovery could treat its cmux cell as a shared Codex surface and enter the destructive close path. The roster now carries the launch wrapper's intended carrier separately from current health, and both the W1-dead probe and discovery exclude effective or intended private carriers. The new regression failed before the fix (`585 passed, 1 failed`) and the complete cmux suite now passes `586/586`, while the existing non-private W1-dead cleanup remains green.

Second, the Bridge killed patrol snapshots after 15 seconds even though the invoked visibility verifier has a bounded 75-second probe. Third, Raya migration initialization and proof collection imposed 30-second or default 15-second limits on the same live verification. The Bridge snapshot budget and the shared Raya live-verification budget are now 120 seconds, with executable tests asserting that both exceed the verifier bound and that all three Raya call sites receive the shared value. The focused TeamLead suite passes `39/39`; roster and v2 focused suites pass `26/26` and `11/11`; changed-shell syntax and diff checks pass; `pnpm lint` exits zero with only the repository's existing warnings; and `pnpm -r build` exits zero. The local full package aggregate remains intentionally unrun under the explicit QA4/Lead boundary. This milestone is again the literal last commit; this review-fix head requires a new exact-head review and ordinary PR CI before handoff.

The next exact-head review found one remaining HIGH: the snapshot child had the new 120-second budget, but the Bridge HTTP route and the Lead-side capability handler still applied their generic 15-second aborts to `patrol.snapshot`. The timeout policy is now nested explicitly: 120 seconds for the child, 125 seconds for Bridge cleanup and receipt work, and 145 seconds for the Lead-side GitHub prefetch plus the Bridge request. Regression assertions against the actual route and handler failed on the old timers and now pass; the focused suites pass `45/45`, `pnpm lint` exits zero with only existing repository diagnostics, and `pnpm -r build` passes all 24 participating workspace projects. The local full package aggregate remains intentionally unrun under the explicit QA4/Lead boundary. All MEDIUM/LOW review findings remain PR follow-ups, not blockers. This milestone is the literal last commit; the new head requires fresh exact-head code review and ordinary PR CI before handoff.

Ordinary CI run `35422849592` then caught that the packaged-layout patrol fixture copied the snapshot module but not its newly extracted timeout dependency, so Node module resolution failed before the intended missing-helper fallback could run. The failure reproduced locally; the fixture census now materializes the timeout module at its actual packaged path. The packaged-layout suite passes `3/3`, the exact Bridge route and Lead-handler timeout cases pass individually, and the TeamLead build is green. The same superseded run also contains an unrelated timeout in the unchanged Lead in-flight mailbox adoption shell test; no rerun was requested. This milestone is again the literal last commit, and the pushed head requires a new exact-head code review and ordinary PR CI.

The next exact-head review found that capability-v2 patrol still crossed one unextended outer deadline: the broker and both Unix-socket peers retained the generic 15-16 second policy, so they aborted before the 145-second Lead handler budget could matter. A regression through the actual `LeadCapabilityBroker` failed at the 145-second checkpoint before the fix. `patrol.snapshot` now receives a 150-second broker deadline, a 151-second server-socket deadline, and a 152-second client-socket deadline, preserving explicit nesting outside the existing 120/125/145-second child, Bridge, and handler budgets. The focused flywheel-comm client suite passes `6/6`; the broker regression passes; the complete broker, broker-socket, handler, snapshot, and packaged-config selection passed `76/77`, with only the known unrelated 15-second disabled-Discord fixture timeout while every FLY-2643 case passed. `pnpm lint` exits zero with existing repository diagnostics, and `pnpm -r build` passes all 24 participating workspace projects. The local full package aggregate remains intentionally unrun under the explicit Lead boundary. This milestone is the literal last commit; the new head requires fresh exact-head code review and ordinary PR CI.

While the prior exact-head CI rerun was waiting on the unrelated FLY-1870 capacity tripwire, current `origin/main` advanced to `e844b45456dcfc857bd8eb49111a8b85336e1198`. The contract-authorized technical merge conflicted only in `scripts/flywheel-cmux-sync.sh` and `scripts/test-cmux-sync.sh`. The resolution preserves FLY-2643's generation-pinned proof that the canonical session, staging session, and original window id are all absent before retiring an abandoned `claim_intent`; it also preserves FLY-2656's stable `retired entity-free claim_intent view WAL` audit key, conclusive-census regression, cleanup admission/pre-prune regression, and startup/priority regression. The uninterrupted combined cmux suite passes `595/595`, including both issue families in the same process.

Post-sync verification also passes `pnpm install --frozen-lockfile`, the flywheel-comm timeout client suite `6/6`, the TeamLead broker and broker-socket suites `29/29`, `pnpm lint` with only existing warning-class diagnostics, recursive build across all 24 participating workspace projects, changed-shell syntax, and diff checks. The Lead-authorized single rerun of prior-head CI run `35424585742` was submitted exactly once; that run remains evidence for prior head `c48aa56590d291db2631c602fcbb1c592edee43c`, not authority for this merged head. The local full package aggregate remains intentionally unrun under the explicit QA4/Lead boundary. This milestone is the literal last commit before push; the resulting exact head requires a new code-review verdict and ordinary PR CI. No `ci-full ensure`, QA dispatch, service restart, production mutation, merge-to-main, or ship action was performed.

Current `origin/main` then advanced to `4b8bed5c8e8a0a2357aab1c4d0c6bb512895d1e8` with the unrelated FLY-2745 statusline change. The contract-authorized technical merge `81d299a91` was conflict-free and changed no FLY-2643 implementation path. Fresh verification on the merged tree passes `pnpm install --frozen-lockfile`, the complete cmux synchronization suite `595/595`, the flywheel-comm timeout client suite `6/6`, the TeamLead broker/socket/Bridge patrol suites `52/52`, changed-shell syntax under `/bin/bash`, and `git diff --check`. `pnpm lint` exits zero with only the repository's existing warning-class diagnostics, and `pnpm -r build` passes all 24 participating workspace projects.

Ordinary PR CI run `35429145299` was started for prior head `e293990f770ac0d3712d53491cfc425905bb144a`; regardless of its eventual result, it is not exact-head evidence for this post-merge milestone commit. The local full package aggregate remains intentionally unrun under the explicit QA4/Lead boundary. This milestone is the literal last commit before push; the resulting exact head requires a fresh code-review verdict and ordinary PR CI. No `ci-full ensure`, QA dispatch, service restart, production mutation, merge-to-main, or ship action was performed.

The replacement runner resumed from Lead-preserved WIP commit `c1a11028a` rather than redoing the approved design. Its three new public-verifier regressions first failed `33 passed, 3 failed`: macOS `launchctl print` exit `113` was being flattened into `probe_unavailable`, and Runner carrier/title probes inherited Bridge's ambient `TMUX`/`TMUX_TMPDIR`, allowing the wrong socket either to hide a healthy target or admit a decoy. Commit `d7817eab0` formalizes the stranded test work with the minimal implementation: status `113` is a determinate `carrier_not_running`, other launchctl probe failures remain inconclusive, and both Runner tmux reads use the existing shared-socket clean-room wrapper. The same suite then passed `36/36`.

Focused verification for the resumed rework passed: public visibility `36/36`, Lead install/verification `74/74`, restart services `159/159`, patrol snapshot `413/413`, visibility-rule injection, changed-shell syntax, and the complete cmux subsystem `595/595`. The latter includes QA4's abandoned `claim_intent` continuation and exited/unreceipted Codex Lead `ops_rebuild` convergence with a committed ledger receipt and no watcher signal. The local full package aggregate was not run, honoring the replacement dispatch's targeted-test budget and the existing QA4 prohibition on `pnpm test:packages:run`.

Current `origin/main` advanced to `a62456f76e4838a81aad9bb166d8fd5ea0e37cc4` with FLY-2638. The contract-authorized technical merge `02fd58720` was conflict-free and did not change `scripts/flywheel-cmux-sync.sh`, `scripts/test-cmux-sync.sh`, the public visibility verifier, or its regression file. Post-sync verification passed public visibility `36/36`, the combined Codex rotation/persona suite `53/53`, changed-shell syntax, and `git diff --check`. The merge-tree against current main is clean. A raw merge-tree against already-merged PR #1256's pre-squash head `4f0bee6bebe46563177a81c6667ce570c5033169` still reports duplicate-patch conflicts in `ProjectConfig.ts`, `lead-persona-activation.test.ts`, and `codex-lead-tui-runtime.rotation.test.ts`; #1256's shipped content is already present through main, the current rotation suite is green, and this rework changed neither the rotation test nor `CodexTmuxAdapter.ts`.

Prior-head CI run `35430551572` is not current-head authority: all unit jobs and Quick Gate passed, while script shards 2/5 and 3/5 crossed the FLY-1870 elapsed-time capacity tripwire, making aggregate `CI OK` red. No CI workflow or shard layout was changed in this rework. This milestone is the literal last commit before push; the resulting head requires a fresh exact-head code-review verdict and ordinary PR CI. No `ci-full ensure`, QA dispatch, production tmux/service mutation, merge-to-main, or ship action was performed.

Ordinary PR CI run `35544542642` passed every job, including aggregate `CI OK`, at prior exact head `17320347c81a9bdde436e8aea829d5e66e4d4fb3`. Exact-head code review then found one HIGH correctness defect: the new durable `visible` path retained row, topology, pane, and receipt proofs but skipped the existing client-count and rendered-screen checks. That admitted the original failure shape—a managed cmux workspace whose attach process had fallen back to a detached bare shell—as visible. The finding matches the approved plan's requirement that every subject prove an attached client and a non-empty model terminal surface.

The review regression first produced `594 passed, 1 failed`, with detached and unreadable Runner and private-Lead surfaces all incorrectly returning `pass`. Durable and interactive admission now share the attached-client and non-empty-render requirements without requiring the tab to be selected. A determinate zero-client or empty-render state is a failure; a read-screen probe failure is inconclusive rather than success. The complete cmux synchronization suite now passes `595/595`, including the new four-shape regression and both QA4 recovery cases; changed-shell syntax and diff checks also pass. The review's five MEDIUM/LOW findings remain non-blocking advisories and were not expanded into this bounded correction.

The local full package aggregate remains intentionally unrun under the explicit replacement-runner and QA4 test boundary. This milestone is the literal last commit before push; the resulting exact head requires a new code-review verdict and ordinary PR CI. No `ci-full ensure`, QA dispatch, production tmux/service mutation, merge-to-main, deployment, or ship action was performed.

Exact-head code review approved `4d6cc6948f64fc18cf9f1e322d91894000191187` with non-blocking advisories. Ordinary CI run `35546472163` then passed all assertions in Script shards 2/5 and 3/5 before both crossed the FLY-1870 elapsed-time capacity tripwire; Unit heavy also timed out once in the unchanged `runner-env-isolation.real-tmux.test.ts` five-second fixture after 1,351 assertions passed. The Lead promoted two review advisories into the final required rework and directed the new head to rerun that Unit-heavy job at most once if it fails again, investigating any second failure; if only the capacity tripwire remains red, the implementation may hand off as `needs_review`.

The final rework first added executable regressions. A healthy durable Runner target now proves the full two-sample client/render path can pass, balancing the existing detached, unreadable, empty-render, and private-Lead negative cases. A 17-Lead restart fixture then proved the old cmux-absent path multiplied one host-wide outage into 153 verifier calls and retained a retry episode. The minimal fix performs one bounded cmux preflight: a missing socket becomes one `cmux_app_unavailable` unproven observation with zero per-Lead verifier calls, while inconclusive preflight failures preserve the bounded per-Lead probes. That pure aggregate outage consumes only its own retry marker; any determinate missing Lead or carrier failure still blocks marker removal.

Post-fix verification passes the complete restart-services suite `160/160`, the complete cmux synchronization suite `596/596`, changed-shell syntax, and `git diff --check`. The implementation commit is `98bd6e664`; the local full package aggregate remains intentionally unrun under the replacement dispatch's targeted-test budget and the explicit QA4 boundary. This milestone is again the literal last commit before push; the new exact head requires fresh code review and ordinary PR CI. No `ci-full ensure`, failed-job rerun, QA dispatch, production tmux/service mutation, merge-to-main, deployment, or ship action was performed.

QA5 then measured the public visibility gate on the founder host and proved the previous 60-second child deadline was unsatisfiable: successful cmux proofs took `84, 88, 91, 121, 158` seconds, while every run completing inside 60 seconds returned inconclusive. The nearest-rank p95 of the successful distribution is therefore 158 seconds; the required p95 × 1.5 floor is 237 seconds. Four post-deploy-shaped public-verifier attempts all hit the old child deadline at 65–66 seconds and returned `inconclusive/probe_unavailable`, which would have made patrol STEP 1 unavailable and rejected healthy Lead installs.

Commit `9c2453b67` makes the measured budget one shared source of truth in `scripts/lib/agent-visibility.sh`: the cmux child defaults to 240 seconds and the public/patrol wrapper to 250 seconds, with validated trusted-environment overrides `FLYWHEEL_VISIBILITY_CMUX_TIMEOUT_SECONDS` and `FLYWHEEL_VISIBILITY_TIMEOUT_SECONDS` that may only raise the measured floor while preserving `outer > inner`. A child timeout now returns determinate `fail/visibility_timeout` instead of an inconclusive coverage gap. The remaining parent deadlines preserve strict nesting at 270-second snapshot execution, 275-second Bridge cleanup, 295-second Lead-side prefetch/request, 300-second broker, 301/302-second socket peers, and 305-second Codex MCP allowance. Install receives 270 seconds, and the five-batch 17-Lead restart barrier receives 1,320 seconds plus final carrier revalidation.

The timeout regression records the 240-second cost budget and proves rc 124 produces the explicit failure reason. The FLY-2600 mutation gap is also closed: an unchanged live pane tuple whose second sample runs `zsh` with `modelAlive:false` can never complete the stable visibility proof. Post-QA5 focused verification passes public visibility `39/39`, patrol snapshot `414/414`, restart services `161/161`, Lead install/verification `75/75`, focused TeamLead `80/80`, flywheel-comm `6/6`, packaging/convergence `21/21`, affected TeamLead and flywheel-comm builds, exact-file Biome checks, changed-shell syntax, and `git diff --check`. The local full package aggregate remains intentionally unrun under the explicit targeted-test boundary. This milestone is the literal last commit before push; the resulting head requires a fresh exact-head review and ordinary PR CI. No `ci-full ensure`, QA dispatch, production mutation, merge-to-main, deployment, or ship action was performed.

Exact-head review approved `8fad8d635` (gate `ccdbd7c5`) before `origin/main` advanced through FLY-2755, FLY-2753, FLY-2749, and FLY-2763 to `e82193650`. The contract-authorized technical merge `2173d078a` had exactly one conflict: FLY-2755 moved the FLY-1364 cmux shell step from Script Tests 5/6 into the rebalanced 2/6 shard. The resolution keeps main's shard layout and re-adds the two FLY-2643 suites (`agent-visibility-rules.test.sh`, `agent-visibility.test.sh`) to the end of that relocated step; the FLY-2567 rule-budget fixture and the `ci-structure` inventory auto-merged, and the pinned ci-ubicloud source fixture is a snapshot by contract and was left untouched. Main did not modify `scripts/flywheel-cmux-sync.sh`, `scripts/test-cmux-sync.sh`, the verifier, or any other FLY-2643 file, so the previously reviewed implementation is byte-identical after the merge.

Post-merge focused verification at the merged head passed lint, the recursive TeamLead build, typecheck of every flywheel-comm and flywheel-config dependent, the CI structure contract, focused TeamLead `326/326` across the sixteen changed test files, flywheel-comm `6/6`, flywheel-config flag-truth `119/119`, public visibility `39/39`, Lead rule injection, Lead install/verification `75/75`, patrol snapshot `414/414`, restart services `161/161`, the complete cmux synchronization suite `597/597` under `/bin/bash 3.2`, and `git diff --check`. Real-tmux and Unix-socket cases were rerun under a short `TMPDIR` because the runner's `~/.flywheel/runner-state/<exec>/browser-tmp` temp root exceeds the macOS socket path limit; that is an environment constraint, not a code change. The local full package aggregate remains intentionally unrun under the FLY-2753 targeted-verification rule. This milestone is the literal last commit before push; the resulting exact head requires a fresh same-family machine review and ordinary PR CI. No `ci-full ensure`, QA dispatch, production tmux/service mutation, merge-to-main, deployment, or ship action was performed.

Head lineage note: the merge-sync milestone commit `1ba51d0e8` was followed by a progress-ledger commit, which would have left a non-milestone commit as the PR head. This refresh restores the milestone as the literal last commit; it changes no code, and the review request registered against `1ba51d0e8` is superseded by a fresh exact-head review of this head.

Ordinary CI run `35573860552` at `339f9dc67` passed all six rebalanced script shards, Unit light, Unit observation performance, Unit teamlead 1/3/4, and NPM payload distribution. Three jobs were red. Unit teamlead 2/4 failed this branch's own real-tmux regression `real tmux preserves a one-second TUI child exit code under remain-on-exit` with `qa-tui 1`: tmux had marked the pane dead on pty EOF but had not yet reported `pane_dead_status` from SIGCHLD when the fixed thirty-sample loop (about 3.4 seconds) ran out. Commit `fc8e715c5` keeps the exact `qa-tui 1 42` assertion and polls against a 20-second budget with an explicit test timeout; the production reader already returns `exitStatus: null` for that transient shape, so no implementation change was needed. Quick Gate failed the unchanged main-side package-gate guard `simultaneous first-use initialization converges on one database` with `database is locked`, and Unit heavy timed out once more in the unchanged claude-runner `runner-env-isolation.real-tmux.test.ts` five-second fixture, the same shape the Lead previously ruled eligible for one rerun. Neither touches a FLY-2643 file; the new head simply reruns them. The edited test file passes `30/30` locally under a short `TMPDIR`, Biome, and `git diff --check`. This milestone is the literal last commit before push; the resulting exact head requires a fresh same-family machine review and ordinary PR CI. No `ci-full ensure`, failed-job rerun, QA dispatch, production mutation, merge-to-main, deployment, or ship action was performed.

