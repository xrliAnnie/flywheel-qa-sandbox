# FLY-2701 — 语音按需启动：会议预热、静音等待到点、按需失败预算

**Status**: ⏳ Pending ship
**PR**: #1282
**Date**: 2026-09-22

语音不再常驻。平时主机上没有语音进程；耳机模式一发起、或预约会议到了提前量，Bridge 才请 launchd
把它拉起来，结束后空闲自行 exit 0。

本 PR 是设计的第二半（第一半按需生命周期已在同分支）：

- **会议预约**：`voice_schedules` / `voice_schedule_requests`。预约带自己的 revision，改期与取消能
  fence 掉在飞的副作用，并在同一次提交里撤销关联 session 的启动意图；请求收据可精确重放；未来的预约
  不占房间唯一槽。
- **预热**：`/api/voice/schedules` + Bridge 调度 runtime，在 T−提前量（部署配置，60–300s，默认 120s）
  预订并 provision，唤醒交给有预算的 wake lane。调用方下单后可以直接退出。
- **静音等待**：预热会话报 `ready` 但不 live。live floor 在 StateStore 里守；到点之前进出音频、
  转写、capture 一律不通过——bot 在房里，但那段时间不属于会议。
- **失败预算**：`voice_launch_attempts` 取代了按需模式下的常驻 restart-storm gate。三个启动窗口空过
  才耗尽；unknown、被合并的请求、以及探针超时都不消耗预算。
- **常驻→按需迁移**：部署时若本机仍是旧常驻 plist，在证明无人通话后才 bootout 并换上按需契约；
  通话中或读不到 session 表则以显式 `deferred` 跳过，不失败部署；未知 drift 仍然拒绝。

founder 2026-09-22 的口径也落进来了：「空闲」= 房里没人，所以等她的窗口从 2 分钟提到 10 分钟，预约
会话用的是自己的绝对 presence deadline（提前启动不会缩短等她的时间）；她在房里就不退，她退房才结算。
转写落点按她的要求保持现状。

**本单不含**：班车 drain fence（计划 §8 slice F）、wrapper 侧 boot-id 上报、FLY-2655 联调 —— 均已在
PR 的 Follow-ups 段写明影响（通话进行中遇到班车重启仍只靠 lease 窗口）。

QA 真机跑通了休眠 plist、exit 0 后不被拉起、kickstart 47–51ms、迁移判定阴性两例；它同时退回三个
精确头 CI 红（flag 未登记、kill-path 清单未重冻、测试桩写死 uid 导致非本机必红）和一个覆盖缺口——
founder 那条「空闲退出竞态」阴性对照此前没有被任何测试守住（删掉退出前最后一次读取，31 个测试仍全绿）。
四项都已修，竞态阴性对照补在 `qa2701-final-read-race.test.ts`。

FLY-2655（恢复 Discord 语音接收路径）合入 main 后与本单撞了 9 个文件。合并按「两边语义都保」
处理：2655 重写过的 `session.ts` 取其全文为基底，再把本单的进房静音门与「当前在场」重新按新契约
实装；`voice_sessions` 两边新增列全留；schema 清单与 demand 测试里那两处按位置复制行的 SQL 改成
显式列名——git 曾把两边的列增量合成一个表里根本不存在的顺序，这类冲突以后不会再有。

工程侧已测；按 PRD FLY-2642 v2 §2.3 语音五格的 ⑤，**founder 亲测一场之后**才算产品验收通过。

合并头在本地复验：lint 通过；`flywheel-voice-codex` 315 项、teamlead 十个 voice 套件 120 项、
`restart-voice-on-demand` / `flywheel-voice-wrapper` / `launchd-units-manifest` /
`qa-fly1501-brake-missing-alert` / `ci-structure` / `install-voice-launchd` /
`test-restart-services`（161 项）与 claude-runner kill-path 冻结清单全绿。

Codex 代码复审 R1（review 5285486483，评审头 `81fea8f00`）给了 CHANGES REQUESTED，9 条
（3 HIGH + 6 MEDIUM），逐条核对后**全部成立、全部已修**，多数是违反已批准 plan 的明文：
同一会议双入口去重（plan §5 专节）、reserve/link 原子性（§5 伪代码）、presence/live TOCTOU、
正常 no_human 被持久化成故障（§7 明文禁止）、启动串行（§7 / 切片 D）、调度 lane 被单次外部
调用冻结、收据重放晚于时变校验、legacy plist 只核四字段就 bootout、launchctl 超时被算成已
证实失败。处置逐条见 `engineering/doc/FLY-2701-voice-on-demand/review-disposition.md`。

R2 又开了 5 条（2 HIGH + 3 MEDIUM），同样全部成立、全部已修：并行启动的清理依赖双分支
settle（room 落地而 frontend 永不 settle 时会永久遗留已进房的 bot）、presence 快照回滚更新
事件、`warming→ended/no_human` 未等绝对 presence deadline、`schedule_bound` 被误报成冲突、
reaper 抢在健康 daemon 之前把正常缺席写成失败。R2 同时确认该头精确头 CI 16/16 全绿。

R3 再开 4 条（1 HIGH + 3 MEDIUM），全部成立、全部已修。其中 HIGH 那条是**本分支 R2 自己引入的
回归**：R2 为了「返回已有预约」把 Lead 侧改成 succeeded，但生产 handler 要求 `voice-session:`
resourceRef 并按旧 schema 解析，真实 Lead 只会拿到 scope denial —— 只在 router 单测里成立。现已把
schedule 结果做成端到端合同并**穿过生产 handler** 测试；去重结果也在同一事务写下可重放的 intent
收据。其余三条：Discord 启动直到调用返回才看 abort、`start()` 返回被事件超越的旧快照、reaper 等待
无上限。

R4 三条（Codex 全标 landing blocker）与 R5 三点复核均已收口。合计五轮、21 条 findings，逐条对着
代码与已批准 plan 核实后全部成立、全部修复。其中两条是本分支自己引入又自己修掉的回归，已在 PR body
如实记录 —— 一条是 R2 为「返回已有预约」改成 succeeded 却过不了生产 handler，一条是 R4 的 fail-fast
让测试出现 unhandled rejection 导致 CI exit 1（漏检原因：此前只看测试计数不看 exit code，已改正）。
R5 的点 ③ 按 Codex 给的修法改动后未再复审，由 Lead 裁定 5c63d3fc 直接收口，leadAcceptance 记在
code-review 收据里。

收口时精确头 CI 又红一次，是本单自找的：预热预算那条测试用 `Date.now()` 断言真实耗时上界，被
`required-wall-clock-thresholds` 治理套件拦下。已改为按结果断言（预算只剩 15ms 就会到期结算；给
足 120s 的实现根本不会结算），并补一条「调用方不给 deadline 时预算就是上限」的对照。
