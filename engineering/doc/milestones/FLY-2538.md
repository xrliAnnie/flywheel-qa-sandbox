# FLY-2538 — 报告托管账号轮换

**Status**: ⏳ Pending review / QA / ship
**Date**: 2026-09-13

新增 migrate:report-hosting 的 retarget、deploy-gateway-only、usage-check 模式。轮换以私有 store、断点 journal、保留集 CAS 和网关探针确保切换可重跑；解密凭据交接到 0600 文件，由 operator 更新配置。Bridge 按调用热读凭据并校验 store 绑定。

HTML gzip 数据门、固定 Epic 页 digest 跳过、每日清理减少重复用量；存储 80% 和配额状态告警按日去重。fixture HTML 31,025 → 6,587 bytes（减少 78.77%），不推导生产月账单。

lint、全仓 build 通过。两次本地全量包测试失败和隔离 146/146 通过的回执见 engineering/doc/FLY-2538-report-hosting-retarget/implementation.md；Lead 已裁定以精确头 CI 为准，不扩大到无关代码/超时。代码评审、CI、QA 分别确认；实现阶段未部署、未重启服务。

R2 修复两项 HIGH：独立保留大小写敏感的 Vercel API store identity；Epic 稳定 token 持久化 mutable 标记并从只读权威 publication 表恢复旧身份，避免迁移 manifest 永久冻结刷新页。普通报告仍按原始时间过期。相关 43 项与 122 项测试通过，teamlead 构建完成；以本提交为新评审/CI 冻结头。剩余 advisories 按 Lead 裁定留后续。
