# FLY-2174 — 529 QA room 环境契约与 Codex reaper 隔离

**Status**: ✅ Implementation complete; needs review
**PR**: pending (created after this required last commit)
**Date**: 2026-09-01

本批次收清 FLY-2165 QA 暴露的可实施环境债：`--alerts` 不再向 Lead 注入第二份
`FLYWHEEL_PROJECTS_FILE`，generalized room 的 master/ingest auth 分离，Bridge 的三类 Codex
盘点根全部绑定到 slot，teardown 以 lexical `/tmp` socket 做 `lsof` 存活证明，并保留短
`TMPDIR`/tmux socket fallback。生产数据裁决、生产修复与独立的 membership dead-letter 没有
借本 PR 越权处理，仍在下文逐项留账。

## 交付内容

- `scripts/test-deploy.sh --alerts` 只保留 launchd-v2 carrier 的 canonical Lead identity，消除
  `wrapper-v2 Lead identity_launch_env_conflict`；alert repair bot identity 仍独立传递。
- generalized room 生成独立的 runner ingest bearer，Bridge 启动边界会清除 ambient production
  auth；master token 与 ingest token 不再复用。
- Bridge 显式注入 slot-local `FLYWHEEL_CODEX_HOMES_ROOT`、
  `FLYWHEEL_CODEX_SESSION_DIR`、`FLYWHEEL_CODEX_DAEMON_SOCKET_ROOT`。orphan reaper 不再用
  slot-local active executions 对照 production-default Codex inventory，因而不能误杀 production
  resident process group。
- teardown 新增 fail-closed slot Codex daemon reaper：同时要求 slot ledger 与 Unix socket holder
  证明，先 TERM、后 KILL，并验证 process group 退出后才释放 slot。socket 路径保留 lexical
  `/tmp` 形状供 `lsof` 查询，realpath 只用于 containment 校验。
- 短 `TMPDIR` 与 tmux native socket fallback 有正反测试；89 字符 runner-pane TMPDIR 不再让
  Unix `sun_path` 超限。

## 主机验收

- exact code/review head `0a05f0876b4eae653d4d829ee77e0381cb7b762b` 两次在非 sandbox
  slot 4 以 generalized + alerts + real runner 启动成功，readiness 5/5，Bridge build identity
  与该 head 一致。
- 第一次真实复跑中四个 resident Codex process 连续存活 16 分钟；修复前同一路径在 5 分钟内
  误杀 6 个 production resident process。teardown 退出 0，slot process/socket 零残留。
- 第二次复跑把单步预算扩到 45 分钟：真实 design 完成四轮 review 并通过 design gate，随后成功
  mint Codex implement worker；worker 连续存活完整 45 分钟后才由测试 teardown 主动回收。
  `qa-codex-reap` 返回 `outcome=reaped`，slot 目录、Bridge、tmux server、端口与 worker socket
  全部清空。
- 完整 1–9 driver 的两次非零都不是 FLY-2174 回归：第一次使用了不足以覆盖 xhigh design
  review 的旧 15 分钟预算；第二次 cross-family code reviewer 遇到 Claude 429 session limit，
  retry 时间晚于 45 分钟边界。Lead 已以 resident survival + zero-residue 作为本单专属验收核心
  收讫，并另行启动 rerun3 作非阻断补强。

## 验证

- `pnpm lint`：PASS（14 条既有 non-blocking warning）
- `pnpm -r build`：PASS（22/23 workspace projects）
- `scripts/__tests__/test-deploy-fly1389.test.sh`：13/13 PASS
- `scripts/__tests__/test-deploy-generalized.test.sh`：PASS，包含真实 detached Unix socket holder、
  real `lsof` 与 process-group reaping positive fixture
- `scripts/__tests__/test-deploy-qa-room.test.sh`：18/18 PASS
- claude-runner daemon runtime：55/55 PASS；teamlead orphan reaper：19/19 PASS
- Codex sandbox 下 `pnpm test:packages:run` 的 core 为 219/221；仅两条真实 Terminal.app
  AppleScript 测试因 GUI/XPC `Connection invalid` 失败。非 sandbox host 同一 core 套件为
  221/221；该 host 全套随后另有一条与本分支 byte-identical 的
  `qa-result.realgit.test.ts` 5 秒 timeout。

## Code review

request-driven cross-family code review round 4 在 exact head
`0a05f0876b4eae653d4d829ee77e0381cb7b762b` 得到 **APPROVED**，无 blocking finding。
两条 MEDIUM ingest-token hardening 与三条 LOW test-stub/coverage advisory 已按治理规则报告
Lead；它们不阻断本轮 review gate。

## 明确保留的尾账

- 生产 flywheel CommDB 共有 63,914 条 torn identity 候选；只读副本实跑修复 63,911 条，
  耗时 19 分 18 秒。剩余 3 条缺 ACKED/DEAD terminal 时间戳，工具按设计拒绝伪造。
  需要 founder/人工按史实裁决为保守 archived，或登记永久豁免；本 PR 不替人写终态。
- production `~/.flywheel/comm/flywheel/comm.db` 仍未修。须等 FLY-2165/#991 对应 build 合入部署，
  再排约 19 分钟停服窗，按 dry-run → backup → apply → checkpoint 顺序执行。
- slot patrol tick 的 `dead-letter:membership_conflict` 在本批次动手前已经存在，与 FLY-2165
  identity 修法无因果关系。本 PR 不猜测修改 Lead membership 解析；应由独立投递链路 follow-up
  复现和裁决。
- 本批次没有生产写入、没有部署、没有重启 fleet service，也没有申请或执行 merge。
