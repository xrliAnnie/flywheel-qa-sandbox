# FLY-2140 — Epic 页面内容模型与首版生成

**Status**: ✅ Implementation complete — fresh exact-head review
**PR**: [#1044](https://github.com/xrliAnnie/flywheel/pull/1044)
**Date**: 2026-09-03

交付通用 Epic 执行页面的首版生成。当前范围由 Linear 实时声明：绑定边界内所有 `state.type=started`、有子单的顶层父单完整子树，加必须存在的「日常」常驻父单；Backlog 子单不展示。零活动父单、缺「日常」、父单读取失败或分页不完整都会 fail loud，不会回退到整个 Project。

founder 二次返工已彻底取消批次。顶部先展示「现在可以开始的」，再展示当前执行范围与 founder/done/gaps；每张子单独立成卡，包含编号、标题、状态、是什么、为什么、做完你看到、账面执行体、是否回来找 founder，以及「等谁 / 谁在等我」双向依赖。依赖两侧都显示 Linear issue 编号和标题；卡底保留来源链接、观察时间、来源更新时间与可展开的 14 格完整审计。页面没有新增开关。

`ready.v1` 每次渲染从 Linear live snapshot 计算：范围内、非 Backlog、未完成/未取消、全部 blocker 的状态严格为 `completed`；最后只按 Linear priority `1 → 4 → 0`、再按 identifier 排序。StateStore 的 session/run/attempt/land 只作为执行上下文展示，不改变 ready。`scope.v1`、`ready.v1`、`dependents.v1` 均写明输入 Cell 与 founder 裁定；其余派生规则继续标注「未获 founder 裁定的默认规则」。

Bridge 和 `flywheel-comm epic-page generate|show|render` 已移除 Epic/version 旋钮；JSON、Markdown、HTML 全部走同一个 live POST 入口。`epic_page` 表仅保留 write-only render receipt：生成时间、触发方式、source digest，以及 Linear/StateStore 来源的 path/provenance/observed_at/source_updated_at。它不存页面值、ready、next、batch 或顺序，且没有渲染读取路径。旧整页表会在单事务内迁移为 source-only receipt；存储守卫拒绝任何计算顺序字段或计算结果路径。

TDD 证据：接任 implement 节点于 2026-09-03T22:39:04Z 重跑 teamlead 相关 10 files / 100 tests 与 Epic Page CLI 8/8，全部通过；两个受影响 package 的 TypeScript 检查也已通过。随后 exact-head review 找出 ActiveScopeRoots 使用了 Linear `IssueCollectionFilter` 不支持的 `children.null`；先把测试期望改为 `children.length.gt` 并得到 1/6 红灯，再做一处生产修复，得到 6/6 与完整相关范围 10 files / 100 tests 绿灯。真实 Linear GraphQL 冒烟也已接受修正后的输入（执行时符合条件的根单为 0，因此只把它记录为 schema coercion 成功，不写成完整页面生成成功）。该旧头的 cross-family review 与 GitHub CI 已通过；本次 Lead 裁决后的新头仍须重新通过 exact-head review 与 CI。

Lead 裁入本单的三项硬化均按 RED → GREEN 完成：`activeScopeFilter` 现在由 type-only `LinearDocument.IssueFilter` 约束，旧 `children.null` mutation 会被 `tsc` 以 TS2353 拒绝；空验收正文返回 missing 并进入 `no_acceptance_section` 缺口；Markdown 的 Linear 链接目标用 CommonMark 角括号保护含括号 URL。规模返工也已加入可执行回归：60-item HTML 由 770,804 降为 457,364 bytes，低于 512 KiB 托管上限；200-item 为 1,513,873 bytes，仍由 CLI 明确警示超限。最终低负载定向门为 4 files / 50 tests、teamlead typecheck、7 个变更 TypeScript 文件 Biome 与 diff check 全绿。可选的 dependents 非空断言未强改：移除它需要新增内部 invariant 破坏时的模型失败语义，不属于“顺手改”边界。

**生产 Linear 前置条件（截至 2026-09-03 尚未满足）**：操作者必须先把目标 Epic 拖进 **In Progress**（`state.type=started`），并在同一绑定范围建立一个标题含「日常」、有子单且同为 started 的顶层父单。真实查询当前返回 0 个符合条件的根单，所以生产今天不能生成页面；满足两项声明后才有可生成的 active scope。`pnpm -r build` 的既有全仓成功、本机精确门的宿主限制及本轮 Lead 要求只跑定向测试的边界均逐项记录。本分支没有新增 `scripts/__tests__/*.test.sh`。完整红绿记录、迁移/重启证明、宿主限制与 DOM 对账见 `implementation-evidence.md`；本 milestone-only 封口提交继续由 fresh exact-head review 与 GitHub CI 门禁。

fixture 产品 HTML 为 50,617 bytes：5 张子单卡、5 个依赖块、2 个 ready pill、2 个 active-root pill、零表格、零 `data-batch`、零「批次」。Chromium、临时 WebKit 与 WeasyPrint 都被当前宿主进程沙箱/缺少 Pango 阻止，因此没有伪造新截图；仓库内旧 PNG 仅是已废止批次版的历史基线。最终视觉与 Linux 全量结论由本提交头的 fresh review 与 GitHub CI 继续门禁。
