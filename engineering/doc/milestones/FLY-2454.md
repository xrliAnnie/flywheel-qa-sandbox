# FLY-2454 — Test slot 隔离封口

**Status**: ⏳ QA attempt 1 FAIL; rework attempt 2 frozen for full gates and fresh exact-head review
**PR**: https://github.com/xrliAnnie/flywheel/pull/1137
**Date**: 2026-09-09

## 交付内容

- 用 `FLYWHEEL_ISOLATION_ROOT` 与 `FLYWHEEL_ISOLATION_CONTRACT` 建立默认关闭、slot
  显式开启的跨进程 mutation 边界。生产环境未设置隔离坐标时保持原路径；slot 中任何无法证明
  目标属于当前 root 的 PID、PGID、Codex socket 或 tmux window 都 fail closed，并留下统一
  `isolation_boundary_refused` 审计记录。
- 将 claude-runner、TeamLead、Edge Worker 的既有 kill call sites 接入同一守卫，补齐机械
  inventory mutation test 与 audited-caller census，防止后来新增的裸 `process.kill`、group kill、
  tmux kill 或 socket orphan reap 绕过边界。
- 新增 declarative slot env contract，单一 JSON 同时驱动 `test-deploy.sh` 的 allowlist、deny-family
  清扫、slot 坐标投影与测试。Bridge 在任何有副作用模块 import 前执行 boot fence；slot CommDB、
  TMPDIR、Codex home/state/socket/tmux 均固定在 slot root，未分类的生产 env 明确清空。
- `test-teardown.sh` 在删房前 best-effort 归档 kill ledger、精确 boundary/orphan events 与 launch
  manifest；归档失败会大声告警，但不会扩大 kill 权限。新增 byte-stable fleet snapshot helper，供
  host 权限环境比较 test-deploy 前后生产 Codex socket census 与默认 tmux window census。
- Review R1 后用 same-socket `list-windows` census 取代“post-kill 空 stdout 即成功”的宽松判定；
  tmux 3.7c 的空回包只在目标 window id 被非空、合法 census 证明缺席后才是 `cleaned`，
  否则仍 fail closed。真进程 fixture 改为 Linux/macOS 均有的 `/tmp`，CI 缺 tmux/lsof/ps 时直接红；
  `test-deploy` 会在 teamlead 前构建并校验 claude-runner boundary exports。
- 最终 root-cause 修复撤销了无必要的 boundary-first preflight，保留原有“只读 identity probe →
  单一 audited mutation choke point”顺序与 FLY-2211 静态 registry。pre-kill 四字段改用 TmuxAdapter
  已采用的 printable `|` identity；post-kill 不再把 tmux 对消失 `@window` 回落到别窗的非空输出
  当作目标仍存活，而是一律用同 socket 的合法 `#{window_id}` census 判定 `present/cleaned`。
- 更新 529 房路书、QA framework contract、FLY-1182 drill 与 FLY-2271 注记；没有修改
  `CLAUDE.md`、FLY-2352 修复代码或 slot Lead bootstrap。R4 证明 printable separator 修复会让既有
  exact-window cleanup/rollback 在生产首次真正生效；该行为变化由 exact socket、server start time、
  window/exec/generation/fingerprint、audited mutation boundary 与 post-kill same-socket census 共同收窄，
  不是新增宽泛 kill path，但必须按生产 rollout 观察，不能再表述为“生产行为零变化”。

## TDD 与本地验证

- FLY-2454 新增 shell suites 全绿：slot env contract、Bridge boot fence、fleet snapshot helper；
  teardown finalizer 7/7、teardown ownership 3/3、lease contract 8/8。
- CI inventory 明确分类 299 个 shell suites（245 CI、55 manual-only），并枚举 3 个 generalized
  Node suites；本单新增的三个 shell suites 均已注册到 CI。
- claude-runner 完整 package 以单 worker 运行：46 files passed，1129 tests passed、2 skipped。
  TeamLead 真实 tmux 隔离套件 2 passed、1 skipped；Codex holder 用例只因 resident sandbox 禁止
  `/bin/ps` 跳过，两个 real tmux kill 拒绝路径均实跑通过。
- Review R1 修复先得到可消费 RED：workflow cleanup 3 tests failed，preflight 静态守卫 exit 1。
  当时的本地 resident GREEN 为 workflow cleanup + real-process 11 passed/1 `ps` sandbox skip；
  `test-deploy-fly1389` 22/22、qa-room 18/18、cycle 31/31、teardown 7/7，generalized helper
  全套与 Lead CommDB 脚本套件均绿；claude-runner/teamlead builds 均绿。
- R2/R3 reviewer 在 `fc671e146`、`085287bde` 将同一真实 2287 用例稳定复现为 `present`：先在
  isolated assertion、后在 production `expected cleaned` assertion；R3 同时证明 boundary-first hoist
  使 FLY-2211 registry 退化为 24/25。按 Lead ruling 撤销 hoist 后 registry 恢复；测试 fixture 改用
  与 TmuxAdapter 相同的 exact target 与 printable identity，随后本地真实 production assertion 得到
  同一 RED（received `present`）。进一步 unit 将 tmux kill 后回落成 `@0|||` 固化为单一 RED；最终
  unit 10/10 GREEN，真实 2287 在 tmux 3.7c 与 3.5a 各自 GREEN，FLY-2211 inventory GREEN，
  TeamLead build 通过。本 resident 因 `/bin/ps` 权限只能记录每次 2 passed/1 skipped；CI 的 3/3
  是 R4/exact-head CI 必须给出的独立证据。
- `pnpm lint` exit 0（14 条仓库既存 warning、0 error）；`pnpm -r build` 的 22 个参与 workspace
  全绿。
- PR 首轮 exact-head CI（run `34333021964`）在 Quick Gate 真实发现 structure contract 漏项：workflow
  已注册 `FLY-2454 slot isolation contracts`，但 shard-2 的有序期望清单尚未同步。本地先复现同一
  `script-tests-2 test inventory/order drifted` RED，再仅补这一条期望；`ci-structure.test.sh` 与
  `ci-shell-suite-enumeration.test.sh` 随后均 GREEN。
- 原样 `pnpm test:packages:run` 精确运行两次都 exit 1，不能记成全仓绿。两次各只在
  claude-runner 出现一个不同的既有负载敏感 timeout，并伴随 Vitest worker RPC timeout：第一次
  是 500ms `async-exec-file`，第二次是 5s `prompt-overflow.real-tmux`；对应完整文件隔离复跑分别
  7/7、2/2 全绿，claude-runner 完整单 worker 也全绿。最终放行证据必须取本 PR 的 exact-head CI。

## 真实舰队验收边界

在任何 `test-deploy` mutation 之前，按新 helper 采集 host 基线即被 resident sandbox 拒绝：
`/bin/ps: Operation not permitted`。因此本节点没有在缺少 before snapshot 的情况下贸然起房，也
没有把 hermetic helper test 冒充真实 `ps` / tmux 舰队验收。具备 host `ps` 权限的后继 QA 必须
从本 exact head 执行：before snapshot → `test-deploy.sh` → after-deploy snapshot → teardown →
after-teardown snapshot，并要求三份规范化文件 byte-for-byte 相同；差异为零之前不得声称该项通过。

## 审查与边界

本地 `/codex:rescue` review-only companion 已按合同实际启动，没有调用 raw `codex exec`；运行约
两分半始终无输出，人工中止后也没有产生可消费 verdict，因此不计为 review PASS。request-driven
cross-family R1 在 `84c57150f71478784b2c1f9a7fed020e76350c42` 给出 `CHANGES_REQUESTED`；已修复
`realproc-hardcoded-private-tmp`、旧轮 `real-process-tmux-tab-red` 以及 Lead 指定同批处理的
`testdeploy-missing-claude-runner-build`。R2 在 `fc671e1464b30e3bd9642d29861dc841654e406e` 再次
给出 `CHANGES_REQUESTED`：`real-process-2287-tmux-case-fails`。Lead ruling 明确它是本单产品不变量，
R3 在 `085287bde360fe404930e2ba3db56def9b5ab78a` 证明 `f629283b1` 只是移动失败并破坏 inventory；
现已用 `793409b18` 撤销该路径并修复真实 tmux identity/post-kill fallback。同一真实用例的 MEDIUM
weak-oracle 保持 source/target-specific ledger。R4 在 `247a7d0f4ee453634b45f377ec31fd1f9da18720`
给出 `APPROVED`（仅 MEDIUM/LOW advisories）；随后 exact-head CI 的结构守卫要求新增一行 test inventory
期望，因此正式放行只接受包含 `23f43142f` 与本 milestone 的新 exact HEAD 的 R5 及其 CI。

R1 政策允许的非阻塞 follow-up 已记录、未在本轮扩张：
`workflow-window-fence-newly-activated-in-production`（已在本 milestone 与 PR rollout note 显式披露）、
`terminal-mcp-commdb-path-not-relocated`、`plugin-commdb-runtime-lease-hardcoded`、
`claude-lead-commdb-test-not-in-ci`、`workflow-cleanup-empty-stdout-fail-open`(已通过上述 census 消除)、
`isolation-root-empty-string-inconsistency`、`teardown-archives-empty-evidence-for-absent-slot`、
`resolve-isolation-root-throws-through-audited-signal`、`fleet-snapshot-hard-fails-without-default-tmux-server`。

本节点不 dispatch QA、不请求 ship、不 merge、不 deploy、不改生产配置，也不重启 Bridge/Lead。
正常部署仍由独立 updater 在获准窗口执行。

## QA attempt 1 与返工 attempt 2（2026-09-09）

QA claim 977 判 FAIL，阻塞为当前 main 的 Blueprint import 冲突和 PR 正文证据缺项。
Lead question `1c53ec3c-383a-4ea9-8e31-579855a99be2` 明确解除旧冻结指令：只 merge
origin/main、保留双方 type imports，补正文，完整门禁后一次 push，再一次 exact-head review，
最后 needs_review；不碰 slot 或生产。

`df452c541` 是唯一 main merge，包含 `04ff8800aea9e796aef5f3f9f2a3d9004530f65b`；
`git show --remerge-diff` 证明手工 resolution 仅去掉 import 块的冲突标记，同时保留
BoundaryEvidence、CodexAgentHomeIdentity 和 CodexMemorySeedSourceSet。无另增产品代码。

PR 正文已按计划 §6 逐项列证据，补 exact-head CI 链接、未做、§4 回滚、逐条审查 follow-up，
并纠正“生产零行为变化”表述；批准计划保持不变。R6 的真实 reviewedHeadSha 为
`2f0b62338958282085e213600a86eeee6e0c205a`，question
`48779b2a-54c0-429e-9ab0-21c7c8a2a50a`，effective APPROVED；R6 的 bash <4.4 空数组
MEDIUM advisory 按 Lead 裁定继续 follow-up，本轮不修。旧头 CI run 34335701122 14/14 SUCCESS
不代表本次 merge 后新头通过。新的 gate 日志位于 `/private/tmp/FLY-2454-rework2/`，
最终状态与对应 SHA/CI/review 以 PR 正文和 handoff receipt 为准。

QA 亲自执行了旧头 slot 2 真机段，持久证据目录为
`~/.flywheel/qa-evidence/FLY-2454/20260909T1754Z-real-2287-zero-harm/`。
`terminate-response.txt` 是 HTTP 202、inProgress=true，目标 closed=false（tmux identity pending）；
不能把请求接受等同于目标已清理。teardown 完成；诱饵在 before/after 快照均为
`fly2454-decoy|@3634|fly2454-decoy`。原样 snapshot diff 有 4 条新增、0 条删除，
不是空 diff。完整 census 为 codex 35→37、app-server 22→23、exec-app-server-resume 4→2、
tmux windows 125→129、sessions 91→93；起房前 pre-decoy 到正式 before 另有 36→35。
这些计数的归因由 QA/Lead 核实，不能表述为全部计数逐字相同。

原样快照、diff、响应、日志、14 行 ledger 与 boundary-events/launch-manifest 归档均在该目录。
QA 报告其余 mutation 目标属于 slot，并存在真实 mcp no_evidence boundary refusal。
2231 真机长期停留按 Lead 裁定未做；strength-two receipt 未登记（提交时房已拆），
也未发布 QA PASS ship report。上述旧头真机证据不替代新头 QA。


## CommDB 审查返工（2026-09-09，替代实现体）

`53408572-5dfe-45a2-a6c6-f0c9e9dba71a` 在旧冻结头 `7fc824cf9`
返回 CHANGES_REQUESTED，HIGH 为 runner / Bridge CommDB 分裂。
Lead instruction `38377789-3089-4805-bd21-3dc025e6465b` 授权四项最小返工。
`50f3a9639` 完成共享 config CommDB helper 与 Blueprint、plugin rollback lease、
cleanup 消费迁移，修 bash 3.2 空数组，并在契约/路书披露 launch-commit 未隔离坐标。
`2fd551442` 仅修 re-export 排序。完整因果与 RED/GREEN 见同目录任务文档
`engineering/doc/FLY-2454-test-slot-isolation/rework-commdb-evidence.md`。

Blueprint 39/39、plugin+parity 8/8、cleanup 19/19；实际 bash 3.2 空/非空数组
用例均 GREEN。`pnpm lint` 首次两处排序错误，修后 exit 0；`pnpm -r build` exit 0。
完整 package 门禁与后续 shell 守卫正在运行，最终回执位置
`/private/tmp/FLY-2454-commdb-rework/receipts.json`。本笔为新审查头的最后提交；
最终门禁、exact-head CI、review verdict 记入 PR 正文和 handoff receipt，未通过不交接。

产品源码已变化；旧头真机证据不能用作新头验收，QA 需重跑 2287 真机段。
三个 LOW（invalid-root throws、empty-string gating、absent-default-tmux snapshot）
继续保留为 follow-up。不改变两个 launch-commit consumer 路径、不改 2352 或
slot Lead bootstrap、不请求 ship、不合并、不部署。

## QA socket 返工（Lead c7cef5ff，2026-09-09）

QA 在旧头 42f3e74eb 的 fresh slot 3 发现 runner-test-slot-3 被创建到生产
默认 tmux server。旧 slot 2 同名 session 掩盖了创建，不能据旧证据放行新头。

ae18b3b29 仅修默认 session rescue 的 socket 根选择：合法绝对 override 优先，
其次 TMUX_TMPDIR，最后保留原 /tmp 默认。slot override 仍 clear/mustBeAbsent。
显式 -S 调用者登记为 TMUX_TMPDIR confined consumer，inventory 锁定登记与规则；
unconfinedConsumers 披露增加删除守卫。批准计划与 2231 长停留裁定未改动。

默认调用路径在 spawn 边界的 RED 捕获生产 socket 创建命令，修后 3/3
覆盖 slot、override 与生产默认；与既有 TUI/inventory 聚焦合计 68/68。
登记缺失 RED、披露删除 mutation RED，契约恢复后 shell GREEN。
测试拦截创建命令，不冒充真机执行。新头 QA 使用非 2、非 3 的新房号重跑。

lint exit 0，build exit 0。完整 package 和 shell 门禁最终退出码以
PR 正文及 /private/tmp/FLY-2454-socket-rework/receipts.json 为准。
本 milestone 固化后一次正常 push，fresh exact-head review 和 CI 14/14
均通过才能 needs_review；不 merge、不 deploy、不操作生产服务。

本轮完整命令 `pnpm test:packages:run` exit 1，仅 claude-runner onTaskUpdate RPC
timeout：48 files / 1166 passed / 2 skipped，零 assertion failure。
随后六包 no-bail 补测 exit 1，仅 TeamLead 同类 RPC timeout：901 files /
12269 passed / 7 skipped，零 assertion failure；其余五包通过。
五项 shell suite exit 0。不能称本地完整门禁全绿。
最终日志与退出码已归档 `~/.flywheel/qa-evidence/FLY-2454/ae18b3b29-socket-rework/`。
