# FLY-2655 — Discord DAVE 收音恢复与合入前 529 语音验证

**Status**: ⏳ Pending final-head code review and founder 529 QA
**PR**: [#1243](https://github.com/xrliAnnie/flywheel/pull/1243)
**Date**: 2026-09-20

`voice-codex` 的 Discord 收音入口现在复用 `voice-bridge` 的 DAVE-aware 连接，显式启用 DAVE、有界解密容忍与受控诊断。收音链把解密失败、无 PCM 与订阅结束分类为可观测健康事件，用新一代 capture generation、lease fencing、有界重试和冷却后单次 probation 恢复。失败达上限时会进入 degraded/cooldown，不再把整场 session 立即判 failed；下行播报继续可用。冷却期间 founder 离开后，新的合法 QA speaker 可替换 probation 候选人，避免恢复窗口继续盯住已离场用户。

接收健康摘要以单调序列持久化到 `StateStore`，续租窗口会携带它，老 Bridge 不识别新字段时有 400 兼容回退。root session card 只投影有界、去敏的健康事实，使用 digest 去重与重试背压。新的 `voice.session.start/status/stop` 只通过 Lead capability broker 暴露，默认关闭，并在 project/Lead/current-activation 边界内持久意图和幂等 requestId。ON/OFF 两套 department Lead 规则包含相同的 manifest-only 语音指令；不允许 shell、旧 huddle、通用 master API 或调用方自带 project/bot/channel/token 权限字段。

529 QA 工具链增加了独占 slot fixture、slot-private config/CommDB/进程身份、测试 bot 与频道 allowlist、production-disjoint 守卫，以及 prepare/start/stop/verify 收据命令。它要求分支构建的精确 head 与产物哈希，记录进出过渡、协商/解密诊断、framesPassed 与 transcript 存在性，但不复制 production 凭据、不写 production 注册表、不保存 transcript 内容。同一 bot 不允许同时占用生产房和 QA 房；QA 必须使用隔离 bot，或在 Lead 明确协调下让生产 daemon 临时让位并验证还原。

TDD 覆盖 DAVE 配置、解密诊断去敏、几何退避、cooldown/probation、lease 丢失、旧 Bridge 兼容、健康序列持久化、card 去重、Lead 权限/幂等和 QA 隔离负面守卫。QA 返工后的 `voice-codex` 为 23 files / 203 tests；此前 `voice-bridge` 61 / 659、`voice-core` 32 passed + 2 skipped / 330 passed + 4 skipped；Lead 规则漂移定向套件 17/17，rules bundle 30/30；QA launcher 11/11、qa-room 19/19、CI structure、Bridge isolation boot、cycle-bridge 31/31 均通过。`pnpm lint` 与 `pnpm -r build` 通过，lint 仅保留仓库已有 warnings。

本地 `pnpm test:packages:run` receipt 绑定较早的 `31c900f67`：18 个包均到达完整 receipt，其中 16 个通过；`flywheel-claude-runner` 的 3 个 inventory 计数断言在后续提交修复并定向 5/5，`flywheel-teamlead` 的 16 个 token-savings 哈希级联失败在后续提交修复并定向 17/17；teamlead receipt 还有一个已知 `onTaskUpdate` worker RPC error。因此不把这份本地 aggregate 声称为绿；最终权威是不再移动的 milestone head 上的 effective code review 与完整 GitHub CI OK。

首轮 exact-head review 在 `a96532bf5` 找到一个阻塞项：voice fixture 把 `FLYWHEEL_COMM_DB` 放进 Bridge 环境，而 QA 隔离契约规定该变量必须不存在，Bridge 会在启动边界 exit 78。红侧新增的 fixture 边界断言稳定得到 9/10；修复只从 `BRIDGE_EXTRA_ENV` 移除该变量，保留 Lead/voice 进程的 slot-private CommDB 赋值，改后 10/10。`qa-slot-env-contract`、qa-room 19/19、launch-boundary 和 generalized 全部回归通过，且 shell 语法与新测试的 Biome 检查为绿。第二轮 effective review 对 `8d2f2db24` 给出 `APPROVED`；6 个 MEDIUM 和 1 个 LOW advisory 已按协议回报 Lead，没有阻塞项。

`8d2f2db24` 的首轮 GitHub CI 中，scope、Quick Gate、NPM、5 个脚本分片、heavy/light/observation 和 teamlead 2/4、3/4 共 12 个执行项通过。teamlead 4/4 暴露两个确定性的 `lead-token-savings-launch` 红测：FLY-2655 已同步 ON/OFF voice duty 和 compatibility rationale，但遗漏了 OFF 历史源与完整 bundle 的受管字节基线。该失败在本地稳定复现为 2/5；修复只刷新 legacy source/bundle 的 revision、bytes、SHA 和说明，随后 launch、drift、bootstrap、generator oracle、materializer、truth 与 rules bundle 相邻套件共 67 pass、1 个受管沙箱跳过。teamlead 1/4 另有一个 `lead-capability-read` 15 秒超时；同一测试本地独立通过（11.7 秒），没有对应功能改动，交给新 HEAD 的完整 CI 复验。`fc3b43f2a` 之后必须以本文件的新 literal-last head 重做 effective review 与 CI，旧 HEAD 的审批和 CI 结论不得复用。

原 implement 头 `318fe0331` 随后取得第三轮 effective review `APPROVED` 与完整 exact-head `CI OK`，但 QA attempt 1 在真实 macOS 529 slot 暴露工装阻塞并判 FAIL。`test-deploy.sh` 成功把同一头部署到 slot 2；随后 `fly2655-voice-room.mjs` 用 `/tmp/flywheel-test-slot-2` 调用会报 `529_slot_directory_required`，用其真实路径 `/private/tmp/flywheel-test-slot-2` 调用会报 `path_outside_slot`。根因是 slot 根经 `realpath` 规范化为 `/private/tmp`，而 `room-info.json` 与 fixture 清单仍保存 `/tmp`；旧 containment 将这两个 macOS 别名字面比较，导致 prepare/start 都不可达。QA 未叫 founder 进房、未伪造真人证据，并已拆除 slot；因此旧 QA FAIL 保持有效。

返工只执行 Lead 指定的四项。真实 `loadSlot` 级红测先稳定得到 `comm_db_outside_slot`，随后路径解析仅把 `/tmp` 与 `/private/tmp` 下相同 `flywheel-test-slot-N` 视为同一逻辑坐标，并从规范根逐段 `lstat`；不同 slot ID、`..`/绝对逃逸和 slot 内 symlink 仍 fail-close。测试同时调用 `/tmp` 与 `/private/tmp` 两种根，防止再次只测纯函数。第二个红测证明旧 room 在健康短句结束 30 秒后仍使用第二档 1000ms retry；修复把稳定窗口绑定到 room 的“30 秒无接收失败”，正常短句结束不取消，新的 packet/decoder/no-PCM 失败会取消，转绿后下一次故障恢复首档 250ms。计划 §7.2 的 voice-room 参数同步为脚本真实支持的 `--expected-head`；`test-deploy.sh` 自身的 `--expect-head` 不变。其余八条 review advisory 已由 Lead 建 follow-up，本次未扩展处理。

返工 focused 验证为 QA harness 11/11、`discord-room` 7/7、`voice-codex` 23 files / 203 tests、CI structure 通过；`pnpm lint` 与 `pnpm -r build` 通过。依 Lead 指令没有启动第二轮本地 aggregate；本文件所在 literal-last 新头必须重新取得 effective code review 与 exact-head CI，旧 `318fe0331` 的 review/CI 不可复用。

该返工头 `d2105f64a` 取得第四轮 effective review `APPROVED` 与完整 exact-head `CI OK` 后，QA attempt 2 在真实 slot 证明 `/tmp`/`/private/tmp` 修复有效：部署、`loadSlot` 和三个 voice 包构建均通过。prepare 随后在 Discord 远端预检返回 `403 / code 20001 Bots cannot use this endpoint`。根因是工装用 bot token 调用了只允许 OAuth 用户 token 的 `GET /users/@me/guilds/{guild}/member`；同一个 QA bot 调用 bot 支持的 `GET /guilds/{guild}/members/{botUserId}` 返回 200，并通过现有 voice target 与 View/Connect/Speak 权限校验。QA 在 bot 进房前 fail-close、没有叫 founder，并清除了 slot；因此 attempt 2 的 FAIL 保持有效。

第三次 implement 返工先增加工装级守卫，明确禁止任何 `/users/@me/guilds/` 路径并要求 guild-member 查询绑定刚由 `/users/@me` 验证过的 `bot.id`；红侧 QA harness 为 11/12，且只在旧 OAuth-only 路径断言失败。最小修复把成员查询改为 `/guilds/${guildId}/members/${bot.id}`，不改权限计算、token 来源或会话生命周期，转绿后 QA harness 为 12/12。`pnpm lint`（仅仓库既有 warnings）、`pnpm -r build`、CI structure 与 milestone layout 32/32 均通过；依 Lead 指令仍未重复运行本地全量 package aggregate。当前 literal-last 新头必须再次取得 effective code review 与完整 exact-head CI，`d2105f64a` 的旧结论不可复用。

该头 `a682ac026` 取得第五轮 effective review `APPROVED` 与完整 exact-head `CI OK` 后，QA attempt 3 证明 Discord endpoint 修复有效：真实 slot 的 `prepare` 到达 READY，bot 身份、529 语音房、权限与配置预检全部通过。Claude carrier 因没有 self-filter socket 在 `start` 时 fail-close；Lead 随后授权把 slot 2 临时切为 Codex/full-access。Codex slot 在 Lead 启动前暴露两个确定性工装阻塞：voice fixture 在 `LEAD_EXTRA_ENV` 重复声明 launcher 已注入的 `FLYWHEEL_COMM_DB`，被环境赋值去重守卫拒绝；去掉 fixture 后，full-access sandbox 守卫又把 home writer 保存的 `/tmp/flywheel-test-slot-N/lead-workspace` 与运行时 `realpath` 得到的 `/private/tmp/...` 误判为不同目录。bot 因此仍未入房，QA 没有叫 founder，也没有伪造真人证据；slot 注册表已字节级还原，生产 PID/配置未变，attempt 3 的 FAIL 保持有效。QA 遗留的静态 slot 目录/锁及其 teardown 缺陷由 QA/Lead 独立处理，本返工没有触碰宿主残留。

第四次 implement 返工严格处理 Lead 裁定的两个点。第一项先用静态 launcher 契约把 `FLYWHEEL_COMM_DB` 的 Lead 声明数锁为一份，旧代码稳定红为 11/12；最小修复仅删除 voice fixture 的重复赋值，保留 launcher 的 slot-private CommDB，转绿为 12/12。第二项在真实 macOS `/tmp` slot 上新增同目录 `/private/tmp` 别名测试，旧守卫稳定红为 16/17；修复保留字面相等快速路径，仅在不同字符串时比较两侧 `realpath`，不存在、不可解析、多个 root 或实际不同目录仍 fail-close，转绿为 17/17。focused 验证还包括 Codex Lead 环境契约 12/12、launch boundary、test-deploy 集成 24/24、TUI home 集成 64/64、CI structure 与 milestone layout 32/32；`pnpm lint`（仅仓库既有 warnings）和 `pnpm -r build` 通过。依 Lead 既有裁定未重复本地全量 package aggregate；本文件所在 literal-last 新头必须重新取得 effective review 与完整 exact-head CI，`a682ac026` 的旧结论不可复用。

该头 `50a71b161` 取得第六轮 effective review `APPROVED` 与完整 exact-head `CI OK` 后，QA attempt 4 证明前两项工装修复有效：真实 slot 的 config gate、CommDB 与 writable-roots 均通过。新的确定性阻塞发生在 Codex Lead 启动后的 summary tuning 读取：slot 配置和 registry 位于隔离 home，但 `lead-runtime-tuning` 仍从进程 `homedir()` 查 summary receipt，于是把 slot registry 与生产 home receipt 混为一个 scope，返回 `summary_registry_projection_mismatch`。本次返工不改 summary registry、认证 symlink 或 launcher；receipt 与 registry 统一按 `FLYWHEEL_SUMMARY_CONFIG_HOME`、`config.projectsFile` 所属 home、最后 `homedir()` 的严格顺序解析，且 TUI 继续把同一份 config 传入读取器。两个新红测在旧实现稳定得到 projection mismatch，修复后 slot env、显式 projects registry 与 production fallback 共 9/9；TUI config 透传与相邻 rotation 套件共 55/55。

第六轮旧头复审还发现 DAVE 逐包诊断调用同步 `fsync` 的 HIGH：接收包率会把事件循环变成磁盘同步热点。返工将诊断记录放入有界内存批次，默认 100 条或 250ms 写盘但不逐包 `fsync`；session 结束、失败以及外部 receipt 写入仍走持久 append，先排空批次并执行一次 `fsync`。红测先证明 `appendBuffered` 不存在；转绿后连续 64 条诊断在定时 flush 后为 0 次 `fsync`，随后 `session_failed` 边界只触发 1 次。voice focused 为 3 files / 20 tests，evidence 单测 2/2；两个改动包 typecheck、changed-file Biome、`git diff --check`、`pnpm lint`（仅仓库既有 warnings）和 `pnpm -r build` 均通过。

本地全量按 Lead 裁定跳过（2026-09-18 05:50Z），理由 FLY-2467 并发假红 + 九体争抢，aggregate 证据 = 精确头 CI run `<pending>`。被 Lead 终止前的尝试曾在极高负载下报告一个 claude-json 锁释放断言和两个 `lead-capability-read` 断言；终止后未调查、未重启 aggregate，按单 fork 精确隔离分别 1/1 与 20/20 通过。最终权威必须是本文件所在 literal-last 新头的新 effective code review 与完整 16-job exact-head CI；旧 `50a71b161` 的 review/CI 不可复用。

QA attempt 5 在 implement 接手前留下两半未提交的 slot receipt 修复；本轮严格按 Lead 指定的 stash object `117e962e492a1f87d7ddf9df5fa8b7802b847170` 恢复，没有按可变的 `stash@{0}` 猜测。第一半让 Codex slot resolver 把 canonical summary home 投影到 Lead 环境；第二半在最终 slot registry 写定后生成并立即验证 summary migration receipt，再把同一文件字节复制给每个 Lead，避免 registry 与 receipt 分属两个时点。真实 QA deploy 大套件要求的 fixture bot id 同步改为合法的 17 位 Discord snowflake 形状；这只修正测试输入，不放宽生产验证。

本轮同时落实 Lead 要求的三个 code-review advisory。DAVE 诊断 allowlist 新增六个有界、去敏事件：session downgrade/upgrade、transition invalidation、pending transition count、session initialized/reinitialized；先新增 6 个失败断言，再转绿为 16/16。v1 Lead identity digest 只在 `codexVoiceActions === true` 时绑定该 opt-in；absent/false 保持旧 digest 字节兼容，true 必须改变 digest，定向 11/11。计划 §6 的语音连接套件路径已改为实际存在的 `bridge-voice.test.ts`，并把 TeamLead B/C 清单拆成三个显式五文件组，实际分别 47/47、35/35、102/102。

最终本地验证：`voice-bridge` 61 files / 665 tests、`voice-codex` 23 files / 204 tests；四个相关包 typecheck、`pnpm lint`（仅 22 个仓库既有 warning）和 `pnpm -r build` 通过。config 与 flywheel-comm 分包在和多个重包并发时分别出现 1 个与 8 个资源型超时/竞态；按单 fork/单文件复验后 config 11/11，flywheel-comm 228/228（另 1 skip）通过。529/部署工具定向结果为 voice-room 12/12、qa-launchd 53/53、multilead 29/29；fly1389 在 fixture 修正后曾完整 24/24 通过，末次重跑时固定 `/tmp/flywheel-test-slot-{30..35}` 被另一清理者在执行中删除而产生连续 ENOENT，故不把该次碰撞写成绿，交由隔离的 exact-head CI 重新裁定。依 Lead 指令没有运行本地全仓 `pnpm test:packages:run`。

四项非阻塞 advisory 不在本单扩 scope，统一由 FLY-2742 跟进：meeting mode 在 voice opt-in 下默认开放；start resolver 绑定 meeting owner 而不是 caller；有界 resubscribe 无法恢复 wedged DAVE session，需要评估有界 connection re-establish；SDK debug 需要默认关闭、限时及 token 风险控制。它们必须同时出现在 PR Follow-ups，不能被本轮三项已落实 advisory 掩盖。

GitHub billing 恢复后，Lead 授权先落 FLY-2702 再同步本分支；`origin/main` 的 squash 头 `73edeb188` 已通过技术合并 `fcb8e4740` 进入分支。唯一冲突位于 FLY-2567 legacy bundle 夹具：合并态实际生成的 OFF bundle 同时包含 FLY-2655 语音 duty 与 FLY-2702 package-gate WAITING contract，权威结果为 239282 bytes、SHA-256 `c0cd2fd0c147eae5e56bad3dd13724f6d9f6c10aaebd5c786e615d64135effe4`；字节级 materializer 与 FLY-2702 修复的 activation 夹具合计 13/13 通过。同步后的完整 focused 复验为 `voice-bridge` 61 files / 665 tests、`voice-codex` 23 files / 204 tests；TeamLead 三组分别 47/47、35/35、102/102；config 58 files / 884 tests；flywheel-comm 208 files / 2777 passed（3 skipped）；voice-room 12/12、qa-launchd 53/53、multilead 29/29；四个相关包 typecheck、`pnpm lint`（25 个仓库既有 warning）、`pnpm -r build` 与 `git diff --check` 均通过。没有再次运行本地全仓 aggregate；本 milestone 的 literal-last 新头仍必须取得新的 effective code review 与完整 exact-head CI，任何旧头结论均不可复用。

QA attempt 6 证明 Discord/DAVE 收音链已恢复：DAVE transitioned、0 次 `DecryptionFailed`，founder 的三段音频都进入 voice 进程并成功调用 `appendAudio`。新的决定性断点在 Codex app-server 0.153.2 的 realtime v2 集成层：OpenAI 会话已创建，但 app-server 把 `session.created` 标为 unsupported，后续不投影任何服务端事件；同一受管 key/model/PCM 直连 OpenAI Realtime 却能返回转写与 44 个下行音频 delta。本轮因此不改 Codex 版本，按已批准方案将 `voice-codex` 的上下行从 app-server 改为固定 `wss://api.openai.com/v1/realtime?model=gpt-realtime-1.5` 的直连适配器；endpoint、redirect、model alias/snapshot、`session.updated` 回读和证据身份都 fail-close，不接受运行时自由 URL 或模型漂移。

输入侧改为按 utterance 排序的 append/commit 队列，并在容量、音频时间轴和 tombstone 边界内定义稳定 ID；输出侧每次只允许一条 out-of-band audio response，必须同时通过 response/item/content/readback 身份与完整性校验才能进入播放。function/tool item、多 item、错误 PCM、错误 readback 和过期 generation 均不能释放音频。播放以 20ms/960-byte PCM24 帧速率限制，带 backpressure、lease fencing、generation/playback timeout；只有真实提交给播放器的输出才确认 reply。OpenAI 针对单次 `response.create` 的 error 只失败该句、保持会话；无关联协议错误仍 fail-close。语音文本先经确定性去敏和分段（默认不超过 80 Unicode code points）；投影后为空时会发送简短的可观测通知并把收据标为 dropped，不会伪造播报。评审指定的两个边界也已落实：出站 receipt 先 claim 再投影/丢弃；会话在 live 前正常结束时仍以 `realtime_pre_live_end` 判 failed，近到期会话不再接收新输入。

本轮实现头 `1a598f936` 的 focused 验证为：`voice-codex` 25 files / 223 tests；TeamLead voice StateStore/route 37/37；real macOS `/tmp` ↔ `/private/tmp` slot fixture 15/15；离线 Realtime 脚本收据证明 fixed endpoint、redirect disabled、0 API requests、0 Codex processes、no persisted auth/key bytes；`voice-codex` 与 TeamLead typecheck/build、`pnpm lint`（仅仓库既有 warnings）、`pnpm -r build` 与 `git diff --check` 均通过。过程未读取或使用真实 OpenAI/Discord 凭据，未连接真实 OpenAI Realtime，也未运行 529 或 production voice。

本地 `pnpm test:packages:run` 保留为非绿 receipt，不写成 aggregate green。首轮的 47 个 `visual-capture` 失败都是全局 ProofShot 端口锁 `ELOCK_TIMEOUT`，停止后锁已消失，同一文件定向 65/65 通过。第二轮在 teamlead 重包里出现两个与本改动无关的 route 测试运行态碰撞（预期 JSON 但收到纯文本 404），两文件立即同命令定向复验为 100/100；Lead 随后下令终止本机全量，不再为非相关基线竞争消耗唯一 Codex 额度。`1a598f936` 自动 CI 的所有功能执行项通过，唯一红是 Script Tests 3/5 末尾的 FLY-1870 容量闸：前置脚本 0 failures，但 elapsed 1036s 超过 1020s 预警阈值（86%）后 tripwire 主动 exit 1。这不授权 ship，最终 milestone 头的 exact-head CI 由 QA/CI 按既有规则裁定，implement 不调用 `ci-full ensure`。

首轮 direct-Realtime 代码复审在 literal-last 头 `4ac99bb8e` 找到一个阻塞项：服务端 VAD 使用前后 padding，而 Uplink 在本地 speech gate 外持续追加 `null-owner` 静音；原归属算法要求服务端 speech range 覆盖的每一帧都有 owner，因而会把 founder 的有效转写误判为未知说话人并丢弃。新增端到端事件序列先稳定复现为 1/20 红；最小修复仍要求整段音频连续、归属区间内部无空洞且只属于同一个 owner/utterance，仅容忍归属区间两端的无主 padding。配对负面用例继续拒绝归属区间内部的 `null-owner` 缺口。修复后 transport 20/20、`voice-codex` 25 files / 225 tests、typecheck、相关 build、changed-file Biome 与 `git diff --check` 全绿；其余复审 advisory 不扩本单 scope，等待 Lead 后续裁定。

QA attempt 3 在 `4ac99bb8e` 之后的头上用真实构建产物直连 OpenAI Realtime 做协议预检：上行 speech_started/stopped/committed 与转写全通，但下行回读校验 0/3 放行（`speech_readback_rejected`），Raya 永远不出声。根因是 `appendSpeech` 把待朗读文本作为 `role:'user'` + `input_text` 放进 `response.create.input`，模型把它当成待回答的用户消息去回答而不是照读；中文 instructions 压不住 role 语义。R4 返工严格按 Lead 指定的四项：文本改由 `response.instructions` 承载并把 `input` 留空（选这条而不是 `role:'assistant'` + `output_text`，因为它是文档记载的无上下文 out-of-band 形态，不需要伪造一条 assistant 历史项，且与 `conversation:'none'` 语义一致，理由已写入 commit）；`response.done` 之后的 readback 拒绝与 response 失败不再发 `response.cancel`（真服务端对已终结的 response 回 `response_cancel_not_active`），主动取消仍只发一次；新增 API-key 门控的真 API 最小回读契约测试 `realtime-live.test.ts`，用 QA 的三条文本（两英一中）断言 verbatim=true，无 `OPENAI_API_KEY` 时显式 SKIP 并标明；计划 §7.2 的 `--expect-head`/`--expected-head` 说明已与脚本一致。

R4 头的 focused 验证：受管 key 由 shell `source ~/.flywheel/.env` 只读传入、未打印、未复制，真 API 回读契约 3/3 逐字通过（单条 1.5–2.3 秒）；`realtime-transport` 22/22；`voice-codex` 除 live 文件外 25 files / 227 tests；QA harness `fly2655-voice-room` 15/15；`flywheel-voice-codex...` build 与 `...flywheel-voice-codex` typecheck 通过；`pnpm lint` exit 0（25 个仓库既有 warning，无一在 voice-codex）。没有运行本地全仓 aggregate，没有部署 529 slot，没有连接 Discord。QA 重验顺序按 Lead 指令：新头协议预检看到音频放行 → 重部 529 slot 2 → 才邀请 founder 进房；预检不过不得邀请 founder。本轮 mock WebSocket 单测对该缺陷完全不敏感（225 断言全绿仍漏掉），这正是必须补真 API 契约的原因。

2026-09-21 按 Lead 指令（Codex 全部账号额度打满至 9/27，本 run 改在 Claude 上续跑，代码复审走同家族 Claude 复审）把 `origin/main` 通过技术合并 `90c90d192` 同步进分支，带入 FLY-2755 六分片 CI、FLY-2753 本机定向验证与 FLY-2763。唯一冲突在 `scripts/__tests__/ci-structure.test.sh` 的期望分片清单：取 main 的六分片清单，并把合并后 `ci.yml` 实际落在 script-tests-6 末尾的「FLY-2655 isolated 529 voice room」步登记到同一位置，分支自有的「该步只跑一次」断言随之从 shard 5 改到 shard 6；产品文件语义未动。合并头定向验证：CI structure 契约 PASS；`flywheel-voice-codex...` build 与 teamlead / voice-codex typecheck 通过；`voice-codex` 除 live 文件外 25 files / 227 tests；FLY-2567 rules bundle 五文件 42/42；voice StateStore/route 五文件 56/56；codex lead-actions mcp-config 三文件 37/37；voice-room 15/15；`pnpm lint` exit 0（25 个仓库既有 warning，无一在本分支改动文件）。`git diff --check` 报的两处尾随空格来自 main 上其它 issue 的 progress.md，不属于本分支。依 FLY-2753 未在本机跑全量 `pnpm test:packages:run`，全量交 exact-head CI。

本文件是 PR 的 literal-last commit。实现节点没有启动 production daemon，没有重启 Bridge/Lead，没有使用生产凭据，也没有 dispatch QA、merge 或 deploy。合入前 founder 在 529 QA 房的真人双向 RG + meeting、从 `#raya` 文字「语音」自主发起、session events 与 thread 镜像取证，仍是 QA 的硬判据，没有这场不得 PASS 或出 ship 卡。合入后的紧急重启是 Lead/发布流程的独立动作，不属于本节点权限。
