# FLY-2517 — 换体后退役旧 rework phase wake

**Status**: Pending code review / PR CI / ship
**Date**: 2026-09-11

引擎 proven-dead replacement 与 writer convergence 原子记录旧 wake 的精确退役证明。CommDB 幂等关闭旧父通知/phase wake，迟到投递与 start claim 返回 disposed；投影、watch 和最终 hold 事务不再把已换体的旧信提升为 run hold。

历史 phase-wake hold 只在 exact retirement proof 成立时提供 cancel。已完成对应任务的替身通过旧→新绑定、当前 route、内容送达及 completion receipt 验证后，reroute_to 只关闭旧义务，不创建通知或新 route。FLY-2518 staging 失败操作写唯一 failed receipt，解除永久处理中状态。

本地 lint/build 通过。批准的 TeamLead 聚焦组 217 PASS、1 SKIP，Comm 90 PASS，Codex lifecycle/daemon 92 PASS；真实完成后超时再提交 QA PASS、多代换体、双库重启、迁移及 master HTTP 恢复均有可执行回归。完整 package suite 按 Lead 指令交由最终 PR exact-head CI；本文件不宣称该 CI、code review 或 QA 已完成。证据见 ../FLY-2517-replacement-wake-retirement/implementation-validation.md。

PR #1168：R1 effective/raw APPROVED。首次 exact-head CI 发现新增 backfill 未更新两个维护清单契约；已补 census marker 数与四段 drain 顺序断言，10 项聚焦以及全仓 lint/build 通过。该 CI 的 Vitest RPC timeout 仍作为独立失败保留；新 HEAD 等待 R2 与完整 CI，未宣称交付门已完成。

2026-09-12 冲突返工：merge e5d9fa26f 将 origin/main 4bad8ae26 合入（含 FLY-2498、FLY-2520、FLY-2393）。只解决 StateStore import 与 retention count 冲突，保留双方语义；无额外功能改动。合并后全仓 lint/build PASS，TeamLead 定向 142 PASS / 1 SKIP、Comm 20 PASS、Codex lifecycle/daemon 92 PASS。完整 package suites 按 Lead brief 927b750b-266b-40ca-80ca-e11bdaaa239e 交新 HEAD CI；旧头 review/QA/founder approval 不替代本轮重新验证。
