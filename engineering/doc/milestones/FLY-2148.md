# FLY-2148 — Runner 记忆收口回执与分流归因

**Status**: ⏳ Implementation and code review complete; pending independent QA and ship review
**PR**: #1074
**Date**: 2026-09-04

在 FLY-2147 的 `(project, role)` 持久目录和有界短索引之上，补齐每个 DAG 节点在 `complete` / `qa-result` 前写入、量测和回报记忆的收口合同。Claude/Codex role arm 现在携带同一份 spawn 快照；Bridge 持久化 arm、目录、spawn 快照、闭合状态和经校验的回执，因此第二次同角色同项目执行、跨 issue/worktree 复用与 worktree 删除后的保留都可由独立 QA 直接核验。

索引读取仍受 64 KiB 扫描上限、160 行/20 KB 预算和 200 行/25 KB 硬限保护；超限会显式给出 `over_budget` 与首个可能丢失行，不会静默截断。默认模式仍为 `off`，角色之间仍隔离，写入/量测失败不阻塞原完成路径。

验证覆盖 `flywheel-config` 47 文件/759 测试、edge-worker 109 文件/1,294 测试、Claude/Codex adapter 274 测试、comm closeout 165 测试和 teamlead 持久化/路由 147 测试；六个受影响 package 分别构建通过，`pnpm lint` 无错误。所有预变更 golden 均由可执行断言读取，并以临时单字节 mutation 完成 RED→GREEN 证明。请求驱动的跨家族代码审查在 `0453927ce7cd69a9c540e622550662231ded021a` 上通过（gate `4e6cbb75-804b-4628-a252-7842308e8e79`），无阻塞 finding。

按 Lead 的资源保护指令，本节点没有运行递归 monorepo gate、真实 runner E2E、部署、合并或 QA dispatch；PR 记录了所有非阻塞 advisory 及其 disposition，后续由独立 QA/ship 流程接手。

**QA 账本**:独立 QA round 1 在 `9ca0f9ce4` 上判 **FAIL** —— 已入册 generalized DAG 节点用 `complete` 收口时,回执在 `event-route` 的第一个 `session_completed` 分支被提前 return 丢弃,`runner_memory_closeout` 恒为 NULL;修复提交 `19d1a16ce`(配套红测 `c1595546e`),round 2 在 `51a58d005` 上复验 PASS(6/6 个 `complete` 节点全部落列,三态齐)。
