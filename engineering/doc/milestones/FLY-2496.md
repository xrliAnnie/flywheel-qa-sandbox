# FLY-2496 Raya 宿主激活 — 里程碑
Issue: FLY-2496 (https://linear.app/geoforge3d/issue/FLY-2496)
日期: 2026-09-13
基于: ../FLY-2496-raya-host-activation/implementation.md

**Status**: ⏳ Pending ship

班车先完成授权核验、精确目标构建、身份及静默窗口复验，再停旧 Raya；停机恢复与 P3 对账均有持久边界。提供 source-only 初始化、人工对账和激活 proof 工具，支持 pending-install 与健康全舰重启后的 SHA 重绑定。

lint/build 通过；迁移与 proof 集中测试 78/78，prestop 20/20、updater 38/38、wrapper 37/37。全量 package 套件仍保留红结果：新增生存探针清单漏项已补齐、隔离 5/5；另有 onTaskUpdate worker timeout。按 Lead 裁定不重跑全量，以精确 HEAD CI 为准。

首轮 CI 发现 summary 读者保留期分类漏项，已补精确 candidate_guarded 登记；gate 测试 8/8 与全源码扫描通过。PR #1179 保持非 draft，修复后的新 HEAD 重新接受评审和 CI。

R2 HIGH 已修复：P2 stop intent、P3、P4b 可在冻结其余绑定并通过 Bridge/registered verify 后重绑定 Flywheel SHA，继续原 checkpoint。七项 MEDIUM 由 Lead 后续处理。另修 Linux 初始化测试的可执行文件 fixture，隔离 16/16 通过；新 HEAD 需重新通过 CI 和代码审查。

未执行生产 register/install/restart/deploy。宿主激活、v2 receipt/public verify、下一轮 summary 阅读合并及来源标记检查留给授权后的运行与 QA。

R3 在 fd9d5ab76 上 APPROVED，八项 MEDIUM 已报告 Lead。该头 CI 的变量分类漏项已补为 SHA marker 路径，flag drift 隔离测试由 2 failed / 12 passed 转为 14/14。最终分类修复后的 HEAD 重新注册 review 并运行 CI。

Engine conflict rework：从 51931efec 同步 main e551e320，唯一冲突保留两侧 retention consumer 登记（merge d7c27bebb），无额外功能修改。复验 lint/build exit 0；consumer 8/8 + scanner ok；迁移 69/69、prestop 20/20、updater 38/38、wrapper 37/37。新 HEAD 重新请求 review/CI，生产激活仍为独立授权步骤。

Rework review HIGH（Lead 423db2f5 授权）：旧 job 在 bootout 前持久 disable 并读回/落盘；恢复、proof、P7 与后续班车核验旧 job 不会因 RunAtLoad 和遗留 dist 在登录时复活。隔离正反重启测试通过；迁移/proof 70/70、updater 38/38、prestop 20/20、wrapper 37/37、lint/build exit 0。最终修复头需新 review/CI；其余建议保持 follow-up，未执行生产操作。

macOS readback 更正（Lead 33257716）：两个解析器接受 disabled/true、拒绝 enabled/false/未知值；真实格式夹具先红后绿，proof 14/14、shell 39/39、lint/build exit 0。仅更正格式，不移动 stop intent。最终修复头重新评审/CI；未执行生产操作。

Resume conflict-only sync（Lead a9398009）：main14866f7e5 已合入 feature branch；保留双方 retention 登记，合并重复的 SHA path 分类（先 lint/build 红、后绿）。lint/build exit0，migration/proof73、prestop20、updater39、wrapper37、alert31、config53、consumer8+scanner 全部通过。原4d1160a5e CI34809758763 attempt3 全绿；新头待独立 review/CI。本提交为推送前最后一提交，review 后不推文档。生产激活未执行。

TUI proof HIGH（Lead8d1f34cb）：真实 tmux3.7c pane command 的外层引号/转义导致旧 parser 拒绝，私有 socket 回归先红；改用 list-panes 与只解码的 shlex，保留唯一活 pane、home/workspace/remote/thread 与出生时间约束。proof24/24、lint/build exit0；旧两项 HIGH 未动。真实 ps 在测试环境 EPERM，进程与 verifier 用夹具，不能作为生产 proof。旧头060383130 CI34820844552 全绿但 review CHANGES；本修复最终头须新 review/CI。此里程碑为最后提交，评审期间不再推送。

Locale-free TUI correction（Lead75986202 最后一轮窄修）：私有 tmux 无 LANG/LC_* 的回归先红；可打印竖线分隔身份字段、完整保留命令内竖线，-u 辅助 Unicode。移除 -u 对照仍通过，proof24/24、lint/build exit0。旧 HIGH/M-L 不动，ps/verifier 夹具限制不变。新头重送评审/CI；若第三次同源 HIGH，冻结交 Lead 治理，不自行接受。
