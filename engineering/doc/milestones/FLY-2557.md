# FLY-2557 — Epic started intake

**Status**: ⏳ Pending review, CI and isolated QA
**PR**: [#1199](https://github.com/xrliAnnie/flywheel/pull/1199)
**Date**: 2026-09-14

Adds durable department-routed `epic_intake` events on the existing Bridge clock, continuous-started episode deduplication, childless Epic cards and pending patrol work, authenticated result evidence, and Lead §0.11 decomposition/ledger/thread handling. Runner dispatch and ship approval semantics remain unchanged.

Local full aggregate was **interrupted — not relied on** per Lead host-capacity instruction; exact-head CI is the handoff authority. Focused verification, actual Lead rule assembly, isolation incident and remaining live QA checks are recorded in `engineering/doc/FLY-2557-epic-intake/implementation-evidence.md` and `qa-runbook.md`. No deployment or production restart is included.

Bounded review fix `4dbd2ab34` isolates per-root history failures as `intake_history_unavailable`, preserves the project watermark and lets healthy intake/page/dependency/resolve work continue. Fresh focused verification: 87/87; lint passed with 18 warnings; recursive build passed. New exact-head review and CI pending; isolated live QA remains outstanding.

Second review round: classify real missing-entity lookup errors; batch pending metadata and share bounded validated-history cache. The 17-root hourly fixture drops from 4,183 to 341 requests; resolved/inactive episodes stop direct polling. Focused 100/100 plus cache suite 7/7 (overlapping), lint and recursive build pass. Fresh exact-head review/CI required; no live QA acceptance claimed.

Authorized dirty-refresh rework (`f83502685`): terminal intake rows cannot reactivate; successful hosted snapshots clear captured dirty revisions even when cells are omitted; persistent failures use durable one-/five-minute retry delays then stop after three attempts. Database reopen, legacy migration and concurrent revision protection verified; 50/50 focused tests, lint/build and retention gate pass. Fresh-head review/CI pending; further HIGHs require Lead governance.

Engine conflict rework (2026-09-15): merged main `7f6e88d39` via `e95b2c585`, retaining intake behavior and current Epic presentation/mailbox semantics. Fresh recursive build, typecheck, lint (21 warnings), retention gate and its 8 tests pass. Materialize isolated rerun passes 7/7. Main's unchanged audit-only archive regression remains failing alone; scope question `20c36632-1a39-4d38-9f09-e2b3f1c5ff32` awaits Lead disposition. New code review, exact-head CI and QA retest are required; this is not QA acceptance or ship authority.

Final rework: Lead authorized the upstream 2563+2567 candidate predicate fix in `79505b971`; its existing regression now passes. Main `b9418b780` synced in `c2832c003` with both imports preserved. Final archive/inbox/intake suites **78/78**, recursive build and lint pass (21 warnings). New exact-head review/CI and controller QA retest remain required.

Compatibility addendum authorized by Lead `15821a99`: computed four drifting source hashes and recorded factual ON/OFF rationales in the compatibility manifest; no product source changes. Drift/oracle/bootstrap/intake rules pass **23/23**. Prior head's CI performance check failed 51.83ms against <50ms; final-head review and CI must independently pass before handoff.

OFF-bundle HIGH fixed in `5819c1b75`: exact approved §0.11 added while preserving all historical text, with digest-validated additive capture of both source/body goldens under Lead rulings e1eff36e/f535c40e/dd6d00cc. Two capture runs are byte-identical; bad overlays fail before writes. Existing Codex/OFF-oracle/drift/intake tests **50/50**, Claude/CoS launcher **54/54**, lint passes with 21 warnings. No test assertions weakened. Fresh effective code review and exact-head CI remain required.

Final authorized sync to main `451812138` in `0e632c2da` preserves intake and FLY-2447 business-wake payloads/formatters. db.ts compatibility remains unchanged; two runtime digests/rationale updated for business wake. Final focused **58/58**, Claude/CoS **54/54**, typecheck/build/lint pass (21 warnings). Review `f19acfd9` approved the prior head and must be automatically requeued onto this final head under ruling ae563ea8; exact-head CI remains required. No further main sync without reporting to Lead.
