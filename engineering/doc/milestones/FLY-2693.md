# FLY-2693 — 语音健康可见与连续失败告警

**Status**: ⏳ Pending exact-head effective code review and automatic PR CI; frozen-head QA request and host acceptance are QA-owned
**Date**: 2026-09-19

`com.flywheel.voice` 不再只能用“进程还活着”冒充健康。daemon 把空转成功、封闭分类的失败、连续失败 episode、live 后续租成功的恢复证明和会话结束写入独立 durable ledger；最近成功在进程退出后仍可读取。SQLite 使用 WAL、`synchronous=FULL`、`wal_autocheckpoint=0`，只在事务 commit 后做有界 `PASSIVE` checkpoint，checkpoint 争用不会把已提交记录截断或回报成失败。任意错误文本、token、完整 URL 和会话正文都不进入投影或告警。

健康判断以 StateStore 的权威需求投影为前提，不看 PID：无需求且无进程是正常休眠；有需求未起、超过 60 秒仍未 ready、没有 live + renew 的恢复证明、或连续 3 次空转失败都会打开 `voice_daemon_unhealthy` episode。真实空转成功只清 poll failure；它不能关闭进房失败。可信 live + renew、正常 terminal proof 或明确取消只关闭对应 scope。同一 episode 只发一次，恢复后熄灭，再次故障才产生新告警；长期故障不周期刷频道，但持续显示在固定页。

前两轮有效 code review 的六个 HIGH 已全部关掉：

1. daemon 先写 failed/recovered、Bridge 后到的终态事件及 baseline-only attempt 都能幂等对账；延迟的旧失败不会覆盖较新的 daemon failure boundary。
2. `lead-alert.sh` 发送前按 source high-water 读取最终 authority，6,605+ 变更历史仍可完成 POST 捕获，不再被固定 200/6,400 条扫描卡死。
3. Bridge 重启后可验证已持久 prefix，只应用未见 suffix，cursor 不再永久 `demand_change_gap`。
4. 已知 trigger 合同变化会轮换 demand source；新 source 分页不继承旧 revision。Vitest 的 voice state root 已隔离，未再写生产默认根。相同 revision / 不同 digest 会先事务性发布 `unknown/unavailable + demand_source_unavailable`，保留旧 source/cursor/digest 和未解决故障，再 fail-closed；匹配权威快照才恢复 available。
5. startup spool 只在唯一权威需求存在且 `observedAt >= requiredAt` 时归属；helper 去重不再绑定后来的 demandId。多进程以 OS 释放的 flock claim，helper durable receipt 前保留原件，成功后原子归档；崩溃、并发、symlink/权限和容量边界有回归。旧版含 demandId 的 ledger digest 仅在持久字段完全匹配时原子迁移，篡改仍拒绝。
6. `no_human` 与 `daemon_shutdown` 投影为非故障取消，生产 payload 经过真实 helper 后不会创建 episode、notification 或 failure count。

R3 新增的唯一阻断 HIGH 也已关闭：语音健康 section 现在只在 Flywheel 固定页渲染；其它项目不会被永久插入“语音健康 / 尚未采集”，同时 Flywheel 自身缺少 cell 时仍 fail-closed 显示未知。HTML 与 Markdown 使用同一 project gate，并有非 Flywheel 回归。

设计复审 advisory 逐项处置如下：demand digest 只绑定 trigger 实际观察列（另含稳定 session identity），无关字段更新不改变 revision/digest；WAL 自动 checkpoint 已关闭并改为 commit 后 passive checkpoint；同一 spool 的并发实例不会互相删除未确认原件。固定 HOME spool 在不同 state-root Bridge incarnation 间仍无独立 namespace，记 follow-up；manual demand 不猜 retry 关系，杜绝假恢复，但无显式 retry 链时的老化/预算仍记 follow-up；长期 episode 保持“一次告警 + 固定页持续显示”，周期再提醒预算仍记 follow-up。完整处置与其它非阻断限制见同任务 `follow-ups.md`。

告警复用 FLY-2669 已合入主干的 Lead alert route、频道解析、权限和 strict-delivery 合同；voice ledger 仍是 claim/retry/恢复事实的唯一来源。Bridge 使用既有 cadence 驱动投影与投递，没有新增 daemon 或第二调度器。

现场约 50 次 2 秒超时与主机 load 40–50 同时出现，但相同已部署 client + 当前环境已有 68/10/6 ms 连续成功证据，不足以证明扩大超时或新增退避更正确；本单保留 2 秒默认值，以结构化健康证据供后续校准。founder 要求的“无需求无进程”由 FLY-2701 的按需启动/空闲退出生命周期承接；本单保留 lease 自愈及通话不随 Bridge 重启而死的设计边界，没有改造或重启生产 launchd。

当前 review-revision 的聚焦验证：Python helper **47/47**；六个 TeamLead 显式套件 **62/62**；6,605+ 历史的 voice alert route **PASS**；TeamLead `tsc --noEmit` **PASS**。自动 PR CI 首轮进一步暴露 `FLYWHEEL_VOICE_IDLE_HTTP_TIMEOUT_MS` 缺少 non-flag accounting；补入 FLY-2693 numeric-tuning ledger 后，config truth/drift **54/54**、config `tsc --noEmit` 与改动文件 Biome **PASS**。后续自动 PR CI 的 heavy job 在 **1/1,342** 处精确暴露新增 helper-crash 测试的 `SIGKILL` 缺少 FLY-2211 inventory 登记；补入 `qa-only` 清单后，focused kill-path suite **5/5**、JSON Biome 与 `git diff --check` **PASS**。下一轮自动 Script Tests 2/5 暴露抽取式 brake-alert harness 未提供新增的正交 spool helper；隔离该 side effect 后 brake-alert suite **55/55**，真实 voice-wrapper suite **33/33**。R3 HIGH 修复后 Epic clarity + voice-health suites **12/12**，TeamLead typecheck 与相关 Biome **PASS**；timeout allowlist 文案修正后 config flag truth **40/40**。安装包 smoke 随后暴露旧断言仍期待原始 credential 错误；改为精确验证封闭脱敏错误后，隔离 npm cache 的真实 pack/install smoke **26/26**。实现主体的独立 spec review 与 code-quality re-review 均 **PASS**，最终精确头仍待 effective review。按 Lead 明确指令，本轮没有运行全仓 test aggregate，也没有调用 `ci-full ensure`；只为 focused packaging 证据构建了明确 package 闭包。自动 PR CI 必须在交 QA 前对最终头全绿，但不能替代 QA 后续冻结头的权威请求与真机验收。

恢复节点把 `origin/main@487799b80` 技术合并为 `b0819d2a9`。`plugin.ts` 冲突保留 voice projector 的启动/既有 cadence tick，同时接受主干对 legacy auto-narrow 启动路径的退休；`lead-alert.sh` 冲突同时保留 voice source-owned route 与主干 `codex_home_migration_overdue` 隔离 route。合并后的新鲜验证：`pnpm -r build` **PASS**；voice-codex **26 files / 252 tests PASS**；TeamLead voice/demand/projector/session 与 kind/copy/owner/Epic/delivery **7 files / 65 tests + 5 files / 82 tests PASS**；Python health + startup spool **47 + 4 tests PASS**；voice route、Codex-home migration route、CI structure 均 **PASS**；wrapper **33/33**、brake harness **55/55**、config contracts **96/96**；TeamLead/voice-codex/config typecheck **PASS**；`pnpm lint` exit 0（仓库既有 advisory diagnostics，无 error），相对 `origin/main` 的 branch diff check 与冲突标记审计 **PASS**。全仓 package aggregate 仍按 Lead 指令不在 implement 节点本地运行，最终头的自动 PR CI 与 effective code review 仍是交接前门禁。

该头的自动 PR CI run `35422890902` 随后在 TeamLead shard 2 精确暴露 child-process census 漏登记：`startup-alert.ts` 已有原 meta-alert 与新增 startup spool 两个有 timeout 的 `spawnSync`，清单仍写 1。隔离 `bridge-child-process-census.test.ts` 本地复现同一 RED 后，`409502e6f` 只把 `sync_child` 更新为 2，保留 `standalone_runtime_bounded` disposition；同一 targeted test **1/1 PASS**、该 JSON Biome 与 `git diff --check` **PASS**。旧 run 与旧 review 绑定前一头，不作为修订头门禁；推送 literal-last milestone 后重新申请 exact-head review，并由新的自动 CI 验证修订头。

随后 effective review R4 在 `f34da4ddd` 指出三个 HIGH，并均以 RED→GREEN 关闭。Bridge startup guard 不再把超过 60 秒但仍持有有效租约的 `claimed/warming` 会话误报为 `startup_not_ready`；两种状态的真实 StateStore 回归均先复现额外 `record-startup`，修后只调用 `evaluate`。helper 在 startup/daemon terminal 已锁存后，会持久消费后到的权威 demand transition 并推进 cursor，而不是以 `demand_event_invalid` 回滚整页；startup failure→两次 required→normal completed 的真实 helper 链已证明 episode 熄灭且 cursor 到 3。`voice_daemon_unhealthy` 由 source-owned ledger 完整承载，所以 shell 的所有 voice early-return 路径跳过 generic release-readiness preflight intent，不再制造永远无法退休的 `capture_gap`；429→重试→sent→duplicate/recovery 路由同时证明零 readiness gap。修订后 Python helper **48/48**、TeamLead 相关套件 **6 files / 40 tests**、voice-codex health **3 files / 34 tests**、voice route、generic readiness observation、Codex-home migration route、wrapper **33/33** 与 brake harness **55/55** 全绿；`pnpm -r build`、TeamLead typecheck、改动文件 Biome、Python compile、shell parse 和 `git diff --check` 均通过，`pnpm lint` exit 0（仅仓库既有 advisory diagnostics）。

精确头 `44e4e9afc` 的自动 PR CI run `35424536139` attempt 2 在配额恢复后实际执行了全部矩阵；除 TeamLead 1/4、4/4 及汇总门外均绿。1/4 暴露本单新增全局 `FLYWHEEL_STATE_DIR` 测试隔离与旧 fleet shell 套件自有 HOME 沙箱冲突，导致三个套件统一读错配置根；4/4 暴露新增固定页语音健康 unknown 标记吃掉旧的 512 KiB 夹具余量，以及完整 Bridge 启停在并行 CI 负载下撞到默认 5 秒测试上限。`e95d73cf2` 只清空 hermetic shell 子进程的 state-root 覆盖、为合法新增 markup 留出 500 bytes 夹具余量，并把完整 Bridge 启停用例上限放宽到 15 秒。修后受影响 fleet cases **3/3**、Epic publisher **23/23**、Bridge 启停 **1/1**、TeamLead typecheck、改动文件 Biome、`pnpm -r build`、`pnpm lint`（仅既有 advisory）与 `git diff --check` 全绿；fleet 整文件 **11/11 assertions PASS**，唯一非零原因是已知 `[vitest-worker]: Timeout calling "onTaskUpdate"`，不冒充 aggregate green。该测试修订头仍须重新取得 effective review 与自动 PR CI。

测试隔离修订头 `3db7557ea` 随后取得 effective code review R4 APPROVED，并由自动 PR CI run `35428664611` 在全部 14 个执行 job 与 `CI OK` 汇总门上验证通过。最终审计发现远端主干已前进到 `origin/main@4b8bed5c8`，因此没有沿用旧头收据：在 TURN 下以 merge commit `3edbfc648` 无冲突完成技术同步。同步后 `origin/main` 已是当前分支祖先，`pnpm -r build` **PASS**，`pnpm lint` exit 0（仅既有 advisory），三个受影响 TeamLead 文件的精确用例 **5/5 PASS**，branch diff check **PASS**。该技术同步使旧 review/CI 不再绑定最终头；本 literal-last milestone 推送后重新申请 exact-head effective review，并等待该精确头的自动 PR CI。

2026-09-20 implement 恢复节点在 TURN epoch 23 下重新核验设计 R2 effective APPROVED，plan SHA256 仍为 `96d1d5ded7c195ac49888673378aeac98bd7ba045e6740134e53e98309211310`。冻结 lockfile 安装缺失依赖后，`pnpm -r build` **PASS**；voice-codex 两组显式回归 **113/113**、TeamLead 两组显式回归 **112/112**、Python health/startup spool **52/52**、voice alert route 与 CI structure **PASS**、wrapper **33/33**、brake harness **55/55**、config truth **41/41**；`pnpm lint` exit 0（25 条仓库既有 warning，无 error）。计划要求的变异体阳照也重新取证：临时移除 daemon 故障分支的 `observeHealth(failure)` 后，真实 `VoiceDaemon.run()` 回归按预期收到零失败回执并 **RED**；恢复该调用后同一用例 **GREEN**，工作树无变异残留。macOS 本地未运行会触发真实 `Terminal.app` 的全包 aggregate；自动 Linux PR CI 仍是最终全量权威。Lead 在 question `46e4792d-18cd-4022-bc11-74bc0f3682d3` 裁定不等 FLY-2755 / PR #1276，要求最终 head 的自动 CI 逐 job 全绿后正常 exact-head review 与 `needs_review`；ship 前主干同步由 land 阶段受控处理。若唯一红仍为 Script Tests 容量闸，本节点只回问 Lead，不改 CI、不手动 rerun。

精确头 `fd93e053d` 的 effective code review 随后指出唯一阻断 HIGH：projector 每 tick 最多读取 32×200 条变化，却只在读到最终投影后写 cursor；积压超过 6,400 条时每轮都从原 cursor 重读并永久停滞。修订先用 33 页真实导出回归复现首轮 32 次读取后 cursor 仍为 **0**，再让 page-cap 错误原子携带已验证的 source/cursor，由 unavailable 路径保存中间 cursor；下一 tick 从 **32** 继续并在 **33** 应用最终权威投影。source-reset 守卫同时改为比较已投影 payload 的 source，确保新 source 的分段追赶不能绕过“旧 unresolved fault 不得被空源抹掉”。修后 TeamLead voice demand/projection/delivery/projector/spool/Epic 七个显式文件 **44/44**、TeamLead build、改动文件 Biome、`git diff --check` 与 `pnpm lint` **PASS**（仍仅 25 条仓库既有 warning）。旧 review 与旧 CI 只绑定 `fd93e053d`；本 literal-last milestone 推送后重新申请新 head review，并只接受新 head 的自动 CI。

精确头 `0151856f7` 的 round-2 effective review **APPROVED** 并确认 page-cap HIGH 已关闭；自动 CI run `35541330881` 的 15 个 assertion job 全部通过，唯一红是 Script Tests 3/5 在所有断言结束后的 FLY-1870 容量闸（`elapsed=1056s`、budget `1020s`、20 分钟 cap 的 88%），`CI OK` 仅级联红。Lead 在 reports/questions `c78bddcf-b2b9-4ddc-be56-0b9242e49034`、`51a94070-2e65-44f2-a3c5-ce8ae5c95e1e` 裁定本 PR 仍必修两项：`heartbeat_stale` 必须由真实成功/renew 时间戳生产，非 `sent` 回执不得声称 `message_id`；新头若再次仅红 FLY-1870 容量闸，可先 `needs_review` 交 QA，但 ship 前仍须在 FLY-2755 后取得真全绿 exact-head。修订先以两个 helper 用例复现 idle/active stale 均无 episode，再让 required+idle 只按真实 `lastIterationSuccessAt`、required+active 只按真实 renew `lastProgressAt` 的 60 秒边界开 `heartbeat_stale`；真实 idle success 或 active progress 分 scope 熄灭。shell 阳照模拟 Discord 已返回 ID、但 source 只确认 retryable 状态，旧输出错误携带 `message_id`；修后 `delivery_unknown` 如实不带 ID，后续 retry 仍能 `sent`。定向验证：Python helper **50/50**、voice alert route **PASS**、strict delivery **31/31**、Python compile、shell parse 与 `git diff --check` **PASS**。本 literal-last milestone 推送后重新申请新 head effective review，并由新头自动 CI 逐 job 取证。

QA claim `1355` 随后确认告警、恢复、固定页、启动故障、幂等和进程退出后可读性均已通过，但在真 daemon 连续 95 秒 / 20 次成功迭代后，stdout/stderr 仍为 0 字节；这证明 durable health 已有事实、`/tmp/flywheel-voice.log` 却仍无法直接证明成功。返工严格限定该日志臂：`VoiceHealthReporter` 只在 helper 已提交 `record-result` 后输出结构化成功行，首次完整 idle 成功、poll failure 后恢复、以及之后每 60 秒有界 heartbeat 各有独立 event；行内的 generation、producer/observation seq、last success 与累计 counters 全取提交回执，不以本地猜测冒充库状态。生产 CLI 将该 callback 接到 stdout，因此 launchd 的既有 `/tmp/flywheel-voice.log` 路径可见；重复回执也携带 counters，响应丢失后的幂等重试仍能完成日志。TDD 红测先得到 0 行对期望 3 行，最小实现后真实 helper + SQLite 用例 **19/19**，voice health / daemon health / alert 三个显式文件 **35/35**，voice-codex typecheck、改动文件 Biome、`git diff --check` 均通过；`pnpm lint` exit 0（只报告仓库既有 advisory）。旧 `883b8ee85` 的 review/CI 不覆盖返工；本 literal-last milestone 推送后重新申请 exact-head effective review，并等待同一精确头的自动 PR CI。

成功日志返工头 `f25d2455e` 的自动 PR CI run `35548358198` 随后执行全部 16 个检查并全绿（含汇总 `CI OK`），但 effective review round 1 指出唯一阻断 HIGH `voice-health-incident-bound-mismatch`：helper export 最多给 projector 200 个 open episode，而固定页 schema 只接受 32 个 active incident；第 33 个会使整页 materialization 持续抛错。修订用 33 个长期未恢复的 session episode 先复现旧投影错误地产生 `complete + 33`，再让 projector 与 schema 共享 `MAX_VOICE_HEALTH_ACTIVE_INCIDENTS=32`，仅投影前 32 个并把 source 标为 `truncated`，保留 unhealthy 真相且不让整页停更。projector / StateStore projection / voice fixed-page 三层显式回归 **21/21**、TeamLead typecheck、改动文件 Biome、`git diff --check` 与 `pnpm lint` **PASS**（lint 仍只有仓库既有 advisory）。round 1 的其余 6 个 MEDIUM/LOW 保持非阻断 advisory，未夹带扩项；旧 review 与旧 CI 不覆盖本修订，本 literal-last milestone 推送后重新申请新头 review 和自动 CI。

有界 incident 修订头 `da467ba87` 的自动 PR CI run `35549592084` 全绿、effective review round 2 APPROVED，但 QA attempt 2 在真 daemon 上以差分隔离证明第三条日志臂未生效：生产 5 秒轮询小于 20 秒 `idle_success` 聚合窗口，首次成功之后每次观测都进入聚合分支，而定时器触发的 `flushAggregate` 只刷新 `lastNormalEnqueuedAt`、从不调用 `successLogEvent()`，60 秒 heartbeat 判据因此永久冻结（256 秒 / 48 次成功 / 0 条 heartbeat）；先前的假单调时钟单测从不触发真实聚合定时器，是假绿。R3 返工选择让聚合 flush 也承载 heartbeat：`flushAggregate` 在 kind 为 `idle_success` 时按同一 60 秒判据申请 log event，日志仍绑定 helper 已提交的 `record-result` 回执，counters 与 observationSeq 不取本地内存；未选择把判据挪到回执侧，因为回执路径没有单调时钟且会把日志耦合到持久化重试。新增真实时间回归 `health-heartbeat-realtime.test.ts` 使用真 Python helper、真 SQLite、真 `setTimeout` 与墙钟，以 5 秒间隔连续观测 135 秒，断言至少 2 条 heartbeat、间隔 ≥55 秒、counters 单调且失败计数为零，并在 `stop()` 之前快照以排除关停 flush 的干扰；同一形态在修复前复现 **0 条**，修复后 **1/1 GREEN**（135 秒）。initial / recovered 两行与 incident 绑定修正未动。定向验证：voice-codex health / daemon-health / health-alert / daemon / startup-alert 五个显式文件 **63/63**，`vitest related` **32/32**，voice-codex `tsc --noEmit`、改动文件 Biome、`pnpm --filter "flywheel-voice-codex..." build`、`git diff --check` 与 `pnpm lint` **PASS**（lint 仍只有 25 条仓库既有 warning）。旧 `da467ba87` 的 review 与 CI 不覆盖本返工；本 literal-last milestone 推送后重新申请 exact-head effective review，并等待同一精确头的自动 PR CI。

R3 返工头 `b9cb1b22d` 推送后 PR 变为 CONFLICTING，GitHub 因此不为该头启动 pull_request CI：主干 FLY-2755 已把 Script Tests 按 p75 权重拆成六片，而本单的 `Test — FLY-2693 voice health source` 步骤与其 ci-structure 守卫仍写在旧的单片位置。在 TURN 下以 merge commit `166f8e3c6` 技术同步 `origin/main@e82193650`：该步骤原样迁入 `script-tests-6`，紧跟 FLY-1814 launchd fleet contracts（voice wrapper 与 install-voice-launchd 同片，步骤本身仅数秒）；ci-structure 守卫改为经六片 union 定位，保留 exactly-once、不可条件化、必须使 PR gate 失败与精确命令四项断言，并同时接受主干对 FLY-1948 守卫的同类改写。产品代码没有因解决冲突而改动。合并后的新鲜验证：FLY-2693 步骤三条命令本地 **PASS**（Python health、startup spool、alert route），ci-structure 守卫 **PASS**，`pnpm --filter "flywheel-teamlead..." --filter "flywheel-voice-codex..." build` **PASS**，voice-codex 五个显式文件 **63/63**，TeamLead voice-health demand/projection/delivery/projector/recorder/spool/Epic/poller 八个显式文件 **48/48**，voice-codex 与 TeamLead `tsc --noEmit`、`pnpm lint`（仍仅 25 条仓库既有 warning）与 `git diff --check` **PASS**。macOS 本地运行 Python helper 套件时须关闭 stdin（`</dev/null`），否则 `init` 子命令会继承交互 socket 而阻塞；CI 的 stdin 已关闭，不受影响。该技术同步使 `b9cb1b22d` 的 review 请求不再绑定最终头；本 literal-last milestone 推送后重新申请 exact-head effective review，并只接受新头的自动 PR CI。

同步头 `edf595581` 的自动 PR CI run `35574073817` 共 17 项，其中 14 项绿（含 Unit light 内的 voice-codex 全包套件与新增 135 秒真实时间回归）；两项红各有明确归因。Unit (teamlead 1 of 4) 唯一失败是 required wall-clock 审计守卫：新回归把 `lastIterationSuccessAt` 与 `Date.now()` 派生的下界比较，被判为真实时长的绝对比较；该断言并非必要，改为状态断言——每条成功日志的 `lastIterationSuccessAt` 必须是本测试提交过的 `observedAt` 之一。修后审计守卫本地 **1/1 PASS**，真实时间回归仍 **1/1 GREEN**（135 秒、≥2 条 heartbeat），voice-codex `tsc --noEmit` 与 `git diff --check` **PASS**；产品代码未动。Script Tests 6/6 的失败在 `test-cmux-sync-hooks-integration.sh` Scenario F（FLY-1272）：real tmux server 在该场景前消失（`no server running on …/tmux-hooks-integration.sock`），本单 diff 未触及任何 cmux/tmux 文件，且同一步骤在 FLY-2643 / 2688 / 2654 / 2655 的最新 run 均通过，判为孤立 flake；已向 Lead 申请重跑授权，不自行 rerun。CI OK 仅为级联红。旧 review 请求与旧 CI 绑定 `edf595581`；本 literal-last milestone 推送后重新申请新头 effective review，并只接受新头的自动 PR CI。

审计修订头 `6a33134ca` 的自动 PR CI run `35575874610` **17/17 全绿**（含 CI OK、wall-clock 审计所在的 teamlead 1/4，以及上一轮抖动的 Script Tests 6/6）。同头 effective review round 5（request `d4608668`，同家族 Claude 复审）返回 CHANGES_REQUESTED：5 个 HIGH、21 个 MEDIUM、11 个 LOW（policy：MEDIUM/LOW 非阻断）。五个 HIGH 全部落在 R3 之前、Codex 前四轮已批准的代码上；逐条对照源码核实为真后，Lead 裁定本轮全修，并明确 F4 以 founder 批过的 plan §3 为准、F3 不再 defer。每项先 RED 后 GREEN：(1) demand 快照的 `observedAt` 只是行/源的数据时间戳，休眠主机 90 秒后固定页永久「读数过期」——runtime 改用 Bridge 观察时钟盖 `observedAt`，runtime 回归先复现 20 秒后仍冻结在 T0；(2) HTML/Markdown 的 unavailable/unknown/stale 早返回位于 unhealthy 分支之前，会丢掉投影刻意保留的活动故障——两种渲染面改为保留故障列表并以 caveat（unavailable / stale / unknown / truncated）标注，`data-voice-health-caveat` 可被断言，与班车状态段同一合同，同时覆盖 truncated 的 MEDIUM；(3) trigger_invalid / change_gap / demand_overflow 快照在 Bridge 内抛错、从未到达 helper，掉触发被渲染成「正常休眠」——recorder 改为以 `sourceStatus` 透传、不带分页，helper 发布 unknown/unavailable（`demand_source_unavailable`）且保留 source/cursor/digest，`record_result` / `evaluate` 通过 `_restore_demand_marker` 不再抹掉标记（对应 MEDIUM `result-write-erases-demand-source-unavailable`），只有权威 available 快照能恢复；(4) 持有效租约的 claimed/warming 会话被永久排除在 startup_not_ready 之外——按 plan §3 加 `createdAt + 60s + 60s grace` 上限，过线即以 createdAt+60s 为阈值开 episode；R4 那条 Codex 排除意见与 plan 冲突，按 Lead 裁定以 plan 为准收回；(5) 租约续期的 progress 清零 `failure_streak` 并重锚 `first_failure_at`，两个 poll 阈值都被击穿（复现：30 秒内 4 次失败零告警）——progress 不再触碰 poll 范围的失败窗口，只有真实 idle_success 才清；编码旧行为的 helper 用例改写为新合同。验证：Python helper **52/52**、startup spool 与 alert route **PASS**、ci-structure 守卫 **PASS**；TeamLead voice / epic-page / wall-clock 审计 **64 个文件全绿**（`voice-self-filter-probe` 在 runner 长 TMPDIR 下 unix socket 路径超长，改短 TMPDIR 后 **24/24**，与改动无关）；`vitest related` **130 文件 / 1488 用例**；TeamLead build 与 `tsc --noEmit`、`pnpm lint`（仍仅 25 条仓库既有 warning）、`git diff --check` **PASS**。21 MEDIUM + 11 LOW 逐项处置已写入 `follow-ups.md`（另已顺手关闭 truncated 渲染、标记保留、follow-ups 文案与 CI 步骤耗时注释四项）。按 Lead 指令：修完送 round 6；若 round 6 只剩 MEDIUM/LOW 或对旧代码新挖 HIGH，交 Lead 以 review-ruling 收口；同头自动 CI 须 17/17 再交卷。本 literal-last milestone 推送后重新申请新头 effective review。

R5 返工头 `d40241c45` 的自动 PR CI run 全部 **17/17 绿**（含 Script Tests 6/6），同头 effective review round 6（request `4e773e4c`）**APPROVED**、无 HIGH，剩余 19 MEDIUM + 10 LOW 非阻断。Lead 以 review-ruling 收口：其中 MEDIUM `realtime-heartbeat-test-wallclock-flake` 被复审在负载机上实际复现（135 秒窗口只出 1 条 heartbeat，因 20 秒聚合定时器漂移），一个进主干后会间歇红 required job 的测试比多一轮复审更贵，故裁定 test-only 修：期望心跳数改为按测试自己提交的观测跨度推导——floor((span − 20 秒聚合窗 − 10 秒漂移余量)/60 秒) 且 ≥1，运行 165 秒以保住 R3 的「≥2 条」要求，成功计数下限同法推导，≥55 秒间隔断言不变；所有断言都是观测跨度上的操作计数下限，主机越慢只会放宽，required wall-clock 审计 **1/1 PASS**，回归本地 **1/1 GREEN**（165 秒），voice-codex `tsc --noEmit` 与 `git diff --check` **PASS**；产品代码未动。新增 LOW `leased-startup-grace-shorter-than-presence-grace`（60+60 秒上限比 daemon 120 秒 presence grace 紧数秒，至多一次自愈告警）与 LOW `demand-marker-restore-drops-reason-class`（标记恢复未带回 reason_class）按裁定记入 `follow-ups.md`，其余沿用 R5 处置。本 literal-last milestone 推送后对新头申请 round 7 同家族 exact-head review；Lead 已明示 round 7 若再有新 HIGH 直接上报、不再自修；同头自动 CI 须 17/17 后再 `complete --route needs_review`。

本节点没有复制/删除生产数据库，没有读取真实凭据，没有清理审查发现的 `~/.flywheel/state/voice-health/observations.sqlite`，没有重启 Bridge/Lead/voice 服务，没有制造生产故障或发送真实频道告警，也没有 dispatch QA、merge、ship 或 deploy。真机 success heartbeat、依赖断开阈值内告警、恢复熄灭、固定页状态以及 live ≥60 秒仍由 QA 在受权环境验证；“进程在跑 / launchd 健康”不算证据。

本文件是 PR 的 literal-last commit。推送后只为该精确头请求 effective code review；implement 节点完成后交还控制器，不自行冻结 QA 头、不请求 full CI、不 dispatch successor。
