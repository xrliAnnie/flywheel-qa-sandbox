# FLY-2408 — Founder ship-gate 标题铃铛

**Status**: ⏳ Pending ship
**PR**: #1118
**Date**: 2026-09-07

## 交付

- Discord issue thread 进入 snapshot 定义的 founder approval gate 时，标题显示行首 `🔔 ⏳待批`；旧式 main runner 的 `approve` stage 同样渲染该标记。
- `🔔` 是受管 overlay，保留原有主状态、model marker、issue key 和标题长度上限；解析器只在后接已知主状态时消费受管铃铛，不会删除用户标题中的普通 `🔔`。
- approval、design/implement/QA kickback、blocked、reconnect title ownership、durable `approved_to_ship` 以及 code-review/QA/merge hold 均不会错误鸣铃；hold 会从现有状态恢复真实 pre-gate stage，不新增状态存储。
- active workflow `run_id/current_node_id` 已加入显示 fingerprint，重启与 sweep 会幂等重渲染真实状态。

## 验证

- TDD：在生产标题刷新调用点新增 Auto-QA、Codex pending、merge block 回归，先观察 3 个预期失败，再以统一 review-hold 判定修复至通过；Lead 指定的 held-stage truth 调整同样先观察 `code_review → approve` 失败，再修复为 `code_review`。
- 聚焦 TeamLead 标题套件：5 files / 222 tests passed。
- `pnpm lint`：exit 0（14 个既有 warning）；`pnpm -r build`：exit 0。
- core：19 files / 219 tests passed；按 Lead 指示排除会真实驱动 Terminal.app 的 `**/tmux-viewer.macos.test.ts`。
- non-core 聚合运行：单个无关 tmux guard 断言在并发环境下失败，精确隔离复跑 1/1 passed；最终全仓 verdict 以 PR exact-head CI 为准。
- 代码审查 Round 2：`APPROVED`（reviewed head `65e98bbb8`）；其后仅追加 reviewer advisory 中 Lead 明确要求的 held-stage truth 修正、progress 与本 milestone 文档，并按 Lead 指示不再扩展其余 advisory 或开启新轮次。

## 验收边界

- 未修改 gate transition、approve 识别、merge 或 deploy 逻辑。
- Founder 手机 Discord 侧栏截图属于后续 QA/founder acceptance；本 implement 节点不派发 QA，也不伪报该人工证据。
- 回滚时先移除 refresher 的 attention 映射，再移除 writer overlay，最后移除 parser 的受管 `🔔` 识别，避免旧标题无法收敛。
