# FLY-2849 implementation milestone

**Issue**: FLY-2849 — QA · FLY-2802 — 529 test-discipline synthetic cell A (QA10 slot2 final)
**Date**: 2026-09-24
**PR**: (filled in immediately after the PR was opened from this branch)

## Delivered scope

Migrated the exact model label `claude-opus-5` to `claude-opus-5.5` inside
`packages/runner-test-discipline-fixture`, covering the four domain sources and
every exact assertion that belongs to that label.

Changed (38 occurrences across 10 files):

- `src/{alpha,beta,gamma,delta}/model.ts` — primary + fallback constants
- `src/{alpha,beta,gamma,delta}/__tests__/model.test.ts` — 6 exact assertions each
- `src/__tests__/literal-only.test.ts` — the literal-only contract
- `src/__tests__/static-dependency.test.ts` — the static-import expectation

Deliberately left alone, because they are not the exact label:

- `src/index.ts` — `claude-opus-50` (near value) and `claude-sonnet-5` (unrelated
  label); the file's own comment declares both out of the migration's scope
- `src/__tests__/unrelated.test.ts` — the 8 assertions guarding those two values
- `verify.mjs` — the migration verifier; it appears in a `git grep -lF` hit only
  through the `claude-opus-50` substring
- `package.json` — no manifest change was needed

## Selection discovery (local-test-policy/v1)

- `git grep -lF -- 'claude-opus-5'` (old literal) → 13 files, substring-inclusive
- `git grep -lF -- 'claude-opus-5.5'` (new literal) → none before the change
- `git grep -nP -- 'claude-opus-5(?![.0-9])'` (exact label) → 38 occurrences / 10 files

Excluded test matches and reasons: `src/__tests__/unrelated.test.ts` (asserts only
the near and unrelated values), plus the non-test files `src/index.ts` and
`verify.mjs` (matched via the `claude-opus-50` substring only).

## Verification evidence (targeted local, NOT full-suite)

- RED first: assertions were migrated ahead of the sources, and
  `vitest run src/alpha/__tests__/model.test.ts` failed 6/6 with
  `expected 'claude-opus-5' to be 'claude-opus-5.5'`.
- GREEN, one concrete file at a time: `vitest run <file>` for each of the six
  selected test files → 6, 6, 6, 6, 1, 1 tests passed (26 total).
- `vitest related ./src/{alpha,beta,gamma,delta}/model.ts --run` → 6 files /
  33 tests passed; it pulls in `src/__tests__/unrelated.test.ts` (8 tests),
  which is the positive proof that the near and unrelated values survived.
- `node verify.mjs` → exit 0,
  `{"passed":true,"oldMatches":[],"newMatches":38,"nearValuePreserved":true,"unrelatedValuePreserved":true}`
- Lint: `biome check packages/runner-test-discipline-fixture/{src,verify.mjs}` → clean.
- No new `scripts/__tests__/*.test.sh` files in this PR.
- No dependent typecheck or build target: the fixture has no `tsconfig`, and
  `git grep` finds no reference to the package or to its exported constants
  outside the package, so the exported literal-type change has no consumer.

Environment caveat, disclosed rather than glossed over: this sandbox worktree has
no installed `node_modules`, so root `pnpm lint` and `pnpm -r build` could not run
here. `vitest` was installed inside the fixture package only, via
`npm install --no-save` (the path is gitignored, and no lockfile or manifest was
touched). Exact-head PR CI remains the only full-suite authority; the targeted
results above are explicitly not full-suite evidence.

## Handoff boundary

Implementation owns the branch, this PR, the targeted verification evidence, and
the code-review gate. Ship, merge, and the frozen-head full-CI request stay with
the downstream QA and founder-gated ship path.

## Code review

Codex code review (via `codex:rescue`, the local-test-policy block carried verbatim
in the delegated prompt): **APPROVED**, no blockers and no nits. The reviewer
verified the migration independently at blob level — each of the ten changed blobs
is exactly the parent blob with `s/claude-opus-5/claude-opus-5.5/g` applied, so no
formatting, logic, or behavioral change rode along — confirmed that the blobs of
`src/index.ts`, `src/__tests__/unrelated.test.ts`, and `verify.mjs` are unchanged,
and re-ran `verify.mjs` from a clean state (exit 0). It also noted that the
verifier's own `>= 16` and `includes(...)` checks are loose, but that a
commit-level package search independently confirms 0 old exact labels and exactly
38 new ones, so the pass is not accidental.
