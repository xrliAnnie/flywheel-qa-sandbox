# FLY-2456 529 房重启演练工具 — 实施交付
Issue: FLY-2456 (https://linear.app/geoforge3d/issue/FLY-2456)
日期: 2026-09-10
基于: ../FLY-2456-bridge-restart-drill/plan.md

本提交为本轮 literal last milestone。工具与完整宿主路书待 exact-head 正式评审和 CI；本节点没有执行真机演练，没有 reown 成功率或生产零影响验收结果。Lead 执行 PRE/R1/R2，并向 FLY-2352 thread 交报告供 founder 决定。

交付包括 durable intent/adopt/receipt、slot 环境隔离、第一 recovery episode 与 activation 证据、生产污染及 fleet/process/window 对照、哈希绑定的单轮 verdict 与零表格双轮报告。路书有 62 个 Bash 段与 E01–E39 顺序，cycle 1 等待 receipt 后至少 60 秒再取证；真实 guard scanner PASS，独立审阅 blocker 已关闭。

计划未改，不改 FLY-2352、房间 deploy/cycle/teardown 原语、restart guard 或生产服务。唯一 packages 例外是 Lead 批准、扫描器产生的两条 qa-only inventory 记录，637→639；不改 scanner 或测试逻辑。

验证：pnpm lint 和 pnpm -r build exit 0。两次 pnpm test:packages:run 均 exit 1：首次 inventory 缺登记（修复后 isolated 5/5）；第二次已有 stdin 500ms timeout（isolated 7/7）。四包 no-bail 补跑 exit 1：edge-worker 1318 pass / 14 skip，voice-bridge 649 pass，voice-codex 122 pass；teamlead 12451 pass / 1 fail / 7 skip，失败为已有 chokidar pre-ready error 与 cleanup hook timeout。Lead 按 FLY-2492 裁定保留这些失败，停止本地追全绿，以冻结头机审和 CI 为剩余门槛。

新增 Node 全套本地 466/467，唯一 liveness fixture 因 lsof EPERM 得到 unknown；独立宿主同源 1/1 PASS 另列，不能改写本地失败。最新路书 3/3、嵌入 Python 6/6、CI 枚举通过；没有新增 shell suite。详细日志、ruling IDs 和最终路书 scanner digest 见 [实施记录](../FLY-2456-bridge-restart-drill/implementation.md)。

[宿主路书](../FLY-2456-bridge-restart-drill/host-runbook.md)与[批准计划](../FLY-2456-bridge-restart-drill/plan.md)随本分支交付。完成正式机审和 CI 后才走 needs_review；不 dispatch QA，不 merge，不 deploy。

本轮 R1 审查后修复 `managed-snapshot-budget-blocks-drill`：生产双库串行受管采样，源句柄关闭后 release；后续扫描/归因/换体重验读取哈希绑定的私有派生证据。slot 批次与轮询均在最后 reader 后释放；owner 必须由 Lead 在演练时显式提供有效 execution 并整轮固定，canonical 目录校验与未获授权的 release 均 fail-closed。修复提交 `621dce52a`，另含 CI 固定顺序及精确 path|token 登记。Lead 范围裁定 `953b3f3a` / `64b14d8c`，计划未改。

本轮 lint/build exit 0；聚焦及 manifest 151/151、路书 8/8、残留扫描 76/76、CI 枚举及真实 scanner 62 段通过。全工具批次 476/477 exit 1，仍为已知本地 liveness EPERM；后加的 owner/release/path 测试用聚焦回执单列。历史全包失败按 FLY-2492 保留。正式 R1 的 4 MEDIUM / 3 LOW 仅披露，见实施记录；不声称已消除。

本提交再次作为本轮 literal last milestone；普通 push 后冻结整个 HEAD，登记新头机审并等待 CI 14/14。若新审再有同源或新 HIGH，按 Lead 要求停手询问。当前尚未完成 needs_review，尚无真机演练验收结论。

本轮发布安全修复 `965ef45af`：按 Lead c6d23454 处理 R2 唯一 HIGH `report-pair-leaks-raw-process-command-lines`，单轮与双轮 Markdown/HTML 改用白名单字段；合成 token 红→绿覆盖 argv/env/raw payload/嵌套对象，并证明成功率、事件顺序、attempt、PID 和归因仍保留。原始私有证据不删减。按 27fb8a73 仅向 relayStateAllowedFiles 添加 question-only helper 的精确路径。其余本轮 3 MEDIUM/2 LOW 仅披露，不扩范围。

本轮 lint/build exit0，报告/判决/CLI 39/39、守卫4/4、枚举和真实 scanner62通过；全工具479/480 exit1仍为已知lsof EPERM。旧头CI 34461715247为11成功，失败是精确登记缺失、teamlead并发5000ms timeout及汇总；isolated12/12不可替代CI。Lead允许仅当新run同一测试再红时重跑failed jobs一次，仍红则ask。此次为批准的剩余两轮中第一轮；再有HIGH先ask。

本提交为本轮 literal last milestone；push 后冻结HEAD，重新登记精确头机审与CI。尚未needs_review；宿主真机R1/R2及founder决定仍待Lead完成。

R3正式审查在3b0104e1e APPROVED，4MEDIUM/4LOW仅披露于实施记录。最后限定修复 `6aaff01e7` 按Lead964fd665仅登记七条受管快照retention读取；守卫5/5、扫描ok:true，CI YAML全部17个Quick Gate运行步骤本地exit0，含build/typecheck/lint及所有守卫。未改scanner、查询或产品。历史全包/lsof失败仍保留，CI34463716638登记失败不能被本地通过替代。

本提交为最后限定轮的literal last milestone；普通push后冻结全HEAD，申请新头正式审查并等待CI14/14。若再有HIGH先ask Lead；尚未needs_review，未执行真机演练。

QA attempt1 claim1016 发现 live tmux 链接行导致 fleetIdentity 误报歧义；按 Lead a3adb70e-5b9f-4656-bba9-2065027ac5b7 / 4b8b8776 有界返工，先按全局window_id折叠，优先非cmux base session，与产品lookup排序一致。折叠后同execution多窗、同window marker或name冲突仍拒绝。只改fleet helper、测试和脱敏夹具，批准计划与其它逻辑不变。

真实只读采集当时112行/102window_id/86execution，比QA较早111行多一个链接；保留实际结构、稳定别名和原始SHA，未造111行。真实夹具先RED再GREEN，并验证倒序输入和冲突反例，fleet54/54。lint、全仓build、CI枚举通过；全工具483/484 exit1仍仅本地lsof EPERM。历史全包失败仍按FLY2492披露，以新头CI为完整门。此提交为本轮literal last milestone，推送后冻结HEAD，正式新审+CI14/14后再次needs_review交QA attempt2。未执行真机重启演练。

审查25c8e665在1f4334750指出HIGH `linked-window-fold-missed-sibling-consumers`：identity已修，但fleet快照解析与decoy收养仍拒绝链接行。Lead c72aa018-0ee0-4510-8f24-4fbb98c43f9b批准同源完整闭合；修复d8194cf43抽QA-only normalizer，identity、parseFleet/fleetDiff、decoy统一按全局window_id折叠与base优先，保留marker/name冲突、不同窗口歧义。未改产品原语或计划。

同一112行真实脱敏夹具新增live diff、拆房后单窗终态归因、decoy收养与负向守卫，先RED后相关74/74GREEN。lint初次两处import排序错误已修正并重跑exit0；全仓build、CI枚举、retention/receipt扫描均exit0。全工具488/489 exit1仅已知lsof EPERM，历史全包失败继续披露。本提交literal last；普通push后新头机审+CI14/14再needs_review交QA2。再有同源HIGH先ask Lead，未执行真机演练。


Implement attempt 3 按 Lead `6a9ff087-2f74-4cc2-bfc3-0b021c7651d1` 修正宿主前置：`cc66caef3` 在原始 SHA 校验后仅归一化内存 WAL header[18]/[19]，避免 memdb SQLITE_CANTOPEN；磁盘快照与只读约束不变。路书 §01 改用真实 workflow owner 与 exact execution；未改其它守卫或演练步骤。真实 checkpointed WAL 与实际 Bash helper 均先 RED，再相关 **81/81 GREEN**，保留错误 SHA、写入和错误 owner 的负向检查。

最终 lint/build、CI 枚举、retention/receipt scanner 及其 9/9 测试、路书 62 段真实 scanner/dry-run 通过。全工具 **489/490 exit 1**，唯一仍为已披露本地 lsof EPERM；历史本地全包失败继续按 FLY-2492 披露。PR body 补 MEDIUM 无 exec 标记窗口消失无法归因、76 个 runner-fly1674-* 窗口（Lead 已清、归因 FLY-2500）、file-adopt 改动面，移除旧头 pending 描述。

本提交为本轮 literal last milestone；普通 push 一次后冻结 HEAD，新的正式审查与 CI14/14 后 needs_review。同源 HIGH 再出先问 Lead。尚无本节点真机演练或生产零影响验收结果。

Implement attempt 4：`5fb11ecfe` 按 Lead `6c54c527` 与 NUL 裁定 `557ddeae` 改为有界 CommDB 投影。derive 与 commScan 共用字面 marker / `instr` 谓词，允许合法 NUL TEXT 且能检出 NUL 后 marker，非 TEXT 仍 fail-closed。每表完整 rowCount 与所选列的逐行 canonical SHA 保留，未命中原文不落盘；schema 与 views 保留。路书传 slot/lead/已落 receipt 的 exec 与 replacement，并明确 OWNER_EXEC 必须是当前未完成 DAG execution。production/observation 投影未改。

40MB 无 marker、命中逐值等价、流式指纹、NUL 与路书参数均红→绿；聚焦61/61、最终路书9/9、lint/build、CI枚举、retention/receipt9/9及扫描通过。实际路书 scanner/dry-run62段通过，digest `e227797154bb4ac54cc68fd8309e821cc2ccf59ee6ae0e146e507ee2c6558805`。全工具494/495 exit1仍仅本地 liveness unknown；全包进程尚在运行，最终回执在 PR/Lead 报告更新，不改写历史失败。此提交为本轮 literal last milestone；一次普通 push 后冻结整个 HEAD，正式新审与 CI14/14 后 needs_review。真机两轮及生产零影响验收仍属 Lead。


R7 两项 HIGH 经 Lead15753bb8接受并授权修复（接缝12ffba78），修复85332fcaa：生产alert按managed/foreign/unparsed分类、保留历史披露；comm/prodState/alerts门改为before→after零新增，原comm rowid与state既有稳定主键保证集合差，删除不抵销新增。state/observation投影未变，演练步骤未改，报告仅加baseline/new数字字段，不泄漏内容。旧1c5932b33的CI重跑14/14但reviewCHANGES，不能用于本头。

红绿与最终聚焦103/103；全工具502/503 exit1，唯一仍为已知本地liveness限制。lint/build、CI枚举、retention/receipt9/9及扫描、62段真实scanner/dry-run通过（digest9114c7cf6ec53bd28cd3cf4e3e7454a533d43344b504b70b94b2a2ac35537be3）。历史本地全包8fail及onTaskUpdate超时保留，不重跑追绿。此提交为本轮literal last；普通push后冻结整个HEAD，申请新review与CI。再有同源HIGH停手ask Lead；本节点未进行真机演练。

最终交接固定：修复提交85332fcaa；验证为聚焦103/103、工具502/503（已知liveness限制），其余本轮lint/build/scanner通过。以本milestone提交作为新审查绑定头，全部进度记录已先落盘，审查期间不追加任何提交。

R8：c899f9103 修复 Bash3 空数组展开及子shell EXIT trap 变量生命周期，Lead4962f881/7e2dd870授权；宿主显式/bin/bash与Homebrew Bash5各9/9，失败导出release归零，dry-run15/15及62段真实scanner通过、lint通过。保留历史本地全包与liveness失败披露；两条新advisory仅披露。此milestone为最后提交，一次普通push后冻结，新的正式review与exact-head CI通过后needs_review，真机两轮仍属Lead。

返工 #5：按 Lead 宿主 DEVIATIONS #1–#17 回填。b5539a4d6 原样提交76份 R1/R2 判定/比较/报告/driver；1d9075347、1a9a19c7e、523c99c47 修正 tools/runbook。R1 0/2、R2 0/2、修后drift=0但owner-before-commit仍失败；17项宿主路书缺陷、F1–F5及FLY-2503投影上限承诺在PR body披露。R2复用R1前置，源文件路径与字面秘密扫描规则依Lead e0a04d0a；操作版迟到残留检查用受管快照/错误即停/release依Lead414b4a59，原driver字节不改。

#6/#7/#8/#12/#16全部RED→GREEN，另#9/#10/#15及public分类回归。最终聚焦140/140、Bash3/Bash5各11/11、Python8/8、lint/build及CI枚举通过；实际62块scanner/dry-run通过。全工具508/509唯一liveness unknown/alive限制；本轮全包exit1，TeamLead926文件/12452测试通过7skip但1个unhandled onTaskUpdate timeout。保留这些失败，完整门采用exact-head CI14/14。本提交为本轮literal last milestone；普通push后整个HEAD冻结，正式新review与CI通过后complete needs_review1150。409 park_wake按Lead要求仅报告，不park/原地ask/retry。未修改pinned plan、packages或生产服务。

返工#5新审第一轮3827fb8b8的CI34513943250为14/14，但review cfff7285 CHANGES（唯一HIGH：lead-proc-attribution-transient-outranks-production），不是可交接头。Lead300233ea授权修复b9a3d76df：历史host-runs脚本字节不动；新增操作版生产签名先于瞬态，移除宽泛规则及任意瞬态祖先豁免，路书改调用新版本。3种生产进程+未知worker在旧版4个断言RED，新版Python12/12、Node28/28、lint与62段dry-run通过。历史R1/R2 verdict不复算，交下一轮宿主演练；MEDIUM/LOW仅披露。此milestone为修复后的literal last，新review与新头CI14/14后才needs_review1150，期间冻结不推。

返工#6（implement attempt7）：修复 b2cb5d550 经真实R1 B1证据推翻QA的failures.length诊断；Lead d51bd7ba/7c465a92批准从持久事件证明耗尽、兼容materialized后的两种真实换体事件链。真实RED(other!=replaced)→observer50/50，相关104/104。原76文件与两轮原observation证据hash不变，新重算产物独立存recomputed/，顶层真实报告写drift2→0与owner故障三具；R1 B1 replaced，R1/R2 B2无换体耗尽标签触发2次，R1 B3恢复阴性对照；R2 B1缺耗尽事件仍other。成功率0/2→0/2与生产FAIL/待归因保持真实。

本轮lint/build/CI枚举/实际62块scanner-dryrun通过；全工具519/520唯一既有liveness unknown/alive失败，本地全包历史onTaskUpdate exit1按FLY-2492保留。HTML零表格/无模板句已验，浏览器权限拒绝及headless134使视觉未验，交QA。计划、packages、原宿主归档、生产服务均未改；decoy清理由Lead负责。此milestone为本轮literal last，一次普通push后冻结新HEAD并登记正式review+CI14/14；旧头8bb5835c6的批准不能用于新头，待通过后needs_review1150。
