# FLY-2026 — 浏览器按需启动、空闲态 census 与 crashpad 归因披露

**Status**: ✅ Merged (PR #943)
**PR**: #943
**Date**: 2026-08-25

普通项目复用 `setup-mcp-on-demand.sh` 的 default-off 写入、Playwright 正向 QA 与 full-MCP 回滚，仅 QA browser、ProofShot、Claude-in-Chrome 等真实调用按需拉起；ProofShot 异常路径由 `finally` 补 stop，正常 stop 失败仍保留原语义。只读 `fly-2026-browser-idle-census.mjs --once --print` 以一次冻结采样和完整 ancestry 区分 managed active、PPID 1 orphan 与 external，空闲 `<10` 计入 active + orphan，证据缺失即 `unknown` 非零退出；receipt 绑定 exact `HEAD` 与构建身份。R1/R2/R3 exact-head code review 均已 APPROVED；attempt 2 已 rebase 到 `origin/main@88c3df6b9`，`CLAUDE.md` 冲突双保留 FLY-2014 与 FLY-2026、零源码冲突改动，GitHub 对 rebased head `a94f61638` 给出非空 `merge_commit_sha=df3b94acb1014e0e5cbfe601d57f8ec9f132b1c2` 且 exact-head CI 正常出现。QA 指出的 PPID 1 Chrome crashpad 不再静默丢弃：census 以 `ruledOut.unattributedPpid1CrashpadHandlers` 显式列出 executable basename 精确为 `chrome_crashpad_handler` 且 `ppid=1` 的行；候选缺 comm/command/age 时 fail-close，已披露行不进入 managed `<10` 分母。R4 review 有效证伪首版只认 `--type=crashpad-handler` 的合成条件：live host 8/8 handler 为 PPID 1、0/8 含该 token；最终 fixture 与 classifier 改认真实 macOS `--monitor-self` / `--monitor-self-annotation=ptype=crashpad-handler` 形状，并以 exact executable identity 防止 Chrome main/child handler 误入。research/plan/runbook 同时固化边界两半：经验上每个活浏览器约有界伴随 1–2 个 crashpad helper，零活浏览器或无界增长不得免责；本 implement sandbox 的 `ps` 仍因 `EPERM` 被拒，宿主相关性与零可见窗为独立 QA/operator 的 unverified 项。attempt 3 收口了真实 CI 暴露的两个边界：40 字符 helper 改名为 `isReparentedChromeCrashpadHandler`，JSON 字段 `unattributedPpid1CrashpadHandlers` 保持兼容，production secret scanner 对旧名命中、对新名放行，隔离 npm cache 的 payload real-install smoke 14/14；command-pass 现在独立识别 `ppid=1` 且完整命令含 `/chrome_crashpad_handler` path segment 的候选，真实带空格 Chrome 路径在 comm 缺失或空白时均 fail-close。最新验证：census 25/25、相关 classifier/census/reaper 59 pass/1 skip、built census shell 6/6、全仓 lint 0 error（8 条既有 warning）、22 workspace build 全绿；TeamLead 全包 9,512 pass/6 skip，28 个高负载 timeout 均不在本次路径；canonical package gate 仍仅被受管无 GUI 会话的 2 个真实 Terminal.app/HiServices 用例阻断。旧 exact-head CI run `32807536042` 的 Script Tests 2/2 是 63-step 真实 runner，失败步精确为 payload real-install smoke；attempt-3 code head CI run `32814297941` 的 11/11 jobs 全部 terminal success，Script Tests 2/2 以同样 63 steps 成功，Script Tests 1/2 为 34 steps 成功，均有真实 runner_name，最终 `CI OK` 3 steps 成功。R6 对实现与里程碑 head `28d167451` APPROVED，R7 对只含 progress/CLAUDE 差异的 `6f134299b` APPROVED；随后以 merge commit 同步 `origin/main@5a8fe51bf`，唯一冲突为 CLAUDE 里程碑相邻插入并双保留 FLY-2026/FLY-2022，FLY-2026 源码无冲突改动，非阻塞 advisories 已转 Lead。未执行 settings apply、进程清理、重启、部署或 PR merge。

---

**账目来源(FLY-2045 cutover)。** 这条里程碑原本写在 `CLAUDE.md` 的那张表里,由 #943
在 2026-08-25 合入(merge commit `d27e85a3f`)。FLY-2045 把账本搬出 `CLAUDE.md` 时,
它已经在表内,所以那一行**逐字节冻结**在 `ARCHIVE-pre-FLY-2045.md`(受 sha256 + 字节数双 pin);
本文件是同一条账目在新机制下的**活入口**,正文取自那一行,一字未改。
表内那行的 status cell 原文是:`✅ PR #943 needs review；founder-gated host acceptance pending` —— 写于 PR 合入之前,保留原样以便对照;
上面的 `**Status**` 是合入后的事实。

**这是一次性 cutover 例外,不是新常态。** 按 `README.md` 的
[单写者合同](README.md#FLY-2045-SINGLE-WRITER),一个 issue 的里程碑文件只由它自己的 ship PR 写。
FLY-2026 的 ship PR 在本机制存在之前就已合入,不可能自己建这个文件,所以由 FLY-2045 的 cutover PR 代建。
往后 FLY-2026 的任何状态更新仍只由 FLY-2026 自己的后续 PR 串行做。
