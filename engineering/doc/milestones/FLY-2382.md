# FLY-2382 — Summary 到点叫与缺席可见

**Status**: ⏳ Implementation complete; PR CI pending
**PR**: pending creation
**Date**: 2026-09-06

## 交付

- 既有 `summary_absorption_cadence_ms` 成为读写两拍的唯一运行期时钟；当前值仍为 6 小时，管理台改值后下一 pass 即采用新 cadence，没有新增 flag、表、daemon、launchd、cron 或 Lead 本地 timer。
- 第一拍按当前 summary granularity 和各 Lead 的 canonical duty 配置冻结 producer 名单，以稳定 `summary_due:<project>/<lead>:<slotISO>` 身份写入 `lead_events`，再经既有 Lead inbox 队列首投/补投。事件携带精确 period、上次 Raya PR 状态与可直接执行的 `flywheel-comm summary` 命令。
- 第二拍在 `min(30min,cadence)` 后以独立 GitHub PR 账本和 inbox settlement 两条证据轴结算。只有 exact period 分支上的 OPEN/MERGED PR 算已交；账本不可得和收件箱状态不可得都不会被误报成缺席。
- 结算结果先以 `summary_slot_settled:<slotISO>` write-ahead 冻结，再重放到 Raya 的既有 `summary_absorption_round`。Raya 无论本轮是否有吸收活动都被要求把完整 `report_line` 发到 `#raya`；没有 Raya 注册时明确记录 DEGRADED，而不是假装已完成汇报。
- 未送达的 `summary_due` 按 slot 聚合为一个稳定 warning，经既有 alert sink 重放；单 Lead enqueue、Raya enqueue、alert 或单 slot 失败均不会阻断其他副作用。
- producer 规则明确 `[summary_due]` 是唯一节奏来源、必须原样使用事件 period、无 Facts/Judgment 可跳过且「未交」是可见性而非催促，并继续禁止自建 timer/cron/launchd。

## TDD、验证与评审

- 每个行为块均先有可观察 RED 再做最小 GREEN；R1 的 blocking finding 也先以三条回归复现：Raya payload 身份被冻结行覆盖、成功 PR 遮蔽 inbox 失败、CLOSED PR 被误算已交。修复后 Raya envelope 身份保持 authoritative，两条证据轴正交，CLOSED 不再满足交付。
- summary 聚焦套件最终为 Teamlead 85/85、flywheel-comm 86/86，共 171/171；覆盖 60 秒 cadence 八种相位、冷启动、热切换、崩溃重放、两拍快照、strict `gh` 边界、formatter parity、规则 bundle 与 StateStore 事务回滚。
- `pnpm lint` 退出 0（仅 14 条仓库既有 warning/info）；`pnpm -r build` 的 21 个 workspace 全部通过；`git diff --check` 通过。本分支没有新增 `scripts/__tests__/*.test.sh`。
- 最终精确 `pnpm test:packages:run` 在 config 包 771 tests 已通过后，只因未改动的 boolean-census 测试耗时 5.115 秒超过固定 5 秒而 first-fail；同一 `drift-scan.test.ts` 保持原 timeout、单 worker 隔离复跑 27/27 通过，该断言耗时 4.606 秒。没有放宽 timeout 或修改无关测试。
- Request-driven `codex:rescue` review R1 在 `1ec83b479d2e37a8ef1e69b6bbdfecfab5ea481e` 找到并阻塞 Raya payload identity clobber；修复提交为 `1f49e8a24`。合入最新 main 后，R2 在 `31cb8430196b43f391c86953a8d115590bf66c9b` 返回 `APPROVED`，无 blocking finding。
- R2 三项非阻塞 advisory 已通过正式报告通道交 Lead：非法 roster 配置也会暂停 legacy Raya round；inbox 机制失败文案可能被快速误读；未来若归档 `summary_slot_settled` 必须同时补 archived frozen-row lookup。

## 生产消费者与边界

- G12 审计证明 `StateStore.listUndeliveredLeadEvents()` 在生产代码中没有调用者；Lead inbox redrive 的 `listUndeliveredLeadInboxEvents()` 只选择 `workflow_replacement_eligibility` 与 `workflow_claim_recorded`；patrol tick 从 `project.leads` 枚举；`summary-clock` 只在本 rider 和其测试出现。因此新增未投递行不会被旧全表消费者误认成 patrol 工作。
- 当前生产未注册 Raya，A2 的真实 `#raya` 汇报仍是 rollout prerequisite；implement 节点没有修改 projects.json、激活 Raya、重启 Bridge、dispatch QA、请求 ship、merge 或 deploy。
- 已知窗口限制保持获批 plan 原样：停机超过一个 cadence、热切换后落出 `{T,T-cadence}` 的旧 slot、以及窗口外未完成副作用不回补；审计行保留。低于 10 分钟 cadence 长期运行前需要另单扩 archive retention。
- 本 milestone 是 PR 分支的 literal last commit；包含它的 exact head 交给最终 review 与 PR CI，CI 未绿前不宣称可 ship。
