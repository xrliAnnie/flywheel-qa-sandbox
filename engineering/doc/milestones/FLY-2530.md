# FLY-2530 — Codex Lead 启动预检自锁修复

**Status**: ⏳ Pending ship
**PR**: #1172
**Date**: 2026-09-13

Mufasa 和 infra-bot 的 TUI launcher 在启动 home-link 预检时移除离线 cutover 专用的 `--lead` 参数，避免把自身 launchd job 判为另一个运行中的 Lead。离线 launchd 栅栏及活动 Codex 进程保护保留。新增两个 launcher 的隔离 launchd 回归测试并注册 CI。

启动回归 6/6、home-rule 7/7、link-truth 14/14、cutover 5/5 通过；全仓 lint/build 通过。全仓 package tests 因 config 扫描超时失败，隔离复核仍有一项超时；详见 `engineering/doc/FLY-2530-mufasa-preflight/verification.md`。本节点未执行生产重启或部署，真机 TUI/inbox/Bridge probe 验收由部署后流程完成。

评审建议中的旧 launcher 参数断言已同步：infra-bot 19/19 通过，Mufasa 旧夹具原有四项启动前失败保留记录。启动 registry-home 交叉校验由 Lead 明确留作后续工作。
