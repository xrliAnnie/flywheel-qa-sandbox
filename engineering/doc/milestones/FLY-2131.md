# FLY-2131 — Raya 大脑 summary 吸收、追问与可见汇报

**Status**: ✅ Ready for review
**PR**: #981
**Date**: 2026-08-28

Raya M2 从「summary 能回流」推进到可运行的吸收闭环：6 小时默认且可热调的
durable rider 向唯一 Raya recipient 投递确定性 roundId；Raya 按轮 review、看不懂就
向项目 Lead 追问、把理解写入带 provenance 的 `MEMORY.md`，并在 `#raya` 汇报时间、
数量与动作。新增的 `flywheel-comm summary merge` 对当前 head 做完整验证，并且所有
merge 调用机械携带 `--match-head-commit`；非 Raya recipient 在触达 GitHub 前即被拒绝。
summary registry activation 的 CLI 源缺失现为 fail-closed，Raya 激活另有 installed-build
dry-run、canonical identity、workspace/memory、full-access TUI 四合一 preflight。

模型链现支持 Codex Lead 的 `model`、`model_reasoning_effort` 与
`model_context_window`，Raya 钉为 `gpt-5.6-sol / xhigh / 1,000,000`；当前 thread 的
token usage 按既有 v1 合同落盘并在失败时留下 unavailable 证据。Raya companion PR
[#8](https://github.com/xrliAnnie/raya/pull/8) 同步交付吸收/恢复/可见汇报身份合同，以及
每个语音开场都自称 Raya 并称呼 Annie 的 production prompt。

主分支整合后的返工补齐两项 Lead 强制修复：非法 cadence seed 只告警并回落默认值，
不再阻断 Bridge 启动；Codex tuning 字段加入 canonical payload 时保持既有 v1 identity digest
稳定，同时保留 projects digest 的变化证据。整合中也恢复了 strict value toggle admission，
避免把伪装成 value-kind 的 boolean flag 暴露给直接管理入口。

验证：代码审查在 head `cab0d56cc` APPROVED（无 blocking finding），PR #981 代码头 CI
10/10 jobs 与 `CI OK` 全绿。全仓 lint 无 error、22 workspace build 全绿；受影响 config 698 项、teamlead
420 项、flywheel-comm 目标套件与 21 项 shell 契约全绿；registry CLI 连跑 20/20；
真实 release tree assemble/pack/package gates 全绿；Raya lint/typecheck/build 与 298 项
测试全绿。全包测试另命中本机缺 Terminal.app 与既有 `claude-runner` 高负载 timeout
抖动，已隔离证明不在本单改动面。
