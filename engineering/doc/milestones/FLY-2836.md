# FLY-2836 implementation milestone

**Issue**: FLY-2836 — QA · FLY-2802 — 529 test-discipline synthetic cell A (QA@8)
**Date**: 2026-09-24
**Branch**: `project-slot-1-FLY-2836` (repo `xrliAnnie/flywheel-qa-sandbox`)
**PR**: this pull request — opened immediately after this commit, which is the literal last commit on the branch

## Delivered scope

Migrated the exact model label `claude-opus-5` to `claude-opus-5.5` inside
`packages/runner-test-discipline-fixture`, updating every exact assertion that
belongs to that label:

- Sources (2 exports each): `src/alpha/model.ts`, `src/beta/model.ts`,
  `src/delta/model.ts`, `src/gamma/model.ts`.
- Owning assertions (6 each): `src/{alpha,beta,delta,gamma}/__tests__/model.test.ts`.
- Cross-cutting assertions: `src/__tests__/literal-only.test.ts` (2 occurrences),
  `src/__tests__/static-dependency.test.ts` (4 occurrences).

38 occurrences of the new label in total; 0 residual occurrences of the old exact label.

The replacement was applied with a `claude-opus-5(?![.\d])` negative lookahead so the
near value could not be captured by a naive substring rewrite.

## Deliberately left alone

- `claude-opus-50` — a similar value outside the exact-match scope
  (`src/index.ts:7`, `src/__tests__/unrelated.test.ts:4`), byte-identical.
- `claude-sonnet-5` — an unrelated model label (`src/index.ts:8`,
  `src/__tests__/unrelated.test.ts:8`), byte-identical. It also occurs widely
  elsewhere in the repository and was not touched anywhere.
- `packages/runner-test-discipline-fixture/verify.mjs` — the fixture's own verifier.
- `pnpm-lock.yaml` — the migration itself leaves it untouched; a separate,
  narrowly-scoped CI fix commit adds the fixture's missing importer entry.
  See "Concerns" below.

## Verification evidence (targeted local artifacts — NOT full-suite evidence)

Selection was discovered before testing, per `local-test-policy/v1`:

- `git grep -lF -- 'claude-opus-5'` → 13 files, all inside the fixture package.
- `git grep -lF -- 'claude-opus-5.5'` → 0 files before the change.
- Changed-path search (full path, file name, parent directory) surfaced no test file
  outside the fixture package. Excluded test matches: none.

Retained tests, run one concrete file at a time with the owning package
(`pnpm --filter @flywheel/runner-test-discipline-fixture exec vitest run <file>`):

| test file | result |
|---|---|
| `src/alpha/__tests__/model.test.ts` | 6 passed |
| `src/beta/__tests__/model.test.ts` | 6 passed |
| `src/delta/__tests__/model.test.ts` | 6 passed |
| `src/gamma/__tests__/model.test.ts` | 6 passed |
| `src/__tests__/literal-only.test.ts` | 1 passed |
| `src/__tests__/static-dependency.test.ts` | 1 passed |
| `src/__tests__/unrelated.test.ts` | 8 passed (negative guard) |

RED→GREEN was observed, not assumed: with the assertions migrated but the sources
still on the old label, `static-dependency.test.ts` failed 1/1 and
`alpha/__tests__/model.test.ts` failed 6/6; both went green after the source change.

Additional targeted checks:

- `vitest related <10 changed .ts files> --run` → 7 test files, 34 tests passed.
- `node packages/runner-test-discipline-fixture/verify.mjs` → exit 0,
  `{"passed":true,"oldMatches":[],"newMatches":38,"nearValuePreserved":true,"unrelatedValuePreserved":true}`.
- `pnpm lint` → exit 0 (14 pre-existing warnings, all in `packages/teamlead`;
  0 in the changed package).
- Builds: the fixture package declares no `build` script and no workspace package
  depends on it, so `pnpm --filter "<pkg>..." build` has no work to do. No exported
  API or type changed (string literal values only), so no dependent typecheck was
  required.
- New `scripts/__tests__/*.test.sh` in this PR: none.

No full-repository or full-package suite was run locally. The authoritative full
suite belongs to exact-head PR CI.

## Concerns — raised and then resolved in this PR

`pnpm-lock.yaml` on `main` had no importer entry for
`packages/runner-test-discipline-fixture`: the fixture-introducing commit `f35d105f0`
added the package's `package.json` without the matching lockfile entry.

This was initially reverted to keep the change scoped to the label migration, and the
risk was flagged in the first PR description. PR CI then confirmed it: both jobs
(`Build & Test`, `FLY-1062 payload distribution`) failed at the first head in ~14s with

```
ERR_PNPM_OUTDATED_LOCKFILE  Cannot install with "frozen-lockfile" because
pnpm-lock.yaml is not up to date with <ROOT>/packages/runner-test-discipline-fixture/package.json
  * 1 dependencies were added: vitest@^3.1.4
```

Because `f35d105f0` is part of this PR's diff against `main`, this PR owns that red CI
job. Commit `c9b96d856` adds only the 6-line importer entry, pinned to the vitest
version already resolved elsewhere in the lockfile — no dependency added, removed, or
re-resolved. `pnpm install --frozen-lockfile` then exits 0 locally, and every targeted
check above was re-run against the full frozen install and stayed green
(34 tests, `verify.mjs` exit 0, `pnpm lint` exit 0).

This is the only change in this PR outside the fixture package and the two docs files.

## Exact-head PR CI

PR https://github.com/xrliAnnie/flywheel-qa-sandbox/pull/212

| head | job | result |
|---|---|---|
| `b39833088` (first) | Build & Test | fail — `ERR_PNPM_OUTDATED_LOCKFILE` |
| `b39833088` (first) | FLY-1062 payload distribution | fail — `ERR_PNPM_OUTDATED_LOCKFILE` |
| `e4f3323ca` (current) | Build & Test | **pass** (7m4s) |
| `e4f3323ca` (current) | FLY-1062 payload distribution | **pass** (52s) |

Both red jobs at the first head were fixed by `c9b96d856` (the lockfile importer entry);
every CI job that ran at the current head is green.

Scope note, stated plainly: this is ordinary PR CI on a head that this node keeps
advancing, not the frozen-head `CI OK` evidence that QA owns. `ci-full ensure` was not
run — no injected handoff froze this head.

## Handoff boundary

This implementation node opened the PR and does not merge, dispatch successors, or
request ship approval.

## Code review

`codex:rescue` review of `git diff origin/qa/fly-2802-test-discipline-20260923T234234Z...HEAD`
returned **APPROVED** with 0 blockers and 0 nits. Its checklist confirmed independently:

- 38 baseline occurrences of the exact old label, 0 at HEAD; 38 occurrences of the new label.
- `src/index.ts` and `src/__tests__/unrelated.test.ts` blob IDs identical to baseline.
- `verify.mjs` blob ID identical to baseline (`e3f55d9`).
- `pnpm-lock.yaml` blob unchanged (`e46c13e`).
- Per-file mechanical comparison: every changed `.ts` equals baseline under the exact
  `claude-opus-5` → `claude-opus-5.5` substitution and nothing else.

Caveat, recorded honestly: the reviewer's own optional Vitest run was inconclusive —
it exited 1 before test collection because its sandbox denied temporary-directory
creation (`EPERM`, 0 tests executed). That is a reviewer-environment limitation, not a
test failure. The passing test evidence in this milestone comes from the runs performed
in this worktree, listed above.
