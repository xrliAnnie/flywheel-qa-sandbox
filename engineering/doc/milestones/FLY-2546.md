# FLY-2546 — 同步 janitor SIGTERM 测试

**Status**: ⏳ Pending verification and ship
**PR**: 本里程碑随 FLY-2546 实现 PR 同推；PR 内保存最终精确头回执。
**Date**: 2026-09-14

SIGTERM 用例以现有 lsof 注入点的 ready/release 握手等待真实工作阶段，取代 600 文件及固定睡眠的时序假设。保留发信号成功、143 退出和锁释放断言；只改测试夹具与清理逻辑（含 kill-path 清单测试 JSON），不改生产脚本/src。

修前自然完整套件 40/40，延迟发送调度实验红；历史 CI 首次 sent=1 rc=0 红与零 diff 已核验。修后同步单用例在 PATH Bash 和 /bin/bash 绿；错误 exit0/继续执行 trap 两种变体均红。bash-n、ShellCheck、CI 枚举/结构检查绿。完整套件连续 20/20 全绿，每轮 40/40（总 800 条通过）。安装/lint/build 通过，全量 package 留 3 个原有 5s timeout 红，隔离 78/78 绿。未宣称全量 package 绿。

design review APPROVED；旧头 code review APPROVED。CI heavy 暴露 kill-path 清单夹具漏同步，本地先红后绿 5/5，按 scanner 更新该测试的 qa-only 项并与本账本同推。返工头须重新 code review 和同一精确头 shell2 两次绿，在 PR body/结构化 report 验证后才完成 implement handoff。不 merge、不 deploy。详细命令见 engineering/doc/FLY-2546-janitor-sigterm-race/verification.md。

## 清单并发集成约定

与 FLY-2548（PR #1189）共改 packages/claude-runner/test/fixtures/kill-path-inventory.json；本单仅登记自己的测试清理路径。后合者需同步 main、重登记并在新精确头重新验证/review。现在不互相合分支，等待 Lead 的明确返工令。
