# FLY-2365 — DAG 节点重新连上 Agent 手册

**Status**: ⏳ Implementation complete; corrective exact-head review and CI pending
**PR**: #1102
**Date**: 2026-09-06

## 交付

- Workflow manifest 新增 schema 3：执行节点必须携带稳定的 `handbook_ref`，gate / land 节点明确禁止；schema 1 / 2 的精确 key、digest 与读取兼容性保持不变。
- `handbook_ref` 只承担展示元数据，不恢复已删除的 `role` 运行时语义。system menu 的 DAG seed 升到 schema 3，并由 registry node id 同源生成 handbook ref；StateStore admission、四条 release SQL 和 founder-approval terminal fallback 均显式接受 schema 2 / 3。
- 生产 `verify-workflow-seeds` 按 manifest 版本显式选择 V1 / V2 / V3 snapshot builder；六个 schema 3 menu seed 的 manifest validation 与 gate-authority resolution 均通过。
- 管理台 snapshot 以精确 canonical key 解析 Agent handbook：Flywheel execution nodes 对应同 id 卡片，Personal Assistant 的 `general` 精确映射到 canonical `life` roster；其余 legacy 项目与 schema 2 manifest 显示明确的「未关联」原因，不做 fuzzy match，也不写回数据库。
- DAG 节点 chip 支持 hover / focus / click / keyboard 打开对应 Agent 卡片；关联卡片保留原生 tab 顺序，chip 同时显示节点类型与关联状态，且所有动态文案继续经过既有 HTML escaping。
- 新增生产 snapshot 基线证据与 before / after delta。Evidence harness 直接通过编译后的生产 config、topology、workflow-menu、management projection 与 HTML 模块构造 DTO，不手写后端字段，也不依赖本机绝对路径。六个项目各覆盖六种 DAG，Flywheel 的 7 个唯一 handbook ref / 9 个 agent chip 全部唯一落到一张卡；Personal Assistant canonical roster、四个 legacy 未关联项目和 schema 2 未关联路径均有断言与截图。

## TDD 与验证

- Manifest RED→GREEN 覆盖 schema 3 required / forbidden / exact-key 规则、schema 1 / 2 digest 兼容、menu-mode snapshot 与 schema 2 migration census；管理投影和 DOM RED→GREEN 覆盖 canonical link、完整 unlinked reason、原生键盘 tab 顺序、可见节点类型 / fallback 与双说明去重。
- PR quick gate 首轮揭示生产 seed verifier 仍把 schema 3 交给 V2 builder；新增执行真实 verifier 的 regression test 先稳定复现 RED，再以按版本 dispatch 的最小修复转 GREEN。完整 affected file 9/9 与 `pnpm verify:workflow-seeds` 六个 seed 全绿。
- Evidence harness 的逆向 self-check 能稳定触发 `FLY2365_MISSING_UNIQUE_ROLE`，恢复后真实页面核验通过；浏览器 console / page errors 与非 GET 请求均为 0，116px chip 与“节点类型 · 关联状态”布局人工抽查无截断。
- `pnpm lint` 通过（仅仓库既有 warnings）；`pnpm -r build` 通过全部 workspace；本分支没有新增 `scripts/__tests__/*.test.sh`。
- 里程碑布局守卫 32/32 通过，`git diff --check origin/main...HEAD` 通过。
- 精确 `pnpm test:packages:run` 完成所有本分支相关覆盖；并行宿主负载下，claude-runner 在 1,084 个通过后出现两个未改动的短时限失败，Teamlead 在 11,398 个通过后出现 29 个未改动的 timing / real CLI / Git / bash / cross-process 失败及 Vitest worker RPC timeout。失败文件按原时限串行复跑，13 个文件 144/144 通过；唯一仍受串行组负载影响的 Claude profile 文件再独占复跑 11/11 通过，claude-runner 两个文件也各自通过。没有放宽无关 timeout 或改写无关测试，最终 aggregate 结论交由 PR exact-head CI。
- TypeScript 静态检查未覆盖的 persistence boundary 已逐项人工审计：StateStore schema admission、四条 release SQL 与 founder-approval terminal fallback 均接受 schema 2 / 3，且 schema 1 / 2 reader 路径保持原样。

## 边界与交接

本实现没有修改获批 plan、`CLAUDE.md`、runtime dispatch 语义、线上配置或数据库中的历史 manifest，也没有 dispatch QA、请求 ship approval、merge、deploy 或重启 Bridge / Lead。`handbook_ref` 与 registry node id 的冗余由 schema validation 和精确 projection 一致性测试共同约束；三条 MEDIUM review advisories 已在一个 corrective commit 中处理，四条 LOW 仅记录为 PR follow-ups。包含本 milestone 的字面 branch head 由新的 request-driven cross-family code review 与 PR CI 接续验证。
