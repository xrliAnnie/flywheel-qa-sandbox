# FLY-2207 — cmux-watcher 生命周期自愈

**Status**: ⏳ Pending independent QA
**PR**: [#1014](https://github.com/xrliAnnie/flywheel/pull/1014)
**Date**: 2026-08-31

cmux watcher 的进程生命周期现在有完整闭环：外部 `cmux --json` 调用受独立超时约束且不会让
主循环累计退出，现有 Bridge patrol 会在 label 消失、owner 失配或 heartbeat 停滞时确认
generation，并通过专用 rebuild 正门恢复 launchd job；异常继续借既有统一告警通道上报，没有
新增告警层，也不再经过 FLY-913 的部署面护栏。

runner 视图生命周期也从“只创建”补齐为可收束、可重建。终态 runner 的 source window 与
Flywheel-owned workspace 按精确 execution/registry/tmux 身份事务化关闭；死窗期或 watcher
恢复后缺失的 viewer 用现有 build WAL、UUID receipt ledger 和 attach-heal 原语重建。每次突变前
都会重验 kill switch、lease、cmux generation、workspace UUID 与 source `@id`，失败按首次突变
边界决定重试或锁存，避免 founder 同名 workspace、重复 execution 和 stale receipt 被误伤。

返工后的 Linux 可移植性缺口已收口：restart 测试用 `id -u` 构造 launchd domain，
trusted alert env 的权限检查先用 GNU `stat -c`、再回退 BSD `stat -f`，并用伪 GNU
`stat -f` 成功值回归覆盖原故障。冷启动补窗行为测试证明 watcher 在不借助
reopen sweep 时只创建一个 workspace，提交 UUID-bound ledger 并建立精确 linked view。

验证方面，`pnpm lint`（仅 14 条既有 warning）和 `pnpm -r build` 通过；cmux 主套件
580/580，cleanup lifecycle 8/8，view rebind 10/10，node presence 22/22，lead-alert 46/46，
watcher restart 20/20，restart guard 286/286 通过。完整 `pnpm test:packages:run` 仅保留已确认的
本机 Terminal.app / hiservices 前置失败；非 GUI core 219/219 和 config 27/27 隔离复跑通过。

cross-family code review round 5 在 `34273d0b5` 上 APPROVED；新增 1 条与既有 10 条
非阻塞 advisory 已报告 Lead。exact-head Linux CI run
[`33453242956`](https://github.com/xrliAnnie/flywheel/actions/runs/33453242956) 全绿，包含两组 shell suites、
全部 unit matrix、build/typecheck/lint 与 packaging。真机 kill/rebuild 与 529 room 由独立 QA 在此修复头上执行。
