# FLY-2249 — Raya barge-in v2 平台触发重做

**Status**: ⏳ QA@5 complete; founder approved B 1/9 waiver; pending QA@6 replay
**Raya PR**: [#14](https://github.com/xrliAnnie/raya/pull/14) — requires separate founder merge authority
**Date**: 2026-09-02

本批次废弃 FLY-2178 被 founder 否决的自研能量/密度检测方向,把 Codex session 一直发送但 Raya
曾静默丢弃的 `input_audio_buffer.speech_started` 接成首要停口触发。确认层改为本地 Silero 神经 VAD,
在 OpenAI 上行之前把呼吸/噪声静音,真语音开门后等待平台事件;平台事件缺席时只有一个 600ms
fail-open fallback,不与平台结论制造双重触发。既有 0.55s 下行冲刷、11s suppression 硬界、四值仲裁、
被打断条目恢复和真房耳侧/N=3 指纹体系均保留。

`conversation.item.truncate` 对 platform `speech_started.item_id` 不可达的问题没有绕过:Downlink 使用统一
48kHz stereo 字节账本核销 FrameQueue 与 PassThrough,由 `HeardPosition` 估算保守最后可听位置,止声时立即
追加 generation-bound user side-note 告诉模型「她只听到哪儿」,文本明确标记 `[旁注,勿回应]` 且只
`appendText`,不发 `createResponse`。player idle recovery 只重置新 stream 的
PassThrough 核销账本,保持 accepted byte origin 与 transcript checkpoints 单调一致;mic mute/unmute 同一说话
窗口会重新开始 gate chain,同步入口异常会 fail-open 而不会冲出 stream listener 杀死进程;Silero degrade
当刻按原 push 顺序冲刷延迟帧,不再把句首拼到句尾。

## Review 返工

- round 1 在 Raya `a6229c0` 拦下两条 HIGH:mute/unmute 后 no-active-chain 崩进程、degrade 后帧序
  `[14..30,1..13]`;Lead 同时把 idle recovery 的 `ledgerUnknown` 永久锁死升为必修。
- `08ef0bf` 用六条回归覆盖 active gate restart、runtime 入口 fail-open、严格 1..30 帧序、idle 后 ledger
  known 与 heard-position note;`aee03de` 把四条 calibration probe tests 接入 root QA。
- round 2 硬门通过但指出 idle rebase 会让 byte origin 后退;`8776a6c` 改为 origin 单调,并收口 gate delay
  helper、独立 ingest-failure evidence kind 与显式 degrade-loop break。
- fresh round 3 gate `9d5c1072-a265-41a6-b762-3bd228a0d120` 在精确 Raya head
  `8776a6c256f7f9a4d0d5b903562b28031811e79f` **APPROVED**。
- 独立 QA 随后在该头给出 FAIL:developer role 异步杀会话、长 founder 发言跨 2.5s window 后恢复旧条目、
  台架以 server tap 抢占耳侧停口、note-off expected count 错绑。原始证据保留在 `qa-report.md`,没有覆写。
- `c839c4c`–`2b5ecd3` 用严格 TDD 完成 user side-note、耳侧唯一停口候选、持续 speaking 重布防、note 配置
  绑定、逐 utterance score p50/p99 evidence 和 suppression lease 竞态修复。
- fresh round 4 gate `900ec44f-7645-428a-8ef6-96bb98b49d9f` 在精确 Raya head
  `2b5ecd370f39270458f9051110868cf0ed3b6f1e` **APPROVED**。
- 独立 QA attempt 2 在该头证明产品链成立:note-on N=5 全部存活、耳侧停口 p95 `804.4ms`、停后
  可听帧 5/5 为 0、人工逐 `transcriptId` 核验 5/5 无 note 所致新 response。其 FAIL 判决的唯一产品
  缺口是 C7.5 founder 真人语料;同时暴露 F7–F10 四项台架归因缺口。
- `68af071`–`85cb8a4` 以 TDD 收紧台架:停口候选必须晚于 platform trigger、守静排除被打断终稿、
  note 新响应按新 assistant `transcriptId` 归因、true-speech 必须有真实 score evidence 且 p99 `≤5ms`、
  零 score 的 p50/p99 保持 `null` 而不能真空通过。格式化后的 exact Raya head 为
  `f13a2c9e253962683bb334ab299430c781a9778f`。
- fresh round 5 gate `ba5bae12-4c6b-4c37-98ca-3cb43ea40870` 在该 exact head **APPROVED**。
- 独立 QA attempt 3 随后确认公开 `"Yeah."` 样本由 QA bot 而非 founder 发出;按已锁 D-ARB2b,
  非 founder 声音被当作真打断是正确行为,该轮没有测到 L3。Lead 仍锁定第 4 轮只补默认英文 L3
  词表与 `"Yeah."` 回归,不动 `200ms / 0.5`、不加 `minSpeech`、不改架构。
- `c0db13a` 以红→绿默认配置测试补齐 `yep / sure`,保留既有
  `yeah / uh-huh / mm-hmm / right / ok / okay`;`f950685` 只应用测试排版。fresh round 6 gate
  `0233eae1-725a-4a21-8d01-b129ab207038` 在 exact Raya head
  `f9506854fa1484464b799dba0613c559da1a72cc` **APPROVED**。
- 独立 QA attempt 4 在该 exact head 重跑静态全门仍为 831 tests 全绿,但所有新真房臂都在 Realtime
  Live 前被外部额度阻断。直连 OpenAI WebSocket 三次均先完成 HTTP 101,随后返回
  `insufficient_quota / credit_balance_exhausted` 并以 1013 关闭;认证正常,平台 API credit 已耗尽。
  platform trigger、L3 仲裁、神经门、heard-position 与 room harness 相对 attempt 3 均逐字节未变,
  没有代码或台架缺陷可返工。完整证据见 `qa-report-attempt4.md`。
- founder 于 11:18 PT 完成平台充值。`9e476b4` 随后只修 room harness 的两个测量缺口:非
  `true_speech` 的 ear-gap 搜索窗延到 `min(userFinal, stimulusStart+3s)`;backchannel 不再用
  `speech_started!=0` 单独失败,改以无下行冲刷、无耳侧停口、旧输出完整为三个硬门。
  694ms 真实 summary 夹具量得 `audibleGap=+900ms / 1220ms` 与 `audibleStopLatencyMs=900ms`;
  trigger 改到 `+920ms` 时仍由 F8 pre-trigger guard 拒绝。
- fresh review round 7 gate `0d6767bc-5664-4da1-abe5-8fa074e0462a` 在 exact Raya head
  `9e476b4906a107b019cf38daee4cb56970405ca6` **APPROVED**;没有 blocking finding。
- QA@5 随后只在 `probes/` 扩充短刺激、per-sample containment、leak/recovery、条目复位、schema-v2
  L1 evidence 与未知 mode fail-closed 台架。implement 在 QA 持有 TURN 时按 Lead 要求做了四轮跨族只读
  复审,逐条抓出并闭合无触发窗、恢复起点、transition `false_trigger + front`、never-resumed 与混合
  unknown-mode 空洞;最终 harness exact head 为 `f8c638be765ff4a9043fb5ab07980ed884374573`。
- QA attempt 5 在 Realtime Live 真房得到:A 真语音 n=5 全部耳侧 `714–857ms` 停口且停后 0 可听帧/
  0 新终稿,D 轻声 3/3 到平台,C 附和 leak 1/3 只记数;B 呼吸/清嗓合并 n=9 为 8 干净/1 泄漏,
  且泄漏 rep 没有逐 rep L1 摘要,原始总判决为 FAIL。完整数据见 `qa-report-attempt5.md`。
- founder 实听后原话 **「目前来看效果还挺不错的」**;Lead 登记 Option A,对 B 的 1/9 泄漏作本单治理
  豁免。implement@6 因此零产品、零阈值、零 harness 改动,只刷新 QA@6 handoff 与 milestone;原始 FAIL
  数据仍保留,没有改写成「B 零泄漏」。
- `/codex:rescue` companion 按合同调用,但 resident 外层 macOS seatbelt 在读取仓库前拒绝 nested sandbox
  (status 71),未伪记 PASS;上述 request-driven cross-family verdict 是有效 review 证据。

## 验证

- Raya:`pnpm lint`、`pnpm -r build`、`pnpm typecheck` 全绿;contracts 62、brain 125、voice 518、
  QA 132,共 837 tests 全绿,attempt-5 room probe 合同 20/20。当前 exact head 为 `f8c638b`;每条
  `uplink_gate_utterance` 现记录真实
  `scoreCount / scoreMsP50 / scoreMsP99`;
  显式串行 Silero/AudioClock 7 轮每轮 1,000 次评分,max
  `0.156–0.496ms`,p99 `0.112–0.166ms`,fired ticks `4`,missed ticks `0`。
- Flywheel 锚仓:`pnpm lint` exit 0(14 条既有 warning/diagnostic),`pnpm -r build` 22/23 projects 全绿。
  精确 `pnpm test:packages:run` 在受管无 GUI 会话中只有两条真实 Terminal.app/HiServices 用例因
  `Connection invalid` 失败;headless core 19 files / 219 pass / 3 skip。随后 aggregate 的五个未改动文件
  在并行负载下出现 6 条 timeout/state-collision failure,逐文件串行复跑 5 files / 91 tests 全绿;
  没有把 aggregate 冒充为全绿。

## 待 QA / founder 硬门

**C7.5 状态严格保持:未完成·待真人语料。** QA 必须在 exact Raya head 上取得 founder 日常设备的呼吸、
「嗯/对/哈哈」、轻声、犹豫分段与正常真插话 WAV,完成 100/200/300ms × 0.4/0.5/0.6 校准网格后,
再跑真房矩阵。硬判据仍是耳侧真插话 `<1000ms`、呼吸/附和零误触、停后守静、最后可听词人工确认、
9/9 interrupted-item 恢复、N=3 双端指纹一致且 `uplink_gate_degraded/droppedOverflow/audio_clock_stall=0`。
合成控制组不能冒充真人校准,bot 侧事件不能替代耳侧听感。note-on 真打断另需 N≥5,逐轮证明没有可归因
于 note 的新 response/转写;任一失败即关闭 note,heard-position 只保留 evidence,不继续调 prompt 后宣布通过。

第 4 轮 backchannel/soft/hesitant 归属座使用被测实例已有的 `RAYA_FOUNDER_DISCORD_USER_ID`:将其设为
QA emitter 的 Discord user id,不新增 harness 开关。Lead 最新 C 臂裁决允许 platform
`speech_started=1`;最终判据是 founder 归属附和不产生耳侧停口、下行冲刷或旧输出截断,现有 evaluator 的
`speech_started_nonzero` 字面结果不再单独构成失败。

QA@6 使用 exact Raya head `f8c638b`,不重开产品设计、不调 `200ms / 0.5`、不增加 min-speech 规则。
它核对工件与 fail-closed 合同,把 B 的原始 1/9 FAIL 和 founder Option-A 豁免并列写入结论。B 泄漏
缺逐 rep L1 摘要仍是测量归因限制;豁免结果不等于该证据链已可靠。若未来另开 B 诊断,范围只能是让
gate-utterance 摘要逐 rep 关闭或周期落盘后追因,不能在本单暗调阈值。C7.5 仍须新的明确裁定或同设备
真人 WAV 网格才能关闭,founder 本轮实听与 B 豁免不能由 QA 自行扩写为 C7.5 完成。

APPROVED review 的非阻塞 follow-up 已报告 Lead:pre-trigger ear gap 的 fail-loud 可能中止整次 room capture、
Discord speaking-end 丢失时仲裁重布防没有绝对上限、user-role note 仍需上述真人语义证明(MEDIUM);
无 idle 事件的静默 player stall 会让 PassThrough ledger 增长、removed-option guard 字符串可读性(LOW)。
round 7 另指出:acted `speech_started` 在当前 runtime 必然立即 local-yield,新的 backchannel outcome gate
可能稳定报 `downlink_flushed`;扩窗若捞到早于当前 stimulus 的上一轮迟到 user final 也会 fail loud(MEDIUM)。
这些是 QA@5 必须原样保留的诊断边界,不是已验收行为或 blocking finding。
本实现节点没有执行真房验收、merge、部署、重启、ship 或 successor dispatch。
