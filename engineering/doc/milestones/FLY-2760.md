# FLY-2760 — Codex Lead 模型行显示真实公司、型号与 effort

**Status**: ⏳ Pending ship
**PR**: [#1321](https://github.com/xrliAnnie/flywheel/pull/1321)
**Date**: 2026-09-24

Fleet console 不再把 `backend=codex-app-server` 且未在 `projects.json` 钉型号的 Lead 显示成
「Anthropic / 账户默认 / 账户默认」。公司现在由 backend 通用派生为 OpenAI，effort 保留注册表原值，
型号则读取 Codex runtime 写下并与当前 thread id 绑定的 `thread-settings.json` 证据；证据尚未出现时，
页面明确显示未钉型号的诚实占位。实现没有按 Lead 名字特判。

未钉 Codex Lead 的这一行保持只读，已钉 Codex Lead、Claude Lead、Runner、DAG 与 Cron 的既有选择语义
保持不变。证据 reader 拒绝软链接、非普通文件、超 4 KiB、非法字段和旧线程证据；runtime 写证据失败只
降级记录日志，不影响 Lead 工作。

QA 已在真浏览器复核：`mufasa-lead` 与 `codex-infra-bot-lead` 均显示 OpenAI / 运行中 ·
GPT-6 Astra / high，Claude 行与页顶免责声明保持不变。证据为
`fly2760-growth.png`（sha256 `0dd8115d…`）与 `fly2760-infra.png`（sha256 `3018f82b…`）。

实现头验证：8 个聚焦文件共 165 项通过；`vitest related` 的 config 依赖图 7 文件/77 项、teamlead
依赖图 147 文件/1841 项通过；受影响包及依赖构建通过；`flywheel-config` 的 13 个依赖包 typecheck
通过；本次变更文件 Biome 无错误（`plugin.ts` 两个既有 warning 保留）。根 `pnpm lint` 仍被本分支外
既有文件的 10 个错误拦住。QA 返回的 package-onboard gate④ 登记漂移已修复：只更新
`audit-grep-allowlist.tsv` 中该 renderer 的一条精确行，未放宽规则；`package-onboard.test.sh`
36/36 与 `gate4-allowlist-masking.test.sh` 13/13 通过，其中新增 fetch/clone 以及改 href 的阴性守卫仍被拒绝。
