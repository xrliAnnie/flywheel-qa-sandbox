# FLY-2238 — Fable 5.1 单旋钮模型权威

**Status**: ⏳ Pending independent QA / post-deploy publication
**PR**: [#1025](https://github.com/xrliAnnie/flywheel/pull/1025)
**Date**: 2026-09-01

本实现把 builtin `fable` / `fable-1m` 别名升级到 Fable 5.1，同时把 Fable 5 保留为
legacy-accepted，已 pin 的旧 run 不会中断。runner 默认、heavy tier fallback、Lead
launch/restart receipt、context admission、用量计价与 updater sync 现在都从 model registry
binding 派生，不再各自保存可漂移的 canonical id。下一次同族升级由既有 updater shuttle
更新 models authority，不新增 daemon、timer 或告警层。

模板侧新增 governed、CAS-protected publication：生产数据库没有在 implement 节点被修改。
merge 与 shuttle deploy 后，downstream owner 必须运行
`node packages/teamlead/dist/bin/publish-fable-template-alias.js --template tpl_code --node eng_design`，
再证明新 revision 原样保存 `model: fable`、`seed_owner: founder`，且新 run snapshot 一次
canonicalize 后以 `dispatchPinned: true` 固定到当前 dispatch model。已运行/已 pin run 在
restart/recast 下保持不变。`tpl_eng_heavy` 已 retired 且 unbound，因此不迁移。

严格 TDD 覆盖 builtin/legacy、overlay metadata、sync rollback、role/config/argv、Lead receipt
与 fail-closed resume、pricing/report、template publication、snapshot pinning。最终验证中
`pnpm lint` 与 22-package build 通过；9 个 changed shell suites 140/140、core 非 GUI
219/219、config census 27/27、flywheel-comm isolation 50/50 全绿，packaged smoke 真实完成
npm install、native module load、Bridge `/health` 与 Lead dry-run。全仓 aggregate 唯一不可执行
项是 resident runner 没有 Terminal.app 服务导致的 2 个 real-osascript case；固定时限并发
case 均已隔离复跑通过。

首个 GitHub CI run `33570370754` 的 8 个非 shell-1 job 全绿；shell-1 唯一失败来自测试 harness
先尝试 BSD `stat -f`，GNU `stat` 在 fallback 前输出 filesystem 数据。返工把探测顺序改为
GNU `stat -c` 优先、BSD fallback，并分别以 native BSD 与 GNU shim 证明 receipt suite 9/9。

cross-family code review round 2 在 exact code head
`e31dbfe56e79c6bb9620ac0ec11181996bedbed7` APPROVED；其一个 MEDIUM 与两个 LOW advisory
(`sync-verification-couples-to-unrelated-heavy-tier`、`sync-rollback-outcomes-not-warned`、
`receipt-model-shape-unvalidated`) 已全部在 `97e3088a1` 以独立 RED→GREEN coverage 收口。
fresh round 3 在 `95766202ed8e0fe0a775fdc6a74b41e8f6b8d3b5` APPROVED；唯一 LOW advisory
`milestone-advisory-status-stale` 由本条最终 milestone 更新直接关闭。

本 implement 节点没有 merge、deploy、重启服务、修改生产 template DB、请求 ship approval
或 dispatch QA；PR 后续由独立 QA 与 post-deploy publication receipt 收口。
