# FLY-2100 — flag_values 支持逐项目 scope

**Status**: ⏳ Pending ship
**PR**: #971
**Date**: 2026-08-27

为 `flag_values` / changelog 增加 `(flag_name, scope)` 身份与安全迁移，项目 flag
按「项目行 → `*` 行 → config → 名册默认」解析；管理页与 CLI 可按名册项目写入或
清除 DB 行，周扫描按 `(flag, scope)` 记录稳定天数，同时保持现有 flag 级退役流程
与 config.yaml 双读兼容窗口。
