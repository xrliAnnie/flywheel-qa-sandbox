# FLY-2452 — Claude dead account 自动接管

**Status**: ⏳ Pending review
**PR**: Pending
**Date**: 2026-09-09

## 交付

- quota monitor 将 Claude 403 disabled、订阅取消/到期及终态 auth/org 错误分类为
  `account_dead`，隔离当前 profile、立即切换，并发送带
  `account_dead:<profile>` 原因的告警；连续 poll 失败会升级真实 `claude -p` 探针。
- account switch 的 dead 标记在候选 quota exhausted 等失败分支中保持，防止失效账号
  被重新选中；runner auth 扫描也补齐 witness、daemon wake 与 account-dead 告警。
- reviewer 403/auth 失败进入等待切号的可重试状态；成功 reconcile 后按原 request id
  自动重入队仍开启 gate 的 job，并按事故轮次扫描、验证后唤醒曾命中 403 的 Claude
  pane/session，不需要 runner 重 POST、founder 手动 send 或重启 Bridge。
- FLY-1182 隔离演练补齐 S8/S9：不可用目标不得移动 Keychain/active profile；清除
  unavailable 标记后，真实 quota guard 与公开 `use` 路径可恢复切换，且 argv 不泄露
  credential marker。

## 证据

- TDD 修复批次：`runner-auth-scan` + `switch-executor` 81/81；相邻
  `account-switch-e2e` + `runner-quota-scan` + `LeadAlertNotifier` 76/76；
  `claude-profile-cli.integration` 隔离 11/11；Bridge 隔离 39/39。
- `pnpm lint` exit 0（14 条既有 warning）；`pnpm -r build` 22/23 workspace 通过。
- `pnpm test:packages:run` 完整聚合非绿：Claude Runner 1103 passed / 2 failed /
  2 skipped，失败为两个负载超时及 Vitest worker `onTaskUpdate` timeout；两个失败文件
  隔离复跑 9/9 通过。TeamLead 887/891 files、12169/12180 tests 通过；其中
  `bridge`、`claude-profile-cli` 与 FLY-2339 性能文件隔离复跑分别 39/39、11/11、
  9/9 通过，非本单 `StructuredInboxRouter` watcher 清理失败仍可单独复现。完整聚合
  因此如实保留为非绿，最终聚合权威留给 exact-head CI。
- `scripts/__tests__/lead-alert-strict-delivery.test.sh` 28/28；FLY-2271 dead-account
  evidence e2e PASS。FLY-1182 隔离演练 22 passed / 1 failed，S8/S9 通过，唯一失败为
  Lead 明确认可的既有 S2 rollback 红线，本实现未越界修复。
- 第一轮 code review 在 `e90ea564e` APPROVED；Lead 指定的两项 MEDIUM acceptance
  blocker 已由 `31f4eff97` 修复，其余 MEDIUM/LOW 作为已接受 follow-up。最终
  exact-head review 已注册，冻结本 milestone 所在 head 后才允许创建 PR。

## 边界

本阶段未 dispatch QA、未请求 ship approval、未 merge/deploy、未重启 Bridge/Lead，
也未改动 founder-only 权限边界。切号后的 credential 生效继续依赖主机 Keychain，
Bridge 子进程不需要随账号切换重启。
