# FLY-2298 — founder_review 等待纳入 DWELL 判据

**Status**: ⏳ Implementation complete; exact-head CI pending
**PR**: [#1100](https://github.com/xrliAnnie/flywheel/pull/1100)
**Date**: 2026-09-06

## 交付

- STEP DWELL 现在把与当前 run / execution 精确绑定、尚未回答的 `founder_review` 卡识别为 founder-wait，不再把停在卡上的普通 `pm` 节点每个阈值窗路由到 `deep_dive`。
- 三种 founder-wait 形状统一使用持久化的 episode 起点去重；上一轮迟到收据、跨 run / execution 卡、已回答或已注销卡都不能错误抑制当前 episode。
- `waiting_founder` writer 强制记录 episode provenance，并拒绝早于节点启动或位于未来的时间；旧库通过原位迁移保留既有收据。

## TDD、验证与评审

- 定向 TeamLead 3 files / 41 tests 全绿；修改过的 `lead-patrol-snapshot.test.sh` 为 331 passed、0 failed。
- `pnpm lint` 与 `pnpm -r build` 通过。本机按 Lead 批准口径显式排除 macOS GUI 专项并单 fork 跑完整 packages：846 files / 11,271 tests passed、6 skipped；唯一失败是未修改的 FLY-2118 真 tmux fixture，本机 tmux 拒绝含 TAB 的窗口名。
- request-driven code review Round 3 在 `f0ca875f05177a4422cae1f962a08711c6766af8` 返回 `APPROVED`，无 blocking finding；两条 LOW residue 已记入实施证据并正式回报 Lead，按 closeout 指令不再开启 Round 4。

本实现没有修改 pinned `plan.md`、`CLAUDE.md`，没有 dispatch QA、请求 ship approval、merge、deploy 或重启 Bridge / Lead。包含本 milestone 的字面最终 HEAD 由 PR 未过滤 CI 验证；14/14 全绿前不完成 implement handoff。
