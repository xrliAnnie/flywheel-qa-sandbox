# FLY-2860 — 删掉 /gemini、/gemini-advanced、/eleven、/glaw 三套旧语音

**Status**: ⏳ Pending review, exact-head CI and QA retest
**PR**: #1334
**Date**: 2026-09-25

founder 9-24 直令：Gemini 语音、ElevenLabs 语音与 `/glaw` 旧会议的全部实现和测试台架都删掉。本单按设计阶段脚本算出的引用图删除 252 个文件（`deletion-manifest.tsv` 逐文件附证据，`git diff --diff-filter=D` 与之逐行相等）：`gemini-agent` 整包、voice-bridge 的三套命令与守护进程、voice-core 的 Gemini Live 对话面 / POC `talk` / 常驻脑、守护进程的部署壳、flag 登记、e2e 台架、spike 与证据文件。voice-bridge 保留为 voice-codex 在用的 Discord 接线库，`DiscordDeps` 去掉了斜杠命令注册能力。

1. **旧产物退役。** `tsc` 不删已删源码的 `dist` 产物，而打包会原样复制 `dist/`；两个语音包的 build 先跑 `remove-retired-dist.mjs`（受版本控制的清单、越界拒绝、先规划后删除、幂等）。
2. **部署壳先红后绿。** `restart-services.sh` 删掉 voice-bridge 步骤与回滚分支；新契约测试在旧树上 8 条红、控制流断言绿，改后 14/14。
3. **launchd 墓碑（偏离原计划，Lead 同意）。** `units.manifest` 保留 `com.flywheel.voice-bridge` 的 managed 墓碑行而不是删行：convergence 会 bootstrap 任何未登记的 `com.flywheel.*` plist，删行会让残留主机把指向已删 wrapper 的任务拉起来（census 新用例先红后绿）。
4. **huddle 块退化为冲突标记。** 字段不再校验、原样保留，四处 `legacy_voice_conflict` 守卫不变。
5. **Discord 残留与残留门。** `scripts/retire-legacy-voice.mjs`（默认只读、精确白名单、必查目标未全覆盖即失败、token 不出现在回执）供 QA 在 founder 知情下跑一次 `--apply`；`residue-check.sh` 给 QA 判据 1 机械判定（逐路径逐词元允许清单，最终头 0 未放行、667 条放行，自测通过）。

验证只在本机跑了定向测试、相关构建、依赖方 typecheck 与改动涉及的全部脚本测试，`pnpm lint` 0 error；全量交给精确头 CI。代码审查：codex:rescue 一轮无 blocker（唯一 HIGH 即 Lead 已裁定为 follow-up 的 FLY-1436 耦合）；Bridge 硬闸的 Codex（gpt-5.6-sol xhigh）R1 提出 1 条 MEDIUM——退役脚本遇到名称命中但字段畸形的命令时仍以 0 退出——已改为 fail-closed 并补测试；返工头的 R3 提出 1 条 HIGH——共用 token 时 `/meet` 会扩散到非 staged 的 guild——已改为按 (token, guild) 绑定名字集合并补回归测试；R4 的 MEDIUM（必查覆盖按整组合并导致误判未覆盖）已改为按 guild 判定。QA@1 返工：退役目标补上 `flywheel-test-1`（必查）与全部 QA slot bot，并退役 staged 台架注册的 `/meet`（按目标的 `extraCommandNames`，全局白名单与项目 Lead bot 不变），只读 dry-run 找到全部 10 条遗留命令且必查目标全覆盖；同步了因 `RETIRED_FLAGS` 墓碑而变化的 teamlead flag mutator 用例。Bridge 侧 gemini-agent scoped token、FLY-1436 切换证据读 `gemini-agent/dist` 的路径耦合、`resident/claim` 路由等列为 PR follow-up。实现节点没有请求 full CI、没有派 QA、没有合并或部署。本文件是 PR 的 literal-last commit。
