# FLY-2555 — 终态 Codex daemon 安全回收

**Status**: ⏳ Pending ship
**PR**: https://github.com/xrliAnnie/flywheel/pull/1193
**Date**: 2026-09-14

在 full reconcile pass 复用既有 daemon teardown，仅对终态、无 owner/hold/TURN 且 ownership 证明充分的 Codex 执行发送 SIGTERM；下一轮真实 absence 后清理 CommDB。保留未知证据 veto 与 Claude 行为。受控生产行重放仅移除目标，8 个对照不变；FLY-2537 原因描述已纠正。lint、build、最终 17 包 aggregate 通过，代码审查 APPROVED。生产目标关闭与 QA 验收尚未完成，详见 FLY-2555-codex-terminal-absence/verification.md。

按 Lead 指令同步主线 FLY-2556 超时修复，founder-budget 单独验证 3/3 通过。保留原 CI 超时回执；同步后精确头 CI 与最终审查仍待通过。
