# FLY-2284 — slot Bridge 环境隔离

**Status**: ⏳ Implementation complete; PR #1083 awaiting independent review
**PR**: [#1083](https://github.com/xrliAnnie/flywheel/pull/1083)
**Date**: 2026-09-04

## 交付

- `scripts/test-deploy.sh` 保留普通 caller 环境，但在三条 Bridge capture branch 进入 assignment 前，
  动态删除 `FLYWHEEL_*|DELIVERY_*|*_DB|*_DIR|*_TOKEN` 与 `CODEX_HOME` 坐标；环境名先校验为 shell
  identifier，`GH_TOKEN`/`GITHUB_TOKEN` 是两项明确例外。
- Bridge 的 CommDB 固定为 caller `HOME` 下的 slot project 账，state、delivery secret、Codex home、Codex
  daemon/session/socket inventory、reports、bin/hooks 与 tmux 坐标均显式投影到 slot 所有资源。
  `DISCORD_GUILD_ID` 与 `TEAMLEAD_ISSUE_PREFIXES` 也由已解析配置显式恢复；slot `CODEX_HOME` 以 0700 创建。
- generalized branch 在 scrub 后显式恢复已验证的 slot ingest token。唯一 fixture child control
  `FLY1389_ENV_DUMP_NODE` 同样先 unset、再按名字恢复；没有放行整组 fixture namespace，也没有新增开关。
- launch spec 继续作为初次启动与 `test-cycle-bridge.sh` replay 的共同环境权威；capture schema、Bridge
  product resolver、Lead launcher 与生产配置均未修改。

## TDD 与验证

- RED：新增回归先在正常完成 Bridge env dump、cycle 与 teardown 的前提下得到 `14 passed, 6 failed`；
  失败精确显示 production-like CommDB/Codex home 进入 spec/live env，未知
  `FLYWHEEL_NOVEL_WEBHOOK_TOKEN` 进入 live evidence 与 mode-0600 sidecar。
- GREEN：同一 `test-deploy-fly1389.test.sh` 最终 `15 passed, 0 failed`，证明 mandatory slot 值、普通
  caller sentinel、GitHub token 例外、spec/live 等价与 cycle replay；任何 `FLYWHEEL_*` 值均不得含注入的
  production prefix，未知 Flywheel token 在 spec、live JSON/text 和 sidecar 中均不存在。
- `test-deploy-launch-boundary.test.sh` PASS；临时 subject 删除一处分支的共同 deny 展开后精确以
  `all three Bridge launch branches must consume the shared dynamic deny arguments` 失败，恢复的真实 subject
  再次通过。
- `test-deploy-generalized.test.sh` PASS；`test-deploy-qa-room.test.sh` 为 18/18；
  `test-cycle-bridge.test.sh` 为 31/31。为满足 generalized 既有 fixture，只按依赖拓扑逐包构建
  `agent-team-transport`、`config`、`core`、`token-usage`、`flywheel-comm`、`claude-runner`，全部 exit 0。
- 按 Lead 的舰队限流裁定，本 shell-only 变更没有运行 `pnpm -r`、packages-wide tests 或 Vitest 默认池；
  没有调用任何 Vitest。

## Conflict rework（attempt 2）

- 合入 `origin/main`（`3f45262f2`），在共同的 `BRIDGE_EXTRA_ENV` 装配点同时保留 FLY-2293 的
  `FLYWHEEL_LINEAR_STARTED_SYNC=0` 与本单的动态 deny / slot-local 显式投影；最终
  `FLYWHEEL_STATE_DIR` 仍保持 later-wins 的最后一项。
- 在合并后的树上按单线程顺序重跑五个触及面 shell 套件：launch-boundary、generalized、qa-room、
  fly1389 与 cycle-bridge，全部 exit 0；其中 qa-room 为 18/18、fly1389 为 15/15、cycle-bridge 为
  31/31。
- 本轮没有运行 `pnpm -r`、packages-wide tests 或 Vitest，也没有扩大实现范围。

real-Bridge manual probe: not run (prerequisites absent)

该 prerequisite-gated 手动检查由 QA node 执行，本 implement 节点没有用 fixture listener 冒充真实探针。

## 边界

本 implement 节点没有修改 `CLAUDE.md`，没有启动真实 slot Bridge、dispatch QA、请求 ship approval、
merge、deploy 或重启 Bridge/Lead 服务。第三方 `*_KEY|*_SECRET|*_PASSWORD|*_URL` credential 继续按已批准
范围继承；本单只隔离 identity/state coordinate 命名轴。
