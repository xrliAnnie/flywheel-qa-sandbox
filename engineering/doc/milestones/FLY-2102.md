# FLY-2102 — 启动期 env flag 固化删除

**Status**: ⏳ Pending ship
**PR**: #970
**Date**: 2026-08-27

固化删除 9 个启动/CLI 期 flag 身份：SQLite flag store 写死常开并移除 `.env` 旁路与恢复臂；ghost guard 固定 90 秒，cmux symlink/view helper/node presence 固定开启，issue-display sweep 固定 60 tick，Lead lease 写授权不再接受全局 bypass。FLY-1353 headless voice presence seam 从产品 flag registry 降为仅允许 loopback staged Bridge 的具名 QA 豁免。完整删除从未启用的 publish broker、socket 客户端、Bridge 挂载、结构测试与 CI 步骤，保留 GitHub Actions `payload-activation.yml` 发布路径；R1 后追加与 broker 解耦的 Bridge 启动前 publish credential scrub，并把 ghost/cadence 反旋钮、可执行 doc residue、authoring runbook 与 CI inventory 守卫收紧。FLY-2102 shell 守卫 46/46、22-workspace build 与触及包测试通过；全仓重跑只剩未修改 lint baseline 和当前无 Aqua 会话的 2 条 Terminal.app host 测试限制，Code Review R1 APPROVED。
