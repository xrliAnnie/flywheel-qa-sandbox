# FLY-2664 — Verified cleanup for merged runner branches

**Status**: ⏳ Pending exact-head review and CI
**PR**: [#1238](https://github.com/xrliAnnie/flywheel/pull/1238)
**Date**: 2026-09-17

Land closeout now keeps the original branch-name safety path and adds a narrowly fenced exception for runner-created branches such as `docs/…`: the trusted MERGED receipt must identify the configured repository and `main`, the registered HEAD must equal the approved PR head or be an ancestor of a freshly fetched `origin/main`, the bound path and generation must still match, and the worktree must be clean. The actual registered branch is protected-name checked, the worktree removal is verified, and its local ref is deleted with an expected-SHA compare-and-delete. A prepared audit receipt makes a crash between path and ref deletion resumable without weakening FLY-2616's ENOENT-only `absent` semantics.

Completion now refreshes `worktree_binding_branch` to the actual branch only across the same run/path/generation cohort, while finalization can still use the merge proof when an older binding retains the derived `flywheel-FLY-*` name. Closeout causes remain typed as `branch_mismatch`, `not_registered`, `dirty`, `clean_unknown`, `binding_mismatch`, or `remove_failed`; remote branch deletion rejects this local-only proof. The read-only `scripts/audit-merged-worktrees.mjs` command inventories merged residual worktrees for FLY-2628 and has no deletion mode.

Real Git regressions cover the legacy matching path, a merged `docs/…` branch that physically removes the directory, registration, and branch, an unmerged branch refusal, and tracked plus untracked dirty refusals; a CAS race also proves a moved ref is preserved and closeout remains incomplete. The derived FLY-2601-shaped sandbox replay completed twice with stable fixture digest `8ae570fe8f67ffb6ae7b88cf6cfce90a9d6e641b1a61ae4c7c06cc2f406b6e86`, no network or remote mutation, and all three local artifacts absent. This is executable isolated evidence, not production acceptance.

`pnpm lint` completed with the existing warning baseline, `pnpm -r build` passed, focused and affected suites passed, stub hygiene passed 74/74, the read-only inventory test passed, and CI structure passed. The full package gate ran all packages but is honestly red in TeamLead after 1,475 files: 17,559 tests passed, 7 skipped, and the unchanged Xiaohongshu offline-inventory test exceeded Vitest's five-second limit under aggregate host contention alongside an `onTaskUpdate` worker timeout; its required single-fork rerun passed 1/1 in 3.27 seconds. Exact pushed-head CI remains the aggregate authority. This file is the literal last commit before opening the PR; effective code review, exact-head CI, independent QA, production cleanup, merge, and deployment remain separate downstream evidence.

Round-one code review found one blocking regression outside the cleanup path: adding actual-branch observation had made `resolveBoundRepositoryAuthority` reject a normal detached HEAD. A new real-repository test reproduced the rejection before the branch probe was separated from remote/HEAD authority. `git symbolic-ref` exit 1 now yields an empty optional branch, while aborts and every other probe failure remain fail-closed. The authority callers pass 141/141, the focused authority/binding tests pass 10/10, TeamLead typecheck and package build pass, and Biome is clean. This refreshed milestone is again the literal last commit; the new exact head requires a fresh effective review and CI run.
