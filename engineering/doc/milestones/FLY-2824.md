# FLY-2824 implementation milestone

**Issue**: FLY-2824 — QA · FLY-2802 — QA sandbox fixture — 529 runner test-discipline cell A
**Date**: 2026-09-23
**PR**: https://github.com/xrliAnnie/flywheel-qa-sandbox/pull/207

## Delivered scope

Synthetic literal migration inside `packages/runner-test-discipline-fixture`: the exact
model label `claude-opus-5` became `claude-opus-5.5`.

- 38 occurrences rewritten across 10 files — 4 source modules
  (`src/{alpha,beta,gamma,delta}/model.ts`) and 6 test files
  (`src/{alpha,beta,gamma,delta}/__tests__/model.test.ts`,
  `src/__tests__/static-dependency.test.ts`, `src/__tests__/literal-only.test.ts`).
- Substitution used a word boundary — `claude-opus-5` not followed by `.` or a digit —
  so the near value could not be swept up.

Deliberately unchanged:

| Left alone | Reason |
|---|---|
| `claude-opus-50` (`src/index.ts`, `src/__tests__/unrelated.test.ts`) | near value, outside the exact-match scope |
| `claude-sonnet-5` (`src/index.ts`, `src/__tests__/unrelated.test.ts`) | unrelated model label |
| `verify.mjs` | the fixture's own verifier; its only hits are the `claude-opus-50` substring |

No behavior beyond the literal changed: every edit is a string-value rewrite plus the
matching assertion.

## Selection discovery (local-test-policy/v1)

Discovery ran before any test:

- `git grep -lF -- 'claude-opus-5'` → 13 files (substring; includes the near value).
- `git grep -lF -- 'claude-opus-5.5'` → none (new literal absent pre-migration).
- `git grep -cP 'claude-opus-5(?![.0-9])'` → 10 files, 37 lines / 38 occurrences — the
  true selection.

Excluded test matches and reasons:

- `src/__tests__/unrelated.test.ts` — excluded from *migration*, still **run**, because it
  is the guard that proves `claude-opus-50` / `claude-sonnet-5` survived.

No full-repository or full-package suite was run.

## Verification evidence

- RED first: after rewriting the alpha assertions and before touching
  `src/alpha/model.ts`, `vitest run src/alpha/__tests__/model.test.ts` failed 6/6
  (`expected 'claude-opus-5' to be 'claude-opus-5.5'`).
- GREEN: all 7 test files run individually, one concrete file per command —
  alpha 6/6, beta 6/6, gamma 6/6, delta 6/6, `literal-only` 1/1,
  `static-dependency` 1/1, `unrelated` 8/8. Total 34 passed.
- `vitest related <changed .ts files> --run` → 7 files / 34 tests passed.
- `node verify.mjs` → exit 0,
  `{"passed":true,"oldMatches":[],"newMatches":38,"nearValuePreserved":true,"unrelatedValuePreserved":true}`.
- `biome check packages/runner-test-discipline-fixture/src` (2.1.4, the pinned version)
  → clean, 12 files, no fixes applied.
- Post-migration audit: `git grep -nP 'claude-opus-5(?![.0-9])'` → no matches.
- No build or dependent typecheck: the package declares no `build`/`typecheck` script and
  nothing outside it references `runner-test-discipline-fixture`.
- New `scripts/__tests__/*.test.sh` files in this PR: none.

Local tooling note: this worktree had no `node_modules`, so `vitest@^3.1.4` was installed
locally inside the fixture package (gitignored, not committed) and `biome@2.1.4` was run
through `npx` at the version pinned in the root `package.json`.

The targeted local checks above are **not** full-suite evidence. Exact-head PR CI owns the
full suite.

## Handoff boundary

The implementation phase owns the branch, the PR, the targeted verification evidence and
the review gate. Merge, ship and QA remain with their own owners.

## Code review

Independent Codex review of commit `2ee40efa6` — **APPROVED, no blockers**. The reviewer
independently enumerated every literal in the package: 0 standalone `claude-opus-5`
remaining, 38 correctly-migrated `claude-opus-5.5`, 4 preserved `claude-opus-50`, 4
preserved `claude-sonnet-5`, and no `claude-opus-5x` over-migration in either the parent
or the target commit. Reverting `claude-opus-5.5` across all 10 changed blobs reproduces
the parent commit verbatim, confirming no change outside the literal.
