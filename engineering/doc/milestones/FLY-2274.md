# FLY-2274 — FLY-2264 cutover 窗口件

**Status**: ✅ Implementation complete; code review r4 approved
**PR**: pending creation from `flywheel-FLY-2274`
**Date**: 2026-09-02

本单在 `scripts/cutover/FLY-2264/` 交付了受审窗口脚本、恢复件与安装器：launchd supervisor
label 生成器/固定样本、恢复优先且保护 updater 的 bootout、只按原 plist bootstrap 的 restore、按
PID/start/image/socket 重证后精确 `kill-server` 的旧 tmux 关闭脚本、30 秒 phase-B link/pin 合同，以及
输出七份私有 JSON artifact 的原生 tmux 验收器。`install-window-artifacts.sh` 只发布到固定
`$WINDOW_DIR/artifacts` 子目录，以 0700 安装可执行文件并生成相对路径 sha256 manifest；已知 runtime
artifact 可在不改受审字节的前提下重复校验。

tmux census 保留 `preflight-receipt.processInventory` 的原始 lsof endpoint 形状。socket-owning、且 argv
符合 `-D`/`-S`/`-L`/`new-session` allowlist 的进程按 server 处理；只有没有 filesystem socket 的
`attach-session` 进程作为 `role=client,socket=n/a` informational。server 的 socket owner probe 使用真实
tmux 支持的两次独立 `#{pid}` / `#{socket_path}` 查询，不依赖 tmux 不会展开的 `\t`。未知命令形状、
多 socket、probe/coalition/transport 不确定、PID reuse 或 union 外 server 都在首次 kill 前 fail closed。

验证：`pnpm lint` 与 `pnpm -r build` 通过；分支新增 shell tests 全绿（installer 9/9、phase-B 4/4、
supervisor 9/9、stop 8/8、verifier 14/14，另有 CI structure），host cutover 原合同 21/21；Linux golden
artifact 的 0600 断言也使用数值校验后的 GNU-first/BSD-fallback probe。真实 Intel
tmux 3.5a 私有 socket 的 owner probe 已执行通过；完整三-server stop drill 在 resident sandbox 的 process
inspection guard 处跳过；QA 首次运行因 BSD `mktemp -d -t` 生成的路径令 socket `sun_path` 超长，在创建
socket 时 rc=1 失败，改为短 `/private/tmp/f2264.XXXXXX` 根后已实证 3 个 target 停止且独立 control 存活。
精确 `pnpm test:packages:run` 已执行，只有无法连接 Terminal.app/HiServices 的
两条 Core real-osascript 用例失败，其余 Core 219/221 通过。request-driven code review r4 在
`65684a6f6` APPROVED（gate `829871c6-7af4-4f48-9548-b7562751ed60`）。

Handoff advisories：按 Lead 裁定，本单不改原 `host-terminal-cutover.sh`，其 union census 的
transient-exit 重跑要求及与新 library 的刻意分叉保留；
Bridge PATH 证据是 launchd plist + reviewed wrapper 的静态合同，不冒充 runtime env；socket-less Intel
attach client 会被 artifact 明列但不阻塞 server census。

本实现和测试没有 bootout production label、link/unlink Homebrew、创建重启票、操作 production tmux
socket、部署、merge 或 dispatch QA。
