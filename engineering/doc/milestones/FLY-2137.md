# FLY-2137 — founder 日历写入治理

**Status**: ⏳ Pending ship
**PR**: #1007
**Date**: 2026-08-31

为 founder Google Calendar 增加行为与审计边界：直接 `gog` / `gws` Calendar 写入由 P6 guard 统一识别，支持 audit → enforce 切换，仅精确放行 QA 日历，并且不能通过既有 bypass 绕过；同时增加每日只读 sweep，汇总无 `raya_meeting_id` 标记的可疑事件、P6 写入活动和治理模式异常，经既有 Lead alert 通道报告。LaunchAgent 安装保持显式，不随本 PR 部署或激活。

TDD 覆盖包括 guard 282/282、sweep 21/21、installer 5/5、config 33/33、TeamLead alert 37/37；`pnpm lint` 与 `pnpm -r build` 通过。PR 首轮 CI 暴露的新 shell suite 枚举缺口已补入 shard 2，枚举与 CI 结构守卫均转绿。精确 `pnpm test:packages:run` 在当前 macOS 环境停于不可用的 `com.apple.hiservices-xpcservice` 导致的两项 tmux-viewer 测试；headless 全包运行的断言全部通过，但 Vitest worker 在长时 claude-profile 套件后出现一次 `onTaskUpdate` 超时。

跨模型代码审查第三轮在 `d0578efb2` 通过。凭据本身的不可读隔离不由本单解决，继续由 blocking follow-up FLY-2204 跟踪；`CLAUDE.md` 治理规则也按 Lead 指示留到 founder 激活时统一加入，本 PR 不修改该共享文件。
