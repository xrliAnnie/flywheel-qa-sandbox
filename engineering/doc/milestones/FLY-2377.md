# FLY-2377 — Self-ship 后线程立即归档

**Status**: ⏳ Implementation complete; PR CI pending
**PR**: pending creation
**Date**: 2026-09-06

## 交付

- post-ship finalization 现在在同一 tick 使用 terminal authority 立即归档 issue thread，不再依赖「最后一条消息后安静 60 分钟」窗口。
- 保留 FLY-2028 的 per-thread lock、Discord 现态探测、frontier fence、归档后验证与 reopen 补偿；`ISSUE_THREAD_QUIET_WINDOW_MS` 未改，非 ship 归档仍走原 quiet 语义。
- execution / land closeout 使用确定性 `post_ship_archive` receipt。完全匹配的热库或冷库 receipt 会零 Discord I/O 收敛重放，避免 founder 后续重新打开的线程被旧 closeout 再次归档；无效或不匹配 payload fail closed。
- post-ship-only quiet queue enqueue 与跨层注入已删除；done-cleanup / terminate / abandon 的归档语义和非 ship targeted queue 保持不变。

## TDD 与验证

- RED → GREEN 覆盖 immediate recent-message、热/冷 receipt replay、跨写入竞争、已有/缺失 archive epoch、重开 frontier、PATCH 后验证失败、land/execution receipt、founder 重开零 I/O、post-ship 顺序和 queue wiring 删除。
- 最终聚焦归档套件 5 files / 133 tests，相关 wiring 集成 5 files / 66 tests，stub hygiene 2 files / 64 tests，全部通过。
- TeamLead 最终头 858 files / 11,412 tests passed / 6 skipped；无断言失败。Vitest 在断言完成后的既有 worker RPC 收尾超时使进程 exit 1；相关 mailbox 与 visual-capture 宿主抖动均已隔离复跑全绿。
- `pnpm -r build` 21 个 workspace 全绿；milestone layout shell gate 32/32；`git diff --check` 与本次 13 个可处理 TS 文件的 Biome 检查通过。
- 根 `pnpm lint` 的唯一 error 是基线已超 1 MiB 的 `StateStore.ts`（origin/main 2,163,682 bytes，本头 2,164,932 bytes）；未修改 lint 配置。安全 package 聚合明确排除了会真实驱动 Terminal.app 的 core GUI 文件；详细运行与隔离证据见同目录 `implementation-evidence.md`。

## 评审与边界

request-driven cross-family code review R1 在精确实现头 `1208102febbfcb90c76451466040c518eccd2c0b` 返回 `APPROVED`，无 HIGH/blocking finding。两项 MEDIUM advisory（跨进程 receipt race 的本地补偿收敛、legacy 非 resumable 失败的分钟级 retry）和一项 LOW advisory（payload schema 与 receipt ID 前缀联动）已按合同报告 Lead，留作后续治理判断。

本实现没有修改获批 plan、`CLAUDE.md`、quiet-window 数值、线上配置或 secrets，也没有 dispatch QA、请求 ship approval、merge、deploy 或重启服务。包含本 milestone 的 literal branch head 将由 PR exact-head CI 执行最终聚合门禁。
