# FLY-2634 — 周期摘要写侧跳过无实质变化

**Status**: ⏳ QA rework verified locally; pending fresh exact-head review and CI
**PR**: [#1229](https://github.com/xrliAnnie/flywheel/pull/1229)
**Date**: 2026-09-16

在既有六小时 `summary_due` first-beat 中加入默认开启、可热关闭的活动门：用持久化 Lead 事件、项目 CommDB 与受支持的 Linear 变更区分 active、quiet 与 unavailable；GitHub 摘要账本继续独立判断是否有未读内容需要 Raya 轮次。已知静默且观测连续的 producer 记 `summary_due_skipped`，不投递唤醒；来源未知、游标不连续、collector 失败或绑定 Linear 无法证明零变化时一律 fail open，仍按原路径投递。静默后的新成果、阶段/阻塞变化、明确 @Lead 派活或已有未读摘要会恢复正常交付；摘要自己的周期事件、对账噪音与 Raya 消费不会自激下一轮。

实现同时让结算后台区分有依据的跳过、应交未交与不可观测，并保留显式一次性 `flywheel-comm summary`、六小时节拍和原有 Lead 职责。聚焦验证覆盖 activity probe、StateStore/CommDB 游标、Linear 正命中与不可证明零变化、rider/round 真值表、render、规则 bundle、flag 热切换和三包构建。合入不等于部署或生效；本实现没有合并、部署、重启、改写生产 flag override、关闭 Lead 职责或改动 Raya 运行规则，生产 writer/reader 调用与 token 下降需由后续部署后完整 slot 的独立证据证明。

QA attempt 1 的功能重放在 28 个真实六小时 slot 上得到 gate OFF 335 次、gate ON 100 次唤醒，235 次有依据跳过且零误判；返工仅处理与 `origin/main` 的夹具冲突，并新增 G7 缩样重放护栏。该用例覆盖冷启动、连续静默、业务变化恢复与再次静默，分别对“把 skipped 行并入投递列表”和“让 due 查询纳入 skipped”两枚变异体打红，恢复真实实现后通过。同步后 focused 93/93、milestone 32/32、Biome 与 config/comm/edge-worker/teamlead 构建均绿。
