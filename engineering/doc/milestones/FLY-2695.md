# FLY-2695 — Raya 并仓 S2:宿主 shim `raya-cos.sh` + 收敛器 retired/residue 合同

**Status**: ⏳ Pending code review, exact-head CI, and QA
**PR**: #1320
**Date**: 2026-09-24

Epic FLY-2679 的 S2,按已合入的 FLY-2680 plan §6.2(L1 收敛 shim)、§10.2 注、§12 S2 行施工。
新增 `scripts/raya-cos.sh`:只经 host-config 解析 `FLYWHEEL_DIR`(ENV > `host.json` > `$HOME/Dev/flywheel`),
然后 `exec node $FLYWHEEL_DIR/packages/raya-cos/dist/cli.js "$@"`。它不改 cwd、不改 argv、不 source `.env`,
配置失败一律 78(含 host-config.sh 存在却加载失败的情形;为此绕开了 macOS bash 3.2 在 `set -e` 下 `source` 失败即退出的怪癖);因收敛器 1024 B 的不可调门槛,体积用实质注释撑到 3129 B。
`converge-flywheel-bin.sh` 把它追加进 monorepo `FILES` 与首次采纳白名单(packaged 分支不装)。
同时修了一个对全部白名单名字生效的并发缺陷:采纳资格改在检查之前判定,两个 Lead 启动时重叠的收敛不再把预期 rollout 误报成 severe 漂移。
另新增精确的 `RETIRED_FILES`:bin 副本与 adoption marker 都走 `strict_discard`,删除必须被证明,
不可证则 rc=1 并告警;packaged 树当前版本即把 `raya-cos.sh` 视为 retired;`FILES ∩ RETIRED_FILES` 在任何写之前 exit 1;
形态谓词按 file/link/absent 顺序,并发删除不会误判;forward-rollback 四步写进了代码注释。

测试:`converge-flywheel-bin.test.sh` C9d 6→7,新增 C13–C19(含旧谓词顺序的变异对照),28/0;
新增 hermetic `raya-cos-shim.test.sh` 9/0 并登记进 `ci.yml`;四个兄弟夹具补了枚举
(`converge-fly1389`、`fly1577-cmux-bin-closure`、`fly1577-alert-arrival`,以及 research 漏列的 `codex-quota-summary`),
每个都先确认因 `raya-cos.sh` 而红,再改绿。QA ④ 两层都通过:(A) 从 Raya 实跑的 Codex 0.156.1 二进制切出的
Seatbelt 策略复演,shim `status` rc=0,`$HOME`、`~/Dev/flywheel` 与 shim 所在 bin 三处写入均被拒;
(B) 隔离 `CODEX_HOME`(school 池快照,未发生 token 轮换)下的真 `codex exec` 回合,工具事件显示写 shim bin 为
`Operation not permitted`,经 shim 跑 `status` exit 0 且输出 `{"schemaVersion":2,"operations":[]}`。
实施中的新发现:`status` 会在 cwd 建空的 `state/cos/operations/` 骨架(不写文件),research/plan 写的「不落盘」不准确,已更正判据。
证据全文见 `engineering/doc/FLY-2695-host-shim-converge/validation.md`。

合入并部署后生产 Raya 行为零变化(人设在 S3 之前仍指 `business/current`)。
`flywheel-codex-lead-wrapper-raya-tui-fullaccess.sh` 按 Lead 裁定留给 T6,未列入 retired。
实施节点没有请求 full CI,也没有 dispatch QA、merge、ship、deploy 或 restart。本文件是 PR 的 literal-last commit。
