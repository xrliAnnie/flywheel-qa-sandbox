# FLY-2796 — Engine-independent headphone mode minimum

**Status**: Pending exact-head code review and independent QA
**PR**: [#1309](https://github.com/xrliAnnie/flywheel/pull/1309)
**Date**: 2026-09-24

This branch establishes one exported `RoomIO` v1 contract and
`ROOM_IO_VERSION = 1`, moves the physical Discord room implementation into
voice-bridge, and leaves voice-codex as a compatibility adapter. The same room
implementation now backs the preserved `/gemini` and `/eleven` paths. Its
contract covers attributed input frames, versioned audio formats, streaming
output with backpressure, local playback cancellation, presence, receive
health, audible-tail estimates, barge-in observation, and generation/lease
fencing. Resident session ownership provides the single-session slot and emits
mechanically comparable room identity receipts.

The restored and refactored Raya `InboxReader` and `SpeechBrief` rules feed an
engine-independent `HeadphoneMode` through the complete V1 session surface.
Entry speaks the current pending questions and reports, new durable inbox items
are inserted without waiting for a user prompt, and the configurable heartbeat
defaults to five minutes with the explicit note that this is an engineering
default pending founder use. A durable Bridge collector, snapshot cursor,
claim-before-speak attempt, content-proof receipt, and ack fence preserve
at-least-once delivery without treating a speech request as proof that audio
played. Source failures and voice failures remain explicit rather than being
reported as successful status.

Founder-directed durable questions now retain their CommDB question identity
through projection and Discord-card ingestion. Pending/resolved state comes
from that persisted authority rather than message-text heuristics, while
ordinary Runner-to-Lead questions remain outside the founder headphone feed.
The same identity coalesces an uncarded projection with its later Discord card.
Invalid or stale bindings are isolated per message, and a bad projected row
cannot block later questions or trigger a partial resolved-state reconciliation.

The same public layer adds durable final-transcript receipts, guarded spoken
exit, and the typed `handoffToLead` carrier. Handoffs validate a current room
binding and founder transcript before persisting, use a deterministic mailbox
identity before I/O, reconcile ambiguous dispatch without blind retry, and
expose a per-handoff replayable result stream. An authenticated production SSE
transport now implements
`subscribeReplies(binding, listener): () => void`; wake events contain only
`sessionId`, `generation`, and `handoffId`, so durable result replay remains the
content authority. Runtime disconnects are recorded and reconnected without a
polling fallback, reconnect wakes each registered handoff once, and the
unsubscribe function is idempotent.

The first exact-head code review rejected the always-on production background
collector and reconciler because no deployed consumer currently drains that
feed and unbounded history could grow indefinitely. The review revision keeps
all authenticated HTTP/SSE contracts available but starts those two background
loops only when `FLYWHEEL_HEADPHONE_BACKGROUND_ENABLED=1`. The opt-in collector
also prunes at most 1,000 expired, unclaimed revisions after each tick. Inbox
retention defaults to 30 days and accepts a bounded 1–365 day override through
`FLYWHEEL_HEADPHONE_INBOX_RETENTION_DAYS`; active claims are never pruned. This
is an operational safety gate, not a claim that the production headphone
consumer is deployed.

QA follow-up then established that this opt-in default made the delivered entry
briefing and proactive inbox speech unreachable under the staged deployment.
The current rework supersedes that temporary review revision: the registered
`headphone_background` boolean flag is now **on by default**, and
`FLYWHEEL_HEADPHONE_BACKGROUND_ENABLED=0` is the emergency kill switch. With a
canonical founder identity present, the Bridge owns a 5-second collector timer
and a 1-second ambiguous-handoff reconciliation timer. With the flag off those
timers remain as lightweight no-op clocks, but each callback returns
before reading Discord sources, pruning inbox state, opening CommDB, or
processing a handoff. Without a canonical founder identity neither timer is
created. Retention remains the bounded 30-day default described above.

The two staged Gemini and Eleven rigs now supply the resident lease `leadId`
and canonical founder identity required by the fail-closed room contract. Bot
user identities remain derived from the authenticated Discord clients instead
of being accepted as configurable claims. Production `HeadphoneSession`
composition is intentionally not duplicated here: approved PR #1312 at
`50c32fefd` supplies the V1 session adapter and composition root on top of this
branch. Required merge order is **#1309, then #1312**.

QA round 2 froze `0add6558c` and correctly failed it on two PR-caused full-CI
reds. The new headphone environment controls were missing from feature-flag
truth accounting, and the Silero model had moved to `voice-bridge` while the
packaged-runtime closure still referenced `voice-codex`. The rework records the
two boot-time controls as named operational non-flags, moves every package
asset/allowlist/native-load reference to `voice-bridge`, includes the
`voice-headphone` workspace dependency in the payload, removes the now-dead
`voice-codex` models declaration, and restores both original Silero unit/smoke
tests beside the moved implementation. Those tests are byte-identical to their
pre-move versions except for the model's relative path; no VAD behavior or
frozen assertion changed.

`HeadphoneSession` also exposes the public
`createUtteranceProjection(session): HeadphoneUtteranceProjection` assembly
seam. It passes the unmodified V1 utterance source to an injected projection,
owns projection start/close with the session, and lets FLY-2798 attach its
`LiveCaptionProjection` and room TIV `CaptionSink` without a circular package
dependency. The focused proof sends `frontend` and authoritative `lead:<id>`
utterances through the same session and preserves the distinct `🤖 前台` and
`💬 Lead` labels at the sink. Query, judgment, and action are carrier
classifications only; no ship-specific spoken authority, filtering memory, or
interruption behavior was added.

The reply/caption code head `41b22a6cf` passed:

- voice-headphone changed-file `vitest related`: 2 files, 18/18 tests;
- Teamlead handoff-route `vitest related`: 102 files, 1176/1176 tests;
- direct reply subscription/client checks: 14/14; direct handoff route checks:
  6/6; direct headphone session/projection checks: 4/4;
- affected dependency builds: 14 workspace packages;
- dependent typechecks: voice-headphone, Teamlead, and voice-codex;
- repository lint completed with no errors and 25 pre-existing warnings; exact
  changed files pass Biome, and both worktree and branch `git diff --check` are
  clean;
- PR Quick Gate and `CI Scope OK` passed at that code head. `CI Scope OK` is
  scope evidence only, not full exact-head CI.

The background-ingestion review revision code head `44a8f0b37` passed:

- direct runtime-config and inbox-retention tests: 17/17;
- Teamlead typecheck and the affected dependency build: 13 workspace packages;
- repository lint with no errors and the same 25 pre-existing warnings;
- `git diff --check`.

Its required Teamlead `vitest related` selection covered 572 files and recorded
8005 passes, four skips, and three failures in the unrelated
`runs-route.dag-entry.test.ts` fixture, where temporary start calls returned
404. The exact failing file immediately passed 70/70 in isolation. The broad
related invocation is therefore disclosed as red rather than presented as
green; the failure did not reproduce in its focused rerun.

The QA-rework code heads `ec0406fca` and `6c3811e20` passed:

- the exact formerly-red config drift file: 14/14; config changed-file
  `vitest related`: 18 files, 326/326 tests;
- the restored Silero tests: 2 files, 5/5 tests, including corrupted-model
  rejection, incompatible ONNX metadata, state immutability and the frozen
  real-speech score (`243/298` positive chunks);
- the real package-onboard smoke with isolated npm cache: 26/26, including
  tarball gates, zero `workspace:*` dependencies, real npm install, native
  Silero execution from `voice-bridge`, every embedded-package bare import,
  packaged Bridge `/health`, and launcher dry-runs;
- retained direct consumers: `fly1674-residue` 85/85,
  `fly2102-flag-freeze` 46/46, `package-onboard` 36/36, version injection
  10/10, Lead packaging 6/6, packaged seams 18/18, release-contract vectors
  22/22, and Raya COS package contract 7/7;
- affected filtered builds completed across the config and voice dependency
  closures; repository lint completed with no errors and 25 pre-existing
  warnings; `git diff --check` is clean;
- `npm pack --dry-run` reports 141 `voice-codex` files with zero model assets
  and 243 `voice-bridge` files with exactly the model and license.

The QA-round-3 rework code head `04db08d61` passed:

- exact config, store-policy, registry, and drift checks: 4 files, 100/100;
- exact Teamlead runtime-config and flag-store checks: 2 files, 38/38;
- staged-rig identity checks: 1 file, 7/7, plus syntax checks for both real rig
  entrypoints;
- changed-file `vitest related`: config 21 files and 378/378; Teamlead 170
  files and 2384 passes with one intentional skip; voice-bridge 1 file and
  7/7;
- affected dependency builds completed across 14 workspace packages, including
  Teamlead and voice-bridge; both affected packages typechecked;
- repository lint completed with no errors and 25 pre-existing warnings;
  changed-file formatting and `git diff --check` passed.

Consumer discovery retained the exact flag wrapper/read site, config policy and
registry suites, runtime-config parser, both staged-rig entrypoints, and the rig
helper test. Broad basename and parent-directory hits under `packages/config/src`,
`packages/teamlead/src/bridge`, and `packages/voice-bridge/e2e` were inspected
and excluded when they did not import or execute the changed symbols. No new or
changed `scripts/__tests__/*.test.sh` exists in this rework.

The first scoped review of the default-on follow-up bound itself to
`aac265ce3` and returned `CHANGES_REQUESTED`. Lead authorized exactly two
blocking fixes: `collector-scope-starvation` and
`classify-events-scan-blocks-bridge`. The collector now sorts every eligible
scope by its persisted last-poll time, using bootstrap state only as a
same-timestamp tie-break, so a completed short channel cannot starve the rest.
Question-card resolution now performs one batch lookup per non-empty Discord
page and scans persisted ship-gate bindings at most once for the whole page,
rather than once per message. Empty pages perform no binding lookup.

Lead initially ruled `resident-session-no-reaper` overruled for this PR and
authorized a second scoped review covering only the two fixes above plus the
preceding default-on and staged-rig changes. That review found a separate HIGH:
an immutable-revision conflict left its source with the oldest scheduling key,
so the new fair sorter could retry that poisoned source forever and starve the
remaining scopes. Lead then revoked the resident-reaper ruling and authorized
one third and final scoped review covering exactly the inbox-conflict recovery
and resident-lease expiry recovery below.

The review-fix code head `3e5dc7711` passed:

- TDD red proved the prior behavior (`questionIdByMessage` invoked three times
  for one three-message page, and three eligible scopes polled as `a,a,a`);
- the focused headphone inbox file then passed 17/17, including one batch
  lookup for a non-empty page, zero lookup for an empty page, and bounded
  `a,b,c` scope rotation;
- changed-file `vitest related` passed 102 files and 1187/1187 tests;
- Teamlead typecheck and its 13-package dependency build passed;
- repository lint completed with no errors and 25 pre-existing warnings;
  changed-file Biome and `git diff --check` passed.

The final-review-fix code head `73acd6329` preserves immutable inbox evidence
while updating only the poisoned source's scheduling timestamp and recovery
health, so the page remains retryable after every other eligible scope gets a
turn. It also expires an abandoned resident lease transactionally before a new
claim. The previous owner becomes `failed` with
`resident_lease_expired`; a live heartbeat renewal extends exclusivity, while a
claim at or after the extended expiry can take over. The existing resident
clients continue renewing at one third of the lease TTL, and old owners cannot
renew after the terminal transition.

This code head passed:

- TDD red first reproduced all three missing behaviors: an ingest conflict
  escaped the collector, an expired resident owner still blocked takeover, and
  a renewed lease never became reclaimable at its extended expiry;
- focused Teamlead collector and resident-store tests: 2 files, 23/23;
- retained voice-bridge resident, Gemini-assistant, and Eleven session tests:
  3 files, 61/61, including the existing heartbeat wiring;
- changed-file Teamlead `vitest related`: 571 files, 8013 passes and four
  intentional skips;
- Teamlead typecheck and its 13-package dependency build;
- repository lint with no errors and the same 25 pre-existing warnings;
  changed-file formatting and `git diff --check` passed.

QA then froze `70c1a0ca0` and passed exact-head full CI, resident lease
expiry/heartbeat recovery, the default-on kill switch, ingest fairness, and the
Gemini staged rig. Its one hard red was isolated to the Eleven staged rig's
same-process `all` path: boot A replaced `FLYWHEEL_BRIDGE_URL` with its mutex
black hole and then deleted the variable, so boot B deterministically exited
from `baseConfig()` before starting. The rework code head `3ce272d76` captures
the caller's staged Bridge URL and resolved API token before boot A, uses those
values for the temporary mutex environment, and restores both before boot B.
It does not touch RoomIO, resident leases, command wiring, or audio behavior.

This rework passed:

- TDD red: the new lifecycle regression check failed 1/8 because the caller
  Bridge configuration was not captured or restored before boot B;
- TDD green and the required changed-TypeScript `vitest related`: 8/8 each;
- `node --check` for the real Eleven rig entrypoint;
- the retained `fly2102-flag-freeze` shell consumer: 46/46;
- the voice-bridge dependency build across 12 workspace packages;
- repository lint with no errors and the same 25 pre-existing warnings;
  `git diff --check` passed.

The follow-up c2 code head `1363fb9fb` closes the remaining startup race.
Resident claim and renew HTTP calls now have a configurable hard bound through
the already truth-accounted operational control
`FLYWHEEL_VOICE_LEASE_HTTP_TIMEOUT_MS`; the voice-bridge resolver documents and
supplies a 2,000 ms default, and rejects zero, fractional, non-numeric, or
unsafe-integer values. `/gemini`, `/eleven`, and `/glaw` report the failed
resident claim as `语音不可用` with its stable reason. Gemini and Eleven acquire
the shared local slot before awaiting preflight or the resident claim, release
it on every failure, and synchronously replace the provisional holder with the
authoritative resident lease on success. A concurrent command therefore gets
an immediate busy response instead of starting another network claim.

Lead ruling allowed one configuration-only correction in the unchanged Eleven
mutex boot: its synthetic `/gemini` holder now receives the same staged
resident `leadId` already supplied to `/eleven`. Missing production identity
remains fail-closed; no product fallback was added. The rig assertion, timing,
and mutex verdict logic are unchanged.

This c2 rework passed:

- TDD red/green for blackholed claim and renew deadlines, founder-facing
  unavailable replies, shared-slot exclusion during a pending claim, release
  on every failure, and the staged holder's resident Lead identity;
- direct voice-bridge tests: 6 files, 77/77; changed-TypeScript `vitest
  related`: 22 files, 245/245;
- config truth/drift tests: 56/56; retained `fly2102-flag-freeze`: 46/46;
- the unweakened staged Eleven mutex leg passed in the QA guild. `/eleven`
  received busy at `2026-09-24T15:16:02.965Z`, while `/gemini` reported
  `resident_voice_claim_timeout:2000ms` at `15:16:04.701Z`;
- voice-bridge plus 11 dependencies built; voice-bridge and voice-codex
  typechecked; repository lint passed with the same 25 pre-existing warnings;
  changed-file Biome, entrypoint syntax, and `git diff --check` passed.

Consumer discovery used every changed file's full path, filename, and parent
directory. Retained consumers were the six direct voice-bridge tests, the full
245-test related selection, config truth/drift, `fly2102-flag-freeze`, the
staged Eleven entrypoint, and the existing rig-config source contract. The 379
generic `config.ts`, 227 generic `cli.ts`, historical engineering/product
evidence, audit inventories, unrelated e2e entrypoints, and other basename or
parent-directory hits were inspected and excluded because they neither import
the changed symbols nor execute the resident startup path. No new or changed
`scripts/__tests__/*.test.sh` exists in this batch.

The fourth scoped review bound itself to `c371bfd8a` and returned
`CHANGES_REQUESTED` on two HIGH findings. The review-fix code head
`ce98197e1` addresses both without changing RoomIO or the staged rig verdict:

- `/glaw` now starts resident lease renewal before its potentially long meeting
  assembly. A renewal loss tears the matching meeting down, stale callbacks
  cannot tear down a successor, normal release stops renewal, and the remote
  lease closes before the local slot becomes available again.
- Both `/eleven` preflight fetches (ElevenLabs agent and local shim) now carry
  the same configured hard timeout as resident lease HTTP. Either timeout
  returns a service-specific founder-facing reason and the command releases
  its provisional shared slot.

The review-fix head passed:

- TDD red first reproduced the missing `/glaw` heartbeat owner and the hung
  agent/shim preflight; focused green passed 11/11;
- the complete retained c2 direct set passed 8 files and 88/88; changed-file
  `vitest related` passed 6 files and 49/49;
- config truth/drift passed 56/56 and `fly2102-flag-freeze` passed 46/46;
- voice-bridge plus 11 dependencies built; voice-bridge and voice-codex
  typechecked; repository lint passed with 25 pre-existing warnings; changed
  files passed Biome and `git diff --check`;
- the unmodified staged mutex leg passed again: busy at
  `2026-09-24T15:34:54.887Z`, followed by the explicit 2,000 ms resident claim
  timeout at `15:34:57.478Z`.

Consumer discovery retained the new heartbeat unit, Eleven wiring tests, all
six direct `vitest related` consumers (including daemon health/brain-port and
assistant wiring), and the staged mutex entrypoint. Generic `wiring.ts`,
`cli.ts`, `packages/voice-bridge/src`, historical documents, and unrelated
e2e/test-directory matches were excluded because they neither import the new
heartbeat helper nor execute the bounded preflight path. No new or changed
`scripts/__tests__/*.test.sh` exists in this review-fix batch.

Consumer discovery used the changed files' full paths, filenames, and parent
directories. The rig lifecycle test was retained as the direct executable
consumer, and `fly2102-flag-freeze` was retained because it scans that test
file. Historical engineering/product evidence, generic directory scans, and
other e2e entrypoints that neither import nor execute the Eleven rig were
excluded as non-consumers. No new or changed `scripts/__tests__/*.test.sh`
exists in this rework.

Consumer discovery retained the production collector wiring, the resident
claim route and service tests, and the focused collector/store tests. The
complete Teamlead related set above exercised transitive `StateStore`
consumers. Broad `packages/teamlead/src` and bridge-directory hits that did not
import the changed symbols were excluded as basename or parent-directory
collisions. No new or changed `scripts/__tests__/*.test.sh` exists in this
final-review-fix batch.

Consumer discovery again used each changed file's full path, filename, and
parent directory. The retained executable consumer is the Teamlead related set
above, including the focused inbox test. The broad `plugin.ts` and
`packages/teamlead/src/bridge` hits were excluded as basename/directory
collisions when they neither import the two changed helpers nor exercise their
collector path. No new or changed `scripts/__tests__/*.test.sh` exists in this
review-fix batch.

Consumer discovery ran `git grep -lF` with the full path, basename and parent
directory of all seven rework files. Retained executable consumers are the
config related suite, the two flag-residue shell gates, the package-onboard
core/smoke/version tests, packaged seams, Lead packaging, release-contract
vectors, Raya COS package contract and the CI smoke entry; all are listed
above. Excluded full-path hits were inspected individually: credential-cutover,
credential-sweep and link-truth tests only grep their own curated entries;
Gate4 tests exercise unchanged gate functions; launchd, log-rotate and resident
recovery tests inspect unrelated allowlist rows; `check-voice-api-auth-local.mjs`
reads package identity rather than its `files` array; the kill-path inventory
stores only a fixture location; `fly2278-red/M4.txt` is a negative fixture; and
the FLY-1645 JSON rows are path-only residue inventory. Historical engineering,
product and contract prose plus generic `package.json`, `scripts`,
`scripts/__tests__` and feature-flags basename/parent collisions were excluded
as non-consumers. The two newly restored tests have no runtime consumers.

QA round 4 froze `22724980f` and passed exact-head full CI, the blackhole/busy
startup proof, the kill-9 lease recovery proof, and the Gemini staged rig. It
found two current-head reds. First, the new resident lease callers closed normal
Gemini sessions without a reason and Eleven sessions with `manual`/`shutdown`,
while the durable state contract accepts `voice-stop`; the row therefore stayed
live until TTL expiry. Second, the Eleven rig's default `all` mode ran its two
boots in one process despite its own stale Discord connection warning, and its
STOP check counted the canonical RoomIO's continuous silence clock as audible
speech.

The QA-round-4 rework code head `b01cbe4bb` fixes those failures without changing
RoomIO or its identity digest. `ResidentVoiceLease.close` now requires a typed
terminal reason, and Gemini/Eleven share the canonical normal reason
`voice-stop`. The durable-store proof transitions a live resident row to ended
and admits a competing room claim immediately. The default Eleven `all` path is
now a fail-closed parent that runs `mutex` and `audio` in isolated child
processes. Its audio proof counts non-silent PCM, so the silence clock remains
observable without masquerading as old speech. The existing Eleven local
barge-in path also cancels a RoomIO speech whose platform stream has ended but
whose audible tail is still estimated as present; idle speech with no estimated
tail retains the prior non-suppressing behavior.

This rework passed:

- lease TDD red/green: the focused Gemini/Eleven caller set first failed 2/59,
  then passed 59/59 with `voice-stop`;
- rig/tail TDD red/green: four new assertions first failed, then the three
  focused files passed 66/66;
- retained direct voice-bridge consumers: 13 files, 175/175; changed-production
  `vitest related`: 13 files, 148/148;
- Teamlead normal-end/immediate-reclaim contract: 6/6;
- voice-bridge plus 11 dependencies built; Teamlead plus 12 dependencies built;
  voice-bridge and dependent voice-codex typechecked;
- the Eleven rig entrypoint passed `node --check`; all 11 changed files passed
  Biome and `git diff --check`.

Repository-wide `pnpm lint` was rerun after the retention revision and exits
zero with the existing warning inventory. The two changed files also pass
Biome and `git diff --check`. No full CI was requested for this new head; QA
owns the frozen-head rerun.

Consumer discovery used every changed source's full path, filename, parent
directory, and emitted import path. Retained consumers were the lease client,
Gemini/Eleven session and wiring/command tests, assistant speaker live-chain and
waiting-cue tests, rig-config executable contract, Teamlead resident store test,
and the required related selections above. Historical engineering/product
documents, audit inventories, generic assistant/eleven directory matches, and
other e2e entrypoints were excluded because they neither import the changed
symbols nor execute these close/tail/rig paths. No new or changed
`scripts/__tests__/*.test.sh` exists in this rework.

Earlier focused branch verification also passed:

- voice-core headphone mode and durable transcript: 14/14;
- voice-codex room, audio, and session: 50/50 on the implementation pass, plus
  33/33 related tests before the two moved Silero tests were restored;
- voice-bridge RoomIO/runtime plus Gemini and Eleven wiring: 56/56;
- Teamlead inbox/routes/handoff/retention: 51/51 on the implementation pass,
  11/11 on the first review revision, and 14/14 after the failure-isolation
  revision;
- affected Teamlead and voice-codex dependency builds and typechecks.

The broader required `vitest related` runs selected most of Teamlead and comm.
The implementation pass recorded 9616 Teamlead passes with four skips and one
comm timeout under aggregate contention; isolated reruns passed the changed
retention registry 36/36, two unrelated timeout files 57/57 and 70/70, and the
comm timeout test. The review-revision Teamlead selection recorded 7983 passes
with four skips and two failures in an unrelated shared-state route test; its
exact 27-test rerun passed. The remaining unrelated Teamlead 500 ms
child-process fixture from the implementation pass is byte-identical to
`origin/main` and still times out locally; it is not presented as green. These
focused checks are not full-suite evidence. The implementation node did not
request full CI. QA requested it for `0add6558c`, where it failed on the two
repaired reds above; no full CI has been requested for this rework head, and QA
owns that next frozen-head request.

The scoped review of `b56a36939` found one new HIGH retention defect: an
acknowledged old founder question lost its durable ack when the payload row was
pruned, so the still-open authority projection could recreate the same revision
as unheard. Code head `4f3a4a609` fixes only that blocker by excluding rows with
a matching `voice_headphone_ack` from retention. Its regression test claims and
acknowledges an old authority item, runs retention, reprojects the identical
item, and proves the item remains suppressed. The test first failed because
retention returned one deletion, then passed with zero deletions and no replay.

Verification for this review revision:

- the focused inbox test passes 19/19, and the direct inbox/routes consumer set
  passes 21/21;
- Teamlead plus its 12 workspace dependencies build successfully;
- the required Teamlead `vitest related` expansion passed 570 files and 8,014
  tests with four skips, with one unrelated retry-route JSON failure under the
  aggregate run; that exact failing test passed immediately in isolation;
- repository lint exits zero, changed-file Biome passes, and `git diff --check`
  passes.

Consumer discovery used both changed files' full paths, filenames, and parent
directories, plus the emitted `headphone-inbox.js` import. Retained consumers
are `StateStore`, collector, question-authority, routes, the focused inbox
tests, and route tests. The FLY-2796 plan filename hit is documentation only;
the broad bridge and test-directory matches are unrelated basename/directory
collisions and were excluded. No new or changed
`scripts/__tests__/*.test.sh` exists in this revision.

The review's non-blocking findings remain explicit PR Follow-ups:
`headphone-retention-drops-unspoken`,
`headphone-background-always-on-cost`,
`headphone-ack-chunk-limit-hardcoded`, and
`comm-imports-voice-core-barrel`. They are not folded into this bounded HIGH
fix.

This implementation rework used no bot, ElevenLabs, tunnel, or production
credentials and did not open a Discord room. QA round 4 supplied the staged
shim/tunnel evidence and restored its temporary cloud-agent URL afterward.
QA's preceding evidence records the Gemini G1-d room PASS. Exact-head full CI,
the default-`all` real-rig retest, deployment, restart, QA dispatch, merge, and
ship remain separate authorized work. This file is the PR's literal-last
commit; the rework code review must bind its exact HEAD. If review requires
another code change, the branch must create a new literal-last milestone head
and obtain a fresh review.
