# FLY-2751 — Epic 固定页 Blob 预算收敛

**Status**: ⏳ Pending exact-head re-review and QA-owned CI
**PR**: #1274 (https://github.com/xrliAnnie/flywheel/pull/1274)
**Date**: 2026-09-21

Epic 固定页的 hosted 写入已收缩为唯一显式命令 `flywheel-comm epic-page publish` ：事件 refresher、residual scan、`generate`、`show`、`render` 和 `dependency show` 仍可重新物化或读页，但不调用 hosted publisher。路由只接受严格布尔 publish intent，显式发布每次重新物化最新 production 数据面并强制更新稳定链接。页面顶栏改为“按需刷新·页面快照截至”，status 暴露由权威 publication 加 14 天推导的 `expires_at`；200 条未发布 history 也不会再覆盖真实 `last_published`。

每次 mutable publish 的 audit 清理不再扫描 `r/` 前缀：上一版 HTML 已知的 current/previous hash 直接确定唯一可淘汰 audit，提交后 best-effort DEL，publish-path `list=0`。一小时 11 个代表性真实注册 reason 的 counting-client 回放证明自动路径 `put=0/list=0`；显式手动首发为 1 GET + 2 PUT，稳态最多再加 1 DEL，即自动事件预算 0 次/天、每次手动发布 3–4 次 Blob 操作。每日 retention sweep 的 store-wide LIST 独立保留，不在单次 fixed-page publish 路径内。

第一轮 effective code review 通过后指出 intake retry latch 只在 hosted publish 成功时清零，与本地 scan 成功应重置重试状态的约定不一致。修正已按 TDD 加入先红后绿的回归：非 intake 的成功本地 materialization 现在会重新 arm intake refresh。定向验证覆盖 flywheel-comm CLI、publisher/refresher/route、StateStore freshness/intake、renderer/model、audit/blob store、liveness/lead-note、plugin wiring、rules 与 hosting migration，共 20 个文件、308/308 测试通过；`pnpm typecheck` 在 24/25 workspace projects 通过，`pnpm -r build` 在 24/25 workspace projects 通过，`pnpm lint` 退出 0（仅仓库既有 25 条 warning）。push 前另外通过 `git diff --check`、25 个变更 TypeScript 文件的 Biome check（仅既有 warning）和 milestone layout 32/32 guard。

GitHub Actions run `35424179906` attempt 2 在旧精确头 `95feb7f79` 终态失败：teamlead 2/4 和 3/4 都因 FLY-2751 已同步的 ON/OFF patrol rule 字节未登记到 FLY-2567 兼容性收据而红，其余所有实质 job 成功（包括 Script Tests 3/5 容量闸）。根因修复只更新 reviewed compatibility manifest、legacy source receipt 和完整 bundle golden，不改发布行为；原失败套件先红后绿，随后 `lead-token-savings-launch`、`lead-token-savings-drift`、`lead-rules-bundle` 和 `fly2567-rule-budget` 合计 54/54 通过，`git diff --check` 通过。按 Lead 指令没有对修订 head 自行重跑 full CI；QA 负责冻结头的新 CI 请求。

修订头 `5a1be4295` 的 GitHub Actions run `35428465556` 只剩 Script Tests 3/5 红：`ci-job-elapsed-tripwire` 报 elapsed 1040s 超出 1020s 预算（FLY-1870 容量闸，日志明示“NOT flakiness”），其余全部实质 job 成功，包括先前修复的 teamlead 2/4 与 3/4。该分片容量问题已由 main 上的 FLY-2755 六分片重平衡解决，因此本次 resume 把 `origin/main` 合入分支两次（`e894b275c` 合入 `e816e5d49`，`f957f0398` 合入 `60f2fd04c`），没有请求 ship 或 verify-approval。第二次合并在 fly2567 兼容性 fixture 上真实冲突：FLY-2643 与 FLY-2751 各自登记了不同的 patrol rule 字节。解决方式不是选边，而是按合并后的真实字节重算：当前 patrol 源 sha256 `e23d5d7a…`，OFF 历史 bundle 用测试同一条 materialize 管道重算得 239088 字节、sha256 `70d5c3a0…`（= FLY-2643 的 238817 + 本单 271 字节增量），rationale/meaning 取两边并集，evolution 记录改为以 FLY-2643 bundle 为 before、合并结果为 after；OFF patrol 源与 runbook 哈希不变。合并后重建 flywheel-comm 与 flywheel-teamlead 及依赖、`pnpm --filter "...flywheel-teamlead" typecheck` 通过、`pnpm lint` 退出 0（仍仅 25 条既有 warning）；定向验证扩展到 55 个 teamlead 文件 821/821（含 FLY-2727 触碰的 founder-render、founder-view、rules、labels、residual 等重叠套件）、flywheel-comm epic-page/dependency 52/52、`vitest related` 对 9 个变更源文件牵出 609 文件 8633 通过/4 skipped、规则兼容 17 文件 151/151、milestone layout 32/32、`git diff --check` 干净（仅 progress.md 由 `flywheel-comm progress` 生成的尾随空格除外）。progress 账本已在本头之前同步为 review_pr=in_progress。本分支没有部署、使用生产凭据、dispatch QA、请求 ship 或合并。本文件是 PR 的 literal-last commit，后续只对该精确头做 effective code re-review、push 与 `needs_review` 交接。
