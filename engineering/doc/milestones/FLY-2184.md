# FLY-2184 — 收敛 529 driver 身份与 PR head authority

**Status**: ⏳ Pending ship
**PR**: #1008
**Date**: 2026-08-31

generalized 529 driver 不再手抄 Lead identity 到子进程 env：它改为消费
`lead-identity resolve --format env` 的 canonical 投影，并把 summary identity home、
lease DB 与 CommDB / projects registry 一起固定到当前 slot。四个 summary 身份变量因此与
房内真实身份一致，真 `authorizeLeadWrite` 对照测试同时锁住通过路径和删键后的
`identity_env_conflict` 拒绝路径。

stub 在 push 前先确认 durable / open PR authority，push 后以 5 秒 × 12 次有界轮询等待
GitHub `headRefOid` 收敛；driver step 8 复用同一分类与轮询逻辑，再把最终观察交回既有 A3
preflight。fresh branch 只 create 一次，known PR 路径不会因短暂空读重复建 PR。沙箱孤儿 PR
清理由 FLY-2164 负责，本 PR 不扩大范围。

## 验证与 review

- `node --test scripts/__tests__/qa-generalized-e2e-lib.test.mjs`：38/38 通过，包含真 dist CLI +
  `authorizeLeadWrite` 正反对照。
- `bash scripts/__tests__/test-deploy-generalized.test.sh`：全绿；相关 mjs `node --check`、shell
  `bash -n` 与 `git diff --check` 通过。
- `pnpm lint` 与 `pnpm -r build` 通过。
- 精确 `pnpm test:packages:run` 在本 resident sandbox 只剩两条真实 Terminal.app 测试因
  `osascript` 无法连接 GUI 失败，其余同批 core 219 条通过。headless 全包复跑避开 GUI 后，
  teamlead 在聚合负载下出现 7 条 timeout；对应 5 个文件单独复跑 62/62 通过。PR exact-head
  CI 继续提供非 sandbox 宿主判定。
- cross-family code review 在 `76dafe019` 上 **APPROVED**，无阻塞 finding。3 个 MEDIUM
  advisory（step 8 未记录 poll 元数据、单次 `gh` 异常按批准设计 fail-fast、两条身份失败残留
  路径仍可回落到 operator HOME）与 2 个 LOW advisory（已关闭 durable PR 的窄恢复场景、
  测试 CLI 路径依赖 cwd）已完整报告 Lead，未夹带扩 scope 修复。

## 诚实边界

本地 Codex 原生只读 review 因 resident runner 的嵌套 macOS sandbox 与强制
`approval_policy=never` 无法初始化；没有绕过策略或修改文件，硬 gate 由上述跨模型 review
完成。生产 runner pane → generalized 529 房的真实九步验收属于后续独立 QA 节点。
