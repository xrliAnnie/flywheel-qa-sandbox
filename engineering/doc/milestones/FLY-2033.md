# FLY-2033 — 会议产物与闭环

**Status**: ⏳ Pending ship
**PR**: #985
**Date**: 2026-08-29

实现 Lead-neutral 的会议 V5 每场自动建单与幂等对账：参会 Lead 来自每场 `leadId`，Raya/meeting runtime 只负责会议编排与原始证据，Product Lead route 承接独立会后 note taker。最终 `meeting-notes.html` 提交进仓并经现有 founder-review 卡投到 `[FLY-xx]` thread。固定 `meeting` label 仅在零会议证据的首次 bootstrap 自动创建，并以 canonical marker 锁定 identity；标签改名、删除或历史扫描异常一律 fail closed，避免重复开单。launchd 保持 hold，QA 必须在 ship 前用真实会议拿到 issue/thread/card/founder-feedback 全链证据，不能延后到激活。
