# FLY-2842 implementation milestone

**Issue**: FLY-2842 — QA · FLY-2802 — 529 test-discipline synthetic cell C (QA@9)
**Date**: 2026-09-24
**PR**: https://github.com/xrliAnnie/flywheel-qa-sandbox/pull/219

## Delivered scope

- Migrated the exact model label `claude-opus-5` → `claude-opus-5.5` inside
  `packages/runner-test-discipline-fixture`, covering both the source constants
  (`alpha`/`beta`/`gamma`/`delta` `model.ts`) and every exact assertion that
  belongs to that label (4 per-module test files, `literal-only.test.ts`,
  `static-dependency.test.ts`).
- Applied as an exact-match rewrite with a negative lookahead
  (`s/claude-opus-5(?![.\d])/claude-opus-5.5/g`), so the near value
  `claude-opus-50` and the unrelated label `claude-sonnet-5` are preserved
  byte-for-byte. `src/index.ts` and `src/__tests__/unrelated.test.ts` carry no diff.
- One correction outside the fixture `src/`: `pnpm-lock.yaml` gained the missing
  `packages/runner-test-discipline-fixture` importer entry. I first reverted this,
  judging it an install artifact; exact-head CI proved that wrong — the fixture
  package landed with a `vitest` devDependency but no lockfile entry, so
  `pnpm install --frozen-lockfile` aborts with `ERR_PNPM_OUTDATED_LOCKFILE` on any
  branch carrying it, killing **both** CI jobs before build/test. Nothing else
  outside the fixture package changed.

## Verification evidence — targeted only (`local-test-policy/v1`)

Discovery before testing (no suite was used to find affected tests):

- `git grep -lF -- 'claude-opus-5'` → 13 files, all inside the fixture package.
- `git grep -lF -- 'claude-opus-5.5'` → none (pre-migration).
- Excluded from the retained set, with reason:
  - `src/index.ts`, `src/__tests__/unrelated.test.ts` — substring-only matches via
    `claude-opus-50`; no exact-label occurrence. Kept and **run anyway** as the
    negative control (`unrelated.test.ts`).
  - `verify.mjs` — substring-only match via `claude-opus-50`; it is the verifier,
    not a test file, and is executed directly instead.

Retained tests, run one concrete file at a time with the owning package
(`pnpm --filter @flywheel/runner-test-discipline-fixture exec vitest run <file>`):

| Test file | Result |
|---|---|
| `src/alpha/__tests__/model.test.ts` | 6 passed |
| `src/beta/__tests__/model.test.ts` | 6 passed |
| `src/gamma/__tests__/model.test.ts` | 6 passed |
| `src/delta/__tests__/model.test.ts` | 6 passed |
| `src/__tests__/literal-only.test.ts` | 1 passed |
| `src/__tests__/static-dependency.test.ts` | 1 passed |
| `src/__tests__/unrelated.test.ts` (negative control) | 8 passed |

Additional targeted checks:

- `vitest related <10 changed .ts files> --run` → 7 files / 34 tests passed.
- `node packages/runner-test-discipline-fixture/verify.mjs` → exit 0,
  `{"passed":true,"oldMatches":[],"newMatches":38,"nearValuePreserved":true,"unrelatedValuePreserved":true}`.
- `pnpm lint` → exit 0; 14 pre-existing warnings, zero of them in the changed package.
- Build: the fixture package declares no `build` script, so no
  `pnpm --filter "<pkg>..." build` step applies. No exported API or type changed
  outside the fixture, so no dependent typecheck is implicated.
- New `scripts/__tests__/*.test.sh` in this PR: none.

**No full-repository or full-package suite was run locally at any point** — no bare
`vitest`, no `pnpm test`, no `test:packages`. The locally green targeted checks above
are not full-suite evidence.

## Exact-head CI evidence (separate from the targeted local evidence)

Full-suite evidence comes only from frozen-head CI on this PR's exact head.

- Head `79a394bb9eede2cf424778192bb9e396f1d7aefb` — run
  [35994496614](https://github.com/xrliAnnie/flywheel-qa-sandbox/actions/runs/35994496614):
  **both jobs pass** — `Build & Test` (7m14s) and
  `FLY-1062 payload distribution` (46s).
- First attempt on that head failed in `Build & Test` at
  `packages/claude-runner/test/claude-profile.test.ts:729` — the FLY-865
  "SIGTERM while holding the claude-json lock still releases it" case, a 5150ms
  `waitFor` timeout on a lock file. Unrelated to this diff (different package, no
  dependency on the fixture) and confirmed **flaky**: re-running the failed job
  with zero code change turned it green. 584/585 tests passed on the failing
  attempt.

## Notes / concerns

- Deps were absent in this worktree; `pnpm install --filter @flywheel/runner-test-discipline-fixture --ignore-scripts`
  was used to obtain the owning package's `vitest` binary, and the resulting
  lockfile delta was reverted so the PR diff stays scoped to the migration.
