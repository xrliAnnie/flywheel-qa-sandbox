# FLY-2444 — 任意仓起一个 Lead 最小闭环

**Status**: Review round 3 rework fixed; final exact-head gates, CI, and one replacement review pending
**PR**: https://github.com/xrliAnnie/flywheel/pull/1126
**Date**: 2026-09-08

## 交付内容

- 新增可安装的 `flywheel-lead` 入口，统一承载 `register`、`recover`、`preflight`、`run`、`install`、`stop` 与分阶段 `verify`，按 manifest 选择 Claude 或 Codex harness。
- 新增事务化 `flywheel-comm lead-registry add|recover|selector`：候选 `projects.json`、summary activation receipt 与外仓 manifest 通过锁、planned digest、nofollow 与 crash recovery 收敛，`--dry-run` 保证零写。
- Codex 路径使用独立 state/CODEX_HOME、full-access TUI、项目 root validator、host-tmux 只读 probe 与通用 launchd carrier；Claude 路径复用现有 wrapper-v2，并在缺受管 Discord 插件资产时 fail-loud 给出可执行提示。
- 把 launcher、host-gate helper、registry CLI/runtime 与 migration resolver 纳入 npm payload、首次 provision、bootstrap 与 checksum convergence；没有复制或改造 FLY-2388 / FLY-1835~1842 的发布流水线。
- 一页外仓 runbook 位于 `engineering/doc/FLY-2444-flywheel-lead-launcher/lead-in-any-repo.md`，覆盖安装、注册、启动、第一条 mailbox 消息验证、恢复、停止与手工注销边界。
- restart lifecycle 与 child-process census 以精确 argv / backend / profile / manifest 识别通用 Codex carrier；未知或漂移载体 fail-closed。

## TDD 与验证

- `flywheel-lead.test.sh`：29/29 PASS；其中新增 launchd 默认 `PATH=/usr/bin:/bin:/usr/sbin:/sbin` 红灯先稳定复现 `node is required`，再由 carrier 自有 user/Homebrew toolchain PATH 修至绿。`flywheel-lead-packaging.test.sh`：4/4 PASS；`package-onboard-smoke.test.sh`：15/15 PASS；`provision-prebuilt.test.sh`：6/6 PASS。
- 新增的其余 shell suites：host probe 3/3、generic carrier 10/10 PASS；修改过的 state parity、Codex args、host gate/census、package-onboard、packaged seams 与 converge suites 全绿。
- PR 首轮 exact-head Quick Gate 暴露 `ci-structure.test.sh` 的精确 shard 清单未同步新增 FLY-2444 step；以该红灯补齐唯一预期项后，CI structure 与 293 shell / 3 generalized Node 枚举均在本地转绿。
- 下一轮 exact-head Script Tests 2/5 暴露 FLY-519 的最小 repo fixture 未携带 provisioner 新增的强制 `lead-host-tmux-gate.sh` support lib；同步 fixture 闭包后，FLY-519 四命令整步本地转绿（sanitize 41/41、capture 11/11、provision 20/20、fleet secret scan PASS）。
- exact head `ce5170b61` 的 Script Tests 2/5 继续暴露 FLY-1389 / FLY-1577 convergence fixture 未携带新的 `flywheel-lead.sh` 与 `lead-host-tmux-gate.sh` 受管资产；补齐 fake repo、steady-state closure 与 PATH hygiene registry 后，FLY-1389 整个 CI step 的十个命令本地全绿。
- 同一 head 的 Script Tests 1/5 最终暴露 `codex-lead-state-root.test.sh` 仍变异已经迁移走的旧 `STATE_DIR=` call site；将 A/B mutant 对准生产 resolver 的 `state_root=` 唯一行后，该用例与 293 shell / 3 generalized Node 枚举均绿。
- FLY-2444 的 CI inventory 现在精确固定六个命令，并把既有 `codex-lead-args.test.sh`（12/12）与 `codex-lead-state-dir-parity.test.sh`（6/6）纳入 named step；完整 named step 29/29、4/4、3/3、10/10、12/12、6/6 全绿。
- exact head `5de77bd8e` 的 Script Tests 2/5 在本单 named step 暴露实际 registry transaction 后的只读 `verify-activation` 断言漏传 fixture HOME，因而在干净 CI home 报 `summary_granularity_unselected`；空 HOME 本地同形先稳定复现 28/29，再由 `df0601297` 仅给该断言补 `HOME="$H"` 修至 29/29，六命令整步在空 HOME 下全绿。旧 head 的其余 12 个 CI job 全绿，未以 rerun 掩盖该真失败。
- 按一页 runbook 重走真实 npm pack/install 的隔离外仓 smoke：第一次在进入产品路径前被本机 `~/.npm` 所有权错误拦截；未改用户目录，改用一次性 `NPM_CONFIG_CACHE` 后同一脚本 15/15 全绿，覆盖安装树、Bridge health、Claude/Codex preflight 与 Codex dry-run。真实频道、获批 Bridge 重启和 #10 仍保留给 QA/FLY-2442 边界，没有伪造成产品验收。
- `pnpm lint` exit 0（14 条仓库既存 warning、0 error）；`pnpm -r build` 的 22 个参与 workspace 全绿。
- `pnpm test:packages:run` 精确运行两次，均在 TeamLead 11,914 PASS / 6 SKIP 后因一个无 branch diff 的并发用例及 Vitest worker RPC timeout 退出 1：第一次为 FLY-945 reserved attribution 404/400，第二次为 `StructuredInboxRouter` pre-ready error teardown。前者目标用例独立 5/5 PASS；后者完整文件 19/19 PASS、目标用例另 5/5 PASS。两次红均未写成绿。
- `[lead-instruction a1e8188f-65fe-4c68-a2ca-79ab7eeaaecb]` 指定该本机全包并发不稳定性按 FLY-2406 如实披露、不无限重跑；受影响定向套件作为本地证明，最终放行证据取 PR exact-head CI。

## FLY-2442 解锁后的收口

FLY-2442 已由 PR #1124 合入 `main`（merge `eca5feb8a`）。本轮 fresh fetch 后将 `origin/main@ee113cab9` 合入分支，并只完成原先锁住的收口项：

- 通用 Codex launcher 绑定 `FLYWHEEL_CODEX_LEAD_OUTBOUND=bridge`、`FLYWHEEL_BRIDGE_URL`、`FLYWHEEL_API_TOKEN`；缺 token 时 fail-loud。
- 新增只读 `inspect-lead-outbound`，以精确 `delivery_id` 贯穿 journal member、completed entry、`${entry_id}:out` 本地 outbox、Bridge dedup sent receipt 与 `message_id`。
- `flywheel-lead verify --stage live` 的 #10 从 `SKIP` 变为真实 inspector 调用；成功输出 exact `delivery_id`、`entry_id`、`idempotency_key`、`message_id`。
- TDD 证据：launcher 29/29、inspector 7/7；六个断链负向 guard 与一个无关更晚 outbound row 均不能误过。

## 本轮验证边界

- `pnpm lint` exit 0（14 条既存 warning、0 error）；`pnpm -r build` 22 个 workspace 全绿。
- `pnpm test:packages:run` 如实为 RED：唯一 assertion failure 是零 branch diff 的 Claude runner 500ms timeout，串行文件 7/7 PASS。
- 为分离并发影响，Comm 与 TeamLead 完整套件另行并发执行；全部 assertions 分别 2,111 PASS / 2 SKIP 与 11,964 PASS / 6 SKIP，但两个进程同时发生 Vitest worker `onTaskUpdate` timeout，因此仍记录为红色 harness 尝试，不冒充全门绿。
- 新增和本轮相关的 shell suites 全绿：launcher 29/29、packaging 4/4、npm installed smoke 15/15、host probe 3/3、generic carrier 10/10、Codex args 12/12、state parity 6/6。
- Replacement review 的唯一 HIGH 先由污染 `.env` fixture 稳定复现为 launcher 28/29，再通过 generic Codex carrier 显式清除 roundtable/core/mention channel-scope env 修至 29/29；同轮 lint、22-workspace build、packaging 4/4、host probe 3/3、generic carrier 10/10、Codex args 12/12 与 state parity 6/6 全绿。
- Replacement 的 `pnpm test:packages:run` 如实为 RED：一次误过滤启动的 TeamLead 全套件与正式全包套件争用全局 ProofShot 锁，`visual-capture.test.ts` 47 格统一报 `ELOCK_TIMEOUT`；随后隔离完整文件时另一进程仍持锁，同样为红。该文件与本次两文件 shell diff 零交集，不以局部结果改写全门状态；replacement exact-head CI 仍是最终放行证据。
- 批准计划列出的 `scripts/__tests__/migrate-summary-registry-cli-resolve.test.sh` 在 branch/main 均不存在，直接执行 exit 127；当前 summary registry CLI/migration SSOT 定向套件 11/11 PASS。
- 最终放行只取本收口 frozen head 的新 exact-head CI，不复用旧 head `bf598a3f4` 的 14/14 green。

## Code review 与 QA handoff

旧实现曾在 `5de77bd8e` 获正式 review APPROVED；该 verdict 不冒充覆盖后续代码。`cfda01d2` 的 request-driven review 发现一个 HIGH：共享 `.env` 的 roundtable/core 频道变量会泄漏进已禁用 cross-dept 的 generic Codex runtime，并在配置解析时 crash-loop。`0dce3a79c` 只修该 blocking finding，并加污染输入与 child-env null 断言；MEDIUM/LOW findings 不扩修，留作 PR follow-up。本 milestone 是 replacement frozen head 的 literal last commit；push 后将在新 head 发起全新 request-driven review，review 运行期间不移动 head。动态 review 与 exact-head CI receipt 只写 PR / workflow handoff，避免修改本文导致 head 漂移。

QA 交接继续使用 Lead 指定的 `qa-criteria/FLY-2444.md`，并明确写：**本轮 #10 已从 SKIP 转为实测通过**。直接证据为 launcher 29/29 与 inspector 7/7，前序 #1–#9 与 QA claim 950 保持有效。

## QA packaged-symlink rework

- QA 证明 npm compat mirror 会让 `packages/teamlead` 成为指向安装包的 symlink；两个 CLI 的 lexical ESM main guard 因此不执行，造成 packaged #10 假红、project-root preflight 假绿。
- `4cf5c32c5` 仅把 `inspect-lead-outbound` 与 `preflight-codex-project-root` 的 argv 入口改为 `realpathSync` 后比较，并让真实 npm package smoke 在 compat mirror 建成后直接调用两条 symlink CLI。
- TDD 红灯为 package smoke 15 PASS / 2 FAIL（两条 CLI 均静默 `rc=0`）；绿灯为 17/17，focused CLI tests 14/14。QA harness 的 packaged inspector 返回完整成功 JSON（delivery `chat:qa-probe-lead:7503193750579970048`，Bridge message `1546986792266240010`），无效 packaged project root 返回 78。
- `pnpm lint` 与 `pnpm -r build` 全绿。本机默认并发完整套件两次出现无改动关联的随机超时；所有失败文件隔离转绿。单 workspace / 单 Vitest fork 的完整套件跑完 TeamLead 885 files、11,964 PASS / 6 SKIP、0 assertion failure，仍在最终 worker `onTaskUpdate` RPC 回报超时并退出 1，因此没有冒充本地全门绿。最终放行必须取本次新 head 的 exact-head CI。
- 本提交取代上文旧 frozen-head 描述，作为 rework 后的 literal last commit。推送后 head 不再移动，并重新发起 request-driven code review。

QA 后继必须重新执行 **packaged #10** 与 **packaged project-root preflight**；不得以 real source path、旧 head 或本实现节点的隔离结果代替安装态复验。

## Review round 3 / Lead round 4 rework

旧 frozen head `98825dbf9` 的 review round 3 返回一个 HIGH 与三个 MEDIUM。Lead 将本轮修复范围锁为这四项，并要求三个 LOW 只登记 follow-up：

- `3f0b41e01`：补齐第三个 packaged ESM CLI `validate-projects.js` 的 canonical main guard。真实 npm compat-symlink 红灯是非法名册 `rc=0, output=`；绿灯为 package smoke 18/18，其中 `②e/②f/②g` 分别覆盖 inspector、project-root preflight 与 projects validator。
- `06606dc0c`：`run` 与 `preflight/verify #4` 复用同一 Codex child-env 合成，preflight 用 `FLYWHEEL_LEAD_DRY_RUN=1` 经过真实 `codex-lead.sh` / `parseCodexLeadRuntimeConfig`；Raya 相对 `RAYA_METRICS_DIR` 红灯从错误通过修为退出 78，且 dry-run 不创建 state dir。
- `5db79b7b9`：production `register` 在 load/lock/write 前拒绝 `--projects-file`、`--receipt-file`、`--summary-config-home`（含 `--flag=value`），退出 64 并点名具体 flag。
- `0d6fa1c79`：向既有 project 追加 Lead 时，对调用方实际提供的 `projectRepo`、`generalChannel`、`memoryAllowedUsers` 做一致性比较；冲突以 `lead_registry_project_options_conflict` 和准确字段名 fail-loud，省略则继承。

四项完成后的定向证据为：launcher 31/31、packaging 4/4、host probe 3/3、generic carrier 10/10、Codex args 12/12、state parity 6/6、registry planner 11/11、真实 npm installed smoke 18/18。`pnpm lint` exit 0（14 条既存 warning、0 error），`pnpm -r build` 22 个参与 workspace全绿。`pnpm test:packages:run` 仍如实为 RED：唯一失败是 Comm `cli.test.ts` 的既有 `runner-stopped` 测试在全仓并发中超过 5 秒；同一完整文件单 worker 54/54 PASS，目标用例 453ms，不用该隔离绿改写全门状态。

三个 LOW follow-up（本单不修）：

1. launchd exec spec 的空格分词与 plist XML escaping；
2. install/stop 对 `python3` 的未声明依赖；
3. legacy Codex state-dir 的 shell `-d` 与 Bridge `existsSync` 判据差异。

冻结前 fresh fetch 发现 main 已推进；按 Lead 明确裁定，本分支 merge `origin/main@d7b75c755`，不 rebase、不 force-push，merged-head `ci-structure.test.sh` 已 PASS。本 milestone 是 round 4 的 literal last commit；其后只运行 exact-head gates、正常 push 一次、等待 exact-head CI 与一个全新 request-driven code review，不再移动 head。

QA 后继必须重新执行 **packaged #10** 与 **packaged project-root preflight**，并补验 packaged `validate-projects.js` 的非法名册负向 guard；所有三项都必须走 npm 安装后的 compat symlink，不能以 source-tree real path 或旧 head 代替。`verify #4` 还应以真实 runtime-parser 拒绝条件证明 fail-closed。

本节点没有修改 `CLAUDE.md`，没有 dispatch QA、请求 ship、merge、deploy、改生产 registry、重启 Bridge/Lead 服务、安装生产 plist，亦没有冒充 Raya 真频道验收；生产迁移仍由 FLY-2445 的独立部署窗口承担。
