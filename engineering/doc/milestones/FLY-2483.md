# FLY-2483 — 现在要你看与讨论串跳转

**Status**: 实现完成，最终 HEAD 代码评审与 CI 待完成；尚未交接 QA 或上线。
**Date**: 2026-09-10
**PR**: https://github.com/xrliAnnie/flywheel/pull/1147

## 交付

三路 attention 独立于 Epic 合并当前 founder 门、项目未答问题和 Linear founder-review 非终态单。固定四段展示，缺失明确说明，空态明确显示；单独 declared_blocked 不进入。保留问题身份、来源和角色，未知身份不猜测。

discord_config 实例单行持久化 guild；真实 chat_threads 经过同项目、别名、权威频道与冲突验证后生成链接，缺 guild/thread 时禁用。严格 v2 Cell、来源健康度和实际 JSON/HTML 字节预算覆盖失败与截断；原有 Epic 成功路径不改。

## 验证与限制

`pnpm lint` 与 `pnpm -r build` exit 0；相关 25 文件 / 376 项通过，CLI 真实 materializer HTTP seam 42 项通过。没有新增 shell suite。

本地完整包门如实为失败：默认两次在既有 FLY-1715 CLI 用例 5000ms 超时，隔离重跑 474ms 通过。包并发 1 / worker 2 时 CommDB 2209 项通过，runner 1224 项通过，但 `Error: [vitest-worker]: Timeout calling "onTaskUpdate"` 导致 exit 1。没有改相关测试、断言、超时。后续单 worker 诊断没有绿色回执；停止信号受 sandbox 权限限制，不能宣称已终止。

Lead ruling `874b4bd7-118e-4bf6-8798-71a29add1cdc` 允许保留上述失败开 PR；必须在最终 HEAD 获 Bridge review APPROVED 和 CI 14/14 后才能 needs_review，不能声称本地完整门绿。

本地没有截图或视频：proofshot、隔离 Chrome 和 CDP 尝试失败。Lead ruling `3ccc5ece-c938-4b62-b1ff-d1854ade207d` 允许带缺口交接；QA 必须真实点击生成的 thread_url，截图证明进入对应讨论串。markup 测试和 fixture 地址不替代此证据。

codex:rescue 包装器初始化被 sandbox_apply 拒绝，helper exit 71，没有评审结论。Lead ruling `051cf112-4d7b-44bf-b7aa-e4b3c67c5a41` 指定本环境使用最终 HEAD 的 Bridge 跨模型 request-review；不重复 rescue 尝试。

## 回退与交接

回退代码保留增量配置表与历史回执，旧程序可忽略新表。来源读取不是全局原子快照；配置撤销到下一次页面刷新才体现；标签等待起点未知时显示不知道。

批准 plan.md 保持固定，实施证据见 `engineering/doc/FLY-2483-attention-thread-links/implementation-report.md`。本文件是 literal last commit；正常推送一次后冻结整个 HEAD，评审与 CI 状态通过 PR / comm 更新。本节点不请求 ship、不调度 QA、不合并或部署。

## 首轮 CI 修复后的替换交接

旧 HEAD a57d8c714 的正式评审 APPROVED，零 HIGH；五项 MEDIUM/LOW 建议已报告 Lead 并保留 follow-up，见实施报告。该 HEAD 的 CI 11 项通过、Quick Gate/TeamLead 第 1 分片及汇总门失败，不能交接。

832f4dd91 按 Lead 授权关闭三个实际遗漏：问题域内导出 pending attention 状态，页面不消费原始 relay_state；liveness E2E 提供明确 attention/current generator；discord_config 纳入 protectedCurrentOrReference 并同步精确 schema fixture。没有放宽 guard、删除规则、接口必填或超时。

本地 RED→GREEN：问题域新断言 1 fail→8 pass；旧 E2E/retention 5 fail→相关三文件 46 pass；原始 residue gate exit1→exit0，guard 负向测试 4 pass。替换版本 lint/build exit0。本地完整包门仍无绿色回执，按 Lead 裁定不追逐主机负载重跑；浏览器与 rescue 缺口仍按前述裁定披露。

本次提交取代上文旧冻结描述，是替换版本的 literal last commit。获准一次正常替换推送后重新冻结整个 HEAD，必须新 request-review APPROVED 与同 HEAD CI 14/14 后才执行 needs_review，再 park；旧评审不冒充覆盖新代码。

## 主线冲突返工后的替换交接

Implement attempt 2 按 Lead 48681a1c 指令以单个 merge commit 00f4f9ae5 合入 origin/main 977660ab3（含 #1151，尚无 #1148）。attention 仍独立页首展开，四段与 Discord href 保留；新折叠卡、审计 sidecar、scope.v2 及计数保留。修复无 scope 与新 roots 校验交叉点，并更新新卡 DOM 与真实 attention 组合预算测试。批准计划原字节未变。

本轮实测：TeamLead 33 文件 512 项、Comm 4 文件 66 项通过；lint/build exit 0。真实 attention + 60 子单 + 68 个合成预留 Lead 格子为 342203 bytes。相对 main 的 diff check 通过；合入 main 的既有文档 EOF 空行未另作修改。没有新增 shell suite。本地完整包门保留此前失败记录，本轮依返工令不重跑；新布局 390px/1440px 与真实 Discord 点击证据由 QA 重取，当前未测。

本文件为本轮 literal last commit。一次普通 push 后冻结新头，等待新 Bridge review 与 CI；完成走 needs_review PR 1147。最新 Lead 指令不自行 park，不 ship、不合并 PR、不部署。

## 第三次 implement 主线返工交接

单个 merge commit 7acfbc700 合入 origin/main 92532e22a（含 #1148、#1149、#1151）。保留页首独立默认展开的 attention、四段与真实 href，保留 Lead 注释折叠卡头展示及衰减。无 scope 注释 freshness 空指针经 RED→GREEN 修复；两侧配置表均列入保护，精确 schema 计数同步；批准计划字节不变。

实测：TeamLead 41 文件 589 项、Comm 4 文件 66 项通过；pnpm lint 与 pnpm -r build 通过。组合 fixture 为 60 子单、3 条 attention、76 处真实 280 字 Lead 注释，443109 HTML bytes；8 个卡头注释均可见。没有新增 shell suite。首次运行的旧 dist 与并发超时已在构建后两 worker 重跑通过，未改超时/断言。本地完整包门保留原失败记录，本轮依定向返工令不重跑。

新头 Bridge review、CI 待推送后验证；真实 Discord 点击和浏览器布局仍待 QA，不能据 DOM 声称通过。相对 main diff check 通过；主线带入的历史证据空白未修改。实现报告已记录本轮证据。

本文件为本轮 literal last commit。按 cb97301f-a163-4995-89f7-626b95161ddd 普通 push 一次后冻结头，完成走 needs_review PR 1147，不自行 park、不合并 PR、不部署。

## HIGH 修复替换交接

旧冻结头 8dd6bbbe2 CI14/14 通过，正式评审发现一个 HIGH：protected 非 founder checkpoint 未答问题被静默丢弃。按 Lead fec5f6c2 裁定，9eeb05ffc 仅修此项；普通问题在投递保护前后仍可见，已答即退出，founder 类型继续严格 allowlist。其余 advisories 不改，批准计划不改。

实测 RED4→GREEN：Comm 4 文件69项、TeamLead attention 3文件71项通过，lint/build通过。无新增 shell suite；完整包门既有失败与 QA 真实浏览器缺口保持披露。替换头新评审与CI待完成，不引用旧头结果作为新头批准。

此为 literal last commit；一次普通 push 后冻结，用同 request-id dcd8c6c0-180e-4dcc-a340-e820c47e2f22 重登记。最多再一轮，若有任何 HIGH，原地带 findingKey 问 Lead。APPROVED + 新头CI14/14后 needs_review PR1147；不 park、不合并PR、不部署。
