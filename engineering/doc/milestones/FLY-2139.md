# FLY-2139 — Bridge 定期清理与热查询索引审计

**Status**: ⏳ Pending ship
**PR**: #986
**Date**: 2026-08-29

把 Bridge 的文件、SQLite 与热查询维护收敛成一套可长期运行的机制：janitor 每次执行先生成全量 dry-run receipt，再对已证明终结且过期的 gate marker、runner state、备份、session/release/log、死 tmux socket 与遗留 loose gate-archive JSON 做有界归档或清理；数据库 retention 采用 inventory → activation → sealed batch，未激活的 inventory 也按周节流，cycle 只生成一份 evidence 且始终只保留最近两份。registry 新增 `patrol_orphan_watch` 并用真实 `StateStore`/`MailboxQueue` 当前 schema 常驻对账；健康探针默认端口纠正为生产 `9876`。mailbox retention anti-join 补 `ref_id`/`superseded_by` partial index，同规模 66,272 行从 QA 的 510s 降到 74ms；连续 receipt 还会把候选积压增量与 apply 排水率写入 summary/founder HTML，并仅在连续两周期 `mint > drain` 时告警。restart window 对 `busy=1 && log==checkpointed` 记录 `checkpointed_not_truncated` 后继续 VACUUM，不再误升 deploy severe；真正未落盘页仍 fail closed。全程不改变业务 status 或读路径语义，也保留既有 66,272 mailbox 真跑通、6,982 gate-marker 精确归档与未激活零删除证据。
