# FLY-2163 — 隔离 slot Bridge 状态根目录

**Status**: ⏳ Pending ship
**PR**: #997
**Date**: 2026-08-30

为 generalized 529 测试房的 Bridge 补齐最后一条租户隔离环境变量：以 later-wins 方式注入
`FLYWHEEL_STATE_DIR=${SLOT_DIR}`，使遵循 Flywheel state-root 合同的 meta-alert、Bridge marker
等写入只落到对应 slot，不再继承生产目录。

回归测试执行真实 `scripts/meta-alert.sh`，确认 marker 落在 slot 根目录下，同时对伪生产状态树
做写前写后字节校验并证明零变化；另用真实 Bridge marker resolver 证明根目录语义正确，不产生
`state/state` 双重嵌套。实现仅新增一行生产配置，跨模型代码审查 APPROVED；lint、全仓 build
和 generalized helper suite 通过。HOME 派生的 alert queue/dead-letter/claims 与独立 alert-smoke
harness 不在本单锁定范围内，未宣称已隔离。
