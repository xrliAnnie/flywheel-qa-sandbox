# FLY-2597 — Thread 要你答与 Epic 同源待看
Issue: FLY-2597 (https://linear.app/geoforge3d/issue/FLY-2597)
日期: 2026-09-16
基于: ../FLY-2597-thread-title-needs-you/implementation.md

**Status**: Lead followup repaired; pending new exact-head code review and CI

新增机器管理的 `🔔要你答`：显式 Lead 提问、转交 Runner 问题及 founder-bound 非 ship gate 驱动现有 title refresher。founder 回复或问题回答消除提醒；`待批` 保持优先。Epic 固定页从相同事实和 lifecycle/hold 投影只列有效 thread 链接，无 session 的 Epic 也保留持久重试与清除上下文。

Epic intake complete/superseded 采用服务端 Bridge receipt，纯记录不发 Discord；needs_founder 保留实际问题证据。两套 Lead 规则同步，停用周期手写进展，summary-inflow 不变。

测试、review 与 CI 的精确范围见 [verification.md](../FLY-2597-thread-title-needs-you/verification.md)。回滚工具默认只读，需显式 inventory；仅移除 owned prefix。真机 Discord title 变化、founder 真人回帖与固定页发布证据未执行，由 QA 在专属台架验证。尚未 merge/deploy/restart。

R1 修复：真实 StateStore fixture 证明中间 review holder 被权威层排除；缺链接或 visibility unknown 明确显示清单不完整；项目内 UUID/identifier 共用 title 与 reply 水位。按 Lead 指令普通 merge main，合入双方语义，无 rebase。合入后 lint/build、155 项同源回归、retention 8 项、shard 9 项与 CI structure 通过。完整包测试失败 receipt 保留，独立失败用例复查通过不能替代 aggregate；依 Lead 指令不重跑完整/性能测试，以这一轮唯一推送 head 的 CI 与有效 review 作为后续依据。详情及失败数见验证记录。

Lead 补充核查（1504c4de）：自动刷新原来漏接 child thread reader / scope snapshot，现已与手动路由一致；Markdown 子任务原来不显示 thread URL，现按严格 Discord URL 校验输出。HTML 原已排除 Lead-recipient 问题，但 Markdown 原遍历全部 attention；现两者同用 founder 投影，34 条 Lead 问题测试确认不再进入 founder 区。原 head 25506459c 的 review APPROVED 与完整 CI success 已归档，此补修 head 须新 review/CI。接线3、页面60、最终渲染90项测试分别通过（有重叠不累加），lint/build通过；未改变 title 轮转/授权语义，未运行生产发布或真机 QA。
