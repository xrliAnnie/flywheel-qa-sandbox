# FLY-1945 — 巡检状态证据与机制缺陷完成门

**Status**: Code review APPROVED; pending documentation-head CI / QA / ship
**Date**: 2026-09-14

巡检以 exact execution/activation/TURN/repo 身份记录语义状态和远端 head，撤销由终端底栏冻结导致的 STALLED 假报。原日期 %7/#891 与 %8/#892 回放为观察窗口内 ACTIVE。不可读证据显式 UNKNOWN；有期限的 long_task 保持合法等待，完整 owner inventory 清理退役 sidecar 条目。

机制声明必须一一对应 existing/created/no_issue 去向和 JSON 证据，Linear 查重/回读仍由 Lead 执行；旧 Bridge、pane 和 DWELL 门保留。UNKNOWN 与其他 finding 共存，源码/托管链接/payload launcher 均验证。

170 项定向测试、41 项快照聚焦测试、最终规则 59 项、lint/build/launcher 已通过。完整快照、package gate、有效代码评审和 PR 精确头 CI 的最终回执仍待收齐，见 FLY-1945-patrol-evidence-gate 实施证据。本阶段不声明 QA 接受、生产验证、部署或合并。

**PR**: #1198（非 draft）。首轮 CI 的固定 shell 测试清单漏项已补齐，CI structure 及 shell/Node 枚举先红后绿；新头仍需完整 CI 和有效评审。

完整快照 392 项通过。第二轮 CI 的 unit 全部分片、Quick Gate 和 payload 已通过；两个 shell job 的 launcher fixture/只读 TURN 例外清单漏项已修正，对应 22、7、79 项回归转绿。本次最后提交保持里程碑文件，最终头 CI 和有效代码评审仍待回执。

Lead 容量指令 `87babdab-fcc9-4169-870a-828818781d16`：本地全量包门已停止，记录为 **interrupted — not relied on**，不再重启；交接测试以最终精确头 CI 为准。


第三轮有效代码审查已通过：request `123d984f-4380-4da3-b9d2-f47bb79f46ef`、gate `caab0840-f3c2-495e-bf0b-c58f9e6c28e2`，reviewed head `2fccdc6cd5d2440de374f17647c7a9b24f58157e`；该头 CI 15/15 成功（run 34910049178）。Lead 指令 `27f0dea8-6198-4126-a68e-f6d032464fd6` 要求五项 MEDIUM/LOW 仅记 follow-ups，不重新开审。此后仅更新 follow-ups、progress 和本 milestone；本提交保持最后一条，等待文档新头 CI 通过后交接 needs_review。QA 与生产验证仍由后续流程负责。
