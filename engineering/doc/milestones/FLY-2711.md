# FLY-2711 — 语音载体准入补齐

**Status**: ⏳ Implemented; pending QA and ship
**PR**: https://github.com/xrliAnnie/flywheel/pull/1318
**Date**: 2026-09-24

Claude Discord plugin 0.0.8 在分叉 PR [#29](https://github.com/xrliAnnie/claude-plugins-official/pull/29) 提交，冻结 head 为 `5865b2d813711df2a0ff477c6e41659b92235a7b`；它保留固定 bot 身份以接收 Discord resume replay，连接态只控制语音 probe readiness，并用 macOS 内核 owner lock 安全回收崩溃残留与维持 `voice-self-filter.sock`。该 PR supersedes #28，implement 节点未合入，因此 merge SHA 尚无。

主仓 PR #1318 只新增 Claude newline 正向 probe、跨仓 HMAC golden vectors、合同/运行手册和实现证据；`packages/*/src/**` 与 `scripts/**` 的生产代码零变更，原 FLY-2712 的 Codex `/tmp` ↔ `/private/tmp` 修复保持不变。定向 Vitest 与 `vitest related` 均为 36/36，受影响包及依赖构建通过，仓库 lint exit 0；分叉仓 Bun 测试 257/257，macOS 真锁子集 19/19，主仓 scoped CI 与分叉 CI 均绿。

QA 收据路径：pending，由独立 QA 节点在冻结两仓 head 后写入并回填交付记录。必须以 claude-code test slot 证明 `start` 不再 503 且会话到 `live`，同时保留 Codex 路径回归；本 implement 节点没有安装生产插件、重启 Lead、起房、合并或部署。
