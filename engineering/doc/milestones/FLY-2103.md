# FLY-2103 — 项目配置 flag 迁入 scoped SQLite

**Status**: ⏳ Pending ship
**PR**: #987
**Date**: 2026-08-29

退役 `config.yaml` 中 9 类 project flag：checkpoint 声明直接启用、Xiaohongshu collection 固化自动创建，
其余 7 类 flag 改由 `flag_values` 的项目行解析。新增幂等的 dry-run/apply 分阶段迁移、G1 receipt 与
post-deploy 校验，`ConfigLoader` 对残留旧 key fail-loud；同时清理 6 个项目配置及 Flywheel 内建生成器。
真 Bridge 对 6 个项目的迁移前后解析结果保持一致。

QA attempt 1 返工把迁移真源改为每个项目的 committed upstream：G1 receipt 绑定 ref、commit、blob 与
content SHA，工作树的任一迁移 key 与提交态不一致即在写库前逐项报错。真实 dry-run 已在
GeoForge3D 的 `doc_flow.enabled` 分歧处按预期阻断，未替外部仓决定脏树去留。Lead 规则包的四处旧
`config.yaml` 指引已改为 project-scoped `feature-flags set`，项目配置拒绝也接入既有 founder
meta-alert 通路；残留的 DAG 补救话术同步改为 flag-store 口径。

全仓 lint 与 22 包 build 通过；定向 116 tests 通过，teamlead 全量 9,684 tests 通过，18 条并发/宿主
失败在单 worker 复跑后仅剩 1 条真实 tmux socket 宿主失败。fresh Codex code review R6 在
`c0a28354f` 上 APPROVED（gate `596833b1-5ee9-4b85-b148-a809928d232e`）。
