# FLY-2103 — 项目配置 flag 迁入 scoped SQLite

**Status**: ⏳ Pending ship
**PR**: #987
**Date**: 2026-08-29

退役 `config.yaml` 中 9 类 project flag：checkpoint 声明直接启用、Xiaohongshu collection 固化自动创建，
其余 7 类 flag 改由 `flag_values` 的项目行解析。新增幂等的 dry-run/apply 分阶段迁移、G1 receipt 与
post-deploy 校验，`ConfigLoader` 对残留旧 key fail-loud；同时清理 6 个项目配置及 Flywheel 内建生成器。
真 Bridge 对 6 个项目的迁移前后解析结果保持一致，Codex code review R5 已批准。
