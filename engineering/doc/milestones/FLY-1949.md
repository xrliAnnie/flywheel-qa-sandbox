# FLY-1949 — 可选评审并发限额

**Status**: ⏳ Pending ship
**PR**: 本分支的 FLY-1949 PR（创建时绑定）
**Date**: 2026-09-10

保持默认无限跨 execution 并发，按 Lead 裁定新增可选环境变量限额；构造时读取，不做热更新。FIFO 等待不丢弃持久化请求，保留同 execution 串行及原有 gate fail-close。代码评审 R1 APPROVED；140 个 coordinator 测试、lint、build 通过。本机 aggregate 有失败，详见本单 verification.md；最终 PR HEAD 的 CI 和后续 QA 独立验收。
