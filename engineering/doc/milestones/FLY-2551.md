# FLY-2551 — Founder-bound Xiaohongshu write receipts

Date: 2026-09-16
Issue: https://linear.app/geoforge3d/issue/FLY-2551
Status: PR #1214 ready; renewed code review and exact-head CI pending.

Adds dedicated-authority content/account/time-bound founder receipts, one-use
execution and provider replay guards, controlled media and private transport,
and an independent root fixture signer with a validated public-input offline
artifact builder. Production activation remains fail closed; real host probes
and the host-activation proof protocol require the separate Lead-owned design
follow-up documented in FLY-2551-xhs-founder-gate/follow-ups.md.

PR #1191 is merged; the one origin/main merge is complete and PR #1214 targets
main. A follow-up rename fixes the Quick Gate collision with the retired
receipt API denylist without changing authorization behavior. Residue checks
passed (5 tests, 2757 scanned files); affected XHS tests passed 50/50.
CI portability repair changes test fixtures and the dedicated-authority process
census only: 15 affected files passed 52 tests across bounded runs. Auto-narrow
36/36 passed unchanged; its CI mutant failures were expected (3/3 killed).
Prior root review passed with 15 advisories; this new head requires a fresh
review and exact-head CI, then serial provider review. Rescue companion was
unavailable due to sandbox helper exit 71; this is not a review pass.
Provider PR #9 remains at 6cd94d3. Both PRs must be declared during structured
implementation completion; the new root head requires fresh review and CI.

Validation: focused tests, standalone typecheck, full lint (exit0,18 warnings),
full recursive build and inherited new shell test passed. Host package aggregate
was interrupted under contention and is not relied on; exact-head CI remains
the aggregate of record. No root installation, real account action, host
acceptance, activation, QA dispatch, merge or deployment has occurred.

Replacement execution progress is recorded at implement 5/7; implementation
handoff remains pending both formal reviews and the final root CI result.

Latest bounded CI repair replaces two nonnumeric ship-card fixture references
with the existing numeric SHIP_CARD constant; all64 founder-reply tests pass.
No assertion or production semantic change. Prior root review process failed
nonzero_exit per Lead; a fresh review and CI must bind the new head.
