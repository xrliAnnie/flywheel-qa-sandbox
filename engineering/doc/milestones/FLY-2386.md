# FLY-2386 — Alerts 值守三件收口

**Status**: ⏳ Implementation complete; exact-head review and PR CI pending
**PR**: pending creation
**Date**: 2026-09-06

## 交付

- 两条告警车道现在共享一套可持久化终态：thread ticket 与 Lead mailbox ledger 都能从
  `unreviewed` 进入 `in_duty`、`handed_off` 或 `resolved_in_window`；统一 board 保留全部未解决项，
  同时展示回填债务与转交信的实际投递状态。
- 非点名 infra 告警优先进入 Claw 值守车道；只有 PRD 锁定的 direct-owner kind、明确转交信与
  token 未配置时的 fail-open fallback 才进 Lead inbox。Bridge 启动日志、seat 探针与 provisioning
  状态都显式报告 write path 是否 configured。
- `oncall-draft` 与 `alert-ticket` 提供 crash-safe 回填闭环：`no_entry` 转交先写 contact-book owed
  receipt；解决前必须生成、读取并绑定与告警身份完全一致的 runbook draft；harvest 只消费受校验的
  pending receipt，并留下 landed receipt。路径、frontmatter、目标 marker、重复与冲突都 fail closed。
- `.lead/claude-infra-bot-lead/identity.md`、`doc/oncall/README.md` 与
  `doc/architecture/infra-alerts-spec.md` 已写明值守动作、两车道语义、通用写法和 supersession，未写死
  本机路径、账号或 secret。

## PRD 验收映射

- R3/R4：每次 `no_entry` 与每次 resolve 都产生可追踪 receipt；draft 经过 harvest 才进入两本册子，
  所以“兜底一次，回填一次”不再依赖口头记忆。
- R6：board 的四档状态、未解决全量列表、handoff delivery settlement 与 ledger write-error 证据让
  每条告警要么已解决，要么仍然可见且有责任终点。
- R7：routing 在投信前完成 direct-owner / duty-reroute / duty-fallback 分档；未被点名的 Lead 不再
  收普通值守信，被点名或接到显式转交的 Lead 仍进入 durable inbox。

## TDD 与验证

- 全部行为按 RED → GREEN 落地：mailbox ledger、recipient tiering、两车道 board、receipt store、
  Bridge wiring、CLI resolve、真实 runtime recipient identity 与角色/文档哨兵均先有失败断言再实现。
- `pnpm lint` PASS（仅仓库既有 warning/info）；`pnpm -r build` PASS；`git diff --check` PASS。
- Core 按批准计划排除本机真实 Terminal.app 用例后为 19 files / 219 tests PASS：
  `pnpm --filter ./packages/core exec vitest run --exclude '**/tmux-viewer.macos.test.ts'`。
- Alert duty shell gates：identity sentinel 1/1、provisioning 6/6、apply gate 8/8 PASS；根目录
  `check-flag-truth` 3/3、retention consumer tests 4/4 与 live consumer audit PASS。
- Teamlead aggregate 为 11,460 PASS / 6 SKIP，三个 unchanged test 在宿主高负载下超时或进程失败；
  随后分别隔离复跑为 profile CLI 11/11、bounded maintenance 9/9、runs registration 1/1 PASS。
- Config 的唯一 aggregate timeout 隔离复跑 11/11 PASS。`flywheel-comm` aggregate 的四个 unchanged
  失败中，CLI 文件已隔离 54/54 PASS，archive 文件隔离 17/17 PASS；目标 alert-ticket 文件 18/18 PASS。
- Claude runner 的重型 profile 文件完成 126/126 assertions；Vitest 仅在文件结束后报告 worker
  `onTaskUpdate` RPC timeout 并返回非零，未出现 assertion failure。其余 scoped packages 均通过，
  包括 Edge Worker 1,307 tests 与 Voice Bridge 649 tests。PR exact-head CI 仍是 aggregate gate 的最终裁决。

## Review 与边界

- 受保护的 `codex:rescue` companion 已用 read-only `task --fresh` 入口实际调用，但在读取仓库前因
  `AGENTS.md` session 初始化 broken pipe 退出；没有调用 raw `codex exec`，也没有把工具失败冒充 verdict。
- 本 commit 冻结全部验证文字与 milestone。随后新开的 request-driven `review_code` gate 必须绑定本
  exact HEAD；其结构化 verdict 与 findings 是权威审查记录。任何 blocking finding 都先修复、复验，
  再以 milestone-only commit 重新冻结并开启新 review round。
- 本实现未修改批准 plan 或 `CLAUDE.md`，未配置生产 token，未执行 529 房真人验收，未 dispatch QA、
  请求 ship approval、merge、deploy 或重启 Bridge/Lead 服务；这些仍由后续 DAG/部署节点承担。
