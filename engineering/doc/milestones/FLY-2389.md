# FLY-2389 — 私有 payload 下载与保留期

**Status**: Implementation prepared; exact-head review / CI / independent QA pending
**Date**: 2026-09-10
**PR**: Created from branch `flywheel-FLY-2389` after this final milestone commit.

## 交付

Worker 以 entitlement 可见集、完整 manifest 校验和对象 HEAD 校验签发最多 60 秒的 SigV4 GET。签名起点在鉴权 I/O 前，历史 URL 不能越过 14/28 天截止；current 不按年龄失效。壳不将对象端 403 误判为 key 失效，也不回显底层网络异常。

license key 使用条件创建，精确重放零写、授权不可变更、吊销不可复活；rotation 部分失败提供单独撤销恢复路径。上传完成后的检查保留并发提交的 current，物理删除只经 tombstone barrier。cleanup-only 能力与每小时独立工作流只执行 expire/tombstone/delete；未激活明确 skip，不能作为成功证据。

激活先校验私有入口、bucket/凭据身份和完整原生 lifecycle，再经 stdin 注入 signer/cleanup secret；原生规则仅中止 7 天未完成 multipart。合同、runbook、实施记录与 QA 验收矩阵同步，pinned plan 未改动。

## 验证

- 端点 91/91，key-cleanup 21/21，workflow structure 23/23，B1 pipeline 42/42，promote-controls 4/4，customer E2E 8/8，contract consistency 五套件通过；Worker dry-run/metafile 验证真实签名库入包。
- 补充同链真实 B1 client reserve/upload/readback/prepare/commit→302→真实壳 hash 验证，双 origin 2/2；beta 14 天与 customer 28 天精确截止、当前版本一年保留、撤销/隔离不再 mint 均有执行断言。
- lint exit 0（14 warnings）；补齐已声明依赖后完整 build exit 0。两次完整 packages exit 1：R1 comm 四个既有 CLI timeout + onTaskUpdate（失败文件独立 62/62）；R2 comm 完整 2190 passed、claude-runner 1224 passed 但 onTaskUpdate timeout。余六包补跑五包通过，teamlead 12452 passed / 7 skipped 但同样 RPC timeout，exit 1。无本地 full-green；Lead question 7272fc03-c4ad-481f-9f4a-0a651af6cd1e 确认保留失败收据，以最终 HEAD CI 14/14 为完整门禁。
- codex:rescue 在 thread 创建前因 sandbox_apply Operation not permitted（helper exit 71）失败，无 rescue verdict；Lead question 41996fff-2eed-44cb-8677-27e7bab16218 明确以 Bridge exact-head registered request-review 为当前 runtime 代码门禁。该最终 HEAD 的评审/CI 结果登记在 PR 与外部报告，不再追加进度提交。

## 验收边界

未执行真实 Cloudflare/R2 写入、激活/发布 workflow、QA dispatch、merge 或 deploy。真实 R2 私有性、签名只读范围、篡改/方法/到期拒绝、cache header override 与 B1 联合发布留独立 QA。完整 validator 随历史增长的预算测量留后续，不增加另一索引或存储。回退不能复活 key 或撤销 tombstone；stream 回退须保留 current/key 安全修复。
