# FLY-2753 本机定向验证
Issue: FLY-2753 (https://linear.app/geoforge3d/issue/FLY-2753/守则吞吐-实现qa-守则要求每具-runner-交卷前本机跑全量-pnpm-testpackagesrun-十几具同时跑每具-1-2)
日期: 2026-09-20
基于: ../FLY-2753-targeted-local-tests/implementation.md

当前 sandbox 生效的 engineer、QA、general 守则改为本机 lint、受影响包构建/类型检查、所属包及直接消费者定向测试。全量只接受 exact-head CI 的完整证据；QA 见任一当前 head job 红灯必须 FAIL。QA parallel helper 的全量入口推荐已删除，其余角色职责保留。

本机证据：前轮合同 RED→GREEN；本轮重新运行合同（4 份 prompt、7 个变异负例）、全仓 lint（1899 文件、14 个既有警告）、shell 语法、三份条款一致性及 diff 检查，均通过；设计 HTML 脚本 14 项检查通过。57 次消费者搜索与 107 个匹配的逐文件处置记录在 test-selection.md。无编译资产变更，build/typecheck N/A；未运行本机全量包套件。

重新派发执行 `72841877-9325-48d7-b508-3ceb1ed9b635` 从既有 PR #206 复核获批实现，本轮未改行为。指定 sentinel 已实际执行，输出 `FLY2753-SENTINEL-1789952384`。本文件为新头的最后提交；新头有效代码评审、PR 与阶段完成证据经注入 CLI 报告，不沿用旧头审批。

范围限制：生产 nodes 与 sync-phase-protocols 不在此授权 sandbox，生产投影未验证；已报告 Lead。QA 必须在最终 PR head 核验完整 CI，本里程碑不代表 QA PASS、ship 授权、合并或部署。
