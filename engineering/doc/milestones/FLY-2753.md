# FLY-2753 本机定向测试守则 — 实施里程碑
Issue: FLY-2753 (https://linear.app/geoforge3d/issue/FLY-2753/守则吞吐-实现qa-守则要求每具-runner-交卷前本机跑全量-pnpm-testpackagesrun-十几具同时跑每具-1-2)
日期: 2026-09-20
基于: ../FLY-2753-targeted-local-tests/implementation.md

Three actual sandbox executor roles now require targeted local verification and reserve full-suite evidence for complete CI on the final commit. One canonical policy and a fixed-target synchronizer prevent drift; teamlead prebuild runs the read-only check. Existing role routing, real-behavior QA, review, and ship clauses are preserved.

Validation: real old-policy RED (3 failures); 26 policy/synchronizer tests and 38 routing tests passed; TypeScript related selection passed; repository lint passed (14 existing warnings); affected package/dependency build, teamlead typecheck, sync --check, negative grep and out-of-scope byte comparison passed. No local full package suite was run. No new shell tests, runtime APIs, or exports/types changed.

Effective design review approved. Code review request registered as `62019f90-e189-4722-9fbb-d7a893c6ad62`; final verdict, PR number/head, and CI links are external handoff evidence in the PR and flywheel-comm reports. A pending review or CI run is not a pass. QA owns any subsequent frozen-head CI request; no ship or merge authority is claimed.

Detailed local evidence and consumer exclusions: `engineering/doc/FLY-2753-targeted-local-tests/implementation.md`. Rollback reverts the policy, three projections, synchronizer, prebuild and regression test together. No database migration or service restart is needed.
