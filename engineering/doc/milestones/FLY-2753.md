# FLY-2753 — 本机定向验证、全量交给精确头 CI

**Status**: ⏳ Pending exact-head review and CI
**PR**: pending creation from this milestone commit
**Date**: 2026-09-18

implement、qa、engineer 三份活守则不再要求每具 runner 本机运行 `pnpm test:packages:run`，也删除 `PACKAGE_GATE_RECEIPT` / `onTaskUpdate` 的本机全量宽免路径。新合同保留全仓 `pnpm lint`，只 build/typecheck 受影响 package，并用改动文件所在包及其直接测试消费者选择定向测试；完整路径、文件名、父目录三类 consumer discovery 的排除项都必须留下理由，新增 `scripts/__tests__/*.test.sh` 仍全跑。

全量包套件唯一由 frozen-head full-mode exact-head CI 负责；只有 `CI OK` 是全量证据，`CI Scope OK` 不是。implement/engineer 仍须修复当前 HEAD 实际运行的任一红 CI job；QA 遇红判 FAIL 并交回作者。canonical phase protocol、九个 managed projection、CI workflow 与 package-gate implementation 均未改。

本地按新合同验证：`pnpm lint`、`pnpm --filter "flywheel-edge-worker..." build`、edge-worker typecheck 均 exit 0；phase protocol 9/9 projection check、handbook contract 22/22、FLY-2121 shell contract 12/12、FLY-2533 prompt/budget 18 passed / 12 skipped。三份守则的旧本机全量/receipt 词负 grep 为零命中，新选择规则与 exact-head CI 责任均有正 grep。没有运行本机全量 package suite，也没有 merge、deploy、restart、dispatch QA 或 production mutation；本文件是 PR 的 literal-last commit，后续 review 与 CI 必须绑定此精确头。
