# FLY-2799 — Codex 临时语音容器

**Status**: Founder local-playback and consecutive-output rework complete; pending fresh exact-head code review and independent QA
**PR**: https://github.com/xrliAnnie/flywheel/pull/1306
**Date**: 2026-09-23

本单按 FLY-2795 固定合同 `313befcfa3a7039dca2fc7eb1178803d47699026` 实现引擎 B。每场语音会话新建一个隔离的 Codex 0.156.1 realtime 容器，把指定 Lead 的身份、授权 memory 文件和开场状态快照装入会话；房间结束前关闭容器并把纪要交回本体。实现没有给 Lead 建常驻分身，没有重做进房、收音或放音，也没有修改舰队 `~/.local/bin/codex` 或 codex-242 current。

## 交付与 fail-closed 边界

- standalone Codex 0.156.1、release SHA、V2 与 `gpt-realtime-2.1` 的启动和回执核验；第 1 步实测证据继续使用已完成结果，没有重复消耗额度。
- 临时 0700 home/work、只读且无执行能力的 voice profile、逐场新 process/thread/realtime、generation fencing、确定性关闭；额度或认证不可用时明确返回「语音不可用」，不静默换引擎、模型或订阅认证。
- Bridge 按注册身份装配 Raya / Honey Lemon 的身份、授权 memory、当前事项与待决定问题，并拒绝路径逃逸、身份冲突、缺失/过期/超预算上下文。
- 在共用 VoiceBackend/ConversationSession 和通用房间层上接入 PCM16/24k/mono 输入及输出；播报回执绑定 `pendingKey`、`requestDigest` 与逐句 proof。
- `bargeIn` 用取消旧 generation、等待确认关闭、只重启 realtime、重放重启期间房间音频实现。重启缓冲若溢出，先把新 transport 的输入归因标成无效再送剩余帧；不会把有缺口的输入伪装成连续 owner。
- 实时输入背压只记录 gap/drop 证据，不再以 fatal backend error 关闭房间。可信归因只在 provider speech-start item、completed user item、同一 owner/utterance、全程无 gap 同时成立时产生；混人或缺口保持 unknown。
- 动作边界使用显式 `handoffToLead` seam。语音分身本身没有派单、批准或改动权限；fake trusted mode-layer E2E 证明只有显式 handoff 的动作进入 Raya 本体信箱，闲聊不进入。生产 initiator/authority 绑定仍归 FLY-2796/2797，CLI 保持 `capture:false`。
- B 的静态 `verbatim`、`attribution`、`bargeIn` 能力仍为 false，直到 live 准入矩阵逐项通过；RPC 存在、实现代码或抽样成功均不被冒充为能力准入。
- 转写、handoff、回执和会末纪要持久化具有幂等恢复与负向保护；模型口头声称“已执行”不会产生执行权限。
- `scripts/qa/fly2799-codex-container.mjs` 只接受 0600 私有 manifest、精确 HEAD、关闭的 G1/G2 receipt、固定 binary digest/version/features、两个互斥测试 slot 和 `productionRouting=false`。准入不全时只产出 `INCOMPLETE_CAPABILITY_GATES`，不会伪造整项 PASS。

## QA FAIL rework

独立 QA 在旧头 `395f1f720e761ea9605c735a54bb3f4c51df8853` 返回 FAIL。当前 rework 修复如下：

- Quick Gate：在 `.github/workflows/ci.yml` 显式枚举 `scripts/__tests__/fly2799-codex-container.test.mjs`。
- Unit light：为 `FLYWHEEL_VOICE_BACKEND` 增加非运行时 flag truth allowlist 条目。
- Unit heavy：为 `child.kill("SIGKILL")` 增加精确 kill-path inventory 条目。
- interrupt：从永久 fence 改为确认关闭后重建 realtime generation，并把房间 response state 接到 founder barge-in；重启期间输入有界缓存，溢出后归因 fail closed。
- backpressure：drop 不再触发致命 session error。
- attribution / handoff：为连续可信 owner 建立逐 item 归因；新增受信 mode-layer 显式 handoff 接口和真实 HTTP router/client fake E2E，保持生产 classifier 与 CLI capture 不越界。

对应提交为 `d8518c7ee`、`67775816f`、`63a96a077`。旧头评审与旧头 CI 均不作为当前头通过证据。

代码评审 R3 在 `b9c27237a` 指出一个 HIGH：V2 user transcript 没有 item id，旧实现却用最后出现的 user item 做时间邻近绑定；交替说话且旧 transcript 延迟到达时，会把 guest 的文字写成 founder attribution。`3ba702935` 删除了这条推断：user transcript 只有在通知本身携带精确 `itemId`、且命中同一 ownership map 时才可 known；当前实测 V2 的 itemless transcript 一律 `unattributed`。新增回归复现 guest item → founder item → guest delayed transcript，证明不会绑定 founder；混人和背压负控也都加入 `speech_started`、completed item 与显式 item id，确保它们真正检验 `valid=false`，不是因缺少 item binding 而虚假通过。R3 其余 MEDIUM/LOW 项按评审策略为 advisory，不作为本轮 blocker；新头仍须重新评审。

Founder 2026-09-23 18:19Z 要求插话立刻停掉正在播的那句。旧路径把 provider item id 作为 `WaitingMouth` 的排队 id，却用 `"__conversation__"` 调 `cancelSpeech`，所以本地队列无动作。当前修复给房间增加按会话取消全部本地 speech 的显式入口；打断先重启 Codex realtime generation，再同步 flush 真实 `WaitingMouth` 队列。旧 generation 的预期 `speech_playback_stopped` 不再升级为当前 transport fatal error，当前 generation 的真实播放错误仍然 fail closed。

行为用例使用真实 `WaitingMouth`、`GenericVoiceSession`、`CodexRoomFrontend` 与 `CodexVoiceBackend`：先送出旧句一帧，再触发 founder 插话，一次性实测本地 playback promise 在 **0.125 ms** 内拒绝，之后的旧句帧全部为 silence；确认旧 realtime 关闭并进入 generation 2 后，插话音频送入新 transport，新回答正常播完，整场未结束。required CI 用例不把宿主机速度当门槛，而是确定性断言 barge-in 后的 microtask 已收到取消回执、下一次真实播放 tick 不再输出旧句。`CODEX_VOICE_CAPABILITIES.bargeIn` 仍为 `false`：room detect、local flush、generation restart 虽有本地行为覆盖，但最终 binary/model/profile 下真 RealtimeTransport + 真房间三件齐的 exact-stack 准入仍待独立 QA，不能用组件测试冒充 R-12。

Founder 2026-09-23 22:45Z 又要求修复连续两句 assistant 输出。旧 `WaitingMouth` 只有一个播放槽，第一句仍在播时第二个 transcript/done 会被拒为 `speech_playback_invalid`，再由 `CodexVoiceBackend` 升级成 fatal transport error，导致第二句听不到且整场结束。`92cfd56fe` 把该单槽改成按 provider item id 排列的 FIFO：每句仍逐帧提交，前一句完成后才开始下一句；重复 id 和无效 PCM 仍 fail closed。`cancelAllSpeech` 会按队列顺序同步拒绝当前句和所有后续句，因此 founder 插话不会让旧 generation 的排队句在之后冒出。测试只断言提交/结算顺序与状态，没有增加宿主机毫秒门槛。

代码评审 round 5 在 `c07af2ccc` 发现该 FIFO 引入一个 HIGH：每个 assistant item 播完都单独发 `response-done`，所以第一句完成时会提前解除 `GenericVoiceSession` 的插话 guard，即使第二句已经在播、第三句仍在排队。红测复现了第一句结算后 `response-done=1`，以及 founder 音频既不取消第二/三句也不触发 realtime restart。`a2c1c3306` 让 response state 覆盖当前 generation 的全部未结算 playback：只有最后一个成功播放完成才发一次 `response-done`；中途插话仍先发 `response-cancelled` 并同步 flush 当前句与后续队列。修复后同一 exact-stack 用例在第一句完成、第二句已播一帧、第三句待播时插话，第二/三句都结算为 `speech_playback_stopped`，generation 2 继续正常回答。round 5 的无界队列 MEDIUM 按 active policy 作为 PR Follow-up，不在本轮 HIGH blocker 修复中扩范围。

Founder local-playback 增量后的代码评审 round 1 在 `f7d787e8e6f16a6114e09a9ab8542c0d01288d9f` 返回 `CHANGES_REQUESTED`，唯一 HIGH `voice-minutes-messageid-not-snowflake` 证明会末纪要用 `voice-minutes-<sha256>` 调真实 `chat-ingest` 时必定以 `messageId must be a Discord snowflake` 退出，纪要 job 会留在 `dispatching` 且本体收不到。`202cf6e45` 从 durable job digest 稳定派生 19–20 位 unsigned snowflake-shaped message id，让 queue deliveryId 与 CLI carrier 共用同一身份；同一 schema v1 的旧无效 deliveryId 在恢复时规范化，因此旧 `dispatching` job 不会变成 corrupt 或继续永久失败。先写的真实 child-process 用例确实红在上述 carrier 错误，修复后贯通 `VoiceMinutesQueue → FlywheelCommDelivery → flywheel-comm chat-ingest → message-status` 并核对 origin、session、author 与正文。该轮 MEDIUM/LOW findings 仍按策略作为 PR Follow-ups，不随 blocker 越界修改。

## 当前本地验证

- TDD 红灯：6 条 voice 回归先证明无 restart、背压 fatal、无 attribution/response-state；kill inventory 和显式 handoff 也先失败。R3 又先以交替说话 + 延迟 transcript 复现 guest 被误记为 founder。修复后对应定向测试全绿。
- `pnpm --filter flywheel-voice-codex exec vitest related <本轮全部 voice-codex TS> --run`：11 files / 167 tests passed。
- `pnpm --filter flywheel-teamlead exec vitest related src/__tests__/voice-handoff.test.ts --run`：1 file / 11 tests passed。
- `pnpm --filter flywheel-config exec vitest related src/feature-flags/truth.ts --run`：18 files / 326 tests passed。
- voice-codex 核心定向测试：76 tests passed；Bridge client 26、teamlead handoff 11、config flag 56、kill inventory 5、FLY-2799 QA driver 4 均通过。
- CI 枚举守卫通过：86 个 Node suites、344 个 shell classifications；CI structure、scope、full-reuse、workflow-startup 守卫分别通过，直接消费者 `flywheel-voice-wrapper`、`check-flag-truth`、`fly1981-final-ledgers` 通过。
- `pnpm --filter "flywheel-voice-codex..." build`：15 个受影响 workspace packages passed。
- `pnpm --filter "...flywheel-voice-codex" typecheck`：passed。
- Founder local-playback TDD：初始用例以 playback promise 未被取消失败；加入 room→mouth 全量取消后第二次以 `backend-protocol` 导致整场结束失败；限定旧 generation 的预期取消后通过。`audio`、`codex-room`、`discord-room`、`session` 四个定向文件 **60 tests passed**，同一改动集 `vitest related` 也是 **4 files / 60 tests passed**。
- 会末纪要 carrier TDD：真实 carrier 用例先以 `messageId must be a Discord snowflake` 失败，旧 job 恢复用例同时证明仍保留无效 ID；修复后 `voice-minutes.test.ts` **6/6**，加 `adapters.test.ts` 合计 **9/9**。`vitest related` 对本轮 3 个 TypeScript 文件收敛到 `voice-minutes.test.ts`，同为 **6/6**。
- 本轮 3 个 TypeScript 文件的 `biome check`：passed。`pnpm lint` 当前 exit 0；输出仅含本单外既有 warning，没有改写那些文件。
- QA exact-head full CI `35926081403` 暴露的本单回归已按 TDD 修正：`required-wall-clock-thresholds` 先稳定红在 `codex-room.test.ts:495` 的真实 `<20 ms` 上限，`20fbcd2c9` 改为上述状态/操作顺序断言后该守卫 **1/1**、Codex room **11/11** 通过。
- 同一 full CI 的另一个红项 `tui-window.test.ts` 在本 PR 中没有改动，对应实现也与 main 一致；Ubuntu runner 单次等待 20.075s 后得到 `pane_dead=1`、空 `pane_dead_status`。本机同为 tmux 3.7c，完整文件 **30/30** 通过，精确失败用例连续 **10/10** 通过（1.20–1.60s），因此保留为独立 runner/tmux 偶发证据，不越过 founder 锁定范围修改无关 TUI 逻辑；新头仍须由 QA exact-head full CI 复核。
- 本次 rework 后 `vitest related codex-room.test.ts` **11/11**；语音定向 6 files / **69/69**；`required-wall-clock-thresholds` + 完整 `tui-window` 合计 **31/31**。affected 15-package build、dependent typecheck、改动文件 Biome 与 `pnpm lint` 均 exit 0；lint 只报告本单外既有 warnings。
- 连续输出增量先稳定红在三处：`WaitingMouth` 拒第二句、backend 产生 `backend-protocol`、插话时第二句结算为 invalid 而非 stopped。FIFO 修复后，改动集 `vitest related` **3 files / 29 tests**，真实 `WaitingMouth` + room/backend/session 定向 **4 files / 62 tests**，`required-wall-clock-thresholds` **1/1**，`flywheel-voice-wrapper` **32/32**。affected 15-package build、dependent typecheck、改动文件 Biome 与 `pnpm lint` 都 exit 0；lint 仍只报告本单外既有 warnings。
- Round 5 dead-zone 红测先得到 `responseDone=1`（预期 0）且插话后的 cancellation reasons 为空；最小计数修复后 Codex room **12/12**。对包含 `CodexVoiceBackend.ts` 的改动集重跑 `vitest related` 仍为 **3 files / 29 tests**，语音四文件 **62/62**，wall-clock 守卫 **1/1**；affected build、dependent typecheck、Biome 与 lint 再次 exit 0。
- 没有运行本地 full package suite；没有新增 `scripts/__tests__/*.test.sh`。

## 消费者发现与排除

按每个改动文件的完整路径、文件名和父目录运行了 `git grep -lF`。保留并执行了实际消费者：`scripts/__tests__/flywheel-voice-wrapper.test.sh`、`packages/config/src/__tests__/fly1981-final-ledgers.test.ts`、`scripts/__tests__/check-flag-truth.test.sh`，以及上述 CI workflow 守卫。Codex 源码消费者使用 `.js` import，字面搜索只命中文档/路径清单，因此以 owning package 的 `vitest related` import graph 覆盖。

本次 founder local-playback 增量再次按 6 个改动文件的完整路径、文件名、父目录检索；可执行直接消费者只有 `audio.test.ts`、`codex-room.test.ts`、`discord-room.test.ts`、`session.test.ts`，已全部保留并运行。其余新增命中仍是历史工程文档、评审 JSON、HTML/build-page、路径 inventory 或无关同名 `session.ts` fixture，均不执行；`vitest related` 的 import graph 与上述 4 文件一致。

本次 minutes blocker 增量按 `voice-minutes.ts`、`cli.ts`、`voice-minutes.test.ts` 的完整路径、文件名、父目录再次检索。保留的可执行直接消费者是 `voice-minutes.test.ts`，并额外运行 carrier 所经的 `adapters.test.ts`；`vitest related` 也只保留 `voice-minutes.test.ts`。排除的每个字面命中均属于：FLY-2799 plan 中的路径/测试清单；历史工程 plan/research/review JSON/HTML/build-page；CI path inventory；`child-process-census.json` 中其他 voice-codex 文件；`flywheel-voice-wrapper.test.sh` 内固定的 `daemon.ts` changed-files fixture；或仓库其他包无关同名 `cli.ts`。这些都不 import、调用或断言本轮 minutes/CLI identity 行为。

本次 QA full-CI rework 只修改 `codex-room.test.ts` 的宿主时钟断言。按完整路径、文件名与父目录检索只命中本单 plan/milestone、历史 continuation/review/follow-up 文档和 CI path inventory；没有新增可执行消费者。实际行为仍由 owning-package `vitest related` 与上述真实 `WaitingMouth` 定向用例覆盖。

本次连续输出增量对 `audio.ts`、`audio.test.ts`、`codex-room.test.ts` 的完整路径、文件名和父目录运行 `git grep -lF`。保留的可执行消费者是 `audio.test.ts`、`codex-room.test.ts`、`discord-room.test.ts`、`session.test.ts` 与 `flywheel-voice-wrapper.test.sh`，均已运行；`WaitingMouth` 符号检索只额外命中生产 `discord-room.ts`，由 related/定向用例覆盖。其余命中全部是历史工程文档、评审 JSON、HTML/build-page、CI path inventory、milestone/plan 路径清单或 `child-process-census.json`，不 import、调用或断言播放队列行为，逐类排除且未作为测试运行。

Round 5 修复又对 `CodexVoiceBackend.ts` 与 `codex-room.test.ts` 的完整路径、文件名和父目录执行同样检索；字面路径只命中 FLY-2799 plan/milestone、历史 review/follow-up 文档与 CI path inventory，全部是非执行消费者。符号检索确认生产调用方为 `cli.ts`、`CodexRoomFrontend.ts`，行为消费者为 `codex-room.test.ts`；由 owning-package related 与上述 exact-stack 定向用例覆盖。

排除项逐类记录如下：

- 历史工程文档、计划/研究、HTML/SVG/MMD、数据快照和路径 inventory：非可执行消费者。
- `packages/edge-worker/src/__tests__/cipher-dimensions.test.ts`：仅为字面 fixture `src/auth/session.ts`，与本单 `session.ts` 无关。
- `fly1674-residue.test.sh`、`fly2102-flag-freeze.test.sh`：仅检查其他已退役 flag 的精确 tombstone key。
- `packages/config/src/feature-flags/registry.ts`：仅注释命中。
- FLY-2278 fixture/test：命中 fixture 路径，不读取 live `truth.ts`。
- `test-deploy-generalized`：只命中显式 glob exclusion。
- flag-provenance 文件：目标是 `registry.ts`，不是本单改动的 `truth.ts`。

## CI、评审与 QA 边界

旧头 GitHub Actions run `35868620448` 的 teamlead shards、observation、六组 script tests 和 payload job 成功，但 Quick Gate、Unit light、Unit heavy 与最终 `CI OK` 失败；这些失败正是早期 rework 的修复对象。R3 评审头 `b9c27237a` 的 scoped run `35876830757` 中 scope classification、Quick Gate 和 `CI Scope OK` 成功，unit/script/payload 因 scoped run 跳过；这不是 full CI，也不覆盖后续 R3 修复头。QA 在 `8ee602525` 发起的 exact-head full run `35926081403` 中 Quick Gate、六组 script tests、payload、heavy/light、observation 与 teamlead shards 3/4、4/4 通过；teamlead 1/4 因本单 wall-clock guard 失败，teamlead 2/4 因上述未改 TUI 用例偶发失败，最终 `CI OK` 因此失败。`20fbcd2c9` 修复确定的本单回归；这个新头没有请求或声称 full exact-head CI，冻结头 full CI 仍由 QA 发起。

旧头代码评审均已被后续 rework 失效；`f7d787e8e` 的 round 1 已由上述 HIGH blocker 正确阻止。round 3 在 `11d5a4963` 返回 `reviewVerdict=APPROVED`，同头 automatic scoped CI `35929102999` success；它们在最终 progress 与本里程碑提交后按精确头纪律失效。`c07af2ccc` 的 automatic scoped CI `35931134163` success，但 round 5 正确阻止了上述插话 dead zone；两者均被 `a2c1c3306` 失效。当前字面最后 milestone 头必须重新注册 exact-head code review，并只把新的 automatic run 当 scoped CI；实现阶段不请求 full CI，冻结头 full CI 仍由 QA 发起。

G1/G2 集成准入仍未关闭，且没有授权私有测试房 manifest，因此本阶段没有运行 Raya / Honey Lemon 两场真人测试房，也不声称人格事实、当前事项、本体信箱或纪要的 live QA 通过。没有读取或改写生产凭据，没有修改生产运行时配置/flag 状态，没有启动真人房间、重启 Bridge/Lead、dispatch QA、merge、deploy、push main 或请求 ship approval。

本文件必须是 PR 的字面最后一笔提交。独立 QA 后续负责冻结当前头、请求 full exact-head CI，并在 G1/G2 与 manifest 就绪后完成两场 live 验收。
