# FLY-2413 — 按表拆分 retention 分类登记

**Status**: ⏳ Pending ship
**PR**: [#1196](https://github.com/xrliAnnie/flywheel/pull/1196)
**Date**: 2026-09-14

将集中表清单迁为按数据库、表命名的独立 JSON 片段，保持原有 257 个身份的保留分类。严格加载、独立 schema 观测和完整输入摘要继续拒绝遗漏、非法输入及旧清理授权；四个变异体与 A/B 合并实验验证守卫有效。实施验证与待完成 gates 见本 issue 的 implementation-evidence.md；未进行生产清理或激活。

恢复验收补充：同一份可执行共享测试在模拟第三方加表未登记时失败、补独立片段后通过、丢失片段后再次失败；测试字节全程不变。257 条分类已从 Git 基线重新逐条比对，无差异。最终精确头 CI、评审与阶段交接以 PR 和结构化回执为准。

2026-09-15 冲突返工：合入 main `8e28a264c`，把其新增的三张 FLY-2563 表迁为独立保护片段，并保留专属保护断言。合并后 260 条 identity/classification 与该 main 基线逐条相同；移除三个新片段时，未改共享守卫明确报出三张未登记表。返工提交 `7283c0c74`；新头评审、CI、QA 重测和 founder gate 仍分别以控制器回执为准。

同日继续同步 main `b9418b780`：保留 FLY-2567 移动后的 patrol 文档 exact paths，retention 两个片段例外不变。FLY-1674 82/82、FLY-1645 和 CI 结构检查通过；260 条分类未再变化。

同日按条件式授权同步 PR #1199 合入后的 main `84a65da2e`：两张 Epic intake 表保留原保护分类并迁为独立片段，262 条分类与 main 逐条一致。一次定向验证 111/111、消费者检查及 residue 检查通过；该记录随唯一合并提交保存，新的精确头评审和 CI 待独立回执。
