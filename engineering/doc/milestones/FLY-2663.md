# FLY-2663 — required CI 去真实墙钟阈值

**Status**: ⏳ Pending exact-head review and CI
**PR**: [#1234](https://github.com/xrliAnnie/flywheel/pull/1234)
**Date**: 2026-09-17

required package 与 workflow 脚本测试不再用共享 runner 的真实毫秒上界决定通过或失败。`observation-performance.test.ts` 保留 170 万 events、500 holders、25,000 outcomes、WAL checkpoint、分页候选上界、游标推进、steady/tail 与两种 `modeTick` 结果，但删除 `<50ms/<100ms` 采样硬门；其它 required 测试按 fake timer、timeout/state receipt、锁配置恢复、子进程清理或确定性输出替换真实 elapsed 上界。新增的 required-check 审计由 package tests 和 `ci.yml` 的脚本命令推导范围，拒绝 real-clock duration 的绝对上界，而不禁止时间戳顺序、逻辑时钟或操作数断言。首轮 exact-head CI 还暴露 `automated-message-inventory.test.ts` 的全仓 AST 扫描撞上 Vitest 默认 5 秒；两个同成本扫描现均显式使用 60 秒纯 hang guard，语义断言未变。

QA1 又定位到两个依赖生产 25ms `performance.now` budget、随后断言精确 count/cursor 的套件。`outcomes.test.ts` 与 `verdict-cursor.test.ts` 现在在测试边界注入恒定 `performance.now`；required wall-clock guard 显式登记这两个预算敏感套件并要求时钟注入。QA 的 `performance.now += 0.2ms` 预加载探针在红侧稳定复现 4 项失败，改后两个套件 40/40 全绿。`flywheel-comm` CLI 的目标测试继续要求 exit code 0 且不允许任意非诊断 stderr，只忽略合法的结构化 `[slow-sql]` 告警；`performance.now += 300ms` 探针也通过。生产路径、25ms observation budget、CI workflow、land policy、migration 和部署均未修改；`lead-capability-read` 超时及 FLY-2098 的 `policy_blocked` 自愈仍在范围外。

QA2 的功能判据全部通过，唯一剩余阻塞是 `unit-tests` job 的 15 分钟上限：共享 runner 慢速时，teamlead 分片测试已通过却会在后置 `test:stub-hygiene` 前后被取消。此次路由返工只把该 job 的 `timeout-minutes` 从 15 提升到 25，并同步把既有 `fly-889-ci-workflow-timeout-guard.test.ts` 的精确下限从 15 提升到 25；heavy unit 等其它 job 均未改动。审计红侧先以现有 workflow 的 15 分钟值稳定失败，workflow 改动后 4/4 通过。

TDD 红侧先让审计列出现存真实墙钟 sink，再逐文件改绿。初始改写后的 observation 场景在同一 checkout 连续 20/20 次通过；QA1 返工的 focused suites 为 teamlead 41/41、flywheel-comm CLI 60/60。QA2 路由返工后，`pnpm lint`、`pnpm -r build`、`scripts/__tests__/ci-structure.test.sh` 与 timeout guard 4/4 均通过；`pnpm test:packages:run` 再次生成 passed receipt：18 个包全部通过、所有 receipt 均为 0 failed，其中 teamlead 为 1,471 files / 17,444 passed，flywheel-comm 为 202 files / 2,682 passed。

本文件是 PR 的 literal-last commit；后续只允许推送一次，并对不再移动的精确头做一次 effective code review 与一次完整 GitHub CI OK 验证。结果由结构化 gate/CI receipt 判定。本实现不 dispatch QA、不请求 ship、不 merge 或 deploy。
