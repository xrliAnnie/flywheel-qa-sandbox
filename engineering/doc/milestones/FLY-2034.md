# FLY-2034 — Belle 完整 Lead 席位的工程接入件

**Status**: ⏳ Pending ship
**PR**: #946
**Date**: 2026-08-25

已创建 private `xrliAnnie/belle-workspace`，`main` 固定到 `726c5204afc9dca22a8989c01ad0a10b93accd69`：原地兼容 `~/Dev/personal-assistant`，含 `.claude/skills/` + 顶层 `skills` 相对 symlink、`archive/`/weekly 台账、`MEMORY.md`、`life-executor`、generic menu roster/adoption 与 identity 派工职责升级稿。Founder question gate 明确裁定使用 LEARN `personal-assistant` label；已创建并回读唯一 label `eb1437bf-4599-4535-b2ac-7bbf348d71d8`。Flywheel 侧把 cross-dept roster 中 Belle 从 companion 修正为 runner-owning life dept Lead，并以行为测试钉住只有 Mufasa 仍是 companion；附带两个 fail-closed verifier，分别验证 scaffold/旧目录完整折入（含 directory/symlink 类型冲突）/无凭据与机器路径/真实 ConfigLoader + generic v2 snapshot/三条负向对照，以及 `projects.json` raw JSON 只允许 `projectRepo`、memory allowlist、删 companion、开 spawn、matcher 改为 `personal-assistant` 五项语义 delta，并钉住既有 `department=life` 联合不变量。接入清单覆盖仓外备份、隐藏与 staged 文件凭据扫描、逐路径归档、identity 单独 commit、0600 同目录原子切换、部署与 launcher 收敛、对称回滚和由 Belle 本人发起菜单任务的完整证据链。实现节点遵守边界：没有改 live `~/Dev/personal-assistant`、`~/.flywheel/projects.json`、identity、manifest/plist/launcher，没有重启或冒用 Belle 真派工；这些与归档 PR 验收留后续 founder 维护窗 + DAG QA。验证：lint 0 error（既有 warning）、22 workspace build、rules 21/21 + shell 93/93、staged index text/binary credential gate、CI shell census 214/214（162 CI + 52 manual-only）+ ci-structure、workspace/projects 双 verifier 正负路径全绿；canonical package aggregate 的 TeamLead 为 9,450 pass/6 skip，49 个非本单失败均为并发固定预算 timeout 或宿主 root-owned npm cache，代表性 6 files/72 tests 串行复验与临时 cache `npm pack` 24/24 全绿；core 非 GUI 219/219，真实 Terminal.app/HiServices 用例仍是 headless 宿主边界，未伪报原 aggregate 全绿。
