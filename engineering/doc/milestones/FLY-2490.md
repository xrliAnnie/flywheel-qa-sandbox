# FLY-2490 — 未启动失败体的 shipped 收尾

**Status**: ⏳ Pending ship
**PR**: #1167
**Date**: 2026-09-11

对 Bridge 内部 durable pre-adapter receipt 证明从未进入 adapter 的 failed/blocked Codex 节点，在 closed claim 与 daemon/通用缺席证据成立时允许 issue closeout 确认死亡；保留 crash-preserve 门。HTTP 禁止写 pre-adapter failure，新增节点未确认消失的诊断 token，避免误报 lifecycle_conflict。

附带默认只读的历史补写脚本及审计，未执行生产补写。历史 held operation 的补写/resume 由运维单独执行。定向测试、完整 lint/build 已通过；精确 HEAD CI 与代码评审作为 PR 交付门。物理 DB 写边界由 FLY-2526 承接。

R1 唯一 HIGH 与 CI 清单遗漏已修复；R2 与新 HEAD CI 待确认。

QA attempt 1 已确认运行时修复；attempt 2 修正历史补写读取字段为平铺 `payload.failureKind`。真实形状回归及负例、完整 lint/build 通过。此返工的新 HEAD 仍须代码评审、CI 与 QA retest，尚未具备 ship 资格。

Engine attempt 3（2026-09-13）：合并 main 并仅解决冲突，保留双方导入与 consumer 清单，按实际合并表清单更新 retention 计数。lint、251 个定向测试及 8 个脚本测试通过；宿主全包测试因 config 三个超时失败，构建待依赖同步后复核。此最终 HEAD 须重新取得代码评审、精确 HEAD CI 与 QA retest，结果由交接报告记录。

Engine attempt 4（2026-09-14 UTC）：合并 main `573159a15`，仅手工合并 StateStore 双方 imports（`6fe9c1eba`）。探活/receipt/retention 字节不变，保留 main 独立函数。完整 lint/build、468 项定向测试及 8 项脚本测试通过；全包测试、此最终 HEAD 的全新 review/CI 终态以交接回执为准。
