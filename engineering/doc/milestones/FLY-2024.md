# FLY-2024 — 小红书 MCP Chromium 泄漏治理

**Status**: ⏳ Pending ship
**PR**: #954
**Date**: 2026-08-25

修复 xiaohongshu-mcp 中 rod 的 `Context(ctx)` 覆盖页面 timeout、导致卡住调用永不返回且浏览器清理不可达的问题；同时在 Flywheel Bridge 增加 30 分钟 stale rod 主进程兜底回收，使用精确进程身份复核、TERM→KILL 有界升级与脱敏审计回执。源码修复在 xrliAnnie/xiaohongshu-mcp#8，Flywheel 兜底与完整设计证据在 #954；合入不自动部署，xiaohongshu-mcp 的 rebuild/restart 需另行取得 founder 确认。
