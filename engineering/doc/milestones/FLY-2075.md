# FLY-2075 — 恢复显式升级的告警 Hub 主链

**Status**: ⏳ Pending ship
**PR**: #962
**Date**: 2026-08-26

因果链已钉死：2026-08-14 合入的 FLY-1764 Flow 2 把普通 ticket 从 `AlertChannelHub` 改投 Claw mailbox，Discord copy 默认关闭；Hub 本身一直正常构造，但上游不再调用它。08-15～21 的残余少量 `alert_threads` 来自旧 `alert-queue` 回放，08-21 06:42Z 后回放源枯竭，因而绝对归零；watchdog 死亡与 FLY-1981 flag retirement 都不是断流刀口。

founder 最终选择「普通告警留在 Claw mailbox；只有真升级进 Discord」。实现复用现有 `enqueueInfraAlert`：全部普通告警（含原先 Discord root-only 的九种 informational kind）持久写 `source_kind='infra_alert'`，不建 Discord thread / `alert_threads`；仅携带合法 Discord snowflake 的显式 mention，或能注入合法 canonical founder id 的 `workflow_engine_escalation`，才走 Hub 根消息 → thread → `alert_threads`。非法 mention 不能绕过 mailbox；Hub 已启用但 founder id 非法时 boot 打红灯并复用 `alert_unreachable_config` meta-alert。FLY-2076 因此必须消费全部 `infra_alert` mailbox 行，不能只读 `alert_threads`。

保留并验证本 PR 的配套修复：Hub 不再自动 @founder / 自动置 `ESCALATED`，无调用者的 founder-page / runbook-gap 路径已删除；`repair_status IS NULL` retention、退役可选表、独立 per-Lead `alert_unreachable_config`、Cass 误归功修复与 FLY-1674 精确豁免均保留。当前相关回归 133/133、全仓 lint（仅 8 条既有 warning）与 22-package build 通过；此前全包门无本单归属失败。

新增 `scripts/qa-fly-2075-alert-route-e2e.mjs` 作为最终语义的隔离真 Discord 验收：先证普通告警仅写 durable mailbox、账本零行，再证可辨识 workflow escalation 完成频道根消息 → thread → `alert_threads` 并带 founder mention。Implement 环境缺 `TEST_BOT_TOKEN_1`，未伪造真机结果；交由 QA 节点持凭据执行。旧 `[QA1082-406794]` 只证明 Hub/ARC 组件链，不作为最终双路由验收。
