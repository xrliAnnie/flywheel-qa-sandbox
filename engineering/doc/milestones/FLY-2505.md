# FLY-2505 — Recovery owner 失败留因与有界重试

**Status**: ⏳ Pending ship
**PR**: https://github.com/xrliAnnie/flywheel/pull/1159
**Date**: 2026-09-11

Codex recovery precommit 失败保存结构化、安全诊断与原子结算回执。确认排空的 daemon/socket readiness 使用固定 3 次/15 分钟窗口；其他失败保留 2 次 charged 预算。持久化 reservation 序号、共享 commit 事实与有限 Heartbeat 保护，耗尽事件和 Lead outbox 原子冻结；演练消费者兼容旧证据并识别新计数。

[实施验证与残留](../FLY-2505-recovery-owner-diagnostics/implementation.md)：lint/build 通过，最终定向 TeamLead184、runner269、core9 通过。全量 package suite 因 config 超时失败，隔离27通过；演练全套531通过/1失败，liveness 隔离为 EPERM。Lead a60e3994 允许保留宿主/沙箱限制继续 review/PR，以 exact-head CI/QA 为准；同测试 CI 再红须停下报告。未验证真机同体恢复，未合并或部署。

R1 返工：修复跨 revision 的过期 reservation 接管死锁、quota preauth 错误标识、普通 Error 留因与 dispatch/postcommit 输出保留；同步 main 后 lint/build PASS，定向 TeamLead82、runner272、core10 PASS。全包 runner 曾因旧通用文案断言失败（已校正并定向绿），另有 onTaskUpdate 超时；全量红回执保留。R2 与 exact-head CI 待验证。

R3 前五项补修完成：UUID/snake_case 与 abort 原因保留、dispatch 启动原文、claim_lost 非账务观察、active-run 告警绑定和候选隔离/事务回滚留痕。lint/build/typecheck PASS，TeamLead195、演练消费者89 PASS；全包 runner1283断言通过但 onTaskUpdate 超时使命令红，证据如实保留。R3 与新头 CI 待验证。

Engine 冲突返工（implement attempt 2）：合入 main `ca869ad6d`，merge commit `7642ea204`；唯一文本冲突为 StateStore 顶部 import，两侧均保留。main 的通信终态/收件人防线和 review 并发配置保留，recovery 行为未重写。`pnpm lint`、`pnpm -r build` PASS；相关 TeamLead 7 文件 242 测试与 Comm 2 文件 19 测试 PASS。按 Lead be0b6b76-f321-4fec-b0fe-decdeb0dd2ba 指令不在宿主跑全量包套件，等待本次冻结头 code review 与精确头 CI，之后交 QA 重测。日志：`/tmp/FLY-2505-rework/`。

Engine 冲突返工（implement attempt 3，2026-09-13）：合入 main `26ebc4931`，merge commit `6157277f4`。`git show --remerge-diff` 证明手工处理仅移除 StateStore 顶部冲突标记，保留 recovery、rework wake 与 BetaReleaseStore import；plugin 自动合并，无功能重写。`pnpm lint` PASS；初次 build 缺新 main 依赖 yauzl，`pnpm install --frozen-lockfile` 后 `pnpm -r build` PASS。定向 TeamLead 8 文件178测试 PASS（recovery/lease/wiring/runtime、wake retirement、beta store、heartbeat），演练消费者89 PASS。本次未新增 shell 测试。

本地 `pnpm test:packages:run` FAIL：config drift-scan 5000ms timeout、fly1981-final-ledgers 15000ms timeout（816 PASS / 2 FAIL）；两文件隔离复查37 PASS / 1 FAIL，final-ledgers 仍15000ms timeout。保留失败证据，不宣称本地全包绿，不修改无关治理测试。日志 `/tmp/FLY-2505-rework-attempt3/`。本提交作为最后 milestone 提交；新头 code review 和精确头 CI 待验证，再交 QA 重测。未执行重启、合并 main 或部署。
