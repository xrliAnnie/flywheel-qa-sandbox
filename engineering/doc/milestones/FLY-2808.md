# FLY-2808 — 全节点退下与原会话拉起

**Status**: 🔎 Pending exact-head scoped code review
**PR**: #1299
**Date**: 2026-09-24

定稿并按 Lead 后续授权实现所有 DAG 节点、Claude/Codex 共用的主动退下与意外死亡区分：持久 process-body 代数状态、六处死亡消费者 guard、精确原会话/model/cwd 恢复核验、失败可读与一次 fresh fallback、恢复/fault 独立预算，以及 founder 的 working / standby / problem 三态。实现默认关闭；未部署、未启用生产、未派 QA。

R2 与 R3 的 HIGH 已修：恢复复用原共享工作树；失败进程只在精确 generation/demand/owner fence 下做物理 cleanup，保留 lifecycle/CommDB 记录并证明 execution-wide death；resume 同时观察 launch outcome 与身份回报；`resuming` 具有 5 分钟 lease，长于 180 秒身份超时。退休投影、Codex strict identity、终态显示、CAS 结果、死 process body 与 adapter callback 隔离也已收口。R4 在 `a351d044b` 通过后，QA 的 full exact-head CI run `35862850957` 仍为红；后续先修其中可归因的 flag-governance 回归：`FLYWHEEL_NODE_STANDBY_RESUME` 进入统一 default-off flag store，fresh/rework admission 都在调用时读同一 resolver，`FLYWHEEL_CLAUDE_SESSION_DIR` 明确分类为路径 plumbing。

R5 对 flag rework 旧头 `b39a2ea75` effective `APPROVED`，但 Lead 将 `cleanup_unconfirmed` advisory 明确列为交卷前必修。当前 latch 对 founder 投影 `canResume=false`；新增 master-token-only、无 actions alias 的审计重开路径，事务内记录 server-derived actor、时间、原因和 prior attempt 边界，再恢复独立 resume budget；旧尝试不删除，fault replacement 额度不受影响。R6 对 `ff72e9d8c` effective `APPROVED`，随后因 PR 与 main 冲突按 Lead 指令作废旧头绑定。

本轮 Lead 必修 TDD：精确 2 files / 67 PASS；teamlead related 为 567/568 files、7983 PASS、4 个既有 SKIP，仅一个既有 chat-thread 404 JSON 断言瞬态红，同头精确复跑该文件 68/68 PASS。随后 merge `origin/main` `fef2dd43d`，唯一 truth.ts 冲突同时保留 FLY-2808 Claude session-dir 与 FLY-2766 native baseline；merge 后 feature-flag guards 113 PASS、StateStore/reopen 67 PASS、CodexTmuxAdapter 134 PASS。

QA 529 真机在旧头 `2a28fdd26` 发现启用开关后首节点未 enrollment、`process_retirement_pending` 未进入既有 park settlement 消费者、step 4 仍断言旧 actor 存活。本轮代码提交 `61f8d5cce` 只修这三点：fresh 首节点与 Lead retry 热读同一 governed flag；QA pass、resident grace expiry 与终态 closeout 都结算 retirement park；529 step 4 等待 enrolled body=`standby` 且物理进程已退出。先红 5 个 Vitest + 2 个 shell guard，后绿 3 files / 129 PASS 与全部 generalized helper guards；13 个受影响包及依赖 build、teamlead 反向依赖 typecheck、5134-file lint 均通过。park reason 的全部字面消费者和测试/fixture 排除项已记入 validation.md。

推送后 PR 因 main 前进再次 conflicting，已以 merge commit `9f5143fb6` 同步当时 `origin/main`；唯一 TmuxAdapter 冲突同时保留本单 standby retirement 与 main workflow-usage import。同步后 teamlead 129 PASS、Claude/Codex adapter 311 PASS、generalized helper shell 串行全绿；同一 13-package build、teamlead/claude-runner typecheck 与 5155-file lint 通过。仍保留并发/metrics LOW 与 dead env LOW follow-up。下一步只请求当前 literal-last head 的有效 code review；不请求 full CI、不 merge 到 main、不 deploy，冻结头 full CI 与 529 真机复验由 QA 节点负责。

返工 code review round 1 对 `be80b3e2c` 返回唯一 blocking HIGH：旧快照模型 alias 与 adapter canonical id 比较会杀掉启用 enrollment 后的首个进程。提交 `aaf185841` 在 admission 单点规范化 registry alias；Claude `fable` 与 Codex `astra` 用例先红 2、后绿，最终相关 6 files / 332 PASS，build/typecheck/lint 复核通过。2 MEDIUM + 1 LOW 已记入 follow-ups.md 并交 Lead 取舍；新 literal-last head 必须开新 review，旧 verdict 不复用。

2026-09-24 的 implement 恢复批次从 Lead 指定的 `c15b085d8` + stash `d954108f` 继续 3/4→4/4。提交 `63af1af6d` 为 Claude/Codex 补 durable 60 秒退休 watchdog、精确 stop/drain/窗口消失证明与 `retirement_unconfirmed` 闩锁；同一 master-token 审计重开路径现在也覆盖尚无 demand 的退休失败。QA 529 step 2 改为等待 enrolled design body=`standby` 且物理进程 dead。定向 adapter/StateStore/route/消费者/shell、13-package build、四包 typecheck 与 5157-file lint 均绿；完整证据见 validation.md。仍未运行 exact-head full CI 或真 Discord 529，不派 QA、不 merge、不 deploy；下一步仅在 literal-last 头请求 R8。

R8 对 `34c5cf900` 返回两条 blocking HIGH。提交 `3f8565228` 将过期 resume owner 改为 founder 不可拉起的审计 latch，避免仅凭 lease 超时双开同一 session；同时把 workflow ledger 的 trigger drop 移到 purpose backfill 前，保证新版→旧版→新版 roll-forward 可启动。两项均先红后绿，最终 StateStore/ledger/route 100 PASS、rework coordinator 45 PASS，build/typecheck/lint 通过。2 MEDIUM + 2 LOW 保留并记录在 follow-ups.md；新 literal-last 头需开 R9，R8 不复用。

R9 对旧 exact head `ffb3136a9` 已通过，随后 frozen-head full CI 为绿；但 QA@2 的真 Discord 529 在 Claude `eng_design` 首节点复现 process body 长期 `retiring`、PID/window 仍活并最终超时，因此旧头不能交卷。本轮提交 `eb48567da` 只收口该 blocking path：`/api/runs/start` entry 与后续 engine 一样传入 process lifecycle；Bridge 既有 patrol tick 独立扫描 durable `retirement_requested_at`，超过 60 秒后以 generation/request fence 清理精确 process body，只有物理死亡可证明才写 `standby`，否则写 founder `canResume=false` 的 `retirement_unconfirmed`。点名测试 71 + 80 + 71 PASS；owning-package related 584 files / 8243 PASS / 4 个既有 SKIP；13-package build、teamlead 反向依赖 typecheck、5159-file lint 通过。完整证据与消费者排除项见 validation.md。

本节点没有重跑真 Discord 529，也没有请求新的 full CI；新 literal-last exact head 必须先通过 R10 code review，再交 QA 冻结并复验 529 与 full CI。不 merge、不 deploy、不自行派 QA。

交卷前以 merge commit `423e0d7e3` 同步 `origin/main@049fbf194`，未 rebase、未 force-push；唯一重叠的 `plugin.ts` 自动合并后，retirement patrol 与 main 的 quota-page wiring 均保留。合并后 9 个点名文件 / 281 PASS，13-package build、teamlead + voice-codex typecheck、5165-file lint 全绿。新的 milestone 提交保持为字面最后提交，R9 旧头 approval 不复用。

QA claim 1475 在旧头 `ca2ccc1c2` 证明入口节点已真实到达 `standby`，但 Claude finally 在 reconciler 先结算 body 后撤销 retirement approval，并由迟到 `session_completed` 把 `ship_parked` 改成终态。本轮提交 `eaa33480c` 让 entry、后续 node 与 resume 共用同代 `retiring|standby` approval 判定，并在 StateStore 只对 `ship_parked` + `retiring|standby` body 保留迟到终态；active body 负例仍正常终态化。直接回归 159 PASS；owning-package related 571 files / 8025 PASS / 4 个既有 SKIP；13-package build、teamlead + voice-codex typecheck、5165-file lint 均绿。adapter false-branch 原因码日志保留为非阻塞 follow-up。当前 literal-last head 必须通过 R11 scoped code review，再由 QA 冻结并重跑 529 与 full CI；本节点不请求 full CI、不 merge、不 deploy、不派 QA。

R11 对旧头 `be4aa4ee2` 返回唯一 blocking HIGH：Claude resume 在真实进程启动前用 manifest 输入自证身份，且首 prompt/tool 没有 durable gate。提交 `516b8b490` 改为观察真实 Claude `SessionStart` 的 `source=resume/session/model/canonical cwd`，只有 controller 将同 generation 原子提交为 `active` 后才写逐 launch ack；UserPromptSubmit/PreToolUse 在 ack 前 fail closed。点名 Tmux 181 PASS、HookCallbackServer 26 PASS、coordinator/sync/config 133 PASS 与 hook shell guards 全绿；五包 owning related 合计 core 23、config 328、edge 26、Claude 446、teamlead 1203 PASS，另有 4 个环境 SKIP。core 9-package 反向依赖 typecheck、13-package affected build 与 5167-file lint 通过。R11 其余 advisory 已列 follow-ups.md；当前 literal-last head 只请求 R12 scoped code review，仍不请求 full CI、不 merge、不 deploy、不派 QA。

R12 对旧头 `5445d6389` 返回唯一 blocking HIGH：Claude 2.1.281 的真实 `--resume` SessionStart 不提供 model。提交 `a1154435b` 按 plan §5.3 保留真实 SessionStart 的 source/session/cwd 证明，并在 model 缺席时要求精确 `<session-id>.jsonl` transcript，从最后 assistant record 读取模型元数据，与已确认 launch manifest 精确比较；缺失、损坏或不一致继续 exit 2。真实 resume 形状 fixture 先红后绿，错误模型、缺 transcript、no-ack 均 fail closed；`bash -n`、shellcheck、直接 hook test、5167-file lint 与 diff check 全绿。R12 advisory 已列 follow-ups.md；新的 literal-last head 只请求 R13 scoped code review，仍不请求 full CI、不 merge、不 deploy、不派 QA。

QA claim 1479 在旧头 `682036f0e` 的真机 stub lane 证明 tmux 3.7c 会把不存在 target 的 `display-message` 静默回退到当前 window，导致 Claude 入口节点无法确认退场并错误锁成 `retirement_unconfirmed`。Lead 后续 R1–R3 指令已由提交 `6d4dbd102` 收口：Claude/Codex 共用逐行 `list-windows` 三态 helper；`unknown` 保持 `retiring`，只有正向 `present` 才落失败锁；adapter grace 从自身首次观察计时，与 durable 60 秒兜底分账；Claude watchdog 四个静默分支补闭集 reason code + execution id。点名四文件合计 332 PASS，Claude/teamlead typecheck、13-package affected build、5167-file lint 与 diff check 全绿；真实 tmux 3.7c 隔离探针固定 `display-message rc=0/output=@0` 而 inventory 中目标已消失。完整红绿、消费者与排除项见 validation.md。本节点不请求 full CI、不跑真 Discord 529、不 merge、不 deploy、不派 QA；当前 literal-last exact head 只请求 scoped code review，通过后交 `needs_review`。

该 R1–R3 literal-last 头 `caa176421` 的 scoped review 返回唯一 blocking HIGH：物理 standby resume 早于 activation admission、credentials 与 TURN。提交 `d64754fe5` 只重排这一边界：durable resume reservation 可先建立，但 `resumeStandbyActor` 必须等 admission、credential mint/rotation、TURN grant 与 TURN projection 全部成功；身份核验后才激活 holder、最后 wake。顺序用例先红 1，后绿 45/45；两个直接依赖文件另 15 PASS，teamlead typecheck、13-package affected build、5167-file lint 与 diff check 全绿。其余 4 MEDIUM + 4 LOW 保留 disposition；新的 literal-last exact head 必须重新 scoped review，旧 review 不复用，也不把本地证据称为 full CI。

Founder rework 在旧头 `0f893cabf` 明确要求 Codex 与 Claude 都恢复原会话、用运行时实际观测身份核验，并在 `retiring` grace 内优先取消退场。本轮提交 `261b92e16` 只收口这三条：Codex 从既存 immutable launch snapshot 与 `session.json` 精确恢复同一 thread，app-server `thread/resume` 的实际 id/model/canonical cwd 缺失或不一致均 fail closed；identity callback 先交 controller 持久化，再在首个 goal 输入前等待同 generation body=`active`。rework prompt 只走后续 wake，不覆盖 immutable launch identity。`retiring` body 以同 generation CAS 回到 active 并审计取消，旧 completion replay 被 fence；若取消恰好输给 standby confirmation，则重读 durable body 并进入同一 resume 路径，不误走 fault replacement 或消耗故障额度。Claude 既有真实 SessionStart/transcript 身份门保持不变。红绿覆盖 exact snapshot、空/错 thread、model/cwd、durable gate、grace cancellation 与 standby race；直接点名 485 PASS，相关选择器在最终两批分别 571 files / 8085 PASS / 4 skipped 与 104 files / 1234 PASS，两个受影响包及依赖 build、两包 typecheck、repo lint（exit 0，只有既有警告）与 diff check 全绿。当前 literal-last head 只请求 scoped code review；exact-head full CI 与真实 Claude/Codex continuity 仍由 QA，未 merge、未 deploy、未派 QA。

该 exact-head review 返回唯一 blocking HIGH：Bridge 的 resume 请求不会重带 fresh launch 的 generalized capability 字段，导致 immutable snapshot capability digest 在真实路径恒不一致。提交 `02a61094f` 从已冻结 snapshot 的 `rehydrationContext` 恢复 allowed tools、Ponytail/skill-disable、Matt skills source presence、workflow submission 与 founder-review flags，再执行既有 digest 校验；缺失 rehydration context 继续 fail closed。真实 Bridge 形状用例先红后绿，adapter 142 PASS、相关 3 files / 160 PASS，Claude runner typecheck、受影响包及依赖 build、repo lint（exit 0，仅既有警告）与 diff check 全绿。新的 literal-last exact head 必须开新 scoped review，旧 review 不复用；仍不请求 full CI、不 merge、不 deploy、不派 QA。

后续 exact-head review 发现同一 Bridge 形状还会省略 loop-target plumbing；这会让常见的 QA→implement rework 在 snapshot 的 `loopTargetNodeId` 上继续拒绝恢复。提交 `36618356b` 让 process-resume 仅以 immutable snapshot 重建该比较输入，同时不在身份核验前安装第二套 resident lifecycle（durable process lifecycle 已拥有本次 launch，holder 仍按既定顺序在核验后激活）。implement loop-target 用例先红后绿；adapter 142 PASS、相关 3 files / 160 PASS，Claude runner typecheck、受影响包及依赖 build、repo lint（exit 0，仅既有警告）与 diff check 全绿。新的 literal-last exact head 需重新 scoped review；旧 verdict 不复用，full CI 与真机 continuity 仍交 QA。

QA claim 1508 在旧头 `29d3afe85` 真机「叫回来」Claude 0/1、Codex 0/2。本轮五处先红后绿：Claude SessionStart 身份 hook 改用 POST（真 hook → 真 HookCallbackServer 用例，修前 405 红）；Codex 拉起请求补原 Lead（CommDB 原始登记）与原节点，恢复 CommDB 可写根并 admit 线程所在 agent home，核验前失败立即以 `launch_snapshot_mismatch:<字段>` 回报并写日志；fly1560 守卫注释改词；会话被前任退场随最后窗口销毁时，后继只在会话确实不在时重建一次，退场判定把 `can't find session:` 视为窗口已不在；`retiring|standby|resuming|resume_failed` body 在 enrolled 与 legacy 两个 sink 里都保留 `ship_parked`，清理锁存下 attempt 仍记真实原因、期望身份与耗时。点名测试：HookCallbackServer 27、Blueprint fly1356/fly887/fly2147 93、Claude runner Tmux/Codex/process-retirement/kill-path 338、teamlead StateStore/DirectEventSink/coordinator/resume-identity/close-runner/reopen/fly1560/census 等 270+ PASS；13-package build、两包 typecheck、5178-file lint 全绿。disposition 见 follow-ups.md。本节点不请求 full CI、不跑 529、不 merge、不 deploy；当前 literal-last head 只请求 scoped code review。

founder 按卡后 land 发现 PR #1299 与 main 冲突。本轮只以 merge commit `e2f033c2d` 同步 `origin/main@59123848a`（未 rebase、未 force-push），不改其它行为。唯一真冲突在 `CodexTmuxAdapter.ts`（FLY-2877 #1327「Codex home lease 与 codex 进程同生同灭」）：保留本单的 `snapshotExecution` 与 2877 的 `RetireOptions`/`holdOwnershipUntil`；收尾顺序采用 2877 的「drain 前先杀 founder 窗口 → 等 holder 退出 → 退 lease（迟到 TUI 窗口时保留 lease）」，两条分支上退 lease 之后的重复杀窗口一并去掉，因此 `founderWindowTeardownAttempted` 不再有读者而删除；本单「退场已批准时 founder 窗口必须读到 absent，present 判退场失败、unknown 判未确认」的核验原样保留。其余 7 个自动合并文件（truth.ts、Blueprint.ts、codex-session-reown.ts、plugin.ts 与对应测试）逐行核对为两边增删的精确并集。点名测试：claude-runner CodexTmuxAdapter/codex-home/codex-process-snapshot/index-exports 396、config flag drift/truth/registry/ledgers 125、edge-worker Blueprint fly1356/fly887 81、teamlead codex-session-reown/wiring/orphan-reaper/home-isolation 106 PASS，test-deploy-generalized shell 210 PASS；teamlead 及依赖 13-package build、5209-file lint（exit 0，仅既有 warning）通过。本节点不请求 full CI、不 merge、不 deploy、不派 QA；当前 literal-last head 只请求 scoped code review。
