# FLY-2808 — 全节点退下与原会话拉起

**Status**: 🔎 Pending exact-head code review
**PR**: #1299
**Date**: 2026-09-23

定稿并按 Lead 后续授权实现所有 DAG 节点、Claude/Codex 共用的主动退下与意外死亡区分：持久 process-body 代数状态、六处死亡消费者 guard、精确原会话/model/cwd 恢复核验、失败可读与一次 fresh fallback、恢复/fault 独立预算，以及 founder 的 working / standby / problem 三态。实现默认关闭；未部署、未启用生产、未派 QA。

R2 的三个 HIGH 已修：恢复复用原共享工作树，失败/超时清理已启动进程，`resuming` 具有 180 秒 lease 与过期接管；退休投影、Codex strict identity、终态显示、CAS 结果及死 process body 等 MEDIUM 同步收口。当前本地证据：teamlead related 570/570 files、8029 PASS/4 个既有 SKIP，adapter/core/edge related 绿，affected build 与 core 下游 typecheck 绿，`pnpm lint` 退出 0。以上不是 full CI 或 QA。

保留 LOW follow-up：同 worktree 串行/跨 worktree 最大 2 的 limiter，以及首个模型消费时点的 `queueMs`/`totalMs` receipt。下一步只请求当前 literal-last head 的有效 code review；不请求 full CI、不 merge、不 deploy。
