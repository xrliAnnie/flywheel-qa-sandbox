# FLY-2808 — 全节点退下与原会话拉起

**Status**: 🔎 Pending R5 exact-head code review after QA rework
**PR**: #1299
**Date**: 2026-09-23

定稿并按 Lead 后续授权实现所有 DAG 节点、Claude/Codex 共用的主动退下与意外死亡区分：持久 process-body 代数状态、六处死亡消费者 guard、精确原会话/model/cwd 恢复核验、失败可读与一次 fresh fallback、恢复/fault 独立预算，以及 founder 的 working / standby / problem 三态。实现默认关闭；未部署、未启用生产、未派 QA。

R2 与 R3 的 HIGH 已修：恢复复用原共享工作树；失败进程只在精确 generation/demand/owner fence 下做物理 cleanup，保留 lifecycle/CommDB 记录并证明 execution-wide death；resume 同时观察 launch outcome 与身份回报；`resuming` 具有 5 分钟 lease，长于 180 秒身份超时。退休投影、Codex strict identity、终态显示、CAS 结果、死 process body 与 adapter callback 隔离也已收口。R4 在 `a351d044b` 通过后，QA 的 full exact-head CI run `35862850957` 仍为红；本轮只修其中可归因的 flag-governance 回归：`FLYWHEEL_NODE_STANDBY_RESUME` 进入统一 default-off flag store，fresh/rework admission 都在调用时读同一 resolver，`FLYWHEEL_CLAUDE_SESSION_DIR` 明确分类为路径 plumbing。

当前 rework 本地证据：config focused 113 PASS、related 21 files / 380 PASS；teamlead focused 290 PASS/1 个既有 SKIP、related 575 files / 8083 PASS/4 个既有 SKIP；旧 CI 的真实 tmux 精确用例 1 PASS；affected 13-package build、13 个 config 反向依赖 typecheck、`pnpm lint` 均绿。未改动的 load-probe 本机运行遇到与 CI 不同的宿主进程身份失败并超时，诊断 run 已终止；没有修改无关脚本。以上仍不是 full CI 或 QA 通过证据。

保留 R4 的 MEDIUM follow-up：`cleanup_unconfirmed` 缺 Lead 可审计 reopen，founder `canResume` 需与 latch 语义同步；保留 LOW follow-up：同 worktree 串行/跨 worktree 最大 2 的 limiter，以及首个模型消费时点的 `queueMs`/`totalMs` receipt。下一步只请求当前 literal-last head 的有效 R5 code review；不请求 full CI、不 merge、不 deploy。
