# FLY-2145 — Lead 记忆私有仓首搬

**Status**: ⏳ Needs independent QA
**PR**: #1064
**Date**: 2026-09-03

已建立单一永久私有 GitHub 仓 `xrliAnnie/lead-memory`，按 Lead 分成 12 个目录。首搬提交
`0d5c497bcbfeee1bd111ca6ad2f5f42d06287609` 包含 1145 个记忆文件、27.8 MB；gitleaks
8.30.1 与 TruffleHog 3.97.2 对不可变 Git tree 的终扫均为 0 命中，确定性阳性对照分别
8/8 与 4/4 命中，Lead 对当次 blob 绑定的 36/36 抽样逐条亲审。扫描台账不含 secret
值并随仓保存。鉴权 API 确认仓为 private，匿名访问返回 404。

一次性 fresh clone 重建出精确 12 夹和相同扫描 tree，跨夹读取成功；普通 commit、
`commit --no-verify`、pre-push 与 CI `check-range` 均拒绝写入别家目录，远端 SHA 保持
不变；自家夹正向写入与清理均通过 GitHub Actions。该护栏是事故防护和审计，不冒充
Git/GitHub 的目录级权限边界。

## 正确 clone 入口返工

此前的一次性 fresh clone 没有触碰 canonical live path，因此**不等价于**
`bootstrap.sh --clone` 的换位路径。2026-09-04，Lead 在已有 live 目录的机器上运行旧脚本，
rc=0，逐字输出：

```text
lead-memory-bootstrap: cloned /Users/xiaorongli/.claude/agent-memory
lead-memory-bootstrap: previous directory preserved at /Users/xiaorongli/.claude/agent-memory.pre-clone-20260904T033324Z-210
```

这不是数据丢失：原目录完整保存在上述 backup；但 canonical path 被 557 文件的导入快照
无提示顶换，换位前 live 目录有 566 文件，并包含更新的
`reference_codex_login_status_is_not_a_credential_probe.md`、
`feedback_quiet_wait_kills_codex_body.md` 和 `reference_claude_accounts_roster.md`。Lead 已手工
恢复并逐项验证 566 文件及三处更新，确认零丢失；诊断 clone 保留为
`~/.claude/agent-memory.clone-artifact-<timestamp>`。

提交 `c0f2611d4` 增加最小护栏：validated snapshot 与已有目录不同或可能含更新 live state
时，在任何 `mv` 前打印 WARNING 和精确 backup path，并要求输入精确 `REPLACE`；EOF 或
其他输入删除临时 clone、保持 live 原样。比较异常也 fail-closed。没有新 bypass、没有自动
merge，原 backup/swap rollback 语义不变。提交 `869351e2d` 补齐比较器异常负向测试。

严格 TDD 先复现旧脚本无输入仍成功顶换；GREEN 覆盖拒绝、显式确认、精确路径和清理；
mutation 阳照仅中和确认调用后再次复现无 WARNING/无确认顶换，并证明原目录仍在 backup。
bootstrap 最终 56/56，guard 56/56，hooks 23/23，scan 21/21，real scan、workflow、CI
structure、shellcheck 与 shell 语法检查均通过。

## 最终门禁与边界

`pnpm lint`、`pnpm -r build` 通过。resident 的 `pnpm test:packages:run` 仅在未修改的
Terminal.app AppleEvents 两用例失败（core 219/221），如实保留宿主输出。PR run
[33835489090](https://github.com/xrliAnnie/flywheel/actions/runs/33835489090) 中本单所在
`Script Tests 4/4` 与其余 shell jobs、Quick Gate、NPM、Unit light、三个 teamlead shards
全部通过。唯一红灯 `Unit (heavy)` 是 5 个 edge-worker Blueprint golden：Lead 独立确认
它来自当前 main 的 #1067 × #1056 语义合并冲突，本分支 `packages/` 改动为 0；修复已归
FLY-2318，本单按明确裁决不修改这些逐字节守卫，也不把它写成环境问题或本单绿灯。

request-driven cross-family review 在 implementation code head `6b3493ede` 返回
**APPROVED**，无 HIGH/blocking finding；milestone 作为字面最后提交后仍会重新注册 final-head
review，只有通过才交 `needs_review`。新 confirmation bootstrap 与 README 暂不发布到私仓：
Lead 明确顺序为 implementation → 独立 QA（含 mutation 阳照）→ Lead 发布并验证两个文件
哈希，实施节点不提前执行该受控管理变更。

`sub-lead/assets` 约 17.7 MB 媒体不在 secret scanners 的语义覆盖内，也未进入 36 个人审
样本；只按已批准范围放在永久私仓，不宣称已扫描/已人审。clone sibling ignore、gitleaks
临时路径指纹、scan live index、archive path-set 四个 MEDIUM 与诊断/清理类 LOW 均已在
PR 正文逐项写明决定、理由与影响并回报 Lead，没有伪装为已修复。

真第二台机器的 canonical-path 执行仍是 founder follow-up；临时 fresh clone 与本机正确入口
实跑是两类证据，不再称前者等价覆盖后者。
