# FLY-2029 — Raya V1 地基

**Status**: ⏳ Pending ship
**PR**: #960
**Date**: 2026-08-27

建立独立的 Raya code / memory 仓、隔离 Codex 身份、`#raya` Discord 身份面、可在登录/重启后
从 `~/Library/LaunchAgents` 自愈注册的 launchd runtime，以及 RSS / swap / actual-context
试用证据合同。Foundation code 的 exact head `0b954db` 已通过 code review 与 53 tests；brain / voice
plist 已持久安装，brain 的 bootout + LaunchAgents bootstrap 模拟后为 running。真实 reboot 留待
founder 下次正常重启后由 Lead 核验。

Foundation code 和 memory 分别由 `xrliAnnie/raya#1`、`xrliAnnie/raya-memory#1` 交付；
`apps/voice` 与真实进房出声 QA 按 founder 拆分由 FLY-2074 承接。
