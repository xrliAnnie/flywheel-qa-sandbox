# FLY-108 implementation milestone

**Issue**: FLY-108 — Session status 不 flip — Runner `session_completed` 两类 bug
**Date**: 2026-09-01
**PR**: pending

## Delivered scope

- Preserved the approved as-built design boundary: the functional FLY-108 fix is already inherited from `main`, so this implementation branch adds the durable exploration, research, approved plan, founder report, and verification ledger without duplicating or redesigning runtime code.
- Verified the Runner completion contract: `flywheel-comm complete` requires a valid route, emits a populated `session_completed` payload, retries transient delivery failures, and writes fail-close evidence when delivery is exhausted.
- Verified the Bridge completion contract: empty or foreign routes cannot bypass the FSM; the approved-to-ship natural completion and narrow stage-completed recovery paths retain their legal transitions and exactly-once post-ship finalization.
- Kept the approved follow-up boundary intact: DirectEventSink parity coverage remains explicitly deferred rather than being silently broadened into this implementation node.

## Verification evidence

- `pnpm lint`: exit 0 with zero errors and 14 pre-existing warnings.
- `pnpm -r build`: exit 0 for all workspace packages after `pnpm install --frozen-lockfile` restored the initially absent dependency tree; tracked source and the lockfile remained unchanged.
- `packages/flywheel-comm/src/__tests__/complete.test.ts`: 28/28 passed.
- Focused TeamLead FLY-108 route, guard, finalization, marker, and integration coverage: 195/195 passed with the host's live workflow-claims toggle isolated.
- Exact `pnpm test:packages:run`: invoked unchanged and exited 1 because this managed macOS runner cannot use the real Terminal AppleEvents service. A follow-up isolated replay reached 7,048 passing TeamLead tests, including the FLY-108 Variant B exactly-once finalization case, before unrelated legacy suites hit sandbox-denied `ps`/`pgrep`, BSD system-temp, and npm-cache access. Authoritative Linux CI remains the full-suite control.
- New `scripts/__tests__/*.test.sh` files in this PR: none.

## Review evidence and infrastructure holds

- The mandated Codex rescue review was attempted through both the companion lane and `codex-with-fallback`; both failed before a model session started because nested `sandbox-exec` is forbidden by the managed runner sandbox.
- Review gate `07464b36-baa6-43f8-b16b-8c74babd25b0` was opened, but Bridge reviewer registration request `9fd3456f-af40-44cd-b2e6-6798ccc660a3` is fail-closed while `http://localhost:19873` is unavailable. No unregistered gate is being treated as approved.
- Lead disposition questions `9c563ea1-4e28-425b-9f08-6052bfb3a093` and `3d40524e-0338-4e84-b4bb-e033733686a6` remain pending at milestone creation.

## Handoff boundary

This implementation node owns the docs-only verification branch, milestone, PR creation, and mandatory review registration retry. It does not dispatch QA, request ship approval, merge, deploy, or restart Bridge/Lead services.
