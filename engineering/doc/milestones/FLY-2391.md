# FLY-2391 — Customer release with a single founder veto window

**Status**: ⏳ Pending review and ship
**PR**: https://github.com/xrliAnnie/flywheel/pull/1209
**Date**: 2026-09-15

Implement B4 customer auto-release: immutable prepared artifact, verified one-time notice, durable founder veto/manual go, fresh fail-closed decision, narrow conditional executor and unknown/fence recovery. Linear/GitHub audit projections use fixed activation-bound records and independent readback. Daily reports remain read-only. The feature is off by default; no production activation, release, merge or service restart was performed.

The approved-plan audit, validation receipts and activation/rollback runbook are in `engineering/doc/FLY-2391-auto-release-veto/`. Rendered digest verification remains an explicit QA-host handoff gap per Lead; the committed static fixture generator reproduces it. Final effective review and exact-head CI are required before implement handoff; ship and founder activation remain separate authorities.

CI rework fixes workflow input references, registers the unshipped flag as `auto_release_on_silence_enabled` without relaxing provenance validation, and counts all ten protected B4 tables. Focused teamlead/config tests each passed 76/76; workflow startup, lint and full build passed. Exact-head CI and effective review remain pending.

Final test correction aligns workflow recovery-title assertions with their declared inputs. All locally reproducible Quick Gate run steps passed, including root Node suites, build, typecheck, lint and retention; release workflow structure passed 25/25. The next exact-head CI and review still determine handoff readiness.

R2 HIGH repairs add time-partitioned ready-attempt discovery and isolate Bug-source health to the canonical activation probe. The 5-second freshness/limit and fail-closed source-loss semantics remain; ordinary issue label lookups cannot mutate release health. Endpoint 217/217, release/readiness/route 544/544 and local Quick Gate passed. R3 effective review and new exact-head CI remain required; production activation is still off.

Engine conflict rework merges main `156601a2e` into the approved B4 head. Both StateStore additions and all shutdown handlers are retained; the merged retention inventory contains 241 tables (238 after excluding the three retired tables). Endpoint tests passed 217/217, release workflow structure 25/25, and CI structure passed. Full build, package aggregate, and fresh exact-head review/CI are in progress; no activation authority is implied.

Post-sync review repairs use the B3 14-day evidence window for pending Bug invalidation and preserve founder-confirmed manual work when only automatic release is disabled. Gateway/restart cancellation remains intact. Deterministic boundary and lifecycle regressions went red before the repair; focused verification passed 123/123. Final-head review/CI remain required. The host aggregate was interrupted under contention and is not relied on.

Post-#1199 synchronization merges main `84a65da2e` and preserves both flag/inventory changes; retention counts are 243 total / 240 without retired tables. Automatic advance failures now preserve manual-ready work while cancelling automatic cycles. Build/lint and focused groups 850/646/5/217/82 plus shell groups 19/44/22 passed; CI structure and render generation passed. Local workflow-structure exclusion follows Lead ruling 40bad27b; exact-head CI and effective review remain required. No production activation.

Land-conflict rework merges main `6556b0751` once at `adb038597`. Preserve the new retention fragment loader and independent schema checks; migrate all ten B4 authority tables into protected fragments and retain their explicit assertion. No release behavior change. Recursive build and lint passed (22 warnings); targeted retention 65/65, release/readiness 459/459, kill inventory 5/5, Node retention 12/12, and CI structure passed. Receipt: `land-conflict-validation.json`. Fresh exact-head review, CI and QA remain required; production activation stays off.

Post-conflict review HIGH repair: cache `StateStore.customerReleases` by database connection identity, preventing host session restart on each tick while refreshing the wrapper when the database changes. The production-shaped regression first observed three restarts instead of one, then passed with owner-rotation coverage and stopped-host database-reopen identity coverage. Host/runtime/lifecycle 12/12, recursive build and lint passed (22 warnings). MEDIUM/LOW and the separate live database-recovery follow-up are reported. New exact-head review and CI remain required; activation stays off.
