# FLY-2101 — Batch 1 运行时 flag 固化删

**Status**: ⏳ Pending ship
**PR**: #974
**Date**: 2026-08-27

13 个从未在生产被调整的 runtime env flag 已按现值固化，registry 与源码读点清零，
`LEGACY_UNMANAGED_BASELINE` 从 31 项只减到 18 项。`mailbox_queue` 固化常开后，FLY-1572
旧 push 投递、单条 ACK 和部署 barrier 子系统一并净删；merge approval 门改为无全局旁路，
supersede 只保留 enforce 路径。改动净删约 4.1k 行，没有新增旋钮或依赖。

全仓 lint 与 build 通过；13 个 token、registry 名和旧 mailbox API 的源码扫描均为零命中，
restart-services、flag truth、FLY-1674 residue 守卫全绿。全包执行中只有真实 Terminal.app
集成受当前 HIServices XPC 环境限制，其余 core 219 条通过；teamlead 的并发超时/npm cache
噪声均已逐项隔离复跑转绿。部署时由 Lead 删除生产 `.env` 中残留的 mailbox queue 行及
可能存在的旧 barrier marker，随常规班车上线，不投紧急重启票。
