# FLY-2396 — founder 门 exact head 与作者事实

**Status**: ✅ Implementation complete; final exact-head review and CI required
**PR**: #1114
**Date**: 2026-09-06

## 交付

- 新增不可变 `workflow_founder_gate_verdict` 台账；每个写入行都在原判决事务中冻结 `(repo, pr_number, head_sha)`，并由外键、CHECK、digest、禁止更新/删除触发器保护。
- `founder_authored` 与 `authority` 分离：直写响应使用 capture 时 founder ID，operator 引用使用 Discord 作者、thread、card 与时序核验；不读取或猜测 `founder_feedback_verbatim`。
- 活跃 `land / git_head` founder gate 的 approve、feedback、carryover 与可解析 operator 路径写 exact-head verdict；新表没有任何 merge / ship 放行读者。
- 只读 WAL-safe 回溯报出冻结 55 条中的 54 条有 head、53 条有完整三元组；全集 63 条为 62/61。作者事实为 5 attested / 58 未判定 / 0 猜测，五个阳性对照为 1,1,0,0,0。
- post-cutoff 缺少 exact-head ledger 的判决有独立且始终打印的 `unrecorded` 桶；当前只读副本为 3 条（3 approval / 0 rework），逐项列出 claim identity，并明确 `missing = unknown`。
- 规格复现值保留为 358/358 与 0；最终 live 副本增长到 427/427 与 0，作为 drift 分开报告。既存 FLY-2006 FK 基线为 7 registered / 0 outside。

## 验证与评审

- TDD 聚焦行为 75/75、首轮 code-review 回归文件 25/25、授权隔离与 retention 24/24；TeamLead typecheck、根 lint、21-workspace build 全绿。
- retro report 13/13；两个 `NOT EXISTS` 分支各有一条已记账负向对照，分别做过 mutation proof，错误关联会稳定变红。
- 精确 `pnpm test:packages:run` 已多轮运行。pre-rebase `ce7bff6e5` 的 CI 除两条 TeamLead 测试外其余 11 个真实 job 通过；其中一条是本分支响应合同导致的确定性旧断言，不是宿主负载问题，另一条是测试未声明的 `rg` 二进制依赖。两条均以纯测试改动修复（pre-rebase `220f4eb4d41f48c1de272e0dae0e7fb57a21d158`，rebase 后 `1082dc403`）；相关 9 文件聚焦复跑 143/143，另有 1 条既有 skip。此前未改动的 `flywheel-comm` 本地固定 5 秒进程超时仅作为独立宿主负载观测保留，不再用于解释上述确定性红灯。
- 去除 `rg` 后做了真变异证明：临时加入读取 `founder_authored` 的 `land-executor` 源文件时守卫按预期变红并精确列出该文件，移除后复绿。全仓消费方审计只找到 route 响应生产者、endpoint metadata 与测试，没有 dashboard、Lead 脚本或其它旧响应形状读取者；新响应合同的批准出处为 `plan.md` §3.3 第 4 步。
- 与 `origin/main=010e5d9c760b4a36b51c78923bf705132ec3baed` 的 retention 漂移检测器冲突按真实并集合并：39 个 authority 表含 `workflow_founder_gate_verdict`，117 个 current/reference 表含 main 新增的两个 `ship_relevant_*` 表，fixture 总数 176（排除 3 个 optional-retired 后 173）。临时在真实 StateStore schema 新建未登记的 `fly2396_rebase_mutant` 表时测试以 `schema_unclassified:teamlead:fly2396_rebase_mutant` 变红，移除后 23/23 复绿。
- rebase 后重新执行根 `pnpm lint`（仅既有 warning）与 22-workspace `pnpm -r build`，均通过。精确 `pnpm test:packages:run` 在 5 分半高负载的 `claude-runner` profile suite 后出现两条固定超时（500ms async exec、5s real-tmux overflow），本 milestone 不把该轮记为全绿；两个未改文件隔离复跑 9/9 通过。完成仍以最终 binding head 的 GitHub CI 全绿为硬条件。
- request-driven cross-family review R1 在 `092e24a80989bcb1918eba5a3e08d471412363f5` 找到 12 个旧审批路径回归；修复后 R2 在 exact implementation head `a2fafc9602b91d84cbe80e7c9775a16ea10ccc12` 返回 `APPROVED`。Lead 指定的 unrecorded 报告与负向对照随后分别通过 R3；R4 在 exact head `e3951630578d7b1d59e7a44438d18bb7a00b1993` 返回 `APPROVED`。确定性 CI 测试修复后，R5 在 pre-rebase exact head `220f4eb4d41f48c1de272e0dae0e7fb57a21d158` 再次返回 `APPROVED`，无 blocking finding。最终 binding head 只允许在 rebased implementation 上增加本 milestone 文件；推送后须再做 fresh exact-head review，并以同一 binding head 的绿色 CI run 作为完成证据。

## 边界

既有非 `land` 的 `runner_ship / engine_terminal` approval 与无引用 operator recovery 行为保持不变，因此不会为了写事实台账而改变 merge 授权合同；这些休眠路径可能没有 verdict 行，且非 land feedback 仍严格 fail-closed。缺行现由独立汇总及逐项明细显式暴露，B4 消费方不得把「无行」解释成 author 结论。feedback 对称性及 land 后 founder-message 引用按 Lead 裁定保留为后续项。

本实现没有修改获批 plan、`CLAUDE.md`、线上数据、secrets 或任何自动放行条件，也没有 dispatch QA、请求 ship approval、merge、deploy 或重启服务。
