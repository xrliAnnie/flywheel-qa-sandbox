# FLY-2508 — beta 取源策略与本机部署身份

**Status**: ⏳ Pending review and CI; runtime helper deferred
**PR**: 本分支 PR，创建后由交付回执关联
**Date**: 2026-09-13

新增 beta_release.source_commit 策略、冻结 source_origin、默认分支祖先守卫和管理台当前配置标签。默认策略保持原行为；本机取源缺失或非法时 fail-closed，不回退 main HEAD。配置、迁移、重启、调度、回执身份和 DOM 定向测试已通过。

B3 真 helper 接线待 #1156 合入后由 Lead 开 rework 接续（指令 8e2111e8-ca3a-4fab-9ab5-9dbb3d9d54d6）。当前 runtime 未注入读取器，本机策略会进入 beta_source_unavailable；本 PR 不构成真实 beta 对齐验收。后续 QA 仍需真实 published/no_change 回执及 B3 同 SHA 证据，covered_by_newer 不算通过。

本轮 lint/build exit 0，完整包测试仍在执行。此前完整包与 teamlead 失败原始回执保留在 implementation.md；Lead 允许最终 HEAD CI 裁定，DOM 用于评审，真实浏览器验收交 QA。没有部署、接管或合并。

本文件为开 PR 前 literal 最后 commit；随后冻结 HEAD 做代码评审、CI 和 needs_review 交接。
