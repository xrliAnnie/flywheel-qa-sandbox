# FLY-2399 — 自动审批三点意见与学习闭环

**Status**: ⏳ Pending CI and QA
**PR**: 本分支 PR，创建后由交付回执关联
**Date**: 2026-09-11

Flywheel dry_run ship 卡增加需求对齐、冲突和 QA 覆盖意见；冻结输入、限制模型与刷新预算，以只追加可见回执配对 founder 决定，并提供分歧澄清、进度预览和分页历史。不改变 R1、auto 批准、merge/updater 契约或生产开关；一键 revert 不在批准范围内。

R1 在 beaed3e6a 上 effective/reviewer APPROVED，17 条 advisory 完整归档且未修。后继仅文档与两份精确清单：census 一条、retention consumer 五条；运行代码不变。完整 Quick Gate 28 条命令 exit 0，census RED/GREEN 和 retention 守卫通过。本地全量包 R3 仍为失败回执，精确头 CI 与独立 QA 分别验收，不以聚焦绿代替。

本文件为开 PR 前 literal 最后提交。实现交接走 needs_review，不合并、不部署、不派发 QA。

Lead 后续将 off 行为升级为 blocking：off 停止学习观察、写入和投递，迟到 fetch 不落库，待发 ack/恢复扫描暂停到重新启用。33 项学习/runtime 回归通过；32 项取消用例验证 off 零写入后恢复原归因；完整 Quick Gate 第三轮 exit 0。表数三处确认 main 的 144/205/205 + 八表 = 152/213/213；与 #1156/#1160 后落地的分支需重新核对金标。最终头仍须 review/CI 通过后才交接。

R2 唯一 HIGH 为 Bridge 测试遗留 off 发送预期，已仅修测试：off 零网络/零写入后再切 dry_run 验证原发送上限及缺 bot 分支；页面预算单用例按 Lead 批准设 15000ms，断言未改。两文件十项通过，生产源码与 87aab52be 相同。旧 CI 的两个红分片保留，等待最终头 review/CI 后交接。

R2 后续裁定要求 off 保留本地 setMode 账务收口：既有意见显示 history，原消息不变；不观察、不发网络、不调用模型，重复 off 不增加写入。该项 RED→GREEN，相关 76 项通过，最终完整 Quick Gate 28 条命令 exit 0。剩余六条 R2 advisory 不修。统一推送后登记最后获准的 R3，非 APPROVED 报 Lead，不开 R4。

QA attempt 1 在 a4d8c0b05 判 FAIL 后，本轮按 Lead 限定完成 F1 timer/start 异常隔离、F2 CLI 参数/身份环境/envelope 兼容、F3 映射与覆盖的配对变异负例。真实子进程两项 RED exit 1 → GREEN 下一 tick 继续；10 项聚焦与 4 项脚本通过。4 次真实 CLI 顺序调用均 evaluated，C02/补充正例 pass/pass，C03 fail/fail，C01 按实际缺资料保留 unknown。输入/原始输出及边界见 rework.md。真模型变异复测依裁定 55ae7905 归 QA。完整 Quick Gate exit 0；宿主全包套件依 Lead c85c5f46 只归档、不等待它交接，以精确头 CI 为准。此提交为返工推送前 literal 最后提交；随后 R1 code review、精确头 CI、needs_review，评审期间不推送。

返工 R1 在 dfb394214 为 CHANGES_REQUESTED，唯一 HIGH 是 Epic 主 JSON 内联完整机器证据无上限。R2 提交 b9c61c721 将每项证据限制 2048 UTF-8 字节，超限明确标识并保留审计 ID，完整原始数据仍可只读查询；真实 StateStore 60 项大证据 RED→GREEN，393 项 Epic 回归、4 项进程/变异脚本通过，完整 Quick Gate 28 命令 exit 0。4 条 advisory 原样归档不处理。宿主全包明确为「Lead 主动结束，未采信」，不重跑。此为 R2 推送前最后提交，随后新头 R2 review/CI；旧头 CI 的网络失败及一次重跑不冒充新头证据。

QA attempt 2 在 44b17c33b 暴露 F4 用户配置泄漏后，仅修这一项（8ff42e346）：独立 HOME/config 仅复制订阅认证，固定 language/thinking/effort，完整 settings、argv 与目录白名单进入 digest，认证轮换字节除外。真实 CLI A/B RED 英文/中文 → GREEN 都为英文；默认认证完整 evaluator 正例 pass/pass。本轮真实调用 5 次、零重试，197 项回归与 4 项脚本通过，完整 Quick Gate 28 命令 exit 0。材料 rework-f4.md 与 rework-f4-fixtures；原用户设置未修改。宿主全包仍为「Lead 主动结束，未采信」，依令不重跑。此为 F4 推送前 literal 最后提交，随后重置 R1 review 与新精确头 CI，成功后 needs_review；其它 advisory 不修。

2026-09-13 重起 run：按 Lead e7f5f8c5-07f4-4058-b128-1ad401083aa9，仅合入 main 0c923c697（merge 37c472e6b）。四冲突保留双方 imports/表/消费者，表数 216；未扩功能。lint exit0；冻结锁文件安装补齐 main 新增依赖后 pnpm -r build exit0；231 判断/保留策略 + 129 旧批准路径 + 17 脚本通过。全包 exit1（voice-core 800ms 子进程 stdout 空），按令仅隔离一次 6/6 pass，原全包红保留。详见 rework-sync.md。此为本轮推送 literal 最后提交；新头 review/CI 尚待，随后 needs_review 交 QA。

同步头2aca30140 CI34799185158为14/14 SUCCESS，但新review为CHANGES_REQUESTED。依Lead裁定28500a40-a595-4318-b030-01d9bac3866b，只修两条HIGH（51029ba91）：生产模型入口改真实claude、GitHub数字repository Link与slug资源等价解析且只消费页码。两个RED分别复现切号脚本误接与invalid_pagination，GREEN runtime5/github11；373项相关回归、17项脚本、lint及全仓build通过。其余17条advisory原样留档sync-r1-review.json；不扩面。全包本轮仍在claude-runner运行，最终结果由PR/Lead回执补充，不能记全绿；前轮全包红继续保留。此为本轮单次推送最后提交，随后新头review/CI与全包观察并行，齐备后needs_review交QA。

QA真实体积返工（Lead2642264b-6601-4e72-b8e4-6fa121df310f）：14e9cf265改20项分页/2MiB响应界限，单PR文件/身份失败记录不完整并继续采集，其余17条advisory不动。≥800KiB真实形状fixture与失败隔离/恢复RED→GREEN，378项相关回归、lint/build通过。只读真实API+内存StateStore整轮48.5s/109调用完成39/39 PR，#1163收齐203文件，快照61639B；无生产DB或模型调用。目标/同仓缺文件仍conflict unknown，异仓失败不污染本卡；项目级限流/预算仍关闭整轮。详见rework-volume.md和volume-live/sweep.json。本轮按令不重跑宿主全包，既有红回执保留；本文件为推送前最后提交，随后新头review/CI及needs_review交QA。

2026-09-14 engine 冲突返工：上线后每张 Flywheel dry_run ship 卡会看到一条需求对齐/在飞冲突/QA 覆盖机器意见，并将可见意见与 founder 决定配对进台账；线上自动批准仍只走既有纯文档窄口。57a875ef0 合 main fd9e3064d，8 处冲突保留双方能力；历史托管适配凭据绑定/轮换、gzip、异步提交，9dedf971d 与 f8e394dd2 补齐锁等待取消的 RED/GREEN 及类型校验。最终 pnpm -r build、pnpm lint exit0，typecheck exit0；266 聚焦、7 托管恢复、10 脚本测试通过。旧授权/页面525/526与全包comm2542通过/7超时的原红回执保留；单worker152/153，剩余comm与页面容量在15秒诊断窗口通过（实际2693/3713ms，未改测试文件/CI timeout）。容量cap69/521503B，60-child hardened462671B。详见rework-main-20260914.md。18条既有advisory不扩修；新头review/精确CI及QA必须另证。本文件为本轮推送的literal最后提交，随后仅走needs_review，不ship、不部署。

同轮第二次 main 同步：3d1e14d7e 推送后因 main 新合 #1156 无法启动 CI，45c110662 合入14866f7e5。三处相加冲突保留 release readiness 与 ship judgment；真实普查发现 Git 静默保留双方旧156，按 main156+本单8校正164保护/228总/225非retired。最终全仓build/lint exit0、retention consumer gate通过；迁移普查31/31、同轮其余store/history9/9通过。首次因新main编译产物尚未生成的模块加载错误及计时超时仅作诊断，不称GREEN；前段全包诚实边界继续有效。本文件仍为新冻结头literal最后提交，旧f85ec916审查仅绑定已被替代的3d1e14d7e，新头必须新review/CI后needs_review交QA。
