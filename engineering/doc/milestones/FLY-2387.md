# FLY-2387 — 锁定发布合同 v1

**Status**: ⏳ Implementation complete; final exact-head review + CI pending
**PR**: #1104
**Date**: 2026-09-06

## 交付

- 新增零运行时依赖的 `flywheel-release-contract`，集中定义 clean / beta 版本语法、
  `latest | beta` 两个通道、不可变 payload object key，以及 beta、release、veto 三组身份派生。
- 新增 closed-world manifest / customer-view JSON Schema、`C-0` 到 `C-10` 稳定错误码和关系校验器；
  六个有效 fixture、shape / relation 反例 corpus 与文档内 canonical 示例均执行同一套验证。
- payload endpoint、release scripts、package-onboard 与 activation / CI workflow 已改为消费中央合同；
  promote-commit 在 mutation-time 重派生绑定，已提交重试只校验固定身份并保持零写入。
- `packages/release-contract/CONTRACT.md` 固定版本、通道、manifest、发布不变量和 customer wire；
  REQ-0 逐条对照无违反项，PRD §7.3 与 FLY-1063 Option B 只引用，不在本单重写。

## TDD 与验证

- 版本 / schema / identity / validator / consumer contract 21/21 通过；共享 shell 派生向量 22/22 通过。
- payload release pipeline 34/34、workflow structure 19/19、package version injection 10/10、
  endpoint 64/64、cleanup 15/15、kill-path inventory 623/623 通过；Wrangler dry-run 证明 worker
  bundle 含中央合同源码。
- `pnpm lint` 通过（仅仓库既有 warnings）；`pnpm -r build` 的 22 个 workspace 全部通过；
  `git diff --check` 通过。
- 精确 `pnpm test:packages:run` 在当前 61-user、load average 约 203 的宿主机跑到 changed contract
  21/21 与 flywheel-comm 1984/1984 全通过后，被 Vitest worker RPC timeout 标记失败；此前 config
  772/772、voice-core 320/320 单包复跑通过。Lead 按 FLY-2406 接受这项本机资源 flake；完整
  aggregate 交由 PR exact-head CI 判定。
- 首轮 request-driven review 在 `846aec7d9` APPROVED；`afc662250` 修完全部 3 项 MEDIUM advisory
  与 4 项 LOW advisory，`6946d0746` 修复 exact-head CI 揭示的 kill-path inventory 漂移。本 milestone
  提交后只进行一次最终 exact-head review；该 review 或 CI 运行期间头保持冻结。

## 边界与交接

本单没有发布 payload、切换通道、写生产 manifest、添加 broker / credential、dispatch QA、请求 ship、
merge、deploy 或重启服务，也没有修改获批 plan 或 `CLAUDE.md`。B1 / B2 可仅依赖
`packages/release-contract/CONTRACT.md`、导出的类型与 schema 并行开工；本 milestone 所在 exact head
需通过 request-driven code review 后才进入 implement handoff。
