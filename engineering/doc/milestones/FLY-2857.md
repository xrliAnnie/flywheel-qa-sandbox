# FLY-2857 implementation milestone

**Issue**: FLY-2857 — QA-SBX FLY-2802 r13 cell A synthetic literal migration
**Date**: 2026-09-24
**Branch**: `project-slot-1-FLY-2857` (base `b60b3a143`)

## Delivered scope

- Migrated the exact model label `claude-opus-5` → `claude-opus-5.5` in `packages/runner-test-discipline-fixture`: 8 source labels in `src/{alpha,beta,gamma,delta}/model.ts`, plus every exact assertion of that label in the four model tests, `static-dependency.test.ts` and `literal-only.test.ts`.
- Left the near value `claude-opus-50`, the unrelated `claude-sonnet-5`, `unrelated.test.ts`, `verify.mjs` and `package.json` unchanged.
- Added a 6-line importer-only `pnpm-lock.yaml` entry for the fixture as a separate commit. It landed on main without one, so `pnpm install --frozen-lockfile` failed with `ERR_PNPM_OUTDATED_LOCKFILE`. Lead was notified through question `70c5b7ff-6610-46c4-8ad5-3e45d3e6d69e`, which was still unanswered at PR time.

## Verification evidence (targeted local artifacts, not full-suite evidence)

- Discovery: `git grep -lF` for the old and new literals, plus full-path, file-name and parent-dir searches for all 10 changed files. Retained 7 fixture test files. Excluded 3 teamlead tests that matched only the `model.js` file name; they import an unrelated `fleet-console-model.js`.
- RED: 25 assertions failed across 5 files with the source still on the old label. `literal-only` compares a literal with itself, so it stays green by construction.
- GREEN, one file per run: 34/34 across 7 files. `vitest related <changed files> --run`: exit 0, 34/34.
- `verify.mjs`: exit 0, `passed:true`, 0 old matches, 38 new matches, near and unrelated values preserved.
- `pnpm lint`: exit 0 (14 pre-existing warnings). The fixture has no build or typecheck surface. No new `scripts/__tests__/*.test.sh`.
- Code review: Codex via `codex:rescue` returned APPROVED, with one LOW note that reported no correctness issue.
- Full suite: owned by exact-head PR CI and not claimed here.

## Handoff boundary

The implement node owns the branch, PR, targeted evidence and review. QA owns the frozen-head full-CI request and the merge decision.
