# FLY-2616 — Provable land closeout and dead-owner recovery

**Status**: ⏳ Pending re-review and exact-head CI
**PR**: [#1224](https://github.com/xrliAnnie/flywheel/pull/1224)
**Date**: 2026-09-16

Land closeout now retains a durable execution/worktree inventory, classifies a runner as gone only when multi-source negative evidence has no live veto, and finalizes physically gone parked or sessionless runners through guarded CommDB receipts. A bound worktree whose verified path is already absent is recorded as cleaned without weakening branch/generation checks. Exhausted closeout attempts persist a Lead-readable escalation and an authenticated closeout-only retry door. Land-lane ownership is heartbeat-renewed and a dead local owner is reclaimed with an audit row and stale-owner effect fencing.

The four incident-derived, zero-network fixtures for 2391, 2588, 2413, and 2602 converge locally to completed operation/run state with local-double archive and Linear dispositions; live and unknown controls do not exercise those side effects. The real child-process lane drill reclaimed in 2,015 ms against a 10,000 ms limit and denied the stale owner. These are derived fixtures, not production or external-sandbox acceptance: the managed snapshot helper refused the production snapshot because its owner token was unavailable, and the two source databases exceed the 2 GiB combined execution cap. Discord `#cos-test` readback preflight succeeded, but `FLY-SBX-1` returned `Entity not found` for the injected Linear credential; the evidence therefore records `externalSandboxReadback.status=not_run` with null readback/call fields instead of fabricated values.

QA rework at code head `2b18a0bac` adds a red-to-green regression for the operation-audit branch: the generation-fenced audit transaction now records `archived_at` and clears `reopen_compensation_pending` atomically, including exact-receipt replay, so a closeout retry cannot unarchive an already archived thread. The two affected suites pass 93/93; TeamLead typecheck, changed-file Biome, `pnpm -r build`, and `pnpm lint` complete successfully (lint retains only pre-existing diagnostics outside this scope), and the refreshed local replay passes. The repository package gate is honestly red only in TeamLead: 1,237 files and 15,663 tests passed, 7 skipped, while two untouched files produced three load-sensitive failures (`not found` JSON parsing and a five-second migration timeout); immediate single-fork reruns passed 27/27 and 1/1. This is not aggregate green. Exact pushed-head CI and the new effective code-review verdict remain the authority before handoff; independent QA, real sandbox acceptance, production replay, ship-report publication, merge, and deployment remain separate downstream evidence.

Round-one code review at `cf160cca4` found one blocking retry defect: the workflow dispatcher re-probed already-cleaned worktrees even when the operation retained a complete v1 target snapshot. After merging `origin/main` at `5bad6f6df`, a focused regression reproduced the unwanted probe, then passed after the dispatcher reused the verified snapshot. Verification at code head `063ed6e1c` passed the complete dispatcher plus token-savings drift files (137 tests), the merged flywheel-comm conflict surface (38 tests), both package typechecks, and changed-file Biome. The review advisories remain non-blocking audit context; the final pushed head still requires effective re-review and exact-head CI.
