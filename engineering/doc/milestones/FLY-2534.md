# FLY-2534 — Beta workflow startup validation

**Status**: ⏳ Pending ship
**PR**: #1170
**Date**: 2026-09-13

Fix the unsupported runner context in beta job env and require checksum-pinned actionlint validation of every workflow through Quick Gate / CI OK. Preserve shared queue:max and receiver semantics. Focused workflow checks, lint and build pass; local package aggregate has two config timeouts. Exact-head review and GitHub CI are required before implementation handoff. No production release or deployment is included.
