# FLY-2404 — 全机共享一份 Codex 凭据

**Status**: ⏳ Pending ship
**PR**: #1112
**Date**: 2026-09-06

新建 runner Codex home 统一把 `auth.json` 链接到主机凭据真身；存量 runner、获批 Lead 与 Raya home 只在排空班车窗口通过同一套进程、lease、authority 栅栏迁移，并由全局健康探针、良性刷新竞争恢复和保守清理器守护。实现阶段没有切换或刷新生产凭据；部署后的一个完整班车周期健康验收留给独立 QA。

末轮实现复核补齐 `link-missing + --keep-backup`：缺失链接会在栅栏后恢复，但不会伪造不存在的备份。
三支新增 credential shell suite 已显式进入 CI `Script Tests 3/5`，并固定在 CI structure shard 顺序守卫中，不依赖本机手动执行。
首轮 CI 暴露的 Linux 权限断言与 packaged audit 闭包均已补齐，并由对应完整本地 suite 复验。
末轮评审指出的 access-token 有效期语义、pause ownership 栅栏以及四项防御性建议已批量修复并补回归测试。
两条 HIGH 的回归 fixture 全为伪造数据，并分别覆盖交叉过期 JWT 与真实/不匹配的 Bridge health 契约。
最终复审的 tokens 缺失 advisory 已按 Lead 裁决在本轮修复：无可用 token 结构时不再信任 `last_refresh`，而是进入可升级的 `truth-unparseable`。
