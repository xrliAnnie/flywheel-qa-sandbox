# FLY-2549 — Resolve restart preflight workspace imports from source

**Status**: ⏳ Pending code review and ship
**PR**: Created from branch `flywheel-FLY-2549` after this final milestone commit.
**Date**: 2026-09-14

The summary activation preflight now exports a dedicated TSX source map, inherited by its TeamLead validator child. It can validate new workspace exports while deployed dist remains old, without building or replacing live runtime artifacts before the stopped-service window. Exact mappings cover config, core, and comm/lead-identity. No production TypeScript, updater scheduling, R4 authority, restart order, or rollback behavior changes.

The real stale-dist regression fails against the old script and passes against the fix. Valid and stale receipt cases preserve fixture dist bytes and dependency links. Existing activation guards pass (9/9); source resolution and current-checkout real verify both pass. Lint, recursive build, CI structure/enumeration, and workflow-startup checks pass. The aggregate package command failed on unchanged baseline flywheel-comm tests (9 failures plus worker timeout); a single-worker rerun passed 150/151, with qa-result-lock still timing out. This is not an aggregate-green claim.

Evidence: `engineering/doc/FLY-2549-preflight-workspace-build/verification.md`. Code review and exact-head CI are subsequent independent receipts. After merge and normal fetch, the 12:00 PT shuttle is expected to self-heal this stale-dist blocker. Deployment has not been executed or observed by this implementation node.
