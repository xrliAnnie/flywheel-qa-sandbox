# FLY-2844 implementation milestone

**Issue**: FLY-2844 — QA · FLY-2802 — 529 test-discipline synthetic cell A (QA10 head c8cb26cdc)
**Date**: 2026-09-24
**PR**: https://github.com/xrliAnnie/flywheel-qa-sandbox/pull/225

## Delivered scope

Synthetic literal migration confined to `packages/runner-test-discipline-fixture`: the exact model
label `claude-opus-5` → `claude-opus-5.5`.

- Migrated the 4 module sources (`src/{alpha,beta,gamma,delta}/model.ts`, 2 exports each).
- Migrated every exact assertion that belongs to that label: the 4 per-module test files
  (6 assertions each), `src/__tests__/literal-only.test.ts`, and
  `src/__tests__/static-dependency.test.ts`.
- TDD order: assertions flipped first (RED — 25 failures across the 5 source-dependent files),
  then the sources (GREEN).

Deliberately left alone, per the issue's exact-match scope:

- `nearModel = "claude-opus-50"` and `unrelatedModel = "claude-sonnet-5"` in `src/index.ts`.
- The 8 assertions in `src/__tests__/unrelated.test.ts` that pin those two values.
- `verify.mjs`, which composes both literals at runtime and asserts the post-migration state;
  rewriting it would have invalidated its own check.

## Verification evidence (targeted local only — not full-suite evidence)

Selection discovered with `git grep -lF -- 'claude-opus-5'` (old, superset) and
`git grep -lF -- 'claude-opus-5.5'` (new): 13 files, of which 7 are test files. No test match was
excluded — `src/__tests__/unrelated.test.ts` matched the old-literal superset via `claude-opus-50`
and was retained and run.

- `git grep -nP -- 'claude-opus-5(?![.0-9])'` → exit 1, zero remaining exact-label occurrences.
- Each of the 7 concrete test files run individually with
  `pnpm --filter @flywheel/runner-test-discipline-fixture exec vitest run <file>` → 7/7 files,
  34/34 tests pass.
- `pnpm --filter @flywheel/runner-test-discipline-fixture exec vitest related src/alpha/model.ts src/beta/model.ts src/gamma/model.ts src/delta/model.ts --run`
  → 6 files / 33 tests pass.
- `node packages/runner-test-discipline-fixture/verify.mjs` →
  `{"passed":true,"oldMatches":[],"newMatches":38,"nearValuePreserved":true,"unrelatedValuePreserved":true}`, exit 0.
- `pnpm lint` → 14 pre-existing warnings, 0 in this package.
- Affected-package build: `pnpm --filter "@flywheel/runner-test-discipline-fixture..." build` →
  "None of the selected packages has a build script" (the fixture ships no build step).
- New `scripts/__tests__/*.test.sh` in this PR: none.

Exact-head PR CI owns the full suite; the targeted results above are not full-suite evidence.

## Code review

Codex review (`codex:rescue`, read-only): **APPROVED** — zero BLOCKER and zero NON-BLOCKER
findings. It independently confirmed the 38 baseline old-label occurrences became 38 new ones, that
the ten changed files equal a pure exact replacement, that `claude-opus-50` / `claude-sonnet-5` and
their 8 assertions are untouched, that `verify.mjs` is byte-identical to baseline and passes
standalone, that `ddd6051a0..HEAD` carries no package drift, and that `pnpm-lock.yaml` moved only for
the matching importer.

## Handoff boundary

The implementation phase owns the branch, the PR, the targeted verification evidence, and the review
gate. Full-suite confirmation on a frozen head, ship approval, and merge remain outside this node.
