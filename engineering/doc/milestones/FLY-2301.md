# FLY-2301 — 529 房支持 Codex 形状常驻 Lead

**Status**: ✅ Implementation complete; needs review
**PR**: #1057
**Date**: 2026-09-03

529 slot 常驻 Lead 台架现在按载体形状分流：既有 `claude-code` 继续使用原来的 wrapper + manifest + private `=main` tmux 验活；`codex-app-server` 使用 `/bin/bash <slot-wrapper>`、slot 独立 `CODEX_HOME` / state / CommDB / tmux server，以及 launchd PID + runtime argv + `CODEX_HOME` + heartbeat + 单一 TUI window 的组合验活。没有新增运行时开关，新增形状由 slot 的 Lead 载体声明直接选择。

## 交付内容

- 冻结 Claude plist、`.env`、per-Lead manifest、launch manifest、stdout 与两条日志基线；八个定向变异体先证明产物字节变化，再证明对应守卫必红。
- 新增 Codex plist、wrapper、环境投影、独立 home 铸造、state-root 注入、形状元组校验，以及 companion / full-access 两种 profile 的真实生产解析层验证。
- 新增 Codex 专用 topology / readiness 判据、KeepAlive `crash` 与 `kickstart` 演练、心跳字节归档和房外证据包。
- registry v2 与拆房只在含 Codex 条目时执行 runtime / daemon / updater / socket / private tmux 收敛；纯 Claude registry 继续走原有逐字节 fail-fast 循环。
- `test-deploy.sh` 的 main 或 extra Lead 均可承载 Codex 形状，并保证 home / state / workspace / label / TUI window 五坐标互异；当前真实白名单只有一个 source 且 lease 独占，因此一次真实 campaign 最多一条 Codex 行，roundtable 在产物前明确拒绝。
- Codex `.env` 将 `FLYWHEEL_COMM_DB` 固定在 slot 的 `state/comm/<project>/comm.db`；公开 `codexLead` JSON 仍保持冻结的七字段合同，不用自报路径代替真实环境验证。
- Codex source home 改为正向白名单：真实路径只接受精确的 `~/.codex-259-qa`，测试只接受精确命名的 `/tmp` fixture 路径类；`.codex-mufasa-med3` 与其它未列路径全部在写入前拒绝。
- 起房对 source credential 持独占 lease，并以真实 daemon start/stop 代替无区分力的 `codex login status`；start 成功必须同时证明 JSON PID、活进程 incarnation 与 control socket，stop 后必须证明同一 incarnation 消失。拆房在 runtime、daemon、精确 argv updater 全部收敛后才以 compare-and-swap 原子写回刷新后的 `auth.json`。
- 写回前要求 baseline 与 slot auth 都是非空 JSON object 且顶层 key 集合一致；截断、空文件或结构漂移报 `result=invalid`，与 CAS `result=conflict` 一样保留 source 原字节、lease 和 slot baseline，无重试且日志不含路径或 token。registry 注册失败和 EXIT cleanup 不再吞掉该诊断，清理未收敛时保留 slot / campaign lock。
- 未启动的已铸造条目可经 registry 安全回收 lease；一旦写入私有 launch-attempt marker，updater 零匹配就 fail-closed，argv 漂移不再被误报为成功清理。
- 与最新 `main` 的 slot report-host / deterministic TMPDIR 改动已合并；stdout 仍由单一可测 helper 渲染，同时保留 `bridgeTmpDir`、`reportsDir`、`reportHost` 与 Codex lifecycle 坐标。
- 更新 529 playbook、CI shell 枚举、路径卫生与 host-tmux source census。生产路径唯一行为改动是 `codex-lead.sh` 使用注入的 `FLYWHEEL_STATE_DIR`，未设置时保持原值。

## TDD 与验证

- 新增 shell suites 全绿：state-root 1/1、artifact fixtures 7/7、mutants 8/8、Codex layers 12/12、tmux isolation 4/4、drill guards 2/2、metadata snapshot 3/3。
- 核心 launchd suite 50/50；完整 hermetic deploy lifecycle 21/21（同时覆盖最新 report-host、同一 one-use source 的第二次成功起房、register rollback 诊断与 lock 保留）；multi-Lead 25/25；kill-path inventory 1/1（598 条，新增两条 updater fixture liveness probe 均为 `qa-only`）；CI shell enumeration 267/267、CI structure、path hygiene 与全部 shell `bash -n` 通过。
- 最终 Standards 复核发现 teardown 的 `FLYWHEEL_QA_STOP_POLLS` / `FLYWHEEL_QA_STOP_INTERVAL` 违背“不加旋钮”锁定合同；新增行为守卫先稳定红，再把四条生产收敛路径固定为 150×0.2 秒，测试加速仅留在测试内 `sleep` 替身。同步补齐 drill 每个 lifecycle 坐标的缺失/合法错配拒绝，以及 metadata 反向枚举仍逐字节同 hash 的守卫。
- teardown 的测试先后证明并修复：调用者不依赖 `FLYWHEEL_DIR`、kill-server 后 stale socket 收敛、生产 JSON daemon PID 解析、孤儿 updater 的精确身份清理，以及 socket 与父目录已消失时的幂等成功。
- F5 回归以消费过的 refresh-token ledger 模拟真实轮换：第一次 source preflight + slot runtime 消费并刷新 token，teardown 写回后第二间房才可启动；去掉写回会稳定复现 `refresh_token_reused` 形状。另有 burned-token、start 无 daemon 证据、false-success stop 留活进程、lease 冲突、CAS 冲突、空 auth、生产目标拒绝、started-runtime updater 零匹配与 updater argv-drift 负控。
- `pnpm lint`：PASS。旧 PR Quick Gate 的三条错误来自本分支冻结 JSON fixtures；现以精确路径排除修正，不掩盖其他 JSON。
- `pnpm -r build`：PASS。
- `pnpm test:packages:run`：按要求完整执行；本次仅两个真实 macOS Terminal.app / HiServices 用例因 resident sandbox 无 GUI 连接失败，其余 core 19 files / 219 tests 全绿。shell-only 最终修复不触及 package 代码，PR CI 是全仓最终判据。
- daemon PID 的生产 JSON reader 依赖 Bash >= 4；仅有 macOS 系统 Bash 3.2、旧整数记录或截断记录时，teardown 会以 `step=daemon-pid` fail-closed 并保留房间。Codex topology 默认 60×1s，需延长时使用 `FLYWHEEL_QA_LEAD_VERIFY_POLLS`；`--lead-ready-timeout` 不覆盖这段预算。

## Code review

- 本地双轴 read-only review 首轮找到公开 `codexLead.commDb` 输出漂移与不存在 socket 父目录误判；本轮又找到 source preflight 未证明 daemon incarnation、失败清理误释 lease、started-runtime updater 零匹配缺少整链守卫，以及 registry/EXIT rollback 吞错。全部按 TDD 修复，并补齐 lock/lease 保留与无敏感诊断。
- 较早的 request-driven cross-family review 已通过 R4。旧裁定曾要求保留生产 home denylist、不加 QA allowlist；F5 使 source 变为可写后，Lead 明确修订该裁定为上述精确正向白名单 + 独占 lease + CAS 写回。非 UTF-8 renderer 输入仍按原裁定非零、无发布地干净失败但可打印 Python traceback。
- request-driven R5 在旧头指出自动写回未校验 auth 内容这一 HIGH：零字节 slot auth 可覆盖真实 QA source。新回归先稳定复现，再以独立 `result=invalid` 修复；最终 rework 又移除了环境可控的 teardown 预算。本 milestone 作为字面最后提交推送后申请全新 exact-head round，旧头 gate 不作授权；如仍有 blocking finding 将继续修复并重新发起。
- request-driven R6 在 `077fe2303` 找到共享 standalone symlink 下 updater 仅按 binary realpath 匹配会误杀另一 `CODEX_HOME` 的 HIGH。新负控在 `set -e` caller 下先红；现 census、二次身份复核、TERM/KILL 后收敛与最终空集均同时绑定 binary + 精确 `CODEX_HOME`，source preflight 与 slot teardown 各传自身 home。该轮另有 wrapper gate env scrub、零 updater fail-close 与 scandir 关闭三条非阻塞 advisory，保留给最终评审结果报告。

## 独立 QA 结果

独立 QA 首轮已在真实 529 台架完成 T12：Claude slot 2 原始与规范化产物逐字节一致并完成起/验/拆；Codex slot 4 以独立 home/state 完成 A1–A4、`crash` + `kickstart` 两式自愈、拆房、默认 tmux 不变以及生产 root 元数据对照。第二轮确认 Claude byte guard、caller-independent stop、JSON PID、旧/新 topology 变异与 CI 后，发现 source refresh token 单次使用的 F5；A1–A4 与最终 updater 收敛须由独立 QA 在本新 head 重新执行。F4 的两次后续真实进程探针本身有缺陷，因此当前证据边界是代码检查、argv-drift/多进程 hermetic 正负控，以及首轮真实观察，不把缺陷探针写成通过。

明确边界：`FLYWHEEL_RUNNER_MEMORY_ROOT` 仍由继承环境提供，不在本单新增持久隔离；该治理已归 FLY-2305。FLY-2301 不做生产单实例切换、不退旧脑，也不触碰生产 Codex home/state。

本 implement 节点未 dispatch QA、请求 ship、merge、deploy、修改 `CLAUDE.md` 或重启生产服务。
