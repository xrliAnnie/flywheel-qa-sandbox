# FLY-2301 — 529 房支持 Codex 形状常驻 Lead

**Status**: ✅ Implementation complete; needs review
**PR**: pending creation from this milestone commit
**Date**: 2026-09-03

529 slot 常驻 Lead 台架现在按载体形状分流：既有 `claude-code` 继续使用原来的 wrapper + manifest + private `=main` tmux 验活；`codex-app-server` 使用 `/bin/bash <slot-wrapper>`、slot 独立 `CODEX_HOME` / state / tmux server，以及 launchd PID + runtime argv + `CODEX_HOME` + heartbeat + 单一 TUI window 的组合验活。没有新增运行时开关，新增形状由 slot 的 Lead 载体声明直接选择。

## 交付内容

- 冻结 Claude plist、`.env`、per-Lead manifest、launch manifest、stdout 与两条日志基线；八个定向变异体先证明产物字节变化，再证明对应守卫必红。
- 新增 Codex plist、wrapper、环境投影、独立 home 铸造、state-root 注入、形状元组校验，以及 companion / full-access 两种 profile 的真实生产解析层验证。
- 新增 Codex 专用 topology / readiness 判据、KeepAlive `crash` 与 `kickstart` 演练、心跳字节归档和房外证据包。
- registry v2 与拆房只在含 Codex 条目时执行 runtime / daemon / socket / private tmux 收敛；纯 Claude registry 继续走原有逐字节 fail-fast 循环。
- `test-deploy.sh` 的 main 与 extra Lead 都可承载 Codex 形状，并保证 home / state / workspace / label / TUI window 五坐标互异；roundtable 在产物前明确拒绝。
- 与最新 `main` 的 slot report-host / deterministic TMPDIR 改动已合并；stdout 仍由单一可测 helper 渲染，同时保留 `bridgeTmpDir`、`reportsDir`、`reportHost` 与 Codex lifecycle 坐标。
- 更新 529 playbook、CI shell 枚举、路径卫生与 host-tmux source census。生产路径唯一行为改动是 `codex-lead.sh` 使用注入的 `FLYWHEEL_STATE_DIR`，未设置时保持原值。

## TDD 与验证

- 新增 shell suites 全绿：state-root 1/1、artifact fixtures 7/7、mutants 8/8、Codex layers 12/12、tmux isolation 4/4、drill guards 2/2、metadata snapshot 3/3。
- 核心 launchd suite 35/35；完整 hermetic deploy lifecycle 20/20（同时覆盖最新 report-host）；multi-Lead 25/25；CI shell enumeration 267/267、CI structure、path hygiene 与全部 shell `bash -n` 通过。
- `pnpm -r build`：PASS。
- `pnpm lint` 已按要求执行；失败来自共享 resident worktree 内、不在本分支 diff 的 FLY-1547 / FLY-1563 / FLY-2007 研究脚本及主线既有诊断；FLY-2301 文件无 diagnostic。
- `pnpm test:packages:run` 已按要求执行；resident sandbox 的两个真实 Terminal.app AppleScript 用例因 GUI/HiServices 不可用失败，并伴随一个固定 5 秒 census 超时。用 test-only `which osascript` shim 走测试自带 skip 后，core/config/qa-framework 全绿；后续 flywheel-comm 在共享宿主高并发下出现固定时限/性能阈值失败。相关单测在低负载隔离运行通过，PR Linux CI 是最终全仓判据。

## Code review

- 本地 `codex:rescue` companion 已按要求两次以 read-only branch-review 模式启动，但嵌套 reviewer 在读取仓库前分别因 resident sandbox `Broken pipe` / `sandbox_apply: Operation not permitted` 未产生 verdict；没有调用禁止的 raw `codex exec`。
- request-driven cross-family review round 1 对 `5b032c4c3` 提出一个 HIGH：真实 launcher argv 保留 `scripts/../dist`，旧 matcher 会拒绝。修复后 matcher 词法归一化路径，并新增真实 argv 形状守卫；同时修完该轮三个 advisory：凭据改用 NUL stdin 传给 env renderer、拒绝 live-demo `.codex-242`、drill 失败清理临时证据并局部化循环变量。
- 合并最新 `main` 后的修复头 `63db3d107` 通过 cross-family review round 2，无阻塞项。两条 advisory 已报告 Lead：生产 home 保护仍是精确拒绝表（未覆盖 `.codex-mufasa-med3`，改 QA-source allowlist 需另行治理），以及非 UTF-8 stdin 会安全失败并清理临时文件但打印 traceback。最终 milestone 头还会再过一次 exact-head gate，作为 PR 创建前硬门。

## 独立 QA handoff

实现节点尝试了 slot 2 的真实 Claude launchd 启动，但当前 resident sandbox 对正常 plist 与最小 `/bin/sleep` plist 都返回 `launchctl bootstrap: 5: Input/output error`，且写 `~/Library/LaunchAgents` 返回 `Operation not permitted`。失败 slot 已拆除，未尝试绕过沙箱；Lead 已明确要求由独立 QA 在 529 房完成真机 T12。

QA 需在候选 head 上执行：slot 2 Claude 原始/规范化产物对比与起/验/拆；slot 4 Codex 独立 home/state 启动、A1–A3、`crash` + `kickstart` 两式自愈、拆房；并复核默认 tmux server 不变与生产 Codex roots 的元数据差异。实现节点 hermetic 20/20 不是这项真机验收的替代品。

本 implement 节点未 dispatch QA、请求 ship、merge、deploy、修改 `CLAUDE.md` 或重启生产服务。
