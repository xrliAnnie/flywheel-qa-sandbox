# FLY-2301 — 529 房支持 Codex 形状常驻 Lead

**Status**: ✅ Implementation complete; exact-head review pending
**PR**: #1057
**Date**: 2026-09-04

529 slot 常驻 Lead 台架现在按 Lead 载体形状分流。既有 `claude-code` 仍走原 wrapper、manifest 和私有 `=main` tmux 验活；`codex-app-server` 走 `/bin/bash <slot-wrapper>`、slot 独立 `CODEX_HOME` / state / CommDB、生产同源的 Codex home birth path，以及 launchd PID、真实 argv/env、heartbeat 和单一 TUI window 的组合验活。没有新增开关，选择权只来自 slot 的载体声明。

## 本轮 Lead 裁定

已批准 plan 中“专用 QA source home + lease + CAS 写回”的设计被最新 founder/Lead 指令明确取代。plan 是已封存的设计审计物，因此没有改写；实现改为与普通 Codex dispatch 相同的生产 home 铸造路径，不再声明或读取可用的 `codexSourceHome`，也不再维护 QA 自有凭据源。旧字段只保留 fail-closed 退役检查，任何非空值都在生成 slot 产物前拒绝。

## 交付内容

- slot 声明表达式是 `{"backend":"codex-app-server","codexProfile":"companion|full-access"}`。`qa_multilead_lead_config` 只输出载体与 profile；`qa_slot_start_lead` 调用 `qa_launchd_provision_codex_home`，后者通过已构建的 `flywheel-claude-runner` 包直接调用生产 `provisionCodexHome`，仅把 `FLYWHEEL_CODEX_HOMES_ROOT` 指向当前 slot。
- 生产 provisioner 负责从当前 Hub 权威选择并复制认证字节；QA adapter 不复制 `auth.json`、不记录认证值、不修改生产 dispatch。adapter 另行复制当前 standalone Codex release，并删除只属于短命 runner 的 config/contract/skills 产物，让常驻 Lead 的既有 ensure-home 路径生成 carrier 配置。
- 新的“凭据可用”判据不是 `codex login status`。起房必须真实执行 launchd body，并由 `qa_launchd_codex_lead_verify` 同时证明 launchd PID、`/bin/bash <wrapper>` argv、精确 `CODEX_HOME`、heartbeat、app-server control socket 和唯一 TUI window；缺一即停房并失败。
- F5/认证轮换只写 slot 的独立 `CODEX_HOME/auth.json`。拆房在 runtime、daemon、updater、socket 和私有 tmux 全部收敛后删除整个 slot home，绝不回写 active Hub。第二间房不继承第一间房的认证副本，而是再次通过生产 provisioner 从 Hub 权威独立铸造；真实 Hub 上的连续起房与轮换行为留给 QA Path A 验收。
- registry 注册失败会回收精确的 slot-owned home；任一生命周期残留都会保留 slot lock 供恢复。删除路径要求真实目录、非 symlink、位于精确 `/tmp/flywheel-test-slot-N/cdxh/<agent>`，不能越出 QA namespace。
- Codex plist 保持生产 Raya 的单参数形状 `/bin/bash <wrapper>`，并注入 carrier 所需的 `CODEX_HOME`、`FLYWHEEL_CODEX_BIN`、Lead mode/state、CommDB、chat、outbound 与 profile 环境；Claude plist/env/manifest/stdout/log 基线及对应变异守卫保持逐字节语义。
- Codex topology、`crash` 与 `kickstart` 两式 KeepAlive 演练、证据包、slot 清扫和多 Lead 坐标隔离继续保留。纯 Claude registry 仍走原 fail-fast 路径。
- 更新 529 playbook：不再要求 `~/.codex-259-qa`、登录状态或手工复制；明确生产 birth engine、实际 body readiness、slot-only 轮换和 active Hub 不可写边界。

## TDD 与验证

- 本轮先让四组旧 source/lease/CAS 合同按新裁定稳定变红，再做最小实现；相关 shell suites 最终全绿：multi-Lead 25/25、hermetic deploy 22/22、核心 launchd 40/40、Codex layers 12/12、state-root 1/1、mutants 8/8、host tmux 2/2、restart drill 2/2、artifact fixtures 7/7、metadata 3/3。
- hermetic deploy 以假 active Hub 和 standalone release 调用真实生产 provisioner，证明 main/extra 两间房各自得到独立 home、source hash 不变、退役字段在产物前被拒绝；测试只用假凭据，日志与文档无认证值。
- kill-path inventory 因删除两条 QA-only 旧 source 清理路径先红（603 对冻结 605），精确更新后 1/1 绿。所有变更 shell 通过 `bash -n`，Node adapter 通过 `node --check`，`git diff --check` 通过。
- `pnpm lint`：PASS（14 条既有 warning，无 error）。`pnpm -r build`：PASS（21/22 workspace packages）。
- `pnpm test:packages:run`：按要求完整执行；core 的 2 个 macOS Terminal.app 用例因 resident sandbox 无 HiServices/AppleScript GUI 连接失败，其余 core 19 files / 219 tests 全绿；排除该宿主 GUI 文件的 core 定向全跑为 19 files / 219 tests 全绿。
- 其余 workspace 的低并发补跑里，teamlead 有 10,738 项通过，3 项受重载/宿主 tmux 特殊窗口名影响失败；三项隔离复跑分别 19/19、2/2、13/13 全绿。末端 token/voice workspace 额外 1,196 项通过、4 项 smoke skip。`flywheel-comm` 的 FLY-1715 固定 5 秒项在高并发时超时，隔离复跑 457ms 通过。

## 审查与 QA 交接

- 本地双轴 review 以先前远端已审头 `4e2ae197c` 为 fixed point：未发现新的 Standards 硬违规；Spec 唯一差异是封存 plan 的旧 credential 设计，被上述最新 Lead 裁定显式 supersede。未为修正文档历史而改写 plan。
- request-driven R1 在 `a18063f29` 找到一个 HIGH：最终 manifest/report/stdout 发布前已经解除 EXIT trap，report-host 失败会遗留 Bridge、KeepAlive Lead、锁与 credential home。新结构守卫先同时证明 abort helper 晚于首个调用、trap 早于最终发布而红；`1536bb7f3` 将 abort helper 提升为全局，并把 disarm 移到全部发布成功之后。generalized helper 与 hermetic campaign/report-host 回归全绿。
- 同一修复批次处理两条 MEDIUM advisory：macOS `ps eww` 全环境改经 stdin 输入 Python，假 secret argv 守卫先红后绿；从未写 launch marker/PID/socket 的 home 将 daemon-stop 非零视为“未启动”，仍完成 updater 缺席证明与 slot home 退役。不可读 metadata 目录的 LOW 降级策略保持非阻塞后续。
- review 修复后只串行执行四个直接相关 shell 文件；没有运行 vitest。若后续需要 vitest，按 Lead 红线固定 `VITEST_MAX_THREADS=4 VITEST_MIN_THREADS=1`，并始终排除会真实控制 Terminal.app 的 `packages/core/test/tmux-viewer.macos.test.ts`。
- 本 milestone 是重新推送前的字面最后提交；推送后必须对新 exact HEAD 发起 fresh request-driven cross-family code review。旧头审查不转移授权，任何 blocking finding 都需修复后重新申请。
- QA Path A 应在真实 529 房验证：Codex 载体真实起/验/拆、RunAtLoad + KeepAlive、`crash` + `kickstart` 自愈、F5 后拆房再起第二间房，以及既有 Claude 房逐字节与 slot lifecycle 回归。implement 节点不 dispatch QA。

明确边界：本单不做生产单实例切换、不退旧脑、不请求 ship、不 merge/deploy、不修改 `CLAUDE.md`，也不写任何生产 Codex home/state。
