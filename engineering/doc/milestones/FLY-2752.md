# FLY-2752 — 巡检队列证据完成门

**Status**: ⏳ Pending ship
**PR**: #1269
**Date**: 2026-09-18

巡检报告完成门现在接受并严格校验 `queue_request`、`queue_position`、
`queue_wait_seconds`。旧版无 queue 字段的报告保持兼容，未知的 pane/activity
机器字段继续 fail closed；定向单元测试与真实 snapshot 初始、STALLED 骨架回归均已覆盖。
QA rework 同时保留历史 `PANE_EVIDENCE note` / `evidence_note` token；同一批
1209 份生产巡检报告回放为 0 个 valid→invalid，4 份 queue 报告由 invalid→valid；
对应实现提交为 `72046466f`。
