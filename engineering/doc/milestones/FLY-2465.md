# FLY-2465 — Codex 舰队自动配额恢复
Issue: FLY-2465 (https://linear.app/geoforge3d/issue/FLY-2465)
日期: 2026-09-09
基于: ../FLY-2465-codex-fleet-rotation/implementation-evidence.md

**Status**: ⏳ Pending ship
**PR**: [#1143](https://github.com/xrliAnnie/flywheel/pull/1143)
**Date**: 2026-09-09

Codex 明确限额按可信 root/generation 暂停和去重；三号池按重置时间选取，隔离真实 CLI 协议探针通过、安装日志提交后，才通过原 terminate/start 路由恢复。排队保留原 reservation/key，物理 running 才结清；安装中断保全刷新后的候选凭据；全池耗尽或等待老化使用同一事故告警去重。审计投影可由巡检 STEP 2 读取，Claude 切号路径保留。

已通过合成凭据、真实 HTTP/SQLite/子进程六场景台架；lint/build 和新增 shell gates 通过。默认并发完整包命令曾因 Vitest RPC timeout 失败；原 HEAD 单 worker 全量复验已通过。R1 两项 HIGH 已修复，普通合并 main 保留了发布历史；修复后完整复验、精确 HEAD review/CI 在 PR 中继续记录。没有宣称生产接受、在线 Codex 探针成功或部署；自动 rotation 依赖 FLY-2404 的共享凭据迁移和完整 readiness receipt；迁移未完成不阻断普通 Codex launch。

R3 在 `9829de318` effective APPROVED。Lead 随后要求补齐所有同类测试/发布清单，并将 pre-binding API 故障回退修复纳入本批：`d0a787c01` 保留本地 argv/退出码和旧 spool，已绑定审查仍 fail-closed。client 16、guard 46、closure 31、arrival 7、converge 22、packaging 5、seams 17、prebuilt 6、provision 20、smoke 19 全通过；lint/full build 通过。新 HEAD 完整包测试、CI 14/14 与独立审查结果继续在 PR 中记录，当前不宣称最终全绿。


Lead ship-gate rework（implement attempt 2）：`015e3b352` 在开关 OFF、runtime 初始化失败或绑定失败时显式关闭本次 rotation，继续 legacy daemon launch；记录 structured code/cause，并由现有 infra alert 通道按主机限流。构造失败→真实 daemon client/goal loop 的隔离集成测试通过；删除 fail-open 分支变 RED。执行级 pause guard 删除也变 RED，恢复后通过。原绑定身份校验及 Claude 路径保留。

本轮 lint、全仓 build、全部三个新增 shell gate 通过。默认并发完整包命令因 runner onTaskUpdate RPC timeout 退出 1（1,170 passed、零失败断言）；最终单 worker 原完整命令退出 0，runner 47 文件/1,170 passed，teamlead 917 文件/12,392 passed，其他包均成功。未排除测试或修改超时。详细证据见实施目录 implementation-evidence.md。

此文件是唯一一次普通推送前的最后提交。随后冻结 HEAD，等待 fresh exact-head review 和 CI 14/14，结果外记 PR #1143 与 comm。codex:rescue companion 被嵌套 sandbox 初始化拒绝，限制已报告；正式 request-driven 审查仍必需。本轮未部署 529 slot、未生产迁移、未 merge。


Lead-authorized second cycle（H1/H2）：`c39c0255f` 保留 ON typed pause 的
fail-closed 拒绝；OFF 动态移除所有启动栅栏，普通 dispatcher pass 继续已有
用户 admission reservation。已启动/丢失响应的执行只核对存活，不重新 POST；
不自动重起死亡体。scoped→master 重放只保留匹配 authority/reason 的已存 actor，
改 reason/tier 仍 409。暂停事实及审计保留；详见 design-correction.md 的完整裁定。

`326dac3ef` 普通合并 main `5cbd540f1`。合并后全仓 build、lint（14 warnings）、
三项新增 quota shell gate、CI structure 与 retention consumer gate 通过；Runner
focused 159/159，Teamlead 同一 16 文件单 worker focused 341/341，均 exit 0。
Teamlead 首次并发 333 passed/8 failed 的真实失败回执保留。此前单 worker
全量本地通过属于上一轮版本；依 Lead LAST-local-full 裁定，本轮依赖 focused
验证与新的 exact-head CI14，不宣称当前完整本地套件已跑过。

本文件再次作为本授权周期的 literal last commit，之后仅一次普通推送并冻结 HEAD。
新的 review 与 CI 结果外记 PR/comm。若再出现同源 HIGH，停止并报告 Lead，
不做第三轮修复。完成仅走 needs_review + park，goal 保持 ACTIVE；无 merge、
部署、QA dispatch 或生产迁移。


末次测试校正：审查 db271869 唯一 HIGH 是合并后 feature flag 数量 27、测试
预期仍 26。依 Lead85886250 的独立授权，待审查结束后仅修正计数断言；
`c0b488143` 一行测试变更，无生产行为变化。RED 为 53 passed/1 failed，
GREEN 为整个 config 包 49 文件/786 passed，exit 0。其他 MEDIUM/LOW 已报告。
本 milestone 再次为该测试校正周期唯一普通推送前的最后提交，随后冻结 HEAD
等待 fresh exact-head review 与 CI14；仍无 merge/deploy/生产接受声明。

Founder notification rework（implement attempt 3，2026-09-10）：`a5f65945a`
在每次成功 generation commit 的同一事务中入队一条普通切号通知，复用
现有 Bridge `quota_switch_confirmation` 通路；全池耗尽保留原告警、@、
severity 和去重，并补齐同样字段。通知包含 usageLimited、源账号、源额度
reset（无证据时 unknown）、目标账号、受影响 run 数与实际 recovered 数；
queued 不计作重起。两个新增 outbox 测试红绿通过，原通知测试正文逐字保留。

推前 main 推进导致冲突，`027397100` 普通合并 `42869f935`，逐字保留 quota
和 auto-narrow 两个回调；仅将合并后的 flag/retention 测试计数对齐到真实
registry（28 flags，142 reference tables，203 total）。这些计数先 RED 后
GREEN。合并后全仓 build、lint（14 warnings）、四项相关 shell gate 通过；
TeamLead 定向30文件271测试、config68测试通过。

合并前 full run 因需要更换源码基线而主动停止，非通过回执。合并后的原完整
包命令正运行（`/tmp/fly2465-r3-merged-full.log`），结果与新 exact-head review、
CI14 外记 PR/comm；当前不预告通过。本文件为本轮唯一普通推送前 literal
last commit，之后冻结整个 HEAD，包含 progress。完成仅走 needs_review
后 park，goal 保持 ACTIVE；无 merge PR、部署或生产账号操作。

唯一 HIGH 的独立授权修正（2026-09-10）：review `b66518da` 在 `cbf8e08e3`
发现 client 文案分类 `usageLimited` 被 observe 单值白名单拒绝。Lead367a8d7a
授权 `13972c263` 仅移除该冲突检查并追加一条真实客户端文本→HTTP接受/重放
测试；共享 schema 两值限制、429/source/owner/binding 守卫保留。RED400 后
route4/4 GREEN；完整 build、lint、3项 quota shell gate 通过。broader quota
19文件148pass/8个既有probe时序失败保留；原probe文件隔离13/13 PASS。
未改任何旧测试或超时，不重跑完整本地套件（Lead9a4c15ba裁定）。

本文件是该独立授权周期唯一普通推送前的 literal last commit。之后整个 HEAD
再次冻结，新 request-driven review 与 CI14 的结果仅外记PR/comm。若新审查
出现任何 NEW HIGH，立即停止并报告Lead，不继续修复。六条advisories不动；
仅在有效APPROVED和exact-head CI14后needs_review，再park，goal保持ACTIVE。
