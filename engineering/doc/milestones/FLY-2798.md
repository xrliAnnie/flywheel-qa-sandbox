# FLY-2798 — Live 前台快答与后台 Lead

**Status**: ⏳ Main-sync review 930b3f21 blockers fixed; pending fresh scoped exact-head code review, ordinary PR CI, and QA retest
**PR**: #1312
**Date**: 2026-09-24

引擎 A 已按 FLY-2795 V4 合同落在新的 OpenAI Live backend/session 路径，而不是改 legacy `realtime.ts`。`GptLiveBackend` 通过配置化模型与端点创建 Live session；前台可以流式生成简单回答，generation fence 在每个异步边界拦截取消后的晚帧，`turnCancelOrSuppress`、utterance assembler 和 composite speech 保持字幕、音频与最终句的顺序。V1 `speak()` 走同一语音输出面，供 V2/V3 播报与带节奏使用。缺模型、端点或 transport 时显式报语音不可用，不静默降级。

复杂问题使用 provider client delegation 接缝，不另造函数工具，也不把 `delegation.id` 当幂等键。`voice-codex` 只在完整原话已持久化后创建 Lead handoff，保存 `delegation.id ↔ handoffId ↔ transcriptId` 绑定；前台先播“我问下 Lead”。Lead 结果由已鉴权 outbound 路径直接提交到 voice handoff result store，提交先于 Discord mirror，并以 operation id 派生稳定 child/result identity；重放所需的 `createdAt` 固定取权威 source message 时间，不随墙钟漂移。Bridge 的 HTTP 适配层只把与请求 `leadId` 精确匹配的 canonical voice delivery context 提升到可信 envelope，核心 handler 不信任任意 body context，result producer 仍会复核持久化绑定。该路径通过生产注入的 reply subscription 替代语音进程 4 秒轮询与 Bridge 3 秒轮询的结果消费方式：SSE 只作门铃，正文从 durable results route 重读；断线显式记录且不降级轮询，重连对每个已登记 handoff 唤醒一次。旧 poller 仅为 legacy 保留。

字幕 projection 把 Live 前台标为 `🤖 前台`、Lead 结果标为 `💬 Lead`，不会把等待提示误标成 Lead 答案。输入在前台/Lead 声音切换和 Live session 重建期间继续按原 turn 边界缓冲；委托关闭旧 generation，迟到的整句不会泄漏到新 turn。FLY-2767、FLY-2773、FLY-2711 未改，符合明确的 backlog 排除范围。

本地聚焦验证通过：受影响 package dependency build；`voice-core`、`voice-codex`、`teamlead`、`voice-bridge`、`voice-headphone`、`gemini-agent`、`flywheel-comm` typecheck；聚焦测试分别为 97/97、57/57、198/198、107/107；`voice-codex vitest related` 17/17。`teamlead vitest related` 首轮在 1831 项中抓到 delivery-context 信任边界回归，未削弱断言；最小修复后聚焦 44/44，最终扩大图复跑 147 文件、1858/1858。第三轮审查整改后再跑：聚焦 teamlead 27/27、voice-core 5/5，Teamlead related 126 文件 1607/1607，Voice Core related 3 文件 13/13；`flywheel-teamlead...` 13-package build 与 `...flywheel-voice-core` 11-package dependent typecheck 均通过。受影响 11 文件 Biome 零 error；`git diff --check` 通过。根 `pnpm lint` 最终为 5233 files、0 errors、25 warnings、exit 0。没有运行本地全包测试，也没有请求 full CI；`CI Scope OK` 不作为 full-CI 或 ship 证据。

冻结前同步当前 `origin/main@049fbf194`。唯一冲突在 `StateStore.ts` 的同一 cache 区，语义并集同时保留本分支的 `VoiceHandoffStore` / `HeadphoneInboxStore` 与 main 的 `WorkflowScorecardStore`，三者 imports 完整；没有覆盖任一 owner。同步后重新执行 frozen install、受影响依赖 build、teamlead typecheck、voice-codex related 17/17，以及覆盖 voice stores、producer、outbound trust boundary 与 workflow scorecard 的 11 文件 112 passed / 1 skipped；`git diff --check` 通过。同步前审查请求因此作废，本 milestone refresh 后必须使用新精确头复审。

第三轮代码审查的 blocking HIGH `voice-lead-result-producer-not-replay-safe` 已用推进墙钟的 hard-red 回归复现并修复；V1 receipt digest 公式与 HTTP context trust boundary 两项 advisory 也已收紧。reconcile timer guard、unavailable 重试语义与 results route committed-state gate 保留为非阻断 follow-up，须在有效复审后转报 Lead。

QA 在前一精确头 `fd9cde9f33468cf3cdeb50f11c0d49ce80ec1772` 请求的 full CI run `35988198150` 发现四类遗漏：九个 Live runtime 配置未进入非 flag 清单、FFmpeg/Edge TTS 子进程未进入 child census、四个对应 kill path 未进入 inventory，以及 package-onboard 未携带共享 Silero runtime。四类本地 hard-red 均已用最小治理/打包修复转绿；Silero 部分仅 cherry-pick 依赖 PR #1309 已评审提交 `6c3811e20`，资产仍归既有 `voice-bridge/models`，没有合并依赖分支的其他行为。

整改后证据：config drift 14/14、child census 1/1、kill inventory 5/5、package-onboard 26/26；config `vitest related` 18 文件 326/326；Silero `vitest related` 2 文件 5/5；五组直接 consumer 分别 12/12、1/1、3/3、85/85、46/46。`flywheel-voice-codex...` dependency build 通过；根 lint 为 5235 files、0 errors、25 warnings；定向 Biome、shell syntax 和 `git diff --check` 均通过。当前实现节点没有重新请求 full CI，QA 必须在本轮新精确头复跑后才能给出 full-CI/真人路径结论。

Lead 随后确认旧头 `f65795e62` 只覆盖 A1–A4，其 R5 APPROVED 因新增生产激活/订阅要求而作废。本轮新增显式 `FLYWHEEL_VOICE_ENGINE=openai-live` 路由（默认仍是 `legacy-realtime`），把权威 session generation 贯穿 Teamlead projection、RoomIO、V1 engine 与 reply binding；`GenericVoiceSession.speak()` 在 A 分支复用 V1 播报面，供现有会议播报进入 Engine A。生产构造链现为 `GptLiveBackend → LiveLeadAdapter → HeadphoneSession`，并组装 streaming Edge TTS、FFmpeg PCM、durable transcript 与分标字幕。Engine A 部署必须把 `FLYWHEEL_VOICE_EDGE_TTS_STREAM_CMD` 指向安装兼容 `edge-tts` 的 Python 3.10 可执行文件；默认 `python3` 只有在实际解析到该运行时时可用。

新增接线的精确测试为 voice-codex 71/71、Teamlead 30/30、voice-headphone 19/19；changed-TypeScript related 为 config 326/326、voice-core 5/5、Teamlead 自动依赖图 1194/1194、voice-codex 171/171。最终受影响 dependency build 覆盖 16 个 package，voice-core owner/dependent typecheck 覆盖 11 个 package；根 lint 为 5237 files、0 errors、25 warnings，`git diff --check` 通过。以上仍是本地定向证据，不是 exact-head full CI。

R6 随后发现生产 Lead outbound 通过 `VoiceLeadResultProducer` 直接提交 durable result 时，会绕过原先封装在 HTTP results route 内的 SSE subscriber map。整改把 subscriber registry 提升为 `startBridge` 所有的单一 `VoiceReplyNotifier`，同时注入 producer `onCommitted` 与 handoff router；两条提交路径现在共享同一门铃，且只有新 durable sequence 触发通知，相同 operation 重放不会重复唤醒。producer-path 接受测试先红后绿；聚焦 13/13，Teamlead `vitest related` 自动展开 103 文件、1183/1183，Teamlead typecheck、13-package dependency build、根 lint（5238 files、0 errors、25 warnings）与 `git diff --check` 均通过。该本地证据仍不替代新精确头复审或 QA full CI。

消费者查询、选择与逐类排除证据记录在 `engineering/doc/FLY-2798-live-front-lead/implementation-verification.md`。当前实现仍需绑定本 milestone-last 精确头的全新代码审查（旧 R5 不再有效）和普通 PR CI。真人语音 QA 必须在测试房 Raya 按同源录音口径量 10 次“说完 → 听到第一个字”，逐次均 ≤2 秒，并验证复杂问题落地、前台/Lead 字幕分标和 V2/V3 播报；本文不以文字事件时刻替代，也不宣称 QA 已完成。

Required before Done：按 `engineering/doc/FLY-2798-live-front-lead/claude-plugin-hook.md` 在独立的 `claude-plugins-official` PR 接入相同 generic producer contract。该第二 PR 按 Lead 裁定在本 PR/QA 后单独派发；本实现节点未触碰现有 dirty plugin worktree、未声明嵌套 PR，也未 dispatch QA、merge、deploy、restart 或修改生产配置。

QA 第二轮在 `aec8c8ccb` 的真人链路发现五个阻断：RoomIO 首帧 sequence 从 1 开始、handoff 两端幂等键公式不一致、Engine A 下 legacy 两处轮询仍开、headphone/handoff route 位于 catch-all 之后，以及 529 launcher 没有透传三个 Engine A 开关。当前整改分别用 post-increment 首帧、voice-core 共享幂等键 helper、Engine A 双端轮询门控、catch-all 前 late-bound router holder、封闭三项 env allowlist 修复；默认 legacy 引擎与排除项不变。

最终本地证据：聚焦 voice-core 4/4、voice-codex 71/71、Teamlead 37/37、529 launcher 15/15；changed-TypeScript related 为 voice-core 5/5、voice-codex 60/60、Teamlead 自动依赖图 104 文件 1202/1202。受影响 owner/dependency build 覆盖 16 个 package，voice-core owner/dependent typecheck 覆盖 11 个 package，均通过。变更的 15 个源码/测试文件定向 Biome 0 errors；根 `pnpm lint` 已执行，但因本 issue diff 外的 FLY-1547/1563 research scripts 与 FLY-2560 replay tooling 等 3 个既有 error 退出 1，未越界修改。消费者查询、所有保留项与逐类排除详见 `implementation-verification.md`。以上仍是本地定向证据；当前头没有 full CI，真人 10 次延迟、复杂问题落地、真机字幕与 V2/V3 联调仍由 QA 复测。

R8 scoped review 的六项 HIGH 均已用先红后绿的最小修复关闭：Live 输入发送失败被隔离并只记录一次 unavailable；utterance assembler 在新一轮开始时关闭旧 orphan window；FFmpeg PCM decoder 用 stdout pause/resume 对齐有界消费队列并在主动背压时暂停 watchdog；CompositeSpeech 与 Live Lead result delivery 不再缓存失败 receipt、也不把失败结果永久标成已应用；OpenAI Live 字幕 delta 在应用层按 1 秒静默窗口聚合为单个 final caption，但不伪造会截断迟到音频的 response-done；handoff result 的 canonical 原文进入 plugin 验证，不能再由已登记 transcript 身份替代正文匹配。

R8 最终精确代码头验证：voice-core 聚焦 4 文件 28/28、voice-codex 14/14、Teamlead route 7/7；changed-TypeScript related 为 voice-core 20 passed files（196 passed / 4 skipped）、voice-codex 1 文件 14/14、Teamlead 自动依赖图 103 文件 1178/1178。受影响 dependency build 覆盖 16 个 package，voice-core owner/dependent typecheck 覆盖 11 个 package；根 lint 为 5239 files、0 errors、25 warnings；15 个 changed files 定向 Biome 0 errors（2 个既有 warning），`git diff --check` 通过。所有消费者查询与排除理由已追加到 `implementation-verification.md`。当前实现节点仍未请求 full CI；上述证据不替代新精确头代码复审、普通 PR CI 或 QA 真人复测。

QA 在 `b51a597567bf399120b3908f56c55bd68f933d38` 上的 claim 1481 进一步发现：headphone claim 泄漏了含 `speechBrief: null` 的内部 row，使真人 Engine A 在启动后约 15 秒被真实客户端解析器终止；reply SSE 仍为固定 1 秒无限重试；529 production `voiceEnv()` 与 test-deploy slot Bridge 没有完整透传三个 Engine A 环境变量；adapter→route 与 producer→真实 RoomIO 的合同测试仍缺失。

当前 code head `b83e00489` 已关闭这些缺口：list/claim 共用公开 item projector，且真实 route→真实 `BridgeVoiceClient` 配对覆盖 null 行；subscription 使用 1s 起步、30s capped exponential backoff 和 5 次重连预算，耗尽后单次审计且不回退 polling；官方 529 launcher、prepare receipt 来源 env 与 test-deploy `BRIDGE_EXTRA_ENV` 均显式白名单三项 Engine A 设置；真实 `LiveLeadAdapter` request 可提交到真实 handoff router/store，旧 digest key 保留 400 negative guard；`CompositeSpeech` 与 `LiveLeadAdapter` 均通过真实 RoomIO sequence guard。

本轮本地证据：Teamlead 精确 5/5、voice-headphone 15/15、voice-codex 16/16、529 launcher 15/15；最终新配对测试 related 4/4，生产整改后的 changed-file related 为 voice-headphone 15/15、voice-codex 16/16、Teamlead 104 文件 1175/1175。受影响 dependency build 16 个 package、voice-headphone owner/dependent typecheck、根 lint 5241 files / 0 errors / 25 warnings、定向 Biome、shell syntax 与 `git diff --check` 全部通过。完整 consumer discovery 与逐类排除在 `implementation-verification.md`。

部署补充：生产切 Engine A 时，`~/.flywheel/.env` 必须含 `FLYWHEEL_VOICE_ENGINE=openai-live` 与可用的 `FLYWHEEL_VOICE_EDGE_TTS_STREAM_CMD`；Bridge/voice wrapper 以 `set -a` source 该文件。官方 529 test-deploy 则通过 slot-private `BRIDGE_EXTRA_ENV` 显式重建这三项。实现节点没有请求 full CI；真人延迟分布、复杂 handoff、字幕、耳机三件事与 529 N-to-N 仍由 QA 在新复审头重测。

R9 review round 1 在 `ec9384dca` 确认三个 HIGH，当前 code head `19020c964` 已用失败回归关闭：前台音频不再依赖 GPT-Live 不存在的 `response-done`，而是在可配置 idle boundary 后按序 `endSpeech`，迟到音频会开启新的 RoomIO speech；未 drain 的 audible tail 现在按完整 heartbeat interval 重排，不再进入 1ms loop；SSE 重连预算在连接稳定达到 backoff cap 后恢复，短时 flap 仍受原预算限制。真实 RoomIO 测试已移除手工 `response-done`，并证明尾时长计入与迟到帧不丢。

R9 本地证据：voice-core related 3 文件 15/15、voice-headphone related 16/16、voice-codex related 2 文件 16/16；受影响 dependency build 16 个 package、dependent typecheck 11 个 package、根 lint 5241 files / 0 errors / 25 warnings、定向 Biome 与 `git diff --check` 通过。四项非阻断 advisory 已原样进入 `follow-ups.md`；当前 milestone-last 头仍需全新 exact-head review。没有请求 full CI，也不顺延旧头 QA 证据。

R10 review round 2 在 `f692b6b23` 确认唯一 HIGH：idle boundary 调用 `endSpeech` 后丢失前台 speechId，使 WaitingMouth 尚未 drain 的尾音无法被 barge-in 取消。当前 code head `89d3a00be` 保留 `speechId + generation` 到取消边界；真实 RoomIO hard-red 先证明 1 秒尾音下取消调用为 0，修复后 sustained barge-in 精确命中同一 speech 的 `localPlaybackCancel`。close 与 provider cancellation 复用同一取消路径。

R10 本地证据：真实 RoomIO 精确 3/3、changed-file related 2 文件 17/17、16-package dependency build、voice-codex typecheck、根 lint 5241 files / 0 errors / 25 warnings、定向 Biome 与 `git diff --check` 通过。新增 MEDIUM advisory 已进入 `follow-ups.md`；当前 milestone-last 头仍需再次全新 exact-head review。没有请求 full CI，也不顺延旧头 QA 或 scoped CI 证据。

R11 review round 3 在 `0143d079e` 确认唯一 HIGH：旧段 S1 的 pending `writeSpeech` 在新段 S2 已 active 后 reject，原无条件 catch 会误取消当前 S2。当前 code head `9a7a44068` 只在失败所属段仍 active 时取消，失败审计始终保留；真实 RoomIO 超队列 hard-red 先证明 S2 sequence 0 不会写入，修复后 S1 记录 `speech_playback_stopped` 而 S2 sequence 0 正常提交。

R11 本地证据：真实 RoomIO 精确 4/4、changed-file related 2 文件 18/18、16-package dependency build、voice-codex typecheck、根 lint 5241 files / 0 errors / 25 warnings、定向 Biome 与 `git diff --check` 通过。round 3 的 MEDIUM/LOW 已进入 `follow-ups.md`；当前 milestone-last 头仍需再次全新 exact-head review。没有请求 full CI，也不顺延旧头 QA 或 scoped CI 证据。

## QA claim 1494 long-session rework (current head)

The three blockers observed on exact head `50208d4c4` are closed with bounded changes: announcer takeover buffers only frames explicitly attributed to the founder, so continuous `unknown/no_active_speaker` RoomIO uplink cannot overflow the Live input buffer; headphone claim TTL derives from rendered speech length (60-second floor, 250ms per Unicode code point, 15-minute cap), so long deterministic playback can ACK before expiry; and sanitized daemon failures retain a safe enumerated `causeCode` in the per-session evidence log without persisting raw runtime errors.

Each regression was red before the fix. Final local evidence: voice-codex exact 28/28, Teamlead exact 15/15, route/adapter direct consumers 5/5, Engine A composition 1/1; voice-codex related 5 files / 67 tests; Teamlead related naturally expanded through `StateStore` to 572 files / 8010 passed / 4 skipped. The latter is dependency-graph evidence, not full-CI evidence. Affected dependency build covered 16 packages; Teamlead and voice-codex typecheck, changed-file Biome, and `git diff --check` passed. Root `pnpm lint` was run and remains red on 3 pre-existing errors outside this issue diff; those files were not changed. Consumer discovery and exclusions are recorded in `implementation-verification.md`.

Implement did not request full CI or run founder-path QA. QA still owns the exact-head full-CI request, ten real-room first-audible-byte measurements, caption attribution, handoff round trip, and headphone-session observations.

## Main sync after the 12:00 PDT restart (Lead ruling A)

After the reviewed code head `fa815d597` became `CONFLICTING` against `origin/main@637752fcc` (#1295, #1310, #1305), the branch was merged — not rebased — with one conflict in `packages/config/src/feature-flags/truth.ts` `NON_FLAG_ALLOWLIST`, resolved as a union: the nine FLY-2798 `FLYWHEEL_VOICE_*` entries plus main's `FLYWHEEL_CLAUDE_BIN` (FLY-2775). `child-process-census.json`, `kill-path-inventory.json`, `plugin.ts` and `StateStore.ts` auto-merged; no lockfile or `package.json` changed. The Lead-named collision matrix passed on the merge: config `flag-truth` + `feature-flags-drift` 56/56, Teamlead `bridge-child-process-census` 1/1, claude-runner `kill-path-inventory` 5/5; Biome on `truth.ts` clean. No functional code changed relative to `fa815d597`. The previous review request on `fa815d597` was killed by the restart, so this milestone-last head needs a fresh scoped exact-head code review; QA's verified head moves to this head.

## Main-sync review blockers (current head)

The exact-head review of the sync head `069af1144` (request `930b3f21`) reviewed the whole PR and returned 10 HIGH. By Lead ruling, the four FLY-2796 room-layer findings (24 kHz frames to 16 kHz consumers, `/glaw` single-slot handlers, resident lease close reason, `/glaw` lease renewal) are tracked as FLY-2860 follow-ups and no FLY-2796 file was changed here. The six FLY-2798 findings are fixed, each red before green: streamed stdout now finalizes on stdio `close` instead of child `exit` for both the ffmpeg PCM decoder and streaming edge-tts (real ffmpeg with a paced consumer received 104,832 of 290,304 bytes before the fix); voice handoffs are claimed as their own Lead batch; a failed Live resume/replacement or a lost admitted generation ends the session through an explicit `onUnavailable` path instead of wedging or silently dropping founder audio; the founder's non-delegated turn is emitted as a `role:user` utterance ahead of the frontend caption so the spoken exit arms under Engine A (proven with a real `HeadphoneSession`); and the headphone entry briefing and inbox ACK start only after `markLive()`.

Local evidence: named tests voice-core 29/29, voice-codex 61/61, flywheel-comm 114/114, Teamlead consumers 34/34 plus adapter→route 1/1; `vitest related` voice-core 192 passed / 4 skipped and voice-codex 60/60; 16-package dependency build and 11-package dependent typecheck; root lint 5251 files, 0 errors, 26 warnings; Biome and `git diff --check` clean. The 36 MEDIUM/LOW advisories are recorded in `follow-ups.md`. No full CI was requested; QA still owns the exact-head full CI and real-room retest.
