# FLY-2264 — arm64 原生 tmux gate 与 cutover 事务

**Status**: ⏳ Implementation and QA rework complete; pending CI and QA retest
**PR**: #1034
**Date**: 2026-09-02

阶段 A 已把 host tmux selection gate 的生产默认切到 exact
`/opt/homebrew/Cellar/tmux/3.7c/bin/tmux`、`tmux 3.7c`、纯 arm64，并保留 Intel 3.5a、universal/x86
slice、PATH shadow 与 carrier receipt 的 fail-closed 负例。fleet provisioning 同时硬校验 native
Homebrew PATH 必须排在 Intel Homebrew 前。

founder-window runbook 固定了 reviewed main SHA、legacy admission brake、stable-zero quiescence、唯一
`request-restart.sh` 票据、owner lease/handoff、双时钟预算、自动验收与显式 resume。Bridge admission API
支持 durable owner lease、同 owner renew、foreign-owner 409、legacy NULL-owner reason-matched takeover，
并把 lease lapse 写入 API、restart 日志与 0600 host receipt；常规部署只凭本 wave 的 UTC/PID/pause
identifier receipt 接管自己创建的 NULL-owner brake，rollback 的 best-effort pause 失败不会打断恢复。

验证：`pnpm lint` 与 `pnpm -r build` 通过；TeamLead admission/Bridge 57/57、host cutover
GNU-stat 21/21 与 BSD-stat 21/21、restart admission 34/34、tmux gate 19/19、PATH hygiene
13/13、fleet provision 20/20 全绿。QA 提供的 GNU-stat shim 也独立跑过 restart admission
34/34，并记录 14 次 GNU `stat -c` 调用。精确
`pnpm test:packages:run` 已执行，只有 resident host 无法连接 HiServices/Apple Events 导致 core 的两条
real Terminal.app 用例失败，其余 core 219 条通过。QA 返工后的 request-driven code
review r5 在 `d55ab3b28` APPROVED；两条新的非阻断 follow-up advisory 已报告 Lead。

QA 返工同时收紧了 Linux 可移植性与窗口连续性：provisioning PATH 门只在真实/显式
Darwin 上生效；BSD/GNU stat 读取采用静默探测、取值、数字校验和 fallback；legacy
NULL-owner brake 过期后接管会返回、warning 并持久化 `reacquiredAfterLapse:true`，后续健康
renewal 不能擦除该 breach；restart 临时 receipt 无 env bypass 且纳入 EXIT 清理。

本 PR 只交付代码与文档：没有 link tmux、修改 production `.env`、重启、部署、merge 或 dispatch QA。
破坏性阶段 B 仍须 founder 单独批准窗口，且不得进入普通班车部署。
