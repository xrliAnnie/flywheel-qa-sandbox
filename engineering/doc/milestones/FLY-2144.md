# FLY-2144 — 派发容量输入与旧依赖排序路径退役

**Status**: ⏳ Pending ship
**PR**: [#1043](https://github.com/xrliAnnie/flywheel/pull/1043)
**Date**: 2026-09-02

为 Lead 的派发判断新增只读容量事实：以 `memory_pressure` 为唯一内存口径，合成机器负载、admission/pressure hold、在跑 runner 与 Claude quota，并通过巡检 tick 和 master-token `GET /api/capacity` 两个出口提供。容量是 Lead 的判断输入，不是自动拒绝或排队 gate，既有派发/admission 行为保持不变。计数与百分比整数化，连续负载值保留最多两位小数；pressure-hold 来源与 operator/store-backed admission detail 在共享 snapshot builder 内按穷尽式 allowlist 净化，HTTP 与 patrol 两个出口一致降级恶意 provenance。不可解析的 hold 时间只降级手刹格；部分坏账号不再藏掉健康 quota，多个账号诊断也不会互相覆盖。

同一 PR 净删 `flywheel-dag-resolver`、未实例化的 `DagDispatcher` 与两个 v0.1 手动入口；稳定的 `DagNode` 类型收编到 edge-worker。最终代码头消费者 sweep 在主仓与 10 个已存在插件 cache root 均为零引用，缺失的外部 fork root 明确记为未检查。

lint、build、typecheck、新增残留守卫及 CI 结构守卫通过；edge-worker 105 个测试文件 / 1220 个测试通过，容量精确回归 45/45 通过。完整 package suite 的环境/既有失败及独立隔离证据记录在实施文档。round 3 的两个 MEDIUM 已按 Lead 治理作为必修落在 `34dd9be61`；随后 review 新报的默认放行 admission detail MEDIUM 已按同一口径以 TDD 修复在 `fb8ffe637`。QA 二轮的三个必修边界分别落在 `09d198013`、`33c7173be` 与 `b87ee007c`；本 milestone 恢复为 literal last commit 后，交接仍以新 final head 的 fresh formal review 与 GitHub CI 为准。
