# FLY-2031 — Raya V3 随身语音(B):常开流 + 念读筛选 + 用嘴批 ship

**Status**: ✅ Shipped(2026-08-31,founder 授权 Lead 直接 merge —— ship-card 通道因 FLY-2203 结构死锁)
**PRs**: raya#9(语音应用)· flywheel#1001(Bridge 批准权威)· flywheel#1003(本记录 / `__main__` 锚)
**Date**: 2026-08-30 → 2026-08-31

## 一、Raya 侧交付(raya#9,head a7c5b5f34)

交付了结构化语音 inbox、念读筛选与失败回退、带说话人归属的转达动作、同一文字权威通道的回执,以及绑定 gate/card/head 的口头 ship 批准链。Founder replacement rework 已净删除 custom barge-in 整层,恢复正常逐轮对话;新增 `VoiceTextMirror`,把 participant final、Raya final 与 thinking 状态镜像到语音房同路文字区。`ship_gate` 的 prompt cursor、speaker attribution、binding/card/context 校验与 fail-closed 解除合同未改。

已验:全仓 lint(180 files)、递归 build、typecheck、packages tests(contracts 62、voice 322、brain 125)与 root QA/probe 94/94,最终头上 603/603 全通过(该仓无 CI workflow,本地门是唯一门)。

## 二、Bridge 侧交付(flywheel#1001,head b3124108)

Bridge `voice-approval-source` 成为口头 ship 批准的唯一结果判定者:接收 Raya 原样 transcript,在 authority 边界统一处理 NFKC、标点/空白与相邻重复音节,再作全句精确匹配;`不确认`、`确认吗?`、`先别确认` 仍为 unclear。未改 consent evaluator、gate arming、speaker attribution 或 `non_founder_final`。配套 Raya PR #9 只路由候选回答并镜像 Bridge 结果,不再本地写「已批准」结论。

验证:`pnpm lint`、`pnpm -r build`、matcher 6/6、非 GUI core 219/219 通过;CI 11/11 SUCCESS。

## 三、最后一 lap:founder 实测失败 → 四件返工 → 复验

2026-08-31 founder 在真房实测「用嘴批 ship」,说出「确认」却被静默吞掉,系统随后还建议她「联系管理员 / 重新提交」。她在知情后选择「先修再 ship」(A 案),四件返工判据式下达并全部验过:

1. **可观测性先行** —— `ShipGateFlow` 三处静默 `return retry` 改走 `prepareRetry()`,记 `ship_gate_prepare_retry{itemId, reason}`(五种 reason);其余三处裸 return 的相邻 catch 本就有 evidence ⇒ 所有 retry 出口皆有事件。真机复现 `reason=prompt_not_confirmed`,前一晚定不下来的分支当场可定位。
2. **界面与 gate 同步** —— 教学句「要批准就说确认」从 ship prompt 中**删除**,改为独立 `SHIP_APPROVAL_CUE`,只在 gate 武装后注入并记 `ship_gate_approval_cue_confirmed`。真机时序:prompt 念完(全文零「说确认」话术)→ `ship_gate_armed` → 1 ms 后注入 cue → 念出并确认。卡片可被重念任意次而不会教她说确认。
3. **禁编造流程建议** —— 拒绝场景零命中「联系/管理员/重新提交/系统限制/负责人/可能是」,并回归零命中效果宣称词;与她 08-31 原话构成前后差分。
4. **批准词宽容匹配** —— 归一化仅在 normalize 层(NFKC + 去标点空白 + 折叠相邻重复),词表未动、仍是全串相等而非子串。新旧 matcher A/B 23 例:「确认认。」unclear→approve ✅;八个语气词(嗯/哦/啊/好/好的/嗯嗯/嗯。/哦哦哦)新旧一致仍 unclear;「不对」仍 reject;「不不对」「不不批」由 unclear 收紧为 reject。

## 四、边界(未因 merge 而消失)

- **格5 阳性全链未验**:「她本人说确认 → 判定 approve」按设计只认 founder 身份,任何 bot 替不了。合成音全链辅证只证明「转写能送达判定」(ASR 把合成的「确认认」转成了「確認人」,身份闸先拦)。
- **同音异形字边界**:宽容匹配覆盖叠字,不覆盖同音异形;匹配鲁棒上限由 ASR 决定,属结构性边界。真遇到时通道会请求重说,不会静默吞掉,也不会编造建议。
- 格2 筛选、格4 她的话落地、5.4 当面点头,以及上一 lap 修好的条间等待与纯汇报分流,她本人尚未体验。
- follow-up 七条(2178 插嘴独立成单 / 重念循环 / 已修未验两项 / 2030 阻断级前置 / 追问答不出 / relay 矛盾 / 同音异形字)见 ship report 与 QA verdict 第四节。

## 五、Ship 记录

QA 在最终头完成 pre-merge 只读复核:三个 PR 均 OPEN/MERGEABLE/CLEAN;flywheel 两个 PR CI 各 11/11 SUCCESS;raya 分支与 Bridge 分支相对已验字节**零差异**,故既有证据(603/603、23 例 A/B、r21 四项真机取证)全部有效、无需重跑。

founder 授权:2026-08-31 06:40 PT(msg 1543873180718989352,A 案)。ship-card 自动通道当晚不可用,根因见 **FLY-2203**(外部仓 producer 登记了产品仓 PR 号,land 恒 probe `__main__` 仓 → 撞同号老 PR;事后三条修复入口全封)与病根 **FLY-2192**(跨仓头轴污染 ×2)。历史五次跨仓 ship(FLY-2029/2074/2126/2130/2131)均正常,机制本身无缺。
