# FLY-2031 — Raya V3 随身语音(B)

**Status**: ⏳ Pending ship
**PR**: raya#9
**Date**: 2026-08-30

交付了结构化语音 inbox、念读筛选与失败回退、带说话人归属的转达动作、同一文字权威通道的回执，以及绑定 gate/card/head 的口头 ship 批准链。Founder replacement rework 已净删除 custom barge-in 整层，恢复正常逐轮对话；新增 `VoiceTextMirror`，把 participant final、Raya final 与 thinking 状态镜像到语音房同路文字区。`ship_gate` 的 prompt cursor、speaker attribution、binding/card/context 校验与 fail-closed 解除合同未改。

已验：全仓 lint（180 files）、递归 build、typecheck、packages tests（contracts 62、voice 311、brain 125）与 root QA/probe 94/94，共 592 tests 全通过。实际 Discord `voice-test-2` 的 R16 完成长念读（98 audible frames、3314 ms、最大静音间隙 267 ms），participant final、thinking 与 Raya final 共四条消息落入同一文字通道；65 秒无 spoken liveness，20 ms 音频时钟零 stall。最终 PASS 行 SHA-256 为 `a28a5640a3f02e3d733d04c6accac3624c995e8b17fae8114cb3fc8afbdaa64b`，runtime evidence SHA-256 为 `115f9979938f7d21fcc661957704f0001844d970c50267ee917e5116a4e63417`。

边界：R16 使用独立 QA bot，不冒充 Annie 真人原话，也没有触发真实生产 ship 外部副作用。未请求 Founder 测试；未 merge、部署或重启。Raya PR 的 current-head code review 完成后再 handoff。
