# FLY-2533 — Snapshot-owned phase protocols and actionable DAG entry errors

**Status**: ⏳ Pending ship
**PR**: #1175
**Date**: 2026-09-13

New V2/V3 workflow snapshots prepend package-owned, vendor-neutral phase protocols and seal them into existing digests. Missing/malformed/oversized protocols refuse materialization; old snapshots retain their pinned content. Generated legacy role projections preserve domain manuals with <=10% full-prompt growth. Legacy rosters reject QA/implement file aliases. Missing-binding 409 responses explain taskCategory and echo category/auth kind without creating default wildcard bindings.

Scoped regression, both adapter suites, lint and recursive build passed. Local aggregate package tests have config ledger timeouts, recorded separately in the implementation evidence; code review and exact-head CI are independent gates. Real 529 paired Claude QA claim and asset-removal evidence remain the QA node's acceptance work. No merge, deployment or production mutation performed.

CI inventory registration synchronized after its expected guard failure; fresh exact-head review and CI required for handoff. Baseline parser rollback checks accept new schema 2/3 content and digests unchanged.

QA ship-report ordering retains its original contract anchor; legacy selection test fixture uses independent QA/implement files. Updated scoped evidence: 181 passed/2 existing skipped; contract guards and 18 prompt pairs green. Fresh review/CI follows this rework.

Second CI correction batch preserves literal report commands, explicit generator mappings and the mechanical kill-path inventory. Lead-approved temporary domain fixtures preserve existing authority assertions: 343 tests pass, 1 existing skipped. Captured six published production templates also pass real-role validation/materialization across seven type/role combinations. Full prompt growth is at most 9.9142%; lint and recursive build pass. Fresh frozen-head review and CI remain required; no production dispatch or QA B/C acceptance claimed.
