# FLY-2104 — 周扫描裁决改发 Discord

**Status**: ⏳ Pending ship
**PR**: #972
**Date**: 2026-08-27

周日 flag 扫描的裁决页改由既有 `publish-report` 路径投递到 `#flywheel-notification`，不再创建 Linear 裁决 issue，并明确发送 0 候选状态。手动触发路由移到 404 兜底之前并执行完整扫描；候选稳定天数改用 store 的逐 `(flag, scope)` `value_last_changed`，保留 `*` 回退及 NULL/`no_clock` 的安全采样语义。
