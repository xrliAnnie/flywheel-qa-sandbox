# FLY-2394 — Raya Codex 模型默认切换到 GPT-6 Astra

**Status**: ⏳ Implementation complete; independent QA pending
**PR**: [raya#25](https://github.com/xrliAnnie/raya/pull/25)
**Date**: 2026-09-06

## 交付

- 删除写死的 Raya Codex model / reasoning / context 常量，新增共享、强校验的 session policy resolver；owner-only `raya.env` 可通过 `RAYA_CODEX_MODEL`、`RAYA_CODEX_REASONING_EFFORT`、`RAYA_CODEX_CONTEXT_WINDOW` 配置，默认分别为 `gpt-6-astra`、`xhigh`、`1050000`。
- brain、voice 和 C0 probe 的 thread start/resume、preflight 与 receipt guard 共用同一份已解析配置；实际 receipt model 与 configured model 不一致时继续 fail-closed。
- context metrics 区分 requested maximum 与 Codex 返回的稳定 effective window，并拒绝空值、非正数、漂移或超过 requested maximum 的样本；首个 accepted window 写入 requested/actual baseline evidence，summary 明示配置来自 process env、`raya.env` 或 default。
- Codex home 的顶层 `config.toml` 仍禁止写 session model 配置；voice 仅将 Astra 用于 Codex reasoning leg，Realtime 音频链路保持 app-server v2。
- README 记录默认值、配置边界和 `RAYA_CODEX_MODEL=gpt-5.6-sol` 回滚值；实施节点没有改生产 env、重启 Raya、部署或合并。

## 身份权限与 Review

- Lead 在 Raya Business 身份凭据恢复后于 2026-09-07 00:05Z 运行两个真实临时 session：默认 session 返回 `ok`，显式 `gpt-6-astra` session 返回 `astra-ok` 且 receipt 报告 `model: gpt-6-astra`，确认 Raya 身份已有 Astra entitlement。实施节点没有读取或输出凭据。
- `codex:rescue` review-only companion 已实际尝试，但 resident 外层 macOS sandbox 在读取仓库前拒绝 nested `sandbox-exec`（status 71），因此未伪记 PASS，也未使用 raw `codex exec`。
- request-driven cross-family review R1 在 `149cbca` APPROVED；Lead 要求同 PR 处理其两项 MEDIUM、三项 LOW advisory，修复提交为 `3383848`。R2 在 exact final head `3383848ebf9b507642b4ab92fe61f5f1d32a2565` 再次 `APPROVED`，无 HIGH/blocking finding。
- R2 剩余一项 LOW：metrics summary 可进一步突出 effective/configured ratio。Lead 明确不为该新 LOW 开第三轮，已按指令将其列为 Raya PR follow-up 第一条。

## TDD 与验证

- RED 先证明 metrics CLI 没有传播配置 context window、C0 config 没有解析 session policy；最小实现后对应 targeted suites 全绿：contracts 80/80、brain 19/19、voice 114/114、C0 2/2。
- Raya exact head：`pnpm lint`、`pnpm -r build`、`pnpm typecheck` 与完整 `pnpm test` 全绿；完整测试为 contracts 80、brain 134、voice 521、QA 134。该仓没有 `pnpm test:packages:run` 脚本，也没有新增 `scripts/__tests__/*.test.sh`。
- Flywheel 父仓：`pnpm lint` 与 `pnpm -r build` 通过。`pnpm test:packages:run` 在全仓并发负载下得到 teamlead 11,377 通过、27 个跨未改动区域的 timeout/tmux failures，另有 claude-runner prompt timeout；对应 claude-runner 用例隔离复跑 2/2、四个 teamlead 文件隔离复跑 46/46 均通过，未把完整全仓门伪报为绿色。

## QA 交接

独立 QA/ship 节点应在生产 checkout 配置或确认 Astra 默认，重启 brain，检查 preflight 与真实 receipt 显示 `gpt-6-astra`，并验证 `#raya` text + voice 路径。必要时以 owner-only env 设置 `RAYA_CODEX_MODEL=gpt-5.6-sol` 后重启来演练回滚；实施节点不 dispatch QA、不请求 ship、不 merge、不 deploy。
