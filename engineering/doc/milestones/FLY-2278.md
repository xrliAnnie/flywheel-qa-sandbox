# FLY-2278 — loop 冻结、恢复与改派语义收口

**Status**: ⏳ R9 reroute-failure correction complete; pending exact-head CI and R10
**PR**: https://github.com/xrliAnnie/flywheel/pull/1053
**Date**: 2026-09-03

本单完成 FLY-2248 收口后拆出的 loop 稳定性后半。冻结写入现在同时要求超过固定阈值、收件 session 仍为非终态、且共享活性分类器找不到窗口内心跳/状态变化/工人出站证据；任一条件不成立时 run、episode、event 与 alert 四张表零写入。`ship_parked`、`awaiting_review`、`design_done` 等常驻等待体继续按合法收件人处理。

终态未 ACK 的投递不再被 projector 过早核销：先进入固定 grace，期间有当前且活跃的 successor 就改派；无后继且缺活性证据时才写 `operator_required`，改派额度耗尽则保持 run 不冻结并经同一确认门处理。`runHeld:true` 与 `runHeld:false` 两种模式都保持单调，后台维护不会偷跑确认门。mailbox、turn wake、phase wake、rework、carrier 五个家族的子 attempt 都绑定真实物理主键；staged/applied reroute child 在 operations replay 物化 CommDB 行之前保持 live，补偿失败不消耗 reroute 配额而 operation generation 仍单调；rework/carrier 同时携带权威 route/redrive 版本，旧代事件不能推进或核销新代。

`GET /api/runs/:runId/holds`、stage 与 `POST /api/runs/:runId/resume` 已统一挂到现有 master bearer + loopback Host 边界。路径/body runId 不一致会 400，过期或 digest 冲突会 409，同 client request 重放返回幂等成功。run 只有在最后一个 run 级 hold 被解除时才 active，并在同一事务恢复 carrier；已消费 mailbox/turn 源按成功 no-op 投影 `hold_resumed`。

settled attempt 现在在同一事务把 open episode 关为 `terminal:settled:<reason>`，并立即离开 live 列表；所有逻辑调用点复用一个版本感知的 settle helper。CommDB 物理行已消费或清扫时也会收敛：有终态 owning run 记 `run_terminal`，否则记 `source_pruned`，不会永久留下 open episode。时间戳聚合先丢弃不可解析值再计算最大值，避免可空输入把 recent-active 守卫变成 NaN。

旧 `reconcileWorkflowReworkStalls` / `escalateWorkflowReworkStall` 路径与 `FLYWHEEL_ENGINE_REWORK_ALERT_MS`、`FLYWHEEL_ENGINE_REWORK_HOLD_MS` 两个旋钮已删除。rework received 观测并入既有 delivery-contract watch：30/90 分钟的 warning/severe 都重新走同一活性门，alive 时不建新 episode、不告警、绝不 hold。机制预算保持 0 新表、0 新列、0 新索引、0 新旋钮、0 新告警层、0 新合同 entry。

迁移只放宽 `workflow_delivery_operation.kind` 既有 CHECK，并对 live rework/carrier attempt 幂等补齐版本键。`scripts/fly2278-rollback-attempt-versions.sh` 默认只读 dry-run，`--apply` 用事件中的逐字 `before`/`after` 和 attempt id 做双 fence；非规范但合法 JSON 可 byte-exact 恢复，任何 drift、缺失或坏 payload 都 fail closed。

TDD/回归证据：R5 两条 finding 与 R6 advisory 各自 RED→GREEN，相关 reroute/settle 回归 23/23、全部 `fly2278-*` teamlead 测试 75/75，flywheel-comm 精确全套 129 files、1,772 passed、2 skipped；rollback shell 与 CI structure 守卫通过。`pnpm lint` 通过（仅仓库既有 warning/info），`pnpm -r build` 通过 22/23 workspaces。精确 `pnpm test:packages:run` 在 core 219/221 后仅被两条既有 macOS Terminal.app/HiServices 宿主测试阻断。精确 teamlead 全套达到 10,330 passed、6 skipped；五条负载超时按对应四个文件隔离复跑 69/69，剩余 host-only FLY-2118 tmux 含 TAB 窗口名案例为无关宿主基线。GitHub CI 是硬门。

PR #1053 首轮 CI 额外提供了一条真实 RED：FLY-1674 residue guard 拒绝了已批准的 `three_stage_turn_stuck` hold/event 标识。修复仅为该稳定标识的逐路径 + 逐 token compatibility 清单，没有加入通配豁免；本地 `fly1674-residue.test.sh` 从 58/1 转为 68/0，CI structure 与 FLY-2278 rollback shell 同轮保持 GREEN。

QA 529 房复验发现 §6 的 Lead-facing 文案尚未接入生产告警。返工把 `delivery_contract_frozen`、`delivery_reroute_outcome`、`delivery_operation_stalled`、run-bound `delivery_contract_stalled` 四类统一接入 `alert-kind-copy.ts`：每条都显式带 runId、证据戳与 `flywheel-comm hold list --run <id>` 正门；operator-required outcome 另带从同一 hold 形状注册表渲染的 `hold resume` + `requiredDecision`，phase wake 不建议不受支持的 cancel。apply 失败现在也发 outcome 后继续补偿/重试。单条三要素契约测试先以 10 个 soft assertion 失败再转绿，六个真实事件流文件 107/107、hold cancel 门链 6/6、teamlead TypeScript build 均通过；按 Lead 返工令未再执行本地全仓门，最终以 PR exact-head CI 为准。

返工后首轮 exact-head CI 又捕获同一稳定形状在新文案映射文件中的 residue 清单缺口；只增加 `alert-kind-copy.ts|three_stage_turn_stuck` 这一条逐路径、逐 token compatibility 记录，本地守卫由 67/1 转为 69/0。同期 Unit(heavy) 的未改动 claude-runner tmux 分类用例发生一次 `saturated`→`unknown` 波动，与 teamlead delta 无关，随新 head 重跑验证。

**本地全套例外：** `packages/teamlead/src/bridge/__tests__/patrol-orphan-sweeper.test.ts` 的 FLY-2118 real-tmux 用例相对 `origin/main` 逐字节未改，本机 tmux 3.7c 拒绝含 TAB 的窗口名 `SCRATCH\tTAB`；`packages/core/test/tmux-viewer.macos.test.ts` 同样未改，本机 Terminal/HiServices 不可用。teamlead 全套并发触发的五条超时已按对应四个文件隔离复跑 69/69；PR CI 全绿是权威，CI 红不算过。

消费者扫描在 `2026-09-03T12:16:53Z` 完成：`~/.flywheel/repos/claude-plugins-official/external_plugins` 与 `~/.claude/plugins/cache` 均 0 引用，主仓 `scripts/` 0 引用，`packages/` 只剩 `fly2278-retirement.test.ts` 这一条刻意的退役守卫。

本地 `/codex:rescue` companion 通过受保护入口在最终代码 HEAD 再次启动，但在读取仓库前被 resident macOS nested sandbox 以 status 71 拒绝；未使用 raw `codex exec`。正式 request-driven cross-family code review 已完成七轮处置：R1 `f3347400-3354-4542-828a-59bef724fc3c`、R2 `72b2cc3b-e27c-4143-b643-651049c61de2`（stale，未主张通过）、R3 `4d1054c1-f41c-485d-bab9-f580596a8279`、R4 `aa78ea7e-9cc3-410b-8bd6-ad0d85d62c5e`、R5 `74bb5026-123f-4c7d-ba07-19aa9e83efc9`；R5 的 1 HIGH + 1 MEDIUM 由 `cbe50e57b` 修复并分别配 RED→GREEN。R6 `7494a3ac-30ea-4a20-8497-ef16409a8ccf` 已 APPROVED，其一条 MEDIUM advisory 仍由 `483cfce78` 配专门 RED→GREEN 修复。R7 `946994c6-45c0-490c-af1d-4648c57873c9` 已在精确代码 HEAD `37161f740` 上 APPROVED，0 finding、0 advisory。

QA 返工后的 R9 code review 报告 HIGH `failed-reroute-retry-loop-is-unbounded`：持续失败会每轮新增 operation、child attempt 和 founder alert，且永远不到 operator 门。Lead 批准窄修正：不设循环上限、不加 knob，failed operation 仍不扣改派配额且 apply 失败不 hold；同目标首次自动失败后，下一轮以 `runHeld:false` 打开既有 `delivery_undeliverable_no_recipient` 正门。失败告警 UID 现在稳定绑定 source attempt，operator 通过现有 hold-resume 门仍可继续重试，同一 source 再失败不刷第二条告警。

真事件流用例 `bounds repeated compensated failures and exposes the existing operator door` 覆盖首次失败→下一轮恰好一个 operator hold→`requiredDecision: [reroute_to, cancel]`→run 保持 active→operator 授权重试再失败→失败告警仍唯一；相邻事件流还证明目标修复后通过同一门可成功改派。聚焦回归 7 文件 114/114、teamlead TypeScript build 与 `git diff --check` 全绿。锁定计划仅按 Lead 明令追加 §9 design-correction 记录 R9 原文与获批收敛方案；未改既有章节或 §8 延后边界。

本实现没有增加未批准 scope、dispatch QA、merge、deploy、重启服务或触碰 `CLAUDE.md`。
