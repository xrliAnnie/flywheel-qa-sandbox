# FLY-2673 — Codex 已换号通知同形

**Status**: ⏳ Implementation complete; exact-head review and PR CI pending
**PR**: #1259
**Date**: 2026-09-18

## 交付

- Codex 成功切号的 durable outbox 现按 Claude 当前 N1 结构输出：`Codex 已切号` 抬头、`quota:weekly` 原因、原/新账号邮箱，以及同宽 `text` 表格。通知保持 `quota_switch_confirmation`、普通信息级、无 mention，交给既有 notifier 做 durable delivery。
- 选择时把原/新账号、registry 邮箱与同一轮 observation 窗口写入安装事务；generation commit 只复制该 opaque 快照，Discord 渲染时再做严格解析。安装回滚后重选会覆盖旧快照；目标账号与最终安装材料不一致时整张通知降级为明确 `n/a`，不串用旧账号数字。
- Codex 当前协议只有未标注 duration 的窗口数组：仅恰好一个窗口可显示为 `weekly`；零个或两个窗口都显示 `n/a`。无邮箱时沿用 Claude 文案 `邮箱暂时未读到`，无 reset 时显示 `n/a`。重置时间复用 Claude 当前 founder timezone 格式。
- 旧 `codex_quota_install_material` 数据库会原位增加 nullable `notification_json`；旧行读取为无快照。快照序列化失败、超限、畸形或日期越界都只降级通知，不阻断凭据安装或 generation commit。
- 没有打开 `codex_quota_auto_switch`，没有改变 FLY-2523 activation gate，没有执行真实换号、真实 founder 通知、重启、部署或 QA dispatch。

## 验证

- TDD 行为红：既有 outbox 仍发英文 warning、没有 plain delivery，新增 N1 seam 断言失败；最小实现后转绿。
- 聚焦 Vitest：StateStore、runtime、coordinator、outbox、纯格式器共 5 个文件，68/68 通过。覆盖逐字 N1、无数值与歧义窗口、旧库迁移、回滚重选、陈旧快照拒绝和崩溃重放路径。
- 代码审查 round 1 找到 production coordinator wiring 丢弃完整 observation snapshot 的 HIGH finding；回归测试先证明 runtime spy 只收到两参，再将 observations 改为必填并贯通装配，相关测试转绿。最终 head 重新接受独立审查，不复用 round 1 票据。
- `pnpm --filter flywheel-teamlead build` 与 `pnpm -r build` 均退出 0。
- `pnpm lint` 扫描全仓后退出 1：5 个 error 与 23 个 warning 来自本单未改的既有文件（例如 `doc/engineer/research/new/FLY-1547-e2e/e2e-mailbox.mjs`）；本单 11 个文件的定向 Biome 检查退出 0，仅报告 `plugin.ts` 两条既有 `useConst` warning。
- 按 Lead 对 FLY-2467 的明确指令，没有运行本地 `pnpm test:packages:run`；精确 head PR CI 是 aggregate 权威。

## 边界

本节点不 merge、不部署、不重启 Bridge、不 dispatch QA、不请求 ship approval。本文档是开 PR 前的 literal last commit；后续代码审查与 CI 必须绑定其精确 HEAD。
