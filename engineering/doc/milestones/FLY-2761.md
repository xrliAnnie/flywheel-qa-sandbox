# FLY-2761 — 固定页「现在要你看」不再静默丢掉无链接 / 无会话的 founderAsk

**Status**: ⏳ Pending exact-head code re-review, full exact-head CI, and QA retest (qa@2)
**PR**: #1319
**Date**: 2026-09-25

产品判据：founder 面的任何清单，过滤条件只能是「这件事还需不需要她」，不能是「我们能不能把它渲染出来」。

FLY-2736 的 founderAsk（无 session、无 Epic parent、不在 Linear project/label 边界内）在固定页上少报 5h45m，
页面还看起来是干净的。两处成因都修了：`attentionAudience(page, founder=true)` 不再以「有无可点链接」丢行，
无链接的行照常列出、计数，行内显示 issue 标识 +「（无讨论串链接）」；founder_ask 在缺 Linear/Epic 元数据时
用自带的 identifier 作身份（来源标注为 `founder_ask` 行，不冒充 Linear），再走共用的 `chat_threads` 绑定解析，
以 ask 所在频道为权威，于是能拼出指向 ask 讨论串的链接。attention.v1 校验器只在该身份引用本行自己那条 ask
（fact ID、fact 与 since 来源一致，identifier 形状、无 Linear 标题）时放行，变异测试证明两处关键核对都有区分力。

页脚把两类事实分开写，数字对得上：「要你看 N 件，其中 M 件缺讨论串链接」与「已知 N 条记录，清单不完整（…）」。
Markdown 计数改为与 HTML 同口径，并去掉重复的「缺少讨论串链接的记录」折叠。bootstrap `pendingDecisions`
只含 `awaiting_review` 的 session，已在类型处写明不覆盖 founder_ask，未接入以免出现第二份 founder 待办。

证据：线上样本已于 09-22 被 founder 回复 settle，按 issue 约定改用 fixture 复现（先提交红测，改前 3/3 失败）。
阴性对照同一 fixture：改前打 ask 前后都是「现在要你看 · 0 件」，改后 0 → 1 件，行内链接指向 ask 讨论串，settle 后消失。
设计评审 Codex（gpt-6-astra，xhigh）R1 要求收紧 ask 绑定，R2 通过。本地定向验证：lint、teamlead 及依赖 build 通过；dependents typecheck 首跑因新 worktree
未构建 `flywheel-voice-bridge` 而在 voice-codex 失败，补建其上游后通过（此前误记为一次通过，已在 verification.md 更正）；直接消费者 46 文件 / 690 tests、flywheel-comm 42 tests 通过；`vitest related`
首跑 7 个环境性失败（runner TMPDIR 过长的 socket、full-access 根目录守卫、负载超时）在最终字节上以短 TMPDIR 重跑全绿。
明细见 `engineering/doc/FLY-2761-founder-ask-unlinked/verification.md`。

qa@1 返工（Lead 裁定进本 PR）：QA 在线上找到同一症状的第二条路径——FLY-2775 的 ask `d2184079` 在 issue 会话
completed、run 已完成、讨论串随合入归档后被 `visible` 过滤丢掉并减计数，因为 FLY-2597 的标题规则在 completed 时不给 answer。
现在未 settle 的 founder_ask 不再受标题态投影约束，始终出现在「现在要你看」；FLY-2597 规则只继续管普通徽章。
d2184079 形状 fixture 先红后绿；线上只读复算（只读连接证 `SQLITE_READONLY`、真 Linear）改前 FLY-2775 与新出现的同形状
FLY-2711 都不在页上、标题 5 件，改后三条未 settle 的 ask 全在、7 件。点名测试 39 文件 / 624 tests 通过，lint、build 通过。

实现节点没有请求 full CI、dispatch QA、merge、ship 或 deploy，也没有改在线 `teamlead.db` / `comm.db`；
exact-head full CI 与上线后复核由后续 QA/ship 所有者执行。本文件是 PR 的 literal-last commit。
