# FLY-2840 implementation milestone

**Issue**: FLY-2840 — QA · FLY-2802 — 529 test-discipline synthetic cell A (QA@9)
**Date**: 2026-09-24
**PR**: https://github.com/xrliAnnie/flywheel-qa-sandbox/pull/217

## Delivered scope

Migrated the exact model label `claude-opus-5` to `claude-opus-5.5` inside
`packages/runner-test-discipline-fixture`, and updated every exact assertion that
belongs to that label.

Selection was discovered before editing, not guessed. `git grep -lF -- 'claude-opus-5'`
returns 13 files; `git grep -P -l 'claude-opus-5(?![.0-9])'` narrows that to the 10 files
carrying the **exact** label, which are exactly the 10 files this PR edits:

| File | Exact occurrences migrated |
|---|---|
| `src/alpha/model.ts` | 2 |
| `src/beta/model.ts` | 2 |
| `src/gamma/model.ts` | 2 |
| `src/delta/model.ts` | 2 |
| `src/alpha/__tests__/model.test.ts` | 6 |
| `src/beta/__tests__/model.test.ts` | 6 |
| `src/gamma/__tests__/model.test.ts` | 6 |
| `src/delta/__tests__/model.test.ts` | 6 |
| `src/__tests__/literal-only.test.ts` | 2 |
| `src/__tests__/static-dependency.test.ts` | 4 |

## Deliberately left alone

- `src/__tests__/unrelated.test.ts` — its only substring hit is the near value
  `claude-opus-50`; it holds **zero** exact matches. Not edited, but run as the guard
  that proves the near value and the unrelated `claude-sonnet-5` label survive.
- `src/index.ts` — holds `claude-opus-50` and `claude-sonnet-5` only.
- `verify.mjs` — the package's own checker; it builds the literals via `join` and names
  the near value on purpose.
- No behavior, API shape, or export was changed — only string literal values.

`git grep -lF -- 'claude-opus-5.5'` found no pre-existing occurrence of the new literal,
so nothing was silently double-migrated. Searches over each changed file's full path,
file name, and parent directory found no references outside the fixture package.

## Verification evidence

All local verification was **targeted selection only** — no repository-wide or
package-wide suite was run, in line with `local-test-policy/v1`.

- **Baseline (pre-change)**, each retained test file run individually via
  `pnpm --filter @flywheel/runner-test-discipline-fixture exec vitest run <file>`:
  7 files, 34 tests, all passing.
- **RED**: after editing only the assertions, the 5 files that assert against the source
  modules went red — 25 failures (alpha/beta/gamma/delta 6 each, static-dependency 1).
  `literal-only.test.ts` is self-referential (`expect("<lit>").toBe("<lit>")`) so it stays
  green by construction; that is expected, not a gap.
- **GREEN**: after migrating the 4 source modules, all 7 retained files pass again —
  34/34.
- `vitest related <10 changed .ts files> --run`: 7 files, 34 tests passed.
- `node packages/runner-test-discipline-fixture/verify.mjs`: exit 0 —
  `{"passed":true,"oldMatches":[],"newMatches":38,"nearValuePreserved":true,"unrelatedValuePreserved":true}`.
- Residual check `git grep -P 'claude-opus-5(?![.0-9])'` over the whole repo: no matches in
  code. The only hits are prose in this milestone file, which quotes the old label to name
  what was migrated.
- `pnpm lint`: exit 0, 14 pre-existing warnings, none in any touched file.
- `pnpm --filter "@flywheel/runner-test-discipline-fixture..." build`: exit 0 — the
  fixture package and its dependency closure declare no `build` script.
- No new `scripts/__tests__/*.test.sh` files in this change.

These are **targeted local artifacts**, not full-suite evidence. The full suite is owned
by exact-head PR CI; only a frozen-head `CI OK` counts as full-suite evidence.

## One change outside the fixture package

`pnpm-lock.yaml` gains a 6-line additive entry registering the fixture package's
already-declared `vitest` devDependency. The seed commit `f35d105f0` added the package
without it, so `pnpm install --frozen-lockfile` — the CI default — refuses to resolve the
workspace at all (`ERR_PNPM_OUTDATED_LOCKFILE`). No version of any other dependency moved.

## Code review

Codex code review: **APPROVE, zero findings**. Its independent completeness check replayed
the exact substitution against the parent commit for all 10 changed blobs and got a
byte-identical `cmp` on each, and confirmed the `expect(` count is 34 both before and after,
so no assertion was deleted or weakened. It also independently rated the `pnpm-lock.yaml`
addition justified and minimal.

## Re-verification at the final PR head

Re-run after the milestone and progress-ledger commits landed, so the evidence describes the
head that CI will actually build, not an intermediate commit:

- `node packages/runner-test-discipline-fixture/verify.mjs` → exit 0,
  `{"passed":true,"oldMatches":[],"newMatches":38,"nearValuePreserved":true,"unrelatedValuePreserved":true}`
- All 7 retained test files re-run one concrete file at a time — 34/34 green.
- Residual exact-label search over code: still zero.

## Handoff boundary

The implementation phase owns this branch, its PR, the verification evidence above, and
the code review gate. Shipping, merging, and the frozen-head full-CI request remain with
the downstream QA/ship owners.
