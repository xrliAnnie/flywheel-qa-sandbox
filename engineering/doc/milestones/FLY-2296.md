# FLY-2296 — Codex 额度换模提醒出生即关闭

**Status**: ✅ Implementation complete; R6 final code review approved; PR #1060 ready for independent QA recheck
**PR**: https://github.com/xrliAnnie/flywheel/pull/1060
**Date**: 2026-09-03

## 交付

- runner home 在任何凭据或状态写入前，把 Codex 原生配置键
  `notice.hide_rate_limit_model_nudge = true` 钉入最终 TOML；已有 `true` 幂等，`false` 被覆写，
  `[notice.model_migrations]` 等子表保留，无法安全改写的 dotted/inline/重复形状 fail closed。
- pin 校验失败时显式清掉同 execution home 里可能残留的 managed `GH_TOKEN`，同时保留既有
  `auth.json` / `.active` 字节；正常 provision 继续沿用原有 0600/0700 权限与 finally scrub 边界。
- Codex Lead 的 read-only 与 production full-access 两条最终 assembly 路径都调用同一 notice
  postcondition；显式 false、已有未钉 notice 与坏 TOML 都给出可操作修复说明，不会静默启动一个会弹菜单的 TUI。
- 新增 unix-socket-only fake app-server 与真实 Codex TUI 探针。探针只复制去凭据后的
  `config.toml`，不复制 `auth.json`，用独立 tmux server 验证 missing/true/false 三态，并用一次性自修改副本证明
  config-copy guard 确实会变红；不切 luna、不增加 runtime flag/env switch、不改巡检或 goal pause/resume。
- founder HTML builder、设计阶段 legacy probe 与 fake-server cleanup 同步补齐 HTML escape、输入校验、shell quoting、
  显式失败和非 `ENOENT` 错误报告。legacy probe 现在在可写时使用短路径 `/private/tmp`，否则用
  `/tmp`；fake-server cleanup 行为测试的 socket scratch 也走同一短根，不会依赖 runner 的长
  `TMPDIR` 而撞上 Darwin AF_UNIX 104-byte 上限。
- 没有修改 `CLAUDE.md`。

## TDD 与验证

- runner notice/provision suite：106/106；R6 reviewer 在 reviewed head 跑最终四个 FLY-2296 runner suites
  合计 113/113；implement 另以 79-byte `TMPDIR` 跑 home/probe/support 三个 suite 112/112。
- 最终 rework slice：long-`TMPDIR` support 4/4；Lead shell suite 50/50；config flag-truth/scan 59/59；
  legacy-name guard 7/7；milestone layout guard 32/32。
- behavior mutation RED：
  1. legacy probe 改成 `exec  env` 后失去 `EXIT=`，公共 CLI 测试以 status 23 变红；
  2. fake server 改成 `if (false && error.code !== "ENOENT")` 后吞掉注入的 `EACCES`，进程退出码由期望 2 变 0；
  3. disposable probe 加改名 env switch 并注入额外 config，禁掉 runtime diff 后不再给 copy-integrity 诊断；
  4. 长 runner `TMPDIR` 被允许参与 legacy probe socket root 时，新路径 guard 以 status 73 变红；
  5. cleanup 行为测试在 77-byte `TMPDIR` 下以旧 `tmpdir()` scratch 重现单例 5 秒超时；改走短根后，
     同一测试在 78/79-byte `TMPDIR` 下稳定通过。
  最小修复后全部转绿；前三项不再读取生产源码并断言字符串。
- 真实 TUI self-check：无键与显式 false 都出现 `Approaching rate limits` / `Press enter to confirm`，
  `true` 不出现；额外 config mutation 以 exit 2 fail closed。
- 精确最终 reviewed head `b1715f293e4d5f50039f785a7ea430c9eeef59e0`：
  `pnpm lint` PASS（仅 14 条仓库既有 warning/info）；`pnpm -r build` 全 workspace PASS；
  `bash -n`、`shellcheck`、目标 Biome 与 `git diff --check` PASS。
- 精确 `pnpm test:packages:run` 已在 rework 后执行；本 Codex resident session 的 Terminal.app/HiServices
  返回 `Connection Invalid`，仅 Core 两个真实 AppleEvents 用例失败，其余 219/219 通过。独立 QA 在同一台
  机器的另一个 session 重跑 Core 221/221 全绿，因此这两例按 session/environment-specific 记录，不作产品失败。
- GitHub CI run `33824667333` 在精确 reviewed head 13/13 PASS，包含 Quick Gate、Unit (heavy/light)、
  三个 TeamLead shard、四个 shell shard 与聚合 `CI OK`；之前 Linux 红灯
  `mkdir: cannot create directory '/private'` 已由短根 fallback 修复并在 Unit (heavy) 变绿。
- 本单没有新增 root `scripts/__tests__/*.test.sh`；修改的 Lead shell suite 已按上述 50/50 单独执行。

## Review

- 本地 `codex:rescue` companion 在最终 reviewed head 按合同以 read-only `task --fresh` 重试，但 resident 外层
  macOS seatbelt 在读取仓库前拒绝 nested `sandbox-exec`（status 71）；没有调用 raw `codex exec`，
  也没有把失败尝试冒充为 verdict。
- 初始 request-driven cross-family review R1 在代码头
  `f33d1a8cca0438bc8544ebb982894da79d036fee` APPROVED；随后 QA 指出的三条 source-string 假绿都先做
  behavior-level RED，再修复。
- rework R4 在 `ff611568bb737d01351d33964fd66027c194d8a5` APPROVED，并指出 arbitrary runner
  `TMPDIR` 会恢复 Darwin socket 长度风险；该 advisory 由 `149bf1a6d` 以 RED→GREEN 修复。
  Gate `1238dafc-4daa-475e-adf7-ba952aa8177a`，request
  `118ac0c7-865f-4c7f-bb1b-dac67d812791`。
- R5 在 pre-kickback 代码头 `81f41590d5b1dd9d268f12375ace487145926eaa` 返回
  `reviewVerdict=APPROVED`、`reviewerVerdict=APPROVED`，无 HIGH/blocking finding。
  Gate `bcc86686-c317-4c6e-afa3-a06e7d0e2f8c`，request
  `df3d8e96-7529-4d08-aae9-b98b3097908c`。
- QA kickback 后，最终 R6 在精确 reviewed head `b1715f293e4d5f50039f785a7ea430c9eeef59e0`
  返回 `reviewVerdict=APPROVED`、`reviewerVerdict=APPROVED`，无 HIGH/blocking finding。
  Gate `8dd7b968-ae73-4ea6-8a15-5c96186d710b`，request
  `cecec4af-8c05-4cfc-96bb-f66456839267`。
- R6 advisories 已通过唯一 report lane 回传 Lead（receipt
  `0b282412-8ceb-4003-b45f-44a077b46a72`）：
  - MEDIUM Lead existing-subtable 保守拒绝与 root-dotted seed fleet fail-close，均为 PR body 写明恢复动作的已接受边界；
  - LOW 建议单列 socket-length 结构断言；本轮遵守 Lead 的单点修复范围，已有 long-`TMPDIR` 行为测试与
    Linux Unit (heavy) 双向覆盖，不借 advisory 扩张范围；
  - LOW legacy 设计探针使用可预测的短 base root；它是单用户/ephemeral QA support artifact，保留以满足 AF_UNIX 预算；
  - LOW scrub error 遮蔽原诊断与 python3 缺失提示不足，均为已复现并写明恢复动作的非阻断边界；
  - rework 前 stale milestone status 与旧 CI/review 头在本 literal-last milestone commit 中重钉并消除。
  `medium_low_findings_are_non_blocking_v1` 下不借 advisory 扩张已批准产品范围。

## 独立 QA handoff

在隔离 home 上分别跑 fresh runner provision、Lead read-only ensure-home 与 full-access ensure-home，解析最终
`config.toml` 并确认 notice key 为 `true`、原 model 不变、凭据权限/清理不回归。随后对真实生成配置运行
`scripts/codex-tui-nudge-probe.sh --expect nomenu`；在临时副本删除该键后运行 `--expect menu`，两条都必须通过，
且原 home 字节不变、无 `auth.json`/`GH_TOKEN` 进入探针目录。最后观察恢复后的驻留体继续 `Pursuing goal`，
不应出现 luna model drift；goal pause/resume 与账号轮换仍不属于本单验收。

本 implement 节点没有 dispatch QA、请求 ship、merge、deploy、切换模型或重启 Bridge/Lead 服务。
