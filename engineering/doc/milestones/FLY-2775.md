# FLY-2775 — Opus 线切到 Opus 5.5:新体用 5.5,在飞体不换

**Status**: ⏳ Pending effective code review and exact-head CI
**PR**: #1295
**Date**: 2026-09-22

`claude-opus-5-5` 于 2026-09-22 10:07 PT 对本账号开通,按 FLY-2766「按产品线绑定,各随自己
最新版」把 Opus 线从 Opus 5 抬到 5.5。FLY-1467 早已把「身份」与「绑定」拆开,所以这是**改绑定**,
不是改工厂:`model-builtins.ts` 加 `OPUS_55` / `OPUS_55_1M` 身份,`DEFAULT_OPUS_BINDINGS` 与
medium/light/trivial 三档翻到 5.5,`claude-opus-5` 自动降成只读 legacy 条目。

一条必须补的:`model-config.ts` 的 dispatch 白名单**按全名**保住 `claude-opus-5` /
`claude-opus-5[1m]`。一个 id 只在「被绑」时才自带 dispatch 面,退役那代会静默掉出白名单 ——
而 `workflow_run.snapshot` 冻的是 `dispatchPinned` 的**全名**,在飞 run 与历史 pin 会中途
`INVALID_MODEL`。**只按 id、不按别名**:那是 FLY-1496 的既有验收合同(把 `bindings.opus`
指向无 dispatch 面的模型必须让别名熄灭)。实施中曾一度连别名登记,被 `fly1496-qa-acceptance`
§4 照出来后撤回,并据此更正了 plan 的回滚说法 —— 全量回滚 = 回退代码常量;models.json
绑回只是部分回滚。另补 `claude-opus-5-5` / `[1m]` 费率(未登记 = 成本估算静默 $0)。

实测硬前提:**CLI ≥ 2.1.280**。2.1.278 用全名钉 5.5 直接 `API Error: 400`(服务端 `opus`
别名漂到 5.5 是另一条路,Flywheel 派的是全名)。本机 2.1.280。

部署由 Lead 执行,写在 PR body,**顺序是 Codex 代码评审 R1 纠正过的**:**先**删
`~/.flywheel/models.json` 里五项 Opus override(对旧二进制行为不变),**再**部署重启 ——
published 模板的 manifest 冻的是 seed 编译时解析出的全名,而 Bridge 只在启动时编译一次 seed;
先重启会把四个 system-owned 模板按旧 override 冻在 Opus 5,事后改文件只热重载配置、不重编 seed,
run-start 回执却显示 5.5。然后手动重新发布两个 founder-owned 模板 `tpl_code` / `tpl_simple_code`
(导入器对 founder-owned 静默拒收)。`tpl_eng_heavy` 已退役(FLY-1693),不在范围。

验证:config 904 通过(新增 5.5 绑定、退役全名仍派工、未知 id 仍拒、部署窗口用例);
token-usage 175;claude-runner 207;teamlead 与**同机基线做集合差**,本单引入的模型相关失败
为 0;FLY-1496 两套 shell 验收 11/0、4/0;lint / build / typecheck 全过。真实账号
`claude -p --model claude-opus-5-5` 回 `claude-opus-5-5`,阴性对照两条均按预期失败。

设计门:Codex 池 6 号全灭,按 Lead 裁定(question 7d72c521)走 Gemini API-key 独立评审
R1 APPROVED(0 BLOCKING,2 LOW 均吸收)+ leadAcceptance 收口。

代码评审:Codex 当晚恢复(personal 快照,隔离 `CODEX_HOME`)。R1(head `b58eec5de`)
CHANGES REQUESTED:2 BLOCKING 都打在部署说明上且都成立(先重启后改文件会冻结 system 模板;
`tpl_eng_heavy` 不会自动重 seed),3 LOW(三档 tier 改为从 `DEFAULT_OPUS` 派生 —— 真正的单一开关;
两条会同时匹配退役 id 的前缀/包含断言改精确;行尾空格)。五条全部处理,新增 seed 编译时序的对照用例。
收紧 `TmuxAdapter` 断言时它在本机红了,查明是宿主旧 `models.json` 把 `opus` 绑在 5 上
(第 4 条同类宿主耦合),改为以 adapter 自己调用的同一 resolver 为参照做精确比较。

实现节点未 dispatch QA、未 merge、未 ship、未改生产 models.json、未重启 Bridge、未发布生产
模板。本文件是 PR 的 literal-last commit。
