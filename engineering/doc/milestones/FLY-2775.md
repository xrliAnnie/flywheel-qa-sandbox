# FLY-2775 — Opus 线跟最新:新体自动用最新 Opus,在飞体不换

**Status**: ⏳ Pending effective code review and exact-head CI
**PR**: #1295
**Date**: 2026-09-23

founder 2026-09-22 起单把 Opus 线切到 5.5;2026-09-23 00:11Z 追加「不要 hardcode 成 5.5,以后有最新的 Opus
就自动切换」。第一版只是把绑定从 5 改钉到 5.5,QA 判 FAIL(同时 exact-head CI 因常驻规则字节预算红)。
返工照搬 Fable 线已有的 FLY-2766 机制,做成 Opus 同构的一份,并吸收了 Codex 设计审 R1/R2 的全部 BLOCKING。

**怎么跟最新**:updater 每次部署前跑 `opus-model-sync`。它问**本机 claude CLI** `opus` / `opus[1m]` 实际
启动哪个模型(读 `modelUsage`),要求 base 与 `[1m]` 成对,再用精确 id 准入,然后在锁下推进
`models.json` 的 `bindings.opus/opus1m` 和跟 Opus 的三档 tier,写后校验、失败回滚;版本变化告警
**从 models.json 推导**(每次运行比对上次已播报值,送达后才推进),不维护队列。发现权威选 CLI 而不是 `/v1/models` 排序,是因为实测别名由
**客户端按版本**解析(2.1.278 `opus` → 5,2.1.280 → 5.5),钉一个本机 CLI 不认的全名会直接 400。
探测有界(无 shell、stdin 关、输出封顶、超时杀进程组、隔离 argv),按 CLI 版本冷却避免重复付费。

**模板与 Lead**:模板统一存家族别名 `opus`,每个 run 起跑时解析并钉死精确 id(在飞不换)。
种子编译 / 启动预检 / 导入 / 发布 / 回滚共用一个持久化契约 —— 否则启动时种子 hash 对不上会卡死
Bridge(Codex 抓到,反证测试复现),或回滚把别名冻回全名(回滚用例抓到)。8 个 Claude Lead
早已存 `opus[1m]`,共享的启动解析器随绑定走,无需改 `projects.json`。

**其余**:熔断 = store 管理的 kill_switch `opus_model_sync_disabled`(漂移守卫拒收新的 `.env` 开关);
管理台 Lead 下拉加「Opus · 跟最新」;5.5 按公布价 $4/$20 计价(第一版错按 $5/$25);不加 Opus 家族
通配价;`model-routing.md` 最终采用 main 措辞,历史 bundle 为 241531 → 241533(+2 bytes),仍在规则预算内;
只读盘点脚本 `fly2775-opus-inventory.mjs`
作部署闸(今天生产 42 行待迁移 = 6 个模板节点 × 7 个项目)。

部署由 Lead 执行:合入重启(4 个 system 模板自动存别名)→ 跑一次 sync(自动把生产 models.json 推进到
5.5)→ 用受控写入器把 `tpl_code.qa` / `tpl_simple_code.qa` 改成 `opus` → 盘点 exit 0。回滚:打开 kill switch,
再在 models.json 钉精确 id,不改代码不重启。

验证只跑相关文件(Lead 禁止本机整包):sync+CLI+flag 85/85;种子/发布/迁移等 45 文件 928 通过
(含真实启动迁移 + 起跑钉死 + 跟最新的全链路用例);flag 注册表消费者 626/626;token-usage 177/177;
shell:updater 54/0、Lead 解析 19/0(`opus[1m]` → 5.5[1m],sync 推进后 → 6[1m])、flag freeze 46/0、盘点 4/0。

代码复审(Codex,返工):R1 在 `daeb18f8e` 给出 6 BLOCKING + 2 MEDIUM + 1 LOW。其中 5 条 BLOCKING 与全部 MEDIUM/LOW 已修:
归一化也必须先准入;通知两阶段(prepared 持久化失败即不写 authority,每次运行按实际 authority 提交或丢弃;
rollback_failed 持久化失败则交回 CLI 直投);有界进程溢出后不再累积、子进程退出不取消进程组 SIGKILL;
updater 用已验证包路径;盘点脚本 fail-closed;冷却只跳过探测;冷却键用 PATH 解析后的真实二进制。
第 5 条(「重启后 sync 前别名全部不可派工」)用实测驳回:模板 run 走注册表规范化,钉 `claude-opus-5` 仍可派工,
窗口期跑的是 Opus 5 而非故障,已用测试钉住并把部署说明写准。新增回归用例中 7 条在修复前的模块上变红(反证)。

R2 在 `b1d1f58d3` 给出 3 BLOCKING + 2 MEDIUM + 2 LOW,全部处置:终止后等进程组确认消失才返回(进程外用例,
撤修复即红);拿掉持久通知队列,改为每次从 authority 推导告警(并发抹除、失败回滚误报成功、关闭期不结算三条
一起消失);菜单回执按 run 快照重建;pre-sync 窗口用例走真实物化 + 启动解析。

R3 在 `20f2ccf27` 给出 3 BLOCKING + 2 MEDIUM + 2 LOW(全在告警子系统),Lead 裁定只修所列、走限定 R4:
状态文件不可写就不推进 authority;送达的变化记入有界 history;水位 compare-and-set;回滚失败持久化为标记,
告警重投到送达,且在之后某次校验通过前不宣布成功;观察者与事务共用一个判定;正常退出也清进程组。
R4(限定轮)重提并发 / 极端组合形态,Lead 裁定最后一轮只修四项:整次 CLI 运行一把排他锁(消掉并发类)、
回滚标记没记下时本次不宣布成功、送达即记 history、状态不可写时仍重投回滚告警。
R5 剩两项经 Lead 驳回进 Follow-ups;随后走 Bridge 复审 1458e9d5 = APPROVED(6 条非阻塞),按 Lead 裁定修了两条 MEDIUM:
推进时把其余 Opus 代从选择器退役(仍可按 id 派工),停用开关读不到按「已停用」处理并由 updater 传 `--db`。
复审 58698d7e(`c306bfd40`)= APPROVED(其新提的 1 MEDIUM + 1 LOW 进 Follow-ups);随后 PR 与 main 冲突
(`NON_FLAG_ALLOWLIST` 与 FLY-2766 同处追加),合入 origin/main 保留两条,合并头重新走 Bridge 复审。
QA@2 实测判据 3 端到端通过,但因新代码未登记进既有守卫清册判 FAIL;返工只补清册:新 shell 套件进 ci.yml、
子进程清册与 kill 点清册登记、去掉一条墙钟断言、停用开关注释与行为对齐。

Follow-up(Lead 裁定 (a)):从会话记录回填实际服务模型并比对告警;Fable 种子同样冻全名;两个历史 1M 费率缺口;菜单回执的路由级用例(R3 L1)。

实现节点未 dispatch QA、未 merge、未改生产 models.json、未重启 Bridge、未发布生产模板。本文件是 PR 的
literal-last commit。
