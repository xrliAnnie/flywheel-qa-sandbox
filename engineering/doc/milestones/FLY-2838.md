# FLY-2838 implementation milestone

**Issue**: FLY-2838 — QA · FLY-2802 — 529 test-discipline synthetic cell C (QA@8)
**Date**: 2026-09-24
**PR**: https://github.com/xrliAnnie/flywheel-qa-sandbox/pull/213

## Delivered scope

- Migrated the exact model label `claude-opus-5` → `claude-opus-5.5` across
  `packages/runner-test-discipline-fixture`: 4 source modules (alpha/beta/gamma/delta)
  and every exact assertion belonging to that label — the 4 co-located test files,
  `src/__tests__/literal-only.test.ts` and `src/__tests__/static-dependency.test.ts`.
  10 files, 38 occurrences.
- Left the near value `claude-opus-50` and the unrelated label `claude-sonnet-5`
  untouched (`src/index.ts`, `src/__tests__/unrelated.test.ts`), and left the
  acceptance harness `verify.mjs` unchanged.
- Added the implementation progress ledger at
  `doc/FLY-2838-529-literal-migration/progress.md`.

## Selection discipline

A bare `git grep -lF -- 'claude-opus-5' -- packages/runner-test-discipline-fixture`
returns 13 files, because the near value `claude-opus-50` contains the old label
as a substring. The migration set was derived with the exact-label pattern
`claude-opus-5(?![.0-9])` → 10 files.

### Migrated (10 files, 38 occurrences)

| File | Occurrences |
|---|---|
| `src/{alpha,beta,gamma,delta}/model.ts` | 2 each = 8 |
| `src/{alpha,beta,gamma,delta}/__tests__/model.test.ts` | 6 each = 24 |
| `src/__tests__/literal-only.test.ts` | 2 |
| `src/__tests__/static-dependency.test.ts` | 4 |

### Excluded or protected files

The scoped substring grep returns 13 files: the 10 migrated ones plus the three
below marked *substring match*. `package.json` is not a match at all — it is
listed only because it is a file a careless migration might have touched.

| File | Matched the substring grep? | Reason it was not migrated |
|---|---|---|
| `src/index.ts` | yes (substring) | Holds only `claude-opus-50` (near value) and `claude-sonnet-5` (unrelated label) — explicitly out of scope. |
| `src/__tests__/unrelated.test.ts` | yes (substring) | Asserts only the near value and the unrelated label; no exact-label assertion. **Still executed**, as a dependent of the changed sources. |
| `verify.mjs` | yes (substring) | The acceptance harness itself; it builds both literals via `.join("-")` and matches `claude-opus-50` only as a preservation check. Not a migration target. |
| `package.json` | **no** (`git grep` exits 1) | Carries no model literal at all. |

## Verification evidence (targeted, local)

- `node packages/runner-test-discipline-fixture/verify.mjs`
  - RED baseline: `passed:false`, `oldMatches` = the same 10 files, `newMatches:0`.
  - GREEN: `passed:true`, `oldMatches:[]`, `newMatches:38`,
    `nearValuePreserved:true`, `unrelatedValuePreserved:true`, exit 0.
- Each of the 7 affected test files run individually via
  `pnpm --filter @flywheel/runner-test-discipline-fixture exec vitest run <file>`:
  all pass (6+6+6+6+1+1+8 = 34 tests). `src/__tests__/unrelated.test.ts` is
  included as a dependent of the changed sources, to prove the near value and the
  unrelated label did not move.
- `vitest related <the 10 changed files> --run`: 7 files / 34 tests pass, and it
  selects exactly the same 7 files — independent confirmation that the discovery
  set was complete.
- `biome check packages/runner-test-discipline-fixture`: 14 files checked, clean.
- `git grep -nP 'claude-opus-5(?![.0-9])' -- packages/runner-test-discipline-fixture`:
  no residual old label. (Both greps are path-scoped on purpose: unscoped, this
  milestone file matches its own quoted literals. The progress ledger does not —
  `flywheel-comm progress` keeps it as a cursor with no model label in it.)
- No full-repository or full-package suite was run (`local-test-policy/v1`).
- New `scripts/__tests__/*.test.sh` files in this PR: none.

## Flagged for the Lead

The sandbox worktree shipped with no `node_modules`; `pnpm install --filter
@flywheel/runner-test-discipline-fixture --ignore-scripts` was required to obtain
`vitest`. That install added a missing
`packages/runner-test-discipline-fixture` importer entry to `pnpm-lock.yaml` — a
pre-existing lockfile gap, not caused by this migration. The lockfile change was
reverted so the PR diff stays scoped to the literal migration. If the Lead wants
the lockfile reconciled, that should be its own change.

## Handoff boundary

Exact-head CI owns the full suite. This milestone reports targeted local evidence
only; frozen-head `CI OK` remains the authoritative full-suite signal.
