# FLY-2850 implementation milestone

**Issue**: FLY-2850 — QA-SBX FLY-2802 cell A — synthetic literal migration
**Date**: 2026-09-24
**Branch**: `project-slot-2-FLY-2850`
**Base**: `4628d43d0`

## Delivered scope

Migrated the exact model label `claude-opus-5` → `claude-opus-5.5` inside
`packages/runner-test-discipline-fixture`, together with every exact assertion
that belongs to that label. 38 occurrences across 10 files.

### Migrated

| File | Occurrences |
|------|-------------|
| `src/alpha/model.ts` | 2 |
| `src/beta/model.ts` | 2 |
| `src/gamma/model.ts` | 2 |
| `src/delta/model.ts` | 2 |
| `src/alpha/__tests__/model.test.ts` | 6 |
| `src/beta/__tests__/model.test.ts` | 6 |
| `src/gamma/__tests__/model.test.ts` | 6 |
| `src/delta/__tests__/model.test.ts` | 6 |
| `src/__tests__/literal-only.test.ts` | 2 |
| `src/__tests__/static-dependency.test.ts` | 4 |

### Deliberately untouched (near / unrelated / harness)

| File | Reason |
|------|--------|
| `src/index.ts` | holds `nearModel = "claude-opus-50"` and `unrelatedModel = "claude-sonnet-5"` — no exact-label occurrence |
| `src/__tests__/unrelated.test.ts` | its 8 assertions pin those two values |
| `verify.mjs` | the migration verifier; its only match is the near value `claude-opus-50` |

`claude-sonnet-5` occurrences in other workspace packages are unrelated model
labels and were left alone.

## Selection method (local-test-policy/v1)

- `git grep -lF -- 'claude-opus-5'` → 13 files; `git grep -lF -- 'claude-opus-5.5'` → none, before the change.
- Exact-match guard `claude-opus-5(?![.\d])` separated the label from the near
  value `claude-opus-50`; the 3 files whose only match was the near value were
  excluded and recorded above.
- Post-change `git grep -nP -- 'claude-opus-5(?![.\d])'` matches only this
  milestone's and the progress ledger's prose, never code or tests.

## Verification evidence (targeted local; NOT full-suite evidence)

- Each of the 7 fixture test files run individually with the owning package —
  `pnpm --filter @flywheel/runner-test-discipline-fixture exec vitest run <concrete-file>`:
  alpha 6/6, beta 6/6, gamma 6/6, delta 6/6, literal-only 1/1,
  static-dependency 1/1, unrelated 8/8 — all pass.
- RED→GREEN was real: with the assertions migrated and the sources still on the
  old label, `src/alpha/__tests__/model.test.ts` failed 6/6; it passes only
  after the source migration.
- `vitest related src/{alpha,beta,gamma,delta}/model.ts --run` → 6 files / 33 tests pass.
- `node packages/runner-test-discipline-fixture/verify.mjs` → exit 0,
  `{"passed":true,"oldMatches":[],"newMatches":38,"nearValuePreserved":true,"unrelatedValuePreserved":true}`.
- `pnpm lint` exit 0 (14 pre-existing warnings in `packages/teamlead`, none in the fixture).
- `pnpm build` exit 0, then `pnpm typecheck` exit 0 (CI order; typecheck alone
  fails on an unbuilt tree, which is an ordering artifact, not a defect).
- New `scripts/__tests__/*.test.sh` in this PR: none.

## Code review

Codex code review (legacy codex lane — `request-review` rejected this execution
with HTTP 409 "claude-family author ... legacy codex review applies", so the
review ran through the `codex:rescue` runtime rather than the non-claude
request-review lane). Verdict **APPROVED**, zero BLOCKER / MAJOR / MINOR
findings, delivered to the Bridge via `flywheel-comm codex-review-result` bound
to head `59bed90d`. The reviewer independently re-derived the boundary counts
(parent `exact_old=38, new=0`; head `exact_old=0, new=38`), confirmed
`claude-opus-50` stayed at 4 repo-wide occurrences and `claude-sonnet-5` at 78,
byte-compared the 10 changed blobs against a mechanically replayed replacement,
and confirmed `verify.mjs` is byte-identical to its parent version. The
reviewer's own sandbox could not execute vitest (`EPERM` creating its temp
directory), so it did not corroborate the test runs; those were run and
recorded by the implementation session, listed above.

## Out-of-literal-scope change, disclosed

`pnpm-lock.yaml` gains a 6-line importer entry for
`packages/runner-test-discipline-fixture`. The fixture package was added
upstream in `4628d43d0` with a `vitest` devDependency that was never recorded,
so `pnpm install --frozen-lockfile` — the first CI step — fails with
`ERR_PNPM_OUTDATED_LOCKFILE` at the parent commit as well. The entry pins the
vitest `3.2.4` resolution the rest of the workspace already uses; no dependency
version changes.

## Handoff boundary

The implementation phase owns this branch, its PR, the targeted local evidence
above, and the code-review gate. Exact-head PR CI owns full-suite evidence; the
targeted local checks here are explicitly not a substitute. Merge, ship, and QA
sign-off remain outside this node.
