# FLY-2368 — Flag 文案人话化

**Status**: ⏳ Implementation complete; PR CI pending
**PR**: pending creation
**Date**: 2026-09-05

## 交付

- 当前 registry 的 23 条 flag 都新增了非空 `whenOn`，逐条说明「打开 / 取值代表什么」；新增 authoring guard 和精确文案表测试，后续缺文案会被拒绝。
- 管理台 Flags 页和共享 flag 卡片只从 `whenOn` 读取业务含义；缺失时明确显示「这里不猜」，不再根据 `_disabled`、`polarity` 或 `default` 推断。
- 管理台把业务含义与当前值 / 默认偏离拆开显示，技术 `description` 不再作为 founder 主说明；所有 registry 文案仍经既有 HTML escape。
- `validateOnMeansContract()` 删除 `_disabled` 名字后缀启发式；改名不再改变语义校验结果。
- `governance_gate` 在 `packages/` 与 `scripts/` 保持零引用；authoring runbook 明写「治理性策略不做成 flag,写死在代码里,要改走 PR」。
- 没有更改任何 flag 的 `default`、`polarity`、当前值、scope、source、valueKind 或读取路径。23 条不变量投影 SHA-256 仍为 `6d671432f1244e529fc8dbba38f2dfe8a89da113b533ac430113e794946e5506`。

## TDD 与验证

- C1 RED→GREEN：registry guard / 23 条精确文案 / 空白拒绝 / 名字改写不影响校验；config 全套 48 files、766 tests 通过。
- C2/C3 RED→GREEN：resolve、management projection、snapshot、UI contract、DOM 与共享卡片覆盖；focused management 102 tests 通过。
- TeamLead CI-like 三分片共通过 11,236 tests（另有既有 skips）；一个受跨文件状态影响的 consent 用例单线程复跑 22/22 通过。macOS 本机真实 tmux fixture 因窗口名含 TAB 被宿主 tmux 拒绝，未修改该无关路径，最终由 Linux exact-head CI 判定。
- FLY-2105 关联守卫 `qa-generalized-helper.test.mjs` 8/8 通过；本分支没有新增 `scripts/__tests__/*.test.sh`。
- `pnpm lint` 通过（仅仓库既有 warnings）；21 个 workspace 的串行 build 与 typecheck 全部通过；`git diff --check` 通过。
- 1280px Chromium 实页视觉核验通过，控制台 / 服务端错误均为 0；人工抽查 `alert_system`、`cmux_rebind_disabled`、`cmux_watcher_rebuild_disabled`、`database_archive`、`doc_flow`，人话含义与当前值 / 默认状态一致且未显示工程 description。

## 评审与边界

Request-driven cross-family code review R1 在 `addc585f1c1868aa76e746d8e10cb7e6dcdf29a9` 返回 `APPROVED`，没有 blocking finding。非阻塞 advisories 已按合同回报 Lead；其中每周留/清页 wiring 按 `[lead-instruction 2368-scope-cap-20260905T0833]` 明确 defer，本单不扩大 retirement-scan 范围，其余呈现与未来一致性建议留作后续判断。

本实现没有改获批 plan、`CLAUDE.md`、线上配置或治理策略，也没有 dispatch QA、请求 ship approval、merge 或 deploy。包含本 milestone 的字面 branch head 将由 PR exact-head CI 执行全仓 aggregate；CI 未通过前不完成 implement handoff。
