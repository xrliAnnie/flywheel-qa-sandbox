# FLY-2445 — Raya 迁为标准 Codex Lead

**Status**: Implementation complete; exact-head review and CI pending
**PR**: https://github.com/xrliAnnie/flywheel/pull/1134
**Nested PR**: https://github.com/xrliAnnie/raya/pull/61
**Date**: 2026-09-08

## 交付

- Raya 以标准 `codex-app-server` / `full-access` TUI Lead 注册进 Flywheel，复用公共 registrar、mailbox pump、Bridge outbound、summary receipt、通用 carrier 与恢复路径；没有新增 Raya 专用运行壳。
- 公共注册契约补齐 CoS context、roundtable 与 alert；主动出站以 local journal/outbox、Bridge dedup 和非空 message receipt 形成可恢复闭环，ambiguous 结果不盲重发。
- `lead_actions` 现在按标准 `outboundMode` 选择唯一 credential：Raya 的 Bridge 模式无 direct fallback；既有 direct full-access Lead 继续只用 `DISCORD_BOT_TOKEN`，不要求 Bridge 配置。
- updater 以 P0–P7 受控窗口迁移，P3 quiesce、P4b cursor seed/readback、P6 完整证据校验、v2 30 键 deploy receipt 与两仓 rollback pair 全部 fail-closed。
- Raya 仓删除旧 Discord ingest、14 人名册、brain/voice daemon、App Server/thread driver、自建 launchd、可执行 probes 与 2379 旧壳文字通路；只保留 persona、CoS 业务、summary/业务数据与不可执行历史审计材料。
- CoS 的 summaries、日报、portfolio、问题/会议状态继续由 `packages/cos` 承载；共享平台尚未提供的 transport 显式 unavailable，不回落旧 driver，也不伪造 delivered 状态。
- Release-gap 返工补齐 packaged helper 首次 adoption、跨 Git 默认分支的 updater fixture、QA Bridge outbound 注入、installed-package Raya register→preflight，以及 announcement unavailable 的 terminal receipt。

## 验证

- Raya：`pnpm test` 29 files / 138 tests，`pnpm typecheck`、`pnpm build`、`pnpm lint` 全绿。
- Flywheel：`pnpm lint` exit 0（3043 files，14 warnings / 0 errors），`pnpm -r build` exit 0；原 TeamLead 变更集 454/454、review 修复集 209/209、Edge 14/14、TUI home 62/62、新增迁移 shell 11/11，以及 registrar/carrier/updater/convergence/roster/alert/recovery 定向 suites 全绿。
- 新增/返工 shell 覆盖：installed payload smoke 19/19、packaged seams 17/17、converge 15/15、强制 host `init.defaultBranch=master` 的 updater 31/31、Codex QA deploy 22/22。
- 精确 `pnpm test:packages:run` 如实为非绿：所有已运行 assertion 通过，claude-runner 45/45 files、1105 PASS / 2 SKIP 后发生 Vitest worker `onTaskUpdate` RPC timeout，命令 exit 1 且后续 workspace 未跑。此前机器级 ProofShot 锁争用在锁释放后由隔离 65/65 与完整 Comm 149/149 files、2128 PASS / 2 SKIP 复绿；均未被冒充为根全包 gate 通过。
- Raya 活动源码、脚本、依赖与发布入口零自建 Discord/App Server/roster/launchd 驱动；28 个旧术语命中仅位于锁定计划允许保留的历史 docs/evidence，persona 另有一条禁止自建 roster 的负向约束。
- 第一轮 code review 的唯一 HIGH（direct full-access Lead 被强制走 Bridge）已由 `91109faac` 按 RED→GREEN 修复；其余 5 MEDIUM/3 LOW 已以 findingKey 写入实施证据作为 follow-up，未改冻结计划、未扩大批准 scope。Lead 指定返工已落在 Flywheel `086ce8ec0` 与 Raya `d2c5de5`；最终 exact-head review 仍待重新登记。

## 边界与 handoff

- 本提交是 Flywheel 分支的 literal last commit；实现证据见 `engineering/doc/FLY-2445-raya-standard-lead/implementation-evidence.md`。冻结后只运行 exact-head review、push 与 PR/CI handoff，不再移动 head。
- 本迁移删除了 FLY-2383 的 `scripts/qa-voice-concurrency-soak.mjs`：它属于获批删除的旧 Raya voice-soak driver，不是共享平台替代实现；PR body 必须显式披露。
- Mufasa 是受本次公共出站改动影响的既有生产 Lead：其主动 `discord_send` 现在经 `POST /api/lead-outbound/send` 进入 Bridge；这不是 Raya-only 改动，PR body 必须显式披露。
- 本节点未修改生产 registry/mailbox、未重启 Bridge/Lead、未安装 plist、未 dispatch QA、未请求 ship、未 merge 或 deploy。真实 `#raya` 切换与 summaries 连续性只能由获批部署窗口的 founder-only-authority R1 v2 `deploy-receipt.json` 验收；合入不等于上线。
