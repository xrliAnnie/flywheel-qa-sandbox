# FLY-2867 — 529 房额外 Claude Lead 收件箱不起 + Codex 房清理不收敛 + 语音房租约残留

**Status**: ⏳ Pending QA retest (QA attempt 1 passed every hard criterion on `bb6e75973`; routed back only because the PR went DIRTY against main — merged `origin/main` at `a084f3a99`)
**PR**: #1323
**Date**: 2026-09-24

三个独立根因，都已在本机红绿复现（C 为 Lead 追加的同类收敛缺口）。

**A · 额外 Claude Lead 的 `.inbox-ready` 租约永远不出现。** `claude-lead.sh` 只有在被测工作树存在
`packages/inbox-mcp/dist` 时才注册 flywheel-inbox MCP，否则只往 tmux pane 打一行 WARNING 继续启动；
`test-deploy.sh` 预检只构建 teamlead 的依赖闭包，而 inbox-mcp / terminal-mcp 都不在里面。于是凡是只跑过
预检的新工作树（FLY-2862 即是），Claude 载体 Lead 都起不来收件箱，120 秒后无原因中止。与额度无关。
真 `claude-lead.sh` dry-run 复现：缺 dist 时 launch plan 无 `MCP_SERVER flywheel-inbox`，构建后出现。
修复：预检构建两包并断言 dist 入口（缺则 exit 20 点名）；租约超时给出有名原因
（`inbox_mcp_unregistered` / `mcp_config_missing|stale|unreadable` / `lease_absent|live|pid_dead`，只读 `.mcp.json` 的键）。

**B · 中止/teardown 清理不收敛。** 停机要求 home「运行过」就必须找到 `pid-update-loop`；slot home 跑
managed Codex daemon 时根本没有 updater，且 daemon 记录名是 `daemon.pid` 而非 `app-server.pid`，
于是每次 Codex 载体房清理都报 `updater-converge result=not-found`、保留锁。修复：找不到 updater 不再单独判失败；
retire 前统一做按 home 锚定的进程残留普查（只读、有界），为空才收敛，有残留（含崩溃后遗留的 TUI 客户端）
仍 fail-closed 并报 `pids=`；`daemon.pid` 计入「运行过」证据。不新增任何杀进程路径。

**C · teardown 留下语音房租约。** `/tmp/flywheel-voice-room-<guild>-<channel>.lock` 只在 `fly2655-voice-room.mjs stop`
里释放，teardown 不经 `stop` 就拆房时它永远留着，别的 slot 在该语音频道一律冲突。修复：`start` 把 detached 守护进程的
pid+`ps` 身份写进租约；teardown 在删 slot 目录前调用新 CLI `release-slot-leases` 释放本 slot 的租约（守护进程仍活则保留并报告，
失败只告警不阻断拆房）；`acquire` 对「主人 slot 目录已不存在且守护进程不在」的陈旧租约经墓碑改名原子回收，其余照旧冲突。
node 用例 4 新 + 1 改（临时 lease root）、fly1389 E3（旧 teardown 下红）；真实语音锁全程未触碰。

**验证。** 本机：fly1663 60/60（新增 6 例，修复前红）；fly1389 27/27（新增 P1/P1b/P2 与 C 断言，
对旧实现 4 红）；新文件 `fly2867-claude-lead-inbox-lease.test.sh` 4/4（真 claude-lead.sh + 真 inbox-mcp
把租约写到 test-deploy 等待的路径）；17 个直接消费者 shell 套件与 2 个 node 套件全绿；kill-path 清册新增 5 条；
`pnpm lint` 0 error。真机：经 Lead 同意，用修复头 `test-teardown.sh 2` 一次 rc=0 收掉了卡在 `claiming`
的 slot 2（正是缺陷 B 的残留形状），无需手删文件；证据在 `~/.flywheel/qa-evidence/slot-2/20260925T0446Z-2867-teardown-proof/`。
N-to-N 真房在评审头 `acb198024` 上 rc=0（slot 4 被 FLY-2711 占用，经 Lead 同意额外 Lead 用同为 claude-code 载体的 slot 3）：
额外 Claude Lead 启动 41 秒即 `lease and channel live`（此前每次 120 秒超时），`.mcp.json` 注册了 flywheel-inbox，
租约 pid 即本工作树的 inbox-mcp；拆房 rc=0。中止证明：codex 主 Lead 就绪后令额外 Claude Lead 起不来
（harness 接缝 `FLYWHEEL_QA_LEAD_WRAPPER=/usr/bin/false`）→ `campaign_abort` 的清理无 `updater-converge` / `home-residue`
报错、slot 2/3 锁释放、`cdxh/` 无 `auth.json`，随后 teardown rc=0。`--lead-ready-timeout 10` 触发不了中止（热启动几秒即就绪），
不可作确定性手段。证据：`~/.flywheel/qa-evidence/slot-2/20260925T0802Z-2867-n2n-fixhead/`、`…/20260925T0807Z-2867-abort-proof/`；
room-info 在 PR 正文。未做（QA 硬判据）：founder 身份消息 → 额外 Lead 回复的整圈。

代码评审在 C 落地后对新头另走一轮。设计评审：Codex 额度至 2026-09-30 耗尽，经 `review_same_family_allowed` 走 Bridge 同族评审，R1/R2 均 APPROVED，
非阻断 findings 的处置记在 plan §6。代码评审同走 Bridge 同族通道：R1 APPROVED（3 LOW），其中两条
（主 Lead channel 阶段超时补 `reason=channel_not_live`；`find` 失败不判 stale）已修并补用例，环境变量盲区为计划已接受的边界。

**QA attempt 1（`bb6e75973`，真 529 房）。** 硬判据全过：阴性对照（修复前 `e5451989` 同命令复现 120 秒租约超时 +
`updater-converge not-found`，slot 2/3 锁残留）；中止路径 teardown 一次 rc=0、锁释放、无手删；修复头在无 inbox-mcp dist 的全新工作树上
预检自建、部署 rc=0、额外 Lead 提示后 4 秒 lease+channel live，经插件同款 chat-ingest 替身入站后 Lead 29 秒在真 Discord 回复；
Codex 房半拆状态 teardown rc=0；语音租约模块探针 4/4 + 真 teardown 释放合成的 slot-3 租约。唯一退回原因是 PR 与 main 冲突
（Lead 裁定 d2d7b82d 选项 a）。

**冲突返工。** 合并 `origin/main`（`a084f3a99`，11 个提交）；唯一文本冲突在 `kill-path-inventory.json` 同一插入点
（main 加 `fly2829-node-registry`，本分支加 `fly2655`/`fly2867` 条目），按扫描器 `localeCompare` 顺序两边全留，782 = 775 + 7。
`test-deploy.sh` / `fly1389` / `ci.yml` 为自动合并，main 的改动（FLY-2873 Codex 传输、FLY-2829 CI 接线）与本分支改动区段不重叠。
合并树上复跑：kill-path 清册 vitest 5/5、`ci-structure` PASS、`pnpm lint` 0 error，以及本单与 `test-deploy.sh` 直接消费者 shell/node 套件（结果见 PR 正文）。
合并出的新头重走 Bridge 同族代码评审（旧头批准绑头不搬）。

本文件是 PR 的 literal-last commit。
