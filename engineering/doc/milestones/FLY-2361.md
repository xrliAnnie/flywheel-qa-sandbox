# FLY-2361 — Resident Codex Lead 生命周期日志轮转与聚合

**Status**: ⏳ Pending review
**Date**: 2026-09-13

brain/lifecycle.jsonl 复用现有日志轮转工具：2,000,000-byte 活跃阈值、24h 活跃年龄轮转、最多 7 个归档并淘汰超过 7 天的归档。首次各频道 attempt/ok、失败及恢复事件保留；重复成功轮询改为五分钟计数。heartbeat 仍逐次原子更新，patrol 与传感器判定不变，Raya 激活手册的首次事件检查保持兼容。

24h 模拟：86,400 次轮询、8 次失败、9 次首次/恢复成功、500 条消息，共 155,999 bytes；3,500 次失败跨轮转完整性测试通过。配置轮转、observer、patrol、fleet sensor 与既有 shell 轮转验证通过。lint/build exit 0；首次全包测试的三个 config 超时如实保留，单 worker 对应两文件 38/38 复核通过，低并发全包重跑仍因 claude-runner onTaskUpdate 超时 exit 1（1,268 通过、2 跳过），不声称本地全包绿。

真实部署后 24h 主机观测尚未执行；本实现没有部署或重启 Lead。完整合同、retention 边界、回滚说明及验证限制见 `engineering/doc/FLY-2361-lifecycle-log-retention/implementation.md`。按 Lead simple_code 裁定直接实施；因 codex:rescue 沙箱 EPERM，代码审查使用 Lead 明确授权的最终 HEAD Bridge review_code gate。

按 Lead 恢复指令无冲突合入 main 0c923c697（2253afa5d），无额外功能修改。同步后 lint/build exit 0，config 19/19、相关测试 80/80（含完整 24h 与失败保留）、shell 14/14；最终 HEAD 重新绑定代码评审与 CI，回执见 PR #1174。
