# FLY-2130 — Raya 排会同步 Google Calendar

**Status**: ⏳ Pending ship
**PR**: #979 (Flywheel docs) · xrliAnnie/raya#7
**Date**: 2026-08-28

Raya 在 Discord 会议 thread 落地后，通过本机 `gog` 的 founder `personal` 账号创建私密 Google Calendar 事件，写入时间、参与 Lead、议题与 thread 链接；精确改期更新同一事件，取消时删除。Calendar 失败保持排会成功并显式告警，以 5 秒命令超时和 60 秒退避限制队列延迟；receipt 与私有幂等属性持久化，不新增依赖。
