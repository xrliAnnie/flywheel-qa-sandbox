# FLY-2676 — readiness 失败可见降级到手工切号

**Status**: ⏳ Pending exact-head review R3 and CI
**PR**: https://github.com/xrliAnnie/flywheel/pull/1246
**Date**: 2026-09-17

Codex 额度事件现在先读取同一份结构化 automation availability；launch flag 关闭、readiness receipt 缺失/无效、authority/credential/canonical/runtime 不可用时，自动机制保持关闭，不创建自动 incident、轮换或恢复动作。每个真实额度事件都会落一条独立、可去重的 N11 `codex_quota_automation_disabled`，例如“自动切号关着：readiness-receipt 不存在。需要手工切号。”；普通新 admission 退回 Lead 手工而不再被历史 pause 卡死，已确认死亡的 casualty 与 root safety guard 仍保持 fail-closed。

实现补齐了外层稳定事件身份、manual disposition、不可变 capacity fact、最新未解决容量证据权威、首次开旗后当前 generation 仍需人工切换等边界。9-11 事故形状用不含生产凭证的 synthetic replay fixture 覆盖：旧 pause residue 被迁到可见手工态并撤销陈旧超时提醒；历史批次一次冻结最多 10,000 条、每轮最多 1,000 条，1001 条需两轮，单成员连续三次失败后显式封存，且并发新事件仍逐条发 N11。生产共享 maintenance factory 可在 runtime 缺席时执行降级收敛，但本 PR 不激活宿主、不打开自动切号，也不改兑卡 founder authority。

`pnpm lint` 与 `pnpm -r build` 通过；FLY-2676 focused suites、真实 StateStore/HTTP/DirectEventSink/notifier 路径及 retention guards 全绿。本地唯一一次 `pnpm test:packages:run` 在精确头 `88f233fb21ad39200be31449fca3bb1eb075782f` 完整跑完：17 个包中 16 个通过，teamlead 为 1,474 files / 17,527 tests passed，出现 6 个与本改动无关的 5s/15s wall-clock 超时及一个 `onTaskUpdate` RPC timeout；四个失败文件随后按 `VITEST_MAX_FORKS=1` 隔离复跑 13/13 全绿。按 Lead 的共享主机争用裁定不再跑第二次本地全量，最终交付以不再移动的 exact-head GitHub CI 和 effective code review 为权威。

代码审查 R1/R2 的 HIGH 已按原设计收窄修复：安装 material 现在有独立、可迁移的 pending/installed/rolled_back durable verdict，明确回滚后即使下一轮把 incident `failure_code` 覆写为 `observation_unavailable`，也不会重新永久武装 root guard；真正 `installing`/未决安装仍 fail-closed。容量证据只在舰队自动切号旗标开启时参与新 admission guard，原 quota casualty 仍始终 hold。若 rotate 已成功并有 journal/digest，则先提交 generation 的已知事实，再因新的 readiness/manual snapshot 停止自动 recovery 并发 N11，避免留下无法收敛的 `installing`。新增回归覆盖以上边界，受影响的 StateStore/coordinator/runtime/admission replay/route/readiness 七组 81/81 全绿；修复后 `pnpm lint` 与 `pnpm -r build` 再次全绿。

本文件是 PR 的 literal-last commit。后续若 code review 要求改头，必须修复后重新生成最后一笔里程碑提交并重新绑定 review/CI。本实现不 dispatch QA、不请求 ship、不 merge、deploy、restart 或 host activation。
