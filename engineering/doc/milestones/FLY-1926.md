# FLY-1926 — 避免 updater Bridge 收尾复测误报 degraded

**Status**: ⏳ Pending ship
**PR**: #1012
**Date**: 2026-08-31

修复 updater 全量重启收尾的两类契约漂移：Lead 重启统计只保留一行机器可读 stdout，helper 诊断统一隔离到 stderr；Bridge 的有界延迟观测移到 Lead 重启波之前，并与已通过的启动健康检查分离。波前观测暂时不可得不再把成功部署误判为 `degraded` 或发送 `deploy_degraded`，真实 Lead / watcher 失败仍保持 fail-closed。

TDD 覆盖真实 helper stdout 污染、Bridge 探针时序、波前观测不可得、启动状态未知与真实 Lead 失败。`pnpm lint`、`pnpm -r build`、restart-services 148 项及本单涉及的 shell suites 通过；全包聚合运行暴露的固定时限并发超时均已逐文件隔离通过，唯一不可本机执行的边界是缺少 Terminal.app 的两个真实 GUI 用例。Code review `c220f849-c954-42de-aaa2-c6be6bc80e9f` 在实现头 APPROVED；本 milestone exact head 将另开审查。
