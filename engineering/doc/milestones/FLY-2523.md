# FLY-2523 — Self-scheduled Codex home readiness

**Status**: ⏳ Pending exact-head review and CI
**PR**: Pending
**Date**: 2026-09-18

Codex credential-home convergence is now a safe, repeatable operation instead of a manually hunted quiet window. The reconciler derives the approved roster from the two managed Flywheel runner homes and the registered Codex app-server Leads, writes durable current-digest attempt receipts, skips active or unprovable homes without touching their contents, backs up a regular `auth.json` before linking it to canonical truth, clears copy-pending markers, and records already-satisfied homes idempotently. Lead and runner launch paths share admission fences with the migration path, and bounded child-process teardown retains the fence whenever exit cannot be proven.

The operation runs only on existing rhythms: the updater, Bridge health rider, and controlled restart window. No launchd job, plist, crontab, cron entry, interval scheduler, feature-flag writer, or activation wrapper was added. A one-day configurable overdue threshold emits the registered `codex_home_migration_overdue` engineering alert through the normal durable alert queue, including retry and destination-preservation behavior; the integration test uses a real local HTTP receiver and the mutation test proves a disabled overdue condition turns red.

Readiness now exposes two honest scopes. The original global checker remains authoritative for runtime admission and preserves unknown desktop readers. A separate registered-home checker requires the exact five-home roster, current inventory digest and build, durable satisfaction receipts, live symlink topology, no pending markers, collector completeness, and strong resident daemon binding evidence. Its immutable evidence is content-addressed. The read-only CLI prints both registered and global results and always reports `activation.authorized=false`; registered acceptance cannot substitute for global runtime readiness. FLY-2729 deployment/QA evidence is verified only through its immutable artifact, accepted `qa_passed` claim, land mapping, and deployment mapping, and may remain pending without being forged.

The required fixture proofs are green: active homes produce `skipped` with zero target-home writes; idle homes back up, link, clear the marker, and produce `done`; repeat execution produces `already-satisfied` without another backup or home mutation. Missing receipts cross the threshold and really send an alert; registered readiness covers the five-home positive path plus missing/stale receipt, wrong build, pending marker, census failure, registered CommDB orphan, unapproved live home, desktop unknown, resident identity/socket/PID mismatches, and forged or incomplete FLY-2729 chains. Existing link-truth, updater, restart, Lead-launch, global readiness, and runtime suites also pass.

Verification at implementation head `d128c31a39af39aac2b1a18154ce5d133fc07411`: `pnpm lint` exited 0 with the existing warning baseline; `pnpm -r build` passed; all focused TypeScript and shell suites passed; and `pnpm test:packages:run` completed with a passed receipt across all 18 package gates. The largest package, TeamLead, completed 1,484 files with 17,665 passed, 7 skipped, and 0 failed; Claude Runner completed 1,354 passed, 2 skipped, and 0 failed. Static diff inspection found no launchd/plist/crontab/cron path addition or modification and located the new cycle only in the existing updater, restart, and Bridge health entry points.

This PR does not mutate a production home, founder login state, daemon, feature flag, or production incident. It does not claim global readiness or restored automatic switching. Deployment, real-home receipts, current-host registered/global probes, the FLY-2729 dependency proof, desktop credential authority, flag activation, isolated usage-limit recovery proof, independent QA, merge, and release remain separate downstream evidence. This file is the literal last commit before opening the PR.
