# FLY-2523 — Self-scheduled Codex home readiness

**Status**: ⏳ Pending exact-head review and CI
**PR**: #1260
**Date**: 2026-09-18

Codex credential-home convergence is now a safe, repeatable operation instead of a manually hunted quiet window. The reconciler derives the approved roster from the two managed Flywheel runner homes and the registered Codex app-server Leads, writes durable current-digest attempt receipts, skips active or unprovable homes without touching their contents, backs up a regular `auth.json` before linking it to canonical truth, clears copy-pending markers, and records already-satisfied homes idempotently. Lead and runner launch paths share admission fences with the migration path, and bounded child-process teardown retains the fence whenever exit cannot be proven.

The operation runs only on existing rhythms: the updater, Bridge health rider, and controlled restart window. The health rider is fail-closed by default and is enabled only by the canonical production Bridge wrapper. Test-deploy slots still default it off; an explicit `--alerts --codex-home-reconcile` opt-in instead binds the rider to that slot's isolated state root and to the unique Lead/channel/token declared in the slot's mode-0600 projects configuration. The alert path accepts that complete slot tuple only when every mutable path remains inside the slot root and the destination differs from every production general, Lead-alert, and chat channel; a slot tuple aimed at production is rejected. Production mode remains hard-pinned to the production registry and ignores an incomplete leaked slot flag. No launchd job, plist, crontab, cron entry, interval scheduler, feature-flag writer, or activation wrapper was added. A one-day configurable overdue threshold emits the registered `codex_home_migration_overdue` engineering alert through the normal durable alert queue, including retry and destination-preservation behavior; the integration test uses a real local HTTP receiver and the mutation test proves a disabled overdue condition turns red. A generic guard also prevents policy, projects, roster, build, or state corruption from silently suppressing an existing overdue obligation: if no normal alert was produced, it emits a daily deduplicated warning explicitly labeled as an alert-pipeline fault, never passes a mention, and persists a local delivery receipt keyed by UTC day, layer, and reason. Only the actionable missing-receipt-over-N-days path remains severe. The bounded `qa-fly-2523-529-alerts.sh` driver verifies a live QA slot, creates only isolated fixture state, then produces one overdue-severe and one upstream-failure-warning delivery with empty allowed mentions; it returns both Discord message ids for the QA report without exposing the token.

Readiness now exposes two honest scopes. The original global checker remains authoritative for runtime admission and preserves unknown desktop readers. A separate registered-home checker requires the exact five-home roster, current inventory digest and build, durable satisfaction receipts, live symlink topology, no pending markers, collector completeness, and strong resident daemon binding evidence. Its immutable evidence is content-addressed. The read-only CLI prints both registered and global results and always reports `activation.authorized=false`; registered acceptance cannot substitute for global runtime readiness. FLY-2729 deployment/QA evidence is verified only through its immutable artifact, accepted `qa_passed` claim, land mapping, and deployment mapping, and may remain pending without being forged. Host-derived evidence root and deployed SHA bindings cannot be supplied or overridden by the external dependency JSON; an override attempt is rejected as `dependency_binding_override`.

The required fixture proofs are green: active homes produce `skipped` with zero target-home writes; idle homes back up, link, clear the marker, and produce `done`; repeat execution produces `already-satisfied` without another backup or home mutation. Missing receipts cross the threshold and really send an alert; registered readiness covers the five-home positive path plus missing/stale receipt, wrong build, pending marker, census failure, registered CommDB orphan, unapproved live home, desktop unknown, resident identity/socket/PID mismatches, and forged or incomplete FLY-2729 chains. Existing link-truth, updater, restart, Lead-launch, global readiness, and runtime suites also pass. The launch fence now lets a child in the same proven process ancestry reuse its live parent's lease while rejecting a foreign live owner, which preserves the lease across the Lead wrapper's child `ensure-daemon` handoff without weakening cross-process exclusion.

Verification through the latest test-fixture repair `992e39549`: `pnpm lint` exited 0 with the existing warning baseline and `pnpm -r build` passed. The exact FLY-2404 CI block passed locally in workflow order, including all 12 link-truth, reconciliation, launch-fence, alert, readiness, preflight, cutover, and sweep scripts. The FLY-1389 hermetic deployment suite passed 24/24, FLY-1764 passed 13/13, and the TeamLead launcher fixtures passed 35/35 with `CODEX_HOME` deliberately absent from the parent environment; the serial Bridge route fixture also passed. These checks cover the first exact-head CI attempt's relevant failures: a Linux-incompatible test-only `stat`, fake Lead launchers that did not explicitly bind their fixture homes, an incomplete module mock, a fake repository missing the required fence artifact, and an extracted restart-function harness missing the new cadence helper. The next CI attempt exposed one more hermetic harness omission in `codex-lead-args.test.sh`: its copied launcher could not reach the newly required launch fence, so six downstream assertions failed before the fixture runtime. The fixture now supplies a bounded local fence stub; the suite passes 15/15 with parent `CODEX_HOME` absent, and its state-directory parity companion passes 6/6. The unrelated Flywheel Comm failure was again an extra slow-SQL diagnostic under aggregate load (2698 tests passed; one empty-stderr assertion observed the diagnostic) and remains for exact-head CI retry rather than a product-code change. Earlier focused proof also covers eight directly affected TeamLead suites at 40/40, the cycle failure mutations for policy/projects/roster/build/state reads, real alert delivery, overdue mutation, packaged seams at 17/17, process-manager uncertainty, configuration truth inventory, and registered/global readiness. A local `pnpm test:packages:run` reached 11 green package receipts before the Lead deliberately terminated it under the FLY-2467 no-local-full-suite ruling; it is not represented as a product failure or a complete aggregate result.

Rework verification after merging current `origin/main` at `dc05a495a` is also green. `pnpm install --frozen-lockfile` confirmed the workspace lock; `pnpm lint` exited 0 with the repository warning baseline and `pnpm -r build` passed all 24 selected workspace packages. `codex-home-migration-alert.test.sh`, `codex-home-reconcile-cycle.test.sh`, and `codex-home-reconcile-cadence.test.sh` prove slot-to-test delivery, empty mentions, production-channel collision refusal, unchanged production routing, isolated severe/warning execution, and opt-in cadence wiring. `shuttle-alert-route.test.sh` proves the mainline shuttle route survived the merge. The rider and alert-kind contract suites pass 40/40. The config flag-accounting and truth suites pass 54/54. Exact-head run `35375090716` at `af447a689` exposed two PR-specific omissions before final review: the three new slot coordinates lacked reasoned non-flag accounting, and the QA driver's BSD/GNU `stat` fallback rejected its trusted projects file on Linux. Commit `fd8358bce` registers the three configuration coordinates and uses Node `lstat` for portable owner/mode validation; the alert integration now deliberately makes `stat` unavailable and still passes. The bounded slot environment, launch-boundary, QA-room, and generalized deployment suites also passed. One concurrent local run of the broad FLY-1389 deployment suite encountered shared `/tmp` slot and launchd-label contention and is not claimed as a valid receipt; no product change was made for it. Per Lead direction the prohibited local full package suite was not rerun. The final aggregate authority is the required exact-head GitHub CI, whose run id will be recorded in the PR. Static diff inspection found no launchd/plist/crontab/cron path addition or modification and located the new cycle only in the existing updater, restart, and Bridge health entry points.

Code-review round 2 on `5a2e27402` found one blocking packaged-runtime gap: the four shipped reconciliation/fence/receipt entrypoints still imported `../packages/*/dist`, although a prebuilt payload carries those workspaces under `node_modules/<npm-name>/dist` and cannot rely on the later compatibility mirror. Commit `a50176e25` fixes the assembler rather than the source-tree scripts: every curated JS/MJS workspace import is rewritten to the embedded package path, while an unknown or unshipped package fails the build. The hermetic package-onboard test first reproduced the real `ERR_MODULE_NOT_FOUND`, then passed 35/35 after the fix; packaged seams now execute all four real entrypoints before the compatibility mirror and pass 18/18. The version-injection and Codex quota summary packaging suites also pass. The complete affected rework set remains green: migration alert/cycle/cadence/shuttle shell suites, four slot isolation/deployment suites, config 54/54, TeamLead rider/kind contract 40/40, `pnpm lint` with the repository's 25-warning baseline, `pnpm -r build`, shell syntax, and `git diff --check`. The prohibited local full package suite remains intentionally unrun. A fresh exact-head code-review request and GitHub CI run are required after this milestone commit.

This PR does not mutate a production home, founder login state, daemon, feature flag, production channel, or production incident. It does not claim global readiness or restored automatic switching. Implement did not send the live Discord messages: independent QA must run the bounded driver in slot 529, verify the returned severe/warning message ids in `#cos-test`, and attach screenshots to `qa-e2e-529`. Deployment, real-home receipts, current-host registered/global probes, the FLY-2729 dependency proof, desktop credential authority, flag activation, isolated usage-limit recovery proof, independent QA, merge, and release remain separate downstream evidence. This file is the literal last commit in PR #1260.

## 2026-09-18 resumed design handoff

The replacement activation was explicitly confirmed as design-only by Lead answers `fe089c5f` and `d707871b`. It preserved the inherited implementation and PR, recorded the newer G/A/B/C/D/E/K acceptance authority in `design-correction.md`, and refreshed the founder HTML without authoring runtime changes or applying WIP. Before the route clarification arrived, the authorized technical sync merged main `d8b3f3cd5` (#1267) without conflicts in `993e63bcd`. The account-count change does not grant approval to additional credential homes. Supplemental checks passed: 174 account/home tests, 130 Tmux tests, 29 roster/readiness tests, the readiness-receipt shell, and the relevant 13-package dependency build; no local full package suite ran.

Effective design review `6a6559f7-9846-4e46-8d55-1958969fa924` / request `9941da33-2bbe-4998-8c5d-bd880a0fc70b` is APPROVED with 12 non-blocking advisories, all recorded with dispositions in `design-review.md` and reported to the Lead. At the design handoff none was claimed fixed; the later implementation convergence is recorded below. The latest QA criteria remain binding even where an advisory proposes relaxing one; downstream implementation/code review/QA own the final-head proof. The real test destination is the slot's current configured Lead alertChannel, independently proven disjoint from production channels, not a channel name copied from this historical milestone.

The committed 10-section HTML passed static and Node-VM comment-layer checks, not browser visual QA. Its one publish-only attempt returned 502 with null URL/report/message ids and delivered=false. `DESIGN-HTML publish-failed` was reported; Lead `d707871b` explicitly permits design completion and parking despite suspended hosting, with later publish-only follow-up after hosting recovers. No new hosted URL or screenshot exists. No production credential migration, founder login change, service restart, flag activation, QA room, or Discord alert was performed by this design activation. Exact-head CI and full QA remain downstream; prior PASS and old ship cards cannot authorize the new head. This milestone remains the literal last commit before the design handoff.

Lead's final disposition in answer `e3167133` assigns live-lock ownership/reclamation, valid enabled=false monitoring, and explicit exit75 restart recovery to the implement node in this issue. The other nine advisories are now listed individually in PR #1260 Follow-ups. No new issues were opened, no runtime fix was made by design, and D1/D2 plus the new-reason alert criterion were not relaxed.

## 2026-09-18 implementation advisory convergence

Commit `e39093387` closes exactly the three Lead-assigned advisories without changing the other nine follow-up dispositions. Reconciliation locks now always publish PID/start-time holder evidence and therefore cannot be reclaimed merely because their directory age exceeds the stale threshold while the holder remains live; the new concurrent regression test ages a live lock and proves that the contender receives `lock_busy`, only one reconciliation runs, and the original holder releases cleanly. A policy with `enabled=false` is now a valid monitoring state: it skips home mutation while still evaluating and delivering an overdue severe alert, so disabling reconciliation cannot hide the obligation. A restart-window reconciliation exit 75 now resumes admission best-effort, emits the dedicated severe `deploy-codex-home-reconcile-exit-unproven`, records terminal reporting, and returns failure instead of relying on the generic cleanup path.

The complete affected verification is green. `pnpm lint` exited 0 with the existing 25-warning baseline; `pnpm -r build` built 24 of 25 workspace projects. The focused shell suites passed: link-truth 16/16, credential sweep 8/8, reconciliation, launch fence, process detection, cycle, cadence, migration alert, overdue mutation, readiness receipt/check, package onboarding 36/36, packaged seams 18/18, restart admission 52/52, and restart/deploy consistency 16/16. The directly affected TeamLead Vitest selection passed 7 files and 65 tests; the claude-runner reconciliation selection passed 11/11. Per the Lead's FLY-2467 ruling, the prohibited local full package suite was not run.

The prior remote head's Unit shard exposed one test-harness timeout rather than a product assertion failure: `lead-capability-read.test.ts` dynamically imported the full Bridge plugin inside a 15-second test. The larger FLY-2523 module graph made cold transform/collection cross that timer, after which the timed-out fixture leaked into later cases. Commit `390684f08` follows the repository's established static-import pattern, leaving the timeout unchanged and moving only module collection outside the individual behavior timer. The once-failing case now completes in 1.6 seconds, the entire file passes 20/20, `flywheel-teamlead` builds, and Biome reports no issues. A fresh exact-head review and GitHub CI run remain required after this milestone commit; no old-head approval, PASS, or ship card is carried forward.

No production credential home, founder login, service, feature flag, production channel, or production readiness state was touched during this convergence. Independent QA, current-host evidence, activation prerequisites, flag authorization, real usage-limit recovery proof, merge, and deployment remain downstream and explicitly out of this implementation phase.
