# FLY-2542 — DirectEventSink 超时测试的 SDK mock 隔离
Issue: FLY-2542 (https://linear.app/geoforge3d/issue/FLY-2542/flake-directeventsinktestts1605-在-ci-teamlead-shard-3-间歇红9-14)
日期: 2026-09-14
基于: ../FLY-2542-direct-event-flake/verification.md

**Status**: ⏳ Pending ship
**PR**: 本分支 `flywheel-FLY-2542` 的实现 PR，随本提交推送后创建
**Date**: 2026-09-14

前一条 timeout 测试在动态 SDK 导入完成前推进假时钟，导致下一条 tail-throw 测试重叠使用 Vitest 的 importer callstack，绕过 SDK mock。修复仅让 timeout 夹具等待 SDK issue 请求进入挂起状态，再推进时钟。生产逻辑与全部原断言不变，无 skip、依赖或配置改动。

受控延迟导入先复现原零调用错误并拦截真实 SDK 加载；相同条件下 Promise 信号修复通过。文档复现脚本再次确认 red=1 / green=0。修后整文件连续 20 次全部通过，每次 65 项，共 1300 项；lint、全仓 build 退出 0。全包验证在本快照冻结时仍运行；精确头代码评审与两次 CI 相关分片回执由随后创建的 PR 和结构化报告承载，不在评审后追加文档提交。

`onTaskUpdate` 为另一条 60 秒 worker RPC 超时路径：同一 CI run 的 attempt 1 全部断言通过后报 RPC 错误，attempt 2 才出现本次 SDK mock 失效。负载关联未被原始日志证明，本提交不声称修复 RPC 超时。
