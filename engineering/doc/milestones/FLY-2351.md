# FLY-2351 — 快照保留、副本回收与 Data 卷判据

**Status**: ⏳ Final rework verified; exact-head review and production acceptance pending
**PR**: [#1133](https://github.com/xrliAnnie/flywheel/pull/1133)
**Date**: 2026-09-08

## 交付

- 修复快照统一遵循 `FLYWHEEL_STATE_DIR`，写入前以 Data 卷可用空间对预计文件大小执行
  `5×` 准入；保留策略取“24 小时内”和“每 issue、数据库类型、project 最新一份”的并集，
  自动维护每轮同时留下 dry-run 与 apply 明细，无法可靠映射的历史文件不删除。
- QA、实现与人工分析使用 `/tmp/flywheel-snapshots/<exec>/` 受管目录；获取时执行
  2GB 硬预算，可能增长的 rehearsal apply/VACUUM 也在同一锁下预留预算。正常 closeout、
  终态重放和周期维护按可验证 owner 身份回收，孤儿或杂项条目单独告警而不再阻断其余目录。
- STEP 5 和 `/api/capacity` 共享 `/System/Volumes/Data` 的原始可用字节事实；低于
  20GB 记 FINDING。活跃运维规则不再用 `df -h /` 误读 macOS 密封系统卷。
- 三个既有大库分析入口已改用受管快照及 finally 回收；运维 runbook 固化首次 legacy
  inventory、低盘止损顺序、回滚边界和不可冒充生产验收的证据要求。

## 验证与审查

- 每个行为块均先取得预期 RED，再落最小实现并复绿。R1 在
  `b71ff09e2a7272bbbe04baa664ab2bc00f783a21` 提出 1 HIGH、3 MEDIUM、3 LOW；HIGH 与
  三个 MEDIUM 已修复，三个 LOW 按 Lead 指令仅登记在 `implementation-review.md`，未扩张范围。
- PR 首个候选头 `c34f35477476cda8ee832759e61c4c67f8fc266d` 的 CI 在
  `Script Tests 3/5` 失败；QA 同时退回四个阻断项。此次 rework 逐项完成：缺失 snapshot helper
  时仍保留 Raya STEP 5 的结构性不可用（B1）；巡检修复命令从 PATH 中的受管
  `flywheel-patrol-snapshot` realpath 到可信 checkout 的 scripts 目录，再调用同级 helper，
  不依赖受 capability fence 限制的 `FLYWHEEL_DIR`（B2）；单个过期快照删除失败记录
  `delete_failed` 后继续清理其他文件及终态受管目录（B3）；
  runbook 固化低盘盘点、批准回收、重测 `5×`、仍不足即停写上报的顺序，巡检规则直接引用
  该锚点（B4）。未触碰 legacy migration。
- B1–B4 均遵循 RED → 最小修复 → GREEN；受影响单测通过：snapshot storage 23/23、snapshot
  closeout 10/10、patrol rule 33/33、patrol shell 377/377。精确头 `0cec2277` 的后续审查发现
  B2 使用了只有 alert-duty seat 才有的 `FLYWHEEL_DIR`；最后一轮先以规则测试取得单项 RED，
  再用受管 executable 的 `command -v` + symlink realpath 最小修复，并复绿 patrol rule 33/33
  与 capability-fence shell 5/5。
- rework 已重放到当时最新的 `origin/main`。最终候选树通过 Node 22 串行门禁：
  `pnpm lint`（退出 0，仅既有告警）、
  `pnpm -r build`、`pnpm test:packages:run --pool=forks --poolOptions.forks.maxForks=1`
  （TeamLead 891 files / 12082 passed / 6 skipped；其余工作区全部通过），以及改动过的
  `scripts/__tests__/lead-patrol-snapshot.test.sh` 377/377。最后一轮 B2 修复后再次完整执行同一组
  门禁并全部通过；TeamLead 仍为 891 files / 12082 passed / 6 skipped。
- 第一次重放后全套仅有未改动的 `chat-thread-routes.test.ts` 一项触发 5 秒超时；相同单 fork
  隔离复跑 60/60、1.42 秒通过，随后完整全套复跑退出 0（该文件在全套上下文中 60/60、
  1.11 秒）。本里程碑是字面最后提交；候选推送后冻结，并仅发起一轮新的 request-driven
  code review。

## 边界

本实现未读取、复制、清理或修改生产数据库和历史 `/tmp` 遗留；未 dispatch QA、请求 ship、
merge、deploy 或重启 Bridge/Lead。修复头仍需 QA 做 529 N-to-N，因为
`session_completed` / `session_failed` relay 已改变。首次生产 inventory、自动删除结果、真实
节点终态回收与低盘告警仍须由后续 QA/独立部署流程验证，代码合入本身不等于事故修复已在
生产生效。
