# FLY-2351 — 快照保留、副本回收与 Data 卷判据

**Status**: Implementation rework prepared; local full gate NON-GREEN; fresh CI/review and QA pending
**PR**: [#1133](https://github.com/xrliAnnie/flywheel/pull/1133)
**Date**: 2026-09-09

## 交付

- 修复快照写入前检查 Data 卷可用字节至少为预计文件的五倍。自动保留最近 24 小时及每 issue、库类型、project 最新一份的并集；apply 前记录 dry-run，未映射历史文件不删。
- QA、实现和人工分析副本进入执行专属受管目录，默认上限 2,000,000,000 字节。按 owner、终态凭据和强死亡证据回收，失败由周期维护重试。
- STEP 5 和 `/api/capacity` 使用 `/System/Volumes/Data`，低于 20GB 记 FINDING；运维规则与 runbook 纠正 macOS `df -h /` 的错误判据。
- cycle-time 和 rehearsal 顺序获取、使用、释放每库副本。rehearsal 的另一库只读用于跨库保护；行级快照、还原验证副本也在受管目录内回收。单库容量不足留下 finding 后继续另一库，partial 不能冒充成功。

## QA 返工与验证

- 前一候选头 `afc4eaee641587e5a16a17858c46e42b2d09d06e` 的 CI 14/14 通过，但 QA claim 967 发现两项阻断；旧 CI 不作为本返工验证。
- B1 删除 rehearsal 重复建 evidence 目录，用合成库实际执行 inventory、apply、两库 VACUUM 替换源文本正则断言。旧代码及恢复重复 mkdir 的变异均在 EEXIST 处 RED，修复后 GREEN。
- B2 按 Lead 指令 `1e9c9469-5c83-4717-8531-f700d587e3a9` 及答复 `75253571-36f7-4294-8c8a-89c41f3b5b79` 实施。真实 snapshot/budget/cleanup 函数配隔离 fixture 根，验证顺序回收、2GB 稀疏合成源拒绝后继续另一库、跨库活跃记录保护和禁止对 guard context 执行 VACUUM。保留现有摘要消费者所需的顶层 `vacuumDurationsMs`。
- `pnpm lint` 通过（14 项既有告警）；`pnpm -r build` 通过；脚本单测 55/55、巡检 shell 377/377 通过。
- 唯一一次完整重试退出 1：Claude Runner 本次无该错误，所有断言通过；TeamLead 891 文件 / 12087 passed / 6 skipped，但包末出现同样的 onTaskUpdate RPC 超时。**本地整包门非绿**，未第三次重跑。Lead 已通过 `f3e19388-bb79-4e99-b5ab-6633b92f1f44` 确认该 TeamLead RPC 伪影适用例外：不再重跑，PR 明写整包非绿，以新 exact-head CI 14/14 和正式审查通过为交接依据。
- 首轮完整 package 门禁因 Claude Runner 的 Vitest `onTaskUpdate` RPC 超时退出 1（45 文件、1105 测试通过、2 skipped，但有 1 unhandled error），下游工作区未执行；不将其计为绿色全门禁，也未修改代码或超时掩盖失败。
- 本文件是推送候选的字面最后提交。之后按授权只推送一次并请求一轮新的 request-driven code review；新 HIGH 停止并上报，MEDIUM/LOW-only 按 Lead 规则接受并记录。

## 验证边界

本轮只使用合成数据库，没有读取、复制、清理生产库或历史遗留。fixture 的磁盘余量和进程出生身份探针通过测试依赖注入，不能等同真实主机或生产验收。

未修改 pinned plan，未 dispatch QA、请求 ship、merge、deploy 或重启 Bridge/Lead。首次生产 legacy inventory/映射、真实终态回收、低盘告警及 QA 的 529 N-to-N 仍由下游流程验收。预算预留可能在初始获取成功后拒绝某库的 rehearsal；摘要会明确标记该库 finding，2GB 默认值未放宽。
