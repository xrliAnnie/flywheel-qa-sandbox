# FLY-2671 — 确定性选号与选卡阶梯

**Status**: ⏳ Pending ship
**PR**: #1240
**Date**: 2026-09-17

新增无时钟、无 I/O、无宿主副作用的三号重置卡纯选择器：全线周额度停摆后，按卡是否会先于本号周重置过期、过期时刻、周重置最晚、剩卡最多和固定 profile 顺序唯一选出账号，再按最早过期与 card id 唯一选卡；缺失、截断、矛盾或未知输入均显式 fail closed。现有切号器仅补离线重放特征测试，继续保证有额度账号中选择周重置最早者。本单未接入 consume、自动切号、自动重启、部署或宿主激活。

验证：聚焦测试 36/36 通过，`pnpm lint` 退出 0，`pnpm -r build` 通过。`pnpm test:packages:run` 的其余包全部通过；teamlead 为 17,543 通过、7 跳过，并出现一个与本改动无关的 5 秒 `offline-inventory` 超时及一个 `onTaskUpdate` RPC 超时。该超时用例随后以 `VITEST_MAX_FORKS=1` 隔离复验 1/1 通过（总耗时 2.67 秒）。最终以 PR exact-head CI 与代码评审为交付门禁。
