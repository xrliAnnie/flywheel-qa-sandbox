# FLY-2603 — Codex summary 身份透传与吸收回执

**Status**: ⏳ Pending ship
**PR**: #1216
**Date**: 2026-09-15

补齐标准 Codex Lead 的 app-server/TUI daemon 与 lead-actions summary 身份投影；由既有 canonical batch ACK 同步回写 summary_absorption_round 的 lead_events.acked_at，过期清理保留 protocol receipt 直至 ingress 完成。保留秘密过滤、收件人校验和幂等重放。R2 的 167 项定向测试、64 项 shell 检查与 build/lint 通过；R3 补齐第二组旧测试期待，52 项 capabilities 测试通过，生产代码未变；新头审查、CI 与交接另以机器回执为准。roundtable registry 已由 Lead 单独修复，待重启验证，不宣称生产恢复。

R4 按 Lead 授权隔离 FLY-2592 性能门：只在 unit shard helper 排除该文件，由既有 dedicated job 单独运行，阈值和测试体不变。真实 Vitest discovery 与命令回归 2 项通过；新头 review/CI 另验。
