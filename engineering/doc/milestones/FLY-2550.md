# FLY-2550 — 常驻 Lead 空闲线程轮换

**Status**: ⏳ Pending ship
**PR**: https://github.com/xrliAnnie/flywheel/pull/1203
**Date**: 2026-09-15

TUI Lead 在七天到期且有真实对话、安静三十分钟、输入 fence 验证通过后，
切到全新线程，使旧线程有机会进入 Codex 原生记忆候选。采用原子账本、
耐久 attempt 标记、六小时退避与跨代 readiness；不携带旧对话内容。
项目级 SQLite 开关 `codex_lead_thread_rotation` 默认开启，设为 `0` 后
在下一 generation 停用轮换；读取失败也关闭该 generation 的轮换。
已不存在的 pane 满足关闭条件；fence 内异常记录失败退避并恢复输入，
若已落盘 pending 无法清除则保留保护性 fence。

C0 两次延迟 590.55 / 865.26 ms；初始 369 个定向测试通过。
本次纠偏 228 个 Teamlead、69 个 config、5 个 inventory 测试通过，
恢复输入补充断言单独通过；全仓构建和 lint 通过。
R1 对 c6c376f02 有效 APPROVED；该 HEAD 的 CI 两项断言失败已在纠偏中修复。
代码 HEAD cd769c4f7 已获 R2 APPROVED，CI 34921055559 全绿。
后续仅文档差异按 Lead 裁定免新评审，最终 HEAD 仍待 exact-head CI；
独立 QA/生产一周验收尚未完成。
三条 LOW 按 Lead 裁定仅记录后续。未部署或合并。
详见 `engineering/doc/FLY-2550-lead-thread-rotation/implementation-verification.md`
与 `design-correction.md`。

Founder HTML/template 与 d3 图的回滚说明已更新为 SQLite 开关，并以
publish-only 发布：[修正页面](https://fw-reports-624a39.vercel.app/r/6413589e77927e316f52a251aee8898a/)。
线上 HTTP 200、无旧开关、script nonce 与 CSP 一致；没有发送 Discord 消息。
R2 剩余两条代码建议按 Lead 78dacdf5 裁定只记后续。
