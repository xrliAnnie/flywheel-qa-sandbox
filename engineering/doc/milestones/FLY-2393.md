# FLY-2393 — Bridge 每项目 beta 分频

**Status**: ⏳ Pending review and QA
**PR**: Pending creation from flywheel-FLY-2393
**Date**: 2026-09-11

每项目持久化 beta 游标、独立到期/退避/重启对账与受限发布回执。显式接管后可配置6h/24h；未接管保留6h，缺真实workflow的Geo保持unconfigured。客户每周release、判据和auto-ship不新增B6 gate；三个客户workflow各只增加queue:max。

R1指定凭据修复：operator-owned允许集限制token_env读取与HTTP使用，旧绑定同样受限；S4e补回beta单文件secret allowlist与变异测试。R2唯一HIGH（CI漏枚举两个Node套件）已红绿修复，完整结果code-review-r2.json。同步main bece7de15仅解决StateStore双方import冲突。最终头还需R3与exact-head CI通过才交QA。

本地lint/full build通过；凭据19、beta/store/管理台114、合并后核心37、Node9、workflow23、publisher44通过。packages aggregate未全绿：未改动visual-capture全局锁超时（最终34失败/2232通过）；此前no-bail runner3失败/1253通过、teamlead2失败/13099通过并RPC超时，失败文件各隔离一次174/17通过。完整边界和日志指针见implementation.md，不以focused或隔离绿替代aggregate。

visual acceptance deferred to QA (sandbox Chromium unavailable)，依Lead 6a05af77裁定。fixtures/beta-console.html含flywheel ready与geoforge3d unconfigured；DOM/XSS不代替浏览器。无生产库复制/变更、真GitHub dispatch、客户发布、merge或deploy；真实双项目上线、权限和外部队列证据仍属授权激活前置。

本文件是开PR前literal last commit。之后保持HEAD冻结，完成R3/CI并用needs_review交接，再park；不自行ship。
