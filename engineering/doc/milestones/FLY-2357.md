# FLY-2357 — 常驻 Codex Lead 记忆 pin

**Status**: ⏳ Pending ship
**PR**: [#1095](https://github.com/xrliAnnie/flywheel/pull/1095)
**Date**: 2026-09-05

`codex-lead-tui-home.sh` 现在把 `[features].memories = true` 与
`[memories].dedicated_tools = true` 作为两个独立、TOML 实值校验、漂移即 fail-close
的 launcher pin 管理；full-access 重写在同 home staging 中校验，只有 memory、sandbox、
projects 与 lead-actions §10 gate 都成立才原子替换。已有平面 memory/feature 表的键值原样保留，
四个 rollout 节流项与 `disable_on_external_context` 均未修改。

经 Lead 裁定，真实 launcher-managed 三目标是 Raya、Infra Bot、Mufasa；Honey Lemon 是
FLY-1911 voice-avatar home，不在本单目标中。`~/.codex` 与 `~/.codex-honeylemon` 的 SHA-256、
mtime、size 均保持基线不变。Raya 尚无生产 home/LaunchAgent，会在首次受控 activation 时继承 pin；
Infra Bot 与 Mufasa 已部署，但本单没有停止、启动或重启任何生产 Lead。

非生产先行证据包括真实 Codex 0.153.x parser 四形状 probe，以及隔离 full-access launcher →
runtime gate → hermetic daemon 生命周期演练；后者得到 `home OK`、`runtime_gate=pass` 与健康 socket，
无 `Fix ... manually`、launcher die 或 gate failure。聚焦 shell/runtime suite 为 61/61；lint、build、
现代 Bash 语法与 ShellCheck 通过。全仓 package gate 在本机高并发下为 11,232 pass / 6 skip / 4 个
未改动 teamlead 失败；三个 timeout/hook case 单 fork 隔离复跑转绿，唯一稳定项是 tmux 3.7c 拒绝
FLY-2118 fixture 的 TAB window name。Lead 将其定性为 baseline/环境差异并要求整包交给 PR CI，
不得改该测试。cross-family code review R1 对代码 head `c18bca495` 返回 APPROVED，无 blocking finding；
一项 MEDIUM 与五项 LOW advisory 已回传 Lead。

生效仍需要一次受控 daemon 配置重读：merge/deploy 后仅走 R4 00:00/12:00 班车或 founder 重启票，
先备份，一次只处理一个目标并确认正常启动后再继续。遇到 `Fix $CONFIG manually`、die 或 gate failure
立即停队列；真正回滚需先 revert/redeploy launcher，再恢复 pre-image 并重启，单独恢复配置会被 launcher
重新补 pin。一周后的 `find ~ -maxdepth 2 -type d -name memories -path '*/.codex*'` 读数归
FLY-2355·D；本单不把“配置已写”冒充“生产功能已开”。
