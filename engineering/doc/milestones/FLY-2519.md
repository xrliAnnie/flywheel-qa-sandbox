# FLY-2519 — Codex 部门 Lead 能力对等

**Status**: Attempt 9 review corrections and current-main integration implemented; fresh effective review, exact-head CI and QA re-verification pending.
**PR**: #1191 (non-draft). Final gate receipts belong in the PR and structured Lead reports.
**Date**: 2026-09-16

Adds the versioned trusted-parent capability runtime, typed Bridge/provider operations, pinned rules/native skills, default headless/TUI assembly and native Chrome DevTools MCP. Model-facing tools return results; credentials stay in the parent; Bridge remains the StateStore writer. Founder-only/R1–R5 actions remain reserved. See the plan, gap-checklist, implementation-verification and qa-rework documents for full scope and explicit unavailable capabilities.

## QA 1153 rework

- Browser Node startup uses the Lead-approved read-denial design (ruling e3d41f41): runtime file/sysctl reads are allowed while fixed credential roots, resolved aliases, project files and the synthetic probe directory are denied. Writes, execution and forced network egress retain their boundaries. Direct/symlink credential denial assertions remain mandatory. Unregistered credential paths remain a documented limitation of the changed read model.
- Chrome deep signature validation has a 60-second budget and runs once per BrowserWorker launch.
- The real-exec Node regression boots the final Seatbelt policy and checks direct/symlink synthetic credential refusal. Non-macOS/nested environments explicitly skip and supply no host acceptance evidence. QA must obtain exit 0 from the authoritative browser canary on the macOS host.
- The isolated 529 script follows the fixture-only ruling 7eddea27: loopback Bridge outbound route, two synthetic clients, real SQLite outboxes/dedup, fixture Discord readback, replay/scoping and cleanup. Its README explicitly excludes real Discord, model-driven Leads and full v2 authorization/issue-driving acceptance.

## Code-review HIGH corrections

- GitHub PR pagination accepts GitHub's numeric repository Link path while retaining pinned HTTPS origin, next-page validation and fixed owner/repo dispatch. Regression: expected red, then 45/45 passed.
- Terminal status recognizes contiguous permission menus including key-hint footers, boxed titles/descriptions and wrapped options, while retaining stale/intervening-output, session-identity and input guards. Round 3 regression: four expected failures, then 19/19 passed.
- Generic Discord writes use existing Bridge-owned SQLite operation receipts before dispatch (Lead ruling 0bdb68d2). Completed unknown writes can leave the bounded memory map; request UUID replay uses durable receipts and existing read-only reconciliation, never dispatch. Missing receipt storage fails closed. No new schema or StateStore reader. Round 3 also restores optional classification access on automatic-output error paths and supplies the production receipt store to the sibling fixture. The prior sibling suite reproduced 12 failures/25 passes; both corrected Discord suites passed 44/44, including capacity, payload-conflict, missing-store and router-recreation cases.

## Verification

Browser rework's focused source suites passed 20/20 before the subsequent GitHub/terminal/Discord changes. Latest full lint exits 0 with 18 existing warnings; latest full monorepo build exits 0. Rebuilt script checks: one fixture test passed (two fresh drill executions), one host Node test explicitly skipped for nested Seatbelt. Both scripts are registered in CI. Actionlint syntax passed; full ShellCheck integration reported 8 diagnostics in unchanged existing steps.

Previous Discord local route/capacity runs timed out under load. Fresh round 3 focused verification passed 44/44; earlier failures remain recorded in qa-rework.md. Candidate c2de88abb received effective CHANGES_REQUESTED and failed CI run 34914462447. Its complete review receipt is review-code-r3.json; only the two HIGH findings were repaired. Latest full lint/build each exited 0. Rebuilt root script tests passed one fixture test (two isolated runs), with one explicit nested-sandbox skip and no failures. Fresh effective review and CI must certify the new final head.

Local aggregate: **interrupted — not relied on**, stopped under Lead host-capacity instruction 40927957-eedd-4967-a05d-58fb162e19f0. It will not be restarted; exact-head CI is the handoff authority. Fresh effective code review and final-head CI are still required. Prior-head CI and review do not certify this rework. Host Chrome and Honey Lemon's full issue/browser/Linear/Discord drive remain QA acceptance work.

This is the literal milestone-only final commit for review/CI handoff. No production v2 activation, Lead restart, host test-slot, real Discord write, QA dispatch, merge or deployment occurred. No Claude-in-Chrome.


## QA 1158 browser startup correction

Previous milestone `c695ab53d` passed effective review and CI run `34917491131` (15/15), then failed real host browser acceptance. Lead authorized this separate QA rework in instruction `7746e808-8296-42b7-86d4-9ea980788266`.

Correction `b5282557b` admits only self process-info for macOS os_log initialization, selects Chrome arm64 through a fixed system arch alias and washed architecture preference, and adds the five requested shell/package/Git/Docker/Discord credential read denials. Other process targets remain denied; no shell or `/Library/Apple` executable mapping was added. Deep codesign verification remains 60 seconds.

The registered real-exec host test now requires production MCP initialize, pinned tools/list validation and actual Chrome list_pages under the final Seatbelt policy, plus the existing direct/symlink credential refusal assertions. QA must run both this regression and the authoritative browser canary on an unsandboxed macOS host. No host pass is claimed here.

Verification: policy tests produced the expected 3 failures before the fix; all 13 browser suites subsequently passed 97/97. Full lint and build passed (18 existing lint warnings). Root scripts: 1 fixture passed, 1 explicit nested-sandbox host skip, zero failures. No full package aggregate restart. The prior isolated real Discord QA evidence is retained without additional Discord work. Fresh effective review and exact-head CI must certify this final milestone head before needs_review1191.


## QA 1161 argv launcher correction

QA proved the self process-info and all isolation/credential-denial fixes at e58533887, but headed Chrome still failed. Under Lead instruction a5a27b26 and reconciliation gate3cd8995a, correction f73470d7c replaces ARCHPREFERENCE with a fixed mode0500 sh exec wrapper, records its SHA256 in the spec, and admits only the explicit wrapper/sh/arch paths alongside the existing pinned executables. Exclusive creation and O_NOFOLLOW validation reject replaced files/links. The exact Chrome path and original args are shell-quoted/forwarded; exec preserves CDP descriptors without a new live process. The obsolete delimiter restriction is removed. No WindowServer, network, write, credential or codesign-budget widening.

Fresh local verification: browser99/99; full lint/build passed with18 existing warnings; root scripts1 fixture pass1 nested-sandbox host skip. Actual launcher probes cover dotted/quoted paths, literal arguments, fd3 and pinned Chrome --version. The host regression continues to require real final-policy MCP and headed Chrome list_pages; no host pass is claimed. The differing local/QA ARCHPREFERENCE observations are explicitly recorded in implementation-verification.md per Lead, with no further investigation or broader fix.

One push and one fresh exact-head code review/CI follow this literal milestone-only final commit. needs_review1191 remains gated on effective APPROVED and exact-head CI success. QA owns full host canary/GUI acceptance; no QA dispatch, production activation, restart, merge or deployment occurred.


## Direct-bash host correction

Although5fc5f4c7a received effective APPROVED, reviewer host evidence showed /bin/sh re-executing unadmitted /bin/bash. Lead6081ee75 explicitly ruled this a functionality blocker and authorized correction5a4fc2937 before QA: use #!/bin/bash directly, admit only literal /bin/bash, remove /bin/sh, preserve0500/digest/final-target guards, and leave the other13 advisories unchanged.

The registered host regression now exercises the actual generated wrapper --version under the final Seatbelt policy before continuing MCP initialize/tools-list/Chrome list_pages. New interpreter assertions first failed2/9; corrected browser suites99/99 pass, full lint/build exit0, root fixture1 pass and host1 nested skip. Actual final-policy/GUI acceptance remains QA-owned. No WindowServer or broader policy change, production action, merge or QA dispatch. This milestone-only commit is the final head for the newly authorized single push and fresh effective review/CI.

## QA attempt 4: native executable mapping

Lead instruction cff51596-187a-4787-99a5-e705d4cf295d binds the host bisect: the blanket executable-map deny forced Rosetta selection and abort. Correction bb133682c removes only file-map-executable from that deny, retaining default-deny and scoped mapping grants. The arm64 bash launcher, read/write/network boundaries and Rosetta exclusion are unchanged.

Host canary and real-exec regression now require live, profile-bound headed Chrome with P_TRANSLATED clear on an arm64 host after successful MCP list_pages. No browser process-info grant was added. The observation parser has positive and negative coverage and is registered in CI. Launcher replacement remains a follow-up advisory.

Fresh verification: policy red1/green12; browser100/100; lint and full build passed; root2 passed/1 nested-sandbox skip. Full package gate is running; no aggregate green claim. This is the final milestone-only head for fresh effective code review and exact-head CI before needs_review1191. QA must still prove the host canary exits0, including native GUI startup; no production activation, QA dispatch, restart, merge or deployment.

## Main #1198 integration: schema2 patrol parity

PR CI's merge result exposed five patrol contract incompatibilities with main f022a0a7e. Merge cea457c95 reproduced all five locally; this is not a flake. Lead78ea9ac2 approved integrating the canonical schema2 mechanism review into Codex parity. Source8a9b15966 requires the schema2 report header and supports explicit finding identity/category, mechanism review, immutable declarations and complete disposition records. Missing review never defaults to none. Completion includes #1198's canonical report validator; its source/semantics remain unchanged.

Validation: snapshot/route/registration23/23; mechanism4/4 including both no_issue paths and rejection guards; full lint/build passed; root2 pass1 explicit nested-sandbox host skip. Broader capability run had546 pass1 missing-test-import failure, then the affected HTTP file passed20/20 after the import fix. Fresh package gate remains running. Raw pre-sync CI and post-sync five-red evidence remain preserved. No CI retry allowance consumed.

This milestone-only final commit requires new effective code review and CI before needs_review1191. The former b9669e468 review cannot certify this head. Native/headed host canary remains QA-owned. No production activation, restart, QA dispatch, merge to main or deployment.

## Effective review HIGH corrections

Source d19dedb3c rejects v2 terminal CR/LF before observation/authorization/send and at the typed input schema, closing multi-submission reserved-command bypass. Terminal24/24 and catalog8/8 include CR-/exit and CR-/quit zero-send negatives and single-line positive.

Discord's fixture-only300ms local HTTP latency reproduced routing30s timeout plus next-case403 (2 failed/5 passed). Only the hardcoded harness scenario budget changes30s→120s:130 sequential probes and finally cleanup must finish before the next case; production budget15s, request counts and assertions are unchanged. Identical latency now7/7, routing55.235s. Lead db896d95 accepts this correction. Script and raw red/green logs are committed in the task doc folder (690f2a306).

Full lint/build passed. Old7a97dc86c CI succeeded but effective review was CHANGES_REQUESTED; this final milestone head needs new review/CI. Package gate remains running; host native/headed canary remains QA-owned. No production action, QA dispatch or merge to main.

## Reachable legacy terminal path

Lead5b8825aa/7e4b830d confirmed the remaining legacy input path is production-reachable through terminal-mcp runner_terminal_input, registered by claude-lead.sh and shipped by package-onboard. Shared-core CR/LF and /exit,/quit rejection now applies to legacy too, before observation/send; MCP input schema rejects multiline. Unrelated legacy observation compatibility remains.

Before/after: registered handler1 failed/2 passed → complete terminal-mcp51/51; core legacy5 failed/21 passed → core+observation29/29. Negative cases invoke the actual registered handler and assert zero send-keys; legacy single-line positive remains valid. Lint/build passed. Other16 advisories unchanged. New exact-head review/CI required; earlier9f9c APPROVED is historical. Package gate remains active. No production activation, Lead restart, QA dispatch or merge to main.

## Terminal MCP gate registration

Added the missing test:run package script so CI light and package-gate execute the terminal MCP suite, including the registered-handler security regression. The actual script passed51/51 across7 files. This milestone-only head needs fresh effective review and CI; the prior active aggregate started before this registration. Runtime scope and QA-owned host acceptance remain unchanged.

## Main rotation integration

Merged main af74f03f9 (#1203/#1201), preserving both router test groups, delivery-context binding, main atomic thread-id persistence and v1 rotation. Per Leada6e8a11a, v2 explicitly disables rotation with visible FLY-2576 reason and never opens StateStore; typed Bridge flag integration is outside scope. This remaining parity gap is tracked as High FLY-2576. Existing partial-start cleanup now also clears the rotation timer; kill inventory regenerated.

TDD v2 StateStore guard3 red to green; affected runtime/router/parity299/299, rotation+journal62/62 after dependency rebuild, inventory5/5; lint/build/CI structure passed. Complete premerge package receipt is committed: aggregate exit1, parity-drill15s timeout; unchanged single-fork1/1 at3.156s; aaf54 exact-head CI success. Lead52d8e93d explicitly directs retaining the RED aggregate, no rerun and no timeout adjustment. This merged milestone-only final head requires new effective review/CI. Host browser proof remains QA-owned; no production operations or main merge.

## Attempt 5: approved outer Seatbelt Chrome configuration

QA1176 proved0f5afbddd still failed real headed Chrome startup despite native --version success. Leaded48f532/5058a1de explicitly accepts loss of Chrome renderer sandboxing inside the retained outer Seatbelt. Sourceff4474648 adds no-sandbox, crash reporter disable flags, and fixed disposable crash-dump paths (including Chrome153's breakpad-dump-location switch). Accepted risk is documented in design-correction.md and beside the flag. No Mach or host-profile metadata exception has been added; all credential/symlink/write/egress assertions remain unchanged. Original QA verdict and three host diagnosis scripts are committed.

TDD configuration2 red then15/15; complete browser100/100; lint/full build pass18 warnings; root1 pass1 explicit nested-sandbox skip. Prior aggregate RED is preserved under Lead52d8e93d, no rerun or timeout adjustment. Host diagnostic gatebe1736d0 remains pending: this candidate does not claim successful headed launch or full parity. Code review and CI may proceed independently; no further push while review runs. Conditional permissions require actual host evidence; maximum two bounded rounds, then report without further policy broadening.

## Attempt 5: deny-home remediation and final bounded Crashpad trial

The first trial b485257bf passed CI34939052499 but failed host Chrome launch after59562ms and effective review6b0ae131 HIGH browser-worker-broad-file-read-allow. The Lead explicitly rejected accepting broader host-secret exposure (gate4bc8df26). Source c23ef9986/b464cc197 denies home data and metadata, including resolved aliases, with only verified MCP subtree, Chrome bundle, exact Node and disposable qaRoot exceptions. Existing credential/project denials remain; no surrounding worktree or Library cache grant. Per host evidence be1736d0/3d3a3c43, only Crashpad handshake-name Mach access and exact Crashpad directory metadata are added. Renderer sandbox loss remains accepted, broader home-secret access does not.

Regression now requires non-enumerated synthetic home files, shell profiles, repository .env, metadata, symlink and Library enumeration to be denied; Crashpad metadata alone is allowed while contents remain denied. Final browser102/102, full build and final package build pass, lint passes18 warnings. Earlier upstream5000ms timeout remains recorded; unchanged affected tests passed15/15 without concurrent build. Root real-process tests explicitly skip twice inside nested Seatbelt and do not establish host enforcement. Source/dist identity b464cc197; host gate1620cb21 requests both real tests and authoritative canary. One security remediation round and final Chrome trial: failures require exact evidence and Lead decision, no speculative broadening.

This milestone-only candidate requires fresh effective review and exact-head CI; neither earlier CI nor source tests clear the HIGH or host launch requirement. Prior aggregate RED/no-rerun ruling remains. No completion, production activation, restart, provider mutation, QA dispatch, merge or deployment.

## Attempt 5: final Crashpad child metadata correction

Source39c9f72ea follows host evidence1620cb21 and replacement handoffa8122d4f: reporter-disabled Chrome stats Crashpad/new, so the authorized metadata-only exception covers the Crashpad subpath. Home/profile data, parent and child enumeration, synthetic nested dump reads, writes and egress boundaries remain denied. The former19a7e6725 review080dcbbb was effective APPROVED before this edit; that verdict does not certify this final head.

TDD1 red/13 passed to14/14; full browser102/102; full build and lint pass18 warnings; source/dist39c9f72ea. Root regression1 pass2 explicit nested-sandbox skips supplies no host acceptance. Final unsandboxed node+canary retest is registered with Lead as b0f95b33-7e8a-4fcc-a66d-3b8bf1d31596. No further permission widening if this candidate fails. Prior aggregate RED and no-rerun ruling remain preserved.

This milestone-only final commit requires fresh effective code review and exact-head CI. Host success and full parity remain unproven; no completion, production activation, provider mutation, restart, QA dispatch, merge or deployment.

## Attempt 5: Lead metadata confinement and browser hard stop

Source876f585e6 retains ordinary project editing while dropping the explicit workspace .:write override and restoring nested .codex/.git read-only rules. Parent effective-config validation, before thread startup, pins the manifest-selected MCP server set and launch fields; an injected server is rejected. Runner profiles are unchanged. Metadata assertion and injected-server assertion both failed before implementation; permission/parent/readiness17/17 pass. Broader targeted capabilities539 passed1 failed from a fixture omitting CLI MCP config; corrected default-parent integration1/1. Full build/lint pass; original RED targeted and aggregate receipts remain honest.

Host canary at08:12:18Z (source876f585e6, isolated non-temp home) passed: source writes allowed, .codex/config.toml and .git/config/hooks denied, attempted planted MCP absent from codex mcp list. Lead7fe7d0cf confirms this satisfies the first-sufficient policy experiment; no root relocation. Test-onlyd53736acc uses a native helper that applies the exact generated Seatbelt before direct getxattr(path); host probe08:14:04Z passed1/1 with0 skips, preserving Node data/enumeration/symlink guards. Full host receipts are in the task docs.

Native Chrome itself remains fail-closed with browser_lost: host evidence progressed through handshake/stat/new/xattr to settings.dat data open, which remains denied. Leadfe7feec4/b70623a1 prohibits further widening and explicitly defers headed-Chrome compatibility to FLY-2587. This is not successful browser launch or complete live Honey Lemon acceptance.

This milestone-only final head requires fresh effective review and exact-head CI before needs_review1191. Prior aggregate RED/no-rerun ruling remains. No production activation, provider write, restart, QA dispatch, merge or deployment.

## Attempt 5: exact-head CI runtime fixture repair

Head267f9f434 received effective APPROVED (round11 d7ad01b8), with seven nonblocking advisories reported to Lead. Its CI34946008708 failed four runtime fixture assertions: effective config omitted the real MCP argv, so the new security gate correctly rejected the fixture before the intended skills/thread error. Test-only097a0f894 now composes home TOML with parent MCP argv; no production changes or assertion weakening. The six-case table went4 failed2 passed to6/6, exit0 under the original5s timeout.

Two full-file runs remain RED139/141 (initialize5s timeout, following config spy failure); no flake ruling or full-file green claim. Lint and full build exit0; dist identity7e15cdb0d, production source unchanged since876f585e6. Prior aggregate RED/no-rerun preserved. Browser launch remains fail-closed and explicitly deferred FLY-2587.

This milestone-only final head requires new effective review and exact-head CI before needs_review1191. No production activation, provider mutation, restart, QA dispatch, merge or deployment.

## Attempt 6: engine landing conflict rework

Merged origin/main 156601a2e into approved base 215556c8b in 7e8fc888d. The two manual resolutions preserve both sides: Bridge shutdown closes the Lead GitHub provider and stops observation alerts/process resources; outbound SQLite retains SQL timing instrumentation plus durable operation receipt initialization and close-on-constructor-failure. No new capability or permission behavior was added.

Fresh local verification: pnpm lint and pnpm -r build exit 0; four affected suites pass 50/50 (outbound sender, SQLite dedup, operation receipts, observation storage alert); ci-structure shell contract passes. Existing host aggregate interruption and Lead no-rerun instruction remain recorded; no aggregate green claim. Inherited unrelated documentation whitespace was left unchanged.

This milestone-only final head requires fresh effective code review and exact-head CI. Browser capability is delivered fail-closed; follow-up FLY-2587 owns headed Chrome compatibility. QA retest and founder gate remain controller-owned. No production activation, provider mutation, service restart, QA dispatch, merge into main or deployment.

## Attempt 6: reviewed v2 launch and restart corrections

Effective review dedc8147 rejected97e69ff91 with two HIGH findings. Canonical registry v2 projection now clears the generic launcher's legacy sandbox default before both runtimes; direct runtime callers still reject legacy overrides, while v1 keeps its original launch environment. The stable managed home now accepts factory-named previous activation/artifact paths after they have been removed, comparing the complete old profile against the generated policy before replacing it. Extra permissions, foreign paths and MCP additions remain denied.

TDD: launcher identity11/12 to12/12; three root-rotation cases failed before the fix, then home/profile20/20 including tamper guards. Direct parser negative checks7/7. Real launcher/canonical/fixture-child boundary71/71; its initial70/71 missing deployment-verifier fixture failure remains recorded. Full lint and build exit0. No local aggregate rerun or production activation. Old-head CI34995624367 attempt2 SUCCESS after Lead-authorized single npm E404 retry is historical evidence only.

This milestone-only final head requires fresh effective review and exact-head CI. Browser capability remains fail-closed, follow-up FLY-2587. QA retest and founder gate remain controller-owned; no successor dispatch or ship.

## Attempt 6: specialized launcher correction and current-main integration

Review6888c9ca/request935ec889 returned CHANGES_REQUESTED at2e76f7ecd. Source3ec09db88 moves each of five specialized launchers' unchanged legacy sandbox defaults before the single canonical resolver, so v2 clears the legacy value before runtime execution. The actual launcher matrix proved five v2 failures before the correction, then10/10 v1/v2 cases pass; canonical13/13, Infra19/19, Mufasa23/23 each and generic launcher71/71. Older Mufasa fixture-schema failures were retained and corrected without changing assertions.

Provider HIGH upstream-provider-startup-has-no-fallback is overruled by supervised review-ruling52a168f8-b902-4547-8d63-c097fb78eef9 (Lead85d79381): retain activation fail-closed, all pins and no fallback. Twenty-eight advisories remain outside this bounded rework. A new effective verdict is required; the ruling is not review approval.

Main451812138/FLY-2447 merged in58f3c43ca. Outbound conflicts preserve reply/delivery-context and receipt cleanup together with roundtable engagement columns, migrations and identity checks; both test groups retained,58/58 pass. Automatic duplicate crypto import caused build/lint RED and was removed inca811a1a9; final build/lint pass21 warnings. Leadb5ef65f9/61b45fee authorized the two runtime compatibility hashes for upstream business_wake branches, with factual shared rationale and unchanged guards: drift2 failed15 passed to17/17. CI structure passes.

This milestone-only final head needs one bundled push, new effective review and exact-head CI. Earlier CI34999144534 remains FAILED with upstream/unrelated failures preserved separately; no local aggregate rerun. 浏览器能力 fail-closed 交付，跟进单 FLY-2587。QA retest and founder gate remain controller-owned. No production activation, provider write, service restart, QA dispatch, main merge or deployment.

## Attempt 6: conditional sync after FLY-2557 lands

Old77879ce0c effective review APPROVED (98450a72/a570c356), but CI35007382759 FAILED with13 jobs passing and two teamlead jobs failing; CI OK also failed. Auto-narrow/cursor failures are not proven main-only by empty file diffs (Lead70c23910); no failure is reclassified as green.

PR1199 merged18:37:48Z; Lead51033926 conditional authority released exactly one main sync. Merge de4868154 integrates main84a65da2e. The sole compatibility conflict takes main's runtime-pair entries and preserves both database rationale audits; db.ts equals the approved6eaa358a bytes and all member hashes match. No manual behavior changes.

One post-sync targeted run passes158/158 across seven archive/inbox/epic-residual/auto-narrow/outcomes/drift files. Full lint/build and CI structure pass21 warnings. This is not CI negative-matrix or shard-order proof. This milestone-only head now requires one push, one new effective review and green exact-head CI; if auto-narrow/cursor remains red it is bounded FLY-2519 rework before QA. No aggregate rerun/advisory work.

浏览器能力 fail-closed 交付，跟进单 FLY-2587。QA/founder gates remain controller-owned; no production activation, restart, provider write, successor dispatch or ship.

## Attempt 7: engine landing CI conflict rework

Lead instruction 610ab39b-4606-4bb8-82ae-5323f9be0428 authorizes one sync, targeted verification, one push and one exact-head review. Merge 2c2154136 integrates origin/main 6556b07518eec4323cef2067b73b5a27081329d0 into QA-approved base 9d7b4639054a8487c6d6df129fe46e6571443443. The sole manual conflict is .github/workflows/ci.yml: preserve main's FLY-2413 mutation test in the Node step together with FLY-2519 drill/browser tests, and retain the separate patrol step. Automatically merged inventory/residue/CI contracts retain both sides. No manual behavior change.

Post-sync pnpm -r build and pnpm lint exit 0 (21 warnings). Single targeted runs with VITEST_MAX_FORKS=1: teamlead retention registry/migration/mutations and judgment render 41/41; comm Discord ingest 29/29; runner kill-path inventory 5/5. CI structure, bounded patrol facts and residue shell suites pass. Root Node retention/parity/529/browser suites: 16 passed, zero failed, two explicit nested_sandbox_unavailable skips; these are not host acceptance. No aggregate rerun.

This milestone-only final commit binds the next single push and fresh review; earlier review/CI/QA are historical and do not certify this head. 浏览器能力 fail-closed 交付，跟进单 FLY-2587。QA retest and founder gate remain controller-owned. No production activation, provider write, restart, successor dispatch or ship.

## Attempt 7: blocking receipt reconciliation corrections

Review 177dcf4c-e1c2-4220-a06f-14068a69a051 returned effective CHANGES_REQUESTED on 0f4a89a1f, despite exact-head CI35038429709 success (16 jobs). Source 9e1562619 addresses only discord-write-reconcile-missing and github-write-reconcile-rejects-dataless-receipt. The seven nonblocking advisories were reported and remain outside this change.

Discord parent writes now register receipt-only reconciliation using the original UUID, payload and delivery context. Bridge bypasses its process cache for these lookups, reuses the durable result/read-only reconciliation and returns unknown if no receipt exists, without preparing or dispatching a new write. GitHub tier-B reconciliation accepts a validated persisted provider reference without inventing a DTO; initial execution still requires a full DTO. Secret, provider-reference and malformed supplied DTO guards remain active.

TDD: Discord lost-response recovery and absent-Bridge-receipt cases failed before the fix; the five GitHub B operations failed with invalid_provider_output before the broker fix. An initial test fixture used the wrong /github-write route; it was corrected to /github and all five red cases rerun against unchanged broker source before final green. Final four targeted files pass90/90 with VITEST_MAX_FORKS=1, including no duplicate writes, changed-payload rejection, no-dispatch-without-receipt, and invalid receipt output guards. Full pnpm -r build and pnpm lint exit0 (21 warnings). No aggregate rerun or host acceptance claim.

This milestone-only final head requires fresh review and exact-head CI. 浏览器能力 fail-closed 交付，跟进单 FLY-2587。QA/founder gates remain controller-owned; no production activation, restart, provider write, successor dispatch or ship.

## Attempt 8: #1209/#1217 landing conflict rework

Lead instruction a291506c-dde3-4c50-b7a7-0da0c1e9e6ba authorizes one main sync after QA attempt 7 passed. Merge 0aa8f597d integrates origin/main 557d2b00e into e2ff062b plus the mandatory progress cursor. The only manual conflict was Bridge shutdown: it now closes the FLY-2519 Lead GitHub provider and stops main's customer-release host before the remaining process resources. Automatically merged files retain both CI test groups, the FLY-2519 PR-session reader, main's customer-release/FLY-2602 StateStore behavior, and both kill-path inventories. The per-table retention registry remains split into fragments and includes all ten new customer_release fragments; no old monolithic list was restored. No capability, permission or activation behavior was redesigned.

Post-sync `pnpm -r build` exits 0 and `pnpm lint` exits 0 with 22 warnings. The first single-fork targeted batch recorded 114/119 assertions: retention 29/29, workflow-effort migration 13/13, customer-release invalidation 33/33, store 12/12 and Lead Discord 7/7 all passed; one host test failed because the pre-build `flywheel-config` artifact did not export the newly merged parser, and four loaded-host cases exceeded existing budgets. After the required workspace rebuild, customer-release host passes 5/5. Runner kill-path inventory passes 5/5; shell/Node CI inventory reports 324 shell suites classified and 76 Node suites enumerated.

The isolated Lead read file remains honest local RED at its default budgets: 18/20 pass, with one 15-second full-Bridge startup timeout and one GitHub request aborted by the 5-second test timer. Diagnostic headroom proves the selected GitHub direct/full-Bridge pair completes 2/2 without a semantic mismatch; no production timeout or test budget was changed. Exact-head CI is the aggregate/timing authority required by the handoff.

This milestone-only final commit binds the single push and one fresh exact-head review. Earlier review, CI, QA and founder evidence are historical for the prior head only. 浏览器能力 fail-closed 交付，跟进单 FLY-2587。QA re-verification and founder/ship gates remain controller-owned; no production activation, provider mutation, service restart, successor dispatch, main merge or deployment occurred.

## Attempt 9: effective-review corrections and current-main integration

Effective review `4e57ac5b-9f44-4891-bec1-2c9e1e443e10` returned CHANGES_REQUESTED on `1c66b0f9a` with three HIGH findings. Browser startup now disables upstream page-id routing, pins the resulting upstream schema digest, and rejects any upstream-required argument absent from the audited local facade. The model isolation probe must read a real pinned `~/.codex/auth.json` and prove denial; deployment `.env` and `packages/teamlead/.env` are always credential roots. Attachment authorization cancellation now observes the abort promise immediately, preventing the pre-race unhandled rejection. The six intended assertions failed before implementation and the six focused files then passed 20/20.

The formal package-gate receipt at `/var/folders/zl/nz5kfm5976q8p6kbt4d0cpgr0000gn/T/flywheel-package-gate-BmGpX3/summary.json` is retained as RED under Lead instruction `f3487339-27d5-4349-86dc-25c75238bd37`; no further full gate or threshold change was made. All non-teamlead packages passed, including terminal-mcp 51/51. Teamlead recorded 16,291 passes, 7 skips and three failures under host contention plus one onTaskUpdate RPC error. Fresh-TMPDIR, one-file reruns passed lead-events 13/13 and workflow-docs-git 4/4. Heartbeat 22/23 exposed a real same-millisecond event-id collision rather than a flake; UUID disambiguation then passed 23/23. The previously observed quota probe passed 13/13, and StructuredInboxRouter's fixed-sleep race was replaced with an explicit listener-ready wait and passed 19/19. The full gate was not rerun.

One authorized current-main sync merged `origin/main` `5bad6f6df`/`b51fec422`/`66f84d74e`. Its sole conflict was the FLY-2567 compatibility fixture: the resolution preserves FLY-2597 founder-attention rationale and FLY-2519's two read-only session lookups, with the merged `db.ts` digest. Drift and generator-oracle tests pass 19/19. Post-merge `pnpm lint` and `pnpm -r build` both exit 0; lint reports 22 existing warnings.

This milestone-only commit is the literal final commit for one push, a new effective code review and exact-head CI. Browser capability remains fail-closed with diagnostic `chrome_launch_unproven`/`browser_lost`; headed Chrome compatibility remains owned by FLY-2587. No Claude-in-Chrome, production activation, provider mutation, Lead restart, QA dispatch, main merge, ship or deployment occurred.

## Attempt 10: #1220/#1223 landing conflict rework

Engine rework `rework:b8a149e1229ff40c42536a847ef2c411b406cbf6ee4b11259d96c433a22b25d9` required a current-main refresh from approved base `d41b37146`. Merge `ee643f2ce` integrates `origin/main` `bd8a1f7b9`, including FLY-2612/#1220 and FLY-2619/#1223. The only manual conflict was `packages/teamlead/src/bridge/plugin.ts`: the resolution keeps FLY-2519's shared `leadOutboundDedupStore` for broker receipt/lifecycle authority while exposing the same `CodexLeadOutboundHandler` instance to FLY-2619's `SummaryPresentationController`. StateStore and compatibility-fixture changes merged automatically. No FLY-2519 capability, permission, browser or activation behavior was redesigned.

Post-sync focused verification passes 155/155 across eleven FLY-2519 outbound/dedup/wiring, FLY-2612 Linear and FLY-2619 summary suites. `pnpm -r build` exits 0; `pnpm lint` exits 0 with 22 existing warnings. The formal `pnpm test:packages:run` receipt is green for all 18 packages with zero failed assertions: structured receipts record 24,640 passes and 32 skips, plus release-contract's 24 passing Node tests; teamlead contributes 16,411 passes and terminal-mcp `test:run` contributes 51/51.

This milestone-only final commit binds one fast-forward push, a fresh effective code review and exact-head CI. 浏览器能力 fail-closed 交付，跟进单 FLY-2587。QA retest and founder/ship gates remain controller-owned; no Claude-in-Chrome, production activation, provider write, Lead restart, QA dispatch, main merge, ship or deployment occurred.

## Attempt 10: blocking review corrections

Effective review `d98897c3-2332-439d-8d5f-f0991d01fcb6` returned CHANGES_REQUESTED on `1de148104` with three HIGH findings. Source `210f0f357` addresses only those findings. Broker replies now bound socket flush and destroy the server-side connection, so half-open clients cannot retain all 32 slots. Capability-home regeneration now authenticates the managed config with an atomic transition proof and admits only reconstructed legacy path drift while continuing to reject fixed-policy, MCP, socket, artifact and extra-read tampering. Discord/read Bridge routes now resolve Lead identity and carrier authorization once per request, then use cheap project, summary-selection and target-carrier-revision guards across awaited effects instead of repeatedly forking `ps` and reparsing the project registry. Mid-request authorization revocation remains fail closed.

TDD and focused verification pass: the real UDS half-open saturation case, capability-home drift/tamper matrix, and Discord/read request-scope cases total 47/47; the full `runs-route.dag-entry.test.ts` isolation rerun passes 70/70 with `VITEST_MAX_FORKS=1`. `pnpm lint` exits 0 with 22 existing warnings and `pnpm -r build` exits 0. The formal package-gate receipt at `/var/folders/zl/nz5kfm5976q8p6kbt4d0cpgr0000gn/T/flywheel-package-gate-mgrKly/summary.json` remains honestly RED: all 17 non-TeamLead packages pass, while TeamLead records 16,412 passes and 7 skips; Vitest reports two 404 assertions in the single run/menu file under the 1,343-file host-contended aggregate (the reporter receipt counts five failures). The exact file passes 70/70 immediately in single-fork isolation. Per Lead instruction `849b3d4a-02cf-4ca1-9de8-8fecd187b20c`, local full-package convergence is not the gate; exact-head CI is authoritative.

This milestone-only commit is the literal final commit for the fast-forward push, fresh effective review and exact-head CI. 浏览器能力 fail-closed 交付，跟进单 FLY-2587。QA retest and founder/ship gates remain controller-owned; no Claude-in-Chrome, production activation, provider mutation, Lead restart, QA dispatch, main merge, ship or deployment occurred.
