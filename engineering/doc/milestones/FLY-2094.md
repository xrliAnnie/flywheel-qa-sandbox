# FLY-2094 — QA 循环默认无上限

**Status**: ⏳ Pending ship
**PR**: #967
**Date**: 2026-08-27

`code` 与 `simple_code` 的新 run 不再从菜单模板继承 QA 循环额度；无上限
`loop_iteration` 只记录实际轮次，不再携带 `maxIterations`。模板仍可显式声明正整数
`maxIterations` + `onLimit: escalate`，通用撞限 held/恢复路径继续服务这些有界 custom
manifest。

兼容边界：已经冻结的存量 run 不迁移 snapshot，仍可能保留原来的 3/10 轮额度并在撞限后
held；它们继续使用现有 FLY-1707 人工出口。按 founder 直令，本次没有增加第 N 轮提醒或
任何新告警层。
