# FLY-2862 — Codex 载体 Lead 空回答:回话合同 + 空答案守卫 + 语音侧失败播报

**Status**: ⏳ Pending exact-head code review, PR scope CI, and QA
**PR**: #1316
**Date**: 2026-09-24

根因两层,均已复核。① 规则:Codex 载体的回话是本轮 final answer(runtime 发到来源频道/
thread),它没有 Claude Discord 插件的 reply 工具;但 `lead-rules-bundle.sh` 只给「v2 + dept」
追加 `codex-discord-reply-contract.md`,v2 选择器也只给 dept 挂 Codex 适配,于是 cos 与 v1 的
Codex Lead(生产 Raya、529 slot)只看到「N==0 用 plugin reply」,ack 完 batch 就交空答案。
② router:`LeadInputRouter` 把空 output 照样发出 → Discord/Bridge 400 → `ambiguous`;语音侧只有
lead bot 在语音 thread 发非空消息才会关等待音,于是一直放。生产同样中招(只读查 journal):
Raya 76 条、Mufasa 21 条、infra bot 2 条空答案 → 400 → ambiguous。

修复:(1) 扩写 `codex-discord-reply-contract.md` 为 Codex 载体回话合同(final answer = 回复;
ack 不是回复;欠回复时不许空答;主动发/issue thread 走载体真实工具),`compute_lead_rule_bundle`
加可选 `carrier` 参数,只被 Codex 调用的 `assemble_full_access_governance` 传 `codex-app-server`,
cos/dept 均追加且必需;v2 选择器把适配扩到 cos。Claude 规则文件字节不变,parity/字节预算用例不受影响。
(2) router 对空/纯空白答案绝不发送:不欠回复(bridge tick、同伴确认)→ `completed/silent_no_reply`;
欠回复(founder 消息、任何语音房一轮)→ `dead_letter/empty_final_answer` + `onReplyFailed`。
正常、reconcile、resend 三条路径同一守卫;判定只用 Bridge 自写的信号(batch 头、语音 channel 标签),
batch 头格式抽成共享函数防漂移。(3) 新 Bridge 路由 `POST /api/lead-outbound/reply-failed`
(与 `/send` 同 token):记日志,若该 project/Lead 有持有 daemon 的语音会话拥有该 thread,
往 `voice_outbound` 幂等插一条固定短句,daemon 现有循环念出并关等待音;不发 Discord,voice-codex 零改动。
(4) headless 与 TUI 两个 runtime 把 `onReplyFailed` 接到尽力而为的上报(config 优先,回落 pane 已有的
`BRIDGE_URL`/`TEAMLEAD_API_TOKEN`)。

Raya 现网影响:下次启动起多装 Codex 回话合同;tick/同伴确认的空答改记 silent,不再 400;
founder 消息空答改记 dead_letter 并上报 Bridge;她的 rg 语音房空答会被念「回话失败」。走班车上线,
不要求重启,在 Bridge/Raya 下次自然重启时生效。

本机证据:`pnpm lint` 0 error;`flywheel-teamlead...` build 通过;`...flywheel-teamlead`
typecheck(teamlead + voice-codex)通过;受影响 vitest 文件(规则装配/预算/选择器、router/journal/
分类器/上报、inbox-loop、Bridge 路由与语音 store、两个 runtime 的全部消费者)全绿;
`lead-rules-bundle.sh` 的 7 个 shell 消费者全绿。未跑本地全包 suite;全量交 PR CI。
529 真房验收(Codex 载体 slot Lead 对口述回复非空并被念回)由 QA 在 Lead 开的房里做。

代码评审 R1(Codex gpt-5.6-sol xhigh)CHANGES REQUESTED,两条 MEDIUM 均已按建议修复并补 RED 测试:
合同删去「回复已从别处发出可留空」的例外(router 看不到工具回执,会误报失败),改为给指向;
Bridge 匹配语音会话要求 daemon lease 仍有效(过期持有者读不到插入的播报)。

实现节点没有请求 full CI、dispatch QA、merge、ship、deploy、restart,也没有写在线
`comm.db` / `teamlead.db`(生产 journal 只做过 `sqlite3 -readonly` 查询)。本文件是 PR 的 literal-last commit。
