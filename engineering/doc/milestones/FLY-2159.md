# FLY-2159 — Raya 语音响应有界恢复

**Status**: ⏳ Pending review
**PR**: #1004
**Date**: 2026-08-31

为 Codex app-server experimental schema 补齐
`thread/realtime/createResponse`,并在 Raya voice runtime 中增加 attributed user final
后 6 秒无 assistant final 的一次性、有界恢复。客户端每轮至多一次、每场至多三次;
官方 0.151.0 不认识新方法时 fail-open,不以 `appendText` 重放用户输入。

交付由三个 PR 承载:

- Flywheel 锚 PR [#1004](https://github.com/xrliAnnie/flywheel/pull/1004):批准方案、证据、复现 harness 与审查记录。
- Raya companion PR [raya#10](https://github.com/xrliAnnie/raya/pull/10):head `d3d5f9b`,watchdog、配置、事件与 21 条行为测试。
- Codex fork draft [codex#1](https://github.com/xrliAnnie/codex/pull/1):head `ca3bcfa6`,基于钉住的 0.151.0 base;未经 founder 单独授权不向 `openai/codex` 提上游 PR。

I1/I2 证明补丁/官方二进制的 `{}` 受理与 `-32600` fail-open 差异;I5 真上游探针
证明 active-response 拒绝的嵌套 `error.event_id` 精确相关。I3/I4 隔离房正常轮证明
assistant final 后 65 秒无迟发恢复,但归档事件未带 Codex binary SHA,因此只记为回归
证据,不冒充补丁加载的正向控制。

## Follow-up candidates

- 成功收到 assistant response 后回收 server 侧 recovery id,避免超长 conversation
  生命周期累计触及相关性集合上限。
- active-response 拒绝优先按稳定 `error.code` 判别,替代当前 message prefix 匹配。

两项都会改变已批准的相关性语义,本单只留档、不实施;如要推进需另开设计与审查。

Raya 全门禁通过;Codex 目标套件、I1/I2/I5 通过;Flywheel `pnpm lint`、
`pnpm -r build` 与 package tests 已完成,高并发基线超时均精确隔离复跑通过。
正式 code-review verdict 为 **APPROVED**,无阻断发现;可在批准范围内处理的 advisories
已收口。PR 在 milestone 前的 head 已 11/11 CI PASS,本文件作为字面最后一笔提交。
