# FLY-2745 — cmux 状态栏显示会话真实 effort

**Status**: ⏳ Pending exact-head code review and QA
**PR**: [#1266](https://github.com/xrliAnnie/flywheel/pull/1266)
**Date**: 2026-09-18

状态栏的 effort 数据源现按当前会话证据排序：stdin 的 `effort.level` / 标量
`effort`、`CLAUDE_EFFORT`、最近的精确 Claude 祖先进程 `--effort` 参数。所有来源都经过
小写规范化与 allowlist；三者都无有效值时明确显示 `?`。全局
`~/.claude/settings.json.effortLevel` 与型号猜测均不再参与显示，且没有修改 effort 决定链或
全局设置值。若展平后的 Claude argv 出现多个不同的有效 effort（例如 prompt 文本又提到
`--effort`），无法分辨参数边界时同样显示 `?`，不会猜一个看似确定的值。

installer 的候选 smoke 使用互相矛盾的全局 `xhigh` 与会话 stdin `medium`，同时要求只显示
`medium`；既有安装的回滚验证则保留兼容 smoke，避免把可运行旧版本误判为无法回滚。同步
`origin/main` `d8b3f3cd5` 后，聚焦状态栏套件 329 项、安装/回滚套件 121 项、milestone 布局
32 项、main 原 CI 红点的精确 Vitest 2 项均全绿；把实现临时变异回全局 settings 后，首条
stdin 阴性对照按要求以 `Opus 5/xhigh` 变红。Bash 语法与变更相关 ShellCheck 也通过。按任务
硬边界未运行本地 `pnpm test:packages:run`，也未由实现节点请求 full CI；当前 HEAD 与
`origin/main` 的 merge-tree 无冲突。

本实现阶段未安装状态栏、未重启 Lead/runner、未调度 QA。Tadashi `medium` 与 QA runner
`high` 的 cmux 真机截图仍由获授权的 QA/激活阶段在冻结头上验收；合并与部署也不在本阶段授权内。
