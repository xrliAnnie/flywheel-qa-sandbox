# FLY-2478 — Resident 宽限绑定判决

**Status**: ⏳ Pending ship
**PR**: https://github.com/xrliAnnie/flywheel/pull/1164
**Date**: 2026-09-11

PASS 请求释放、FAIL 原体返工，固定三小时兜底；释放事务推进 completed 与 park 结算，支持替身和巡检静默。resident boundary 续期及 stale boundary 采纳已覆盖。缺失登记或 lookup error 不作为独立死亡证据。代码评审、精确头 CI 与独立真机 QA 结果以工作流记录为准。

续跑已修正 finalization 的 typed lookup 测试夹具：68 项相关测试、lint 和全仓 build 通过。旧头 CI 失败；最终头 CI 与重新绑定的代码评审仍待验证。

R2 阻塞项修复：快速释放只调度所属项目，10 秒限频，60 秒后回归原维护通道；未知关闭证据仍保留 pending。161 项相关测试、lint、全仓 build 通过，重新提交精确头 CI 和代码评审。
