# FLY-2453 — 自动合并窄口开关

**Status**: 待本轮 exact-head CI 与代码审；未 ship
**PR**: https://github.com/xrliAnnie/flywheel/pull/1138
**Date**: 2026-09-09

## Implement attempt 3：授权凭据降级提示（当前轮）

QA claim994 / rework fd03c6ab（epoch19），Lead04e67442限定仅修两种stored auto异常：缺控制事件显示dry_run/degraded=true/receipt_missing；revision不匹配显示dry_run/degraded=true/revision_mismatch。runtime返回与founder手机卡使用同一路径；类型只增加这两个reason，其他分支不扩展。

两个案例分别先红后绿；实际enrichment/renderer HTML包含对应降级原因。移除卡片提示的变异使两条测试均红。窄口flag12/12与相关五套件81/81通过，lint退出0。完整lint/build/typecheck/rollback/packages门禁全部exit0，日志/tmp/fly2453-a3-*.log及a3-gates.json；本轮聚合包括runner1138通过/2跳过、TeamLead907文件12334通过/6跳过、voice649通过，没有TUI/RPC失败。

合并main前完整本地门禁exit0。随后main推进至5cbd540f1；按Lead184e09fc普通merge提交2ecdb6bae，保留signal与nonce两侧行为、registry27项与retention188表并集。合并后config68项、冲突文件+全部窄口130项exit0；遵照裁定不重跑本地全仓，合并头完整回归由新头CI14/14证明。

按Lead795edea3先提交修复与milestone使工作区干净；后续Lead6cba7250裁定TURN已有效授予，不再等待失败的正式wake。最终收据已补齐，本milestone为推送前最后提交，随后一次普通push、fresh exact-head review与CI14/14、needs_review1138。未改生产、未merge/ship/部署。

## CI 与追加必修返工（上一批次）

上一头b0aa0f66f审查APPROVED，但CI34413665275两次在TmuxAdapter的1ms墙钟用例失败。Lead授权分支2b，f4d12d10e只固定该用例Date.now并在finally恢复，deadlineMs1与原断言保留；Tmux170/170和runner1138通过/2跳过exit0。

Lead追加要求修复两条advisory。7c84a748d将Wilson下界限制到[0,1]，N21真实SQLite意见插入CHECK失败先红后绿；projector保留posting/uncertain及恢复字段，真实reconciler中POST与投射并发原先重复POST、修复后同一消息恢复并补banner。八个窄口套件79/79通过，lint/build/typecheck/新增rollback shell均exit0。其余六条建议留follow-up，详见acceptance。

完整packages命令exit1：未改动的真实Codex TUI探针observed=nomenu/expected=menu（usage warning），runner1137通过/1失败/2跳过。仅一次隔离探针2/2通过；Lead ed56de38及679a702d裁定为host-env限制，不改探针，以新头CI14/14为最终门禁。保留完整红值和隔离绿值的区别。

未完成包全部串行补验exit0：gemini157、edge1313、headphone54、TeamLead907文件12333测试通过/6跳过、voice649。本次TeamLead无RPC错误；完整聚合exit1仍按上述原因保留。详细日志与退出码见acceptance本轮首节。

本milestone为最后提交，按授权仅一次普通push，再申请新头review并冻结HEAD；CI/review结果写PR和Lead报告。旧头收据不替代本轮；无生产写入、merge、ship或部署。

## QA claim988 限定返工（上一批次）

按Lead `8d59d3d5-f7f9-4189-943b-9f48a3d10af4`，只补损坏flag读回和卡片降级提示。代码0851045df：非法raw仍为dry_run，同时返回degraded=true/reason=invalid_raw；健康raw返回degraded=false；卡片显示「已降级（invalid_raw）」。没有修改三闸、授权、安全降级或8条advisory。

11项runtime/card红绿测试通过；仅移除卡片提示的mutant导致6项HTML断言失败。runtime回归42/42、renderer/report-controls31/31；lint/build/typecheck/新增rollback shell均exit0。完整packages命令exit1（claude-runner1138通过/2跳过，零断言失败，onTaskUpdate RPC错误）；未完成包补验gemini157、edge1313、headphone54、voice649均exit0，TeamLead907文件/12330测试通过、6跳过，零断言失败，但同RPC错误exit1。保留原始失败及逐包收据，沿用既有Lead RPC裁定，新头CI14/14为最终门禁。详情见acceptance.md当前轮首节。

视觉限制：已生成真实reader/renderer HTML并验证markup；隔离Chrome启动exit134，浏览器MCP受approval-never策略拒绝，未获得截图验证，不冒充视觉通过。未写生产flag/DB/Discord，未重发529、未merge/ship/部署。此milestone为本轮push前最后提交；随后只普通push一次，申请新头review并冻结head，CI及review结果写PR/Lead报告。旧头9bb58b9e5批准和CI不替代本轮。

## 交付

项目级 off / dry_run / auto 开关默认 dry_run，每卡给机器意见、三闸得分和 emoji。只有 canonical founder 在配置工程频道的当前原消息经所属 Lead 提交核验，才能开启 auto；停止恢复 dry_run。机器纯文档、Lead pure_docs 声明和同 head 强度二证据同时满足，且没有 founder 打回或待处理输入，才写带审计的自动批准源并走既有 land。

控制拒绝有 fleet denied 审计；每次代批保留 auto_narrow_gate 及四线影子证据。意见使用持卡 Lead 身份，投递有退避上限、恢复与公平扫描。updater 在未投影自动源存在时拒绝回退到旧 projector。R1 仅增加窄口例外。

## Implement attempt 2：A/B 返工

Lead指定仅修复成功投递后的重试预算归零，以及metrics刷新保留posting/uncertain恢复状态。生产代码766338680只改两个SQL写点；3条新增真实writer/reconciler回归先红后绿，approval22/22通过，连续失败上限和两次稳定零扫描约束仍成立。

本轮lint/build/typecheck与新增shell预检exit0，whole-gate变异3/3杀死。完整packages命令exit1：TeamLead904文件通过/1失败、12296测试通过/1失败/6跳过，另有onTaskUpdate RPC；唯一断言失败是既有4271投影身份性能阈值。当前/旧头隔离各9/9，再按Lead追加branch/merge-base各3次，六次均9/9exit0；未改阈值。Lead f06a9fee判为B本地负载/RPC限制，完整红值与六份对照详列acceptance.md，最终仍需新头CI14/14。未完成voice-bridge补验60文件649测试exit0。

上一头bdd900a13的CI14/14、APPROVED与QA claim981属于历史收据；A/B是随后追加硬检查，不能用旧批准代替本轮。其余6条新头前审查建议与此前LOW follow-up未扩展修改。完成此最后milestone提交后仅一次普通push、一次新评审；评审中冻结head。若CI仅main继承枚举红，按Lead9569efc6等FLY-2468合入通知后同头重跑失败项。

## 历史 Attempt 2 验证

- 严格红绿验证原 CI 回归、六种拒绝审计与回退预检；最终 route18项、retro13项通过，fixture未修改。
- 最终 whole-gate 变异3/3杀死，每道闸的 writer和unit负例均红；baseline绿；CI显式运行，529驱动manual-only。
- QA shadow8文件41项通过。按Lead65eadca8裁定将旧STOP用例改为409，引用plan§4/11，保留较新的OPEN。529真机证据沿用attempt1 claim970，本轮不重发。
- lint、build、typecheck退出0；新shell回退预检、CI枚举/结构通过；updater36/36、shadow/retention16/16通过。
- 精确完整package命令退出1：claude-runner1138通过/2跳过，零断言失败，但onTaskUpdate RPC超时。按Leade3236876补验全部未完成包：gemini157、edge1310、headphone54、voice-bridge649测试通过，四包exit0；TeamLead905文件、12294通过/6跳过，零断言失败，但同签名RPC超时exit1。完整聚合及TeamLead包不记为绿，PR明确披露，exact-head CI为最终权威。
- main经68d57730c合入工作分支；更新后的main227058c73与当前分支merge-tree退出0。未force push。

详细命令、记录与限制见 `engineering/doc/FLY-2453-auto-merge-narrow-gate/acceptance.md`。本文件是推送前最后提交；后续CI与新头评审结论写入PR和Lead报告，不修改此冻结头。旧R3不替代新评审。

## 边界

本轮未切生产flag、未写生产数据库、未代按真实ship卡、未部署/重启服务、未请求ship approval、未合并PR或派发QA。非阻断429重试、declared_by名册化及旧R3建议保持follow-up。出现新阻断评审发现即停报，不在评审中推送。
