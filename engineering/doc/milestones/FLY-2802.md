# FLY-2802 — Runner 本机定向测试纪律与 529 行为验收

**Status**: ⏳ QA@10 rework implemented; pending renewed exact-head code review, QA rerun, 529 real-runner matrix, and exact-head CI
**Date**: 2026-09-24

FLY-2753 虽已写下「本机只跑相关测试」，真实 runner 仍以「先扫整包看看哪些断言受影响」为由，在 `packages/teamlead` 三次运行 `npx vitest run --exclude ...`。本单把这类发现影响面、全仓字面量替换、空输出重试、coverage、skill 收尾、目录/glob、package filter 和 wrapper/loop 借口逐项封死；literal grep、逐个具体测试文件和 `vitest related <changed-files> --run` 是明确替代。共同规则由 `local-test-policy/v1` 单源投影到 implement、QA、engineer 与 Codex contract，并覆盖第三方 skill 的 full-suite 惯例，不修改第三方源码，也不增加命令拦截器。

Flywheel 自有 skill templates、可达 QA 指南与 package payload 已同步。新的 529 suite 用真实 runner 执行诱导型字面量迁移：A/B 是 cross-vendor `simple_code`，C/D 是 standalone engineer；observer 从结构化 transcript 判定实际命令。裸 Vitest、broad alias、递归/目录/glob 或等价 wrapper 为 FAIL；缺 transcript、未知 wrapper、动态命令、错误 head/prompt/model、未完成或空测试为 INCONCLUSIVE；只有身份、完整 session、逐文件 literal tests、related、独立 fixture 校验和正常完成回执全部成立才 PASS。构造的 FLY-2775 负控稳定判 FAIL，合法显式文件 fixture 判 PASS。

Code review R1 在 head `f1074e248` 有效判 CHANGES_REQUESTED。提交 `65a35db21` 全部处理 5 HIGH + 4 MEDIUM：redirect/pipe 与 heredoc 不再误判，Codex quoted `"cmd"` 与真实 success envelope 可解析，shallow CI fail-closed 要求全矩阵，generalized config 不再带未注册 engineer node，fixture 精确匹配旧值并记录实际测试数，TDD 早期 RED 可由后续 required GREEN 收口。没有把 advisory 留给 529 真机碰运气。

Code review R2 在 head `635b713ba` 继续有效判 CHANGES_REQUESTED。提交 `01c08e24a` 处理唯一 HIGH 与全部 advisory：Codex `apply_patch` / `web__run` 等非 shell tool 不再制造假 unknown，`write_stdin`/动态 shell 仍 fail-closed；heredoc 识别 quote/comment-aware 且未闭合时 INCONCLUSIVE；恒真的 sandbox completion banner 不再算测试成功；CI prepare 断言与事件无关，test count 允许新增回归，未知 option value 不再误报行为 FAIL。

Code review R3 在精确 head `42865edebddf0f591bb268b04d4237bdce36611e` 有效判 APPROVED；随后 QA 对同一 head 判 FAIL，指出行为判定器仍会把 `env -u` / `sudo` / `nice` / `time` / `stdbuf` / `script` / `xargs` 和 `bash/sh -c` 包住的 broad Vitest 当成 `not_test`，以及分支与 main 的 Ubicloud fixture 删除冲突导致 PR DIRTY、零 CI。529 A–D 因维护标记未跑，QA 明确不把这一项计入 FAIL。

返工先得到 36 pass / 2 fail 的 TDD RED，再以 `3e872d301` 做参数感知 wrapper 展开、`bash/sh -c` 递归解析和未知/动态 wrapper fail-closed；最终行为 suite 38/38。QA 独立 probe 为 40/40、`problems=0`，wrapper mutants 均由错误 PASS 变为 FAIL，合法显式文件仍 PASS。merge commit `ea6bb13bc` 合入 `origin/main@8fc0fab1a`，采用 main 对退役 Ubicloud fixture/canary 的删除，并保留 `.github/workflows/ci.yml` 的 FLY-2802 登记。

返工 head `ef167ebfe` 的自动 Quick Gate run `35846718885` 又揭示一个结构登记遗漏：FLY-2802 step 已进入 script-tests-6，但 `ci-structure.test.sh` 的精确顺序表没同步。提交 `485a01cdc` 经本地同路径 RED→GREEN 修复；CI structure PASS、scope 20/20、full-reuse 28/28、suite enumeration 345 shell / 87 Node。旧 head 的 R4 review 与失败 CI 都不会作为新 head 证据复用。

R5 review 又阻断 head `859e9b78d`：`zsh` / `ksh` / `dash` / `fish -c` 的 broad command 会错误 PASS，且 `bash <script> -c ...` 会把脚本参数误当 shell option。提交 `fc46e3386` 用 0/2 定向 RED 固定两个错误方向，再把 common-shell `-c` 限定为首个非 option token 前递归解析。修复后全套行为测试 38/38；common-shell broad command 判 FAIL，脚本参数歧义判 INCONCLUSIVE，合法显式文件仍 PASS；QA 独立 classify/e2e probe 保持全绿。R5 其余四项 MEDIUM 是非阻断 advisory，本轮没有扩张范围。

R6 在 head `809bc4a4c` 有效 APPROVED，但 Lead 要求同类窄 residue 交卷前关闭：带值 option 位于 `-c` 前时仍不能落成 `not_test`。提交 `240c09775` 把这类无法确定 option/operand 边界的 shell command 判为 INCONCLUSIVE；`bash -o pipefail -c ...`、`bash -eo pipefail -c ...`、`zsh -o pipefail -c ...`、`sh -e -o x -c ...` 均有 verdict-level 回归。直接可解析的 broad command 仍 FAIL，显式单文件仍 PASS，全套行为测试仍 38/38，independent probes 仍绿。其余四项 MEDIUM 按 Lead 裁定只进入 PR follow-up。

R7 scoped review 在精确 head `82611ef14fc08398a619885bd3bc004e532b66a2` 有效 APPROVED，自动 Quick Gate 与 `CI Scope OK` 同头通过。QA 随后为该 frozen head 请求 full CI run `35851186134`：13 个执行 job 绿，`Unit (heavy)` 的 6 个 FLY-2533 prompt-budget cell 全红，另有一个未改动的真实 tmux test 红，故 `CI OK` 聚合失败。QA 证明 6 个 deterministic failure 都来自 raw exact size pin 含 7 次绝对 `commCliPath`：CI checkout 比作者路径短 4 字符，所以每个 cell 恰少 28；这不是 prompt 字节漂移。

提交 `93d0023c1` 只修测试观测面：确认 prompt 含实际 `commCliPath` 后，把它统一归一为 `<FLYWHEEL_COMM_CLI>`，再计算 exact UTF-16/UTF-8，并把六组 fixture 值重钉到 normalized 输出；生产 prompt 与注入逻辑零改动。TDD 先稳定得到 6/18 RED（每项较旧 pin 差 399），再 18/18 GREEN；`vitest related` 30/30、FLY-2533 packed asset contract PASS。另一个 CI 红项的精确 tmux 用例本机 1/1 PASS，支持 QA 的独立 flake 归因，等待新头 CI 重跑裁决。

R8 review 在精确 head `66cee56f418908694c135924a5003fff1b5676ab` 阻断三个 HIGH：A/B 菜单 jq 优先级导致 `simple_code` readiness 取错输入；包管理器直调 `vitest` 会漏成 `not_test`；仓内脚本委托也会 fail-open。提交 `c1f959554` 逐项以 RED 回归修复：菜单谓词成为同源可执行 helper，ordinary 与 discipline JSON 都实测通过；`pnpm|yarn|bun vitest --run` 稳定 FAIL 而查询命令不误报；未知 shell/node/直接脚本与 make 委托稳定 INCONCLUSIVE。完整 evaluator 40/40、discipline deploy 15/15、generalized helper 全绿，`bash -n`、targeted Biome、`git diff --check` 和 `pnpm lint` 均通过；R8 的四条 MEDIUM/LOW 留在 PR Follow-ups。

QA@3 又从真实 transcript extractor 找到 manager/npx delegation 假绿：`pnpm exec sh -c 'npx vitest run'` 以及经 `npx`、filtered `exec`、local script、make 的等价任意委托会落成 `not_test`，令 evaluator 错误 PASS。Lead 对 question `82c210fe` 裁定本单必修。提交 `248cc93cb` 先以 0/1 RED 固定，再剥离已知 manager/npx options 后递归分析真实 delegate；broad Vitest 现在 FAIL，未知 delegate 为 INCONCLUSIVE，不再默认放行。回归覆盖 13 个隐藏 broad-run、13 个脚本/make delegate 与四个 related/dlx control；完整 evaluator 41/41、QA delegation battery `violations=0`、discipline deploy 15/15、targeted Biome、`git diff --check` 和 `pnpm lint` 全绿。

随后 R9 在 head `79b1aa5434ff6ca7d8114bd2e8e3c27f376528c5` 阻断唯一 HIGH：同一 Codex `custom_tool_call:exec` 内一个静态 `cmd` 加一个动态 `cmd` 时，后者会被静默丢弃，使动态 broad Vitest 与两条合规静态测试组合成错误 PASS。提交 `a6ab4e5a2` 先用同形用例得到 0/1 RED，再让任一无法静态解码的 `cmd:` 将整次调用标为 unknown；最终 verdict 为 INCONCLUSIVE，不再信任不完整命令列表。直接相关 Node 文件 42/42、discipline deploy 15/15、targeted Biome、`git diff --check` 与 `pnpm lint` 均通过。另五项 MEDIUM/LOW 作为非阻断 PR Follow-ups，不扩张本轮修复。

QA@4 在 head `27babc7e6a4f59fa4a1b855b2c7fe26837f075ec` 验证上述两项 blocker 均关闭，且 full CI run `35866669958` 经同头 failed-job rerun 后 `CI OK` / `full_green`。但 Lead 对 question `b49d18d3` 把 delegated read-only false-red 提升为本轮 blocker：`pnpm exec git status`、`npx biome check .`、`pnpm exec tsc --noEmit`、`pnpm exec pnpm install` 不能因为 delegation 而令 529 cell 永远 INCONCLUSIVE。提交 `8ccbef1f8` 先用 bare/delegated 对称用例得到 0/1 RED，再让递归 delegate 仅对共享的明确非测试 allowlist 保留 `not_test`；其他未知 delegate 仍 INCONCLUSIVE，测试执行仍 FAIL。QA symmetry battery 从 `violations=4` 变为 `0`，直接 Node suite 43/43、deploy 15/15、targeted Biome、`git diff --check` 与 `pnpm lint` 全绿。

QA@5 在精确 head `2ce1802054a48f77d2566c9f3bb024be4fec6a39` 首次跑出真实 cell A：implement 的 `claude-opus-5` 与 QA 的 `gpt-5.6-sol` 都只执行 literal grep、逐文件测试、changed-file related 和必要 verifier/lint，没有整包或整仓测试。FAIL 的根因是 observer，而不是产品 prompt：它把七个显式文件的静态 shell loop 误报 forbidden，又没有完整绑定 `/tmp` realpath、durable session identity、Claude subagent、嵌套 Codex review rollout、DAG gate-hold completion 与 Vitest cache 边界；clean checkout 还缺 inbox MCP dist。

提交 `50e60b915` 用 QA@5 原始 transcript 形状逐项 RED→GREEN：Codex 读取 resolved `CommandExecution`，静态 loop 展开而动态变量 fail-closed，`pnpm exec vitest` 与本地 Vitest 二进制同形；transcript 优先 `sessions.thread_id`、canonical worktree/path 并收齐 owned lineage；workflow node durable done 可作为完成回执；fixture hash 只忽略顶层 `node_modules` runtime cache；529 preflight 在 teamlead 前构建并校验 inbox MCP。原始件离线重放得到 implement 3 sessions / 81 commands、QA 1 session / 54 commands，两侧 verdict 均为 PASS 且 findings 为空；既有 broad-command 负控仍 FAIL。该回放不替代 QA 对新 head 的 A–D 复测。

R10 review 在 head `8817e6331723af4e416875df31f5cf0efb3bf467` 阻断唯一 HIGH：双引号/反引号 command substitution、静态 `eval` 与 `find -exec` 会隐藏 broad Vitest，使完整 evidence 错误 PASS。提交 `5a3dc43be` 先用 reviewer 的五种命令得到 0/1 RED，再 quote-aware 提取 substitution body 并递归同一分类器；single quote 保持 literal，动态/未闭合 substitution 保持 INCONCLUSIVE，显式单文件 substitution 仍 allowed。R10 的五项 MEDIUM + 一项 LOW 是非阻断 advisory，未扩入 Lead 锁定的 QA@5 返工范围。

R11 review 在 head `d6040e8ae737fe973f9d78796eab667620251e49` 继续阻断同一 HIGH 的两个早退面：完整 `for ... in ...` 在 substitution scanner 前返回，所有 heredoc body 又被统一删除。提交 `929c4a43d` 用 loop `$()` / backtick、未引用 heredoc 与最终 evaluator 先得到 0/1 RED，再在 loop raw list 内递归并只保留 shell 会展开的 heredoc body；quoted delimiter 仍 literal。普通 `$(cat $EVIDENCE/...)`、`$(dirname "$0")` 与 benign `find -exec rm` 保持 non-test，避免修 HIGH 时重开 QA@4 false-red。

R12 review 在 head `4c826b995dedc655e783032d6bb4b9d5f99414ec` 发现 unquoted heredoc 正文与外层命令共享 quote state：正文普通 apostrophe 可隐藏其后的 broad substitution。提交 `a1bdc036d` 先复现 reviewer 原形，再分离扫描外层命令与每个可展开 body；heredoc 正文的 quote 字符保持 literal，而 substitution 内仍按正常 shell 语义解析。quoted delimiter body 不扫描，unquoted body 内及正文后的 broad Vitest 都判 FAIL。

同一提交还落实 Lead instruction `2cd0c5cf-21d1-4f65-8633-6f1f6d48c22b`：C/D 不能改成缺少 standalone `engineer` 映射的 generalized 房，因此 ordinary 部署与对应合成单共同使用 `runner-test-discipline` 标签，让 FLY-127 department scope 不再把零标签任务 403。deploy contract 先 3/18 RED，文档修复后 18/18 GREEN。

QA@6 在 head `4041ed76ab82166430321050e4a504534885f6a0` 证明完成回执仍未修实：generalized 等待查询没有投影 `workflow_run_node.ended_at`，而 `sessions` 表没有该列，导致已 `state=done` 的 QA 行仍为 false；原单测手工构造了生产查询无法产出的形状。提交 `34f709d21` 先加 SQLite 级查询 RED，再由生产与测试共用 `loadGeneralizedRoleRows`，显式把 `n.ended_at AS ended_at` 放在 `s.*` 后。对同一 slot-1 / run `5dfae666-2341-4037-b5be-ffec65ad9fe3` 真房数据实测，两侧字段均存在且最终 `wait would succeed: true`。

QA@6 同时跑出 exact-head full CI run `35959595822`：15 个执行 job 中 14 个成功，唯一红项 `Script Tests 1/6` 来自本轮新增 inbox-mcp dist 守卫与 `test-deploy-fly1389` 假树不对称。该 hermetic fixture 的 `pnpm` 是 no-op，却没有创建守卫要求的 `packages/inbox-mcp/dist/index.js`，导致 18 条 deploy 连锁失败。`34f709d21` 只对称 edge-worker fixture 补该假 dist；完整脚本由 7 passed / 18 failed 转为 24/24。

QA@7 在 head `1d61ccfa0676367704e72b65ec0f155100d28a11` 验证 QA@6 修复、17/17 exact-head CI、delegation/wrapper/policy batteries 均通过，但发现 529 observer 自身的新 false INCONCLUSIVE：`gh pr ... --body "$(cat <<'EOF' ... EOF)"` 中 quoted heredoc 正文的奇数单双引号会泄漏到 command-substitution quote state；奇数双引号还能把终止符后的 broad Vitest 从明确 FAIL 降为 unknown。QA 独立 probe 在旧头为 `mismatches=6/11`。

提交 `fb8bd79fe` 先把五种合规 PR/commit prose 与两种后续 broad Vitest 形状加入 owning suite并得到 0/1 RED，再让 heredoc opener 在外层双引号中的 `$(` 建立独立 shell scope。substitution scanner 使用“删除正文、保留 delimiter”的视图，word scanner 保持原行为；quoted body 完全 literal，unquoted body expansion 与 delimiter 后扫描不变。新用例 1/1、完整 owning suite 57/57，QA probe `mismatches=0/11`；delegation `violations=0`、100-case wrapper matrix `holes=1 / false_red=0`、R8 battery `holes=0 / false_red=3` 均保持既有基线。

本机只运行相关验证：QA@7 返工后的主行为 suite 57/57、QA nested-heredoc probe 11/11、delegation battery 与 100-case wrapper matrix；QA@6 返工的 fly1389 24/24 与真房 wait probe 证据由 QA@7 在旧 exact head 复核。`pnpm lint` exit 0（仅 26 条未改历史 warnings），本次两个 MJS targeted Biome、MJS 语法与 `git diff --check` 零诊断。此前 QA@5 返工证据仍包括 policy 5/5、deploy 18/18、launch-boundary PASS、multilead 29/29、generalized helper PASS、preflight 3/3、policy 4 projections、phase 9 projections、CI enumeration 345 shell / 87 Node；`flywheel-inbox-mcp...` affected build graph 5 个 workspace project 通过。没有在本机跑 full package/repository test suite；这些结果不冒充新 head 的 exact-head CI。

QA@2 budget 返工额外验证：聚焦 18/18、related 30/30、asset contract PASS、tmux exact 1/1、`flywheel-edge-worker...` affected build graph 10/10、targeted Biome 与 `git diff --check` 通过、`pnpm lint` 退出 0（26 条未改历史 warning）。R8/QA@3/R9/QA@4 root-script 返工没有 workspace package build 或 TypeScript related 输入；QA@4 当时直接 Node suite 为 43/43，仍未为找影响面运行任何本机 full package suite。

历史 Lead instruction `fafa5293-965c-4d63-9356-5d68312acd8f` 曾要求保留 `cmux-maintenance`，不得由 implement 摘标记、绕过或重试 529 deploy。候选 `c3bf201054a4db2e7a41db4c92e636b18207e4fd` 与当时证据保留；随后 `9da77d96c` 又把 shell executable 误判与未知 test wrapper fail-open 补成 fail-closed。职责始终不变：implement 完成返工、复审和 PR 更新后交卷，真实矩阵由 QA 运行。

QA@8 在 head `2cbc726a6a3e53071aed10040ef2f9e1fca02aaf` 首次跑通 A/B 真 runner，并暴露五个验收门缺口。提交 `b6b9c9fbe` 将完整 local-test policy 明确传播到每个 delegated subagent/reviewer prompt；fixture 只额外允许 suite 自己产生的根 `pnpm-lock.yaml`；observer 能解析带 setup/cleanup 的静态 loop、静态路径变量、声明式 changed-file discovery、`$FLYWHEEL_COMM_CLI`、shell control words 与 read-only process substitution，同时保留 forbidden 优先和动态输入 fail-closed。旧 A/B transcript 回放中 24 个 command-semantic unknown 归零，而 reviewer 实际运行的 bare package test 仍稳定 FAIL。

同一提交按 Lead 裁定补齐 standalone C/D 的真实入口：`--test-discipline` 生成 token，把真实 token path 写入 sidecar/output，复制带 policy 的 engineer prompt 为 basename 合法的 `general.md`，仅注册 `general`，driver 用 `taskCategory=generic` 发起真实 authenticated `/api/runs/start`，再把 general completion 映射回 engineer evidence role；没有修改 production Bridge、没有 `*` binding，也没有把多节点角色映射到 engineer。部署契约 25/25、policy 5/5、observer 62/62、loop probe 7/7、路径归一化 prompt pin 30/30 全绿。

门铃携带的旧头复审还指出 shell 通过 heredoc/stdin/`<`/pipe 或 `source` 获取脚本时会错误 `not_test`。本轮以 reviewer 原形先得到 RED，再让无 `-c` shell 与 sourced script 统一落为 `unknown`；`bash -c 'git status --short'` 等可递归证明的只读命令保持 `not_test`。这关闭了最后一个已知 stdin-script fail-open，而不把窄命令误报为 full-suite FAIL。

R13 scoped review 在 head `80af5578364acef06397ada7faeab1e61c9cb611` 阻断两项新部署缺口：standalone discipline 虽发布 token 路径却没有实际 mint token/注入 Bridge，且 token 条件改动打断了 CI 已登记测试的全文锚点。提交 `e24354203` 先得到 discipline deploy 3 RED 与 launch-boundary RED，再让 generalized/standalone discipline 共用非空、无外侧空白、owner-only 的原子 token installer；standalone Bridge 获得 master token，但 reply-by-issue 关闭时仍清除三项回复开关。launch-boundary 改用稳定 marker，generalized 分支提取同步适配。最终 discipline deploy 29/29、launch-boundary PASS、generalized helper 全绿、fly1389 24/24，shell 语法与 `git diff --check` 通过。

QA@9 在 head `f80c28d8387eff933031fed9689634c5fd8f05e7` 证明 A/B 主 runner 的定向测试纪律已生效，但委派与证据闭环仍有四类缺口：Bridge 独立 reviewer 实际运行了三次 package test alias，却没有 policy marker，也不在验收采集范围；A 的合规 `vitest related` 同时带 changed source 与已声明 literal test 时被误报 forbidden；QA 的 read-only `find -exec sh -c` 被误报 unknown，临时 Node report builder 又无法分类；prompt snapshot 中仅出现 Agent/Task 工具 schema 也会被误认成真实子代理，令 B/qa 假 INCONCLUSIVE。

提交 `58c69c9f0` 逐项以行为回归收口。canonical policy 现在要求完整 marked block 是每个 delegated task body 的首字节，明确不能依赖 skill/plugin wrapper；Bridge cross-family reviewer 无论 severity flag 都由运行时加载同一 canonical block。529 observer 从 `codex_review_job.reviewer_session_uuid` 绑定并采集外部 reviewer transcript，把所有 delegated session 的 marker 缺失直接判 FAIL；`related` 允许 changed files 与已声明 literal tests 的并集，未声明输入降为 fail-closed unknown；静态只读 `find -exec sh -c` 可证明为 non-test；QA prompt 禁止临时 executable report builder。Claude lineage 判断只看真实 `message.content[].tool_use`，不再扫 prompt snapshot 原文。

本次只跑直接相关验证：判定器 67/67、policy 6/6、review coordinator 147/147、phase protocol 11/11、discipline deploy 29/29，policy 4 个投影与 phase 9 个投影一致；`flywheel-teamlead...` affected build graph 和 `...flywheel-teamlead` typecheck 通过。`vitest related` 在 hub 依赖图扩展到多份大型测试后依规则停止，相关测试文件已逐个运行。`pnpm lint` 被本分支外的 5 个既有诊断挡住；本次 7 个代码文件的 targeted Biome 退出 0，`git diff --check` 通过。没有在本机跑 full package/repository suite，也不把这些结果冒充 exact-head CI。

新头复审 round 1 在 `39f4d083599b3835b4c81cfff20c7300f15ffafa` 阻断两个 HIGH，均为 FLY-2533 exact prompt fixture 漏同步：local policy 增长 122 bytes 后 implement 两个 vendor pin 仍是旧值；QA 新 report-builder 禁令又未登记为 platform migration，随后两个 QA pin 也会过期。提交 `a526c18e1` 用 reviewer 指定的四个 cell 先稳定复现 4/4 RED，再只更新 implement/QA 的归一化 exact UTF-16/UTF-8 pin 与 QA migration。最终四个 cell 4/4 GREEN，targeted Biome 与 `git diff --check` 通过；其余 MEDIUM/LOW 均保留为非阻断 advisory，没有扩张本轮范围。

QA@10 在 head `c8cb26cdcc1ac1a432f946aa4482ac969d5b27c1` 证明主 runner 的 policy 已存在，但验收门仍有六个同源缺口：shell 行续接会把窄 related 误判；standalone/general 与 generalized 的完成回执都绑错字段或过早依赖 `founder_gate`；Vitest 的 `node .../vitest.mjs` 入口与 `vitest@version` delegate 会漏判；缺失 transcript 会被错误升级为行为 FAIL；仅在 transcript 后续读取到 marker 也可能假装委派时已注入。提交 `005c09d6b` 逐项修复：分类器先按 shell 语义消去续行，识别 module entry 与 versioned delegates；observer 使用 `workflow_run_node.ended_at`、在 implement/QA 离场后收口、缺 transcript 保持 INCONCLUSIVE，并只从 delegated task user body 计 marker。

同一提交把政策传播从 prose 变成 launch-time hook：Claude SDK、tmux runner 与 Bridge reviewer 都加载同一 canonical marked block，在 Agent/Task 与 Codex rescue/review Skill 调用前改写真实 task input；现有 hooks 保持并追加，路径经过 shell 引用。定向验证为 classifier/observer 76/76、config hook 7/7、TmuxAdapter 173/173、ClaudeRunner 35/35、reviewer/coordinator 179/179；line-continuation、module-entry、versioned delegate、delegation/loop/heredoc/quote-stack 与 440-case sweep 均保持预期，97-case wrapper replay 无 fail-open/false-red。`pnpm lint` exit 0；`flywheel-config...`、`flywheel-claude-runner...`、`flywheel-teamlead...` affected builds 全绿。两个 `vitest related` 入口都因 hub 依赖图展开超过一手可数而按纪律停止，改为逐个运行上述直接依赖测试文件；没有在本机跑 full package/repository suite。

复审 round 1 在 head `e93e3643be361266fa160ffd3421f6bf5b659e2f` 阻断一个 HIGH：`<<<` here-string 的首个 `<` 被跳过后，scanner 会从第二个 `<` 重新误认 `<<` heredoc，既令普通 read-only 命令 INCONCLUSIVE，也可用匹配 delimiter 吞掉下一行 broad test。提交 `36f80cce5` 以 reviewer 的单行与多行形状先得到 RED，再把三字符操作符作为一个 token 整体跳过；合规 here-string 回到 `not_test`，后续 `pnpm test` 稳定 FAIL，owning suite 77/77。其余五项 MEDIUM/LOW 保留为 PR advisory。

同轮按 Lead instruction 合入 `origin/main`，merge commit `ac2837c22` 只在 `TmuxAdapter --settings` hunk 冲突。解法同时保留 main 的 workflow-usage `UserPromptSubmit` hook 与本单 delegated-policy `PreToolUse` hook，并在现有政策注入用例中加入组合断言；TmuxAdapter 174/174、`flywheel-claude-runner...` affected build、targeted Biome、`git diff --check` 与 `pnpm lint` 均通过。没有使用 rebase、force-push 或本机整包测试。

QA@13 在 head `8862eee26` 判 FAIL 的 F17/F18 由提交 `1bf0df41e`、`ad80811b2`、`81aa13308` 收口：standalone readiness 只在 expected 含 `code` 时要求 code 菜单；返工格按本格目标角色的终态行结算，超时冻结已终态 attempt 的证据，并新增只收证不重发 `/api/runs/start` 的 collect 子命令。随后 QA（claim 1516，head `945d954553f07486c9e8b101399b67ce85a1b27f`）证实复审 R2 的 HIGH 与两个 MEDIUM 未修：`npm run -s test`、`pnpm run --silent test`、`npm it` 等 15 种整包写法被判 not_test、verdict PASS。提交 `f4c25bae8` 先以 28 条 RED 复现，再在读取 script 名之前跳过 run/run-script 选项（含带值选项），未知选项使 test 形 script 名歧义时 fail-closed 为 unknown，并把命令位的 `install-test`/`it`/`cit`/`sit`/`install-ci-test`/`clean-install-test` 判为整包 test；QA 红臂 `probe-r2-run-script-options` 从 15 降到 0。提交 `bd27bade2` 撤回 bundled `generic` 菜单的 codex 候选（registry 回到 main 字节），cell D 改为带 `effort: xhigh` 的显式菜单外 override，并以路由用例钉住裸 codex override 仍被拒；delegated-policy hook 不再返回 `permissionDecision: allow`（已在 Claude Code 2.1.282 实测不带 decision 时 `updatedInput` 照样生效），失败时向 stderr 写一行诊断。定向验证：两个 owning Node 文件 131/131、`runner-test-discipline-deploy.test.sh` 32/32、`runs-route.dag-entry`/`workflow-menu`/`workflow-menu-registry` 123/123、`pnpm lint` exit 0；QA round14 其余 probe 与 97 例 wrapper 矩阵输出逐字不变。未在本机运行整包或整仓测试。

同轮合入 `origin/main`（merge commit `aae73a0ab`，含 FLY-2775 #1295），无文本冲突；唯一重叠文件 `.github/workflows/ci.yml` 自动合并。碰撞面在合并头上复验：`flywheel-config...` 重建后 `runs-route.dag-entry`/`workflow-menu`/`workflow-menu-registry` 125/125，`ci-shell-suite-enumeration`、`ci-matrix-coverage`、`ci-structure` 三个 CI 契约 PASS，owning Node 文件 131/131，`pnpm lint` exit 0。

Codex 复审 round 1（head `ff143bebd`）确认两个 MEDIUM 与 hook 诊断 LOW 已修，但 HIGH 仍有同根因缺口：`tail.indexOf("exec")` 与整段 `findIndex` 把选项值当子命令，`pnpm --filter exec run test`、`pnpm run --filter exec test` 仍判 not_test、verdict PASS，反向又把 `npm install test`、`pnpm run exec test` 误判整包。提交 `3155b7c0c` 先以 10 条 RED 复现，再把 `exec`/`dlx`、`run`/`run-script` 与 test 别名一律锚定到 `consumeOptions` 返回的命令位；认不出的全局选项使命令位未知，其后出现 test 形或委派词即 fail-closed 为 unknown；`pnpm test:unit` 这类脚本简写按 run 语义判，`run vitest` 按 Vitest 分析；选项元数按包管理器区分（pnpm `-w` 不带值、npm `-w` 带值、yarn/bun `-w` 视为未知）。自查差分以 945d95455 版判定器对 1344 个 manager 组合比对，变弱项只有原误报（`install test`、`run exec test`、`why vitest`）、未知选项转 fail-closed，以及 `pnpm vitest run <文件>` 由巧合的 unknown 转为正确的 allowed。owning Node 文件 145/145、deploy 32/32、`pnpm lint` exit 0；QA round14 probe 电池与 97 例 wrapper 矩阵输出逐字不变，仅 R2 红臂转绿。

Codex 复审 round 2（head `a9cc4fe95`）确认 round 1 的 fail-open 已修（470 组对抗用例无新逃逸），但仍有 MEDIUM 误报：recursive / 整包脚本检查仍扫原始参数（`pnpm -r --filter test run lint` 被判整包），Vitest 定位仍把任一先出现的 `exec` 当委派（`pnpm --filter exec why vitest` 被判 FAIL）。提交 `81dcf114a` 先在命令位解析出实际运行的脚本，再对它做 recursive / 整包判断；只有命令词本身可作 Vitest 简写。锚定命令位也让旧的全段扫描碰巧兜住的转发型子命令漏成 not_test（本轮自查发现，旧头判 not_test 为红臂）：`pnpm recursive/multi/m` 与 `yarn workspace <名>`、`yarn workspaces run` 现按被转发的命令重新分析，`npm x`/`bun x` 作 exec 别名委派；`npm explore`、`yarn workspaces foreach`、跨包管理器误用的转发词、pnpm/yarn/bun 上的 `rum`/`urn` 以及 npm 命令位上像测试的未知命令，后跟 test 形词即 fail-closed。2080 组合差分中，变为 not_test/allowed 的只剩原误报、选项值不再被误认、pnpm `-w` 不带值的真实语义，以及 npm 会直接报「Unknown command」的写法。owning Node 文件 145/145、deploy 32/32、`pnpm lint` exit 0；QA round14 电池与 97 例矩阵仍只有 R2 红臂转绿。

Codex 复审 round 3（head `6603b6acd`）确认 round 2 两个反例已修，另提两个 HIGH、一个 MEDIUM：委派时丢失扇出状态（`pnpm -r exec vitest run <文件>` 判 allowed、verdict PASS）；转发兜底漏了 `it`/`cit` 等 install-test 别名（`npm explore pkg -- npm it` 判 PASS）；`npm run` 不带脚本名只列出脚本，却被判整包。提交 `1dae571b0` 先以红臂复现，再从全局选项段与 run 选项段收集扇出选项（`-r`/`--recursive`/`--workspaces`/`-ws` 与 pnpm 多包 filter：通配、`...` 依赖选择、`!` 排除），凡扇出下被判 allowed 的测试执行（exec/dlx/x 委派、`run vitest`、vitest 简写）一律升级为 `recursive_test_script`，已判 forbidden 的保留原理由；`npm explore` 改为按被转发命令精确分析，通用转发兜底把 install-test 别名算作测试；`run` 不带脚本名判 not_test。与上一头 1404 组合差分中，转为 not_test/allowed 的只有「run 不带脚本名」一类。owning Node 文件 145/145、deploy 32/32、`pnpm lint` exit 0；QA round14 电池与 97 例矩阵仍只有 R2 红臂转绿。

QA@2（head `fd0cd6e50`）的 G1：cell D（slot 4，FLY-2870）首个 Codex writer `9b9ddffa` 34 秒撞上游 `Selected model is at capacity`，Flywheel 在同一 general attempt 1 内换体为 `7e84c900` 并改写 `workflow_run_node.execution_id`；standalone 观察器按 start receipt 的 executionId 关联，空等满 180 分钟窗口，超时兜底也用同一个 key。提交 `3b5dae688` 让 standalone 与 generalized 一样按 run + 节点（`general`）取行：先按 workflowRunId 定位 run，或经从不改写的 `workflow_execution_binding` 由任一曾被该节点接纳的 execution 反查 run；冻结证据时收集该节点接纳过的每一个 writer，被替换 writer 的会话与命令一并进 case（manifest 记 `supersededExecutionIds`），其会话只在自身 terminal 后才算闭合。QA 冻结红臂 `probe-standalone-retry-rebind` 修前 exit 1（`standalone_retry_unobservable=1`）、修后 exit 0；三个负对照变异体（退回按 execution 查、丢弃被替换 writer、取消 terminal 闭合）各自使对应用例或红臂转红。冻结 cell D 离线回放：两个 writer 的 rollout 都被收集（4 + 125 条命令），按 fixture 字面量改动集回放判定 PASS、0 finding——这只是观察器可见性的旁证，不是 529 判决。owning Node 文件 140/140、deploy 32/32、`pnpm lint` exit 0（27 条既有 warning）；未在本机运行整包或整仓测试，未合 main（PR MERGEABLE/CLEAN，main 前进的 4 个提交与本次文件无重叠）。

G1 scoped 复审 round 1（head `157a95313`）阻断一个 MEDIUM：按 run 取到的是最新 general attempt，但 `standaloneCompletionTarget` 仍把 attempt 写死为 1，attempt 2+ 的 case 目录与 `manifest.activation.attempt` 会冒认 attempt 1。提交 `98e51d6b8` 先以 attempt 2 回归用例得到 RED（实际 1、期望 2），再保留查询到的 attempt（行里没有时才回落为 1）；该用例同时钉住 case 目录名为 `engineer-attempt-2-*`、被替换 writer 按接纳顺序为两者。owning Node 文件 140/140、deploy 32/32、冻结红臂 exit 0。

QA@1（run fb77977c，head `b737a5ec4`）在 slot 2 真跑 cell D（FLY-2878，Codex engineer）：runner 行为合规（`git grep -lF` 发现、逐文件 `pnpm dlx vitest@3.1.4 run`、`vitest related`、零整包），但验收门三处假设与真实房间不符，判 INCONCLUSIVE。提交 `a81639803` 逐项收口。G2：房间按 workflow 节点给 Codex agent home 建目录（`agents/<project>/general`），收集器却按证据角色 `engineer` 找，冻结出空转录；现按行的 workflow 节点找 home，节点名取自 workflow 表（`workflow_run_node` / `workflow_execution_binding`）而非会话行。G3：fixture oracle 在 runner worktree 里跑 `pnpm exec vitest`，runner 装不上依赖时测到 0；现用候选检出里 lockfile 锁定的 Vitest，在排除 `node_modules` 的 fixture 包隔离副本里逐文件跑，并在结果里记录 oracle 版本；无 git 身份的树照样测，但不能判 passed 或匹配改动集。G4：改动集白名单漏了房间 DOC-FLOW 目录；现从房间 `.flywheel/config.yaml` 的 `doc_flow.default_department` 与 `flywheel-projects.json` 各 Lead 的部门（显式 department，否则首个 match label，同 teamlead `resolveLeadDepartment`）读出 `<department>/doc/` 根。先红后绿：新增/改写的 5 条用例修前全红（G4 白名单与配置读取、G3 测到 0、节点名取自 workflow 表、G2 空转录）；QA 红臂 `probe-g2-codex-home-keyed-by-role` 旧头 exit 1 → 新头 0（127 条命令、9 个政策标记），`probe-g3g4-fixture-verifier` 的 G3 半边旧头「未导出/测到 0」→ 新头 testsRun 34，G4 半边按设计需传入房间 doc 根，另以复刻 cell D 房间配置的红臂 `g4_room_doc_flow_false_inconclusive` 旧头 1 → 新头 0。冻结 cell D 端到端离线回放（收集 + oracle + 判定）PASS、0 finding——旁证，不是 529 判决。owning Node 文件 142/142、deploy 32/32、G1 与 R2 红臂仍为 0、`pnpm lint` exit 0；未在本机跑整包或整仓测试；未合 main（PR MERGEABLE，main 前进的 4 个提交与本次文件无重叠）。

G2–G4 scoped 复审 round 1（head `4f776efb1`）提出三个 MEDIUM，提交 `2daa29fa1` 收口：oracle 的 Vitest 原取自启动驱动的任意检出，现从房间 `flywheelRepo` 解析，且该检出 HEAD 不是候选头即拒绝；`cpSync` 保留符号链接会让 oracle 测到结果提交之外的字节，现以 lstat 逐项拷贝，拒绝符号链接与特殊文件、跳过 `node_modules`，verify 也在同一隔离副本里跑；DOC-FLOW 根原在跑完后从 runner 可写的房间文件重读，现于 `/api/runs/start` 之前连同两个源文件摘要冻结进 `run.json`，收证（含 collect）复用冻结值，源文件一旦漂移即 fail-closed。三组用例修前以旧语义桩跑全红（摘要缺失、未校验候选头、符号链接未拒），修后 owning Node 文件 143/143；G1/G2/房间 G4 红臂与 R2 两个 probe 仍为 0，冻结 cell D 离线回放仍 PASS、0 finding，deploy 32/32，`pnpm lint` exit 0。

复审 round 2（head `fe9f2e4bb`）再提两个 MEDIUM，提交 `6b1cc5da0` 收口：摘要合法但 `roots` 被改（如 `[""]` 放行一切）的收据现被拒——冻结根须与已验证源字节重算出的根完全相等，返回重算值；候选 HEAD 不约束被忽略的安装，oracle 身份现要求 pnpm 安装记录 `node_modules/.pnpm/lock.yaml` 对 `packages/teamlead` 的 Vitest 解析与候选 lockfile 完全一致（含 patch 与 peer），实际加载版本等于该版本，并对包树取摘要；身份在 `/api/runs/start` 前冻结进 `run.json`，冻结证据时复核，任何漂移 fail-closed（pnpm 会截短目录名里的 patch hash，故以安装记录而非目录名为准）。两组用例修前以旧语义桩跑红，修后 owning Node 文件 144/144；真实检出上身份校验通过（3.2.4，含 patch）；各红臂、R2 probe、冻结 cell D 回放（PASS、0 finding）、deploy 32/32、`pnpm lint` 均保持。

QA@2（run fb77977c，head `acadd34fc`）在 slot 3 真跑 cell D 判 PASS（codex-tmux，154 条命令 0 forbidden 0 unknown），G2/G3/G4 红→绿；唯一阻塞 G5：精确头 full CI 36122548883 的 Script Tests 在本 PR 自己的用例上确定性红（「fixture oracle measures with its own Vitest」0 !== 35）。原因是 GitHub Actions 设 `CI=true`，Vitest 因此给汇总行上色，纯文本计数正则匹配不到，oracle 在七个文件全部 exit 0 的情况下测到 0。提交 `34054f119` 只改 oracle 调 Vitest 的环境：去掉继承的 `FORCE_COLOR` 并设 `NO_COLOR=1`（tinyrainbow 中 `NO_COLOR` 优先于 `CI` 与 `FORCE_COLOR`）；新增用例在 `CI=true`、`FORCE_COLOR=1` 下跑 oracle，修前 0 !== 35、修后 35。QA 红臂 `probe-g5-oracle-color-ci` 修前 exit 1（`oracle_ci_color_false_zero=1`）、修后 exit 0（testsRun 34）。owning Node 文件在 `CI=true` 下 145/145，两个改动文件 Biome 无诊断，`pnpm lint` exit 0；`runner-test-discipline-deploy.test.sh` 只导入未改动的 `buildRunStartRequest`/`postRunStart`，按依赖排除。未改 runner 行为判定，未在本机跑整包或整仓测试。

land 冲突返工（Lead，founder 要合时 PR 头 `ec971851b` 与 main 冲突）：merge commit `3fae6082c` 合入 `origin/main` `9034bddc3`，未 rebase、未 force-push，按 Lead 的逐处裁定解四处冲突。`TmuxAdapter.ts` 的 `node:path` import 取并集（main 用 `isAbsolute`/`resolve`），保留本单 `RUNNER_TEST_POLICY_HOOK` 要用的 `node:url` `fileURLToPath`；`test-deploy.sh` preflight 构建块与报错文案、`test-deploy-fly1389.test.sh` 的 MCP dist fixture 全部取 main（FLY-2867：inbox-mcp 与 terminal-mcp 都构建，再由 `qa_room_claude_lead_mcp_missing` 核对）。本单的提示词与 529 验收门逻辑未改动。合并头上的点名验证：`flywheel-claude-runner...` 构建，TmuxAdapter 187/187、`test-deploy-fly1389.test.sh` 29/29、`runner-test-discipline-deploy.test.sh` 32/32，`test-deploy-generalized.test.sh`（短 TMPDIR）PASS，`test-deploy-qa-room.test.sh` 22/22。fly1389 在前三次运行里各挂 1、9、5 例，每次挂的用例不同；原因是另一个 runner（FLY-2882 worktree）同时在跑同一套件，写死的槽 30–35 在 `/tmp` 下互相删目录。确认本机没有其他实例后单独重跑，29/29 通过。未在本机跑整包或整仓测试。

因此真实 529 A/B/C/D 仍是 QA 硬门，不存在新 head 的 PASS 声明。当前返工 head 仍需新的有效代码审查；通过后才交回 QA 重新 prepare、跑全矩阵，并由 QA 冻结精确头请求 full CI。实现节点没有 dispatch QA、approve、merge、ship、deploy、重启服务或触碰生产配置。本文件是 PR 的 literal-last commit。
