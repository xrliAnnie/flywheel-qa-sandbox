# FLY-2349 — poison view 下的 CommDB 安全重建

**Status**: ⏳ Implementation complete; exact-head code review pending
**PR**: pending creation
**Date**: 2026-09-04

## 交付

- `runner_shutdown_controls` 的 FLY-2268 database-open 重建现在只在原有
  `BEGIN IMMEDIATE` 事务内临时启用 SQLite `legacy_alter_table`，并用 `finally`
  在返回连接前恢复为关闭状态。
- 原有 create/copy/drop/rename、receipt 校验与消费、锁顺序、行数守恒和失败语义保持不变；
  本单不 drop、改写或重建 FLY-1572 的 `messages` / `lead_inbox` poison views。
- 新 fixture 从真实 legacy 主键形状出发，并在 WAL checkpoint 前恢复生产同形的两个 poison
  views，避免新代码创建的 schema 让 gate 提前返回。
- 回归覆盖 `CommDB` constructor、共享 `openCommDbWritable` 和 path-based `MailboxQueue`，证明
  shutdown 数据与复合主键正确、view SQL 逐字节不变、receipt 只消费一次、pragma 已 reset，且
  连续三次可写 reopen 不再失败。

## TDD 与验证

- RED：精确 regression 首先稳定复现 `database-open` 阶段的
  `error in view messages: no such table: main.fly1572_poison_messages_use_mailbox`。
- GREEN：最小 pragma scope 修复后，同一测试转绿；扩展后的
  `db.fly2268.test.ts` 最终为 22/22 tests pass（单文件、forks=1）。
- `pnpm lint` 退出 0，仅有 14 条仓库既有 warning/info；`pnpm -r build` 的 21 个 workspace
  全部成功；变更文件 Biome 检查和 `git diff --check` 均通过。
- 按 Lead 对本单的安全裁定，没有在 macOS 本机运行会触发真实 Terminal.app 的
  `pnpm test:packages:run`；全仓测试结论由 PR 的 exact-head CI 提供。本单没有新增
  `scripts/__tests__/*.test.sh`。

## 边界与交接

本实现没有更改获批 plan、目标 schema、receipt 格式、其他 CommDB migration 或
`CLAUDE.md`，也没有触碰生产数据库、启动生产 Bridge、dispatch QA、请求 ship approval、
merge 或 deploy。包含本 milestone 的字面 branch head 是 request-driven cross-family code
review 的唯一目标；blocking finding 必须修复并以新的最终 milestone commit 重新送审。
