# FLY-2570 Dynamic design model split

Design admissions for `code.eng_design` support a hot-read human-set Codex percentage from 0 to 100. The operator CLI displays and updates the effective policy without process restart. Deterministic assignment, content-derived versions, immutable admission receipts and frozen replay retain historical attribution. Explicit mismatched overrides and malformed configuration fail closed. Fable metadata sync shares the same recoverable writer lock. Version-filtered comparisons retain each metric's own sample count.

Verification: root lint/build, full focused admission/menu/dispatch/HTTP regression (127 passed, 2 existing skips), operator and SQL shell suites, and retention gate passed. Host gate 4 retained zero-assertion runner/teamlead receipts with onTaskUpdate RPC failures disclosed. Lead instructed interruption of the internal retry and immediate PR/review; five packages remained unreached. This is not aggregate green. Exact-head CI is the aggregate of record; effective final review and CI are pending.

Evidence and operator procedure: `engineering/doc/FLY-2570-dynamic-design-split/`. Production ratio has not been changed. QA, shipping and activation remain subsequent workflow responsibilities.

CI integration repair: registered the existing operator shell step in the strict CI structure inventory after exact-head Quick Gate reproduced its missing-entry failure. Structure, enumeration, startup, kill-path inventory, lint and recursive build now pass locally. Fresh review and exact-head CI remain required.
