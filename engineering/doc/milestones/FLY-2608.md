# FLY-2608 — Raya Discord thread 提问收件与原线程回复

**Status**: ⏳ QA rework complete; pending exact-head review, CI and QA retest
**PR**: [#1221](https://github.com/xrliAnnie/flywheel/pull/1221)
**Date**: 2026-09-15

在既有 Discord chat ingress 与 Raya mailbox 路径上增加受 rollout 标记约束的 ingest-only thread 发现：按登记的 `lead_id` 与父频道解析 owner，保留源 message id、标准 `chat:raya:<messageId>` delivery id 和原线程 `replyChannelId`，并对未登记、已归档、非 Raya owner、重复输入及冲突 owner 明确拒绝。固定 lower bound、逐线程 dry-run 与一次性 activation receipt 默认关闭，未新建收件器，也未自动回放历史。

定向验证 176/176、teamlead typecheck、recursive build 与 lint 通过。package aggregate 共 23,678 passed / 32 skipped，另有 3 个与本改动无关的超时断言；config 与 teamlead 目标文件串行复跑全绿，claude-runner 目标断言提高运行时超时后 1/1 通过，因此不把 aggregate 声称为全绿。合入最新 `origin/main` 时保留了 `attentionSinceMs` 与 ingest-only 的各自起始游标；同步后评审发现 founder_ask 新路径缺原线程 reply route，已用一行路由修正与一条回归用例闭环。

QA attempt 1 在隔离 Discord/Bridge 房验证了 source message → 标准 mailbox → 同线程回复、主频道回归、隔离和零重复，但发现 Discord 已归档线程仍会被本地登记行扫描，且未登记、非 owner、已归档和重新发现路径缺明确日志。返工仅在 ingest-only 投递前复用 Discord channel metadata 做 fail-closed archive 检查，并为三类 skip 与本地归档但 Discord active 的 rediscovery 补结构化原因；三个相关单文件 78/78、teamlead typecheck 与 Biome 通过。新头 exact-head review、CI 与 QA retest 待执行。未创建生产 rollout 标记、未重启或部署、未恢复两条历史提问，也不声称生产 founder 真验收。
