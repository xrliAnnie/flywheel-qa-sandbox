# FLY-2027 — generalized land 的 generic 收体、park 结算与归档诊断补成老路径对等

**Status**: ✅ Merged (PR #941)
**PR**: #941
**Date**: 2026-08-25

账本取证推翻原「缺收尾链」前提：FLY-2000/2015 的 implement producer 均已自动走完 merge→收体→Linear Done，未归档来自既有 `founder_reopened` 保护；真实缺口是 workflow-bound main actor 没有 phase 同级的 graceful shutdown/husk force/post-ship 1.25/Heartbeat 纵深、`ship_parked` 投影硬编码 implement、`runner_ship_gate_wait` 无 terminal settlement consumer，以及 closure cause 被 plugin 压扁。现以 `workflow_node_id` 纳管全部 workflow actor，generic 收编逐 actor 做 exact run/activation fencing；park 改按 `keepalive_park && creates_pr` capability，terminal caller 可结算两类 park、active rework replacement 只清 rework park，clear receipt 继承 open reason且 skip 事件 replay-safe；typed `ClosureReport` cause 贯穿 post-ship，`partial/needs_operator` 仍非阻塞，`blocked/conflict` 才阻断。founder 在 thread 发过话时继续 fail-closed 保持开放，文案明确不会自动重试、由 Lead 手动归档。generic dispatcher 集成覆盖 producer park→founder gate land receipt→thread archived/Linear Done→run completed→actor terminal→exact park clear；旧 phase/普通 main/🆒 路径保持。D3 的「park 后提示立即退出」与 D5 `isEngineParked` production 未注入已报 Lead 另单，不跨 scope。验证：lint 0 error/8 既有 warning、22 workspace build；core 非 GUI 219/219、config 661/661、claude-runner 864 pass/2 skip、teamlead 9521 pass/6 skip，所有断言全绿。Canonical aggregate 仍因本 sandbox 无 Terminal.app HiServices GUI，以及 Vitest 3.2.4 长同步 suite 收尾的 `onTaskUpdate` RPC timeout 非零；4 workers + 隔离 npm cache 已把此前 11 个并发 timeout/mock/`npm pack` 失败全部收敛，只剩测试 runner 控制通道错误。

---

**账目来源(FLY-2045 cutover)。** 这条里程碑原本写在 `CLAUDE.md` 的那张表里,由 #941
在 2026-08-25 合入(merge commit `888bdb13d`)。FLY-2045 把账本搬出 `CLAUDE.md` 时,
它已经在表内,所以那一行**逐字节冻结**在 `ARCHIVE-pre-FLY-2045.md`(受 sha256 + 字节数双 pin);
本文件是同一条账目在新机制下的**活入口**,正文取自那一行,一字未改。
表内那行的 status cell 原文是:`⏳ exact-head code review；PR + 独立 QA` —— 写于 PR 合入之前,保留原样以便对照;
上面的 `**Status**` 是合入后的事实。

**这是一次性 cutover 例外,不是新常态。** 按 `README.md` 的
[单写者合同](README.md#FLY-2045-SINGLE-WRITER),一个 issue 的里程碑文件只由它自己的 ship PR 写。
FLY-2027 的 ship PR 在本机制存在之前就已合入,不可能自己建这个文件,所以由 FLY-2045 的 cutover PR 代建。
往后 FLY-2027 的任何状态更新仍只由 FLY-2027 自己的后续 PR 串行做。
