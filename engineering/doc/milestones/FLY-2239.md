# FLY-2239 — Resident Codex Lead pane 告警名册化

**Status**: ⏳ Pending review
**Date**: 2026-09-05

`tui_window_lost` guard 不再写死 InfraBot 与 Raya 身份,改为复用 resident Codex Lead 名册并按 `projectName/leadId/leadKey` 三元精确匹配。非 opt-in、身份错配、缺名册均静默不装载;成功装载写通用 ARMED 正证据日志,告警标题统一为 `Codex Lead <project>/<lead> TUI window not visible`。Raya 安装 preflight 同步验证真实 launcher env + opt-in 会 armed,空名册会 disabled。

本轮按 founder/Lead scope-cut 只交付这一处代码改动、测试和 PR。D0–D6 真机强停演练已设计但未执行,待 founder 真正需要时再跑;没有执行 kill、kickstart、bootout、plist、Bridge/runner 重启或生产 cutover,Claw 零触碰。InfraBot 纳入与 pane 告警名册对齐决策及原话链接记录在 `engineering/doc/FLY-2239-codex-lead-cutover/cutover-status.md`。

TDD、两轮变异验证、最终 touched-file 单 fork 85/85、Raya preflight 8/8、shell 去重、lint 和全仓 build 均通过。安全分解的 workspace 回归中唯一剩余本地例外是与 `origin/main` 字节一致的 FLY-2118 real-tmux 夹具:tmux 3.7c 在进入被测逻辑前拒绝含 TAB 的 window name;PR exact-head GitHub CI 为最终硬门。
