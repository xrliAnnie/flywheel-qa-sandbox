# FLY-2638 — Raya Discord 附件内容可达

**Status**: ⏳ Rework pending exact-head code review and CI
**PR**: #1270
**Date**: 2026-09-18

标准 Discord → Bridge/mailbox → Raya Lead 收件现在保留不可伪造的附件 ID，并通过既有 v1 `lead_actions` MCP 提供 `discord_read_attachment`。读取由不可变 mailbox receipt、canonical Lead identity、当前 carrier claim、原 source channel 授权和下载前后复核共同约束；只支持 PNG/JPEG/WebP ≤5 MiB 与 UTF-8 TXT ≤32 KiB。成功返回真实 text 或原生 MCP image content block；缺生产者 identity、类型/尺寸不支持、无权、过期、下载或内容校验失败均显式 unavailable，不再让 metadata 冒充内容。实现不保存 CDN URL、base64、正文或附件文件，不回填历史两条私人消息，也没有新 bot、并行 transport、Raya persona/repo 改动、部署或重启。

Code review R1 在旧冻结头识别出生产 Raya `role=cos` 被错误排除，并给出逐 chunk 复核、Express `text/plain` charset 与空白 projects-file 三项 bounded advisory。`428a50857` 在批准方案内逐项修正并加入回归测试；随后 founder 要求附件读取不能成为 Raya 或 full-access 的特殊能力。Lead rework 要求把这一只读能力开放给所有 v1 Codex carrier Lead，同时保持真正的安全边界不变。

本轮 `2e4ec8c9e` 从工具上下文与 Bridge scope 的初始/重验谓词中移除 role/profile 限制；companion 与 write-capable Codex Lead 的正例现在通过。canonical Codex backend、Bridge mode、v1 transport、exact identity digest、bearer、carrier claim、owned live receipt、source-channel authorization、attachment identity、类型/大小与内容校验均保留；foreign Lead、伪造 identity、错误 bearer、失效 carrier 和 v2 bundle 仍拒绝。没有按 Lead 名字或 Raya 名字增加分支。

同步 `origin/main@487799b801f3147748b1c6188cadd0c035226c1e` 后，原实现定向验证共 19 files / 526 assertions：Flywheel-comm 93、Bridge receipt/download 105、producer/startup 77、native MCP 68、Codex runtime 183。本轮 rework 定向验证为 4 files / 48 assertions，`pnpm --filter flywheel-teamlead build` exit 0，四个改动文件 Biome 与 `git diff --check` exit 0。仓库级 `pnpm lint` 仍因本 diff 外的既有 diagnostics exit 1，本轮不越界修改。Lead 明确要求不跑全量测试；真实附件消费仍由 QA 验收。Lead 同时通知 GitHub Actions 额度耗尽，推头后的 0-step failure 不得自行 rerun，应等待 Lead 通知额度恢复。

本文件是 PR 的 literal-last commit；之后只允许对该精确头做 effective code review、push、PR 和 CI。若评审要求代码变更，必须修复、重验并重新生成新的 literal-last milestone head。
