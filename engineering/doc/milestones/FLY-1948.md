# FLY-1948 — slot Lead Discord 活连接与整圈证据

**Status**: ⏳ Pending ship
**PR**: [#1200](https://github.com/xrliAnnie/flywheel/pull/1200)
**Date**: 2026-09-15

529 slot 的主/extra Claude Lead 必须在 lease 之后通过代际一致的 Discord 通道活连接检查，才发布 ready。每 Lead 写独立坐标与脱敏失败快照；Codex 明确不适用。新增独立巡检和 founder/受限 slot-bot challenge-response 工具，以只读 mailbox 和 Discord 回复记录五段绝对 deadline 证据。NOT_SEEN 日志补充 pane 分类信息，CI 和路书覆盖新路径。

实现代码 `a996d94d3` 已获有效代码评审 APPROVED，exact-head CI 15/15 SUCCESS；完整 hermetic 部署夹具 24/24 通过。七条非阻塞建议见同 issue 文档目录的 follow-ups.md。Lead 指定以 exact-head CI 为交接依据；本地主机旧 package gate 的真实失败保留，不声称 aggregate green。本文档提交后的最终 HEAD 仍须通过 CI 才执行 needs_review。

真机 founder 整圈、重启恢复与负对照由 QA 节点验收；本条不声明验收、合并或部署完成。

## 冲突返工（implement attempt 2，2026-09-15）

同步主干 `8e28a264c`，合并提交 `3046573ed`。保留 FLY-1663 测试迁往第 5 分片，同时保留第 1 分片的 FLY-1948 测试及精确命令清单；功能代码未因冲突而改写。CI 结构守卫与全部五个 channel evidence 测试入口通过（roundtrip 28/28，env 33/33）。本次最终 HEAD 必须重新获得有效代码评审与完整 CI；旧 HEAD 的通过记录不能代替。真实整圈和 QA 重测仍待后续节点。

## Mirror 探针返工（implement attempt 3，2026-09-15）

QA 在已审查且 CI 通过的 `fe75cff1a` 发现 mirror 探针未明确点名目标 Lead，触发共享频道静默规则而超时。`dde12a0fd` 为自动发送和人工提示的共用正文加上目标 bot mention；保留 mirror 支持和原有发送/验收限制。新增两条回归先红后绿，完整 roundtrip 30/30 通过，定向 Biome、全仓 lint/build 通过。新最终 HEAD 仍须重新获得有效代码审查、exact-head CI 与 QA 复测；本记录不声明真实 founder 整圈或发布完成。
