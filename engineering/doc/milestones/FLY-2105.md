# FLY-2105 — Flag 治理关门

**Status**: ⏳ Pending ship
**PR**: #1000
**Date**: 2026-08-30

关闭产品 flag 的旧 authoring 路径：所有 registry 条目都由 SQLite store 管理，CI 拒绝生产 `process.env.FLYWHEEL_*` flag 直读和 tracked `config.yaml` flag key，legacy unmanaged/exemption baseline 归零，并逐条核对 registry scope、default、polarity 与 codec。真实退役扫描已通过生产手动入口执行并投递到 `#flywheel-notification`，如实报告 4 个候选和 6 个 no-clock 项。
