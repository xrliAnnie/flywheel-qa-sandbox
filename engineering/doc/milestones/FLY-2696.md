# FLY-2696 — Raya persona 投影

**Status**: ⏳ Pending ship
**PR**: #1256
**Date**: 2026-09-18

新增默认 dormant 的 Raya-only 显式 persona 投影合同、exact-commit 取源与启动屏障；pre-M0 仅可回退到已验证 A0，post-M0 仅可回退到 B2 前冻结且获授权的 migration-compatible digest，否则拒绝打开 transport。当前 16 个非 Raya Lead 均以完整 projectRepo/identity 条件做零写阴性对照。FLY-2549 的 stale-workspace 负控已覆盖新旧 config 缺失导出名，同时保留 source-mapped preflight 的完整验证；capability 文案使用显式 bundle version，避免重新暴露已灭绝的 Lead v1 carrier 术语；projector 与 startup gate 的同步子进程/标记边界已登记进 FLY-2331 冻结 census。

QA1 返工补齐生产形状：Raya workspace 不在 Git worktree 时按 untracked 放行；在 worktree 内且 persona 被跟踪时仍拒绝；损坏、权限或超时等 Git 异常在创建 `.lead` 前 fail closed。两步本地 Git 探测均有 10 秒上限。测试还固定了 post-M0 取源失败且磁盘仅有正确 A0 时必须拒绝且 A0 字节不变。激活 owner 不得把 Raya commit `90e433e87a68287ed59ba64f2584e3a6bc0da151` 的 persona digest `ca1240f12fbbe868ec3fa117f4882ecfcb78d20a51a0ffa7ad9f37babbcd78f5` 当作 pre-M0 A0：当前生产 A0 digest 为 `4e982448296ac31215dde62ac2a6343f642753eeb39ba96e16767ed921e63b1f`。

QA 返工后的代码评审又发现 dormant Raya 会在无关 `projects.json` 变更后被旧的整表 digest 锁存挡住重建。现已把该锁存限定为 persona 已显式 opt-in 或合同无效的路径：未 opt-in 且无 enrollment marker 时继续原启动路径；一旦 opt-in，registry digest 漂移仍 fail closed。回归测试同时覆盖这两个方向。
