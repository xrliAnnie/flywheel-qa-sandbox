# FLY-2048 — cmux 僵尸视图自动收敛

**Status**: ⏳ Pending ship
**PR**: #959
**Date**: 2026-08-25

cmux watcher 现会在有限时间内把 Flywheel-managed runner 展示集收敛到在世 tmux 会话集：
历史五字段 `prepared` workspace 经 exact receipt guard 进入统一 orphan 清理，历史 attach helper
经两轮独立 observation 铸入既有 bounded TERM/KILL 状态机；新的
`--converge-runners --handover` 为人工全量清扫提供 fail-loud、安全交棒路径，并保留 Lead、手工
及语音 workspace 的所有权边界。真机历史债务清扫后 runner 对账为 `extra=0, missing=0`。
