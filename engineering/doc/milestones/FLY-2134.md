# FLY-2134 — 产出物新鲜度与监控者闭环

**Status**: ⏳ Implementation complete; post-fix exact-head review and CI pending
**PR**: [#1132](https://github.com/xrliAnnie/flywheel/pull/1132)
**Date**: 2026-09-08

## 交付

- 新增五行期望产出物登记表与每小时进程外看者，只按文件 mtime、SQLite 指定列最大日期或 Git 远端 head 的事实判断 `fresh | stale | missing | undetermined`，不读取被观察任务的日志、退出码或 launchd 运行状态。
- 看者为 `incident` 与 `unobservable` 分设 episode 账，持久化 append-only `checks.tsv` 和整轮 commit-marker `last-run.json`；登记表、状态与 sink 任一形状非法均 fail closed。
- 新增 repo-owned launchd `copy` 单元、manifest authority 与审计退役路径；没有 enable 现有 disabled 单元，也没有修复 token usage、chezmoi 或 runner-memory 远端。
- Bridge schema 2 增加可选 W-4 producer、完整 receipt reader 与 Config truth validator；probe 接受未来 schema 3，并把非法、过期或 degraded W-4 折入既有 degraded episode。
- required CI 串行登记登记表、看者主体和 Linux-safe W-4 consumer 三套 shell suite；运维文档记录加行、suspend、退役和 Phase A→B 顺序。

## 本地验证

- `pnpm lint` exit 0（14 条既有 warning）；`pnpm -r build` 22/22 workspace targets 通过。
- 三套新增 shell suite、现有 Bridge producer 交叉、launchd manifest、retire 与 CI structure suite 均通过；TeamLead liveness 9 项与 Config truth 38 项通过。
- `bash -n`、新 plist 的 `plutil -lint` 与 `git diff --check` 通过。
- PR 首轮 exact-head CI 在 `Script Tests 2/5 — fleet/setup/packaging A` 精确发现新 plist 的 native-first mixed-prefix PATH 尚未进入 fail-closed registry；同一 `check-global-path-hygiene` 套件本地复现为 20 passed / 1 failed。最小修复只登记该 plist，复跑为 21 passed / 0 failed，artifact freshness、launchd manifest 与 CI structure 回归组继续通过。
- `pnpm test:packages:run` 原样执行三次均未形成 aggregate green：失败分别落在三个互不重复、相对 `origin/main` 零 diff 的短时限测试集合；对应断言或文件随后隔离运行全部通过。这里只证明失败呈负载敏感，不把 focused PASS 冒充 full gate green，最终 aggregate 判据交给 PR exact-head CI。

## 审查

- 本地 `codex:rescue` companion 在读取仓库前被 macOS nested sandbox status 71 拒绝，未产生 review verdict，也没有使用 raw `codex exec`。
- Request-driven cross-family code review 在 milestone 头 `53f2220d96c0` 给出 `reviewVerdict=APPROVED`、`reviewerVerdict=APPROVED`，无 blocking finding；path registry 修复后仍需对新的最终 exact HEAD 发起新审查。
- 初审记录六项 non-blocking advisory：`_af_post` 应 pin `allowed_mentions` 且补真实 body 测试；`git_remote_head` 的 main-only 约束应文档化；另有 hard-kill 临时目录清理、`..` 路径约束和超长消息归因三项 LOW。已逐项报告 Lead，由 Lead 决定 follow-up。

## 边界

- 本 PR 只交付 Phase A：Bridge 仍产出 schema 2，W-4 在该阶段可选。只有部署后 W-4 连续 24 小时 `fresh/ok`、新 consumer 正负对照与 probe 无 degraded 三项证据齐全，才允许同一 issue 的 Phase B PR 把 producer 升为 schema 3 并强制 W-4。
- 本节点没有部署、生产 launchd/Bridge/Discord mutation、QA dispatch、merge、ship approval 或 Phase B 实现。三方环仍在同一机器和 launchd 域，整机级副本与外部看者不在本 PR。
- 首轮预计出现 token usage、chezmoi remote 与 runner-memory remote 三条阳性；本单让它们出声，不修复其根因。
