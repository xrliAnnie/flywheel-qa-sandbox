# FLY-2259 — Raya 受管常驻切换准备

**Status**: ✅ Implementation complete; activation pending post-merge window
**PR**: #1050 (`flywheel-FLY-2259`)
**Date**: 2026-09-03

本单完成 Raya 迁入受管 Codex Lead 体制所需的代码与窗口物料，但没有执行生产激活。三个生产 Codex Lead launcher 现在都通过 `derive_codex_lead_home <key>` 使用同一条 `~/.codex-<key>` 规则；Mufasa 与 InfraBot 的有效路径保持不变，Raya 的独立路径固定为 `~/.codex-raya`。resident recovery helper 继续保留固定 wrapper allowlist，只把 wrapper 映射到 home key，再由同一函数推导期望路径；旧的 Raya 共享 home 会在任何恢复 mutation 前被拒绝。

随 PR 交付的受审物料包括 Raya registry row、summary assignments、原子 registry registrar、只改两键且可验证的 `raya.env` editor，以及完整 `activation-runbook.md`。runbook 把 founder 登录、summary 样本 PR、部署 SHA、配置覆盖、残留和双活检查做成硬停止线；生产窗口按 registry/receipt、memory/env、preflight、converge、manifest、plist 出生逐层推进，并提供 R1–R5 逆序回滚。班车 N+1 后才允许做有界 SIGSTOP 自愈、真实 Discord 告警、pane 重建与 voice 对照。

验证证据：

- home rule 9/9、resident recovery 19/19、activation preflight 7/7、Raya/Mufasa/InfraBot launcher 10/17/18、carrier 12/12、converge 15/15、packaged seams 17/17、cutover materials 17/17，全部通过。
- QA gate-6 可重跑验证器 4/4 通过：runbook 裸 AND-list 守卫为 0；memory 目的地非空与断链软链都在 `mv` 前失败且源目录不变；目的地为空时正常完成搬迁。回归测试直接提取并执行 runbook 的 §4.3 片段。
- 新 root shell suites 已显式分类：通用 home suite 进入 Linux CI；含 macOS `plutil` 的 materials suite 进入 manual-only inventory 并在本机通过。
- `pnpm lint` 通过（14 条既有 warning）；`pnpm -r build` 22 个 workspace projects 通过。
- 精确 `pnpm test:packages:run` 在当前 resident 无 GUI 宿主只失败于既有的 `packages/core/test/tmux-viewer.macos.test.ts` 两条 Terminal.app/AppleEvents 用例；该测试及实现 blob 与 `origin/main` 相同，Lead 已接受为环境限制，最终硬门是 PR 精确头 GitHub CI 全绿。
- QA 返工把 runbook 中所有 14 条同族裸 AND-list 安全断言改为显式 fail-loud 谓词，并为静态规则与 memory 目的地残留场景补回归测试；运行时代码与配置物料相对 R2 批准头没有变化。
- request-driven cross-family code review R3 在返工实现 head `39e5bec5ed5371ee8f170ad40d20c5b90c51b42a` 为 **APPROVED**（gate `08b97f3f-5eab-476c-8926-f3b4c4a4a92e`，request `e34e7ced-7581-4358-9fd7-cf85496f1051`）。此后只有 durable progress 与本 milestone 文档，没有实现改动。
- literal-last milestone 会生成一个只含本文件的最终 head；它的最终硬门以 PR #1050 的 exact-head GitHub CI 状态为准，不在本文件冻结一个会过时的运行中结论。

R3 评审指出的 milestone 陈旧项由本次 literal-last 改写解决；其余两条非阻塞 advisory 已转报 Lead：materials suite 仍因单个 macOS `plutil` 用例整体列在 manual-only，后续可让 16/17 用例进入 Linux CI；无 supplementary group 的主机上 alternate-gid probe 会退化。

实现节点没有修改 `~/.flywheel/projects.json`、Raya 仓、`raya.env`、`~/Dev/raya-lead-workspace`、`~/.codex-raya`、launchd、tmux、Discord 或生产进程；没有重启 Bridge/Lead，没有 dispatch QA，没有 merge 或 deploy。激活只能在 PR 合入并由班车部署后，由 Lead 与 founder 按 runbook 选择安静窗口执行，真机证据另走 docs-only PR。
