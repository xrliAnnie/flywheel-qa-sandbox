# FLY-2318 — Blueprint golden 同步

**Status**: Ready for independent QA
**PR**: [#1071](https://github.com/xrliAnnie/flywheel/pull/1071)
**Date**: 2026-09-03

## 交付结果

#1067 的 FLY-2222 inbox-pending 提示词合同已同步到 #1056 新增的两份 FLY-2147
完整提示词 golden。生产 `Blueprint.ts`、测试代码、断言与既有 snapshot 均未改动；本次实现只在
`fly1188-prompt-before-fly2147.txt` 和
`fly2147-prompt-golden-unsupported-backend.txt` 各补入一行已批准的提示词。

新内容不是从当前被测输出反推：FLY-2222 的已批准计划明确要求 Lead-backed Claude/Codex runner
把 pending inbox summary 视为未读 mailbox traffic，并对每个 question id 调用 `check`；对应三条语义
测试通过，flywheel-comm 的 mailbox source suite 也以 162/162 证明 inbox 与 `check` 的合同。
#1067 的提交记录进一步证明它加入了同一行并只更新了当时可见的 snapshot；两份 stale fixture
在其旧基线上尚不存在。

## TDD 与验证

- 修复前，两份目标 Blueprint suite 精确复现 5 失败、34 通过；
- 同步两份 golden 后，同一命令 39/39 通过；
- 把两份 golden 改回旧内容，原五条测试精确重新变红；
- 单独变异第一份 fixture 时精确 2 条失败，单独变异第二份时精确 3 条失败，恢复后再次 39/39；
- `pnpm lint` 通过（仅 14 条既有 warning），`pnpm -r build` 通过；
- `pnpm test:packages:run` 在本机停于两条真实 macOS Terminal/osascript 集成测试；在未含本分支
  改动的基线 `7e1c93cf7` 上用相同安装、构建和测试命令精确复现同两条失败，因此不是本修复引入；
- code review round 1 APPROVED，无阻断 finding；reviewed head 为 `391b03314`。

审查指出全文提示词仍由 producer、snapshot 与手写 golden 多点维护，未来并行分支仍可能重演同类
语义合并冲突；该结构性改造已上报 Lead，需单独跟踪，不纳入本次 main 紧急回绿修复。
