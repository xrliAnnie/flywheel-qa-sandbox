# FLY-2080 — 巡检升级为推进器与病根账本

**Status**: ⏳ Pending ship
**PR**: #965
**Date**: 2026-08-26

所有 dispatch-capable department Lead 的既有巡检规则现强制执行两步闭环：先逐条读取真实 guard 条件，在防篡改与引擎漏账之间分流，能补的账当场补并验证 Bridge 确实接力；再把每个 Bridge 问题按形状、根因、处置、重复先例写入 FLY-2072。完成门只接受 `fixed`、`advanced`、`escalated-with-plan`，不再允许 `known-waiting`。病根查重会分页收齐候选后逐张读取完整 description，且 `class_key` 固定在描述首行，避免 Linear 列表截断导致重复子单与 `×N` 失真。附录提供 receipt 死结与 replacement 铸造漏账两套带事务、CAS、备份和非自证接力检查的独立配方；巡检检测面与频率未改变。截断 mutation 已按 RED→GREEN 固化，全仓 lint/build 通过，其余全包用例除已知 macOS/并发环境项外通过且隔离复验转绿。
