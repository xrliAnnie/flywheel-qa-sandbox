# FLY-2793 — Retire the Ubicloud Canary experiment

**Status**: ⏳ Pending final review, PR CI, and QA
**PR**: pending creation from this milestone commit
**Date**: 2026-09-22

Deletes the standalone Ubicloud canary workflow and its canary-only payload
fixture, parity test, receipt collector, collector test, and live runbook
sections. The startup suite no longer imports those retired tests, while the
workflow inventory test continues to guard every surviving workflow and the
FLY-2453 retirement guard now enumerates the non-empty workflow directory.
`.github/actionlint.yaml` remains because the `ubicloud-standard-2` literal
label is still useful outside the deleted canary. The `CI_RUNNER`,
`SHIP_RUNNER`, and `CI_SCOPED_MODE` behavior and both production workflows are
byte-for-byte unchanged.

The before inventory had nine workflows: the deleted canary exposed only
`workflow_dispatch` (`runner` and `capacity` inputs). The surviving eight and
their unchanged triggers are: `ci.yml` (`push` and `pull_request` on `main`,
with opened/synchronize/reopened/labeled PR types); `payload-activation.yml`,
`payload-auto-release.yml`, `payload-promote-commit.yml`, and
`payload-promote.yml` (`workflow_dispatch`); `payload-beta-release.yml`
(`schedule` every six hours plus `workflow_dispatch`);
`payload-cleanup.yml` (hourly schedule at minute 17 plus
`workflow_dispatch`); and `ship-on-comment.yml` (`issue_comment.created`). A
workflow-only diff confirms no other workflow bytes changed.

A filename sweep retains matches only in seven historical records: the
FLY-2684 creation plan; FLY-2746 cutover plan and research; FLY-2755 sharding
research; and FLY-2791's cleanup checklist, research, and founder report. The
first four preserve the creation, cutover, and sharding decisions. The three
FLY-2791 records are the source recommendation, supporting inventory, and
founder-facing decision artifact that authorized this cleanup. No live
workflow, current milestone, test, script, or operational runbook references
the retired filename.

TDD first removed the canary from the workflow classification allowlist: the
test failed because the still-present workflow was unclassified, then passed
after deletion. Focused verification is green: runner-variable inventory 3/3,
workflow startup/actionlint 7/7, and the all-workflow FLY-2453 guard 1/1.
`pnpm lint` exits 0 (pre-existing warnings only), targeted Biome exits 0 with
one pre-existing warning, and `git diff --check` is clean. No local full
package suite was run; exact-head aggregate evidence belongs to PR CI and QA.

Code review R1 and R2 approved their respective heads with non-blocking
advisories. The final commits preserve the non-canary actionlint label, record
the read-only current runner-variable snapshot without mutating it, state the
SHIP_RUNNER receipt boundary, widen the FLY-2453 guard to a non-empty workflow
inventory, and keep the current milestone out of the filename sweep. R3 also
approved before the conflict-free `origin/main` sync that brought the FLY-2791
source documents onto this branch; this final milestone records their expected
historical matches. This file is the PR's literal-last commit; final review and
CI must bind the exact resulting head.
