# FLY-2204 — founder Google Calendar 写凭据隔离

**Status**: ⏳ Pending review
**PR**: #1011
**Companion PR**: [raya#12](https://github.com/xrliAnnie/raya/pull/12)
**Date**: 2026-08-31

交付 founder Calendar 写入的 A/I/W 三角色机器隔离机制：agent 进程 A 不再持有或调用 Calendar
写能力；确定性 ingress I 只接受 founder 精确指令；writer W 通过 Unix socket 文件权限与 PEERCRED
双重校验，只向 I 暴露严格版本化的 create/cancel 协议。Flywheel 侧新增 root-owned service renderer、
独立 OAuth client 强制、QA 测试日历配置，以及 scope、轮换、恢复、回滚和负向越权探针；Raya 伴生
PR 实现 writer、ingress 与只读 voice projection。

本里程碑仅达到 **code-ready**。依照 Lead 指令，本节点没有创建真实系统用户/组、安装或启动
LaunchDaemon、读取/迁移生产凭据、注册 Discord bot 或改动生产 Calendar ACL；A/B 凭据身份选择仍由
founder 决定。因此在 runbook live Final gate 与独立 QA 完成前，FLY-2137 仍不得声称「唯一自动写入方」
已获得机器强制。

验证证据：83 个 FLY-2204 shell case 全通过，QA 固化的四项 delta 只读探针 11/11、Lead 共享的
wrapper 字面量全称性质探针 16/16 通过，Flywheel
build 通过，变更 shell 通过 `bash -n`/shellcheck；Raya 64 contracts、143 brain、325 voice、94 QA
tests 及 typecheck/build/lint 全通过。
Flywheel 精确 lint/package 门禁中只剩未修改的 baseline/UI 环境问题：2 个 Terminal.app case 因
resident runner 无法连接 macOS UI service 而失败；并行 config census 偶发 5 秒超时，同一 case
隔离 440ms 通过。`d01c4609b`
的 Linux CI 功能测试全部通过，唯一失败是 shell shard 2 的容量 tripwire；
`278a2dca7` 已按实测耗时移动两个完整 step，把两 shard 从 929s/1,077s 平衡到约 1,006s/1,000s，
并由显式 inventory 契约钉住。review High 已补：live verifier 由已定义 directory 派生 socket path；
runtime/voice 路径改到 reboot 后保留的 `/var/db/raya-calendar-isolation/run`，两级祖先固定为
`root:wheel 0755` 且 live probe 证明 A 不可替换 endpoint；runbook 的 Final gate 要求真实 reboot
恢复证据。Lead 在 Round 4 后要求的 fail-closed delta 已由 `9eb27f89f` 收口：成员查询严格区分
error/yes/no，W password 支持无尾换行，live verifier 核验 root owner/mode 与生成值，render 的
implicit/explicit live root 均受 authority gate 保护；sweep installer、CI shard 余量、OAuth canary
清理、proxy timeout/fork 恢复登记 follow-up。Round 5 真机复核后，`f2da64e3f` 进一步接受 macOS
明确非成员的 `no + rc67` 合同、继续拒绝未知结果，并把 peer proxy 纳入 `root:wheel 0755` live
断言；R6 仅限这两项。随后 QA attempt 2 的盲变体发现 Bash `//` 与 macOS `/private` firmlink 可绕过
字符串 live-root 判定，且 verifier 未核 writer `NODE`/`WRITER` 与 ingress wrapper 全部 assignment。
`ca357f59b` 以逐项红→绿回归把 root 判定改为 device+inode，并由 root-owned identity plan 钉住
node/calendar-writer/meeting-ingress 精确路径。R7 继续指出 `/System/Volumes/Data` 的 live 写目标映射
与 runtime ownership 尚未执行；`ee3080c1d` 增加 `/etc`、`/usr/local`、`/Library/LaunchDaemons`
逐目标 identity 判定，并在 live verifier 核 node/两个 entrypoint 的类型、属主和不可写位。R8 确认
这两项 advisory 已关闭。随后 exact-head Linux run `33451678264` 发现 Ubuntu 不存在 macOS
`/Library/LaunchDaemons` 时，指向该受保护绝对目标的悬空 symlink 会被 `-e` 跳过；`0d4a59c50`
在 inode 判定前增加直接 symlink target 的 fail-closed 检查。QA attempt 3 随后确认九个 live-root 盲变体
均被拦，但把 writer `PROXY` 换成 `/bin/cat` 时旧 verifier 仍放行，证明逐字段清单无法形成完整 provision
gate。`fbbdea2e6` 改为机械枚举两个 wrapper 的全部静态 shell 赋值，与 root-owned identity plan 的
render 入参派生期望做精确集合/值比较；未知、缺失、重复或漂移均默认拒绝，并把 W/I uid self-check
一并绑定到 plan。共享只读性质探针 sha256
`a0ad272d55981eaaad5387395efee3c1277ee5857f40cb108de50991a6729383` 保持阳性对照且 16/16 通过。
本头本地 lint/build、bash 语法、ShellCheck 与 83 个 shell case 通过，package gate 仍只受两条未修改的
Terminal.app 真 UI 测试和一个并行 5 秒 census 超时限制（census 隔离重跑 800ms 通过）；
等待 fresh code review、exact-head Linux CI 与独立 QA 重验。完整命令、限制与 live evidence 槽位见
`engineering/doc/FLY-2204-calendar-cred-isolation/implementation.md` 和 `runbook.md`。
