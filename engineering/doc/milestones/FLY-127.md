# FLY-127 implementation milestone

**Issue**: FLY-127 — Lead spawns Runner for tasks not assigned to its department
**Date**: 2026-08-30
**PR**: pending creation from `project-slot-2-FLY-127`

## Delivered scope

- Made the default-on Bridge start contract reject missing, blank, or malformed Lead identity before Runner dispatch, while retaining the documented `BRIDGE_DEPT_SCOPE_REJECT=off` rollback path.
- Bound Gemini tool dispatch to the configured project and Lead identity so model-supplied arguments cannot impersonate another department.
- Added the test-slot Lead identity to `scripts/inject-linear-issue.sh`, with typed scope-rejection diagnostics and a hermetic shell regression suite.
- Updated the shared department Lead rules and doc-flow transport example so every spawn carries the current Lead identity and a department mismatch remains silent and undispatched.
- Left `CLAUDE.md` unchanged, as required by the implementation-node contract.

## Acceptance evidence

- Missing, null, empty, blank, and wrong-type `leadId` cases are covered at the public `/api/runs/start` boundary, including error-precedence, rollback, and negative-path guards.
- The cross-department acceptance case proves that a product Lead is rejected for an Ops-labelled issue, the Ops Lead is accepted, and exactly one dispatcher call occurs.
- Gemini registry, client, fixture, and full-stack tests prove the session-bound identity reaches the Bridge wire payload and caller-supplied identity is ignored.
- The QA injection test proves the configured slot `botName` is sent as `leadId` and an exact typed 403 exits nonzero with actionable diagnostics.
- The production caller audit found explicit identity on the Gemini registry, QA injector, Xiaohongshu scheduler, and active Lead templates; deliberate negative tests and non-production fixtures remain documented exceptions.

## Verification evidence

- Focused TeamLead route, stale-blocker, scope-registry, and rules suites: 88/88 passed.
- Focused Gemini registry, Bridge-client, and full-stack suites: 52/52 passed.
- `pnpm --filter flywheel-gemini-agent test:run`: 12 files and 173/173 tests passed.
- `scripts/__tests__/inject-linear-issue-lead-id.test.sh`: 4/4 assertions passed; enumeration confirmed it is the only new `scripts/__tests__/*.test.sh` file.
- `pnpm lint`: exit 0 with 14 existing warnings after formatting the new route tests.
- `pnpm -r build`: exit 0 across all 22 workspace packages.
- `pnpm --filter flywheel-teamlead test:run`: exit 1 with 485 files passed, 11 failed, 7,046 tests passed, 25 failed, and one worker-timeout error. The failures are unrelated local baseline/environment cases: merge-gate state drift, runtime preflight fixtures, sandboxed system-temp/root writes, and npm-cache permission behavior; the FLY-127 affected suites passed inside the run.
- `pnpm test:packages:run`: exit 1 at `packages/core` because two real-Terminal AppleScript integration cases cannot use healthy macOS GUI automation in this runner; 206 core tests passed before pnpm stopped at the first failing package. Authoritative Linux CI remains required before merge.

## Review evidence

- Code review gate `88434100-7e18-4f1a-a3f3-7f3cccf67761`, request `6a67f0d6-c923-4698-8e1d-fa28f417f74c`: `APPROVED` on round 1 for reviewed head `7809c8367a89a39057388bca788b025ec0563ac5`.
- Non-blocking advisories were reported to the Lead: the identity guard follows possible stale-blocker side effects; rollback mode can retain a whitespace identity if legacy resolution fails; shared-token identity remains caller-asserted; and lower-severity fixture, in-repo wording, Codex environment-name, and injector-negative-path coverage drift remain follow-up candidates.

## Rollback and residual risk

- Setting `BRIDGE_DEPT_SCOPE_REJECT=off` restores legacy missing-identity auto-resolution and disables both the new identity-presence and department-scope rejection gates. This is a security regression by design and should be time-boxed to incident rollback only.
- The Bridge still authenticates callers with shared tokens, so `leadId` is asserted rather than cryptographically bound to a Lead. Per-Lead credentials are the durable follow-up.
- Existing stale-blocker handling and Linear hydration can occur before the new missing-identity response; this preserves the approved error-precedence contract but may finalize or alert on an otherwise rejected request.
- Unauthenticated dashboard retry paths and `checkLeadScope` behavior for missing identity remain outside FLY-127 and require a separate hardening change.

## Handoff boundary

The implementation phase owns the committed code, tests, documentation, review evidence, and PR. The DAG orchestrator owns QA advancement; merge and deployment remain outside this node.
