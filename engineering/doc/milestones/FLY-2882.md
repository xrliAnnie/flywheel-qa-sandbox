# FLY-2882 — 「某个 Lead 现在在忙什么」只读接口(Claude / Codex 两种载体)

**Status**: ⏳ Pending review, exact-head CI and QA (QA attempt 2 rework)
**PR**: #1331
**Date**: 2026-09-25

语音耳机模式下 Raya 要先看另一个 Lead 忙不忙,再决定问 founder 是等还是打断。本单新增只读接口 `GET /api/lead-activity`(另有 `/fleet`,master token)和 `flywheel-comm lead-activity`,对 `projects.json` 里每个 Lead 答 `busy | idle | unknown`:busy 带本轮开始时间、已用时长和精度,以及「这轮由哪张单引起」——只在因果链可证明时给单号,否则明写「判断不了」;unknown 必带具体原因。读不到、看不懂、证据不足一律答 unknown,没有任何路径能在证据不足时答 idle。

1. **Claude 载体**:核验过身份的 `capture-pane`(150 行只在内存解析,不输入、不落盘、不回传原文)→ 输入框上方「状态槽位」解析(转圈行 = busy,整轮计时;`… · done …` = idle;任何不认识的第 0 列行 = unknown)。截屏前后各查一次 Claude 子进程(只读 pid/ppid/内核名),必须是同一个活进程,否则 `lead_process_not_running` / `lead_process_unverified`——Claude 退出后留在屏上的旧画面不会被读成空闲。≥1 天的计时按截断分钟的区间中点估算。v1 不给 Claude 的单号。
2. **Codex 载体**:TUI sidecar 每个进程代一个 `LeadTurnStateTracker`,在 demux 扣住/丢弃事件**之前**的原始通知接缝观察 turn 生命周期,校验线程与状态,畸形事件作废信任并重新 seed(`thread/turns/list`,与实时事件用 revision 栅栏线性化);经新的认证 socket 方法 `readTurnState` / 能力 `turn_state_v1` 暴露。Bridge 对回包封装与快照都做严格校验。单号只走 runner 提问与 Bridge 事件两条可证链,只读元数据列。
3. **预算**:单个 Lead 8 s 截止(`read_timed_out`),fleet 并发 6;CLI 单个 15 s、`--all` 45 s。

设计是 codex 批准的 plan v3 加 `design-correction.md`(Lead 转来的 gpt-6-astra 设计补审,3 blocking + 2 advisory,全部实现)。本机只跑了定向测试:保留 264 个测试文件(teamlead 212 个 / 2916 条等),`pnpm lint` 0 error,8 个下游包 typecheck 通过,两处变异验证;用本分支代码只读判定了 14 个生产 Claude Lead(13 idle、1 无状态行)。代码评审用 `codex:rescue`,R1(默认模型)与 R2(gpt-6-astra)共 5 条 MEDIUM 已修,R3(gpt-6-astra)APPROVED。详细记录:`engineering/doc/FLY-2882-lead-busy-state/verification.md`。

QA 返工(implement attempt 2):exact-head CI 的 FLY-2006 保留窗(14 天)清扫消费者守卫报新读者 `turn-trigger-attribution.ts` 读 `mailbox` 未登记;已在 `scripts/fly-2006-retention-consumer-gate.config.json` 登记为 `candidate_guarded`(行被清扫只会答「判断不了」),守卫本地通过。同一轮 CI 另一处红(`qa-fly-1986-load-probe.test.sh`)核实为与本单无关的间歇性失败(样本数与期望不符,根因未证实);QA 沙箱里 17 个 Lead 全答 unknown 属预期(连不上 tmux socket、无 Codex bot token),非沙箱只读实测见 verification §6。

QA 返工(implement attempt 3,QA attempt 2 FAIL):① 529 房里 Claude 载体按构造恒答 `unknown / lead_window_unavailable`——定位器只认生产 LaunchAgents。现在定位器按「本 Bridge 自己的 launchd 权威」找 Lead:房的 Bridge 经 env 合同显式拿到 `FLYWHEEL_LEAD_LAUNCHD_REGISTRY=${SLOT_DIR}/launchd-leads.json`,只读这本注册表,逐跳核验(唯一的房内标签行 → 严格结构化解析的 plist:Label / v2 argv / 绑定的 manifest / Lead 自己的 state dir 都在房内 → manifest 身份与规范 socket);生产不设该变量,读法逐字不变。② 新增 QA 夹具 `scripts/qa-lead-activity-long-turn.mjs`:经插件同款 chat-ingest 让两种载体的 Lead 各跑一轮前台 `sleep`(默认 75 秒),按夹具自己的时钟与 mailbox `notified_at` 判 busy 不间断 ≥60 秒、开始误差 ≤30 秒、前后 idle;Lead 没照做答「不确定」,不算通过。代码评审 gpt-6-astra 七轮,R7 APPROVED;R2 之后的发现全在夹具判定逻辑,已用带种子的性质测试钉住(对三个旧判定都报错)。详见 verification §7。

上线:Bridge 部分随下次 Bridge 重启生效;3 个 Codex Lead 在 sidecar 进程重启到新代码之前如实答 `sidecar_lacks_turn_state`。实现节点没有请求 full CI、没有派 QA、没有合并或部署。本文件是 PR 的 literal-last commit。
