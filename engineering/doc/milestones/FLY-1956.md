# FLY-1956 — 出站超时与可核回执

**Status**: Pending code review / CI / QA handoff
**Date**: 2026-09-13
**Branch**: flywheel-FLY-1956
**PR**: https://github.com/xrliAnnie/flywheel/pull/1180

qa-result 在联网前原子保存跨进程幂等键，整次持锁并同键重放；stage set 先入有序队列，后续 CLI 前置排空，gate/request-review 共享发布锁形成围栏。Bridge 按持久化 claim/event 校验回执、合并相同在途决策，并在全部 stage 义务结算后确认 applied。completed 重放保留真实批准、HEAD、review、QA 校验，未完成收尾继续 pending。

三条 HTTP 路由新增 close-before-finish 压力计数；nudge 超时调整为1500ms，失败说明 durable row 保留。无 schema/env/token 新增、无 Bridge lifecycle 任务；sweeper 与 gate --stage 保留为 Lead follow-up。

确定性慢响应、SIGKILL/重启、真实并发发布与授权重放已有可执行证据。lint 与全仓 build 通过；本地完整测试存在失败，细节及已隔离复测记录见 [verification](../FLY-1956-outbound-timeout-receipt/verification.md)。本实现交付不代表部署、生产高负载验收或 QA 通过。

所有 package suite 已执行；本地失败 aggregate 保留。其他失败文件单次隔离 comm31/31、teamlead75/75、runner9/9；本单 fixture 17/17 通过。正式 exact-head CI 与 code review 通过后按 needs_review 交接。

Code review HIGH：设计 stage 同事件重放改用原持久 manifest/correction，避免计划变更永久卡住队列；旧 revision 被取代后可结算。131/131 相关回归、lint 与全仓 build 通过，等待修复头的重新评审及 CI。
