# FLY-2766 — GPT-6 Sol 精确模型切换准备

**Status**: ⏳ Pending final exact-head review, CI, and QA handoff
**PR**: #1286
**Date**: 2026-09-22

本 PR 完成 founder 收窄后的 Codex/Sol 一侧准备，不实现产品线 alias，也不改生产模板、
CLI config、在飞 run、Raya Astra、Claude Opus 或任何生产状态。registry 新增 exact
`gpt-6-sol`，只开放给 runner/workflow；runner 仅允许 `xhigh`，workflow 保留标准 effort，
显示名为 `GPT-6 Sol`。既有 `codex` alias/default 仍指向 `gpt-5.6-sol`，等待严格 cutover
顺序中的最后配置动作。

founder TUI 的 `codex resume --remote` 现在只保留工作目录 `-C`，不再由 client 重复传
`-s workspace-write` 或 `approval_policy=never`，权限继续由 daemon 统一拥有。native skill
admission 精确接受 `0.153.2 + 旧树`、`0.154.0 + 新树`、`0.156.0 + 新树`，未知版本、
树错配和 prototype-like version 全部 fail-closed；missing-home 的缺失 origin 统一返回合同
错误。home canary 的版本探测使用不可写 `HOME=/dev/null` 且不把选中 `CODEX_HOME` 暴露给
子进程，随后才对完整 skill tree 做只读验证。

Lead rework 在真实主机揭示 `~/.codex-259-qa/packages` 是指向
`/Users/xiaorongli/.codex-242/packages` 的目录软链。此前 baseline 保存逻辑路径，而 origin
canary 要求该字符串等于自身 realpath，三版 origin 虽然内容正确仍全部 fail-closed。代码头
`d89eeb75c` 在受信 pinned-origin 配置边界先 canonicalize，再沿用原有完整树验证；home、树内
目录/文件的 realpath、symlink、file-count 与 digest 守卫没有放宽。新增父目录软链用例先 RED
于 `native_skill_home_unverified`，修复后 4/4 通过。

本地验证覆盖：claude-runner related 6 files / 214 tests，config related 30 files /
498 tests，teamlead 初始 related 20 files / 345 tests；复审修订后 teamlead related 19 files /
341 tests 与 canary 3/3 全绿。真实 0.153.2 home 的 60-file canary 通过；隔离生成的
0.156.0 tree 同时匹配 0.154.0/0.156.0 pin，旧/新错配及 0.155.0 被拒。affected builds、
13-package dependent typecheck、`pnpm lint`（零 error，25 条既有 warning）及
`git diff --check` 均通过。没有运行本地 full package suite，也没有请求 full CI；只有后续
frozen-head `CI OK` 才是完整套件证据。

rework 后再次运行 teamlead `vitest related`，17 files / 326 tests 全绿；已枚举 canary Node
suite 9/9、`flywheel-teamlead...` package-plus-dependencies build、`pnpm lint`（零 error、25 条
既有 warning）、targeted Biome 与 `git diff --check` 均通过。在生产真实 canonical 根
`/Users/xiaorongli/.codex-242/packages/native-skill-baselines` 只读执行 plan T5 校验段，
0.153.2 / 0.154.0 / 0.156.0 全部 `status=passed` 且各返回 6 个 source；0.155.0 阴性仍 exit 1。
每条 receipt 均为 `modelStarted=false`、`productionMutated=false`，没有改动已物化 origin。
自动 scoped CI run `35821692739` 在 `d89eeb75c` 上完成 Quick Gate 与 `CI Scope OK`；重矩阵
按 scope 跳过，因此这不是 full/exact-head `CI OK` 证据，也不授权 ship。

rework effective review（question `fae21518-872a-4db8-aff8-cb13def71ae4`）在
`0479b883a` 返回 `APPROVED`，无 HIGH blocker。3 条 MEDIUM advisory 涉及 manifest receipt
仍记录逻辑软链 origin、home canary receipt 无法区分被验 home、canonical storage 位于代码未
命名的 legacy `.codex-242` home；另有 missing version 诊断、Astra display name 与预期的未提交
milestone 三条 LOW。它们不影响本轮 admission 正确性，已通过 report
`d8e68ef1-3b69-4d45-9170-69622e8ce5b3` 原样转给 Lead；本节点不把 non-blocking advisory
擅自扩大成未批准重构。包含本文件的 literal-last 新头仍须再取一次 exact-head review。

首轮 effective code review 已在 `c2dc1063c` 通过，无 HIGH blocker。其 advisory 已在代码头
`cf7085dda` 中收紧 resolver own-key、缺失 origin 错误、canary child env、重复 root 和行为
测试；验证记录与七项目切换矩阵见
`engineering/doc/FLY-2766-gpt6-sol-switch/verification.md`。首次 milestone 推送后 PR 与
`origin/main@83eb300f2` 冲突；技术同步 `13f89345b` 唯一手工解法是在 non-flag allowlist
同时保留本单 selector 与 main 的两个 standing-authority key。同步后 config related
30 files / 498 tests、teamlead related 20 files / 346 tests、canary 3/3、milestone guard
32/32、affected builds、13-package typecheck、lint 和 diff check 全绿。

自动 PR CI 随后在 Quick Gate 精确指出新 root Node test 未被 `ci.yml` 枚举。没有改
`ci.yml`；代码头 `a004403f5` 把三项 native canary 断言并入既有、已枚举的
`qa-codex-lead-parity.test.mjs` 并删除独立 test 文件。本地合并后的 suite 9/9 通过，root
inventory 证明 85 个 Node suites 全部已枚举。

`plan.md` T5 现在提供一条可复制、可重复执行的幂等物化命令：从仍运行 0.153.2 的共享 home、
仍运行 0.154.0 的 Raya home，以及空隔离 `CODEX_HOME` 启动 0.156.0 得到三棵树；新 snapshot
在同一父目录 staging、去掉写位后 rename，已有 version 目录绝不覆盖。独立校验命令构建
`flywheel-teamlead`，检查 origin 无 owner 写位，再对三个 exact version 逐一运行 read-only canary，
完整 file/source digest 必须与本 PR 登记值一致。QA 先于隔离环境复演并通过；随后两条命令均
只由 Lead 在发 founder ship 卡前对真实路径执行，QA receipt 不能替代 Lead 的 host receipt。
漏做会使部署后的 capability-bundle-v2 cold start 因 committed origin 缺失而 fail-closed，阻断
新 runner/Lead，并失去旧版本 rollback origin。Lead 已在 rework 前把三份生产 origin 物化为
只读目录；本实现节点只读验证其 digest，没有创建、覆盖或修改 origin。

Lead 随后把时序收紧为：QA 先通过，Lead 在发 founder ship 卡前物化并校验，全部成功后才请求
ship approval。物化段只用纯 shell、现有 home 与已安装 0.156.0 binary，不依赖本 PR；校验段
依赖新 resolver/canary，明确从已复审 PR worktree 的 exact reviewed HEAD 执行。这样 origin 在
merge 前已经就位，不与 00:00/12:00 updater 班车竞争，也无需 disable/bootout updater。

精确头 `3ea718d19` 的 review 已通过；它指出的 post-merge shuttle window 已由父提交
`2fa3f37f7` 按上述 Lead 指令消除。更早 review 指出的 clean-checkout build advisory 也已修成
`pnpm --filter "flywheel-teamlead..." build`，并在本地完整执行 13-package build 成功。旧 review
不覆盖本次时序修订；推送后只对包含本文件的精确最终 HEAD 再取一次有效代码复审。

发 founder ship 卡前仍有硬前置：QA 须先在隔离环境完成同一 runbook、0.153.2/0.156.0 runner、
0.156.0 Lead cold start 与 review companion 验证；随后 Lead 必须按 T5 命令物化并逐份验证三个
immutable origin。合入/部署后，四个 Codex home smoke 与 binary/config 全绿，才可
严格最后发布 active `tpl_code` 与 `tpl_simple_code` 的新 revision。
`tpl_eng_heavy` 已 retired，不动。随后逐个新派 flywheel、geoforge3d、growth、
joycon-typeless、personal-assistant、raya、tidal-echo 的 `code` / `simple_code`，核对解析日志及
pane 首屏为 `implement=gpt-6-sol`；Opus/`opus[1m]` 归 FLY-2775。

三个 committed origin 已由 Lead 在生产真实 canonical 根物化且只读，本轮三版 host canary
通过；QA 仍须在最终 reviewed HEAD 独立重跑同一生产真实路径判据，隔离根复演不能替代这一格。
四个 home 稳定运行 0.156.0 一周后由 Lead 删除 0.153.2/0.154.0 临时 pin。本实现节点没有
dispatch QA、merge、ship、deploy、重启服务或修改生产配置。本文件是 PR 的 literal-last commit；
后续 review、自动 PR CI 与完成收据不得再通过提交回填结果而移动 HEAD。
