# FLY-2205 — Raya 插嘴后不再全文重念

**Status**: ✅ Implemented; QA doc-only correction added; pending independent QA rerun
**PRs**: xrliAnnie/raya#11（产品）· Flywheel 本锚 PR（设计/进度/里程碑）
**Date**: 2026-08-31

2026-08-31 founder 终裁取代旧的「默认关闭」说明：重念策略写死为开，`rereadPolicyEnabled` 配置键、运行时开关和 flag-off 分支全部删除；回滚手段只有 git revert，不保留旋钮。`rereadPolicyMode`（`resume`/`discard`）与 `bargeInEnabled` 仍是 FLY-2178 集成语义，本轮不动；策略恒开后，`discard + bargeInEnabled=true` 两键组合可达并继续 fail-closed 拒起。

Raya 的三段语音 inbox 在确认失败后，按本次真实注入点之后的 transcript 顺序覆盖，从第一个未覆盖段继续；已完整 armed 的 ship 卡被打断后改念短卡，并以同一 binding 下的 session cap 有界 defer 到既有文字兜底。默认 runtime 装配测试不再注入 enable 值，直接证明出厂装配为恒开 + `resume`。旧 D1 的 `off == 1c71cd2` 差分被替换为可执行的 rollback oracle：保留 immutable `1c71cd2` trace，并逐项断言当前默认开世界只增加预期的短卡/`reread_*` delta；回滚锚明确为 revert FLY-2205 到该 immutable baseline。

产品最终 rework head `bafd2593f7793cfe14efd181a16df670a23bcc06` 经两轮 cross-family exact-head review 后 APPROVED。Raya 全仓 `pnpm lint`、`pnpm build`、`pnpm typecheck`、`pnpm test` 全绿：contracts 62、voice 378、brain 125、QA/probe 95。Flywheel 锚仓 `pnpm -r build` 通过，`pnpm lint` 以 0 退出并仅报告既有 warning；`pnpm test:packages:run` 仍只有两例依赖真实 macOS Terminal GUI/XPC 的既有测试失败，其余已运行套件通过。实现节点不部署、不 merge；独立 QA 节点按 exact head 复核默认装配、原四组探针与新报告措辞。

QA attempt 2 在上述产品头重跑 660 项与九组探针后确认引擎面全绿，唯一 FAIL 是随锚 PR 合入的历史设计文档仍承诺「默认关 / 试用后再开」。按 QA 修法，本轮不改写历史正文、不动任何代码：`founder-design.html` 首屏新增醒目的 SUPERSEDED 守卫并改正副标题，`plan.md` 在 header、§0.3、§2.1、§5 四处追加失效标注。守卫如实记录 2026-08-31 指令链、写死恒开、无运行时开关、回滚仅靠 git revert、三条激活门全部作废，以及「真机播放积压数据先摆给你看」未执行；不声称 founder 已对 transcript best-effort 残余面作知情同意。标记断言从 RED 到 GREEN，ProofShot 首屏截图确认横幅醒目且无溢出。

doc-only rework 头重新执行 `pnpm lint`（exit 0，仅既有 warning）与 `pnpm -r build`（全绿）。`pnpm test:packages:run` 复现两例 Terminal GUI/XPC 环境失败，并在并发负载下多出 `drift-scan` 的 5 秒超时；同一测试文件脱离包内并发后 27/27 通过（目标 census 537ms），未修改范围外代码或测试时限。本轮没有新增 `scripts/__tests__/*.test.sh`。
