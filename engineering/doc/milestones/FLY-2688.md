# FLY-2688 — 账号额度一览页与切号附链

**Status**: ⏳ Pending exact-head CI and QA retest (implement attempt 2 after a QA fail on Biome formatting)
**PR**: [#1258](https://github.com/xrliAnnie/flywheel/pull/1258)
**Date**: 2026-09-22

新增 `flywheel-comm accounts-page`：命令在 founder 需要时从 Bridge 读取与 `/api/capacity` 同源的额度快照，生成等宽双表 HTML，再交给既有 `publish-report` 托管。返工追加第二个事件入口：Claude 手动切号或 quota-monitor 自动切号的 `account_switched` 通知，在落地前调用同一命令的 `--publish-only` 模式，把托管 URL 放进同一条 info 通知，不另发报告消息、不 @ founder。原按需调用行为保持不变；没有新增定时器或启动钩子。Bridge 原始 HTML 路由继续受 master token 保护，托管成品沿既有报告通道公开读取。

切号已经提交后才进入 durable 通知 drain，因此账号页生成或发布失败不会回滚或阻断切号；通知会如实附上「账号页本次未生成：<原因>」。通知入口给同一 builder + publish 流程一个共享的 5 秒 deadline，为外层 30 秒投递预算中的 Discord、SQLite 与进程开销留出余量；超时固定降级为「账号页本次未生成：timeout」。页面发布发生在同一 switch 事件取得 delivery lease 之后，相同 signature 的重放先命中既有 delivery receipt，不会重复发布同一快照。成功、普通失败与超时三条路径都保留原切号正文。

Claude 区块按账号逐行显示当前号标记、周重置、5h reset、周用量、Fable 独立周用量和订阅到期日。额度守护、监控和候选探测三条真实观测路径都从 usage API 的自描述 `limits[]` 读取 Fable，连同 5h/Fable reset 写入现有账号存储并投影到容量快照；tick 与 HTML 共用一个严格校验的 builder。机器值优先，缺值才使用 founder 2026-09-17 手填基线，并逐格显示来源、读数时间和 stale 灰显。机器值与手填值不一致时，页面页脚保留差异，而不覆盖机器事实。账号缺失、读数过期和字段无数据均有显式负面呈现；公开托管页只渲染账号别名，不显示完整邮箱。tick 只投影机器观测，页面专用的手填 Fable/到期回退在 tick 中明确保持 `n/a`，不伪装成容量读数。

2026-09-19 founder 三项呈现返工保持同一数据口径和通知逻辑不变：所有数值下方的 provenance 改为 9px 的浅灰小字，并明确写成「来源：机器 · 读取于…」或「来源：手填 · 记录于…」；表头改为「周重置日」，让 reset 日期与读取时间在文字和视觉上分离。每行账号名下增加订阅档位，Claude 只读取 profile 凭据中经严格校验的非敏感 `subscriptionType` / `rateLimitTier`，可直接证明时显示 `Max 20x`、`Pro` 等，字段缺失或非法时显示「未知」；不会由 20x 猜测或写死 `$200`，也不会把 access token 放进容量快照或 HTML。Codex 暂无同等可靠档位源，三号逐行如实显示「未知」。档位放在账号格内，额度列顺序不变。

同日追加的当前账号可见性返工只改共享 HTML renderer：Claude 与 Codex 两张表中 `active` 行使用浅绿色整行底色与左侧 4px 绿色色条。founder 2026-09-19 15:39Z 进一步要求去掉重复标识后，两张表的行首 `*` 均已删除，高亮行成为唯一的当前账号标识；页脚图例改为说明高亮含义，并明确 Codex 高亮来自 founder 2026-09-17 手填。非当前行、字段顺序、额度值、来源与通知逻辑均未改变。

2026-09-21 技术性合 main（无产品文件改动）：前一精确头 981b60cab 的 `Script Tests 3/5` 分片两次 attempt 都撞 FLY-1870 的 1020s 容量闸而红，修它的 FLY-2755 六分片布局已合入 main，但同头重跑沿用旧 ci.yml、必需 job 清单也从被测头读取，所以按 QA 2026-09-21 路由性判定把 `origin/main`（e82193650）合进本分支出新头；`git merge-tree` 预检零冲突，新头清单已是六分片。合并后定向验证：两包 build 与 typecheck 退出 0；teamlead 直接归属测试 10 files / 361 tests、flywheel-comm `accounts-page` 7 tests、`lead-alert-strict-delivery.test.sh` 31/31 全过；`vitest related` 覆盖 334 files 中仅 `lifecycle-closeout.test.ts` 的 FLY-2490「missing evidence」1 例失败，本分支未触碰该文件且在 `origin/main` 主检出上本机同样失败，判为预存环境差异，交精确头 CI 裁决；全仓 `pnpm lint` 退出 0（仅既有 warnings）。

FLY-2670 尚未提供 Codex 数值源，因此 Codex 三号保留手填值但整块明确标注“无数值源”，不伪造机器读数；以后接入真实源仍由容量快照合同承接。未执行部署、Bridge 重启、QA 派发、生产 auth 或其他生产配置变更。

返工验证：前轮 `accounts-page` 聚焦 1 file / 7 tests；通知路由与真实 shell delivery receipt 聚焦 2 files / 58 tests，覆盖 URL、普通失败降级、共享 5 秒 deadline、超时精确标注、无 @ founder、同事件不重复发布、托管页仅账号别名与按需默认行为不变。三项呈现返工先在旧实现上得到 4 个预期失败，再完成 3 files / 17 focused tests；当前行高亮返工也先在旧实现上得到聚焦失败，再完成 renderer 1 file / 2 tests。2026-09-20 去星号返工再次先得到 renderer 聚焦 1/2 的预期失败，最小实现后同文件 2/2 通过；首轮 exact-head code review 发现 Bridge route test 的旧星号断言，本地精确复现为 1 failed / 7 passed，移除该旧断言后 route + renderer 两文件 8/8 通过。TeamLead typecheck、package build、受影响文件 Biome 与 `git diff --check` 通过。生成的 10,686-byte HTML 回读确认可见星号与 identity marker 都为 0，Claude `shopping` 与 Codex `personal` 各且仅一行带浅绿色底和左侧色条，700px media rule 与 390px 横向滚动所依赖的 overflow/min-width 合同保持不变，tick 的 `★shopping` 断言仍通过。系统 Chrome、Playwright Chrome、临时 `/tmp` WebKit 与 Firefox 均在页面加载前 SIGABRT，Quick Look 也被 sandbox 拒绝，因此 implement 节点不声称像素截图已通过；可复现 fixture 已随 PR 提交，390px 真机截图仍由独立 QA 完成。按 Lead 红线不运行本地 `pnpm test:packages:run`；新精确 head CI 是 aggregate 权威。未触发 QA、部署、Bridge 重启或生产 auth。本单未产出 exploration/research：Lead 2026-09-18 裁定档位为 plan_only，且不追溯补造过程文档；事实账本见同 issue 文件夹的 `plan.md`。本文件是返工 PR 头的最后一个 commit；有效代码评审、精确 head CI、独立 QA、生产激活、合并与部署仍是下游独立证据。

2026-09-21/22 founder 返工把 Codex 区块从手填换成**真实读数**，并给两张表加上按恢复/重置的排序。新增的 observer 从 `<codex home>/profiles` 枚举槽位（普通目录、非符号链接、含 `auth.json`），因此 registry 未收录的 shopping / personal1 / personal2 与注册三号一起被读到；每个号在隔离 `CODEX_HOME` 里跑 app-server 的 `initialize → account/read → account/rateLimits/read`，逐号 20s、整轮 45s deadline，超时只写已读到的号、未读到的如实记 `deadline`。被 live Codex 进程占用的号直接跳过（inventory 读不到时 fail-closed 判全部占用），保留上一次读数并标「使用中，本次未读」，不编数。读数过程中若 app-server 轮换了 refresh token，新增的 `persistCodexProfileQuotaRefresh` 把新字节写回同一个槽位，守卫与既有 `persistCodexCandidateCredential` 逐条对齐，唯一差别是用「写回前后身份一致 + 目标是该 accountKey 自己的槽位」取代三号白名单；函数里没有任何 profile 名字面量，FLY-2762 把注册表改成 N 号时可直接替换，failover 链路零改动。

读数落在原子写、0600 的 `codex-accounts.json`，只存非敏感字段（token / id_token / email 一律不落盘），由容量快照投影成 `quota.codex`；store 缺席或不合法时输出与改动前逐字节相同的 `{source:null, unavailable:["structural: codex_no_usage_api"]}`，「无数值源」这条阴性对照继续活着。当前 Codex 账号改为机器判定（槽位身份等于 canonical `~/.codex/auth.json`），不再硬编码 `personal`。窗口按 `windowDurationMins` 分到 5h / 周，**没给时长的窗口只计数不归桶**。Codex 表第 5 列改为「credits / 重置兑换」，按 RPC 实际暴露程度显示 `余额 X · 可兑重置 N` / `无可兑重置` / `RPC 未暴露` —— 该 RPC 在窗口级给 `credits{hasCredits,unlimited,balance}`、在顶层给 `rateLimitResetCredits`，字段缺席就明写未暴露，不推测。两张表各自按「下一次变好的时刻」升序（打满的号取恢复时刻，其余取未来最早 reset，都没有的排最后），打满行浅红底并注明「打满 · 恢复 MM-DD HH:mm PT」。探测只由 `?refresh=1` 的按需命令触发，切号通知那条 5 秒预算的路径永不探测、只渲染既有读数；未新增任何定时器或启动钩子。

本轮验证：8 个单元先红后绿；定向套件 account-quota-view 7、capacity-route 9、capacity-snapshot 20、codex-quota 19 files/182、patrol-tick 89、runtime/probe/bench 131、claude-runner 23、flywheel-comm 7 全过；`vitest related` 覆盖 teamlead 175 files / 2630 tests 与 claude-runner 13 files / 582 tests，红的三处（`lifecycle-closeout` FLY-2490、`codex-daemon-runtime` 4 条）在 main 检出上同样红，`workflow-ship-ready-head-enrichment` 单独跑 6/6 通过、只在 175 文件并行批里翻红，判为并行串扰，全部交精确头 CI 裁决。两包 build 与 typecheck 退出 0，改动的 14 个文件 Biome 全清（全仓剩余 3 errors / 25 warnings 全在本分支未触碰的文件）。像素验证这次真的跑通：1440×1200 与 390×900 full-page 截图已随 PR 提交在 `engineering/doc/FLY-2688-account-quota-page/evidence/`，逐项回读确认排序、红行恢复时刻、当前行高亮、credits 列与三条阴性文案，390px 仍由 `min-width:990px` + `overflow-x:auto` 横向滚动。**未对生产 `~/.codex` 跑过真实六号探测**（本轮 dispatch 明令禁止），真机读数、切号链路与公开 200 仍由独立 QA 证明。

精确头代码复审（2026-09-22）走 FLY-2763 的同家族 Claude 口（Codex companion 撞 usage limit，项目 flag `review_same_family_allowed=on`）：request `93f4c4e5` 冻结头 `88330d02d`，verdict APPROVED，8 条非阻断 findings。其中 5 条当场修：in-use 守卫改为每个槽位重取一次库存（对齐 failover runtime 的 per-profile 复查）；库存读不到时不再谎称「使用中」，改记 `inventory_unavailable` → 页面显示「占用状态未知，本次未读」；`profiles` 根读不到时直接抛错而不是把好 store 覆盖成空，页面继续显示上一次读数；崩溃遗留的隔离 CODEX_HOME 在写回成功后被删除，不留 0600 token 副本；`--timeout-ms`（上限 10s，盖不住六号探测）与 `--publish-only` 一样不再触发探测，并给 observer 加了 90s AbortSignal 天花板。其余三条（`claudeEmails` 死管线、切号降级文案带内部 Bridge URL、pid-less stopped 记录的有意差异）保留为 follow-up 并写进 `plan.md`。修复后 observer 9、view 7、route 9、snapshot 20、codex-quota 19 files/185、flywheel-comm 8 全过，两包 build 与 typecheck 退出 0。

复审 R2（新头 `f1b211c9c`，request `38322143`）同样 APPROVED，5 条全 LOW，其中 3 条再修：每槽位库存刷新移到 deadline 检查之后（过期轮次不再白跑扫描）；`accountInUseGuard` 的文档注释改成 `"unknown"` 的真实语义；auth 失效的 Claude 账号不再被写成「打满 · 恢复时刻未知」—— 行模型拆成 `exhausted`（额度打满才显示恢复时刻）与 `unusable`（凭据不可用，显示「凭据失效，需重新登录」、排序排最后、保留红底）。剩下两条（`claudeEmails` 死管线、切号降级文案带内部 Bridge URL）保留为 follow-up。

复审 R3（头 `476d45190`，request `33138d5a`）第三次 APPROVED，3 条全 LOW，修掉新出现的一条：容量快照里 Claude 的 `authUnusable` 同时覆盖 auth 失效与运营手动停用标记，所以「需重新登录」这句话对后者是错的，改成中性的「凭据不可用，需人工处理」；Codex 侧机器可证的 `refresh_invalid` 保留「凭据失效，需重新登录」。剩下两条（`claudeEmails` 死管线、切号降级文案带内部 Bridge URL）继续留作 follow-up。

QA5（exec `c3b49431`）在头 `e098f7399` 上判 FAIL，单一阻断项是精确头 CI 的 Quick Gate 红 —— 本分支新增的 `codex-account-quota-store.ts:181` 有一处 Biome 格式错误（`if` 条件需拆多行）；两项 founder 返工本身 QA 已独立验证通过，并用零凭据探针在真 codex-cli 0.153.2 二进制上证实 `account/rateLimits/read` 是真方法、字段表含 `rateLimitResetCredits`。implement attempt 2 修掉该格式错误，并把自己的 lint 复核方式换成**用退出码当尺子**（此前我用 `grep ':行:列'` 过滤 biome 输出，而 format 类诊断没有行列前缀，整类被漏掉 —— 干净的全绿是假的）。同时按 QA advisory 把「打满 · 恢复 恢复时刻未知」的结巴文案改成「打满 · 恢复时刻未知」。本轮 `pnpm lint` / `pnpm -r typecheck` / `pnpm build` 全部退出 0，FLY-2045 里程碑布局守卫 32/32，定向套件 teamlead 276 + flywheel-comm 8 + claude-runner 23 全过，渲染截图逐字节不变。
