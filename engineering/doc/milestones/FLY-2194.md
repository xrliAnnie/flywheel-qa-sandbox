# FLY-2194 — 良性评审顶替静默

**Status**: ⏳ Pending ship
**PR**: #999
**Date**: 2026-08-30

修复旧 cross-family review gate 被同 execution、同 checkpoint 的新版 gate 正常顶替时，内部
`gate_answered_externally` 失败被误报到 founder thread 的问题。Bridge 现在用 durable
`superseded_at + superseded_by` 及同 owner/checkpoint provenance 将该路径记为
`failed / superseded_by_revision`，保留内部账与 fail-close authority，同时对 founder-facing
`review_job_failed` 保持零事件；cross-execution、foreign answer、expired/missing/mismatch、
stale head 与真实 reviewer failure 继续 fail-loud。

恢复文案改为由 failure reason 与 live gate 状态共同决定：只有可验证 open 且可重放的请求才提示
same-request retry，废门改为新 gate + 新 request，stale head 改为新 requestId，binding 与 CommDB
异常分别给出修复或检查路径。Lead patrol 同步排除已经失效的旧 review 作业，避免把废 request
重新包装成行动项。TDD 覆盖 design/code、boot redrive、scheduled retry、provenance 反例、精确文案
和 patrol 防复活，共 103 个聚焦 Vitest 断言与 203 个 shell 断言通过。

`pnpm -r build`、Teamlead typecheck/build、changed-file Biome 与 diff check 通过。完整仓库命令如实保留
宿主基线：`pnpm lint` 在未改文件有 2 errors/14 warnings；`pnpm test:packages:run` 因无 Terminal.app
GUI 的 2 条 core 用例中止，补偿运行证明 non-GUI core 219/219，余包并发运行 9,863 pass、23 条
未改 timeout-sensitive 用例失败。cross-family code review 在代码头 `cc3a4872e` 上
APPROVED；六条非阻塞 advisory 已报告 Lead 作为后续候选。
