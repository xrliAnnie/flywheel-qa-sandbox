# FLY-2504 — 返工替身完成前验证内容送达 receipt

**Status**: ⏳ Pending ship
**PR**: #1155
**Date**: 2026-09-11

替身 launch 内联稳定返工上下文并记录内容 digest；仅在当前 revision/execution 的已提交 issue delivery 证据匹配时原子标记启动和接受 completion。缺 receipt 时回滚完成、进入既有 operator hold，CLI 不重试或写完成 marker。共享持久化目标围栏覆盖通用重试，receipt UID 支持同请求第二代替身。

Lint/build 和相关聚焦回归通过；两轮全仓 package gate 均因已记录的测试/worker 超时非绿，隔离通过不替代全仓回执。生产只读预检由 Lead 提供（5/17 命中），部署方负责前后复查和处置。Legacy completion-hold-first 的组合恢复由 Lead 明确延后。详见 engineering/doc/FLY-2504-rework-replacement-receipt/implementation-report.md；本节点未合并、部署或更改生产数据。

R2 唯一 HIGH 已修复：返工 remint 后按 family/root_id 关闭旧 open episode，防止拒绝路径触发唯一约束错误；新回归 RED→GREEN，43 项相关测试及 lint/build 通过。MEDIUM/LOW 建议留待后续。

R3 经 Lead 授权修复 sweep 关闭 refusal episode 的 HIGH，并修复缺失/非 replacement binding 的空操作成功返回。真实 watcher 的 live/dead 恢复、settled/superseded 清理与 binding 拒绝完成 RED→GREEN；161 项聚焦、50 项 projector 回归及 lint/build 通过。一次推送申请最终 R4，非 APPROVED 则请求 Lead 裁定。
