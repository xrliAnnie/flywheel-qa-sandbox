# FLY-2276 — 托管报告 inline script CSP

**Status**: ✅ Implementation complete; code review r4 approved
**PR**: pending creation from `flywheel-FLY-2276`
**Date**: 2026-09-03

`publish-report` 的默认 CSP 路径现在会为可执行的 inline `<script>` 统一铸造并写入 nonce，同时注入
`script-src 'nonce-…'`，继续保留 `default-src 'none'`；带 `src` 的外链脚本仍 fail closed。发布器与
`verify-report` 共用 quote-aware HTML tag scanner 和脚本分类规则，覆盖无 `type`、空 `type`、module、
importmap、speculationrules 及 WHATWG JavaScript MIME essence（忽略 MIME 参数），并按
`script-src-elem` 覆盖优先级和 nonce 精确相等关系验证。作者已有 CSP / `__CSP_NONCE__` 约定保持兼容，
默认 CSP 下的 inline event handler 会给出改用 `addEventListener` 的显式错误。

严格 TDD 记录了各批 RED，再在 TeamLead registry/routes 112/112 与 flywheel-comm verify-report
76 passed / 1 platform skip 上转绿；flywheel-comm 全包 1793 passed / 2 skipped。`pnpm lint` 和
`pnpm -r build` 通过。精确 `pnpm test:packages:run` 已执行，仅 Core 两条依赖 Terminal.app/HiServices
的 real-osascript 宿主基线失败，其余 Core 219/221 通过；TeamLead 全包并发运行的 6 个超时在 single-worker
复跑后全部通过，剩余 1 条独立的 tmux TAB 窗口名宿主限制稳定复现。没有新增 shell test。

本地 publish-only 链路使用真实 CLI、ReportRegistry 和 HTTP 页面完成浏览器验证：标签页切换、留言提交、
localStorage 草稿及刷新后恢复均通过；HTML 中 `__CSP_NONCE__` 残留为 0，CSP nonce 与脚本 nonce 精确匹配。
这条链路的 remote deploy seam 是隔离 fake URL，不冒充 Vercel 实发。按 Lead 指示，真实 Vercel 托管验证
留给 QA 在隔离项目完成，本节点没有读取或修改生产 token / registry，也没有部署、merge 或 dispatch QA。

request-driven code review r4 在精确代码 HEAD `2f98369a4` APPROVED（gate
`bd5bdbe3-65fe-4878-b43d-739145d5d8b7`，request `30972621-f5a0-4a83-98c2-aa126babc647`）。三个 LOW
advisory 已转报 Lead：占位符语境下旧 author nonce 的一致归一化、inline event-handler allowlist 的周期更新，
以及 verifier 对手写 `script-src-elem` / `default-src` nonce fallback 的保守误报。它们均不阻塞本范围，且
不把发布器生成的 CSP 放宽。`codex:rescue` 的独立本地补充审查在读取仓库前被 macOS sandbox-exec 71
拒绝；正式 request-driven cross-family 审查已完成并作为权威 verdict。
