# FLY-2684 — Ubicloud CI canary trial

**Status**: ⏳ Pending re-review
**PR**: #1244
**Date**: 2026-09-17

Adds a bounded manual canary, source-parity guards, and atomic trial receipt
collection without switching production CI. Live Ubicloud proof and the later
one-variable production switch remain gated on founder authorization and the
post-FLY-2681 follow-up. The parity guard uses a hash-pinned historical fixture
without imposing a permanent equality constraint on the live `ci.yml`.

QA rework makes receipt totals safe across partial reruns, rejects ambiguous
local-time windows, normalizes accepted numeric offsets to UTC for the API
query, marks open runs/jobs incomplete, and strengthens result-job and
environment-dump mutation guards. The runbook now requires serialized
dispatches, records the Lead-corrected unconfirmed $60 line, and preserves the
current GitHub self-hosted billing status as explicit receipt evidence.
