# FLY-1942 — 通信层收件人、订阅与 guard 防线

**Status**: ⏳ Pending review and CI
**PR**: 本分支 draft PR；插件 fork PR 在交付回执中声明
**Date**: 2026-09-11

Mailbox send/respond 通过持久身份与统一终态策略校验收件人，缺失/终态死信区分并按发件Lead隔离通知。Codex Lead的roundtable动态订阅有TTL/cap、原子账本、恢复先于积压输入、查询与认证退订；restart guard识别可执行命令并给出匹配读数。Claude launcher注入精确own-chat频道，插件fork0.0.8按频道限制探针故障时的拒发并记录诊断。

本地lint/build通过；6个shell/guard套件通过；fork244项Bun测试、bundle与隔离rollback通过。本地完整包门R3因Vitest onTaskUpdate RPC timeout退出1，已运行断言通过但该门仍非绿。Lead c5af927d-42af-4f52-bafc-a42fe69627e2允许保留红回执进入draft/review，下游4包单fork补跑在draft创建时仍进行。精确HEAD CI绿且正式review通过才可needs_review；没有生产上线或QA回放结论。证据见同issue validation.md及各chunk记录。

本文件为创建PR前literal最后commit；此后冻结HEAD进行review/CI。

复核补充：四包指定补跑完成；edge/voice-bridge/voice-codex退出0，TeamLead的47个旧ID夹具失败已最小修正，三份测试53项回归通过。TTL变量通用QA环境清理接线补齐后helper脚本通过。Fork R1 HIGH部署marker遗漏已在988a5a4修复，244项Bun/bundle/rollback通过。当前等待forkR2、rootR1及两仓新HEAD CI；本提交继续为literal最后提交。

主仓 R1 HIGH `restart-guard-ir-fail-open` 已在085d1ccb4修复：21新增回归、完整guard334/0、installer11/0、runbook62块零命中、lint退出0。Fork988a5a4已通过R2及CI；主仓034aedfa5旧HEAD全部14项CI通过，新冻结HEAD仍需R2与CI。本文件继续作为最后提交，冻结后最终结果记入PR说明与工作流回执。

主仓 R2 HIGH coverage-gap 修复已完成：按Lead27252ba2恢复可执行stage/payload的regex第二层，保留静态引用文档负向边界。guard365/0、installer11/0、inventory5/0、runbook62零命中、lint0。新HEAD待root R3（上限）和新CI；fork988a5a4继续APPROVED+green。本文件保持literal最后提交，后续回执写PR与工作流。

Lead f9863a46授权的有界R4 tmux集合/参数修复已完成：9反例三方对照，guard387/0、installer11/0、inventory5/0、runbook62零命中、lint0。仅等待新HEAD有界R4确认及CI；若非APPROVED交Lead不开R5。fork988a5a4未变。本文件为冻结前literal最后提交。
