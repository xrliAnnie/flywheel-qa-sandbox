# FLY-2446 通用 voice 进程 — 实施里程碑
Issue: FLY-2446 (https://linear.app/geoforge3d/issue/FLY-2446)
日期: 2026-09-09
基于: ../FLY-2446-generic-voice-process/plan.md

Flywheel 锚 PR https://github.com/xrliAnnie/flywheel/pull/1140 实现独立 voice daemon、
Bridge 会话控制、mailbox 上行、TTS 下行、模式配置与增量证据账本。配对 Raya PR
https://github.com/xrliAnnie/raya/pull/71 精确头 d4e6b13a4e1aef4cd602bb80301e2686c2996c58
仅注入 voiceIntent、统一公开 unavailable 原因并带 CLI 回归；前置 #1134 / Raya #61 已合入。

主仓旧头135fe75e0 CI14/14通过，但审查86edb63f为CHANGES_REQUESTED：恢复时409
导致旧持久文件保留、启动循环失败。依Lead2b63f247/8b8055d2授权，511f7d8a0仅修复
recover终态回执处理：不可重试4xx清理旧文件，网络/408/429/5xx保留重试。
真实StateStore sweep → daemon.run回归先409失败，修后进入idle、移除文件、不重发回执。
新头需重新取得CI14/14与主仓APPROVED；Raya新审查须待主仓APPROVED后串行登记。

本轮完整voice-codex122项、受影响TeamLead60项、全仓lint/build均exit0。
完整package gate exit1：claude-runner46文件/1138通过/2跳过，零失败断言，但发生
已接受的onTaskUpdate RPC超时；后续包未运行，不能称全仓绿。旧轮完整TeamLead
失败仍按PR正文披露。Raya精确头lint/typecheck/build/157测试/跨仓smoke五项均exit0；
它无CI lane，依Lead2cca6076以这些记录替代，未新增workflow。

QA驱动19项hermetic测试已交付，真房执行归QA，当前依赖FLY-2455 bootstrap，G NOT_RUN。
两场会议波形/SQL账/真实selectMeetingTranscript、RG配置对照、平台credit记录均待QA；
未用事件日志或hermetic测试替代真房验收。零新增realtime用量，零生产配置/数据库/
launchd修改，无QA dispatch、merge或deploy。Raya仓merge不等于deploy；生产checkout/
brain重启/preflight由班车完成，以~/.flywheel/raya/deploy-receipt.json为准。

精确命令、日志、裁定、七条主仓与六条Raya advisory、诚实边界与回滚见PR正文和
implementation-evidence.md。该里程碑是实现证据，正式审查、CI与needs_review交接仍待完成。

本轮更新：Raya d4e6b13 的正式审查1bec58a2已APPROVED，保持冻结。
主仓f44088d31的CI34402118354为14/14，但审查24984c74提出两个HIGH：
KeepAlive检查不接受SuccessfulExit:false，以及wrapper测试继承宿主配置。
Lead a1c77138授权仅修两项，738df8e16已完成各自隔离RED/GREEN；plist未改，
voice重启前后用真实predicate核验仅SuccessfulExit:false，Bridge默认布尔true不变。
测试每次env-i白名单启动，假宿主dotenv/变量不得泄漏，告警只写本地记录器。
wrapper31/31（macOS原生和Linux解析适配）、supervisor9/9、Bridge3/3通过；
lint/build exit0。完整package gate再次exit1，仅同类已接受RPC超时，1138断言通过、
2跳过，后续包未执行；不是全仓绿。新头正式ROOT审查与CI仍待取得。

QA claim 989 退回后的 implement attempt 2（2026-09-09）：
Lead609a1f3c限定修复9ac062d57：会议缺省开启、显式false仍拒绝、RG仍显式opt-in；
运维入口先mode/huddle再evidence目录；两个真实DB续租守卫测试分别杀死token/null
JS守卫删除变异，StateStore生产实现不变。HTTP七组对照与校验阶梯完成红绿回归。
构建后聚焦32/32；lint/build exit0；完整包门禁exit1，claude-runner1138passed/2skipped、
零失败断言、唯一onTaskUpdate RPC超时，依既有Leadb5f3a669保留accepted artifact，
后续包未跑，不能称全仓绿。Raya d4e6b13未动；生产registry/plist/wrapper未改。
本提交为本轮literal last commit；一次普通push后冻结，等待新ROOT审查及CI14/14。
旧ef7786061的APPROVED/CI不代表QA通过，不能重放旧needs_review。真房验证仍归QA。
