# FLY-2121 — workflow 稳定命名与后端展示名

**Status**: ⏳ Pending ship
**PR**: #984
**Date**: 2026-08-29

用两层 agent registry 把 workflow graph、执行 node 与 human-readable label 分开:工程设计节点、产品设计节点和产品设计 flow 获得互不冲突的稳定身份,前端只消费后端 label。catalog cutover 以原子迁移更新新 run,历史 run/event 继续由永久 decoder 可读;founder/custom 模板原样保留,不可执行风险通过启动审计和 typed 409 显式暴露。QA 红线返工删除 `LEGACY_ROSTER_ROLE_BY_NODE` 整层兼容映射:保留未激活项目的 roster loader 分流,但只按 canonical node id 直接查找;Flywheel 自身 roster 同步迁到 `eng_design` / `product_design` / `general` 等稳定 id,且所有路径指向现存 `agents/nodes/*.md`。唯一外部承重数据由 [belle-workspace #4](https://github.com/xrliAnnie/belle-workspace/pull/4) 先行迁移 `generic`→`general`,#984 后合;Lead 以 ruling `78d5cb04` 固化此硬顺序,不允许恢复 alias。旧名守卫补上 canonical-id→retired-role 映射阴性对照,并覆盖 shipped `.lead` prompt,删除最后一处 `product-designer-executor` 活跃提示。最终 R6 在 exact head `1e6bc7178` APPROVED;lint、22 包 topo build、15 条 focused TS 与 12 条 shell contract 全绿,package 总门仅被无 GUI 会话下既有的两条 real Terminal.app/osascript 测试截停(其余 core 219 条通过)。
