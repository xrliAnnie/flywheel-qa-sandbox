# FLY-2520 — Claude 账号到期排序

**Status**: ⏳ Pending ship
**PR**: https://github.com/xrliAnnie/flywheel/pull/1166
**Date**: 2026-09-12

Claude 候选以 reset/retirement 较早时间排序，过期及无效日期账号不可选。活动号到期复用 account_dead 切换和失败节流；到期前 warning 使用既有收据去重。容量/panorama/巡逻行显示 PT 到期日期，revive 窗口受来源退休期限限制。

本地 389 定向测试、29 sender 脚本检查、lint 与全仓 build 通过。精确 HEAD CI、代码评审及 QA 交接另行确认；生产配置和 monitor 重启由 Lead/QA 执行。

Implement@2：退休预警、panorama 和容量消息复用切号器对齐表，每账号独立块，首行带邮箱/到期日期。486 定向测试、29 sender 检查、lint/build 通过。529 两条真实消息与回读已记录于 implementation.md；截图由 Lead/QA 按明确裁定补拍。排序与切号规则保持不变。

R2：按 Lead 裁定保留 capacity JSON 脱敏合同，容量块只显示账号名，邮箱仅用于直接读取 store 的告警。另经明确授权修复两条既有退休健康记账 HIGH（lastPollAt 与 monitor-only 调度/config 告警）。533 定向测试与 lint/build 通过；R1 advisory 仅归档。最终精确头评审/CI 仍须通过后 needs_review。
