# FLY-2401 — 激活 Raya summary 读侧

**Status**: ⏳ Implementation complete; PR CI pending
**PR**: pending creation
**Date**: 2026-09-06

## 交付

- 新增 read-only proposal renderer、三键 `raya.env` 原子 transition/verify/rollback、pre/active
  activation state verifier，以及首轮 DB/merge receipt/GitHub/memory/Discord 五面验收器。
- 把 FLY-2131 B/C 激活要求落成逐步 operator checklist：每步均列 owner、写入面、验证命令、期望
  输出、停止线与 R1–R5 回滚；另附基于只读生产审计的 exact registry/env 变更提案和输入 hash。
- 清单顶部固定 Lead 裁定的 P1–P4：production 追平后重渲染；等待 FLY-2404 shared-business-truth
  helper；17 席 forward/rollback 只搭 updater 舰队班车；Lead 执行生产步骤，QA 只验包与 dry-run。
- `~/.codex-raya/auth.json` 必须是指向 mode-0600 canonical `~/.codex/auth.json` 的 absolute
  symlink；copied auth、相对 link、错误目标或错误 truth mode 均 fail closed。实现节点没有修改
  `projects.json`、`~/.flywheel/**`、launchd、Codex home、Discord 或任何其它生产状态。

## TDD、验证与评审

- 严格 RED→GREEN 覆盖 renderer、env 原子切换、pre/active 状态、共享 auth truth、完整 JSON
  失败报告、首轮五面证据、Discord redirect/token 防护与中英文 round report；最终聚焦套件
  66/66 通过，并纳入 Linux CI。
- `pnpm lint` 与 `pnpm -r build` 通过。真实串行 package gate 仅排除会驱动
  Terminal.app/osascript 的 `packages/core/test/tmux-viewer.macos.test.ts`；本机 config 完成 771/772，
  唯一未完成项是既有 FLY-1981 全仓文件扫描在高负载下超过固定 15 秒。该文件用单 fork、诊断
  timeout 复跑 11/11 通过；完整结论交由 exact-head Linux PR CI。
- Request-driven code review round 3 的唯一 HIGH 指出 checklist 用 `.mode` 读取实际 `.phase`
  输出；新增文档化 gate RED 后修复。Round 4 在 `9876c3bdf` APPROVED。按 Lead 后续裁定，stat
  helper、FLY-2404 shipped helper 路径注入、pre-report 异常捕获三项小 advisory 合入同一 amend
  `6468fc8d8`，不再开第三轮。
- 结构性 advisories 作为后续项交 Lead：transition post-backup 恢复、R3 rollback 顺序、Discord
  structured count contract、intermediate-directory symlink 边界；它们不改变本单 hard review
  verdict 或禁止生产直改的边界。

## 生产交接

当前提案仍是 NON-EXECUTABLE：必须先满足 P1/P2，再由 Lead 在 P3 获批 bus 窗口重渲染、dry-run、
审批并执行。激活后的最终产品验收仍是下一真实 6h 边界出现唯一 delivered
`summary_absorption_round`，且 Raya 通过 summaries-only 窄例外 merge 至少一张自己的 summary PR，
由五面 verifier 绑定真实 Discord 收据。实现节点不 dispatch QA、不 merge、不 deploy、不重启服务。
