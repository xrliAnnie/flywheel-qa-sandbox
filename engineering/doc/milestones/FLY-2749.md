# FLY-2749 — 纯通知停止唤醒 Lead

**Status**: ⏳ Implementation complete; exact-head review and PR CI pending
**PR**: pending creation from this milestone commit
**Date**: 2026-09-18

修复既有 `lead_token_savings` 的实际漏口，不新增合批队列，也不改变 Lead 模型窗口、规则/记忆或用量页面。可信普通阶段、已有 owner 的 review/PR 阶段、已注册 session start、REVIEW gate，以及带 exact completion declaration 的 runner-stop `done` 现在沿用 `audit_only`：保留逐条权威记录，但不单独进入 Lead model adapter。flag OFF/读取失败、非 REVIEW gate、普通 ASK、blocked stop、`session_failed` 与未知证据继续即时走原 model 路径；实现位于共享 Bridge/CommDB 层，对所有 Lead 和 vendor 一致。

CommDB 增量 schema 为旧行默认 `model`，owner-fenced reclassification 只发生在 adapter handoff 前；fresh 混合批次中 quiet member 被移出而 urgent member同轮照常投递。REVIEW 问题仍 pending、可由 reviewer 回答，抑制本身不伪造 response 或 `report_ack`。两个 Lead claim 入口、queue head/window、in-flight limit、deliverable count 与 partial indexes 均过滤 `audit_only`；未新建 notification relay，沿用既有 lifecycle/disposition receipt 边界。

验证：mailbox 定向 45/45、Bridge/Teamlead 定向 272/272 通过；`pnpm lint` 零 error（未触及文件仍有既有 advisory），`pnpm -r build` 24 个 workspace package全绿。冻结 usage baseline 用同一 `measure-usage.py` 重放逐字节一致。未运行 Lead 明确禁止的 `pnpm test:packages:run`；本节点未部署、未做 founder/ASK/failure 真机探针，也不声称上线后连续 24h 节省。相同统计脚本的 after 输出、紧急事件延迟与两 vendor model-call=0 联结由 frozen-head QA/上线后观察完成。11 条非阻断 design advisory 原样保存在 `engineering/doc/FLY-2749-notification-suppression/review-followups.md`，随 PR Follow-ups 交接。
