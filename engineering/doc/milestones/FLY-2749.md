# FLY-2749 — 纯通知停止唤醒 Lead

**Status**: ✅ Lead-required review revision complete; candidate frozen for renewed exact-head code review and CI
**PR**: #1275
**Date**: 2026-09-18

修复既有 `lead_token_savings` 的实际漏口，不新增合批队列，也不改变 Lead 模型窗口、规则/记忆或用量页面。可信普通阶段、已有 owner 的 review/PR 阶段、REVIEW gate，以及带 exact completion declaration 的 runner-stop `done` 现在沿用 `audit_only`：保留逐条权威记录，但不单独进入 Lead model adapter。flag OFF/读取失败、非 REVIEW gate、普通 ASK、blocked stop、`session_failed` 与未知证据继续即时走原 model 路径；实现位于共享 Bridge/CommDB 层，对所有 Lead 和 vendor 一致。`session_started` 明确保留 immediate：它是 Lead 在同一轮取得 issue 与新 execution id、向替身发送交接信的唯一现有信号，不能作为纯通知抑制。

CommDB 增量 schema 为旧行默认 `model`，owner-fenced reclassification 只发生在 adapter handoff 前；fresh 混合批次中 quiet member 被移出而 urgent member同轮照常投递。REVIEW 问题仍 pending、可由 reviewer 回答，抑制本身不伪造 response 或 `report_ack`。两个 Lead claim 入口、queue head/window、in-flight limit、deliverable count 与 partial indexes 均过滤 `audit_only`；未新建 notification relay，沿用既有 lifecycle/disposition receipt 边界。

验证：mailbox 定向 45/45、原 Bridge/Teamlead 定向 272/272 通过；CI 暴露 `session_started` 交接缺口后，纠偏相关 4 文件 138/138 通过。`d97702f71` 的精确 HEAD code review 已 APPROVED（3 MEDIUM、5 LOW advisory 已转报）；额度恢复后的 CI 只在 teamlead shard 暴露两项同源 evidence-contract drift：FLY-2139 query-plan generated block 与 FLY-2567 `db.ts` compatibility manifest。最小同步后两项聚焦回归 19/19 通过，业务代码未变。

2026-09-20 将 `origin/main@a62456f76` 技术合并到分支后，按合并文件重算 compatibility digest，并在源代码等价头重新执行 5 组受影响回归：144/144、45/45、94/94、178/178、11/11；`pnpm lint` exit 0（未触及文件仍有既有 advisory），`pnpm -r build` 的 24 个可构建 workspace package 全绿，branch diff whitespace 检查通过。完整命令与边界见 `engineering/doc/FLY-2749-notification-suppression/validation.md`。这些是定向本地证据，不是 aggregate CI；未运行 Lead handoff 明确留给 frozen-head QA 的 `pnpm test:packages:run`，也未请求 `ci-full ensure`。

同日 `ceea9d207` 的精确 HEAD 代码评审为 APPROVED，但 Lead 要求交接前修掉两条 advisory。修订后，review stage 只有同一 source event 对应的真实 reviewer instruction 才能生成 owner proof；missing-plan correction、skip、裸 `code_review` 和证据读取失败均保持 model。audit-only REVIEW row 可在真实 reviewer response 后进入业务终态，但 compatibility projection 不再把该时间伪装成 `read_at` / `delivered_at`。红侧分别命中错误分类和伪投递时间；绿侧 producer 回归 132/132、CommDB projection/schema 98/98，扩展定向矩阵 171/171、142/142、131/131。其余六条 advisory 明确保留在 `review-followups.md`，没有扩大本次修订范围。按 Lead 指令，本修订只跑定向测试；新候选必须重新获得 exact-head review 与 CI，旧 `ceea9d207` 的回执不能复用。

冻结 usage baseline 用同一 `measure-usage.py` 重放逐字节一致。本节点未部署、未做 founder/ASK/failure 真机探针，也不声称上线后连续 24h 节省。相同统计脚本的 after 输出、紧急事件延迟与两 vendor model-call=0 联结由 frozen-head QA/上线后观察完成。11 条非阻断 design advisory 原样保存在 `engineering/doc/FLY-2749-notification-suppression/review-followups.md`，随 PR Follow-ups 交接。
