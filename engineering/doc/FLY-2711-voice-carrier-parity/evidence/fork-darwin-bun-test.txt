FLY-2711 fork macOS verification
Date: 2026-09-24 America/Los_Angeles
Platform: Darwin 25.6.0 arm64
Bun: 1.3.11 (af24e281)

Command:
  bun test voice-self-filter-socket.test.ts

Result:
  exit_code=0
  19 pass
  0 fail
  214 expect() calls
  Ran 19 tests across 1 file.

Darwin-only tests executed (not skipped):
  PASS reclaims a crashed owner only after the kernel releases its lock
  PASS keeps a live lock owner and hands over on the next retry
  PASS never lets a closing owner unlink its successor across 50 handovers
  PASS refuses a symlinked kernel lock file

Other socket contract checks passed:
  PASS unsafe public paths remain untouched and release the fake lock
  PASS strict private-name cleanup leaves near-miss and unrelated sockets alone
  PASS external public-path deletion rebinds on the 30-second health callback
  PASS linux injection fails closed once without acquiring or observing
  PASS 0600, HMAC, malformed input, 4097-byte rejection, and Node newline framing

Package command after C2:
  bun test

Package result:
  256 pass
  0 fail
  843 expect() calls
  Ran 256 tests across 16 files.

Final package command after golden-vector addition:
  bun test

Final package result:
  257 pass
  0 fail
  845 expect() calls
  Ran 257 tests across 17 files.

Bundle command:
  bun build server.ts --target=bun --outfile=/private/tmp/fly2711-discord-server.js

Bundle result:
  Bundled 744 modules; output 2.67 MB.
