flowchart TB
    subgraph OLD["现状:为什么六步只做两步"]
        direction TB
        O1["步1/2: 给了具体命令"] -->|"100% 被执行"| OK1["✅ 每次都做"]
        O2["步3/4: 只有抽象名词<br/>(库里找不到同名对象)"] -->|"只能靠猜"| SKIP["❌ 静默跳过"]
        O3["步5: 命令有了但没定范围<br/>(tick 说「你名下」)"] --> SKIP
        SKIP --> INVIS["没有产出物要求<br/>→ 少做了没人发现<br/>(包括执行者自己)"]
    end
    OLD --> NEW
    subgraph NEW["修复:四个合同"]
        direction TB
        N1["① 每步一条可直接执行的命令<br/>(账本查询收进一个 CI 测过的脚本)"]
        N2["② 范围写死:检测=整机<br/>处置=只动自己名下"]
        N3["③ 每次巡检必产一份六步报告文件<br/>逐步 OK / FINDING / 执行不了"]
        N4["④ 「执行不了」出口:<br/>报告留痕 + 自动建工程 issue"]
        N1 --> N3
        N2 --> N3
        N3 --> N4
    end
