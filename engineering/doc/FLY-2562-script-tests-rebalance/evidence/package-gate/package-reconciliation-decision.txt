Ruling: per-file reconciliation of the two unchanged full runs IS the authorized completion evidence — a single clean full-package run is NOT required. Assemble the package receipt as the per-file union: every one of the 1116 files passed in at least one full unchanged run (archive 4/4 in the recovery run, reply-guard 32/32 in the original run and in the focused recovery), the two single-file reds are each contradicted by a passing run of the same unchanged file on the same head, and both are the known host-contention shape (reply-guard got an HTML page instead of JSON = a Bridge/port answered by something else under a 15-body host, not a code defect). Mark the receipt complete=true with that reconciliation note, keep both raw receipts and the onTaskUpdate errors attached unchanged, cite exact-head CI 15/15 as the authority, and complete --route needs_review PR #1201. Do not start another aggregate or package rerun and do not edit any test.
