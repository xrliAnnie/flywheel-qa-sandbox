---
issue: FLY-1062
phase: implement
phaseCursor: 8/9
updated: 2026-07-11T21:49:05.520Z
nextStep: "Codex design APPROVED R7 (7 rounds; all 6 threat scenarios closed; no
  §3 design gaps, remaining = #558 impl + broker PR). Now step ② = rework #558
  code to approved form: beta schedule+deterministic
  releaseId+dedup+pre-activation guard; promote prepare-only (delete commit
  job); delete shell-publish.yml; structure lint to new form; runbook to broker
  form. Then re-run Codex CODE review → approve gate → stop."
chunks: []
pointers: {}
---

# FLY-1062 progress
**phase**: implement (8/9)
**next**: Codex design APPROVED R7 (7 rounds; all 6 threat scenarios closed; no §3 design gaps, remaining = #558 impl + broker PR). Now step ② = rework #558 code to approved form: beta schedule+deterministic releaseId+dedup+pre-activation guard; promote prepare-only (delete commit job); delete shell-publish.yml; structure lint to new form; runbook to broker form. Then re-run Codex CODE review → approve gate → stop.
