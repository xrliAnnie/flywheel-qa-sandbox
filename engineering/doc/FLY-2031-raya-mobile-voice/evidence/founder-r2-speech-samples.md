# FLY-2031 八条念读文字稿 — QA 前文字审阅
Issue: FLY-2031 (https://linear.app/geoforge3d/issue/FLY-2031/rayav3-随身语音b常开流-念读筛选-用嘴批-ship)
日期: 2026-08-29
基于: founder-r1-remediation-plan.md

状态：R2 已 APPROVED；以下两处必改已应用。按裁定不再做第三轮文字审，验证通过后直接进入 QA bot 真实 `voice-test-2` 自测；QA PASS 前不得请 Founder 进房。

## 样例一：文字先审

审计栏：内部 fixture id `founder-r2-text-review`；decision；`idMatches=0`。

- what：下一轮语音前，所有要念的话会先整理成文字给工程负责人检查。
- why：这样能先发现听不懂、重复和夹带术语的问题，不再让你进房后才踩坑。
- next：你只要决定，以后是否一直保留这一步。
- 首句判据：只听首句就知道这是「语音稿先转文字给工程负责人检查」。

最终 spoken text：

> 下一轮语音前，所有要念的话会先整理成文字给工程负责人检查。这样能先发现听不懂、重复和夹带术语的问题，不再让你进房后才踩坑。你只要决定，以后是否一直保留这一步。后面还有 5 件，我接着说。

## 样例二：QA bot 先验

审计栏：内部 fixture id `founder-r2-qa-first`；decision；`idMatches=0`。

- what：每次请你进语音房前，测试机器人会先在同一个语音房走完整套流程。
- why：这样能先验证不会重复、可以插嘴，也不会把内部黑话念给你听。
- next：你只要决定，以后是否一直让测试机器人先验一轮。
- 首句判据：只听首句就知道这是「Founder 进房前由测试机器人在同一个语音房先验」。

最终 spoken text：

> 每次请你进语音房前，测试机器人会先在同一个语音房走完整套流程。这样能先验证不会重复、可以插嘴，也不会把内部黑话念给你听。你只要决定，以后是否一直让测试机器人先验一轮。后面还有 4 件，我接着说。

## 样例三：真实 report — 已按决定发布

审计栏：内部 fixture id `founder-r2-merged-result`；report；`idMatches=0`。

- what：你上次同意发布的那项改动，已经放进正式版本了。
- why：现在告诉你，是让你知道团队已经按你的决定往前走。
- next：这条只是同步结果，不用你再决定。
- 首句判据：只听首句就知道「上次批准的改动已经发布」。

最终 spoken text：

> 你上次同意发布的那项改动，已经放进正式版本了。现在告诉你，是让你知道团队已经按你的决定往前走。这条只是同步结果，不用你再决定。后面还有 3 件，我接着说。

## 样例四：真实 report — 语音半断连

审计栏：内部 fixture id `founder-r2-room-disconnected`；report；`idMatches=0`。

- what：我和你的连线断了一半：我还能说话，但听不见你。
- why：所以你现在说什么我都收不到。
- next：想继续的话，重新叫我进房就行。
- 首句判据：只听首句就知道「Raya 还能播报，但当前收不到 Founder 的声音」。

最终 spoken text：

> 我和你的连线断了一半：我还能说话，但听不见你。所以你现在说什么我都收不到。想继续的话，重新叫我进房就行。后面还有 2 件，我接着说。

## 样例五：重复上限

审计栏：内部 fixture id `founder-r2-repeat-cap`；report；`idMatches=0`。

- what：同一件事如果没念成功，只会再试一次，之后先停下来。
- why：这样不会在同一场里反复念同一条，也不会挡住后面的事。
- next：这条只是告诉你已经补好，不用你决定。
- 首句判据：只听首句就知道「失败只再试一次，之后停止」。

最终 spoken text：

> 同一件事如果没念成功，只会再试一次，之后先停下来。这样不会在同一场里反复念同一条，也不会挡住后面的事。这条只是告诉你已经补好，不用你决定。后面还有 1 件，我接着说。

## 样例六：不再主动报平安

审计栏：内部 fixture id `founder-r2-no-liveness`；report；`idMatches=0`。

- what：语音不会再为了证明自己在线而主动报平安。
- why：只有真的有事找你时才会开口，安静的时候就保持安静。
- next：这项改动已经完成，不用你决定。
- 首句判据：只听首句就知道「Raya 不会再主动报平安」。

最终 spoken text：

> 语音不会再为了证明自己在线而主动报平安。只有真的有事找你时才会开口，安静的时候就保持安静。这项改动已经完成，不用你决定。

## 样例七：真实 ship 播报

审计栏：决策类；数据只来自已核验的 issue context 与 gate binding；issue identifier 末句一次；PR 用中文自然读法；不使用不可信 inbox 正文。

- what：Tadashi 想请你决定，要不要把「Raya voice」这项改动送进发布。
- why：它已经走到需要 Founder 最后确认的阶段。
- next：批准说「确认」，不批准说「不批」；末句自然核对单号与 PR。
- 首句判据：只听首句就知道「哪项改动、谁来请我、要我决定是否发布」。

最终 spoken text：

> Tadashi 想请你决定，要不要把「Raya voice」这项改动送进发布。现在找你，是因为它已经走到需要你最后确认的门口。你要批准就说确认，不批准就说不批。最后跟你核一下，是 FLY-2031 这一单、PR 四十二，对吧？

## 样例八：真实 relay 念回

审计栏：动作类；Founder-attributed 原话逐字保留；`actionId` 不出现；末句在引号外明确核对目标名，逐字原话内也保留同一目标与内容；确认后才真的发消息，取消则不发。

- what：要转达的自然意思是「可以开始联调」，与末句逐字原话是同一件事。
- why：确认后会真的把消息发给对方，所以先念回核对。
- next：不对就说「取消」；末句逐字保留 Founder 原话并核目标名。
- 首句判据：只听前三句就知道「要转达什么、确认后会真的发送、如何取消」。

最终 spoken text：

> 你刚才要我转达一条消息。内容是「可以开始联调」。确认后我会真的把消息发给对方；如果不对就说取消，我就不发。最后逐字跟你核一下：目标是 Tadashi，你说的是：「请告诉 Tadashi，可以开始联调 FLY-2031」。
