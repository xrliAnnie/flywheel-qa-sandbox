flowchart LR
    subgraph holder["发货闸门(数据库里已有)"]
        H["workflow_gate_holder<br/>一条『等发货』的登记:<br/>状态 + 冻结的代码指纹"]
    end

    subgraph ledger["持久账本(本单新增,零建表:<br/>复用既有事件表)"]
        M["死胡同标记<br/>键 = 状态+指纹+仓库+PR号<br/>任一变化自动失效重查"]
        O["『已合并』观察记录<br/>可从缺指纹升级为带指纹"]
        Q["隔离标记<br/>账本自相矛盾时全面停用<br/>并发严重告警"]
    end

    subgraph probe["探测器(内存,重启即忘)"]
        P["节流状态:缓存 / 退避 /<br/>每分钟预算 / 公平队列"]
    end

    H -->|"每秒巡检读"| P
    P -->|"结论落盘"| M
    P -->|"结论落盘"| O
    O -->|"重启后回灌<br/>(零 GraphQL)"| P
    M -->|"命中即摘除候选"| H
    O -.->|"驱动不可逆动作前<br/>先用 REST 复核一次"| Q
