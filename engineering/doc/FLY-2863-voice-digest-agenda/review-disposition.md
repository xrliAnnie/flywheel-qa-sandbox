# FLY-2863 语音播报只播三类、Lead 消化后对话 — 设计评审记录
Issue: FLY-2863 (https://linear.app/geoforge3d/issue/FLY-2863/语音v7-播报内容只播三类lead-主动说的-待批-受阻lead-消化后用对话口吻一件一件跟她过内部排队10)
日期: 2026-09-24
基于: plan.md

## 结论

Codex 设计评审共 3 轮（模型 `gpt-6-astra`，effort `xhigh`，thread `01a0d5c0-f631-7da3-9f16-9739663d0565`），第 3 轮 **APPROVED**，阻断项为 0。
被批准的 plan.md blob 是 `d457a1632d47c8ede39efe51699094561198cbf3`（提交 `e0eab3754`）。
这次批准针对的是设计施工合同，**不代表**产品实现、真房 QA 或声线试听已经完成。

## 各轮处置

| 轮次 | 阻断项 | 处置 |
|---|---|---|
| R1 | 8 条：「要你答」默认播出；主动 agenda 请求没有合法来源；前台抢答和双投；close 误用旧收件箱 ack；过期结果会推进队列；来源失败时误关件；口头批准和关件语义冲突；兜底句虚称已发送 | 全部接受。对应 plan §2.1 R1-1/R1-6，§3.1 R1-2，§3.2 R1-5/R1-7，§3.4 R1-8，§4.4 R-T1..R-T4，§4.8 |
| R1 同轮 | Lead 补充（来自 2798 QA 真房）：进房不回放历史收件箱 | 新增 plan §2.2 |
| R2 | 2 条：`agendaBinding` 与「用户 handoff 校验器不变」冲突；切件后迟到回话没有出口 | 全部接受。§4.4 R-T2 改走服务端关联表，新增 R-T5 和 §3.2 的 reply 矩阵 |
| R3 | 0 条 | APPROVED |

## 留给 code run 的非阻断项

1. R1-9：实现主频道 founder 回复水位；urgent 元数据接到真实的主频道发送路径，并用发送成功的 messageId 关联；集中定义语音回显前缀常量。
2. R1-10：待批和受阻要有持久的 episode 身份，重启或普通更新不换 key，真实退出再进入要换 key；受阻的候选集要覆盖标题能显示受阻的全部单；item、order、disposition 贯穿 CLI、typed result 和 SQLite；B 删掉状态 prompt 时同步更新 manifest 和 digest。
3. R2-4：上线基线持久化后不能随重启重置；用固定时钟测水位、基线、24 小时三处边界；窗口过期不能说成「已处理」。
4. R3-1：Q7 里「Lead 已经带回当前件」用绑定当前请求和 item 的结构化结果标志表示，⛔ 不从 say 的文本去猜；测试「已带回就不再补播，未带回且没有新话才补一次」。
