stateDiagram-v2
    direction LR
    [*] --> 零租约: 第一次用到才建家
    零租约 --> 一个租约: 单 A 开跑
    一个租约 --> 两个租约: 单 B 同时开跑(臂沿用家里的)
    两个租约 --> 一个租约: A 结束(令牌保留)
    一个租约 --> 零租约: B 结束(剥掉 GH_TOKEN)
    零租约 --> 零租约: 删家请求 ⇒ 拒绝并报错
    note right of 两个租约
        Bridge 重启时的清扫:
        不在活会话集合里的租约被删
    end note
