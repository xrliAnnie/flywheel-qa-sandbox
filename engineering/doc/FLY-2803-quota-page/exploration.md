# FLY-2803 额度页页面改版 — 探索
Issue: FLY-2803 (https://linear.app/geoforge3d/issue/FLY-2803/额度页页面-页面一张单改完spec-e1-e19呈现改版分组排序在用号染绿进度条时间格式-订阅到期显示与手填记确认人时间-e19)
日期: 2026-09-23
基于: 无

## 目标与权限

一张页面单完成 E1–E19：两表按周额度分组、组内周重置日升序、在用号始终整行绿色、分维度进度条、订阅到期有确认来源。设计节点只交设计与报告，不实现、不探测真实账号、不登录、不派发、不 ship。

## 当前权威

- 任务明确把 FLY-2806 并入本单，FLY-2807 负责数据层；不重新拆单。
- `product/doc/FLY-2777-quota-page-redesign/spec.md` 优先于 mockup/旧 plan；#1287 已合入，head `7b838bc4e297c7853087d2adf9f1cbc2ed0aa4d2`。
- 当前 main `fef2dd43d535517cd55c39045efcf231fa4bd8e3` 含 FLY-2762 `068493b2b`（#1294）。设计开始即以此为基线。
- #1301 于本次核查仍 OPEN，head `59cd03afe36bb0e1b9893d16ec6ab4f488a151bd`。进入实现必须重新核查状态。
- E8 同一行残留“不占背景色”的旧文字；以后写的整行绿底和本任务明令为准。E18 旧稿日期均非授权输入，生产不预填 personal1/business/Codex 名单。
- E20–E23 的取数修复在 2807；本单保留其卡明细与降级语义。E4 的兑换卡列同时是本单展示合同，不能因依赖没合入而永久省略。

## 选择

沿用现有只读额度页面与共享视图，新增页面专用投影（把已有资料整理成页面需要的行），不改轮转策略。手填使用独立 JSON 字段输入文件；Lead 收到 founder 的 FLY-2792 回答后维护。无需新网页写接口，也不把确认人资料堆进每个数据格。

拒绝：重建一套 dashboard；只修 CSS 不改分组；按最早恢复时间/5h/Fable/档位排序；把旧 retiresAt 当取消证据；为了页面删除 token 状态列；用示例数据上线。

## 已知语义

1. 周 96%、Fable 100% 仍在上组；5h 满不改变周分组。
2. 无周数在“本轮读数不可用”组；保留其它可用格子和真实错误提示，不涂灰整行。有效旧值仍按其值分组，过去重置日仍正常升序。
3. active 只改变视觉，不置顶；打满仍在下组且红色进度条可见。
4. 2807 已取消且 usage 非 ok 的行保留“已取消”并不复活历史百分比；若已取消但额度仍有效，按真实周数分组。
5. 机器 profile active 仅证明当前订阅可用，不证明自动续订未取消；手填取消与机器活跃可同时成立。

## 待答与推进

已问 Lead `d7cba6c2-2a7b-4d03-a5ed-016d37ed532c`：缺值组、手填面、2792有无新裁定。非阻塞；按注入任务完成设计，不等待新 founder 确认。设计评审必须 effective APPROVED 后发布最终 HTML。
