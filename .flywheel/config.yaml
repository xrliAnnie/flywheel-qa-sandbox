# Generated by scripts/test-deploy.sh (FLY-115 v1.24.3) — sandbox test config.
# DO NOT commit to xrliAnnie/flywheel-qa-sandbox; teardown wipes HOST_REPO.
project: test-slot-4

linear:
  team_id: FLY

runners:
  default: claude
  available:
    claude:
      type: claude
      model: sonnet

teams:
  - name: default
    orchestrators:
      - type: dag
        runner: claude

decision_layer:
  autonomy_level: advisor
  escalation_channel: discord

checkpoints:
  approve_to_ship:
    enabled: true
    timeout_ms: 14400000     # 4h (FLY-159: ConfigLoader floor; anything lower is warn+raised)
    timeout_behavior: fail-close

# FLY-1466 QA baseline fix: canonical feature switches read directly by the
# edge-worker dispatcher contract tests. Added at the sandbox-main baseline so
# fixture PRs stay clean (Markdown-only) while CI passes. (Tadashi-approved.)
roles:
  runner:
    backend: claude-tmux
    model: claude-opus-4-8[1m]

qa:
  auto: true
  skip_labels: [docs, chore]

doc_flow:
  enabled: true
  default_department: engineering

founder_milestone_report:
  enabled: true
  milestones: [failed, blocked]
