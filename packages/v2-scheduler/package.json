{
	"name": "flywheel-v2-scheduler",
	"version": "0.1.0",
	"description": "Bounded level-triggered scheduler for Flywheel v2",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"bin": {
		"flywheel-v2-scheduler-once": "./dist/cli.js"
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc",
		"test": "pnpm run build && vitest run",
		"test:run": "pnpm run build && vitest run",
		"test:watch": "vitest --watch",
		"typecheck": "pnpm run build"
	},
	"dependencies": {
		"flywheel-v2-kernel": "workspace:*"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"better-sqlite3": "^12.8.0",
		"typescript": "^5.3.3",
		"vitest": "^3.1.4"
	},
	"publishConfig": {
		"access": "public"
	}
}
