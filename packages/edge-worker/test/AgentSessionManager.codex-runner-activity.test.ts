import { describe, it } from "vitest";

// TODO: CodexRunner removed in Phase 1 (codex-runner package deleted)
// Re-enable when multi-runner support is added back
describe.skip("AgentSessionManager - Codex tool activity mapping", () => {
	it("creates Linear action entries for Codex file_change events", () => {
		// Skipped: CodexRunner not available in Phase 1
	});
});
