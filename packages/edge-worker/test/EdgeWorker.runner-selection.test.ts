import { describe, it } from "vitest";

// TODO: Multi-runner selection removed in Phase 1 (codex-runner, cursor-runner, gemini-runner packages deleted)
// Only ClaudeRunner is supported. Re-enable when multi-runner support is added back.
describe.skip("EdgeWorker - Runner Selection Based on Labels", () => {
	it("placeholder", () => {});
});
