{
	"name": "flywheel-voice-core",
	"version": "0.1.0",
	"description": "FLY-543 pluggable voice skill core — voice abstraction (announce = speech-out via Edge TTS) behind one VoiceBackend contract with capability flags; converse backends (voice-codex) plug into the same registry. Shared headphone-mode logic, transcript sinks and the secret scrubber. POC CLI: flywheel-voice-poc say.",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"bin": {
		"flywheel-voice-poc": "dist/cli.js"
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "node ../../scripts/lib/remove-retired-dist.mjs . && tsc",
		"dev": "tsc --watch",
		"test": "vitest run",
		"test:watch": "vitest --watch",
		"test:run": "vitest run",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"flywheel-config": "workspace:*"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.3.3",
		"vitest": "^3.1.4"
	},
	"publishConfig": {
		"access": "public"
	}
}
