{
	"name": "flywheel-v2-engine",
	"version": "0.1.0",
	"description": "Flywheel v2 serial mailbox consumption engine",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc",
		"test": "pnpm run build && tsc -p tsconfig.public-test.json && vitest run",
		"test:watch": "vitest --watch",
		"test:run": "pnpm run build && tsc -p tsconfig.public-test.json && vitest run",
		"typecheck": "pnpm run build && tsc -p tsconfig.public-test.json"
	},
	"dependencies": {
		"flywheel-agent-team-transport": "workspace:*",
		"flywheel-claude-runner": "workspace:*",
		"flywheel-v2-actions": "workspace:*",
		"flywheel-v2-kernel": "workspace:*"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"better-sqlite3": "^12.8.0",
		"typescript": "^5.3.3",
		"vitest": "^3.1.4"
	},
	"publishConfig": {
		"access": "public"
	}
}
