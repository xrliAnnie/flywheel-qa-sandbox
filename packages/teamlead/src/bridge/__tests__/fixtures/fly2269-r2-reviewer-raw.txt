{"verdict": "CHANGES_REQUESTED", "reviewedHeadSha": "39a416ef5fa9e5dd5b2690fff0febb8705d545b4", "findings": [{"id": "start-cwd-breaks-dev-server", "severity": "HIGH", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 43, "title": "R1 修复引入新缺陷：start 也用 cwd=outputDir 会让 --dev-command 在空目录里跑，UI 模式 100% 失败", "detail": "不变量 1 与 B1（plan.md:113「断言 start/exec 的每次 opts.cwd 都等于 outputDir」）要求把 `cwd: outputDir` 一并套到 `proofshot start`。但 ProofShot 1.3.1 的 startCommand 在 `--run` 存在时调用 ensureDevServer，而它用 `spawn(\"sh\", [\"-c\", command], { cwd: process.cwd(), ... })`（dist/bin/proofshot.js:607-611）—— 这个 process.cwd() 就是我们给子进程设的 cwd。visual-capture UI 模式必带 `--run <devCommand>`（commands/visual-capture.ts:370-382），outputDir 是 proofshot-trigger.ts:292 生成的 `~/.flywheel/screens/<execId>/<randomUUID>`，且由 visual-capture.ts:173 刚 mkdir 出来的空目录。于是 `pnpm dev` 在一个没有 package.json 的空目录里执行 → 端口永不 listen → waitForPort 超时（默认 30s）→ startCommand 打印 `Failed to start dev server` 并 process.exit(1)（:850-860）。叠加新的不变量 12（零 PNG fail-loud），每一次 UI capture 都会硬失败——而 UI 正是 proofshot-trigger 的主路径（proofshot-trigger.ts:109-121）。这个缺陷用打桩的 runProofShot 单测完全看不见（B1 断言的恰好就是这个错误的 cwd），只有真机 QA 才会暴露。附带影响：startCommand 的 `git branch --show-current` / `git rev-parse HEAD`（:815-828）同样按 cwd 执行，在 outputDir 下不是 git 仓，metadata.json 的 branch/commitSha 会退化成空串。修复方向（任选其一并写进计划）：(a) start 保持在项目 cwd 执行并显式 `--output {outputDir}/proofshot-artifacts`，只让 exec 用 `cwd: outputDir`，两侧仍解析到同一 session root；(b) 不再让 ProofShot 代跑 dev server，由 visual-capture 自己起（它 3D 模式已有 startLocalModelServer）后改传 `--url`；(c) 保留 cwd=outputDir 但把 dev 命令包成在项目目录执行。无论选哪条，B1 的 cwd 断言必须同步改写，并在 §9 明确要求 AFTER 的 UI 轮真的起得来 dev server。"}, {"id": "hang-stub-test-exceeds-vitest-default-timeout", "severity": "MEDIUM", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 81, "title": "A1 的 ~15 秒 hang stub 测试会撞上 vitest 默认 5000ms 单测超时", "detail": "A1 新增「用 PATH 中真实 hang stub 测量约 15 秒抛出 timeout，断言窗口 13–25 秒」。packages/flywheel-comm/vitest.config.ts 没有设置 testTimeout，vitest 默认单测上限是 5000ms，因此这条测试会先以 `Test timed out in 5000ms` 失败，而不是产出「约 15 秒返回」的证据——RED 阶段会得到一个与被验行为无关的假红，GREEN 阶段也过不了。计划需要显式写出 `it(..., { timeout: 40_000 })`（或等价 testTimeout 覆盖），并说明这条慢测是否要与其他 focused suite 分开跑。另外顺带补一句边界：stub 若只是 `sleep`，证明的是「execFileSync 的 timeout 能杀死直接子进程」；R1 真正担心的形状是 agent-browser 拉起 daemon 后孙进程继承 stdout pipe 导致 spawnSync 在 timeout 之后仍阻塞（本机确有 `~/.agent-browser/default.sock` + `default.pid` 守护进程形态）。建议 stub 显式 fork 一个继承 stdout 的孙进程，或在 §9 的真机探针里点名这个形状。"}, {"id": "shared-start-failure-orphans-own-recording", "severity": "MEDIUM", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 57, "title": "不变量 8 的新残留：shared + start 失败时，本轮自己遗留的 recording 也不会被回收", "detail": "不变量 8 现在只在 owned 或 startSucceeded 时 stop recording——方向正确（我核过 startCommand：`record start` 三次失败后直接 process.exit(1)，saveSession 在其后 :899-912，之后只剩 console 输出不会再抛错，所以「start 抛错 ⇒ 本轮通常没建立 recording」成立）。但有一个可达反例：startRecording 的 `ab(\"record start …\", 1e4)`（:700-702）是客户端 10 秒超时，服务端可能已经真的开始录制，随后第 2/3 次重试报 already active、start 退出 1。此时那条 active recording 是我们自己的，而新规则在 shared/unknown 下禁止 stop，它会一直挂着，并让下一轮 `record start` 继续撞 `Recording already active`——正是本 issue 的原始故障形态。这是一个有意识的取舍（宁可不误停第三方），但计划里没有承认这条残留，§9 第 5 条「注入失败后的下一轮 start 成功」也无法覆盖它（要复现必须专门注入「客户端超时但服务端成功」这一形状）。建议：在 §6 已知取舍里写明该残留与恢复路径（下一次 owned 轮 / `proofshot clean` / 运维介入），并在 §9 里要么点名注入这一形状，要么显式声明超出本单范围。"}, {"id": "discovery-symlink-scope-narrowing", "severity": "LOW", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 123, "title": "B2 把 discovery 的 symlink 语义悄悄收窄，却写成「仍」保持不变", "detail": "B2 写「discoverArtifacts() 改成确定性递归，仍只接受 regular file、不跟随 symlink」。现状并非如此：proofshot/artifact-discovery.ts:57 用的是 `statSync`（跟随 symlink），所以今天一个指向真实 PNG 的 symlink 是会被收录的；改成 lstat 语义后它会被静默丢弃。这是范围收窄而不是「仍」，虽然影响面很小（ProofShot 自己不产 symlink），但按仓库规矩，未论证的范围变更本身就该记为 finding。建议把它写成一条显式决策（递归遍历不跟随 symlink 目录以避免环，代价是不再收录 symlink 形态的 artifact），并在新的 artifact-discovery.test.ts 里各留一条正/负用例。"}, {"id": "implicit-default-config-dependency", "severity": "LOW", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 44, "title": "「session root = {outputDir}/proofshot-artifacts」依赖 ProofShot 默认配置且会被任意祖先目录的 proofshot.config.json 静默改写", "detail": "不变量 1 断言真实 session root 是 `{outputDir}/proofshot-artifacts`。这条只有在 loadConfig 落到 DEFAULT_CONFIG（output=\"./proofshot-artifacts\"，dist/bin/proofshot.js:470-479）时才成立，而 findConfigPath 会从 cwd 一路向上找 `proofshot.config.json`（:480-489）。cwd=outputDir 时向上路径是 `~/.flywheel/screens/<execId>` → `~/.flywheel` → `~` → `/`；今天全仓与 HOME 都没有这个文件（我全仓 find 过，零命中），但一旦谁在任意祖先目录放一个带 `output` 的 config，start/exec 会一起被改写到别处，discovery 又变成空——退化形态和 R1 那条 HIGH 一模一样，只是更隐蔽。既然本单把整条链压在这个默认值上，建议把它从隐式假设升级为显式契约：由 visual-capture 在 outputDir 里写一份最小 `proofshot.config.json`（cwd=outputDir 时 findConfigPath 第一跳就命中，结果完全确定），或在实现里对解析结果做一次断言/探针，并在 §7 负向审计里加一条「祖先目录无 proofshot.config.json」的检查。"}, {"id": "owned-path-has-no-tab-baseline", "severity": "LOW", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 45, "title": "已修复；残留：owned 轮的 baseline 必然失败，会在每次正常运行里刷出一条误导性 cleanup warning", "detail": "不变量 2 改成 owned/shared/unknown 一律采 tab baseline，R1 的判据缺口已经补上，fallback 也只用真实差集——这一条按预期修好了。残留是措辞与可观测性：owned 意味着目标 session 当刻不存在，此时 `agent-browser tab list --json` 只会报错（binary 里的对应文案是 `No browser connected` / `No active session`），按不变量 7「baseline 失败 → warning」处理的话，每一次正常的 owned 运行都会稳定打印一条 cleanup warning，把真正需要注意的 baseline 故障淹掉。建议把「session 明确不存在 ⇒ baseline 记为空集 ∅（预期态，不 warn）」与「session 存在/未知但 tab list 失败 ⇒ warn 且禁止 tab close」在计划里分成两种状态，并各留一条单测——否则就是同一个痕迹对应两种状态。"}]