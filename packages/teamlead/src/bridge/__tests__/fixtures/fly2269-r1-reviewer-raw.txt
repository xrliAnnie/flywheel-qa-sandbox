{"verdict": "CHANGES_REQUESTED", "reviewedHeadSha": "cff6630a36ed9e72b2c3a7233dcbc0afceffedb6", "findings": [{"id": "session-split-silent-empty-manifest", "severity": "HIGH", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 127, "title": "§5 的前提不成立：删掉 proofshot stop 会把今天的硬失败变成 exit 0 + 空 manifest", "detail": "计划 §5 断言「PNG 在 cleanup 前已经落盘，现有 discovery/selection/manifest 逻辑不改」。实测安装字节推翻这个前提：ProofShot 1.3.1 只有 startCommand 读 --output（dist/bin/proofshot.js:790 `if (options.output) config.output = options.output`），execCommand:2491 与 stopCommand:2586 都用 `loadConfig()`，而 loadConfig 的 startDir 默认是 `process.cwd()`（:481），仓库内不存在任何 proofshot.config.json（全仓 find 零命中），所以两者解析到 `<cwd>/proofshot-artifacts`。visual-capture 只给 start 传 --output（commands/visual-capture.ts:370-397），其余调用不带 cwd（:400-411），而 outputDir 由 proofshot-trigger.ts:292 生成为 `~/.flywheel/screens/<execId>/<randomUUID>` —— 两侧永远不可能相等。后果链：(1) 今天 `runProofShot([\"stop\"])`（visual-capture.ts:246）必然走 stopCommand:2589 的 `No active session found` → process.exit(1) → execFileSync 抛错 → visual-capture 整体失败；(2) `exec screenshot step-ui.png` 因 loadSession 为 null 跳过 resolveScreenshotPath(:2393)，落盘到 runner 的 cwd 而不是 outputDir；即使有 session 也只会落进 `{outputDir}/<ts>_<slug>/`，而 discoverArtifacts 明确只枚举顶层一层（proofshot/artifact-discovery.ts:37-50）。本计划删掉唯一会抛错的 stop 之后，discoverArtifacts 在 outputDir 只会看到 `.session.json`（还被 cleanup 先删了），selectVisionArtifacts 返回空，writeCaptureManifest 不校验空集（proofshot/manifest.ts:63-77），命令改为 exit 0 且 `selected: []`。§8 的 QA 口径只量 tab/renderer 数与 start/screenshot/record-stop「成功」，量不到产物，因此这个退化会被判 PASS。要求：要么按 publish-report 已验证的形状把 session split 一并修掉（全部 proofshot 调用 `cwd: outputDir` 且不传 --output，见 publish-report.ts:345-365 的 spike 注释），要么在本单显式加 fail-loud 守卫（零 PNG 即报错）并把「BEFORE/AFTER manifest 产物一致」写进 §8 验收；不能保留一条未经验证的 §5 断言。"}, {"id": "unconditional-record-stop-not-owned", "severity": "MEDIUM", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 36, "title": "recording 没有纳入所有权模型：start 失败时 record stop 停的必然不是本轮的录制", "detail": "不变量 6 让 cleanup 在 startAttempted 为真时无条件跑 `record stop`。但 ProofShot 的 startCommand 在 `record start` 三次重试全失败后直接 process.exit(1)，`saveSession` 在其后（dist/bin/proofshot.js:868-890），也就是说 start 抛错 ⇒ 本轮几乎必然没有成功建立录制。此时 `agent-browser record stop` 停掉并 finalize 的是别人的录制 —— 恰好就是 B1 用例注入的 `Recording already active` 场景。计划对 tab/browser 的所有权判据极为克制（不变量 3/4/5），唯独 recording 被授予无条件权限。相对现状这是新增的第三方损伤面：今天 start 抛错时 finally 完全不动 Chrome。建议要么给 record stop 加一个「本轮 owned browser 或 start 成功返回」的条件，要么在计划里把这条不对称显式记为已知取舍（含 `record start` 客户端超时但服务端已起这一反例）。"}, {"id": "owned-path-has-no-tab-baseline", "severity": "MEDIUM", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 30, "title": "owned 路径没有 tab baseline，不变量 3 在该分支自相矛盾", "detail": "不变量 3 说「新 tab 的唯一所有权证据是 post-minus-pre 的稳定 id」，但沿用的 publish-report 形状只在 `browserWasActive` 为真时采 baseline（publish-report.ts:430-439），owned 分支 `tabIdsBefore` 恒为空集。于是不变量 4 的 fallback（whole close 失败后逐 id 关）在 owned 分支实际会关掉 session 里的每一个 tab —— 一旦 session 探测误判（daemon 正在启动、list 超时被判成 owned 等），blast radius 是整棵树。publish-report 还有 `tab.url === args.url` 兜底，而本计划按 research §1.2 明确取消 URL 过滤，所以这条兜底在 visual-capture 不存在。建议无条件采 baseline（多一次已被 15s 上限约束的调用），让 post-minus-pre 在两个分支都真正成立。"}, {"id": "tab-window-vs-unlocked-agent-browser-users", "severity": "MEDIUM", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 100, "title": "baseline 只保护 start 之前存在的外来 tab；不拿锁的直连 agent-browser 用户在整个 capture 窗口内会被误关", "detail": "exploration §6.1 声称非 Flywheel 外来活动由 baseline fail-safe 保护，但 baseline 只是 start 之前的快照。计划 §4 的状态机把 id 识别放在 cleanup 阶段（不变量 8 让它至少覆盖 start→全部截图；3D 模式还要跨所有 `exec open`/`screenshot`），窗口内任何新开的 tab 都会被算作本轮资产。而 doc/qa/qa-context.md:170 就在推荐 QA 直接跑 `agent-browser open/screenshot/close`，这条路径不走 proofshot lock（lock 只在 visual-capture / publish-report 内取，proofshot/lock.ts:23），并且全机 runner 共享同一个 `~/.agent-browser/default.sock` session 命名空间（TmuxAdapter 只隔离 TMPDIR，不隔离 session）。取消 URL 过滤后这就是相对 publish-report 新增的误关面。建议：像 publish-report.ts:473 那样在 start 之后立即识别一次（cleanup 只在识别为空时补一次），并把「窗口有多宽、为什么可接受」写进计划正文。"}, {"id": "timeout-asserted-not-exercised", "severity": "MEDIUM", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 51, "title": "A1 只断言 timeout 选项被传入，没有任何一步证明调用真的被 15 秒界住", "detail": "Slice A 的全部证据是 mock 掉 execFileSync 后断言 options 含 `timeout: 15_000`，§8 的真机矩阵只注入 proofshot start/recording 失败，没有注入 agent-browser 挂起。这正是「验了 flag、没验行为」：spawnSync 的 timeout 只对直接子进程发 killSignal，若 agent-browser 在 JSON 分支（`stdio: [\"pipe\",\"pipe\",\"inherit\"]`，publish-report.ts:642-648）拉起并让守护进程继承了那个 stdout pipe，父进程仍可能在 timeout 之后继续阻塞 —— 而 agent-browser 确实是 daemon 形态（本机存在 `~/.agent-browser/default.sock` + `default.pid`）。整个 Slice A 的收益（研究里说的那次约两分钟挂起）都押在这个未验证的行为上。建议在 §8 增加一条：用真二进制或一个会挂住并把 stdout 传给孙进程的桩，证明单次调用在 ~15s 返回。"}, {"id": "unspecified-cwd-and-env-seam", "severity": "LOW", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 109, "title": "direct agent-browser 调用的 cwd 未定义，且没有 env 注入口导致 owned/shared 单测依赖环境变量", "detail": "publish-report 给每个 agent-browser 调用固定 `cwd: outputDir`（publish-report.ts:561/577/592），计划全文没有说明 visual-capture 用哪个 cwd（虽然 session socket 在 `~/.agent-browser` 下、cwd 大概率无关，但这属于应当写明的契约）。另外 `VisualCaptureArgs` 没有 `env` 字段，实现只能像 publish-report 读 `AGENT_BROWSER_SESSION` 那样落到 ambient `process.env`，于是 B3 里 owned/shared 判别的单测会随执行环境是否设置 `AGENT_BROWSER_SESSION` 而漂移。建议在 §4 明确 cwd 契约并加一个 env 测试 seam。"}, {"id": "publish-report-behavior-not-strictly-unchanged", "severity": "LOW", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 43, "title": "不变量 10「publish-report 行为不变」与加 timeout 本身矛盾", "detail": "加上固定 timeout 之后，原本会无限等待的调用改为抛错，从而进入既有的 warning/fallback 分支——例如 session preflight 超时会走 publish-report.ts:425-429 把 `browserWasActive` 置为 true（保留 browser）。这是有意的、也是更安全的行为变化，但它不是「行为不变」。建议把不变量 10 改写成「除超时语义外行为不变」，并在 publish-report focused suite 里补一条超时→降级路径的用例，避免评审/QA 拿「行为不变」当免检理由。"}, {"id": "pnpm-codex-rescue-entrypoint-does-not-exist", "severity": "LOW", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 159, "title": "§7 写的 `pnpm codex:rescue -- ...` 不是本仓存在的入口", "detail": "根 package.json 的 scripts 只有 build/dev/test/lint/format/test:packages/test:packages:run/test:apps/test:cycle-time/verify:workflow-seeds/typecheck/prepare，没有 `codex:rescue`；全仓对 `codex:rescue` 的引用都在 `.flywheel/agents/nodes/*.md`，形态是 Claude Code 的 skill（`codex:rescue`，且明确要求 never raw `codex exec`），不是 pnpm script。照 §7 字面执行会直接报 missing script，执行者只能临场改写，而 §7 又是绑 review_code gate 的步骤。建议把命令改成节点文档里的既有写法。"}]}