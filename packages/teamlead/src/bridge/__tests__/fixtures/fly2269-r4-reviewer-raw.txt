{"verdict": "CHANGES_REQUESTED", "reviewedHeadSha": "ce29c2fec5c01e63d8c7c6dfed8a200d5a53e261", "findings": [{"id": "start-cwd-breaks-dev-server", "severity": "HIGH", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 46, "title": "R2 提出后完全未被处理（R2 只转述了 6 条中的 2 条）：start 用 cwd=outputDir 会让 --dev-command 在空目录里跑，UI 模式 100% 失败", "detail": "状态说明：§0 记录 R2 因缺机器可读 verdict 走 no_verdict，Lead 只转述了两项（slow-test / owned-baseline），这条 HIGH 既没被采纳也没被驳回，而是在传递中丢了——不是 governance-settled，请正面处理。\n\n缺陷本身（在 ce29c2fec 上逐条复核仍成立）：不变量 1（:46-47）连同 B1（:123「断言 start/exec 的每次 opts.cwd 都等于 outputDir，start argv 没有 --output」）、B2（:131-132）与 §6 四处一致要求把 `cwd: outputDir` 套到 `proofshot start` 上。但 ProofShot 1.3.1 在 `--run` 存在时走 ensureDevServer，而它是 `spawn(\"sh\", [\"-c\", command], { cwd: process.cwd(), ... })`（~/.npm-global/lib/node_modules/proofshot/dist/bin/proofshot.js:607-611）——这个 process.cwd() 就是我们给子进程设的 cwd。visual-capture UI 模式必带 `--run <devCommand>`（packages/flywheel-comm/src/commands/visual-capture.ts:370-382），而 outputDir 是 packages/teamlead/src/bridge/proofshot-trigger.ts:292 生成的 `~/.flywheel/screens/<execId>/<randomUUID>`、由 visual-capture.ts:173 刚 mkdir 出来的空目录。于是 `pnpm dev` 在一个没有 package.json 的空目录里执行 → 端口永不 listen → waitForPort 超时（默认 30s）→ startCommand 打印 `Failed to start dev server` 并 process.exit(1)（:850-860）。叠加不变量 12（零 PNG fail-loud），每一次 UI capture 都会硬失败；UI 正是 proofshot-trigger 的主路径且是 CLI 默认 kind（proofshot-trigger.ts:109-121，index.ts:1841）。3D 模式走 `--url` 不带 `--run`（visual-capture.ts:383-397）因此不受影响——故障只吃掉主模式，更不容易在抽查里显形。这个缺陷用打桩的 runProofShot 单测完全看不见：B1 断言的恰好就是这个错误的 cwd。plan/research/exploration 全文对 `--run` / dev server cwd 仍是零命中。附带影响：startCommand 的 `git branch --show-current` / `git rev-parse HEAD`（:815-828）同样按 cwd 执行，在 outputDir 下不是 git 仓，metadata.json 的 branch/commitSha 退化成空串。\n\n修复方向（任选其一并写进计划）：(a) start 保持在项目 cwd 执行并显式 `--output {outputDir}/proofshot-artifacts`，只让 exec 用 `cwd: outputDir`，两侧仍解析到同一 session root；(b) 不再让 ProofShot 代跑 dev server，由 visual-capture 自己起（3D 模式已有 startLocalModelServer）后改传 `--url`；(c) 保留 cwd=outputDir 但把 dev 命令包成在项目目录执行。无论选哪条，B1 的 cwd 断言必须同步改写，并在 §9 明确要求 AFTER 的 UI 轮真的起得来 dev server。"}, {"id": "owned-skips-ownership-crosscheck", "severity": "MEDIUM", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 48, "title": "R3 新引入：owned 时直接跳过 pre-list，等于删掉了 whole-close 之前唯一的所有权交叉校验；research.md 也与新文本冲突", "detail": "我 R2 那条 `owned-path-has-no-tab-baseline` 的残留意见只要求把「session 明确不存在」记为可信空集**且不 warn**（观测性问题）。R3 实现成了「不调用 pre-list」（不变量 2 :48-50、不变量 7 :60-62、C2 :157-158、C3#3 :171-173）。差别很实质：owned 分支的收尾动作是整树 `agent-browser close`（不变量 6），而 pre-list 是判定「probe 说不存在，但实际上有一棵别人的树」的唯一独立信号。probe 与真实状态可能不一致（daemon 正在起、session 尚未注册、list 与 start 之间被第三方抢先建 session），此时跳过 pre-list 就没有任何东西能在破坏性动作前推翻这个 owned 结论——正好违反本仓「有直接检查能一锤定音时别只靠单一信号」的判据。保留这次调用的代价只是一次已被 15s 上限约束、通常快速报错的命令（binary 文案 `No browser connected` / `No active session`），收益是把 probe 结果变成可证伪的。\n\n另有一处文档级不一致必须一起解决：本轮只改了 plan.md，research.md 未同步，现在 research.md:100（`every ownership state: tab baseline (fail-safe)`）与 :126（「无论 session probe 结果如何都尝试 tab baseline；非空 baseline 会保守按 shared 处理」）与 plan.md 新文本直接矛盾。按 research.md 实现的人会做出「非空 baseline ⇒ 降级为 shared」的保守逻辑，按 plan.md 实现的人不会。建议：恢复无条件 pre-list，把「明确不存在 + list 成功且为空」记为可信空集且不 warn，「明确不存在但 list 非空」保守降级为 shared 并 warn，「list 失败」按现有 fail-safe 处理；三态各一条单测，并同步更新 research.md §4/§5。"}, {"id": "shared-start-failure-orphans-own-recording", "severity": "MEDIUM", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 62, "title": "仍未处理：shared + start 失败时，本轮自己遗留的 recording 不会被回收，且该残留未在文档中承认", "detail": "同样属于 R2 未被转述的四条之一，本轮无改动。不变量 8（:62-63）只在 owned 或 startSucceeded 时 stop recording——方向正确：startCommand 的 `record start` 三次失败后直接 process.exit(1)，saveSession 在其后（proofshot.js:899-912），之后只剩 console 输出不会再抛错，所以「start 抛错 ⇒ 本轮通常没建立 recording」成立。但有一个可达反例：startRecording 的 `ab(\"record start …\", 1e4)`（:700-702）是客户端 10 秒超时，服务端可能已经真的开录，随后第 2/3 次重试报 already active、start 退出 1。此时那条 active recording 是我们自己的，而新规则在 shared/unknown 下禁止 stop，它会一直挂着，并让下一轮 `record start` 继续撞 `Recording already active`——正是本 issue 的原始故障形态。这是一个可以接受的取舍（宁可不误停第三方），但计划没有承认这条残留，§9 第 5 条「注入失败后的下一轮 start 成功」也覆盖不到它（要复现必须专门注入「客户端超时但服务端成功」这一形状）。建议：在 §6 已知取舍里写明该残留与恢复路径（下一次 owned 轮 / `proofshot clean` / 运维介入），并在 §9 里要么点名注入这一形状，要么显式声明超出本单范围。"}, {"id": "discovery-symlink-scope-narrowing", "severity": "LOW", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 133, "title": "仍未处理：B2 把 discovery 的 symlink 语义悄悄收窄，却写成「仍」保持不变", "detail": "本轮无改动。B2（:133）写「discoverArtifacts() 改成确定性递归，仍只接受 regular file、不跟随 symlink」。现状并非如此：packages/flywheel-comm/src/proofshot/artifact-discovery.ts:57 用的是 `statSync`（跟随 symlink），所以今天一个指向真实 PNG 的 symlink 会被收录；改成 lstat 语义后它会被静默丢弃。这是范围收窄而不是「仍」，影响面虽小（ProofShot 自己不产 symlink），但按仓库规矩，未论证的范围变更本身就该记为 finding。建议把它写成一条显式决策（递归遍历不跟随 symlink 目录以避免环，代价是不再收录 symlink 形态的 artifact），并在新的 artifact-discovery.test.ts 里各留一条正/负用例。"}, {"id": "implicit-default-config-dependency", "severity": "LOW", "file": "engineering/doc/FLY-2269-visual-capture-cleanup/plan.md", "line": 47, "title": "仍未处理：「session root = {outputDir}/proofshot-artifacts」依赖 ProofShot 默认配置，会被任意祖先目录的 proofshot.config.json 静默改写", "detail": "本轮无改动。不变量 1（:46-47）断言真实 session root 是 `{outputDir}/proofshot-artifacts`。这条只有在 loadConfig 落到 DEFAULT_CONFIG（output=\"./proofshot-artifacts\"，proofshot.js:470-479）时才成立，而 findConfigPath 会从 cwd 一路向上找 `proofshot.config.json`（:480-489）。cwd=outputDir 时向上路径是 `~/.flywheel/screens/<execId>` → `~/.flywheel` → `~` → `/`；今天全仓与 HOME 都没有这个文件（全仓 find 零命中），但一旦谁在任意祖先目录放一个带 `output` 的 config，start/exec 会一起被改写到别处、discovery 再次落空——退化形态和 R1 那条 HIGH 一模一样，只是更隐蔽。既然本单把整条链压在这个默认值上，建议把它从隐式假设升级为显式契约：由 visual-capture 在 outputDir 里写一份最小 `proofshot.config.json`（cwd=outputDir 时 findConfigPath 第一跳即命中，结果完全确定），或在实现里对解析结果做一次断言/探针，并在 §7 负向审计里加一条「祖先目录无 proofshot.config.json」的检查。"}]