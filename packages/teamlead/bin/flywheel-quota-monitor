#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ENTRY="$SCRIPT_DIR/../dist/account-heal/quota-monitor-cli.js"

if [[ ! -f "$ENTRY" ]]; then
  echo "FLYWHEEL_QUOTA_MONITOR_UNAVAILABLE: missing $ENTRY" >&2
  exit 31
fi

exec node "$ENTRY" "$@"
