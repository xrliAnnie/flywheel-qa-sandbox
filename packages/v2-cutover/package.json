{
	"name": "flywheel-v2-cutover",
	"version": "0.1.0",
	"description": "Ledgered stop-the-world cutover and rehearsal tool",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"bin": {
		"flywheel-v2-cutover": "./dist/cli.js"
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc",
		"test": "pnpm run build && vitest run",
		"test:run": "pnpm run build && vitest run",
		"test:watch": "vitest --watch",
		"typecheck": "pnpm run build"
	},
	"dependencies": {
		"better-sqlite3": "^12.8.0",
		"flywheel-v2-engine": "workspace:*",
		"flywheel-v2-kernel": "workspace:*"
	},
	"devDependencies": {
		"@types/better-sqlite3": "^7.6.13",
		"@types/node": "^20.0.0",
		"typescript": "^5.3.3",
		"vitest": "^3.1.4"
	},
	"publishConfig": {
		"access": "public"
	}
}
