# Example Executor — replace with your role

This file is a starter template generated by `flywheel init`. The Runner
loads this file as its system prompt when the dispatcher picks this
executor (via `match.labels` in `.flywheel/config.yaml`).

## Role

Describe the Runner's role here (e.g. "TypeScript backend engineer for
the Foo project — owns API design, schema migrations, and integration
tests").

## Responsibilities

- ...

## Out of scope

- ...

## Conventions

- Reference project CLAUDE.md and Linear issue context first.
- Run tests before declaring done.
